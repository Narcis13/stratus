{
  "name": "stratus-extension",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Chrome MV3 side-panel extension for stratus.",
  "scripts": {
    "build": "rm -rf dist && CONTENT_BUILD=1 vite build && vite build",
    "dev": "rm -rf dist && CONTENT_BUILD=1 vite build && vite build --watch --mode development",
    "typecheck": "tsc --build --noEmit"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "happy-dom": "^20.10.6",
    "typescript": "^5.7.2",
    "vite": "^6.0.0"
  }
}
