# Verification debt — unpaid, and not masterplan tasks

> Split out of `STATE.md`'s `next-up` at **GT.9** (2026-07-27). These are **unpaid debts, not history**, so they are deliberately NOT in `STATE-ARCHIVE.md` — they are still owed. They left STATE.md because they had become the single largest block in a file whose whole job is to load in one `Read`, and none of them binds a masterplan task: whoever picks one up does so directly.
>
> **How to use this file:** nothing here needs a plan or a session of its own. Every browser item folds into whichever session next has Chrome open on the panel — deploy, reload `chrome://extensions`, and walk the ones that share a tab. When you pay one, delete its entry (and its `next-up` pointer in STATE.md if it is the last one).

**The debts below survive the masterplan and are NOT masterplan tasks — whoever picks them up does so directly.** **Twenty-five** items (recounted at the reply-craft docs-sync, 2026-08-08 — one added (`0t`), none removed). Cheapest last-in first: `0t` is the deterministic-humanizer suffix tripwire for the already-scheduled humanizer-overhaul session (**no live effect while the project switch stays off**), `0s` is the own-harvest `recent`-scope termination check ($0, needs the extension reloaded, no deploy), `0r` is the ML.4/ML.5 pair (the original-not-translation check on the wire, and the panel's weighted counter / gloss / chip / RTL — $0, blocked on a deploy), `0q` is the CQ.5 Cannon-view pass (**its `--live` half was PAID at ML.6 and struck** — `scripts/smoke-multilingual.ts --live` superseded it, so `0l` is now the only spender left in this file), `0p` is the CQ.6 roster-block pass (needs a deploy carrying `/x/cannon/*`, $0), `0o` is the CQ.4 cannon-capture browser pass (blocked on a deploy, $0), `0n` is the Wave-7 RC debt (the Curate & draft button on a real 40+ tweet queue, $0 — **plus the one `--live` calibration run, ~$0.003**, the only other spender in this file), `0m` is the Wave-7 HM browser-verify (the Radar checkbox + the Settings card, one reload, $0), `0l 0k 0j 0i 0h 0g 0f 0e 0d 0c 0b 0` are the Wave-6 GT/SC/JD browser-verifies (**`0l` is the biggest spender in this file** — ~$0.010 of real LLM calls; only `0n(b)` also spends, at ~$0.003), `1` is the UI.11–UI.16 panel matrix (itself several checks), `2` is CA.2 step 2 (a prod-journal grep rather than a browser check), `3` is **UI.7's**, and `4` is the UI.9/UI.10/RL.5 palette-and-panel debt, moved here at SC.9 — it had been riding a STATE.md header prior, which is how an unpaid debt goes invisible the moment its lane is archived. Plus the Wave-4 block below. The count said "eight" in STATE.md for four tasks running while items kept being appended — if you add one here, fix this number.

**Cheapest useful session:** `0c` + `0f` + `0g` + `0k` + `0d` all live in the Composer/Replies pair on one reload, and `0e` + `0i` add two more tabs to the same walk. `0l` is in the same Composer but decide separately — **since ML.6 struck `0q`'s spending half, it is the only item in this file that costs money.**

- **(0t) The deterministic-humanizer suffix landmine — parked at the reply-craft docs-sync, 2026-08-08, for the humanizer-overhaul session the user already scheduled. It is NOT live today:** `app_settings['humanizer'].enabled === false` at the check that produced this entry, and the Radar doc now tells the operator to keep it off (or set `suffixChance: 0` / empty the pool) until this is paid. `DEFAULT_HUMANIZER.suffixes` is `['well said', 'love this', 'good stuff', 'solid point', 'nice one']`, appended at the Radar pick moment with probability 0.20. Every string is agreement-slop the new drafting prompt explicitly works to remove; because the deterministic layer runs **after** the LLM, one Cannon reply in five can have a generic summarizing closer stapled onto its sharpest word while every prompt test stays green. The later overhaul must decide the deterministic layer's actual remit (the reply-craft contract says LLM owns voice/rhythm/diction; deterministic code owns keystroke noise), remove or redesign the generic suffix arm in the shared core, and cover **both config populations**: changing `DEFAULT_HUMANIZER` does not rewrite an existing raw `app_settings['humanizer']` row, and canned reply lists keep separate stored overrides. Do not declare this paid on a code-default edit alone. Done means: the stored project row and per-list overrides cannot silently preserve the five closers; `scripts/smoke-humanizer.ts` still proves deterministic draws/protected spans/≤280; and a Radar pick inspected after `humanize()` can no longer add a phrase the prompt forbids. This is implementation debt for that scheduled session, not a request to widen RC.10.

- **(0s) The own-harvest `recent` scope on a real own-reply day — parked at the own-harvest docs-sync, 2026-08-07; the newest debt here, $0 in every direction, and it needs NO deploy** (the scope is extension-side plus one whitelist entry the server already carries once `./scripts/deploy.sh` runs; if prod is behind, the run's last call 400s `invalid_scope` and that is the tell). One `chrome://extensions` reload, then **Harvest → My replies (48h) → Harvest** on your own account. The question the automated gates structurally cannot answer: **does a 48-hour window terminate on a 100-reply/day account, or does it hit `HARD_STEP_CAP` first?** Watch the row counter and the finish line — a run that stops on the cap rather than on the window boundary is under-collecting silently, and the fix is the cap, not the window. Three things to confirm while it runs: the preset leaves the form filled but **starts nothing** until you press Harvest; the button is disabled with its reason showing when **Settings → Identity** is blank; and running it a **second** time the same day does not double anything downstream — after the second run, **Playbook → My replies — harvested** must show the same `N measured` as the count of distinct replies, and Today's harvested line must agree with it. (That last check is the read-layer dedup, which `scripts/smoke-own-harvest.ts` already proves against the real DB — here you are only confirming the panel is wired to the same number.)

- **(0r) ML.4 + ML.5's browser-verifies — parked at ML.6, 2026-08-06; the newest debt here, and $0 in every direction.** Both need `./scripts/deploy.sh` + a `chrome://extensions` reload, so they fold into the `0o`/`0p`/`0q` session — **one deploy pays for all four.** *(a) The one that actually matters, and it is a Network-tab check, not a look:* open a tweet X is currently showing **translated** (a foreign-language post with the *"Translated from …"* banner), hit **Reply Master**, and confirm the `/x/replies/generate` request body's `context.text` carries the **original** script — Japanese, not English. That is ML.4's whole claim and the *only* thing that proves it, because reading the panel cannot distinguish "the server detected Japanese" from "X un-translated the post and then the server detected Japanese". Then confirm the draft comes back Japanese. Two things that must NOT happen: any new per-article work in the general `scan` loop (scroll a long timeline and watch for jank — the reveal is per-CLICK by contract), and the reveal firing during a hand-run harvest (it bails on `isHarvestActive()`; start a harvest, click Reply Master on another tab, and the two must not fight over the same buttons). *(b) The panel matrix, one reload:* a Japanese draft's counter reads **~140 remaining at 140 characters, not 280** — that number is the whole of ML.5 — while an Arabic reply of the same length reads ~280, which is what proves the profile table is doing the work rather than a blanket halving. The single non-English variant shows a **muted literal gloss**, and **Copy yields only the reply** (paste it somewhere to check — a gloss that can be pasted by accident is worse than no gloss). **No empty variant tab strip** renders on a one-variant draft, in the panel or on-page. An Arabic variant renders **RTL while its gloss stays LTR**, and the card's *controls* must not mirror — that is the failure mode of flipping a container instead of setting `dir` on the text. The **language chip's tooltip names the rule that fired**, and **In English** redrafts. Then **light theme** (the gloss and the chip are the two new inked surfaces). The build greps already shipped with the task, so do not re-check existence: `grep -F 'weightedLength' dist/sidepanel.js` hits, i.e. the shim really inlined.

- **(0q) CQ.5's browser-verify — parked at CQ.8, 2026-08-05.** Needs a deployed server carrying CQ.3's routes, so it folds into `0o`/`0p`/`0r`'s session — one deploy pays for all of them. **~~Half (b), CQ.7's `--live` language calibration run, is STRUCK: PAID at ML.6 (2026-08-06).~~** It was superseded rather than merely run — `scripts/smoke-multilingual.ts --live` makes the same claim and four stronger ones (exactly one `extends` variant, a non-null gloss on each, the raw variant count *before* the trim, and a provider-call counter proving the specificity-gate skip costs one call), and it prints every variant with its gloss for exactly the human read this item asked for. Findings are in `docs/PHASE-HISTORY.md` and the codemap header; the register read came back clean (plain-form Japanese held consistently; the Arabic mixed one English loanword). **Re-run `bun run scripts/smoke-multilingual.ts --live` after any change to `renderLanguageClause` — that is the recalibration lever, not the code.** This item is now $0 and single-half. *(a) $0, one reload, but it needs a REAL scroll session:* browse x.com until something lands in **Radar → Cannon**, then check the things a unit test structurally cannot. **Three tabs** in the strip, and switching to Cannon must not disturb Queue's contents or order (that byte-identity is a done-when, and the shared buffer is why it could break). Each row leads with a **score chip**; the age is **red past 15 minutes** and the row is **gone past 30** — sit on the tab and watch one cross rather than trusting the unit test, because the cutoff reads a *displayed* age that keeps ticking. Then confirm the row is **still in Queue** after it leaves Cannon (the cutoff is display-time filtering, never a buffer write — if it vanished from both, §7.24 was violated). The head reads `placed today N / T`: take a reply from any surface and confirm **N increments without a page reload**, and that `T` matches the Today tab's replies quest — two different numbers there means the commitment/doctrine resolution forked. Hover a `cannon` band chip for its tooltip. Check both empty states by contriving them: an empty Cannon with nothing recent (*"Nothing scoring above 120…"*) versus one where rows aged out (*"N entries aged out past 30 minutes."*) — **they must not collapse into one line**, that difference is the whole point. Confirm **`Curate & draft` does NOT render in this view** while `Draft replies` does. Then **light theme** (the score chip and `.radar-age-red` are the two new inked surfaces).

- **(0p) CQ.6's browser-verify — $0 in every direction (every route the block calls is $0 by construction), one reload, and it needs a deployed server carrying the `/x/cannon/*` routes.** The build greps that shipped with it cover existence — `dist/sidepanel.js` carries `Rescore ($0)`, `+ add handle`, `under sample` and the three `x/cannon/{targets,candidates,rescore}` call sites, and `dist/assets/sidepanel-*.css` carries 16 `.radar-roster-*` rules — so **do not re-check that the block renders**; check what it does. Open **Radar → Cannon**, expand **Roster** at the foot (it loads on first expand and never again on its own — a second expand must fire no request, that is the no-polling contract) and walk: type a handle with an `@` and a trailing space and confirm it is accepted (normalized client-side), type one with a dot and confirm a **sentence** under the input rather than a bare 400; **Rescore ($0)** → the note reads `scored N · M under sample (needs …)` and the list re-reads; **bench** a target and confirm it dims but keeps its score, **camp** it back; **drop** one and confirm the `confirm()` fires first and that the dropped handle **reappears in Candidates** below (that round-trip is the reload-not-splice decision); click a candidate's **+ add** and confirm it leaves that list and enters the roster in the same beat. The chip is the thing to actually look at: a `belowFloor` target must be visibly amber against the green of a healthy one and the muted grey of an `unscored` one — three tones, one lookup in `chips.ts`, so a fourth colour anywhere means a rule was minted locally. Then **light theme** (the block adds no colour of its own: chips are `.chip-*`, benched rows are opacity only).

- **(0o) CQ.4's browser-verify — $0, no live launch, but it is BLOCKED on a deploy and that is the whole reason it is here.** The capture arm reads `GlanceEntry.isCannon`, which CQ.3 added to `GET /x/people/glance`; the deployed server predates it, so on prod today the roster half of the arm is dead (a camped handle reads `isCannon: undefined` → not camped, the refusal default — which is the safe direction, not a bug). The $0 agreement half is **already paid**: booted locally against the dev DB, `glance`'s `isCannon` key set and `GET /x/cannon/targets`' active set were diffed both directions and matched, including the backfill path (a camped handle with no `people` row still lands in the map with `isCannon: true`). What is owed is the browser pass, after `./scripts/deploy.sh` + a `chrome://extensions` reload, with **at least one handle actually on the cannon roster**: scroll to a fresh (<`x.cannon.maxAgeMin`, default 30 min) post by that handle and confirm it enters **Radar** with a `cannon` chip **even though `data-stratus-band` is absent on the article** — that absence is the point, the arm fires on tweets the classifier passed on. Then the score half, which needs no roster at all: any fresh post whose views/(replies+1) clears `x.cannon.scoreMin` (default 120, the measured p90) must queue as `cannon` from an account you have never heard of. Three things that must NOT happen: the tweet's on-page border/dim changing (radar-only, same contract as GT.8's roster arm), a `cannon` row outranking a `hot` one in the main Queue (`bandWeight` is 0 — the cannon ordering lives in the Cannon view), and a >`maxAgeMin` post appearing. Light theme: `.radar-band-cannon` is a new rule (accent ink, transparent fill) and it renders beside `--strat-muted` roster chips — the one combination to look at.

- **(0n) RC.4's browser-verify + the `--live` calibration run — parked at RC.5, 2026-07-28; the newest debt here and the LAST thing the masterplan left owed. Two halves, and only the second one spends.** *(a) $0, one reload, but it needs a REAL scroll session:* browse x.com until the Radar's fresh queue is bigger than the curated size (default 25 — drop **⚙ → Curated batch size** to 5 if you don't want to scroll for 40 tweets), then confirm the **Curate & draft (N)** button appears beside *Draft replies*, that N is the effective size (the *lower* of the knob and the batch cap — raise the knob past the cap and N must not move), and that it is **absent** below that threshold, which is the expected reading, not a bug. ⊕-pin one tweet first: after the pass it must still be in the queue **with a draft**, whatever the model thought of its neighbours. Click it: both buttons go disabled, the working one reads `Curating…`, the note goes `scored … · drafting…` and lands on `scored S · dropped D · drafted K/N · $x`. Then the thing only a browser can check — **scroll back over the dismissed tweets and confirm they do not re-enter the queue** (dismissal is permanent; it rides the same list ✕ uses, cap 500). Finally check light theme on the second button (`.radar-curate` is new) and that a curated draft's angle tabs behave like any other row's, including **Humanize picks** if you have it on — jitter is applied at pick time, never inside the curated flow, so a curated pick must jitter exactly once. *(b) ~$0.003, and it is a calibration read, not a pass/fail:* run `bun run scripts/smoke-radar-curate.ts --live` and read the printed table. The filler fixture (`drop your link below and let's connect`) SHOULD come back `lowValue` — the script reports it as a note rather than failing, because the category list is an opening guess and the whole point of the run is to find out. If it misses, widen the list in **Settings → Prompts → reply curation**; if it flags the two real posts, narrow it. Do this before trusting the button on a queue you care about.

- **(0m) HM.3+HM.4's browser-verify — $0, no live launch, no seeding, and it is the newest debt here (parked at HM.5, 2026-07-28). Read this first: most of it is already proven, and only the interactions are owed.** The build greps that shipped with HM.5 cover existence — `dist/sidepanel.js` carries `Humanize picks` ×2 (the Radar checkbox and the Settings card copy), `come out changed` ×3 (the Lists odds line + those two), `no jitter this time` ×1 and three `x/humanizer` call sites — so **do not re-check that the controls render**; check what they do. **(1) The Radar half:** deploy + `chrome://extensions` reload, open **Radar** with at least one drafted row, tick **Humanize picks** under the Queue/Clicked strip and confirm the odds beside it read `~56% of picks come out changed` at the shipped chances. Click an angle body **several times across different rows** and confirm the hint reads `copied ✓ · jitter: …` naming real steps, or `copied ✓ · no jitter this time` — at 56% both arms show up inside a handful of picks, and **`no jitter this time` is a success, not a failure**. Paste one jittered pick and then open **Replies** history: the jittered text is what's recorded, **while the Radar's Clicked row still shows the verbatim variant** — that pair IS the pick-time-never-stored contract, and reporting the Clicked row as stale would be the predictable false bug. **(2) The Settings half:** Settings → General → **Reply humanizer** (fifth panel, between *Behavior & privacy* and *Appearance*). Type `0.` into a chance field and confirm the keystroke **survives** (the typed-string state, D182b — a numeric state would eat it); type `2` and confirm a message under the field with **Save disabled** rather than a bare 400; empty the suffix pool, Save, and confirm the odds line drops. Then the one cross-surface check: **flip `enabled` in this card and confirm the Radar checkbox follows on its next mount** — it is one setting, and the card's toggle saves on click while pools/chances wait for Save. Finally **Reset to defaults** → confirm it asks first, and that afterwards the Radar checkbox is **off** (reset deletes the row, and a missing row is `enabled:false`). **Three things that must NOT happen:** the checkbox jittering anything in the **Clicked** view's *first* render (a re-copy from Clicked is a fresh pick and SHOULD jitter — the bug would be the stored text changing), a `SettingsGear` appearing on the card (the row is not a registry knob, so a gear there would render empty), and half-typed pools being wiped when the `enabled` toggle round-trips (D182a — the re-seed key covers the config half only). Light theme is free here: the card reuses `Section`/`SettingRow` and the checkbox reuses the Radar's existing label class, no new colour.

- **(0l) JD.6's browser-verify — the three interaction done-whens, and **since ML.6 struck `0q`(b), the only item in this file that spends**: ~$0.003 for a judge and ~$0.007 for an apply, ~$0.010 to walk it once.** Needs a deployed server with an LLM key (the route rides the `llmConfigured()` mount, so an unconfigured install shows *"No AI provider is configured — add a key in Settings → AI."* — which is itself worth seeing once, by unsetting both keys, because it arrives as an `http_404` rather than a 503 and the mapping is hand-written). Everything else about the panel is already proven by the build greps (`Stranger answerability` and `Run judge (~$0.003)` in `dist/sidepanel.js`, `.judge-fix-quote` in the CSS), so **these three are all that is owed**: **(1)** type a draft, click **Run judge (~$0.003)**, confirm a verdict box renders with score/band/headline; **type one character and confirm it vanishes**; press undo/retype the character and confirm **it comes back without a new request** (that is D167 — the derived reset, and a Network tab with no second call is the assertion); click **Re-judge** and confirm exactly one new call. **(2)** click a fix row and confirm the textarea focuses with **that exact phrase selected** — the whole feature. Then find a verdict whose quotes do not all appear (rare, but a dropped row renders nothing at all rather than a row pointing nowhere). **(3)** click **Apply all fixes (~$0.007)** and confirm ONE of the two honest outcomes: either the text is replaced and the panel says *"Rewritten and re-judged — this version scored better."*, or it is untouched and says *"Kept your version — the rewrite did not score better."* — **the second is a success, not a failure**, and at temperature 0.2 it is the likelier of the two. While it runs, **type in the box**: the swap must be refused with *"You edited the draft while the rewrite was running — it was not swapped in."* rather than overwriting what you typed (there is no undo across a controlled `setText`, which is why that guard exists). Three things that must NOT happen: **Save/Schedule disabled or moved by any score** (decision 4, the one thing that must never regress — check it at `do not post`), a judge panel in **thread mode** or on an already-posted row, and any request at all before the first click (no on-mount fetch, no debounce — unlike the coach box above it). Then light theme: the panel reuses `.coach-tone-*` on `--danger`/`--warn`/`--ok` plus `--strat-*` tokens only, so the thing to look at is the twelve-dimension list where a *high* `negative risk`/`status dependency` must read RED, not green.

- **(0k) SC.8's browser-verify — $0, no live launch, and it folds into 0c/0f/0g's session (same Composer, same reload). It is the one item here where the EXPECTED result is a line that does not appear, so read this before deciding it is broken:** type a draft that ends in a call to action (an `audience_cta` — e.g. a closing `what would you add?` after two lines of setup) and look for a second corpus line in the `.coach` box, directly under the cooldown line and above the fix rows. It must read `Audience CTA · reach band: insufficient data (n=17 of 20)` — a NUMBER-FREE line, because on today's corpus no format clears the gate (D161c). Then type plain prose (two ordinary sentences, no shape) and confirm **no reach line renders at all** — `substance` is exempt, not under-gate, so it is off the axis rather than waiting for data. Both are the designed outcome. Hovering the insufficient line shows `17 measured, 20 needed` plus the no-borrowed-numbers sentence. **What would be a real bug:** any numeric range appearing anywhere (there is no seed table — a number below the gate means the gate leaked), a reach line on plain prose, or the line rendering with a warn/danger colour (it is always `.muted`; it is measurement metadata, never advice). The fitted form only becomes checkable once audience_cta/list/story cross 20 — three or four posts away — at which point the line becomes `Audience CTA · usually X–Y views · N% clear Z`.

- **(0j) GT.8's browser-verify — $0, no live launch, but it is the one debt in this list that needs a real timeline and a little patience:** deploy + `chrome://extensions` reload, then scroll x.com until one of your ~14 circle handles (stage ≥ engaged, or on the Targets roster — `GET /x/people/rankmap` prints the exact list) posts something quiet and recent. It must appear in **Radar** with a muted **`your circle`** chip; hover the chip for the explanation line. Three things that must NOT happen: a stranger's quiet post appearing (that is the 1,596-vs-14 failure and it would be obvious — the queue would fill in seconds), the tweet's on-page **border/dim changing** (the classifier's verdict is untouched by design — a roster capture is radar-only), and a >24h-old post by a circle member appearing. Then the merge rules, both cheap to see: a roster row that catches fire must flip to `hot` on the next scroll past it, and a row that was `hot` earlier must NOT demote to `your circle` when it goes quiet. Finally **Draft replies** must cover the roster row like any other, and after a confirm its Playbook attribution must carry no band (that half is already asserted by a test — eyeball it only if something looks off). Light theme: the chip is `--strat-muted` at 45%/10%, the one combination worth a look is a `your circle` chip on a row that also carries a bright `ally` tier chip.

- **(0i) GT.7's browser-verify — $0, no live launch, and it folds into any session with the panel open:** Today → *Today's quests* must now list **six** rows, with `5 replies to your people` last. On the real account the correct reading is **`0/5` with the note "today went to new faces — circle back to someone"** on any day you posted replies — that is D158, not a bug (every reply in the corpus is a first contact). To see it flip, reply to someone you have replied to before and confirm it posted, then Refresh: `n` becomes 1 and the note disappears. The one thing that must NOT happen: the ⚙ in that header still opens exactly **four** knobs (originals / neglected count / neglected-after / attend window) — the reciprocity target is niche-owned and lives on Settings → General → Niche; the gear's note now names both niche-owned quests, so read it and confirm it matches. Light theme is free here (no new classes — the row reuses `.quest-row`/`.quest-done`).

- **(0h) GT.6 needs no browser-verify to be correct, but ONE eyeball is worth having and it is $0:** open Settings → Niche and confirm the doctrine row now shows **six** inputs (the sixth reads `Replies to my people · min`, default 5), change it, Save, reload — it must come back as you typed it (that round-trip is the D157(d) silent-drop trap, and it is already asserted server-side by a test and by `bun scripts/smoke-niche.ts`). The gate half is genuinely hard to see on purpose: the exemption only shows as the ABSENCE of `Dead post — click to force` on a quiet post by one of the ~13 handles at stage ≥ engaged or on the Targets roster.

- **(0g) SC.6's browser-verify — $0, no live launch, and it folds into 0c/0f's session (same Composer, same reload):** type a hot take (`Unpopular opinion:` on the first line) into the Composer and look for ONE extra line inside the coach box, above the fix rows — it renders only if you have actually published ≥2 hot takes in the last 7 days, so **the correct observation on a quiet week is nothing there**. To force it, hit `GET /x/posts/cooldowns` directly first and read which formats are non-`clear` on your corpus (audience_cta is the likeliest — 68% of measured windows), then type a draft of THAT shape. At `warming` the line is muted; at `cooldown` it is amber and ends `reach decays when the same shape repeats.` Hover it: count, `last on <day>`, a snippet, and the never-blocks sentence. Then the three things that must NOT happen: **Save stays enabled at any count** (decision 4), **exactly one request in the Network tab on first paint and none on subsequent keystrokes** (the fetch is per mount, the classification is local), and **plain prose never lights it** — write four ordinary multi-line posts' worth of text and confirm no line, because `substance` is exempt (D146b). In thread mode confirm the line follows **segment 1's** shape, not the whole thread's. Then light theme (it reuses `--warn` via `.coach-tone-nudge` and `.muted`, no new colour).

- **(0f) SC.7's browser-verify — $0, no live launch, folds into 0c's session:** in the Composer, type a draft whose only specific thing is a word from a channel keyword and no digit (e.g. `Cut the Claude Code run to one pass. No numbers here, just the change.`) and confirm `concrete_detail` does NOT appear as an amber row — then confirm it DOES on a draft with no such word. One `GET /x/coach/lexicon` in the Network tab on first paint and **none on subsequent keystrokes** (the 60 s cache; the score itself is still local). Hit `/x/coach/lexicon` directly and eyeball the payload — `specificTerms` should be your channel keywords + pillar slugs verbatim, `tribeTerms` label words. Then a reply-variant chip: its number should move for the same word.

- **(0e) SC.5's browser-verify — $0, no live launch, and it folds into any session with the panel open:** open **Playbook** and confirm two new sections sit between *Media vs text-only* and *Idea Inbox payoff* — **Post format** (a cascade-ordered table; `Other` renders as a thin row) and **Does the coach score predict anything?** (four band rows `rework`/`almost there`/`ship it`/`top tier`, then two thin fix rows). On the real account expect the honest degenerate reading: nearly every original in `top tier`, `rework`/`almost` at 0, and **both** muted notes showing (`no measurable spread at n=…` and the fix-row one) — that is D154(a), not a bug. Drop the header ⚙ gate to 1 and confirm the page re-reads and the medians appear; the spread line, if it fires, must NAME both bands. Then **light theme** (the two sections reuse `pb-table`/`pb-gated`/`status-line` only — no new colours, so a difference here means a shared token drifted).

- **(0d) SC.4's browser-verify — $0, no live launch, and it folds into 0c's session (same reload, adjacent tabs):** in **Replies**, generate or open a draft with variants and confirm each `V1 · extends` chip carries a small bordered number, that **hovering the number** (not the label) shows `N/100 · band` + at most two issue lines + the floor-not-target sentence, and that hovering the rest of the chip still shows the variant text. Then the same chip on **Conversations** (a drafted open loop), the **Launch Room**'s *First comment* angle chips, and **Radar**'s angle tabs — plus the one asymmetry worth eyeballing: a Radar row with a single draft (pre-variant/CLI) shows the chip at the end of the `click → copies + opens the tweet` hint instead. Confirm **picking order never changes** and nothing is pre-selected. Then **light theme**: the three tones are the same `--danger`/`--warn`/`--ok` aliases as SC.3's column, but here they are a border as well as ink (`currentColor`), so an amber chip on the filled `.reply-variant.active` (accent background, white text) is the one combination to actually look at — the chip carries a `--bg-elev` plate exactly for that case, and whether the plate reads as a hole or as a badge is a light-theme judgement no test can make.

- **(0c) SC.3's browser-verify, and it is the cheapest in the list — $0, no live launch, no seeding, just type:** open the Composer, type into the Text box and confirm a boxed panel appears under the URL/over-limit warnings and above **Schedule**, updating as you type with **no request in the devtools Network tab** (that is the done-when that matters — the whole point is that the score is local). Paste the fixture `I cut editing time by 70%. / Not more tools — one checklist. / Built it after 40 bad drafts. / Steal it. / Then measure. / Keep what works. / Cut the rest. / thoughts?` (blank line between each) and confirm **exactly three rows**: one red "Cut 1 line — 15 raw lines hides the post behind 'show more'…", two amber (em-dash, "thoughts?"), a `26 passing` disclosure that opens, and the head reading `90 /100 · top tier` — **yes, "top tier" WITH a red row, that is D152(b), not a bug.** Then: a URL in the box adds the red $0.20 row (the separate amber URL warn above it stays — two surfaces, one fact, by design); **Save still works at any score** (type three words, score ≤25, button enabled — this is decision 4 and the one thing that must never regress); Thread mode shows `· segment 1` and grades the FIRST segment only; the panel disappears when the box is emptied and on an already-posted row. Finally **light theme** — the three tones are `--danger`/`--warn`/`--ok` aliases (UI.9 opening-guess palette), so check the amber rows are readable, and that the `<details>` marker is visible in both arms.

- **(0b) GT.4's browser-verify, the cheapest of the lot and it needs no live launch — but it DOES need a crossing, which a real account gives you once:** the Milestone card sits between Pinned post and Account health and renders only when the server says a rung was crossed in the last 3 days, so on an ordinary day the correct observation is *nothing there*. To see it, seed `account_snapshots` on the prod DB with a below-rung → above-rung pair inside the last 3 days (or point the panel at a local server seeded that way, which is what the D151 scratchpad script already builds), then confirm: the `.ok` line reads "You crossed **N** followers on {Mon D} — post it. Milestone posts are one of your best formats.", **Draft it** spends ~$0.006 and reports "3 drafts in the Calendar — pick one and slot it.", and those three rows are in the Calendar tab as ordinary drafts. Then check light theme (the `.ok` tint is UI.9 opening-guess palette) and that a second click doesn't double-fire while `Drafting…`.

- **(0) GT.3's own browser-verify, cheapest of the lot and it needs a live launch:** with the extension reloaded, a scheduled post firing opens the room — the checklist must lead with the seed item and a **First comment** head + *Draft seed comment* button must sit above Early repliers; one click spends ~$0.003 and must return three `.reply-variant` angle chips whose text swaps on click, Copy → clipboard + the row showing as `copied` under `@me` in the Replies tab. On a thread whose link lives in the first reply, **both** the seed item and the button are absent and the pin reminder is there instead (D150a). No live launch handy? The room only renders inside its 30-minute window — schedule a post two minutes out rather than faking session storage.

- **(1) The browser-verify matrix owed by UI.11–UI.16** (all panel-convention 2-minute checks; deploy + `chrome://extensions` reload; they fold into whichever session next has Chrome open — UI.16 tried and found the extension not connected). *Cheapest first, UI.15, all four tabs $0:* open **Playbook**, use the header ⚙ to drop **Playbook cell gate** to 5 and confirm the page re-reads itself ONCE a second later (`gate: n≥5 per cell`, and cells reading `insufficient data (n=7/20)` become answers) — **watch for a flicker loop rather than a single refresh, this is the one gear whose effect is a whole-page re-read**; raise it to 100 and confirm confident cells revert; click the reset dot for 20. Then **Replies** (History ⚙ at 20 shortens the list; the *Posted* filter with nothing posted must say `No posted drafts in the last 20 loaded.` rather than claiming you have no drafts; a variant chip still fills when picked, and Conversations' variant pills look identical — they share `.reply-variant`). **Harvest** (start a run: all three pill rows go inert; Following mode still hides Date-range). **Studio** (Brand kit = three sections; Background pills wrap at seven; *Reroll* only on *Blobs*). Then **light theme** on all four. *UI.14:* People's seven-tab stage filter wraps, roster heads read `ally (3)`, the dossier ⚙ at 3 shortens replies/their-mentions/saved-tweets while **the Timeline stays full**; Channels' *My posts* ⚙ must not move the median line above it; Voice's *Saved tweets* ⚙ at 20 says "Showing the first 20"; Ideas' status chips are `open`=accent / `consumed`=green / `discarded`=quiet. *UI.16 (no deploy needed for the two pages):* `/explorer` → the header **◐ system** button twice, dark→light with readable grid text, the choice survives a reload and `/writer` opens wearing it; check row-hover tint, `td.num`/`.dt`/`.json` and the token overlay's scrim in BOTH arms. Then on x.com with the extension reloaded: Save-to-stratus pill, a hot/warm band border, person chips, the `/status/` context panel, the reply-master button's purple — **a broken `font:` shorthand or typo'd token shows up as *unstyled*, not missing**. *UI.13:* the Composer's Schedule ⚙ — retype `3/day anchor hours` to `10, 14, 19`, click OUTSIDE the popover (not the gear) and confirm it saved (the blur-before-close fix, the regression with no test); press Enter in that field and confirm the Composer does NOT save a post; the Calendar's `This week` ⚙ shows the same values. *UI.11:* Tuning renders 14 groups, search `budget` keeps the whole group, a slider drag fires ONE save, an out-of-range value shows the 400 and snaps back. *UI.12:* Today's five ⚙ open and persist; a gear disappears entirely when the server is unreachable while the section still renders.

- **(4) The oldest browser debt in the repo — UI.9's light palette, UI.10's regrouped rail, and RL.5's Lists panel** (moved here from STATE.md's gotchas log at SC.9, for the same reason UI.7's was: an unpaid check parked in a facts list reads as a fact). All three shipped with automated gates only (typecheck + build + lint + suite) and an explicitly deferred visual pass. **UI.9:** the 12-tabs × {dark,light} × {cozy,compact} matrix, with one known trap named in its own plan — AA contrast on warn/ok/band **text over tinted fills** in light (`--strat-warn: #9a6a10`, band-hot `rgb(0,150,100)`, band-warm-text `#8a6100` were re-authored dark enough to double as text; spot-check before trusting). Fix by adjusting `:root[data-theme='light']` values, never the consuming rules. **UI.10:** grouped rail spacing, brandmark, EmptyState, popover. **RL.5:** the Lists panel's live path — create → add → test-render → generate → Overwrite. Item **(1)**'s walk already covers several of these tabs; do them in the same session.

- **(3) UI.7's browser-verify — $0, one PATCH, and it folds into any session with the panel open on x.com:** `PATCH /x/settings` with `x.band.bigViews` set well below a real tweet's view count, wait one mirror sync (≤5 min, or send `stratus/settings-sync`), then confirm a ~500-view tweet's on-page **band badge downgrades**. The point is that badge and gate read the *same twelve numbers* (`src/shared/replyBand.ts` + `src/x/settings/bandThresholds.ts`) — a badge that promises a draft the server then refuses is the fork UI.7 exists to prevent. Restore the knob afterwards with its reset dot, not with a blanket group reset.

- **(2) CA.2 step 2 (still unpaid):** grep the prod journal for `[cost-audit]` — if `includes.tweets` ≈ the console-vs-`/cost/today` gap, drop `referenced_tweets.id` from the metrics-read expansions in `fields.ts` + a `docs/PHASE-HISTORY.md` one-liner; else write the refute one-liner and remove the instrumentation.

## Wave-4 (Authoring 3.0) browser-verify debt — moved here from STATE.md's Wave-4 carry-forward at GT.9

> Older than everything above and still owed. It was written as "cleared by the Wave-5 polish passes"; those passes shipped and cleared the *docs* half, but each explicitly recorded that the browser half was still owed — and then added its own (item **1** above). Same rule as the rest of this file: fold them into whichever session next has the panel open.

- **(1) Browser-verify debt owed, all panel-convention 2-minute user checks (deploy + chrome://extensions reload), cleared by the Wave-5 polish passes:** A3.3 active-times end-to-end row-landing + the passiveCapture-off gate; A3.4 the Composer audience-peak/staleness/blended-Best-time render; A3.7/A3.8 the manual-publish UI (Composer API|Manual toggle, Calendar overdue tint + Mark posted, the alarm→notification→card round-trip); A3.10 the dossier Draft-DM flow + the 422 thin-dossier line; A3.13 the Writer page (create→outline→section→autosave-survives-reload→**Copy-for-X rich-vs-plain**, the risk item — record which clipboard path the prod host accepts); A3.14 the week board (7 columns, draft→slot in two clicks, chips/overdue).
