# Masterplan execution state

> Dynamic memory for `/masterplan`. Updated after EVERY task, in the task's own commit.
> Plan: `plans/MASTERPLAN.md` (static — order, reasoning levels, waves, D1–D10 + Wave-6 seeds D141–D144).
> Codemap: `.claude/skills/plan-feature/references/codemap.md` (updated per task too).
> History: `.claude/skills/masterplan/STATE-ARCHIVE.md` (frozen, grep-only — closed-lane deviations, gotchas, priors, ledger rows).
> Debt: `.claude/skills/masterplan/VERIFY-DEBT.md` (**unpaid**, not history — browser-verify + CA.2. Read it when you have Chrome open; skip it otherwise).

## ✅ Waves 0–5 COMPLETE — 106/106, closed by UI.17 on 2026-07-25 · WAVE 6 IS OPEN (added the same day)

**The next task is in Wave 6.** Waves 0–5 are all closed (106/106); the three backlog plans (`static-coach`, `llm-judge`, `mika-growth-tactics`) were scheduled as **Wave 6** on 2026-07-25 — MASTERPLAN's Wave-6 table + seeds **D141–D144** are the order/deps/reasoning source, the ledger below is the state. **Two of the three sub-plans are CLOSED (GT at GT.9, SC at SC.9). Only JD remains — 5 tasks, JD.3 shipped 2026-07-27.** Unpaid verification debt lives in `.claude/skills/masterplan/VERIFY-DEBT.md`, not the archive, because it is still owed.

**✓ Archive passes paid: GR.10, UI.11, UI.17, GT.9, SC.9, and partial ones at JD.2 and JD.3** — see `STATE-ARCHIVE.md`'s per-pass sections, each of which opens with why its blocks stopped binding.

**Size discipline (added 2026-07-23 at 339 KB; paid seven times since):** STATE.md must stay loadable in a **single `Read`**, and the limit that bites is the tool's **token** cap (~25k), not the 256 KB byte cap. **The budget is measured, and JD.3 re-measured it the hard way: at 61.7 KB a single `Read` came back TRUNCATED at 25,023 tokens — so the real ratio is ≈405 tokens/KB and the ceiling is ≈61.5 KB, not the "roughly 62" SC.9 inferred from a passing read at 61.0. Aim at 60 KB and leave the last 1.5 KB unspent.** Measure BYTES with `wc -c`, not `.length`: this file is full of multibyte `—`/`≥`/`✓` and a character count reads ~1.5 KB under the truth. What lives here: (a) standing rules, (b) entries for the OPEN lane, (c) the ledger + locks. When a sub-plan closes, its D-entries, gotchas, header priors and ledger rows move verbatim to `STATE-ARCHIVE.md` — part of the closing task, not a separate chore. **Four lessons, each of which cost real sessions:** (1) *the breach is never one big entry, it is six good ones that each looked affordable* — Wave 6 went 48 KB → 156 KB in fifteen tasks; (2) *a closing pass archives the lane in front of it and misses the weight behind it* — UI.17 left Wave 5's ledger rows and every session until GT.9 paid for them, so **also ask what stopped binding two waves ago**; (3) *"it still binds an open task" is a reason to DISTIL, not to keep 19 KB* (SC.9 kept a handful of facts inline and archived the entries whole); (4) **an ORDINARY task breaches it too, so `wc -c` before every commit** — JD.2 and again JD.3 each added ~4.5 KB of legitimate spec and each paid by archiving what had stopped binding rather than by trimming the spec. **Measured at JD.3's commit: 60.6 KB, after a SECOND trim pass that the truncated read forced — a pass is not done until you have re-`Read` the file end to end. JD.4 should still expect to archive before it writes.** JD.8 owns the closing pass. Never "fix" a breach by deleting something an open task needs — if a pass slips, pay it as a standalone housekeeping commit (the `55c6d19`/`c9c8ade` precedent).

- **last-commit:** **identity is the SUBJECT LINE — no sha is recorded (D97).** HEAD should read `feat(judge): rubric prompt, schema and parser (JD.3)`, parent `7a5e500` = JD.2. **Step 0 is one command:** `git log -1 --format='%h %s'`, compared against the last ticked subject in the ledger below. **Do not hunt shas** — the per-task chain is in the ledger (parent shas, live and archived) and in `git log`; re-deriving it here is what once made this line 1.8 KB of archaeology. **Out-of-skill commits are NOT ledger drift and are already reconciled** — thirteen of them, all before SC.1, enumerated in `STATE-ARCHIVE.md` §"Archived at JD.2". If Step 0 finds a subject naming neither a ticked task nor one of those thirteen, run the `sync` reconciliation.
- **current wave:** **WAVE 6 (Coach, judge & growth tactics) — OPEN, 20/25 (+ GT.5 `[s]` by design).** **Two of the three sub-plans are CLOSED: GT at GT.9 and SC at SC.9.** Their D-entries/gotchas/priors are in `STATE-ARCHIVE.md` (§"Archived at GT.9" / §"Archived at SC.9"); what still binds live code survives as the **GT facts** and the **SC carry-forward** gotchas below, and the generalizable halves are codemap §7 rules **4a/4b/4c** (GT: measure a membership rule before you gate on it · a counter over a set the counted act can join needs a time qualifier · a client that re-decides a server rule reproduces it with a twin comment and gets diffed on live data) and **33/34** (SC: a borrowed number is a measurement or it is absent · validate the borrowed METHOD too — "latest row wins" is right for displaying one row and wrong for comparing many whenever the re-read is selective). **What is left: JD — 5 tasks, starting at JD.4.** JD.3 ✓ — NEW `src/x/judge/prompt.ts` + its 36-test suite + the 14th registry key; still **no table, no route, no call site**, which is exactly JD.4's job. What JD.4 inherits ready-made: `buildJudgeInput`, `JUDGE_SCHEMA`, `parseVerdict`, `renderJudgeGrounding` (pass it strings — D164c), `judgeTextHash` (normalizes internally — D164d), and `loadPromptSafe('judge')`. **No locked files anywhere** — two chains still serialize: the D141 Composer lane (JD.6 is its LAST link, reading `Composer.tsx` as SC.8 left it) and the `playbook.ts` pair (JD.7). **The migration journal is FREE; the next migrating task owns `0024`** — JD.4 is the wave's only migration and never runs in a parallel session (journal conflicts). Behind it, closed: Waves 0–5 = **106/106** (see the ledger). Registry: **14 groups / 61 knobs, 27 mirrored**; **prompt keys 14** (`registry.test.ts` asserts the exact list + `docs/settings-tab.md`'s two counts); MCP **23 tools** (`src/mcp.test.ts` asserts it exactly + `docs/s2-mcp-server.md`'s three counts); suite **1788**; whole-repo lint **0 errors**; smoke scripts **30**; extension tsconfig `include` shims **6**.
- **next-up:** **the Wave-6 ledger below is the source** — `/masterplan next` takes the first `[ ]` whose deps are `[x]`: **JD.4** (`draft_judgments` + `POST /x/judge` — **the wave's only migration, runs alone**, journal assigns `0024` and the plan's "0018" is stale; the $0 refusal ladder lands before the paid call, §7.4, and the insert is best-effort, §7.8/decision 9. It imports JD.3's `buildJudgeInput`/`JUDGE_SCHEMA`/`parseVerdict`/`judgeTextHash`/`renderJudgeGrounding` and must NOT re-derive the band — `deriveVerdictBand` spreads last. Cache key `${prompt.cacheKey}:${niche.slug}:${niche.updatedAt?.getTime() ?? 0}`, the drafter idiom, because the grounding varies with the niche), then JD.5 → JD.6/JD.7 (the two lane-tail tasks) → **JD.8**, which closes the wave and owns the archive pass D160/D162 spec (read both, plus the SC.9 gotcha, before writing it). Read **D164** before JD.4, **D163** before JD.6, the archived **D149** before touching the few-shot/grounding path, and the **SC carry-forward** gotcha before JD.6 or JD.7 — the judge panel goes BELOW the AI.8 rewrite block, not inside `.coach` (D141), and a judge-score Playbook cell must not be keyed on a band the corpus reports at 97% `top`. **Verification debt is NOT here** — it is `.claude/skills/masterplan/VERIFY-DEBT.md`: thirteen items (ten GT/SC browser-verifies, the UI.11–UI.16 panel matrix, UI.7's band knob, CA.2 step 2), unpaid, none of them a masterplan task, none binding on any open task. Read it when you next have Chrome open on the panel; otherwise skip it, which is why it is no longer in the file every session must read. **Standing gate reminders for any future work:** `bun run test` (bare `bun test` targets the file DB and `inspect.test.ts` fails 2/2); biome forbids `delete process.env.X` / `: any` in `scripts/`, **non-null assertions (`x!`) anywhere**, and a `let` assigned once — it also sorts imports case-sensitively, collapses short chained calls, and rejects a `console.log` string-concat unless every operand interpolates (run `bunx biome check --write`). Extension-touching work adds `cd extension && bun run build` **and** its own `typecheck`, then `grep -cE '^\s*import[ {]' extension/dist/content.js` == 0.
- **prior (JD.3 — `src/x/judge/prompt.ts`, the rubric prompt + schema + parser + the `judge` registry key; high, dep JD.2✓, **$0 — no call site yet**, 5 files [2 new], server-only, no schema/migration/mirror/route/MCP/extension change; suite 1752→1788 +36):** everything JD.4's route needs except the route. The prompt is ours (the study contributes the dimension list only) with `{{GROUNDING}}` then `{{DRAFT}}` at the tail; **`JUDGE_SCHEMA` is BUILT from `JUDGE_DIMENSIONS`, never hand-typed** (§7 rule 4c), which is also what lets a test assert schema-vs-vocabulary equality both ways. **The parser is the deliverable and its asymmetry is the new codemap §7 rule 35** — strict on the thirteen scores, lenient on everything else, because returning `null` costs a call that was already billed. Ranges and caps live in the PARSER, not the schema (`minimum`/`maxItems` are unsupported strict-mode keywords — the threadPrompt/RU.3/AI.8 rejection risk; `['integer','null']` on `audienceMatch` is the documented nullable form and stays). `judgeTextHash` normalizes internally — it is the §7.12 read-time link JD.4 stores and JD.7 joins on. Registry key = the RL.4 4-file move → **14 keys**, `docs/settings-tab.md` 13→14. **No browser-verify owed** (no surface; the Prompts panel renders whatever `GET /x/prompts` returns, and its count copy is the doc that moved). See **D164**.
- **earlier priors (JD.2, UI.6 back through HV.1):** moved verbatim to `STATE-ARCHIVE.md` (UI.6→UI.2 + A3.15 archived at UI.11; A3.1–A3.14 at A3.15; GR.9→HV.1 at GR.10). Their forward-binding content survives here as the two carry-forward gotchas below.

## Ledger

Status: `[ ]` todo · `[~]` in progress (lane claimed) · `[x]` done (sha + date) · `[s]` skipped (reason in deviations).

**Waves 0–5 are CLOSED — all 106 tasks `[x]`.** Per-task entries (shas, parents, dates, notes) moved verbatim to `STATE-ARCHIVE.md` — Waves 0–4 at UI.11 (§"Ledger — Waves 0–4"), Wave 5 at GT.9 (§"Ledger — Wave 5"). Grep by task id. Summary:

- **Wave 0 — Foundations (13/13 ✓)**: UI.1, UI.8, UI.9, UI.10, ST.1–ST.9. Closed by `d1902e5` ST.9 studio docs+smoke.
- **Wave 1 — Prompt & identity core (32/32 ✓)**: RU.1–RU.3, N.1–N.9, ME.1–ME.7, AI.1–AI.13. Closed by `f86e06a` AI.13 AI docs+smoke.
- **Wave 2 — Reply machine & on-page surfaces (29/29 ✓)**: RU.4–RU.10, AX.1–AX.7, RL.1–RL.8, NT.1–NT.7. Closed by `5d4a1a4` NT.7 NT docs+smoke.
- **Wave 3 — Ambient data & guardrails (16/16 ✓)**: HV.1–HV.6, GR.1–GR.10. Closed by `docs(guardrails): GR docs-sync + $0 smoke-guardrails.ts (GR.10)`, parent `7adfbca`.
- **Wave 4 — Authoring 3.0 (15/15 ✓)**: A3.1–A3.15. Took migration `0023_slimy_night_thrasher` (`articles`) — the journal is FREE from `0024`. Closed by `docs(authoring): Authoring 3.0 docs-sync + $0 smoke-authoring3.ts (A3.15)`, parent `819c61d`.
- **Wave 5 — Settings moat + polish (16/16 ✓)**: UI.2–UI.7, UI.11–UI.17. Closed by `docs(ui): cockpit overhaul docs-sync + $0 smoke-settings.ts (UI.17)`, parent `7b10239`. Left the registry at **14 groups / 61 knobs, 27 mirrored**, MCP **23**. Per-task rows archived at GT.9.

### Wave 6 — Coach, judge & growth tactics (added 2026-07-25; OPEN — Wave 5 closed by UI.17)

Order/deps/reasoning: MASTERPLAN Wave-6 table + seeds **D141–D144** (Composer serial lane
SC.3→SC.6→SC.8→JD.6; GT.5 superseded by SC.6; `src/x/replies/*` serialization GT.1→JD.1,
GT.6→GT.8; SC.10/SC.11 not scheduled). JD.4 is the wave's only migration (journal assigns
the number — plan text's "0018" is stale). STATE's live deviation register continues from **D145**.

- [x] SC.1 postCoach check engine (high) — subject `feat(coach): deterministic pre-publish check engine (SC.1)`, parent `db34235`, 2026-07-27 (no sha by design — D97). **Opens Wave 6; suite 1568→1604 (+36). See D145.**
- [x] SC.2 postFormat classifier (high) — subject `feat(coach): deterministic post-format classifier (SC.2)`, parent `dc5b2aa`, 2026-07-27 (no sha by design — D97). **14 formats, real-corpus `other` = 0.7% vs the <35% bar; suite 1604→1632 (+28). See D146.**
- [x] GT.1 echo-anchor reply prompt (xhigh) — subject `feat(replies): echo-anchor rule in reply prompt (GT.1)`, parent `282abb5`, 2026-07-27 (no sha by design — D97). **Voice rules 6→7 in BOTH reply literals; suite 1632 unchanged (no new tests by design). Unblocks JD.1 (D143). See D147.**
- [x] GT.2 reply-bait skeletons post prompt (high) — subject `feat(drafter): proven engagement formats in post prompt (GT.2)`, parent `d9d042f`, 2026-07-27 (no sha by design — D97). **Closes the wave's byte-sync prompt pair; suite 1632 unchanged (no new tests by design). See D148.**
- [x] JD.1 few-shot dilution + trust markers (xhigh) — subject `fix(drafter): dilute machine-drafted posts in the few-shot block (JD.1)`, parent `7abb46f`, 2026-07-27 (no sha by design — D97). **Templates byte-untouched (the label rides on the rendered value); the D143 replies-prompt lane closes here. Suite 1632→1639 (+7). See D149.**
- [x] GT.3 launch seed comment (high) — subject `feat(launch): seed-first-comment checklist + one-click draft (GT.3)`, parent `d3a6588`, 2026-07-27 (no sha by design — D97). **The account's own @handle does not exist anywhere in stratus — the seed context ships `handle:'me'` placeholders. Suite 1639 unchanged (panel convention, no new tests). See D150.**
- [x] GT.4 milestone nudge (high) — subject `feat(brief): milestone crossing nudge + Today card (GT.4)`, parent `1aa4591`, 2026-07-27 (no sha by design — D97). **No new query, no registry knob, no migration; suite 1639→1649 (+10). See D151.**
- [x] SC.3 Composer coach column (high) — subject `feat(composer): live static coach column (SC.3)`, parent `4fac361`, 2026-07-27 (no sha by design — D97). **Opens the D141 Composer lane and claims `Composer.tsx`; suite 1649 unchanged (panel convention, no new tests). See D152.**
- [x] SC.4 reply-variant coach chips (high) — subject `feat(replies): coach score chips on variants (SC.4)`, parent `1fb9e9b`, 2026-07-27 (no sha by design — D97). **Four pickers, not the plan's two; `COACH_TONE`/`COACH_BAND_TONE` moved out of `Composer.tsx` into the new `sidepanel/CoachChip.tsx`. Suite 1649→1654 (+5). See D153.**
- [x] SC.5 playbook format + coach-score cells (high) — subject `feat(playbook): format + coach-score effectiveness cells (SC.5)`, parent `8b68b05`, 2026-07-27 (no sha by design — D97). **Claims `playbook.ts` + `routes/playbook.ts` for the SC.5→JD.7 lane; the `postFormat` extension shim landed here (SC.6 inherits it). Suite 1654→1667 (+13). See D154.**
- [x] SC.6 format cooldown — supersedes GT.5, D142 (high) — subject `feat(composer): format cooldown from own corpus (SC.6)`, parent `9dd2bbb`, 2026-07-27 (no sha by design — D97). **The plan's Jaccard idea-clustering was dropped on measured evidence; the fallback formats are exempt (D146b answered). Suite 1673→1690 (+17). See D156.**
- [x] SC.7 niche coach lexicon (high) — subject `feat(coach): niche-scoped lexicon (SC.7)`, parent `8ac0d4d`, 2026-07-27 (no sha by design — D97). **Touched `Composer.tsx` out of the D141 lane (2 lines, like SC.4) — SC.6 is still next on it. Suite 1667→1673 (+6). See D155.**
- [x] GT.6 band-gate roster exemption + doctrine knob (xhigh) — subject `feat(replies): band-gate roster exemption + reciprocity doctrine knob (GT.6)`, parent `5b0f7a9`, 2026-07-27 (no sha by design — D97). **Membership shipped at `stage ≥ engaged` ∪ roster, NOT the plan's `stage ≥ noticed` — measured, see D157. Claims `routes/replies.ts` for the GT.6→GT.8 lane. Suite 1690→1700 (+10).**
- [x] GT.7 reciprocity quest (high) — subject `feat(quests): daily reciprocity-replies quest (GT.7)`, parent `60e9c83`, 2026-07-27 (no sha by design — D97). **`QUEST_KEYS` is 6. The plan's plain membership test was measured and repaired — the counted set is the DAY-START one (`{asOf}`), or the quest is a copy of the replies quest. Suite 1700→1707 (+7). See D158.**
- [x] GT.8 roster sightings into Radar — after GT.6, D143 (xhigh) — subject `feat(radar): roster sightings — your people enter the queue regardless of band (GT.8)`, parent `b275014`, 2026-07-27 (no sha by design — D97). **Membership is the client twin of the gate's set, NOT the plan's glance-map presence (1,596 vs 14 handles, measured on prod); releases the `routes/replies.ts` lock. Suite 1707→1719 (+12). See D159.**
- [x] GT.9 GT docs-sync + smoke — no cooldown assertion, D142 (high) — subject `docs(growth): GT docs-sync + $0 smoke-growth-tactics.ts (GT.9)`, parent `62ec174`, 2026-07-27 (no sha by design — D97). **CLOSES the GT lane and Wave 6's first sub-plan; owner of the Wave-6 archive pass (156 KB → ~76 KB, incl. Wave 5's ledger rows that UI.17 missed and the verification debt, now `VERIFY-DEBT.md`).** Docs were mostly a verification pass; the owed pieces were GT.1's and GT.2's prompt changes, undocumented until now. Suite 1719 unchanged. See **D160**.
- [x] SC.8 fitted reach band (xhigh) — subject `feat(coach): fitted reach band from own corpus (SC.8)`, parent `cdd2632`, 2026-07-27 (no sha by design — D97). **BOTH house readers the plan named were measured and rejected (latest-snapshot, age-normalization); ships FIRST-snapshot + raw views in the 3–27h window. Zero formats clear the gate today and that is the designed outcome. Releases `Composer.tsx` — JD.6 is the D141 lane's last link. Suite 1719→1733 (+14). See D161.**
- [x] SC.9 SC docs-sync + smoke (high) — subject `docs(coach): static-coach docs-sync + $0 smoke-coach.ts (SC.9)`, parent `524f0ca`, 2026-07-27 (no sha by design — D97). **CLOSES the SC lane and Wave 6's second sub-plan; owner of the SC archive pass (105 KB → 61 KB — one `Read` again, ~1 KB of headroom).** Docs were mostly a verification pass; the owed piece was the cross-cutting `docs/sc-static-coach.md`. Suite 1733 unchanged. See **D162**.
- [x] JD.2 shared/judge.ts (high) — subject `feat(judge): shared verdict types and annotation locator (JD.2)`, parent `2dd85bb`, 2026-07-27 (no sha by design — D97). **Opens the JD lane, Wave 6's last sub-plan. Vocabulary only — no prompt, no schema, no hashing, no call site. Suite 1733→1752 (+19); extension shims 5→6. See D163.**
- [x] JD.3 judge rubric prompt/schema/parser (high) — subject `feat(judge): rubric prompt, schema and parser (JD.3)`, parent `7a5e500`, 2026-07-27 (no sha by design — D97). **`PROMPT_KEYS` 13→14 (`judge`, the RL.4 4-file move); `JUDGE_SCHEMA` is built from `JUDGE_DIMENSIONS`, and the parser — not the schema — owns the ranges and caps. Suite 1752→1788 (+36). See D164.**
- [ ] JD.4 draft_judgments migration + POST /x/judge — migration, runs alone (xhigh)
- [ ] JD.5 POST /x/judge/apply never-worse guard (xhigh)
- [ ] JD.6 Composer judge panel — after SC.3/SC.6/SC.8, D141 (high)
- [ ] JD.7 playbook judgeEffectiveness cell — after SC.5 (high)
- [ ] JD.8 JD docs-sync + smoke (high)
- [s] GT.5 register/pillar cooldown — **skipped by design: superseded by SC.6 (D142)**

## Hot-file locks

A lane claims a file before starting, releases on commit. `owner: —` = free.
**Every file is free right now, but two chains still serialize:** the D141 Composer lane
(SC.8 → JD.6) and the `playbook.ts` pair (SC.5 → JD.7). Each claimant reads the file as the
previous one left it, which is what the rows below record. The migration journal is the third
row and states a standing invariant rather than lane state.
**Rows are archived when their lane closes and no open task binds the file** — 25 went at
UI.17, three more (`content.ts`, `routes/replies.ts`, `replies/prompt.ts`) at GT.9. The
documented way to reach a closed lane's file notes is to **grep `STATE-ARCHIVE.md` by
filename** before touching a file that lane built. Do that; the load-bearing half is also in
codemap §5/§7.

| File | Owner |
|---|---|
| `extension/src/sidepanel/Composer.tsx` | — (**released by SC.8 — the D141 lane's third link is done; `JD.6` is the LAST claimant and reads the file as SC.8 left it.** **SC.8 left the `.coach` box reading head -> cooldown line -> reach line -> fix rows -> nudge rows -> `<details>` passing -> disclaimer, and added a second per-mount fetch (`api.coach.reach`) beside the cooldown one plus two module-level pure helpers `reachNote`/`reachTitle`. `draftFormat` is now read by BOTH corpus lines — reuse it, don't re-classify. JD.6's judge panel goes BELOW the AI.8 rewrite block per D141, i.e. NOT inside `.coach`: everything in that box is free and instant, and a panel that costs an LLM call does not belong in a container the user reads as live.** Standing facts JD.6 inherits: the box sits between the URL/over-limit warns and the Schedule `Section`; `coachDraft`/`coachInput`/`coach` are computed right after `threadSegments`; the tones live in `CoachChip.tsx` (import, never re-declare); and **`canSubmit` must stay ignorant of every score this lane adds** — four tasks have now honoured that, which is how SC decision 4 is enforced rather than merely intended)
| `src/x/playbook.ts` + `src/x/routes/playbook.ts` | — (**released by SC.5 — JD.7 is the only remaining claimant** (`judgeEffectiveness` cell, MASTERPLAN's `playbook.ts SC.5 → JD.7` serialization). Standing facts JD.7 inherits: the own-originals loader is `loadOriginalPostRows` (widened + renamed at SC.5, selects `text`, feeds media + format + coach-score — add a column, never a second query); a new cell is `build*` in the pure file → the loader → the route payload → a `Playbook.tsx` section → `shared/types.ts` + `api.ts`; route tests over these cells must be partition invariants or before/after deltas, never absolutes (other test files seed originals into the shared in-memory DB), and a new describe goes LAST in the file if it inserts originals. **Do not key a new cell on the coach BAND — D154(a) measured it at 97% `top`.**) |
| `src/db/migrations/` journal (any migration task) | — (A3.11 took `0023_slimy_night_thrasher` (`articles`); next owns `0024`. **`JD.4` is Wave 6's only migrating task — never run two in parallel sessions (journal conflicts), and ignore the hardcoded number in its plan text ("0018" is stale): `bun run db:generate` against the current journal, then inspect the SQL for dropped seed INSERTs, codemap §4**) |

## Deviations & decisions register

Pre-seeded from cross-plan analysis (see MASTERPLAN "Cross-plan adaptations" for full text).
Append D11+ as work reveals divergences from plan text.

**Closed-lane D-entries (163 of them: Waves 0–3, the Wave-4 closer D127, the registry lane D128–D133, the polish lane D134–D140, the GT lane D147/D148/D150/D151/D157/D158/D159, the SC lane D145/D146/D152–D156/D161, and — archived at JD.3 as no-longer-binding, each with a pointer line left in place — D11/D13/D149) live in `STATE-ARCHIVE.md` — grep by task id or by the filename the lane built.** What stays below in full is (a) what is **standing** — true of the repo regardless of which plan you are on: **D7** (new UI composes the UI.10 primitives + `--strat-*` tokens), **D97** (subject-line bookkeeping), **D113(d)/(e)** (how to smoke a surface whose reads write — it binds every future smoke) — and (b) the entries for the one OPEN lane and its closer: **D163** and **D164** (JD), **D160** (GT.9's closing pass) and **D162** (SC.9's, which together are JD.8's spec). D1–D10 discharged at UI.11. The forward-binding content of the archived lanes survives as the **two carry-forward gotchas** below (Wave-3, SC), the pointer lines, and the two hot-file-lock rows.

- **D7** (standing): all NEW UI from Wave 1 on uses UI.10 primitives + `--strat-*` tokens; Wave-5 polish passes touch only pre-masterplan tabs.
- **D13** (UI.1) — **ARCHIVED AT JD.3** (verbatim in `STATE-ARCHIVE.md`; grep `registry.ts`). The doctrine-seeding half discharged with UI.2 and the knob lane closed at Wave 5. The clause that outlives it: **settings consumers import the BOUND `getSetting`/`resolveSetting`/`getAllValues`/`setSettings`/`resetSettings` from `src/x/settings/registry.ts`, never `src/settings/store.ts`** (which is platform-agnostic and never imports `src/x/*`). Read the archived block, plus the Wave-5 registry carry-forward, before adding a knob.
- **D97** (HV.5, bookkeeping — supersedes the sha half of D93a/D94a/D95a): **the ledger records the commit SUBJECT LINE, not a sha.** Three tasks in a row wasted a Step-0 investigation proving a recorded sha was an amend-orphan, and every time the resolution was "the subject line is the identity". A sha can only be written into the commit that changes it by amending, which changes it again — the churn is structural, not carelessness. From HV.5 on: ledger entries carry the subject line + the PARENT sha (stable, already in history), the codemap header stamps `<parent>+<TASK>`, and Step 0 is `git log -1 --format='%h %s'` compared against the last ticked subject. No reconciliation, no orphan hunts.
- **D113** (GR.10, trimmed at UI.11 — parts (a)–(c) were GR.10-specific docs history and are in `STATE-ARCHIVE.md`; **(d) and (e) bind every future smoke, incl. UI.17's**): **(d) A smoke over a surface whose READS WRITE cannot follow `smoke-passive-harvest.ts`.** D98c's namespace-then-delete rule assumes every write is attributable; two Guardrails writes are GLOBAL (a complete-run reconcile marks EVERY unseen live handle `gone`; `GET /following/queue` releases rows at read time). So `smoke-guardrails.ts` additionally **snapshots and restores** every foreign `following.status` (from the success path AND from `fail()`), plus `commitments` and the seeded `streaks`/`digests` keys — and, because its fixtures sit inside today's window while `GET /x/brief` writes the C9 diary, it closes by **re-reading the brief** so the diary describes clean data rather than deleted fixtures. Copy this, not D98c alone. **(e) A fixture fact the plan's sequence hides:** "unseen" in the reconcile is `last_run_id != runId` — per **run**, not per batch, so proving `gone`/`confirmed` needs TWO runs.
- **D149** (JD.1) — **ARCHIVED AT JD.3** (verbatim in `STATE-ARCHIVE.md`; grep D149 or `drafter.ts`). **Read it before any few-shot / winners / trust-marker work.** The one clause that outlives it, because it is a trap rather than a decision: **`posts_published.source` is `'scheduled'|'manual'` — the PUBLISH path, not authorship.** The only column that answers "who wrote this" is `scheduled_posts.source` (`'api'` vs `'drafter'`), reached through `posted_tweet_id`, which the publisher and the manual reconcile both stamp.



- **D163** (JD.2, binds JD.3/JD.6/JD.7): **three exports beyond the plan's list, one algorithm deliberately NOT improved, two types JD.3 inherits.** **(a) All three additions are the same move — a fact the panel would otherwise invent.** `JUDGE_SUB_DIMENSIONS` exists because the plan's own done-when has the panel rendering "twelve collapsed dimensions" while `JUDGE_DIMENSIONS` is thirteen; without it JD.6 hand-types a 12-element twin list (§7 rule 4c). `JUDGE_DIMENSION_LABEL` follows three precedents (`FORMAT_LABELS`, `COACH_BAND_LABEL`, `BAND_LABEL` all live in the shared module). **`JUDGE_HIGHER_IS_WORSE` is the load-bearing one:** `negativeRisk` 94 and `statusDependency` 94 are *bad*, and a panel colouring scores green-high says the opposite — polarity is a rubric fact and belongs beside the rubric, while thresholds/tones stay presentation (the `CoachChip.tsx` split). **Its omissions ARE the decision:** `answerEffort`/`strangerAnswerability` are absent because read as targets they are the reply-farm gradient (JD decision 4) — neutral is the honest polarity, and the test pins the set at exactly two with that reason inline. **(b) `locateAnnotations` ships the plan's consumed-offset walk unimproved: annotations arriving out of DOCUMENT order lose the earlier one.** A claimed-range matcher fixes it in ~15 lines and passes every test the plan asks for — but it buys overlap semantics against a failure mode nobody has observed, and the SC-lane doctrine is that a behaviour change is a measurement or it is absent. **The limitation is pinned by a test**, which is what makes changing it deliberate rather than a silent regression. Revisit against real judgments. **(c)** `JudgeVerdict` is the MODEL-derived half only — **no `overall` sibling** (it is `scores.overall`; two copies is how a label and a number start disagreeing) and none of the transport fields (`id`/`textHash`/`model`/`provider`/`costUsd`/`requestId` are the route's). JD.3 kept the `JudgeConfidence` enum as-is, so that half is settled.
- **D164** (JD.3, binds JD.4/JD.5/JD.6/JD.8): **four plan readings resolved, three of them because the plan predates thirteen tasks of its own repo.** **(a) The registry key is the 14th, not the plan's "11th", and `registry.test.ts` DID need changing.** The plan's Task 3 says "add the `judge` key (11th)" and "nothing in `registry.test.ts` needs changing (no key-count assertion exists)" — both were written before `dm` and `article` landed, and the suite has asserted the **exact key list** with `toEqual` since AI.3. So this was the full RL.4 4-file move: `registry.ts` + the pure module + `registry.test.ts` (list + `isPromptKey` + `defaultBody`) + `docs/settings-tab.md` 13→14. **A plan that quotes a count is quoting the day it was written; re-derive it.** The done-when's "`GET /x/prompts` lists 11 prompts" reads as 14. **(b) The schema's numeric bounds were dropped and that is a correctness decision, not a style one.** The plan asks for `scores` as "13 integer 0–100"; `minimum`/`maximum` are on the same strict-structured-outputs UNSUPPORTED list as the `minItems`/`maxItems` the threadPrompt/RU.3/AI.8 schemas already refuse, so shipping them risks a provider rejection on every judge call. The **parser** enforces 0–100 (and the 12/5 caps), which is where the plan already puts the caps anyway. `['integer','null']` on `audienceMatch` is the opposite case — a union with null is the *documented* way to make a required field nullable, so it is not the same risk and it stays. **The generalizable half is codemap §7 rule 35:** what the schema cannot express, the parser enforces, and the parser is what a test pins. **(c) `renderJudgeGrounding` takes STRINGS, not the plan's `(niche, pillarLabels)`.** The plan's Edit list and its How-section disagree ("the route loads the niche and passes strings, so the prompt module stays pure and testable"); the How won. `renderJudgeGrounding({persona?, beliefs?, pillarLabels?}) → string|null`, null when there is nothing real — mirroring `renderIcebreakerGrounding`, and JD.4 supplies the loaded values. It also means the grounding is testable with no DB, which is why 36 tests fit in a $0 pure suite. **(d) `judgeTextHash` uses `Bun.CryptoHasher`, not the plan's `node:crypto`** — same sha256, and it is the house idiom (`promptCacheKey`). It **normalizes internally**, so JD.4's insert and JD.7's Playbook join both hash raw text and cannot disagree about which side was normalized. **Deliberately NOT done, flagged rather than skipped:** the plan's `parseVerdict` line "annotations clamped to 12 **and individually dropped when the quote is empty or over-long**" is implemented, but nothing here validates a quote against the draft — that check is `locateAnnotations` at render time (JD.2), and duplicating it in the parser would make a hallucinated quote a 502 instead of a dropped row.
- **D160** (GT.9, binds SC.9/JD.8 and every future closing docs-sync): **three things the plan's Task 9 asks for that the repo had already answered differently, and one structural debt a closing pass is the only one positioned to see.** **(a) The smoke's spec was written for a feature that was never built and for a `CLAUDE.md` that no longer takes phase entries.** Task 9's step (b) is the register/pillar cooldown — GT.5, `[s]` since the wave opened (superseded by SC.6, D142), so asserting it would test a module that does not exist; SC.9's smoke owns the format arithmetic, and what it should assert there is the **exempt invariant** and a seeded flip, not a raw count over a shared corpus (D156f). Its "CLAUDE.md: one phase-style entry" means `docs/PHASE-HISTORY.md` since the 2026-07-23 refactor. Both are recorded rather than silently skipped. **(b) The docs half was a VERIFICATION pass and `git log --name-only -- docs/` is how you scope one in a single command** — five GT tasks had each paid their own tab doc, exactly as the Wave-3 carry-forward requires, so guessing would have meant re-reading five docs or trusting a plan written five days earlier. **What that command surfaced is the useful part: the two tasks with NO tab doc to update were the two prompt tasks, and they had therefore shipped entirely undocumented.** GT.1's echo anchor and GT.2's four skeletons change what every draft says and appear nowhere a user reads. A prompt task has no natural docs home, so it silently exits the per-task discipline — **a future prompt-only task should write its user-facing paragraph in its own commit**; `docs/replies-tab.md` §"Generating and the three variants" and `docs/composer-tab.md` §"Generating and picking a draft" are now the homes. The same pass caught a stale "two variants in one call" in the cost paragraph, three waves after RU.1 made it three. **(c) `evals/i_mika_el_analysis.md` got more than the plan's one-line pointer, on purpose.** The plan asks for "shipped as GT.1–GT.8". But **three of the study's own recommendations did not survive our corpus** (the `noticed` membership floor, the quest's plain membership test, x-builder's 0.45 Jaccard bar — D157a/D158a/D156a), and an eval doc whose recommendations were silently overruled is worse than no eval doc: the next person to mine it re-proposes the same numbers. The pointer section records what was replaced and by which measurement. **The generalizable rule: when a plan's borrowed number loses to a measurement, the correction belongs in the SOURCE the number came from, not only in the deviation register.** **(d) A closing pass archives the lane in front of it and misses the weight behind it.** UI.17 archived the polish lane and left **Wave 5's own 13 ledger rows** in STATE.md; every session since Step-0'd past them. GT.9's pass therefore moved four things, not one: the GT lane, those rows, the discharged narration in the header, and the verification-debt matrix — which is *unpaid* debt and so could not be archived, but had become the single largest block in the file and binds no open task, so it moved to `VERIFY-DEBT.md` with a pointer. 156 KB → ~76 KB. **So the closing question is not "what did my lane leave" but "what stopped binding anything, ever".**



- **D162** (SC.9, binds JD.8 and every future closing task): **three things a closing pass discovers only by doing it, and one plan done-when that was wrong about its own engine.** **(a) The plan's done-when did not survive contact with the code it specified.** SC's own bar says the em-dash + "thoughts?" + 15-line draft shows "exactly three **Fix** rows"; the engine ships `em_dash` and `weak_closer` as **nudges** (severity is per-rule — D145), so the draft scores **80 / ship it** with exactly ONE fix. The smoke asserts the contract that is actually true — all three rules FIRE, `show_more` is the only fix — with a comment saying why it is not the plan's sentence. A closing task is the last chance to notice that a plan's acceptance criterion describes a different implementation than the one that shipped; **assert the code, record the divergence, never quietly restate the plan.** **(b) The assertion-shape ranking** (invariant > delta > the plan's bar > an exact number, and the last one only in a *pure* step on a synthetic corpus) is stated in full in the **SC.9 gotcha** below — read it there, it is the same paragraph and it belongs with the fixture facts. **(c) Both new files answered a question no per-task doc could.** `git log --name-only -- docs/` showed SC.3–SC.8 each paid their tab doc (D160b), so the owed piece was the one **no tab owns**: the cross-cutting story, including the table of what was deliberately NOT adopted. **A lane whose whole thesis is "a number is a measurement or it is absent" needs a doc that says what it refused to guess** — otherwise the refusals read as missing features. Same reasoning drove the `evals/x-builder-static-engine-analysis.md` postscript (D160c): four of that study's numbers and two of stratus's own house readers lost to the corpus, and an eval doc whose recommendations were silently overruled invites the next reader to re-propose them. **(d) One stale pointer fixed:** `smoke-growth-tactics.ts` named a `smoke-static-coach.ts` that never existed (the plan and STATE both say `smoke-coach.ts`). A forward reference written by the *previous* task is unverifiable when it is written and invisible afterwards — **when you name a file a future task will create, the task that creates it owns the check.**


## Gotchas log

Things the next implementer must know that aren't obvious from the code. Append-only,
one line each, newest last.

**Closed-lane gotchas (182 of them: Waves 0–3, the eleven archived at UI.11, the six UI.1x blocks at UI.17, the seven GT blocks at GT.9, the eight SC blocks + three browser-debt entries at SC.9, the Wave-5 registry carry-forward + UI.17's first two facts at JD.2, and the Wave-4 carry-forward + the JD.2 block at JD.3) live in `STATE-ARCHIVE.md` — grep it by task id or filename before touching a file a closed lane built.** What stays below: standing test/lint/typecheck traps, the JD lane's own gotchas, GT.9's and SC.9's (they are the closing-task spec JD.8 inherits), **two** carry-forward entries (Wave-3, SC) holding the closed lanes' facts that still bind an open task, and **pointer** lines where two others used to be (Wave-5-registry, archived at JD.2; Wave-4, archived at JD.3 — both with their residue distilled inline) — consolidated rather than left as fifty separate lines. **Browser-verification debt is not here — it is `VERIFY-DEBT.md`**, and SC.9 moved the last three stragglers (UI.9's palette, UI.10's rail, RL.5) out of this list for the reason UI.7's went: a debt parked among facts reads as a fact.

- **Settings types are extension-local mirrors — ARCHIVED AT JD.3** (verbatim in `STATE-ARCHIVE.md`). Standing residue: the extension MUST NOT import `src/x/settings/registry.ts` (§5 build-isolation); it mirrors the `GET /x/settings` JSON in `extension/src/shared/types.ts` and a new server-side `SettingDef` field is hand-synced there.
- **`exactOptionalPropertyTypes` pass-through pattern (UI.10):** primitives forwarding a maybe-undefined optional (`Slider.unit`, `SettingRow.onReset`, `GearPopover.onReset`/`label`) type the prop as `T | undefined` explicitly — else TS rejects `prop={maybeUndefined}`. Reuse this for new primitives that relay optionals.
- **`inspect.test.ts` fails 2/2 under bare `bun test`, GREEN under `bun run test`** (the canonical `SQLITE_PATH=:memory: bun test`). `src/db/client.ts` defaults to `:memory:` when `NODE_ENV==='test'`, but `src/x/data/inspect.ts`'s readonly connection still defaults to `./stratus.db` when `SQLITE_PATH` is unset — the primary writes memory, the readonly reads the file, seeded rows are invisible. **Always gate with `bun run test`.** One-line fix for a future data-core task: make `inspect.ts` honor the same `NODE_ENV==='test'` default (or have `client.ts` `process.env.SQLITE_PATH ??=` set it so both agree).
- **RL.4: adding a registry key is a 4-file move** (JD.3 walked it again — see D164a): `PROMPT_KEYS` + `PROMPT_SPECS` in `prompts/registry.ts`, the default exported from a **PURE** module (a route import cycles — AI.5's `voice/extractPrompt.ts` move exists for that reason), `registry.test.ts`'s **exact key list** + `isPromptKey` + `defaultBody` assertions, and `docs/settings-tab.md`'s two prompt-count strings (now **14**). The extension needs nothing — the Prompts panel renders whatever `GET /x/prompts` returns. **`registry.test.ts` also loops every default through `validatePromptBody` asserting `unknown:[]`, so every `{{X}}` in a new template must be in `required`∪`optional`.**
- **NT.1: extension typecheck does NOT cover `*.test.ts`** — `extension/tsconfig.app.json` has `"exclude": ["src/**/*.test.ts"]`, and root `tsconfig.json` only includes `src/**/*` + `scripts/**/*` + `drizzle.config.ts`. So an extension test file is checked by **`bun run test` alone**; a type error in one is invisible to both typechecks. (Same for `earlyReplies.test.ts` — pre-existing, just never written down.)
- **NT.7: `app.request(...)` is typed `Response | Promise<Response>`.** A helper declared `function f(): Promise<Response> { return app.request(...) }` fails root typecheck; make the helper `async`. `bun run typecheck` covers `scripts/**`, so this bites in smoke scripts, not in route suites (which `await` inline).
- **CLAUDE.md was slimmed to guardrails-only (2026-07-23, out-of-skill docs refactor).** The full phase-entry ledger moved **verbatim** to `docs/PHASE-HISTORY.md`. **Every docs-sync task (MASTERPLAN global rule 6, codemap §7 rule 29 — updated) now appends its phase entry to `docs/PHASE-HISTORY.md`, NOT CLAUDE.md**; CLAUDE.md changes only when a guardrail (invariant, workflow, stack quirk) changes. Plan text still saying "CLAUDE.md phase entry" means the history file.


- **Wave-3 carry-forward (the only HV/GR facts that still bind an open task — everything else is in the archive):** **(1) MCP tool count is asserted exactly.** `src/mcp.test.ts` asserts **23** (UI.4 took it 21→23 with `x_settings`+`x_update_setting`); every future tool bumps that number *and* `docs/s2-mcp-server.md`'s three counts (total / curated tier / write tier) **in the same commit** — that doc was silently 5 tools stale for four phases. **(2) Docs now assert user-visible strings, and two Wave-5 tasks own them.** `docs/harvest-tab.md`, `docs/playbook-tab.md`, `docs/people-tab.md`, `docs/today-tab.md`, `docs/composer-tab.md` and `docs/settings-tab.md` quote real copy (the both-off warn line, `Passive: N rows today`, `insufficient data (n=…)`, `Nobody has shown up often enough yet.`, the three schedule-warning sentences, the debt-tier wording, the `Nd left · need X/day · doing Y/day` line). **UI.14 and UI.15 did re-read the matching `docs/*-tab.md` in their own tasks and UI.17 verified all twelve — the standing half is the RULE, not the assignment: reword a quoted string and you owe the doc in the same commit** (`docs/today-tab.md` is numbered §1–§18, so an inserted card renumbers rather than appends; the settings doc's toggle count was fixed at UI.11 and its registry table added at UI.17). **(3) Real-DB fixture rules that outlive their lane:** a seeded `posts_published` row is written **`retired: true`** (NT.7 — otherwise it is a candidate for the daily *billed* pass), a `following` fixture needs a `following_runs` row first (`last_run_id` is a notNull FK), people-layer handles must be **≤15 chars** (longer ones are silently skipped and the assertions go vacuous), and any suite touching `scheduled_posts`/`posts_published` inside the monitor's windows must delete its rows in `afterAll` — `monitor.test.ts` asserts `clusterCount === baseline + 1` and a leaked pending pair breaks a different file. **(4) Five reads WRITE and none of them may be polled:** `/radar/drafts` (48h expiry), `/following/queue` (release + revoke), `/x/goals`, `/x/brief` and `/x/digest` (all three settle `active → achieved|missed`). A3.8's Today card and A3.14's week board both sit on `/x/brief` — a refresh loop there silently advances goal statuses.
- **GR.10: the smoke that closes Wave 3 is the template for smoking a surface whose READS write.** `scripts/smoke-guardrails.ts` ($0, no `--live`, rerunnable, verified to leave 0 rows) mounts following + monitor + goals + me + calendar + brief + digest over the real `./stratus.db`. **Copy its safety shape, not `smoke-passive-harvest.ts`'s, whenever a route mutates rows it did not create:** namespaced ids cover only what you wrote, so it *additionally* snapshots every foreign `following.status` before the first write and restores it from the success path **and** from `fail()` (a complete-run reconcile marks every unseen live handle `gone`; a queue read releases rows), snapshot-restores `commitments` and the seeded `streaks`/`digests` keys rather than deleting them, and **closes with one more `GET /x/brief`** so today's C9 streak row is recomputed from clean data instead of describing the burst fixtures it just deleted. **Three things it discovered that no plan text says:** (a) reconcile "unseen" is per **run** (`last_run_id`), not per batch — proving `gone`/`confirmed` needs TWO runs; (b) the queue needs ≥3 slots of budget for its assertions, so the script fails loudly naming `windowUsed/windowCap` + `dailyUsed/dailyCeiling` rather than through a confusing count mismatch; (c) `?factsOnly=true` returns **before** the digest cache write, so a scorecard read caches nothing — but it still settles goals. Assertions over shared/real data are **baseline-relative** where they must be (`nearDuplicate.pairCount`, `scheduleCluster.clusterCount`) and exact where the fixture owns the whole population (`questAdherence`, the queue batch, the ledger statuses).

- **Wave-4 carry-forward — ARCHIVED AT JD.3** (verbatim in `STATE-ARCHIVE.md`; grep by task id A3.x). Scoped to "an open **Wave-5** task" and Wave 5 closed at UI.17; the one fact a JD task still meets — the spoken-for calendar fixture day-bands — is in the **JD.1** gotcha below. **Read the archived block before touching articles, DM drafts, the calendar, or `mark-posted`.**

- **Wave-5 registry carry-forward — ARCHIVED AT JD.2** (all ten items verbatim in `STATE-ARCHIVE.md`; grep by task id UI.2–UI.7 or by `registry.ts`/`serverSettings.ts`). **Nine of the ten are about adding, patching or mirroring a settings knob, and no JD task does any of those.** The residue that could still bite is stated closer to its use: a route whose success path SPENDS is tested from the refusal side (GT.9 gotcha — assert how far the request gets, `503 llm_not_configured`), a `?param=` overrides the configured baseline **per read, in both directions** (JD.7's Playbook cell — and the SC.5 lock row already requires those tests to be partition invariants or before/after deltas), and Bun auto-loads `.env` so `env -u API_TOKEN` does NOT unset it (JD.8's smoke). **Read the archived block before adding a knob, a mirrored key, or any settings-touching test.**

- **UI.17 — three smoke-writing facts that outlived Wave 5** (the other two — "a closing docs-sync is mostly a VERIFICATION pass, scoped in one command by `git log --name-only -- docs/`" and "assert the plan's BAR, not today's number" — are restated in D160(b), D162(b) and the SC.9 gotcha; the full block is in `STATE-ARCHIVE.md`). **(1) A monotone assertion plus a printed denominator beats a fixture, when the fixture would drag invariants along:** `sufficient` counts across three gates prove the gate is applied without seeding a single row, and printing "0 of 21 gated cells" is what keeps a zero honest. Reach for this whenever the thing under test is a *parameter* and the data belongs to someone else's smoke. **(2) A knob whose loosening direction spends can still be smoked safely from the tightening side** — `x.budgets.imageDailyUsd: 0` makes `spent >= budget` true at $0 spend, so the 429 fires with no `cost_events` fixture at all; biome forbids `delete process.env.X` in `scripts/`, so a key restore assigns `?? ''`. **(3) `bun run <script>` buffers stdout when it is not a TTY.** A run that hangs writes an EMPTY output file, so "no output" tells you nothing about where it stopped — `console.error` (or `2>&1 | head`) is how you bisect a stuck script here. Cost twenty minutes on a hang that turned out to be transient.


- **JD.1 — the DB-fixture shape a shared in-memory suite needs** (full block in `STATE-ARCHIVE.md`, grep JD.1; its prompt half was consumed by JD.3): `drafter.test.ts` is the exemplar — seed **400 days back** (clear of the brief/digest/monitor windows and of the spoken-for calendar bands), view counts ~9M so the rows outrank every other suite's deterministically, **`retired: true` on every `posts_published` row** (NT.7 — an un-retired row joins the *billed* daily pass), and teardown in a **`try/finally` inside the test** as well as `afterAll`, because files share one in-memory DB and `afterAll` alone leaves your rows visible to every later test in the same file. Where a foreign row could take a freed slot, filter the assertion to your prefix instead of comparing whole arrays.

- **GT.9 — facts for SC.9/JD.8 (the next two closing smokes) and anyone writing a smoke that must not spend:** **the strongest $0 assertion for a gate is how FAR a request gets, not what comes back.** `smoke-growth-tactics.ts` force-unsets both LLM keys in a `finally` and asserts **503 `llm_not_configured`** — reaching the LLM layer IS the proof the gate opened, and it is unspendable by construction rather than by remembering not to pass `--live`. It is why the script has no `--live` flag at all: the only paid thing in reach is the call it exists to prove was not made. Copy that shape for any surface whose success path costs money. **Second: assert the QUALIFIER, not the count.** The reciprocity step's load-bearing assertion is not `n === 1` — it is that re-stamping the same person's `stageUpdatedAt` to *now* makes the same posted reply stop counting. A count over a shared real DB is baseline-relative noise; the qualifier is the feature, and it is the one thing a fixture written by someone who already believes the rule would never have checked. **Fixture facts:** handles are `gt9*` (≤15 chars or `normalizePersonHandle` drops them silently and every assertion goes vacuous), tweet ids 887-prefixed; a `people` row needs only `handle`+`stage`; the milestone step is PURE and deliberately seeds no `account_snapshots` (that table is contested by four suites and `smoke-pinned-watch.ts` refuses the real DB over it); and `/x/brief` WRITES — it settles goals and rewrites today's C9 diary — so the run closes on a clean brief read asserting the baseline came back (the GR.10 rule). **Local `./stratus.db` is nearly empty** (prod lives on Hetzner), so the reciprocity set reads as just the script's own seeds; that is the fresh-install path and worth seeing, but it means the smoke proves wiring, never calibration — calibration is the prod `POST /x/data/query` recipe in the archived GT.6/GT.7 gotchas. **Docs discipline this pass established:** a prompt-only task has no tab doc, so it silently exits the per-task docs rule and ships invisible — GT.1 and GT.2 both did. Write the user-facing paragraph in the prompt task's own commit; `replies-tab.md` §"Generating and the three variants" and `composer-tab.md` §"Generating and picking a draft" are where prompt behaviour is explained now.

- **JD.2 — the facts JD.3 did not consume** (full block in `STATE-ARCHIVE.md`, grep JD.2): **a shim with no importer is a real deliverable.** `extension/src/judge.ts` + its `tsconfig.app.json` `include` entry (now **six**) are typechecked from JD.2 but bundled by nothing until JD.6 — zero bytes, and omitting the `include` entry fails `cd extension && bun run typecheck` with a confusing rootDir error. **The list-plus-interface drift check** (`JUDGE_DIMENSIONS` vs the `JudgeScores` interface: sorted-`Object.keys` equality over a populated fixture, because TS checks one direction only) is now asserted in BOTH judge suites — reuse the shape for any such pair. The two red-gate traps it cost are restated in the JD.3 block below.
- **JD.3 — facts for JD.4/JD.5/JD.8 and anyone parsing a PAID response:** **(1) The route inherits a parser that is strict on 13 fields and lenient on six.** `parseVerdict` returns `null` only for an unparseable body or a bad `scores` object — so JD.4's 502 `judge_parse_error` fires on *those* cases and nothing else; a verdict with an empty `headline`, `confidence: null` and zero annotations is a SUCCESS, not a degraded one, and the route must not add a second refusal on top. **(2) The `1e999` recipe:** a non-finite guard is untestable through `JSON.stringify` (it emits `null` for `Infinity`) but perfectly testable through the raw text `JSON.parse` receives — build the fixture with a string `.replace`, which is also closer to what the route actually gets. **(3) Assert a hard-wrapped template through `normalizeJudgeText`,** never against the raw literal: my first contract assertion went red purely because "an exact substring of the draft" straddled a source line break. **(4) Two biome traps this file hit:** `delete obj.staticKey` is an error (`noDelete`) while `delete obj[computed]` is not — use rest-destructuring (`const {audienceMatch: _omitted, ...absent} = scores()`) when you need a key gone; and a bare `1e999` literal is `noPrecisionLoss`. **(5) `judgeTextHash('a  b') === judgeTextHash('a b')` by design** — whitespace-only edits do NOT invalidate a verdict, which is the intended reading of the reset-on-edit rule (the manualReconcile/harvest text-match convention) and is what makes the JD.7 join work against pasted-from-X text. **(6) The out-of-band check is still worth ten seconds** (the GT.1 recipe): `bun -e` printing `PROMPT_KEYS.length`, the rendered prompt length and a `JSON.stringify` round-trip of the schema confirmed 14 / 4347 chars / serializable before commit — the schema is built at module load from a `.map`, so "it compiles" is not the same claim as "it serializes".
- **SC carry-forward (the static-coach facts that still bind an open JD task — the 8 per-task gotchas + D145/D146/D152–D156/D161 are in `STATE-ARCHIVE.md` §"Archived at SC.9", grep by task id or filename):** **(1) The coach band is a CONSTANT, not a weak feature** — 97% `top` over the prod corpus, `rework`/`almost` at zero, and the only `fix` rules that fire are `url_cost` and `answerable_question` (D154a). **JD.7 must not key a judge cell on it**; split on a fix/flag count or report both sides. **(2) Cut points live in ONE place.** `scoreDraft`'s `toBand` owns `<45/45–59/60–84/85+`; re-deriving them in a cell or a panel grades a different number than the Composer showed the moment either side moves (the UI.7 badge-vs-gate fork). Same for the tone maps — import from `sidepanel/CoachChip.tsx`, never re-declare. **(3) The exempt three (`substance`/`one_liner`/`other`) are ONE set with one meaning** — *no format detected* — shared by the cooldown and the reach band, and a consumer never keeps its own copy: cells ship `exempt: true` with a pinned safe status. Anything new on the format axis inherits that or it puts a number on "unknown" (§7.11). **(4) `latestOutcomes` is the house pattern and it is a SELECTED sample** wherever the re-read is selective — 25 of 135 measured originals carry a second snapshot and those are the winner re-reads (10× the views at their first reading). `routes/coach.ts::loadReachRows` is the one loader that inverts it, with a comment; codemap §7 rule **34**. **(5) Panel surfaces here have no test file** (repo convention) — coverage is root+extension typecheck, `cd extension && bun run build`, lint, the pure/route suites, and **bundle greps** (`dist/sidepanel.js` for a copy string + a tone class, `dist/assets/sidepanel-*.css` for the class), which is the only check that proves Vite inlined an out-of-tree shim. `CoachChip` is a **hook-using** component (it loads the lexicon itself) and can never be called as a plain function. **(6) The four $0 verification recipes this lane invented, all reusable by JD:** push a fixture through the SHIM rather than the canonical module; pull the prod corpus with `GET /x/data/posts_published?limit=200&offset=N` (S1 explorer) and run a new `build*` locally at `minN=1`; **replay a rolling window over that corpus** to tell a useful counter from a permanently-lit one; and a single `POST /x/data/query` selecting each row's FIRST and LATEST snapshot to see whether the two cohorts differ before trusting any "latest snapshot" aggregate. **(7) Local `./stratus.db` has ZERO published originals** (prod lives on Hetzner), so every coach route returns the empty-corpus path locally — worth seeing (it is what a fresh install shows), but it means a local run proves wiring, never calibration.

- **SC.9 — facts for JD.8 (the next and last closing task) and anyone writing a smoke over a shared corpus:** **the assertion shapes are ranked, and the ranking is the whole craft of this file** — invariant > delta > the plan's bar > (in a *pure* step only) an exact number. Concretely: `exempt ⇒ clear` and "no insufficient cell carries a number" hold on any corpus including an empty one; cooldown counts and `measuredPosts` are deltas against baselines read **before the first seed**; the gate is proven at `minN=1000`, which no account can ever redden; and the exact `[40,120]` stall range lives in a synthetic-corpus step. **The padding trick that makes an exact quantile assertable:** pad so 15+ of every 20-row trailing window sit at one value — the base is then pinned by construction and a row's ratio is `views / base` — and make the padding an **exempt** format so it can never mint a competing fitted cell. **Fixture facts:** ids are 889-prefixed; every seeded `posts_published` row is `retired: true` (NT.7 — an un-retired row joins the *billed* daily pass, which is the one way this $0 script could cost money); the cooldown/Playbook fixtures deliberately carry snapshots with a **null** `ageAtSnapshotMin` so they stay out of the reach population and its delta stays exactly the two rows it is about; the lexicon step inserts a channel with `niche: null` so it is in scope under whichever niche is active. **Nothing mounted here is a read-that-writes** (unlike `/x/brief` in GT.9's), so the closing re-read is a *cleanup proof*, not a repair. **Docs facts:** `git log --name-only -- docs/ evals/` scopes a closing docs pass in one command (D160b); what it cannot tell you is the piece **no tab owns** — for a lane spanning four tabs that cross-cutting doc IS the owed work, and for a lane whose thesis is "a number is a measurement or it is absent" it has to state the refusals or they read as missing features. Its home is `docs/sc-static-coach.md`, linked from the README term list plus the Composer and Playbook sections.


## Planning-error log

Corrections to MASTERPLAN.md itself (wrong dep, wrong order discovered live).

- **2026-07-16-niche.md Task 3 done-when** — the "`Pitești` hits nothing" grep contradicts the same task's bit-for-bit equivalence requirement (post prompt §6/§9/§0 carry builder biography outside the §1/§5 extraction scope; no niche field exists to hold them). Resolved at N.3 in favor of equivalence; see D28b for the full resolution + what N.9 must scope differently. Source plans are never edited — this entry is the correction.
- **2026-07-17-ai-layer.md — no task owns the post drafter's askLLM migration.** Task 5's edit list names replies/pillars/playbook/voiceExtract; Task 6 only flips drafter's MOUNT gate; Task 7 adds a sibling route. Yet the plan's Why-section + Done-when #1 spirit require every draft surface to honor the provider setting, and the Task-6 gate flip would 502 `/posts/draft` on OpenRouter-only setups. Resolved at AI.5 by migrating `drafter.ts` there (D47b) — the call-site-migration task was the right home.

