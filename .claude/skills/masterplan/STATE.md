# Masterplan execution state

> Dynamic memory for `/masterplan`. Updated after EVERY task, in the task's own commit.
> Plan: `plans/MASTERPLAN.md` (static — order, reasoning levels, waves, D1–D10).
> Codemap: `.claude/skills/plan-feature/references/codemap.md` (updated per task too).
> History: `.claude/skills/masterplan/STATE-ARCHIVE.md` (frozen, grep-only — closed-wave deviations + gotchas).

**Size discipline (added 2026-07-23 at 339 KB; re-applied at GR.10, 128 KB → ~47 KB):** STATE.md must stay loadable in a single `Read`. What lives here is (a) standing rules, (b) entries for the OPEN lane / current wave, (c) unpaid verification debt, (d) the ledger + locks. When a sub-plan closes, its D-entries and gotchas move verbatim to `STATE-ARCHIVE.md` — that move is part of the sub-plan's docs-sync task, not a separate chore.

- **last-commit:** **identity is the SUBJECT LINE — no sha is recorded any more (D97).** HEAD should read `feat(settings): doctrine + quest knobs read from registry (UI.2)`, whose parent is `859a923` = A3.15 `docs(authoring): Authoring 3.0 docs-sync + $0 smoke-authoring3.ts (A3.15)`, whose parent is `819c61d` = A3.14 `feat(extension): calendar week board with blended slot heat + drafts tray (A3.14)`, whose parent is `8a1b7e4` = A3.13 `feat(writer): standalone article writing room at /writer (A3.13)`, whose parent is `0ca596a` = A3.12 `feat(articles): byte-synced article prompt + assist route (outline/section/polish/full) (A3.12)`, whose parent is `cc7704f` = A3.11 `feat(articles): articles table + CRUD routes (A3.11)`, whose parent is `1aabbc5` = A3.10 `feat(extension): DM drafting from dossier and Do-next (A3.10)`, whose parent is `bd8ab7d` = A3.9 `feat(dms): dm_drafts + grounded draft/list/patch routes (C-layer outbound) (A3.9)`, whose parent is `e058e98` = A3.8 `feat(extension): manual-post alarms, notification and Today publish card (A3.8)`, whose parent is `49a05e7` = A3.7 `feat(extension): manual publish mode in Composer + calendar/today chips (A3.7)`, whose parent is `03a3b7a` = A3.6 `feat(metrics): manual-post reconcile links pasted tweets to calendar rows (A3.6)`, whose parent is `1053f5d` = A3.5 `feat(calendar): manual publish status + mark-posted transition (A3.5)`, whose parent is `8d499fb` = A3.4 `feat(composer): audience-blended best-slot ranking + staleness note (A3.4)`, whose parent is `5bb88e2` = A3.3 `feat(extension): passive active-times capture on X Analytics (A3.3)`, whose parent is `cf2f312` = A3.2 `feat(analytics): audience_activity capture table + /x/analytics/active-times routes (A3.2)`, whose parent is `d4869ab` = A3.1 `feat(extension): active-times heatmap parser (shared, fixture-tested) (A3.1)`, whose parent is `49fcd1d` **`wave 3 done`** — an out-of-skill user commit (post-GR.10 fixup: `POST /me/goals` gained `deadline_in_past` + `target_not_above_baseline` 400 guards, +goals-route tests, smoke-me/smoke-guardrails tweaks, one docs/me-tab.md line; no ledger drift — reconciled at A3.1's Step 0). Behind it: `0e891b0` = GR.10 `docs(guardrails): GR docs-sync + $0 smoke-guardrails.ts (GR.10)`, whose parent is `7adfbca` **`feat(goals): weekly scorecard in digest facts + card (GR.9)`**. Behind that, in order: `b59a564` **`ideas`** (an out-of-skill commit adding ONLY `crazy_ideas.md` — no code, no ledger drift), GR.8, GR.7, GR.6, GR.5, GR.4, `55c6d19` **`chore(masterplan): compact STATE.md into a frozen STATE-ARCHIVE.md`** (out-of-skill housekeeping), then the two out-of-masterplan **CA** commits (plan `plans/2026-07-23-cost-audit-followups.md`, executed 2026-07-23 at explicit user direction and deployed the same day), whose parent is `30b0583` (= GR.3). **Step 0 is one command:** `git log -1 --format='%h %s'` — if the subject names the last ticked task (or one of the two out-of-skill lines above), there was no out-of-skill work.
- **current wave:** **WAVE 4 (Authoring 3.0) IS CLOSED — A3.1–A3.15 ✓** (A3.15 = docs-sync + `scripts/smoke-authoring3.ts`). Waves 0–4 all closed (RU/AX/RL/NT/HV/GR/A3). **WAVE 5 (Settings moat + polish) is OPEN — UI.2 ✓ (the first Wave-5 task); UI.3–UI.7, UI.11–UI.17 remain `[ ]`.** No locked files anywhere. **The migration journal is FREE; the next migrating task owns `0024`** (never run two migrating tasks in parallel — journal conflicts). **The D9 `content.ts` chain is COMPLETE** [AX.3✓→…→A3.3✓]; UI.16's token polish is the only future claimant. **D5 FULLY DISCHARGED** (dm + article registered; no open task binds it). **D2 RESOLVED at UI.2 (Option B)** — the 3 niche-owned band keys `x.doctrine.{replyTargetMin,replyTargetMax,weekReplyTargetPct}` were DROPPED from `app_settings` (the niche is the sole owner via `loadDoctrine`); the surviving `doctrine` group = the cadence ladder (`anchors3/anchors4/ladderSwitchAt`), plus the NEW `quests`+`display` groups, now drive `routes/brief.ts`. See **D128**. **D93a standing for UI.17:** phase entries → `docs/PHASE-HISTORY.md`, never CLAUDE.md. **The Wave-4 forward-binders for Wave 5 are consolidated in the Wave-4 carry-forward gotcha below** (browser-verify debt + which UI.12–UI.16 task owns each A3 surface + docs re-reads).
- **next-up:** **UI.3** (People/followups/stage/targets/pinned-watch knobs — high, dep UI.1✓) is the next `[ ]` Wave-5 ledger entry; UI.3/UI.4 are parallel-ok, UI.5 is xhigh (money/AI hard ceilings). Confirm wave order in `plans/MASTERPLAN.md`. **The UI.1 store pattern is now proven by UI.2** (D128): pure modules take an opts object defaulted to today's constant; only routes/workers read via the bound `getSetting` from `../settings/registry.ts` (never the store); a knob whose real owner is elsewhere (a niche field) is DROPPED, never duplicated. UI.3 reads `people/followups.ts` / `people/stage.ts` / `voice.ts` (targetBand) + `brief.ts` pinned-watch constants — see the plan's Task-3 Read-first. **One pending CA.2 verification rides ahead and is NOT a masterplan task:** grep the prod journal for `[cost-audit]` — if `includes.tweets` ≈ the console-vs-`/cost/today` gap, do CA.2 step 2 (drop `referenced_tweets.id` from the metrics-read expansions in `fields.ts`) + a `docs/PHASE-HISTORY.md` one-liner; else write the refute one-liner and remove the instrumentation. **Gate reminders:** `bun run test` (bare `bun test` targets the file DB); biome forbids `delete process.env.X`/`: any` in `scripts/`, sorts imports case-sensitively, collapses short chained calls, and rejects a `console.log` string-concat unless every operand interpolates (run `bunx biome check --write`, `--fix --unsafe` for the string rules). Extension-touching tasks add `cd extension && bun run build` **and** `typecheck`.
- **prior (UI.2 — xhigh, dep UI.1✓+N.5✓, D2-bound, $0, server-only, no schema/migration/extension/MCP change, 7 files; suite 1469→1477 [+8] — first Wave-5 task):** the first real consumers of the UI.1 settings store + the D2 ownership call. **D2 resolved Option B:** DROPPED the 3 niche-owned band keys `x.doctrine.{replyTargetMin,replyTargetMax,weekReplyTargetPct}` UI.1 had seeded (the niche owns them via `loadDoctrine`), so each registry knob has exactly one owner; kept only the cadence ladder (`anchors3/anchors4/ladderSwitchAt`) in `doctrine`, added `quests` (`originalsTarget`/`neglectedTargetsCount`/`neglectedTargetDays`/`launchAttendWindowMin`) + `display` (`sparklineDays`/`leaderCount`) groups, all `server` scope (no `x.quests.replyQuestTarget` — the reply quest derives from the niche band). `quests.ts` stays pure (Decision 6): `computeQuests(i, opts=QUEST_DEFAULTS)`, `neglectedTargetsAtDayStart(…, neglectedDays=)`, `launchesAttended(…, windowMs=)` — every param defaulted to its old constant; `originalsTarget:0` ⇒ vacuously done + note. `routes/brief.ts` reads all six via `getSetting` at request time (`pickAnchors(n, cfg)` gaps/ladder, sparkline query, leader slice, quest helpers); the reply band/ratio stay on `loadDoctrine`. Also updated UI.1's `registry.test.ts` + `settings.test.ts` off the dropped keys (NOT in the plan edit-list — required to keep gates green). See **D128**.
- **prior (A3.15 — high, dep all A3, $0 default [+~$0.02 `--live`], docs+smoke only, no code/schema/route/MCP/test change; suite stays 1469 — CLOSES Wave 4):** NEW `scripts/smoke-authoring3.ts` mounts analytics+calendar+dms+articles over the real DB and drives all four surfaces at **$0** (every DM/article assertion sits on the pre-spend refusal ladder, so it never spends even with a Grok key — the 503 is the ONLY key-gated line): active-times POST/GET + a validation reject; the manual lifecycle (URL text accepted, the pending-only publisher due-select **replayed read-only** [`claimOne` unexported] skips it, mark-posted→posted with NO tweet id, then the reconcile linking a synthetic `retired:true` original via the exported `matchManualRows` + a **replay of the wrapper's atomic stamp** — the private `reconcileManualPosts` runs only in the PAID daily pass, D127); the DM 404→422 ladder + PATCH-sent `manual_dm_logged` idempotency (directly-inserted draft); articles CRUD + the assist ladder (400/404/409, 503 only when Grok off). Namespace-then-delete cleanup (D98c — no read-that-writes here). `--live` = ONE Romanian-idea assist → English (no diacritics) + a `grok` cost row (verified $0.0057 on grok-4.3). Docs: NEW `docs/writer.md`, rewritten `docs/calendar-tab.md` (week board + manual), `docs/composer-tab.md`, `docs/today-tab.md` (§2 Manual post reminder inserted, §1–§18), `docs/people-tab.md`, `docs/settings-tab.md`; the **Authoring 3.0** named entry in `docs/PHASE-HISTORY.md` (D127 — NOT "Phase 10"), a PLAN.md blockquote, a CIRCLES-PLAN.md C5 cross-ref; codemap §8/§9 + header re-stamp. **The Wave-4 D-entries D114–D126 + the A3 header priors + the A3 gotchas were moved verbatim to `STATE-ARCHIVE.md` (§Wave 4) at this task** (the size-discipline archive rule). See **D127**.
- **prior (GR.10 — closed the GR sub-plan + Wave 3):** docs-sync + `scripts/smoke-guardrails.ts` (the template for smoking a surface whose READS write — snapshot/restore what you can't namespace). Full detail in codemap §9 + the **Wave-3 carry-forward** gotcha below.
- **prior (CA.1 + CA.2 step 1, 2026-07-23, out-of-masterplan, $0):** `pullMentions` now pages to `MENTIONS_HARD_CAP=800` (killed the silent 50/day inbox loss since Jul 14); `xFetch` logs `[cost-audit]` on `includes`-carrying reads. **CA.2 step 2 is still unpaid — see the next-up verification.** Detail in `docs/PHASE-HISTORY.md`.
- **earlier priors (A3.14 back through HV.1):** moved verbatim to `STATE-ARCHIVE.md` (A3.1–A3.14 archived at A3.15; GR.9→HV.1 at GR.10). Their forward-binding content survives here as the carry-forward gotchas below.

## Ledger

Status: `[ ]` todo · `[~]` in progress (lane claimed) · `[x]` done (sha + date) · `[s]` skipped (reason in deviations).

### Wave 0 — Foundations
- [x] UI.1 Settings platform (xhigh) — 226368c (code+format+state+codemap; D12 absorbed into 61a04e7) 2026-07-18
- [x] UI.8 Design tokens dark refactor (high) — 1299e43 (styles.css in 61a04e7 per D12) 2026-07-18
- [x] UI.9 Light theme + Appearance (high) — f9ff346 2026-07-18 (sha corrected from orphaned 8c89950)
- [x] UI.10 UI primitives + grouped rail (xhigh) — c37d504 2026-07-18 (sha corrected from orphaned 50948c9 — see D17)
- [x] ST.1 Engine layers + PRNG (high) — 61a04e7 2026-07-18
- [x] ST.2 Studio shell refactor (high) — dffd60a 2026-07-18 (sha corrected from orphaned 860725f — see D16)
- [x] ST.3 Cloud mascot (high) — b82058b 2026-07-18 (sha corrected from orphaned 292603d — see D17)
- [x] ST.4 Patterns + presets (high) — 15f2563 2026-07-18 (sha corrected from orphaned c1b0314 — see D18)
- [x] ST.5 Milestone + streak cards (high) — 1f6b20f 2026-07-18 (sha corrected from orphaned bc3f1e8 — see D19)
- [x] ST.6 Code card + mono font (high) — 00b9615 2026-07-18 (sha corrected from orphaned b65266f — see D19/D20)
- [x] ST.7 Thread cover + list card (high) — acd95ec 2026-07-18 (sha corrected from orphaned 2104f46 — see D21)
- [x] ST.8 Chart card (high) — 289ec2c 2026-07-18 (sha corrected from orphaned d38bbc5 — see D22)
- [x] ST.9 Studio docs+smoke (high) — d1902e5 2026-07-18 (sha corrected from orphaned 47b10d2 — see D23)

### Wave 1 — Prompt & identity core
- [x] RU.1 Reply prompt → 3 variants (xhigh) — 63df913 2026-07-18 (sha corrected from orphaned 9c05464 — see D24)
- [x] RU.2 Migration: source/variants/model columns (high) — ddecb5f 2026-07-18
- [x] RU.3 Batch → 3 variants (xhigh) — d8ecc9a 2026-07-18
- [x] N.1 Niche schema + defaults + store (xhigh) — 0bbca12 2026-07-18 (sha corrected from orphaned da97495 — see D27)
- [x] N.2 Niche CRUD + activation + MCP (high) — c76a29b 2026-07-18 (sha corrected from orphaned 4312a0e — see D28)
- [x] N.3 Post prompt persona extraction (MAX) — 3b1547c 2026-07-18 (sha corrected from orphaned b06770c — see D29)
- [x] N.4 Reply prompt persona extraction (MAX) — 0668451 2026-07-18
- [x] N.5 Doctrine knobs threaded (high) — 7e9213d 2026-07-18 (sha corrected from orphaned 20faa44 — see D31)
- [x] N.6 Pillar/channel niche ownership (xhigh) — 730a009 2026-07-18
- [x] N.7 Settings Niche card (high) — 25a8c99 2026-07-18
- [x] N.8 Niche wizard (high) — 5c1031d 2026-07-18 (sha corrected from orphaned 527ad21 — see D35)
- [x] N.9 Niche docs+smoke (high) — 78d1d93 2026-07-18
- [x] ME.1 me_entries/me_goals + profile module (high) — `0545185` 2026-07-18 (sha corrected from orphaned f0c91f4 — see D37; N.9 sha reconciled e452f7c — see D36)
- [x] ME.2 /x/me routes + loader + smoke (high) — `43e6bc9` 2026-07-18 (sha corrected from orphaned a3a94bb — see D38)
- [x] ME.3 Inject me-context at prompt tails (xhigh) — `82f0291` 2026-07-18
- [x] ME.4 Me tab (high) — `5a4b427` 2026-07-18 (sha corrected from orphaned 92499ab — see D40)
- [x] ME.5 Playbook lift + digest goals fact (high) — `9df22ff` 2026-07-18 (sha corrected from orphaned d611dce — see D41)
- [x] ME.6 MCP x_me + x_add_me_entry tools (high) — `d4ef3dc` 2026-07-18 (sha corrected from orphaned 8686d79 — see D42)
- [x] ME.7 Me docs-sync (high) — `f010bf3` 2026-07-18 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.1 OpenRouter client (xhigh) — `fc5b3c4` 2026-07-18 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.2 askLLM + /llm routes — D1 (xhigh) — `3d3598c` 2026-07-18 (sha corrected from orphaned 87f3bd7 — see D45a)
- [x] AI.3 Prompt registry + overrides (MAX) — `a087a66` 2026-07-18 (sha corrected from orphaned 9f33fb2 — see D45b)
- [x] AI.4 /x/prompts routes (high) — `3c649ce` 2026-07-18 (sha corrected from orphaned 466b090 — see D47a)
- [x] AI.5 Batch/extract/pillar-draft via registry — D3 (MAX) — `7d0a786` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.6 Digest/icebreaker via registry + LLM gate (xhigh) — `e2c91ac` 2026-07-19 (sha corrected from orphaned b82f4ef — see D49)
- [x] AI.7 Thread drafter (xhigh) — `b932a06` 2026-07-19 (sha corrected from orphaned 995a08b — see D50)
- [x] AI.8 Rewrite assist (high) — `b9fe98d` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.9 Idea generator (high) — `8e53621` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.10 Settings AI subtab (high) — `a91387f` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.11 Prompts editor panel (high) — `e7d5be9` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.12 Model-effectiveness cell (high) — `9c59183` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.13 AI docs+smoke (high) — `f86e06a` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles) — **closes Wave 1**

### Wave 2 — Reply machine & on-page surfaces
- [x] RU.4 Buffer/panel variants (high) — `22cf0b1` 2026-07-20 (sha corrected from orphaned a3664af — see D58)
- [x] RU.5 Confirm endpoint (xhigh) — `5935107` 2026-07-20 (sha corrected to `983fbea` — see D59; recorded sha was an amend-orphan)
- [x] RU.6 Panel confirm wiring (high) — `1ed57bd` 2026-07-20 (sha corrected from orphaned 6261673 — see D60)
- [x] AX.1 Glance endpoint (high) — `9467220` 2026-07-20 (sha corrected from orphaned f1d85c2 — see D61)
- [x] AX.2 Glance chip view-model (high) — `e29b629` 2026-07-20 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AX.4 tweetContext view-model (high) — `7ecb8df` 2026-07-21 (sha corrected from orphaned 7abec86 — see D64)
- [x] AX.3 Timeline chips, pill removal (xhigh) — `39fecb6` 2026-07-21 (sha corrected from orphaned bc2c9fe — see D65)
- [x] AX.5 Status-page context panel (xhigh) — `5e31b8d` 2026-07-21 (sha corrected from orphaned 0eb5319 — see D66)
- [x] AX.6 Dossier click-through (high) — `b3fd7b3` 2026-07-21 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] RU.7 On-page variant chips (xhigh) — `f90084b` 2026-07-21 (sha corrected from orphaned bfa2441 — see D69)
- [x] RU.8 Manual add ⊕ (xhigh) — `9ba6fba` 2026-07-21 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] RU.9 Playbook source attribution (high) — `f78b48c` 2026-07-21 (sha corrected from orphaned 899e126 — see D72)
- [x] RU.10 RU docs+smoke (high) — `22099a3` 2026-07-21 (sha stamped post-commit; amend re-orphans it, next session reconciles) — **closes the RU sub-plan**
- [x] AX.7 AX docs+smoke (high) — `00ec8a8` 2026-07-21 (sha stamped post-commit; amend re-orphans it, next session reconciles) — **closes the AX sub-plan (AX.1–AX.7)**
- [x] RL.1 Reply-list engine (xhigh) — `ffbdb3f` 2026-07-22 (sha corrected from orphaned 58903fc — see D77)
- [x] RL.2 Schema + CRUD (high) — `9c0d97b` 2026-07-22 (sha corrected from orphaned a29ffbe — see D79a)
- [x] RL.3 /use route (high) — `c3f44d2` 2026-07-23 (sha corrected from orphaned 0ea02a3 — see D80a)
- [x] RL.4 AI generator — D5 (high) — `99eef6b` 2026-07-23 (sha stamped post-commit; amend re-orphans it, next session reconciles by subject)
- [x] RL.5 Lists subtab UI (high) — `17a530f` 2026-07-23 (sha stamped post-commit; amend re-orphans it, next session reconciles by subject)
- [x] RL.6 QuickReplyPicker (high) — `aae4665` 2026-07-23 (sha stamped post-commit; amend re-orphans it, next session reconciles by subject)
- [x] RL.7 Playbook canned bucket (high) — `3a4ae56` 2026-07-23 (sha corrected from orphaned b2543e7 — see D84a)
- [x] RL.8 RL docs+smoke (high) — `a120207` 2026-07-23 (sha corrected from orphaned 7ac1b5e — see D85c) — **closes the RL sub-plan (RL.1–RL.8)**
- [x] NT.1 Notification parser (high) — `2063b86` 2026-07-23 (sha corrected from orphaned 7f3d2fd — see D86a)
- [x] NT.2 Engagement events + ingest (xhigh) — `4e4d9fc` 2026-07-23
- [x] NT.3 Engagements route (high) — `2b763b1` 2026-07-23 (sha stamped post-commit; the amend re-orphans it, next session reconciles by subject)
- [x] NT.4 Background notif-context (high) — `fe0f6aa` 2026-07-23
- [x] NT.5 Notifications content script (xhigh) — `69784da` 2026-07-23 (sha corrected from orphaned 3a94830 — see D90a; NT.4 sha reconciled to `68039f2` — see D89a)
- [x] NT.6 Fans engagement count (high) — `0c0ab5d` 2026-07-23 (sha corrected from orphaned 532f87d — see D91a)
- [x] NT.7 NT docs+smoke (high) — `5d4a1a4` 2026-07-23 (sha corrected from orphaned 75f15e7 — see D93a) — **closes Wave 2**

### Wave 3 — Ambient data & guardrails
- [x] HV.1 Passive harvest endpoint (xhigh) — `5ea3495` 2026-07-23 (sha corrected from orphaned bf5e7b5 — see D93a)
- [x] HV.2 Passive capture + toggle (xhigh) — `661d039` 2026-07-23 (sha corrected from orphaned 090a8ac — see D94a)
- [x] HV.3 Harvest tab options (high) — `4c2adcf` 2026-07-23 (HV.2 sha reconciled to `661d039` — see D94a)
- [x] HV.4 Timeline affinity (high) — `3132f10` 2026-07-23 (HV.3 sha reconciled to `5cd316a` — see D95a)
- [x] HV.5 Funnel playbook cell (high) — subject `feat(playbook): timeline opportunity-capture funnel over passive harvest rows (HV.5)`, parent `855b424`, 2026-07-23 (no sha by design — D97)
- [x] HV.6 HV docs+smoke (high) — subject `docs(harvest): Harvest 2.0 docs-sync + $0 smoke-passive-harvest.ts (HV.6)`, parent `3416f14`, 2026-07-23 (no sha by design — D97). **Closes the HV sub-plan.**
- [x] GR.1 Following ledger (xhigh) — `feat(following): ledger schema + ingest/ratchet routes (GR.1)` 2026-07-23
- [x] GR.2 Harvester following mode (xhigh) — `feat(following): harvester following-mode scrape (GR.2)` 2026-07-23
- [x] GR.3 Curation queue (xhigh) — `feat(following): curation queue with jittered cadence caps (GR.3)` 2026-07-23
- [x] GR.4 Following subtab (high) — `feat(following): People tab Following subtab (GR.4)` 2026-07-23
- [x] GR.5 Monitor rules + route + MCP (xhigh) — `feat(monitor): activity-pattern rules + /x/monitor + MCP tool (GR.5)` 2026-07-23
- [x] GR.6 Monitor surfacing (high) — `feat(monitor): brief block, Today card, schedule-time warnings (GR.6)` 2026-07-23
- [x] GR.7 Goals + commitments — D4 (xhigh) — `feat(goals): goals + commitments over me_goals, pacing/debt/scorecard core (GR.7)` 2026-07-24
- [x] GR.8 Accountability surfacing (high) — `feat(goals): brief pacing + commitment-driven quests + Today Goals card (GR.8)` 2026-07-24
- [x] GR.9 Digest scorecard (high) — `feat(goals): weekly scorecard in digest facts + card (GR.9)` 2026-07-24
- [x] GR.10 GR docs+smoke (high) — `docs(guardrails): GR docs-sync + $0 smoke-guardrails.ts (GR.10)`, parent `7adfbca`, 2026-07-24. **Closes the GR sub-plan AND Wave 3.**

### Wave 4 — Authoring 3.0
- [x] A3.1 Active-times parser (xhigh) — subject `feat(extension): active-times heatmap parser (shared, fixture-tested) (A3.1)`, parent `49fcd1d`, 2026-07-24 (no sha by design — D97)
- [x] A3.2 audience_activity + routes (high) — subject `feat(analytics): audience_activity capture table + /x/analytics/active-times routes (A3.2)`, parent `d4869ab`, 2026-07-24 (no sha by design — D97)
- [x] A3.3 Analytics capture (xhigh) — subject `feat(extension): passive active-times capture on X Analytics (A3.3)`, parent `cf2f312`, 2026-07-24 (no sha by design — D97). **Completes the D9 chain.**
- [x] A3.4 Audience-blended slots (xhigh) — subject `feat(composer): audience-blended best-slot ranking + staleness note (A3.4)`, parent `5bb88e2`, 2026-07-24 (no sha by design — D97)
- [x] A3.5 Manual publish core (MAX) — subject `feat(calendar): manual publish status + mark-posted transition (A3.5)`, parent `8d499fb`, 2026-07-24 (no sha by design — D97)
- [x] A3.6 Manual reconcile (xhigh) — subject `feat(metrics): manual-post reconcile links pasted tweets to calendar rows (A3.6)`, parent `1053f5d`, 2026-07-24 (no sha by design — D97)
- [x] A3.7 Composer toggle + chips (high) — subject `feat(extension): manual publish mode in Composer + calendar/today chips (A3.7)`, parent `03a3b7a`, 2026-07-24 (no sha by design — D97)
- [x] A3.8 Manual alarms + Today card (xhigh) — subject `feat(extension): manual-post alarms, notification and Today publish card (A3.8)`, parent `49a05e7`, 2026-07-24 (no sha by design — D97)
- [x] A3.9 DM drafts — D5 (xhigh) — subject `feat(dms): dm_drafts + grounded draft/list/patch routes (C-layer outbound) (A3.9)`, parent `e058e98`, 2026-07-24 (no sha by design — D97)
- [x] A3.10 DM UI (high) — subject `feat(extension): DM drafting from dossier and Do-next (A3.10)`, parent `bd8ab7d`, 2026-07-24 (no sha by design — D97)
- [x] A3.11 Articles CRUD (high) — subject `feat(articles): articles table + CRUD routes (A3.11)`, parent `1aabbc5`, 2026-07-24 (no sha by design — D97). **The last Wave-4 migrating task; migration `0023_slimy_night_thrasher`. The journal is now FREE for Wave 5.**
- [x] A3.12 Article prompt + assist — D5 (xhigh) — subject `feat(articles): byte-synced article prompt + assist route (outline/section/polish/full) (A3.12)`, parent `cc7704f`, 2026-07-24 (no sha by design — D97). **Discharges the A3.12-half of D5 — the `article` registry key is registered (→ 13 keys).**
- [x] A3.13 Writer page (xhigh) — subject `feat(writer): standalone article writing room at /writer (A3.13)`, parent `0ca596a`, 2026-07-24 (no sha by design — D97)
- [x] A3.14 Calendar week board (xhigh) — subject `feat(extension): calendar week board with blended slot heat + drafts tray (A3.14)`, parent `8a1b7e4`, 2026-07-24 (no sha by design — D97)
- [x] A3.15 A3 docs+smoke (high) — subject `docs(authoring): Authoring 3.0 docs-sync + $0 smoke-authoring3.ts (A3.15)`, parent `819c61d`, 2026-07-24 (no sha by design — D97). **Closes the A3 sub-plan AND Wave 4.**

### Wave 5 — Settings moat + polish
- [x] UI.2 Doctrine/quests knobs — D2 (xhigh) — subject `feat(settings): doctrine + quest knobs read from registry (UI.2)`, parent `859a923`, 2026-07-24 (no sha by design — D97). **D2 resolved Option B — the 3 niche-owned band keys dropped from `app_settings`; see D128.**
- [ ] UI.3 People/followups knobs (high)
- [ ] UI.4 Gates/workers knobs + MCP (high)
- [ ] UI.5 Money/AI knobs + ceilings (xhigh)
- [ ] UI.6 Extension mirror (xhigh)
- [ ] UI.7 Band thresholds e2e (xhigh)
- [ ] UI.11 Settings tab rebuild — D6, D8 (xhigh)
- [ ] UI.12 Today polish (high)
- [ ] UI.13 Composer/Calendar polish (high)
- [ ] UI.14 People/Channels/Voice/Ideas polish (high)
- [ ] UI.15 Replies/Playbook/Harvest/Studio polish (high)
- [ ] UI.16 Content-script + explorer tokens (high)
- [ ] UI.17 UI docs+smoke (high)

## Hot-file locks

A lane claims a file before starting, releases on commit. `owner: —` = free.
**Everything is free — Wave 3 released the last of it.** Rows below are kept only where
the note binds a task still marked `[ ]`; a closed lane's file with nothing to say to
Wave 4/5 has been dropped (grep `STATE-ARCHIVE.md` for its lane if you need the history).

| File | Owner |
|---|---|
| `extension/src/content.ts` | — (**the D9 chain is COMPLETE at A3.3**; UI.16's token polish is the only future claimant. Both ambient-capture gate orders are perf contracts: passive harvest enabled → `/home` → `!isHarvestActive()` → one `findPermalink` → throttle → only THEN `extractArticle`; active-times pathname → `passiveCaptureEnabled` → done/in-flight flags → only THEN `extractActiveTimesSection`) |
| `extension/src/harvester.ts` | — (GR.2 released it; `runHarvest`'s ingest param is a `ship(profile, store, outcome)` step and the mode dispatch lives in `startRun` — **a fourth mode is one branch there**, plus `MODE_NOUN`/`isHarvestMode`/`targetPath`/`startRun` staying exhaustive over the 3-union) |
| `src/db/migrations/` journal (any migration task) | — (A3.11 took `0023_slimy_night_thrasher` (`articles`); next owns `0024`. **Wave 4 has NO migrating tasks left; the journal is free for Wave 5 — but never run two migrating tasks in parallel sessions (journal conflicts)**) |
| `src/x/playbook.ts` + `src/x/routes/playbook.ts` | — (HV.5 released them; the pure file's sections run to **§12** — the next `build*` is §13, and any plan text counting sections is off by one from AI.12 onward. UI.15 polishes the panel) |
| `src/x/routes/brief.ts` | — (A3.4 released it; the big `Promise.all` now ends with `loadCommitmentsWithDebt` + `loadGoalsWithPacing` + a $0 `audienceActivity` newest-row select, and the response carries `monitor` / `goals` / `commitments` plus each gap's `audienceScore`. `annotateGaps` takes an optional `audience` grid — own-score-first ordering is unchanged, audience is display-only. **A3.5:** the today select is `['pending','manual','posted','failed']` — a manual slot fills its anchor. **The goals loader WRITES**, so `GET /brief` settles goal statuses on every read — A3.8 and A3.14 both build on this payload; keep it out of any polling loop) |
| `extension/src/sidepanel/Today.tsx` | — (GR.8 released it; `AccountHealthCard` between `PinnedWatchCard` and `TodayPlan`, `GoalsCard` right after the quests block, `DebtLine` inside `QuestsSection`. **A3.8 added the manual-post card here (documented as §2 at A3.15); UI.12 polishes it** and owes `docs/today-tab.md` — now numbered §1–§18 — a re-read) |
| `src/x/routes/calendar.ts` | — (A3.5 released it; `manual` is in `STATUSES`/`WRITABLE_STATUSES`, `mark-posted` sits between PATCH and DELETE, and the URL guard is `pending`-only at both sites (decision 5 — don't "fix" it). **A3.7:** `scheduleWarnings` now fires for `pending` AND `manual` rows and counts both as neighbors (`inArray(['pending','manual'])`), still computed **after** the insert inside a try/catch. **PATCH still does NOT warn** — A3.14 did NOT touch this file (Task 14 is extension-only, D126d); the D105 PATCH-warn item is retired as an A3.14 follow-up and is now just an unclaimed server nicety (the board's ghost-click/→best-slot PATCH gets no schedule-cluster advisory, same as the Composer's edit-schedule). The URL guard stays the only content 4xx) |
| `extension/src/sidepanel/Composer.tsx` | — (GR.6 released it; a `warnings: string[]` state renders amber under the notice line and is set **after** `reset()` because reset clears it — **A3.7 kept that order** (the toggle's `setManualMode(false)` is inside `reset()`, warnings still set after). A3.7 added the API|Manual toggle + `manualMode` state + the module-level pure `nextEditStatus`; UI.13 polishes this file next) |
| `extension/src/sidepanel/Settings.tsx` | — (GR.8 released it; the General view renders `<CommitmentsCard/>` between `NicheCard` and the settings form — a separate `.panel` div, NOT inside the form, because the per-key Save is its own `PUT`. D6 still binds: UI.11 absorbs it) |
| `src/x/routes/replies.ts` + `src/x/replies/prompt.ts` | — |
| `src/x/routes/articles.ts` + `src/x/db/schema.ts::articles` | — (A3.11 built the CRUD + table; A3.12 added `POST /articles/:id/assist` to this SAME route file; **A3.13's Writer page CONSUMES both (shipped)** — it did NOT edit this file (a new `public/writer.html` + a `GET /writer` route on the `explorer` router in `data.ts`). D123 + D124 + D125) |
| `src/x/routes/data.ts` + `public/*.html` | — (A3.13 added `GET /writer` to the `explorer` Hono router [already `app.route('/', explorer)`] + `public/writer.html`; both public shells lazy `Bun.file`-cache. A rename/move of the `explorer` router moves BOTH pages; UI.16's content-script+explorer token polish is the only future claimant on `explorer.html`, and it should re-token `writer.html` too. D125) |
| `post prompt.md` / `reply prompt.md` / `thread prompt.md` / **`article prompt.md`** (+ TS literals) | — (**A3.12 added `article prompt.md`** ↔ `ARTICLE_PROMPT_TEMPLATE`, byte-synced + the `article` registry key via the RL.4 4-file move → 13 keys. All four .md↔literal pairs are byte-synced the same way; edit both together — D124) |
| `src/x/routes/harvest.ts` | — (HV.4 released it; POST runs/rows/passive + GET runs/affinity, still **no `:param` route** — keep it that way. `scripts/smoke-passive-harvest.ts` mounts it over the real DB, so a route rename breaks the smoke as well as the suite) |
| `extension/src/shared/harvest.ts` + `sidepanel/Harvest.tsx` | — (GR.2 released them; nothing before UI.15's polish pass claims them again. Sibling `shared/userCell.ts` is free too) |
| `extension/src/sidepanel/People.tsx` | — (GR.4 released it; the panel opens with a `SubTabs` **Roster \| Following** switch that renders only when no dossier is open — a new People surface goes inside one of the two views, not above the switch. **A3.10 landed `DmBox` ("Draft DM" section after Openers in the dossier); UI.14** polishes the file and owes `docs/people-tab.md` a re-read — the Draft-DM surface was documented at A3.15) |
| `src/x/goals.ts` + `src/x/routes/goals.ts` · `src/x/monitor.ts` + `src/x/routes/monitor.ts` · `src/x/connections.ts` | — (the three GR pure/route pairs, all closed. **Import from them, never re-derive**: `loadGoalsWithPacing`/`loadCommitmentsWithDebt`/`loadFlowCurrents`, `loadMonitorInputs`/`runMonitor`/`worstOf`/`shingleJaccard`, `DAILY_CEILING`/`MARK_LOOKBACK_MS`. Every threshold is an exported opening guess — a recalibration is a one-line diff in the pure file and moves every surface at once) |
| `src/x/digest.ts` + `src/x/routes/digest.ts` · `src/x/quests.ts` · `src/x/routes/me.ts` + `src/x/me/profile.ts` | — (closed at GR.7–GR.9. The scorecard is computed **inside** `buildDigestFacts` (D112a); `QuestInputs.originalsTarget?` is optional so pre-GR.8 streaks stay valid; `GOAL_KINDS` is a 5-union with `missed` in `GOAL_STATUSES`, and `POST /me/goals` is the single goal writer) |

## Deviations & decisions register

Pre-seeded from cross-plan analysis (see MASTERPLAN "Cross-plan adaptations" for full text).
Append D11+ as work reveals divergences from plan text.

**Closed-wave D-entries (116 of them, Waves 0–3) live in `STATE-ARCHIVE.md`.** What stays below: the pre-seeded cross-plan adaptations (D1–D11, D13), the standing bookkeeping rule (D97), and the closing Wave-3 entry (D113/D113a) whose (d) and (e) bind every future smoke. The HV/GR entries D93–D112 were archived at GR.10; their forward-binding content survives as the single **Wave-3 carry-forward** gotcha below.

- **D1** (binds AI.2): `app_settings` created once by UI.1; AI.2 reuses it — no second table/migration.
- **D2** (binds UI.2): the 5 doctrine knobs are owned by `niches.doctrine` after N.5; UI.2 must not duplicate them in `app_settings` — read/write through the active niche or drop the keys. **Decision pending at UI.2.**
- **D3** (binds N.4, AI.5): `VOICE_BLOCK_END` renamed by RU.1 to `'## The three variants'`; N.4 substitutes `{{REPLY_PERSONA}}` inside the slice; AI.5 retires slicing — its parity fixture is post-RU/post-N output, and its anti-drift test asserts against defaults containing the niche placeholder.
- **D4** (binds GR.7): no separate `goals` table — extend `me_goals` (baseline fields, `posted_replies`/`originals` metric kinds, `missed` status). One goals system, one digest fact. **Confirm merge shape at GR.7.**
- **D5** (binds RL.4, A3.9, A3.12): after AI.2/AI.6, new LLM calls use `askLLM` + `llmConfigured()`; new prompts register in the registry (`dm`, `article`) so the editor covers them. Refusal-ladder order unchanged.
- **D6** (binds all Settings.tsx tasks pre-UI.11): keep feature Settings UI minimal; UI.11 rebuilds and absorbs.
- **D7** (standing): all NEW UI from Wave 1 on uses UI.10 primitives + `--strat-*` tokens; Wave-5 polish passes touch only pre-masterplan tabs.
- **D8** (binds UI.11): prune the "coming soon" manifest to features still unbuilt at that time.
- **D9** (standing): content.ts task order is fixed: AX.3 → AX.5 → RU.7 → RU.8 → NT.5 → HV.2 → A3.3.
- **D10** (standing): registry knob groups (UI.2–5) run in Wave 5 so they catalog the final constant set, including RU/HV/GR/A3 constants.
- **D11** (UI.8, standing): `biome.json` `files.ignore` now includes `Stratus Design System` — the DS reference folder (Decision 9, plan out-of-scope) was ~112 of the baseline lint errors. Not in UI.8's plan edit-list but a correct, universal fix; whole-repo lint dropped 119→7. The remaining 7 are pre-existing and NOT in any changed file: 2 a11y in `extension/src/sidepanel/Replies.tsx` (UI.15 territory) + 5 biome-format on generated `src/db/migrations/meta/*.json` (a future infra task could ignore `src/db/migrations/meta`).
- **D13** (UI.1, binds UI.2): the **doctrine group is seeded in full by UI.1** — the 6 keys `x.doctrine.{replyTargetMin,replyTargetMax,weekReplyTargetPct,anchors3,anchors4,ladderSwitchAt}`, with the anchors' sorted-unique+range numberArray validation already in place (Task 2's plan attributed that hook to itself; landed early because UI.1 seeds the anchors so it must validate them). **UI.2 must NOT re-declare these doctrine keys** — it only adds the `quests`+`display` groups and wires the consumers (brief quota/ratio/gaps, composer ladder). Store layering (also binds AI.2/D1): `src/settings/store.ts` is platform-agnostic and never imports `src/x/*`; the X registry `src/x/settings/registry.ts` implements the store's `SettingsRegistry` interface and exposes the bound `getSetting`/`resolveSetting`/`getAllValues`/`setSettings`/`resetSettings` — **UI.2+ consumers import those from `../settings/registry.ts`, never the store directly**. biome.json now also ignores `src/db/migrations` (fulfils D11's anticipated infra ignore; the 5 generated-JSON lint errors are gone → whole-repo lint = 2, both pre-existing `Replies.tsx` a11y).
- **D97** (HV.5, bookkeeping — supersedes the sha half of D93a/D94a/D95a): **the ledger records the commit SUBJECT LINE, not a sha.** Three tasks in a row wasted a Step-0 investigation proving a recorded sha was an amend-orphan, and every time the resolution was "the subject line is the identity". A sha can only be written into the commit that changes it by amending, which changes it again — the churn is structural, not carelessness. From HV.5 on: ledger entries carry the subject line + the PARENT sha (stable, already in history), the codemap header stamps `<parent>+<TASK>`, and Step 0 is `git log -1 --format='%h %s'` compared against the last ticked subject. No reconciliation, no orphan hunts.
- **D113** (GR.10): **four places Task 10's text and the repo disagreed, all resolved in the repo's favour, plus one thing the plan could not have known.** **(a) The phase entry went to `docs/PHASE-HISTORY.md`, not CLAUDE.md** (D93a — Task 10 still says CLAUDE.md), and the `PLAN.md` note is a **top blockquote** above §"Product, in one paragraph each", not a §"Phased build" edit (D98b, the RU.10/HV.6 exemplar). **(b) The docs bill was nine items, not the plan's four.** Task 10 lists people/today/harvest/"settings doc if present"; GR.6 also changed the Composer, GR.7 changed the Me tab's goal kinds, and `docs/s2-mcp-server.md` had been wrong about the tool count since **N0.2** (16 in three places; it is 21, and both tier counts were stale too — curated 10→14, write 3→4). A docs-sync task inherits every stale count in the files it touches, not just its own lane's. **(c) `docs/today-tab.md` needed a RENUMBER, not an append.** The Goals card renders after the quests block and Account health after Pinned watch, so the doc's numbered §1–§15 became §1–§17 with two inserts. Verified against `Today.tsx`'s actual JSX order rather than the plan's prose. **(d) The smoke could NOT follow `smoke-passive-harvest.ts` as written.** D98c's rule is namespace-then-delete-what-we-made, which assumes every write is attributable. Two Guardrails writes are **global**: a complete-run reconcile marks EVERY unseen live handle `gone`, and `GET /following/queue` releases rows at read time. So the smoke additionally **snapshots and restores** every foreign `following.status` (from the success path and from `fail()`), plus `commitments` and the seeded `streaks`/`digests` keys — and, because its burst fixtures sit inside today's window while `GET /x/brief` writes the C9 diary, it closes by **re-reading the brief** so the diary is recomputed rather than left describing fixtures. **Any future smoke that exercises a read-that-writes must copy this, not D98c alone.** **(e) A fixture fact the plan's sequence hides:** "unseen" in the reconcile is `last_run_id != runId`, i.e. per **run**, not per batch — a handle shipped in batch 1 and omitted from batch 2 of the SAME run is still seen. The smoke needs **two** runs to demonstrate `gone`/`confirmed`.
- **D113a** (GR.10, bookkeeping): the archive pass moved **20 D-entries (D93–D112, D97 excepted)**, **16 gotchas (HV.1 → GR.9)** and **7 header priors (GR.4 → GR.9)** into `STATE-ARCHIVE.md` verbatim. STATE.md went ~128 KB → **~50 KB** (one `Read` again); the archive is now ~350 KB (grep-only by design). What was deliberately kept live out of the closed lanes: the **carry-forward gotcha** below (the four HV/GR facts that still bind an open task) and nothing else.

- **D127** (A3.15, closes Wave 4): **five plan-vs-repo calls on the docs-sync + smoke, all resolved in the repo's favour.** **(a) The phase title is "Authoring 3.0", NOT the plan's "Phase 10"** — the recent named-phase convention (Harvest 2.0, Guardrails) dropped "Phase N", and "Phase 10" already denotes generated media in `PLAN.md` §"Phased build"; the entry went to `docs/PHASE-HISTORY.md` (D93a) and the PLAN.md note is a top blockquote (the D113a/GR.10 exemplar), never a §"Phased build" edit. **(b) The reconcile is smoked via the EXPORTED `matchManualRows` + a replay of the wrapper's atomic two-sided stamp** — `reconcileManualPosts` is private and its only route (`POST /posts/reconcile`) runs the PAID daily pass, so a $0 smoke can't call the wrapper; the matcher (fully unit-tested at A3.6) + the two-update txn is the faithful $0 realization. **(c) The publisher due-select is replayed read-only** (`status='pending' AND scheduled_for<=now`) — `claimOne` isn't exported and `tickPublisher` needs a token + would call X. **(d) The smoke does NOT re-add the `/writer` shell assertion** (D125b — it lives in `smoke-explorer.ts`); the plan's Task-15 item (e) is discharged there. **(e) The Task-15 codemap bill was mostly already discharged** by A3.1–A3.14's per-task codemap updates (the plan predates that discipline) — A3.15 only added §9 (the smoke) + §8 (the URL-guard manual exemption) + the header re-stamp. **Also:** the whole $0 default run is spend-proof even with `XAI_API_KEY` set (every DM/article assertion is a pre-spend refusal; the 503 is asserted only when `!llmConfigured()`). **Binds nothing forward** except the archive bookkeeping (the Wave-4 D-entries/gotchas/priors moved to `STATE-ARCHIVE.md` §Wave 4).

- **D128** (UI.2, resolves D2/D13/D30c): **the D2 ownership call + four plan-vs-repo divergences, all resolved in the repo's favour.** **(a) D2 = Option B (DROP, not route-through-niche).** The reply band (min/max), week-reply-% and 2–10× multipliers are owned by `niches.doctrine` (N.5, read via `loadDoctrine`); UI.1 had ALSO seeded `x.doctrine.{replyTargetMin,replyTargetMax,weekReplyTargetPct}` in `app_settings`. UI.2 dropped those 3 keys from the registry so each knob has exactly one owner — the D30c-consistent choice ("read/write the 5 through the niche, not the store"; its parenthetical "the anchors/ladderSwitchAt are NOT niche doctrine — those stay `app_settings`" is the tell). The surviving `doctrine` group is the cadence ladder only (`anchors3/anchors4/ladderSwitchAt`). UI.11's Settings tab will link the band to the Niche card (N.7) — a UI concern, not a registry one. **(b) The plan's Task-2 line-refs were stale** — N.5 already moved reply band/ratio to `loadDoctrine` (brief.ts:360-361, 813), so there is no `REPLY_TARGET`/`WEEK_REPLY_TARGET_PCT` module constant at the plan's L42/L43; only anchors/ladder/sparkline/leader were app-level. UI.2's real wiring is brief GAPS (anchors) + ladder pick + `display` + `quests`; the reply-quota/ratio consumers were already niche-wired and are UNTOUCHED. **(c) The plan's Task-2 done-when ("PATCH `x.doctrine.replyTargetMin` → brief quota moves") is IMPOSSIBLE under Option B** — that key no longer exists in the store. The store-owned demonstration instead uses `x.quests.originalsTarget` (deterministic with commitments cleared) + `x.doctrine.anchors3/anchors4` (→ `today.anchors`). **⚠ UI.17's `smoke-settings.ts` MUST pick a store-owned consumer, NOT `replyTargetMin`** (the plan's Measurement § still names it). **(d) Test files not in the plan's edit-list had to change** — dropping the 3 keys broke UI.1's `registry.test.ts` (3 assertions) + `settings.test.ts` (~9); repointed to surviving doctrine keys (`ladderSwitchAt`/`anchors3`), and all PATCH/override tests were kept INSIDE the `doctrine` group so `settings.test.ts`'s `afterEach(resetSettings({group:'doctrine'}))` still cleans up (the settings store is process-global — a stray override leaks between test files). **(e) Target-0 = vacuously-done-with-a-note, NOT literal "omission"** (the plan said "0 ⇒ quest omitted"): omitting a quest would shrink the array and ripple into `allQuestsDone`/`completedMap`/the streak diary; the module's existing loop/launch/targets pattern (target 0 → `done:true` + note) is the faithful, invariant-preserving realization. **Binds forward:** UI.3–UI.5 add their groups the same way (opts-defaulted pure fns, route-only store reads via the bound `getSetting`); **UI.6's extension mirror carries `x.doctrine.anchors3/anchors4/ladderSwitchAt` (`scope:'mirrored'`) into `composerLogic.ts`**; UI.11 owns the Niche-card link for the dropped band keys; UI.17's smoke honors (c).

## Gotchas log

Things the next implementer must know that aren't obvious from the code. Append-only,
one line each, newest last.

**Closed-wave gotchas (143 of them, Waves 0–3) live in `STATE-ARCHIVE.md` — grep it by task id or filename before touching a file a closed lane built.** What stays below: standing test/lint/typecheck traps, the browser-verification debt Wave 5 must clear, and the four HV/GR facts that still bind an open task (consolidated into ONE carry-forward entry at the end rather than sixteen).

- **`bun run lint` was 114–119 pre-existing errors at baseline** — mostly the `Stratus Design System/` reference folder. **UI.8 (D11) added that folder to biome ignore → whole-repo now 7 errors**, all pre-existing and outside any changed file (2 a11y `Replies.tsx`, 5 format on generated `migrations/meta/*.json`). Per-task gate stays "the changed files lint clean" (`bunx biome check <files>`), not "whole-repo green"; don't chase the remaining 7 unless your task owns those files.
- **UI.9 light theme is an OPENING-GUESS palette, unverified in a browser.** Automated gates pass (632 pass / 0 fail incl. 6 new storage tests, typecheck + build + changed-file lint green) but the 12-tabs × {dark,light} × {cozy,compact} visual QA matrix is explicitly manual and deferred to the Wave-5 polish passes (UI.12–16 done-whens). Known trap (plan risk note): AA contrast on warn/ok/band **text over tinted fills** in light — the fill tokens and semantic ink were re-authored dark-enough to double as text (`--strat-warn: #9a6a10`, band-hot `rgb(0,150,100)`, band-warm-text `#8a6100`), but spot-check before trusting. Adjust `:root[data-theme='light']` values, not the consuming rules.
- **Settings types are extension-local mirrors** — `SettingEntry`/`SettingsGroup`/`SettingsResponse`/`SettingsPatchResult`/`SettingsResetResult` in `extension/src/shared/types.ts` (re-exported via api.ts), mirroring the `GET /x/settings` JSON. The extension MUST NOT import `src/x/settings/registry.ts` (§5 build-isolation). **Binds UI.2/UI.11:** if the server `SettingDef` shape gains a field the editor needs, hand-sync it into `SettingEntry`.
- **`exactOptionalPropertyTypes` pass-through pattern (UI.10):** primitives forwarding a maybe-undefined optional (`Slider.unit`, `SettingRow.onReset`, `GearPopover.onReset`/`label`) type the prop as `T | undefined` explicitly — else TS rejects `prop={maybeUndefined}`. Reuse this for new primitives that relay optionals.
- **UI.10 rail regrouping is UNVERIFIED IN A BROWSER** (like UI.9's palette). Extension typecheck + Vite build + changed-file lint are green and the CSS compiles, but the visual QA (grouped rail spacing, brandmark, EmptyState, popover) rides the Wave-5 polish passes (UI.12–16 done-whens), per D7/D14.
- **`inspect.test.ts` fails 2/2 under bare `bun test`, GREEN under `bun run test` (the canonical, package.json script `SQLITE_PATH=:memory: bun test`).** Introduced by `906f2d3` ("eval 1", out-of-skill Wave-0 audit): `src/db/client.ts` defaults to `:memory:` when `NODE_ENV==='test'` (Bun sets it), but `src/x/data/inspect.ts:101`'s readonly connection still defaults to `./stratus.db` when `SQLITE_PATH` is unset — so under bare `bun test` the primary writes `:memory:` while the inspect readonly reads the file → seeded `people` rows invisible → the 2 LIKE-query tests get `[]`. **NOT RU.3 (bisected: 0 fail at `d1902e5`/ST.9, 2 fail from `906f2d3` onward; RU.3 touches none of inspect.ts/client.ts/the people table).** Full suite is **732/0 under `bun run test`** — always gate with `bun run test`, not bare `bun test`. One-line fix for a data-core/S1 task: make `inspect.ts` honor the same `NODE_ENV==='test'`→`:memory:` default as `client.ts` (or `client.ts` should also `process.env.SQLITE_PATH ??=` set it so both agree).
- **RL.4: adding a registry key is a 4-file move.** `PROMPT_KEYS` + `PROMPT_SPECS` in `prompts/registry.ts`, the default exported from a **PURE** module (a route import cycles — AI.5's `voice/extractPrompt.ts` move exists for that reason), `registry.test.ts`'s key-list + defaultBody assertions, and `docs/settings-tab.md`'s prompt-count copy (now **11**). The extension needs nothing — the Prompts panel renders whatever `GET /x/prompts` returns. CLAUDE.md's AI-layer entry still says "10 keys"; that is a historical phase record and stays — **RL.8's phase entry is where the 11th key gets documented.**
- **RL.5 is UNVERIFIED IN A BROWSER**, like every panel component since UI.9 — no tests were added (repo convention: panels are untested, logic lives server-side). Automated gates only: root+extension typecheck, `cd extension && bun run build`, changed-file lint, full suite 1087/0. The plan's live done-whens (create → add → test-render → generate → Overwrite) ride RL.8's smoke + the Wave-5 QA pass. The one server-behavior assumption the UI leans on was **not** left to faith — the `{humanizer:{}}` → defaults round-trip was proven with a throwaway route test (D81b).
- **RL.8: `cellOf` counts `n` only for MEASURED rows** (those with a `metrics_snapshots` row); `posted` counts every attributed reply. The smoke seeds a snapshot so both move by +1 — a future assertion that publishes a reply without a snapshot must assert `canned.posted`, not `canned.n`, or it will read as a silent failure.
- **NT.1: extension typecheck does NOT cover `*.test.ts`** — `extension/tsconfig.app.json` has `"exclude": ["src/**/*.test.ts"]`, and root `tsconfig.json` only includes `src/**/*` + `scripts/**/*` + `drizzle.config.ts`. So an extension test file is checked by **`bun run test` alone**; a type error in one is invisible to both typechecks. (Same for `earlyReplies.test.ts` — pre-existing, just never written down.)
- **NT.2: a test handle longer than 15 chars is silently skipped and the whole test goes vacuous.** `normalizePersonHandle` enforces `/^[A-Za-z0-9_]{1,15}$/`, so `nt2_mutual_person` (17) was dropped by `recordEngagements` while every assertion around it still "passed" (the person row had been inserted by hand). Cost a debug round-trip. Two rules for any people-layer test: keep seeded handles **≤15 chars**, and assert `{processed, skipped}` on the ingest result so a skip can never hide behind downstream assertions.
- **NT.2: seeding `posts_published` in the shared in-memory DB is safe only with an out-of-window date and no metrics row.** The engagements suite needs own posts for target resolution; it uses a **January `postedAt`** and writes no `metrics_snapshots`, which keeps the rows out of every windowed or measured aggregation other suites assert over (playbook medians, reup 14–60d, brief/digest/quest windows, pinned watch). Files run sequentially so the only real hazard is a stray — cleanup runs inline *and* in `afterAll`. Reuse this shape rather than inventing a new seed.
- **NT.3 test-handle rule (again):** the route suite uses `nt3_route_eng` (13 chars). The NT.2 gotcha applies unchanged — any people-layer test handle over 15 chars is silently skipped by `normalizePersonHandle`, and asserting `{processed, skipped}` is what keeps that from hiding.
- **NT.7: a real-DB smoke that seeds `posts_published` must write the row `retired: true`.** `scripts/smoke-notifications.ts` needs an own post for prefix target resolution; a non-retired one left behind by an aborted run is a candidate for the daily 03:00 UTC snapshot pass, i.e. a **real billed X read** for a tweet id that doesn't exist (invariant #7). `retired: true` takes it out of the candidate set permanently and costs the smoke nothing (it never reads metrics). Reuse this whenever a smoke seeds own posts against the real DB — `smoke-followups.ts` predates the rule and gets away with it only because its rows are deleted synchronously.
- **NT.7: `app.request(...)` is typed `Response | Promise<Response>`.** A helper declared `function f(): Promise<Response> { return app.request(...) }` fails root typecheck; make the helper `async`. `bun run typecheck` covers `scripts/**`, so this bites in smoke scripts, not in route suites (which `await` inline).
- **CLAUDE.md was slimmed to guardrails-only (2026-07-23, out-of-skill docs refactor).** The full phase-entry ledger moved **verbatim** to `docs/PHASE-HISTORY.md`. **Every docs-sync task (MASTERPLAN global rule 6, codemap §7 rule 29 — updated) now appends its phase entry to `docs/PHASE-HISTORY.md`, NOT CLAUDE.md**; CLAUDE.md changes only when a guardrail (invariant, workflow, stack quirk) changes. Plan text still saying "CLAUDE.md phase entry" means the history file.
- **STATE.md was compacted 2026-07-23 (out-of-skill housekeeping, no code touched).** It had reached **339 KB** — past the `Read` tool's 256 KB cap, so Step 0's "read STATE.md fully" silently became a chunked-read investigation every session. 96 closed-wave D-entries + 127 closed-wave gotchas + the header's HV/GR prior paragraphs moved **verbatim** into the new `STATE-ARCHIVE.md` (frozen, 269 KB, grep-only — bigger than one `Read` by design); the live file is **74 KB**. The split was generated by line number, not retyped, and verified as a set operation: every non-empty original line lands in **exactly one** of the two files (438/438, zero duplicated). **What this means for you:** a task touching a file Waves 0–2 built should `grep STATE-ARCHIVE.md` for the task id or filename before assuming the live file tells the whole story — e.g. the RL `/use` contract (D79), the NT.5 icon prefixes (D89), the ST registry contract. SKILL.md gained the matching archive rule in Step 6 (a lane's docs-sync task archives that lane) and its Step-0 staleness guard now compares the **subject line**, matching D97 — it still said `--format=%h` five sessions after shas stopped being recorded.

- **Wave-3 carry-forward (the only HV/GR facts that still bind an open task — everything else is in the archive):** **(1) MCP tool count is asserted exactly.** `src/mcp.test.ts` asserts **21**; every future curated tool bumps that number *and* `docs/s2-mcp-server.md`'s three counts (total / curated tier / write tier) **in the same commit** — that doc was silently 5 tools stale for four phases. **(2) Docs now assert user-visible strings, and two Wave-5 tasks own them.** `docs/harvest-tab.md`, `docs/playbook-tab.md`, `docs/people-tab.md`, `docs/today-tab.md`, `docs/composer-tab.md` and `docs/settings-tab.md` quote real copy (the both-off warn line, `Passive: N rows today`, `insufficient data (n=…)`, `Nobody has shown up often enough yet.`, the three schedule-warning sentences, the debt-tier wording, the `Nd left · need X/day · doing Y/day` line). **UI.14 (People/Channels/Voice/Ideas) and UI.15 (Replies/Playbook/Harvest/Studio) must re-read the matching `docs/*-tab.md` in their own task**, and `docs/today-tab.md` is now numbered §1–§17 — an inserted card renumbers, it does not append. `docs/settings-tab.md` still says **four toggles** in two places: UI.2/UI.11's settings rebuild has to move that count, not just add a row. **(3) Real-DB fixture rules that outlive their lane:** a seeded `posts_published` row is written **`retired: true`** (NT.7 — otherwise it is a candidate for the daily *billed* pass), a `following` fixture needs a `following_runs` row first (`last_run_id` is a notNull FK), people-layer handles must be **≤15 chars** (longer ones are silently skipped and the assertions go vacuous), and any suite touching `scheduled_posts`/`posts_published` inside the monitor's windows must delete its rows in `afterAll` — `monitor.test.ts` asserts `clusterCount === baseline + 1` and a leaked pending pair breaks a different file. **(4) Five reads WRITE and none of them may be polled:** `/radar/drafts` (48h expiry), `/following/queue` (release + revoke), `/x/goals`, `/x/brief` and `/x/digest` (all three settle `active → achieved|missed`). A3.8's Today card and A3.14's week board both sit on `/x/brief` — a refresh loop there silently advances goal statuses.
- **GR.10: the smoke that closes Wave 3 is the template for smoking a surface whose READS write.** `scripts/smoke-guardrails.ts` ($0, no `--live`, rerunnable, verified to leave 0 rows) mounts following + monitor + goals + me + calendar + brief + digest over the real `./stratus.db`. **Copy its safety shape, not `smoke-passive-harvest.ts`'s, whenever a route mutates rows it did not create:** namespaced ids cover only what you wrote, so it *additionally* snapshots every foreign `following.status` before the first write and restores it from the success path **and** from `fail()` (a complete-run reconcile marks every unseen live handle `gone`; a queue read releases rows), snapshot-restores `commitments` and the seeded `streaks`/`digests` keys rather than deleting them, and **closes with one more `GET /x/brief`** so today's C9 streak row is recomputed from clean data instead of describing the burst fixtures it just deleted. **Three things it discovered that no plan text says:** (a) reconcile "unseen" is per **run** (`last_run_id`), not per batch — proving `gone`/`confirmed` needs TWO runs; (b) the queue needs ≥3 slots of budget for its assertions, so the script fails loudly naming `windowUsed/windowCap` + `dailyUsed/dailyCeiling` rather than through a confusing count mismatch; (c) `?factsOnly=true` returns **before** the digest cache write, so a scorecard read caches nothing — but it still settles goals. Assertions over shared/real data are **baseline-relative** where they must be (`nearDuplicate.pairCount`, `scheduleCluster.clusterCount`) and exact where the fixture owns the whole population (`questAdherence`, the queue batch, the ledger statuses).

- **Wave-4 carry-forward (the A3 facts that still bind an open Wave-5 task — the per-task A3 gotchas + D114–D126 + the A3.1–A3.14 header priors are in `STATE-ARCHIVE.md` §"Wave 4", grep by task id):** **(1) Browser-verify debt owed, all panel-convention 2-minute user checks (deploy + chrome://extensions reload), cleared by the Wave-5 polish passes:** A3.3 active-times end-to-end row-landing + the passiveCapture-off gate; A3.4 the Composer audience-peak/staleness/blended-Best-time render; A3.7/A3.8 the manual-publish UI (Composer API|Manual toggle, Calendar overdue tint + Mark posted, the alarm→notification→card round-trip); A3.10 the dossier Draft-DM flow + the 422 thin-dossier line; A3.13 the Writer page (create→outline→section→autosave-survives-reload→**Copy-for-X rich-vs-plain**, the risk item — record which clipboard path the prod host accepts); A3.14 the week board (7 columns, draft→slot in two clicks, chips/overdue). **(2) Wave-5 polish ownership:** UI.12 (Today) owns the A3.8 manual card + a `docs/today-tab.md` (now §1–§18) re-read; UI.13 (Composer/Calendar) owns the A3.7 toggle + the A3.14 board (**`Calendar.tsx` is a thin renderer over the pure `calendarLogic.ts` — put board logic THERE**; it reuses the Composer's cadence/best-times helpers by import) + `docs/composer-tab.md`/`docs/calendar-tab.md`; UI.14 (People) owns the A3.10 Draft-DM box + `docs/people-tab.md`; UI.16 (content-script + explorer tokens) owns the A3.3 active-times capture in `content.ts` AND must re-token `public/writer.html` in lockstep with the explorer's `stratus_explorer_token` key (A3.13 — the two public pages share it). **(3) Standing A3 facts:** `article prompt.md`↔`ARTICLE_PROMPT_TEMPLATE` byte-sync joins post/reply/thread/dm as an edit-both-together pair (13-key registry); `mark-posted`/manual reminders render off `status`, NEVER `postedTweetId` (the daily reconcile links the tweet id); the calendar URL guard is `pending`-only (manual = the $0 link path) and articles never hit the API; `dm_drafts`/`articles` PATCH is SELECT-then-guard with terminal-status 409s; `suggestBestSlotDate`/`annotateGaps` take `audience` as a trailing optional arg (no grid ⇒ byte-identical to pre-A3.4); calendar-fixture day bands 90–130d (GR.6) / 150–160d (A3.5) / ~200d (brief) / ~300d (A3.7) are spoken for — a new suite picks a fresh band ≥45 min clear.

- **UI.2 (Wave-5 opener) — three facts the settings moat now binds:** **(1)** the reply band / week-reply-% / 2–10× multipliers are **NOT settings-store keys** — they live on `niches.doctrine`, read via `loadDoctrine()` (voice/brief/followups/playbook/digest). A future task tuning them edits the niche (`PATCH /x/niches/:slug`), never `app_settings`; the registry's `doctrine` group is now ONLY `anchors3/anchors4/ladderSwitchAt`. **(2)** `routes/brief.ts` reads six store keys via the bound `getSetting` (from `../settings/registry.ts`, never the store) at REQUEST time — sync Map-cached, invalidated on write — so a `PATCH /x/settings` moves the next `GET /x/brief` with no restart (the `appliesOn:'immediate'` contract; worker-cadence knobs will be `'restart'`). **(3)** the settings store is **process-global across test files** (one in-memory `app_settings` + one module cache). Any test that `setSettings(...)` MUST reset it in `afterEach`/`afterAll` (`resetSettings({group})` or `{keys}`) or the override leaks into a later suite — `settings.test.ts` + `brief.test.ts` both do; keep new store PATCH assertions inside one registry group so a group-reset cleans them.

## Planning-error log

Corrections to MASTERPLAN.md itself (wrong dep, wrong order discovered live).

- **2026-07-16-niche.md Task 3 done-when** — the "`Pitești` hits nothing" grep contradicts the same task's bit-for-bit equivalence requirement (post prompt §6/§9/§0 carry builder biography outside the §1/§5 extraction scope; no niche field exists to hold them). Resolved at N.3 in favor of equivalence; see D28b for the full resolution + what N.9 must scope differently. Source plans are never edited — this entry is the correction.
- **2026-07-17-ai-layer.md — no task owns the post drafter's askLLM migration.** Task 5's edit list names replies/pillars/playbook/voiceExtract; Task 6 only flips drafter's MOUNT gate; Task 7 adds a sibling route. Yet the plan's Why-section + Done-when #1 spirit require every draft surface to honor the provider setting, and the Task-6 gate flip would 502 `/posts/draft` on OpenRouter-only setups. Resolved at AI.5 by migrating `drafter.ts` there (D47b) — the call-site-migration task was the right home.

