# Masterplan execution state

> Dynamic memory for `/masterplan`. Updated after EVERY task, in the task's own commit.
> Plan: `plans/MASTERPLAN.md` (static — order, reasoning levels, waves, D1–D10 + Wave-6 seeds D141–D144 + Wave-7 seeds D172–D174).
> Codemap: `.claude/skills/plan-feature/references/codemap.md` (updated per task too).
> History: `.claude/skills/masterplan/STATE-ARCHIVE.md` (frozen, grep-only — closed-lane deviations, gotchas, priors, ledger rows).
> Debt: `.claude/skills/masterplan/VERIFY-DEBT.md` (**unpaid**, not history — browser-verify + CA.2. Read it when you have Chrome open; skip it otherwise).

## Waves 0–6 CLOSED at 131/131 (JD.8, 2026-07-27) — Wave 7 OPEN at 7/10 (RC.4, 2026-07-28)

**Wave 7 is the open wave**: HM (`plans/2026-07-28-project-humanizer.md`, 5 tasks) + RC
(`plans/2026-07-28-radar-curated-drafting.md`, 5 tasks), both planned 2026-07-28. Order,
reasoning levels and seeds D172–D174 live in MASTERPLAN.md §Wave 7; ledger rows are below.
HM.1, RC.1, RC.2, HM.2, RC.3, HM.3 and RC.4 have landed; no file is claimed. **D172 and D173
are spent, and so is D179c** — RC.2 took `0025` (no second migration), the Radar.tsx lane is
closed, and `x.radar.curatedCount` now has a client reader. **RC's whole feature is shipped
and reachable** (server + panel); what remains is HM.4's Settings card and the two docs-sync
closers. Only serial constraint left: **HM.5 → RC.5** (D174). Waves 0–5 closed at 106/106,
Wave 6 at 25/25.

**Three out-of-skill commits sit between JD.8 and HM.1 — reconciled at HM.1, no ledger
drift** (the D75 precedent). `61f9601 close wave 6` — source-touching (`playbook.ts`,
`judge/prompt.ts`, `routes/replies.ts`, `shared/postCoach.ts`, Radar/Playbook.tsx, 5 test
files): **it is where the suite went 1855 → 1863**, so a plan quoting "1855+" is quoting
2026-07-27. `17672d1 fix(playbook): tolerate a server older than the extension build` — a
485/375-line `Playbook.tsx` rewrite, **still undocumented in the codemap; HM.5 folds it in**
(its own Task-5 checklist says so). `8121366 wave 7` — the two Wave-7 plans + MASTERPLAN
§Wave 7 + the STATE rows. Step 0 will keep seeing `8121366 wave 7` as HM.1's parent.

**What this file is now.** Only what is true of the repo regardless of which plan you are on:
the size rule, the standing deviations (D7 / D97 / D113), the standing test·lint·docs·fixture
traps, the wave summary, the migration-journal row, and JD.8's own D171. **Everything
lane-specific moved to `STATE-ARCHIVE.md` §"Archived at JD.8"** — Wave 6's 25 ledger rows, the
whole JD register (D167–D170 + the D149/D160/D162/D165/D166 pointers), the JD gotchas, the SC
carry-forward and every "which lane is open" narration. Grep the archive by task id or filename
before touching a file a closed lane built; the load-bearing half is also in codemap §5/§7/§10.
**Unpaid verification debt is NOT there** — it is `VERIFY-DEBT.md`: sixteen items, none binding,
one of which spends (`0l`, ~$0.010).

**Size discipline (added 2026-07-23 at 339 KB; paid ten times since — read this before a new wave
re-grows the file).** STATE.md must stay loadable in a **single `Read`**, and the limit that bites
is the tool's **token** cap (~25k), not the 256 KB byte cap. **The budget is measured: at 61.7 KB a
single `Read` came back TRUNCATED at 25,023 tokens, so the real ratio is ≈405 tokens/KB and the
ceiling is ≈61.5 KB. Aim at 60 KB and leave the last 1.5 KB unspent.** Measure BYTES with `wc -c`,
not `.length` — this file is full of multibyte `—`/`≥`/`✓` and a character count reads ~1.5 KB under
the truth. **Five lessons, each of which cost real sessions:** (1) *the breach is never one big
entry, it is six good ones that each looked affordable* — Wave 6 went 48 KB → 156 KB in fifteen
tasks; (2) *a closing pass archives the lane in front of it and misses the weight behind it* —
UI.17 left Wave 5's ledger rows and every session until GT.9 paid for them, so **also ask what
stopped binding two waves ago**; (3) *"it still binds an open task" is a reason to DISTIL, not to
keep 19 KB*; (4) **an ORDINARY task breaches it too, so `wc -c` between entries, not only before
the commit** — JD.5 needed two archive passes in one task; (5) **when a lane CLOSES, its hot-file
lock row and its pointer lines are weight, not state** — a pointer to an archived block that no
open task needs is 2 KB of "go read this, though nobody has to". Never "fix" a breach by deleting
something an open task needs; if a pass slips, pay it as a standalone housekeeping commit (the
`55c6d19`/`c9c8ade` precedent).

- **last-commit:** **identity is the SUBJECT LINE — no sha is recorded (D97).** HEAD should read `feat(radar): Curate & draft — score the queue, dismiss the noise, draft the top N (RC.4)`, parent `54a9514` = HM.3. **Step 0 is one command:** `git log -1 --format='%h %s'`, compared against the last ticked subject in the ledger below. **Do not hunt shas.** **Out-of-skill commits are NOT ledger drift and are already reconciled** — thirteen of them, all before SC.1, enumerated in `STATE-ARCHIVE.md` §"Archived at JD.2". If Step 0 finds a subject naming neither a ticked task nor one of those thirteen, run the `sync` reconciliation.
- **current state of the repo (the numbers a new plan starts from):** suite **1935** (RC.4 +8, all in the two pure extension modules; **HM.3 added none — panel components are untested by convention**, so a Wave-7 extension task ticking with an unchanged count is correct, not a skipped gate); tables **41** (RC.2 added a COLUMN, not a table: `radar_drafts.curation_score`; migrations through **`0025_curvy_edwin_jarvis`**, the journal is **FREE from `0026`**); registry **14 groups / 62 knobs, 28 mirrored** (RC.3 minted `x.radar.curatedCount`; `registry.test.ts` pins the radar group list, the exact mirrored list AND a max-parity assertion against `x.ai.batchReplyCap`, and `docs/settings-tab.md` carries three count strings — four server-side edits per mirrored knob, **plus three on the extension side that RC.4 found the hard way**: `ServerConfig` + `SERVER_DEFAULTS` + `readServerConfig`, pinned by `serverSettings.test.ts`'s exact `Object.keys` list AND its full-blob `toEqual` (D181d)); **prompt keys 16** (RC.1 appended `reply-curate`; `registry.test.ts` asserts the exact list + `docs/settings-tab.md`'s three counts, all four updated); MCP **23 tools** (`src/mcp.test.ts` asserts it exactly + `docs/s2-mcp-server.md`'s three counts); smoke scripts **31**; extension tsconfig `include` shims **7** (HM.1 added `../src/shared/humanize.ts`); whole-repo lint **0 errors**. **No file is locked; Wave 7's serial chains are D172–D174.**
- **next-up:** **7.8 HM.4** (Settings → General "Reply humanizer" editor card — **high**, deps HM.2✓+HM.3✓, disjoint from everything left) or **7.9 HM.5** (HM docs-sync + `smoke-humanizer.ts`, which must precede RC.5 per D174 — so the lane order from here is HM.4 → HM.5 → RC.5, and only HM.4 and HM.5 are order-flexible with each other). **HM.4 must not reach for the humanizer through the settings registry** — it is a raw `app_settings` row, so the card talks to `api.humanizer.{get,patch,reset}` (already on `api.ts` since HM.2/HM.3) and a `SettingsGear` over it renders nothing (gotcha below). **RC.5 inherits a live feature**: the button ships, so its smoke proves the route layer (manual-pin exemption, `curation_score` persistence, the pre-spend 400 ladder) and its docs pass owns `docs/radar-tab.md` beyond the one cost sentence RC.4 already corrected. **Standing gate reminders for whatever comes next:** `bun run test` (bare `bun test` targets the file DB and `inspect.test.ts` fails 2/2); biome forbids `delete process.env.X` / `: any` in `scripts/`, **non-null assertions (`x!`) anywhere**, a `let` assigned once, and a backtick string with no interpolation — it also sorts imports case-sensitively, collapses short chained calls, and rejects a `console.log` string-concat unless every operand interpolates (run `bunx biome check --write` on the changed files FIRST). Extension-touching work adds `cd extension && bun run build` **and** its own `typecheck`, then `grep -cE '^\s*import[ {]' extension/dist/content.js` == 0.

## Ledger

Status: `[ ]` todo · `[~]` in progress (lane claimed) · `[x]` done (sha + date) · `[s]` skipped (reason in deviations).

**Waves 0–6 are CLOSED — 131/131; Wave 7 is open at 7/10.** Closed-wave per-task entries (shas,
parents, dates, notes) live in `STATE-ARCHIVE.md` — Waves 0–4 at UI.11, Wave 5 at GT.9, **Wave 6
at JD.8**. Grep by task id.

- **Wave 0 — Foundations (13/13 ✓)**: UI.1, UI.8, UI.9, UI.10, ST.1–ST.9. Closed by `d1902e5` ST.9 studio docs+smoke.
- **Wave 1 — Prompt & identity core (32/32 ✓)**: RU.1–RU.3, N.1–N.9, ME.1–ME.7, AI.1–AI.13. Closed by `f86e06a` AI.13 AI docs+smoke.
- **Wave 2 — Reply machine & on-page surfaces (29/29 ✓)**: RU.4–RU.10, AX.1–AX.7, RL.1–RL.8, NT.1–NT.7. Closed by `5d4a1a4` NT.7 NT docs+smoke.
- **Wave 3 — Ambient data & guardrails (16/16 ✓)**: HV.1–HV.6, GR.1–GR.10. Closed by `docs(guardrails): GR docs-sync + $0 smoke-guardrails.ts (GR.10)`, parent `7adfbca`.
- **Wave 4 — Authoring 3.0 (15/15 ✓)**: A3.1–A3.15. Took migration `0023_slimy_night_thrasher` (`articles`). Closed by `docs(authoring): Authoring 3.0 docs-sync + $0 smoke-authoring3.ts (A3.15)`, parent `819c61d`.
- **Wave 5 — Settings moat + polish (16/16 ✓)**: UI.2–UI.7, UI.11–UI.17. Closed by `docs(ui): cockpit overhaul docs-sync + $0 smoke-settings.ts (UI.17)`, parent `7b10239`.
- **Wave 6 — Coach, judge & growth tactics (25/25 ✓, + GT.5 `[s]` by design)**: SC.1–SC.9, GT.1–GT.9, JD.1–JD.8. Took migration `0024_soft_leopardon` (`draft_judgments`). Closed by `docs(judge): LLM-judge docs-sync + $0 smoke-judge.ts (JD.8)`, parent `fc44d1c`. Per-task rows archived at JD.8.
- **Wave 7 — Radar follow-ups: curated drafting & humanize-at-pick (5/10)** — order/reasoning/seeds in MASTERPLAN §Wave 7:
  - [x] 7.1 HM.1 — promote humanize core to `src/shared/humanize.ts` + extension shim — `refactor(humanize): promote the humanizer core to src/shared with an extension shim (HM.1)`, parent `8121366`, 2026-07-28. Pure move + shim + new `jitterOdds`; `engine.test.ts` byte-untouched (`git diff` empty) and green. Suite 1863/0, both typechecks, lint 0, extension build green, `dist/content.js` imports 0. See **D175**.
  - [x] 7.2 RC.1 — pure curation core (`replies/curate.ts`) + `reply-curate` prompt key — `feat(replies): curation scoring core + reply-curate prompt key (RC.1)`, parent `5357e89`, 2026-07-28. New pure module + registry key 15→16 (the RL.4 four-file move, `docs/settings-tab.md` included). Nothing wired — RC.3 is the first call site. Suite 1863→**1884** (+21), typecheck clean, lint 0. See **D176**.
  - [x] 7.3 RC.2 — migration `0025` `radar_drafts.curation_score` + batch threading — `feat(radar): nullable curation_score on radar_drafts (0025) + batch threading (RC.2)`, parent `75f21bc`, 2026-07-28. Took **`0025_curvy_edwin_jarvis`** (one ALTER, inspected, seeds intact) — **the journal is now free from `0026`**. Suite 1884→**1886** (+2), typecheck clean, lint 0. Extension untouched. See **D177**.
  - [x] 7.4 HM.2 — humanizer settings store + `GET/PATCH/DELETE /x/humanizer` — `feat(humanizer): project-level humanizer settings + $0 GET/PATCH/DELETE /x/humanizer (HM.2)`, parent `638c5e1`, 2026-07-28. 4 files (3 new), **no schema/migration/registry key/extension file**. Suite 1886→**1914** (+28), typecheck clean, lint 0. Mount verified through `mountX`, not only the bare-Hono suite. Nothing reads the config yet — HM.3 is the first consumer. See **D178**.
  - [x] 7.5 RC.3 — `POST /x/replies/curate` + `x.radar.curatedCount` knob — `feat(replies): POST /replies/curate scoring endpoint + x.radar.curatedCount (RC.3)`, parent `e1c0b28`, 2026-07-28. 5 files (1 new: the replies router's **first** route suite). Knob is `mirrored` → registry 61→**62 knobs, 27→28 mirrored**; route writes nothing and loads no relationship/guidance/me/pillars. Suite 1914→**1927** (+13), typecheck clean, lint 0. Extension untouched — RC.4 is the first client. See **D179**.
  - [x] 7.6 HM.3 — Radar checkbox + humanize-at-pick — `feat(radar): optional humanize-at-pick with server-owned toggle (HM.3)`, parent `10eb9ca`, 2026-07-28. 5 files (0 new), all extension + `docs/radar-tab.md`; **no server file, no schema, no registry key, no test** (panel convention). Suite **1927 unchanged**, both typechecks, lint 0, extension build green (`dist/sidepanel.js` carries `radar-humanize` + the whole engine now; `dist/content.js` imports 0). Browser verification is HM.5's checklist. See **D180**.
  - [x] 7.7 RC.4 — extension Curate & draft orchestration — `feat(radar): Curate & draft — score the queue, dismiss the noise, draft the top N (RC.4)`, parent `54a9514`, 2026-07-28. 8 files (0 new), all extension + `docs/settings-tab.md`'s Radar-gear sentence; **no server file, no schema, no registry key**. Suite 1927→**1935** (+8, both pure modules — `Radar.tsx` untested by convention), both typechecks, lint 0, extension build green (`dist/content.js` imports 0). **D179c is paid**: `x.radar.curatedCount` now reaches a client reader, mirrored set 27→28. See **D181**.
  - [ ] 7.8 HM.4 — Settings → General "Reply humanizer" editor card
  - [ ] 7.9 HM.5 — HM docs-sync + $0 `smoke-humanizer.ts` (**before RC.5**, D174)
  - [ ] 7.10 RC.5 — RC docs-sync + `smoke-radar-curate.ts`

## Hot-file locks

A lane claims a file before starting, releases on commit. **Every file is free and nothing
serializes anything** — JD.7 released the last pair (`playbook.ts` + `routes/playbook.ts`) and
JD.8 changed no source file at all. The single row below states a standing invariant rather than
lane state. **Rows are archived when their lane closes and no open task binds the file** — 25 went
at UI.17, three at GT.9, the last two at JD.7. The documented way to reach a closed lane's file
notes is to **grep `STATE-ARCHIVE.md` by filename** before touching a file that lane built.

| File | Owner |
|---|---|
| `src/db/migrations/` journal (any migration task) | — (**RC.2 took `0025_curvy_edwin_jarvis` (`radar_drafts.curation_score`); the next migrating task owns `0026`.** JD.4 took `0024_soft_leopardon` (`draft_judgments`) before it. Standing invariant: never run two migration-generating tasks in parallel sessions (journal conflicts), ignore any hardcoded number in plan text — **JD.4's plan said "0018", which is RL.2's `reply_lists`; a plan quoting a migration number is quoting the day it was written** — and always `bun run db:generate` against the current journal, then inspect the SQL for dropped seed INSERTs, codemap §4) |

## Deviations & decisions register

Pre-seeded from cross-plan analysis (see MASTERPLAN "Cross-plan adaptations" for full text).
D172–D174 are the Wave-7 seeds (full text in MASTERPLAN); append D175+ as work reveals
divergences from plan text.

**All 171 prior D-entries are in `STATE-ARCHIVE.md`** — Waves 0–3, the Wave-4 closer D127, the
registry lane D128–D133, the polish lane D134–D140, the GT lane, the SC lane, and the whole JD
lane (D149/D160/D162–D170) at JD.8. Grep by task id or by the filename the lane built. What stays
below is (a) what is **standing** — true of the repo regardless of which plan you are on — and
(b) **D171**, the newest, which the codemap header points at.

- **D7** (standing): all NEW UI from Wave 1 on uses UI.10 primitives + `--strat-*` tokens; Wave-5 polish passes touch only pre-masterplan tabs.
- **D97** (standing, bookkeeping): **the ledger records the commit SUBJECT LINE, not a sha.** Three tasks in a row wasted a Step-0 investigation proving a recorded sha was an amend-orphan, and every time the resolution was "the subject line is the identity". A sha can only be written into the commit that changes it by amending, which changes it again — the churn is structural, not carelessness. Ledger entries carry the subject line + the PARENT sha (stable, already in history), the codemap header stamps `<parent>+<TASK>`, and Step 0 is `git log -1 --format='%h %s'` compared against the last ticked subject.
- **D113(d)/(e)** (GR.10, standing — binds every future smoke; parts (a)–(c) are in the archive): **(d) A smoke over a surface whose READS WRITE cannot follow `smoke-passive-harvest.ts`.** D98c's namespace-then-delete rule assumes every write is attributable; two Guardrails writes are GLOBAL (a complete-run reconcile marks EVERY unseen live handle `gone`; `GET /following/queue` releases rows at read time). So `smoke-guardrails.ts` additionally **snapshots and restores** every foreign `following.status` (from the success path AND from `fail()`), plus `commitments` and the seeded `streaks`/`digests` keys — and, because its fixtures sit inside today's window while `GET /x/brief` writes the C9 diary, it closes by **re-reading the brief** so the diary describes clean data rather than deleted fixtures. Copy this, not D98c alone. **(e) A fixture fact the plan's sequence hides:** "unseen" in the reconcile is `last_run_id != runId` — per **run**, not per batch, so proving `gone`/`confirmed` needs TWO runs.
- **D171** (JD.8, the masterplan's closing task — binds every future closing docs-sync and every future smoke): **four resolutions, and the first is the one worth carrying.** **(a) The per-task docs rule needs a shape of task it does not have a home for, and JD.1 proved it a second time.** GT.9 wrote the rule (D160b: *a prompt-only task has no tab doc, so it ships invisible — write its user-facing paragraph in its own commit*). Three tasks later JD.1 — a provenance/prompt task — shipped with **zero** docs, and only the closing pass caught it. The rule as written is a note, and notes do not fire. Its two halves are now in `docs/composer-tab.md` (the few-shot dilution cap) and `docs/replies-tab.md` (the quoted-tweet trust label). **The generalizable form: a task whose output is a PROMPT or a SELECTION RULE changes what every draft says and belongs in the tab doc of the surface that renders it — "which tab does the user see this on" always has an answer, even when "which tab did I edit" does not.** **(b) The plan's "`PLAN.md` §Phased build: a line for the judge" was written against a section that no longer takes phases.** §Phased build is the original five-phase build-out; every phase since Wave 0 has gone into the blockquote block above §"Product, in one paragraph each", which is where GT.9 and SC.9 put theirs. Followed the convention, recorded the divergence (D160a: record the skip, don't assert it). **(c) `--live` is not a style choice.** GT.9 and SC.9 shipped no flag because the only paid thing in reach was the call they existed to prove was not made; `smoke-judge.ts` ships one because a keyless run structurally cannot claim that the provider closed the JSON inside `JUDGE_MAX_OUTPUT_TOKENS`, that thirteen dimensions survive `parseVerdict`, or that the persisted `text_hash` is the one the Playbook probes. **Ask what a $0 run cannot claim; that list IS the `--live` step, and if it is empty ship no flag.** **(d) Delete fixture rows by the HASH OF THEIR OWN CONTENT when the PK is generated.** `draft_judgments.id` is a random uuid, so an id list dies with the process and a run killed mid-way leaks rows forever; the hash of a fixture sentence is deterministic across runs, cannot collide with a real draft, and covered the `--live` row for free. Reusable for any table with a generated PK and a natural content key.

- **D175** (HM.1 — binds HM.2/HM.3/HM.4 and any future verbatim promotion into `src/shared/`): **four resolutions.** **(a) The split has exactly one genuinely shared private, and the plan asked the right question.** `pickFrom` is used by both `humanize` and the anti-repeat `pickItem`; it is now `export`ed from `humanize.ts` and **imported, not re-exported, by `engine.ts`** — so the engine's public surface is byte-identical to before the move plus the moved names, and nothing outside can start depending on a helper that was private yesterday. The six regex constants the task block flagged as maybe-shared (`LETTER_RE`/`SPACE_RE`/`LETTERS_ONLY_RE`/`LEADING_NON_LETTERS_RE`/`TRAILING_NON_LETTERS_RE`/`URL_ISH_RE`) turned out to be humanizer-only; `EMOJI_RE`/`VAR_TOKEN_RE`/`VAR_SLOT_RE` are render-only. Clean cut. **(b) `jitterOdds` is re-exported from `engine.ts` too**, though it is NEW rather than moved (the plan's Design says "engine re-exports all of it"). Costless and consistent; the only consumer today imports it from the shim. **(c) A `Write` of a file containing `\u` escapes silently emits the LITERAL characters.** Moving `EMOJI_RE` verbatim turned its two escapes into a raw ZWJ + VS16 in the source — behaviour-identical, invisible in every diff view, and `Edit` could not fix it because the old/new strings normalize to the same bytes; `perl -CSD -i -pe 's/…\x{200D}…/…\\u200D…/'` did. **A green suite does not prove a verbatim move was verbatim — `od -c` the lines that carry escapes.** (The same trap bit this task's STATE/codemap prose.) **(d) The plans' repo counts are already stale before Wave 7's first task**: the suite is **1863**, not the "1855+" every HM/RC done-when quotes, because `61f9601 close wave 6` landed out-of-skill in between. D174 already says to re-derive every count from the current code at implementation time — HM.1 is the proof it was not paranoia.

- **D176** (RC.1 — binds RC.3 directly, RC.4 through the contract it consumes): **five resolutions.** **(a) The plan's "export `renderBatchTweet` (one-word change)" is TWO exports, not one.** `substituteReplyPersona` was private too, and `curate.ts` needs the same persona-substitutes-FIRST ordering. Duplicating it would be the §7-rule-4c twin trap on a rule that exists for injection safety, so it is exported and imported, not copied. Both exports are additive; no template byte and no existing behaviour moved. **(b) The tie-break is the WANTED order, not the response order.** The plan says the stable sort's ties "keep input order, which is the panel's ranked order" — but the model's array order is not the panel's. `selectCurated` therefore iterates `wanted` to build its candidate list and tie-breaks on that index, so the same scores arriving in any order produce the same keep set (a test runs forward and reversed and asserts identical output). **(c) `keep` comes back best-first.** Undeclared in the plan, and RC.4 depends on it: its "trim from the tail to `batchReplyCap`" only drops the weakest survivors if the array is score-descending. **(d) `buildCurateInput` projects each tweet to `{tweetId,handle,author,text}` before rendering.** `BatchTweet.relationship` is server-stamped, and although `parseBatchTweets`'s whitelist means the RC.3 route can never carry one, the projection makes decision 5 (text-only scoring) structural instead of incidental — cost one line. **(e) The parser's strict/lenient split is per-field, the JD.3 `parseVerdict` reading, not blanket strictness.** Strict on `id`/`score`/`lowValue` — those three decide which queue rows get dismissed, and a defaulted `lowValue` would delete a tweet nobody flagged; lenient on `reason` (missing → `''`, over-long → clipped at 120) because refusing over display metadata throws away a call already billed. The plan's "non-int → reject-row → null" is honored: a fractional score means the model ignored an `integer` schema, so its keep/drop judgement is not trusted. Out-of-range still clamps.

- **D177** (RC.2 — binds RC.3/RC.4 and every future nullable-numeric column): **three resolutions.** **(a) The §4 seed-INSERT standing check has a false alarm built into it, and the obvious grep triggers it.** `0000`'s pillar seeds are `INSERT OR IGNORE INTO`, so `grep -c 'INSERT INTO' 0000_*.sql` returns **0** and reads exactly like "drizzle dropped the seeds". Two checks that can't lie: `git diff --stat src/db/migrations/` (a generate that drops a seed must have REWRITTEN an existing `.sql` — if the only change is the `_journal.json` append plus two new untracked files, nothing was dropped) and a fresh `:memory:` boot counting `content_pillars` (3). **The rule generalizes: prove the seed by the diff and by the boot, never by grepping for a string whose exact spelling you're guessing.** **(b) `curationScore` cannot be spread with the truthiness test its two neighbours use.** `parseBatchTweets` pushes `...(band ? {band} : {})` and `...(signals ? {signals} : {})`, which is correct for a non-empty string and an object — and **silently wrong for a score of 0**, a value `selectCurated` can genuinely keep when the queue is smaller than the target. The push uses `!== undefined`; `buildRadarDraftRows` uses `?? null`, not `|| null`, for the same reason. The whole column is worthless if 0 and "never curated" collapse into each other, so both spellings are commented at the site and pinned by a test that asserts `[91, 0, null]`. **Any future nullable numeric on a row built by a spread inherits this exactly.** **(c) The score is kept out of the prompt and out of `contextSnapshot` STRUCTURALLY, so no future task can leak it by forgetting.** `renderBatchTweet` builds from four named fields and the confirm endpoint reads named columns — neither spreads a row — so extending `RadarBatchTweet` cannot reach either. That is why this task needed no §7.19 guard test: there is no code path to guard. Say this rather than re-verifying it.

- **D178** (HM.2 — binds HM.3/HM.4 directly, and every future `app_settings` JSON store): **three resolutions.** **(a) The `llm/settings.ts` defaults idiom is UNSAFE the moment the settings object holds an ARRAY, and copying it verbatim would have shipped the bug.** `getAiSettings` returns `{...DEFAULT_AI_SETTINGS}` safely only because every field there is a primitive; `DEFAULT_HUMANIZER` carries `prefixes` and `suffixes`, so a shallow spread hands every caller the module's own array instances and one `.push` edits the process-wide default. All three return paths go through a `cloneDefaults()` that re-copies both pools, and a test mutates a GET response then re-reads to prove it. **Any future store whose defaults contain an array or a nested object inherits this — the exemplar does not cover it.** **(b) The strict/lenient split is a SEMANTIC difference, not duplication.** `parseHumanizerConfig` (the core) reads STORED rows and falls back per field; `parseHumanizerPatch` (the store) reads a human PATCH and must 400. The core's `parseChance` stayed private and the route's `validChance` re-states the same range check on purpose — exporting one predicate for two opposite behaviours would invite a future edit that makes a typo'd chance silently keep the old value. This is the deliberate exception to §7 rule 4c, and it is annotated at both sites. HM.1's D175a clean cut (`pickFrom` is the *only* shared private) survives intact. **(c) `saveHumanizerSettings` takes a FULL settings object; the route merges over `getHumanizerSettings()` first.** So a partial row is unrepresentable — a reader never has to know which fields some past PATCH happened to touch, which matters because HM.3 reads this config on a click path where a missing field would mean "no jitter" rather than "default jitter". **Also worth carrying: `0` and `1` are legal chances**, so every check here is range-based; a truthiness test would make "never fire this jitter" unsettable (the D177(b) trap in a different file).

- **D179** (RC.3 — binds RC.4 directly, and every future task that tests an LLM route or mints a mirrored knob): **four resolutions.** **(a) The replies router had NO route suite, and the plan's edit list assumes one.** Its guards live in `src/app.test.ts` behind `describe.if(authed)` — which needs `API_TOKEN`, so on a clean checkout they **do not run at all**. Worse, a suite over the composed `app` inherits BOTH gates: the bearer middleware and §7.22's LLM-gated mount (the replies router refuses to mount without a provider key, so a keyless machine can't reach the route through `app` even with a token). NEW `src/x/routes/replies.test.ts` mounts the exported router on a bare Hono, the judge.test/drafter.test/prompts.test shape. **Generalizes: a route suite over `app` tests the wiring, a bare-Hono suite tests the route — an LLM route needs the second or its ladder is untested on any machine that isn't fully configured.** **(b) There is no `mock.module` anywhere in this repo**, so the plan's "happy path with a mocked askLLM" had nothing to build on, and inventing a mocking layer inside a route task is how a testing pattern gets shipped by the one task that didn't plan it. The established substitute is two-sided and covers the same ground: the 400 ladder ending in a forced 503 `llm_not_configured` (both provider keys unset in a `finally`) proves the pre-spend half — **and proves for free that `loadPromptSafe('reply-curate')` + `buildCurateInput` render without throwing, since the request reached the LLM layer** — while the post-spend half (`parseCurateScores` / `selectCurated`'s keep·drop·unscored) is already unit-tested in RC.1's `curate.test.ts`. What neither covered was the settings clamp, so `curateKeepTarget()` is **exported** and pinned directly (the JD.5 `rewriteWins` precedent: a rule the live path costs money to reach gets a unit test, not an integration test). **(c) A mirrored knob is a TWO-task contract and RC.3 shipped half of it.** `registry.test.ts`'s exact mirrored list encodes the rule in its own comment — a mirrored key must arrive with a client fallback or "the knob silently does nothing on the client side" — but the plan puts the key in Task 3 and `ServerConfig`/`SERVER_DEFAULTS` in Task 4. Safe in the gap only because `readServerConfig` reads **named keys**, never spreads the blob, so an unknown mirrored key is inert rather than corrupting. **RC.4 must wire `curatedCount` into `ServerConfig` + `SERVER_DEFAULTS` (25) + `readServerConfig` or the knob is permanently decorative.** **(d) Two response facts RC.4 depends on that the plan states nowhere:** `keep` comes back **best-first** (D176c — so trimming from the tail drops the weakest survivors), and `scored` is **anchored** to the asked ids first-occurrence-wins, so `scored ⊆ asked` and `keep ∪ drop ∪ unscored = asked`. The anchor is not load-bearing for the panel's id→score Map (a ghost row would be inert there) — it exists so a later consumer that ITERATES `scored` can't render a verdict for a tweet that was never in the queue.

- **D180** (HM.3 — binds RC.4 directly, HM.4 through the api namespace, and every future task that promotes a shared core the extension already mirrors): **four resolutions.** **(a) HM.1 promoted the ENGINE and left the TYPE behind, and the plan didn't know.** `extension/src/shared/types.ts` has declared its own seven-field `HumanizerConfig` since RL.1 (the per-list config on `ReplyList.humanizer`), so the plan's "import `HumanizerConfig` from `../humanize.ts`" would have been a name collision — and adding `HumanizerSettings` on top of the *local* copy would have made the panel run one module while typing against a hand-kept twin. Resolved by deleting the local declaration: `import type … from '../humanize.ts'` + `export type { HumanizerConfig }`, the JD.6 `JudgeVerdict` precedent, so the extension build now has exactly ONE declaration. Type-only under `verbatimModuleSyntax`, so nothing new reaches the content IIFE. **The generalizable question for any promotion-into-`src/shared/` task: does the extension already mirror the TYPE you just made canonical? Promoting the code and leaving the mirror is how a drift becomes silent — and it only bites at the first task that runs the engine client-side, which is never the promotion task itself.** **(b) A docs rule fired two tasks earlier than the plan expected.** The plan gives all docs to HM.5, but `docs/radar-tab.md:75` QUOTES the pick hint (`copied ✓`) and HM.3 changed it — the Wave-3 carry-forward rule ("reword a quoted string and you owe the doc in the same commit") is not a docs-sync rule, it binds whoever moves the string. HM.3 therefore shipped the hint-line correction **plus** a short §"Humanize picks"; HM.5 still owes the Settings-card half, the round-trip story and the smoke. D171a's shape again from the other side: the question is not "is there a docs-sync task later", it is "did I just change something a doc quotes". **(c) The plan names `RadarRow` but the prop has to cross `RadarGroup`, and the Clicked list renders `RadarRow` too.** Both get `humanizer`. A re-copy from the Clicked view IS a fresh pick (it re-runs `markClicked`+`confirmDraft`+clipboard), so jittering in Queue but not in Clicked would make the clipboard and the recorded `replyTextEdited` disagree for the same row. The CHECKBOX stays queue-only as planned — where you set the flag and where the flag applies are different questions. **(d) `humanize` is called ONCE per pick and the single result feeds both the clipboard and `confirmDraft`.** Calling it twice would be the natural-looking edit (`writeText(humanize(...).text)` next to `confirmDraft(id, humanize(...).text)`) and would silently record a DIFFERENT jitter than the one that got copied — the measurement story ("what actually went out") is only true because there is one draw. Also: `PICK_NOTE_MS` is 2500, not RD.2's 1500 — the note now carries a list of jitter names to read.

- **D181** (RC.4 — binds RC.5 directly, HM.4 not at all, and every future task that adds a MIRRORED knob or a client-side "act on a model's verdict" flow): **four resolutions.** **(a) `partitionForCurate` needed a THIRD bucket the plan didn't have, and the reason is a whole-request refusal.** `parseBatchTweets` rejects an empty `text` with `invalid_tweet_text_${i}` for the **entire** request, and an image-only sighting genuinely captures as `text: ''` (the card renders its url instead — `{s.text || s.url}` has been there since RD.1). The plain "Draft replies" button only ever sent the top ~20, so it mostly dodged this; curation sends the **whole fresh queue**, where one textless row would 400 a 40-tweet pass with an error naming an index the user can't see. So blank-text rows are `skipped` — not scored (there is nothing to grade, decision 5), not drafted (the same refusal would kill the batch call), left queued. **A pinned row with blank text is skipped too**: the curated flow builds its own draft set, and knowingly including a row the server will refuse would trade the whole pass for one pin. **The generalizable trap: a per-item validator that returns a whole-request error turns "one odd row" into "the feature is broken", and widening WHAT you send is what surfaces it — `draftReplies` still carries this exposure over its 20 rows and always did (gotcha below).** **(b) `sendBatch` returning its outcome instead of setting the note is a divergence from the plan text, forced by the plan's own note format.** Task 4 says sendBatch "sets note", and also that the curated note is `scored S · dropped D · drafted K/N · $(curateCost+draftCost)` — a note written inside sendBatch cannot see the scoring call's cost or counts, so it could only ever describe half the click. Both calls now report failure as a **return value** (`BatchOutcome`/`CurateOutcome`); the callers own the note. Free side effect worth having: `curateAndDraft` has no nested try, so "dismiss happens strictly after the grade succeeded" is visible in the control flow rather than argued about in a comment. **(c) The plan's `scored ${scoreable}` is the asked count and would overstate a truncated response.** The note reports `res.scored.length` (what actually came back graded) and adds `· U unscored` only when U > 0 — three states, RL.9's honesty rule: a queue that scored 38 of 42 must not read as "42 was all there was". Also: **every early exit still prints the grading spend** (`nothing left to draft · $0.0051`, `draft failed: … · $0.0051`) — an unreported paid call is how a per-click surface starts feeling free. **(d) A mirrored knob split across two tasks fails SILENTLY, and D179c under-stated how silently.** RC.3 shipped the registry half; for one commit the knob was editable in Settings, present in the mirrored blob, and read by nobody — no warning, no test failure, no type error, because `readServerConfig` reads named keys and never spreads. What DOES fail loudly is the second half: `serverSettings.test.ts` pins an exact sorted `Object.keys(ServerConfig)` list (and the "full blob" `toEqual` pins every value), so the field cannot be added without both edits. **The plan's edit list named neither assertion.** Read codemap §5's mirrored-wire paragraph before adding a mirrored key; it is the only place both sides are written down.

## Gotchas log

Things the next implementer must know that aren't obvious from the code. Append-only,
one line each, newest last.

**All 200+ closed-lane gotchas are in `STATE-ARCHIVE.md`** — grep it by task id or filename before
touching a file a closed lane built. Wave 6's went at JD.8 (§"Archived at JD.8": the JD.1/JD.6/
JD.7/JD.8 blocks, the SC carry-forward, and the four pointer lines). What stays below is the
standing set: test/lint/typecheck traps that bite any task, and the Wave-3 carry-forward, whose
four items are about how this repo's tests, docs and fixtures work rather than about any lane.
**Browser-verification debt is not here — it is `VERIFY-DEBT.md`**; a debt parked among facts
reads as a fact.

- **RC.4: what RC.5 inherits, and one pre-existing bug it deliberately did NOT fix.** `Radar.tsx` now holds `sendBatch`/`runCurate`/`curateAndDraft` and a `busy: 'draft'|'curate'|null` flag (the old boolean `drafting` is gone — a plan or doc saying "drafting" means this). **The un-fixed bug, for whoever wants it:** `draftReplies` still sends every one of its ≤20 rows raw, so a single blank-`text` sighting in the top 20 400s the whole plain batch with `invalid_tweet_text_${i}` — the same trap D181a solved for the curated path only. The fix is one filter in `draftReplies` (or `partitionForCurate(...).skipped` reused); it was left out because widening RC.4's blast radius into the untested happy path of the older button is exactly what an extension task should not do at the end of a lane. **RC.5's smoke owes the route-layer proofs the panel can't give:** a `manual` row never in the curate request but always in the batch one, `curationScore` reaching `radar_drafts`, and the 400 ladder pre-spend. The note strings RC.5 documents are `scored S · [U unscored ·] dropped D · drafted K/N · $x`, `… · nothing left to draft · $x`, `… · draft failed: … · $x`, `Curate failed: …` — RC.4 corrected `docs/radar-tab.md` §"What this tab costs" in its own commit (it said "the only spend is **Draft replies**", which the second button made FALSE — the D180b rule fires on a doc your change makes wrong, not only on a quoted string you reworded), but everything else in that file is still RC.5's: the button itself, the note line, dismissal-is-permanent, the knob, and §"Draft replies (N)" at L52 which still reads as the tab's only drafting action.

- **HM.3: what RC.4 inherits in `Radar.tsx`.** `RadarSection` now holds a second async state (`humanizer`) and a second mount effect; `RadarGroup` and BOTH `RadarRow` call sites (queue groups + the Clicked list) carry a `humanizer` prop — a new prop on `RadarRow` is now a **three**-site edit, not two. `draftReplies` is byte-untouched, so RC.4's `sendBatch(rows, scoreById?)` refactor starts from the plan's shape. **Do not add jitter inside `curateAndDraft` (D172)** — curated rows are drafted, then picked through the same `onPick`, so they are humanized for free; a second call site would double-jitter. `RADAR_KEYS` is still the two batch caps — RC.4 adds `x.radar.curatedCount` there and to `ServerConfig`/`SERVER_DEFAULTS`/`readServerConfig` (D179c). Also: `dist/sidepanel.js` now contains `'come out changed'` **twice** (ReplyLists' odds line + the Radar checkbox), so the codemap's HM.1 inline check is a `>= 1` test, not `== 1`.

- **RC.3: adding a MIRRORED knob is a four-edit move, and two of the four are exact-list assertions.** `registry.ts` (the group array — order inside it decides position in the mirrored list, and RADAR sits between GATES and AI), `registry.test.ts` **twice** (the group's exact key list in the UI.4 test, and the exact mirrored list at ~L331), and `docs/settings-tab.md`'s **three** count strings ("**62 knobs in 14 groups**", the group table's knob count, "Twenty-eight of the 62 are also **mirrored**") — prose asserted by no test. A `server`-scoped knob skips only the mirrored list. Also: no test asserts the registry TOTAL (only `grouped === SETTINGS_REGISTRY.length`), so the doc counts are the only place a miscount shows.

- **HM.2: the `'humanizer'` row is invisible to the settings platform, and HM.4 must not reach for it there.** It is an `app_settings` key like `'ai'`, not a registry entry — `GET /x/settings`, the mirrored blob, `useSettingsEditor` and `x_update_setting` all skip it by construction. So the Settings → General card talks to `api.humanizer.{get,patch,reset}` (an `api.ts` addition HM.3 or HM.4 makes, whichever runs first), and a `SettingsGear` over it renders nothing. Same for HM.3's Radar read. Its counts are unchanged for the same reason: **14 groups / 61 knobs / 27 mirrored, prompt keys 16, MCP 23** — HM.2 minted none of them.

- **RC.2: `parseBatchTweets` and `buildRadarDraftRows` unit tests do NOT live in `src/x/routes/radar.test.ts`** (which exists, and is route-level only) — both are in **`src/test.test.ts`**, `describe('reply batch …')` and `describe('radar drafts (C0)')`. The plan's edit list names `radar.test.ts`; it is the wrong file for a pure-function case. Also: `buildRadarDraftRows`'s first test asserts the row with a whole-object `toEqual`, so **every new `RadarDraftInsert` field breaks it until the expectation is extended** — that assertion is the field-list contract, treat the break as the feature.

- **RC.1: the RL.4 four-file move now says `docs/settings-tab.md` 15 → 16, and D171a decides WHICH doc a prompt task owes.** The three counts (intro "all **16** of them", "The 16 prompts are: …" list, "all 16 prompts snap back") are prose, asserted by no test — only the exact `PROMPT_KEYS` list in `registry.test.ts` is. RC.1 also added a paragraph on what the curation prompt decides, in `settings-tab.md` rather than `radar-tab.md`: the surface that RENDERS the prompt today is the Prompts subtab, and the Radar's button copy does not exist until RC.4. A prompt task should ask "where can the user see this right now", not "which tab will eventually use it".

- **Settings types are extension-local mirrors.** The extension MUST NOT import `src/x/settings/registry.ts` (§5 build-isolation); it mirrors the `GET /x/settings` JSON in `extension/src/shared/types.ts`, and a new server-side `SettingDef` field is hand-synced there. (Full block in the archive; grep `Settings types are`.)
- **`exactOptionalPropertyTypes` pass-through pattern (UI.10):** primitives forwarding a maybe-undefined optional (`Slider.unit`, `SettingRow.onReset`, `GearPopover.onReset`/`label`) type the prop as `T | undefined` explicitly — else TS rejects `prop={maybeUndefined}`. Reuse this for new primitives that relay optionals.
- **`inspect.test.ts` fails 2/2 under bare `bun test`, GREEN under `bun run test`** (the canonical `SQLITE_PATH=:memory: bun test`). `src/db/client.ts` defaults to `:memory:` when `NODE_ENV==='test'`, but `src/x/data/inspect.ts`'s readonly connection still defaults to `./stratus.db` when `SQLITE_PATH` is unset — the primary writes memory, the readonly reads the file, seeded rows are invisible. **Always gate with `bun run test`.** One-line fix for a future data-core task: make `inspect.ts` honor the same `NODE_ENV==='test'` default (or have `client.ts` `process.env.SQLITE_PATH ??=` set it so both agree).
- **RL.4: adding a registry key is a 4-file move** (JD.3 walked it again): `PROMPT_KEYS` + `PROMPT_SPECS` in `prompts/registry.ts`, the default exported from a **PURE** module (a route import cycles — AI.5's `voice/extractPrompt.ts` move exists for that reason), `registry.test.ts`'s **exact key list** + `isPromptKey` + `defaultBody` assertions, and `docs/settings-tab.md`'s prompt-count strings (now **15**). The extension needs nothing — the Prompts panel renders whatever `GET /x/prompts` returns. **`registry.test.ts` also loops every default through `validatePromptBody` asserting `unknown:[]`, so every `{{X}}` in a new template must be in `required`∪`optional`.**
- **NT.1: extension typecheck does NOT cover `*.test.ts`** — `extension/tsconfig.app.json` has `"exclude": ["src/**/*.test.ts"]`, and root `tsconfig.json` only includes `src/**/*` + `scripts/**/*` + `drizzle.config.ts`. So an extension test file is checked by **`bun run test` alone**; a type error in one is invisible to both typechecks.
- **NT.7: `app.request(...)` is typed `Response | Promise<Response>`.** A helper declared `function f(): Promise<Response> { return app.request(...) }` fails root typecheck; make the helper `async`. `bun run typecheck` covers `scripts/**`, so this bites in smoke scripts, not in route suites (which `await` inline).
- **CLAUDE.md was slimmed to guardrails-only (2026-07-23, out-of-skill docs refactor).** The full phase-entry ledger moved **verbatim** to `docs/PHASE-HISTORY.md`. **Every docs-sync task (MASTERPLAN global rule 6, codemap §7 rule 29) appends its phase entry to `docs/PHASE-HISTORY.md`, NOT CLAUDE.md**; CLAUDE.md changes only when a guardrail (invariant, workflow, stack quirk) changes. Plan text still saying "CLAUDE.md phase entry" means the history file.
- **Wave-3 carry-forward — the four repo-wide facts (everything else HV/GR is in the archive):** **(1) MCP tool count is asserted exactly.** `src/mcp.test.ts` asserts **23**; every future tool bumps that number *and* `docs/s2-mcp-server.md`'s three counts (total / curated tier / write tier) **in the same commit** — that doc was silently 5 tools stale for four phases. **(2) Docs assert user-visible strings.** `docs/harvest-tab.md`, `docs/playbook-tab.md`, `docs/people-tab.md`, `docs/today-tab.md`, `docs/composer-tab.md`, `docs/replies-tab.md` and `docs/settings-tab.md` quote real copy — **reword a quoted string and you owe the doc in the same commit** (`docs/today-tab.md` is numbered §1–§18, so an inserted card renumbers rather than appends). **(3) Real-DB fixture rules that outlive their lane:** a seeded `posts_published` row is written **`retired: true`** (NT.7 — otherwise it is a candidate for the daily *billed* pass), a `following` fixture needs a `following_runs` row first (`last_run_id` is a notNull FK), people-layer handles must be **≤15 chars** (longer ones are silently skipped and the assertions go vacuous), and any suite touching `scheduled_posts`/`posts_published` inside the monitor's windows must delete its rows in `afterAll` — `monitor.test.ts` asserts `clusterCount === baseline + 1` and a leaked pending pair breaks a different file. **(4) Five reads WRITE and none of them may be polled:** `/radar/drafts` (48h expiry), `/following/queue` (release + revoke), `/x/goals`, `/x/brief` and `/x/digest` (all three settle `active → achieved|missed`). A3.8's Today card and A3.14's week board both sit on `/x/brief` — a refresh loop there silently advances goal statuses.

## Planning-error log

Corrections to MASTERPLAN.md itself (wrong dep, wrong order discovered live).

- **2026-07-16-niche.md Task 3 done-when** — the "`Pitești` hits nothing" grep contradicts the same task's bit-for-bit equivalence requirement (post prompt §6/§9/§0 carry builder biography outside the §1/§5 extraction scope; no niche field exists to hold them). Resolved at N.3 in favor of equivalence; see D28b for the full resolution + what N.9 must scope differently. Source plans are never edited — this entry is the correction.
- **2026-07-17-ai-layer.md — no task owns the post drafter's askLLM migration.** Task 5's edit list names replies/pillars/playbook/voiceExtract; Task 6 only flips drafter's MOUNT gate; Task 7 adds a sibling route. Yet the plan's Why-section + Done-when #1 spirit require every draft surface to honor the provider setting, and the Task-6 gate flip would 502 `/posts/draft` on OpenRouter-only setups. Resolved at AI.5 by migrating `drafter.ts` there (D47b) — the call-site-migration task was the right home.
