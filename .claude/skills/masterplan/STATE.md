# Masterplan execution state

> Dynamic memory for `/masterplan`. Updated after EVERY task, in the task's own commit.
> Plan: `plans/MASTERPLAN.md` (static — order, reasoning levels, waves, D1–D10).
> Codemap: `.claude/skills/plan-feature/references/codemap.md` (updated per task too).
> History: `.claude/skills/masterplan/STATE-ARCHIVE.md` (frozen, grep-only — closed-wave deviations + gotchas).

**✓ The archive pass UI.7 handed over was PAID at UI.11** (own housekeeping commit, before the task): 105 KB → ~57 KB, one `Read` again. Moved verbatim to `STATE-ARCHIVE.md` §"Archived at UI.11": the six header priors A3.15 → UI.6, the discharged pre-seeded adaptations **D1–D5/D9/D10**, the Wave-4 closer **D127**, the whole registry-lane register **D128–D133**, and eleven gotchas. Split by line number and verified as a set operation (31 lines, each in exactly one file — the D113a precedent). What was kept live: D6/D7/D8/D11/D13/D97/D113/D113a, the standing gate traps, and the three carry-forward gotchas (Wave-3, Wave-4, and the NEW Wave-5 registry one that replaces the five UI.2–UI.7 blocks).

**Size discipline (added 2026-07-23 at 339 KB; re-applied at GR.10, 128 KB → ~47 KB; again at UI.11, 105 KB → ~57 KB):** STATE.md must stay loadable in a single `Read`. What lives here is (a) standing rules, (b) entries for the OPEN lane / current wave, (c) unpaid verification debt, (d) the ledger + locks. When a sub-plan closes, its D-entries and gotchas move verbatim to `STATE-ARCHIVE.md` — that move is part of the sub-plan's docs-sync task, not a separate chore.

- **last-commit:** **identity is the SUBJECT LINE — no sha is recorded any more (D97).** HEAD should read `chore(masterplan): archive the Wave-5 registry lane into STATE-ARCHIVE.md` (the UI.11 housekeeping commit), then `feat(settings): unified Settings cockpit over the registry (UI.11)`. Behind them, newest first: UI.7 `feat(band): configurable thresholds shared server + badge (UI.7)` (parent `b378293`), UI.6 (`398e546`), UI.5 (`7b3f5aa`), UI.4 (`28e0840`), UI.3 (`13dd18e`), UI.2 (`859a923`), A3.15 (`819c61d`), and the A3.14→A3.1 chain down to `49fcd1d` **`wave 3 done`**. **Four out-of-skill commits are in history and are NOT ledger drift** — `wave 3 done` (post-GR.10 goals-route fixup, reconciled at A3.1), `b59a564` **`ideas`** (adds only `crazy_ideas.md`), `55c6d19` **`chore(masterplan): compact STATE.md…`**, and the two **CA** commits (plan `plans/2026-07-23-cost-audit-followups.md`, run 2026-07-23 at explicit user direction, parent `30b0583` = GR.3). **Step 0 is one command:** `git log -1 --format='%h %s'` — if the subject names the last ticked task (or one of the out-of-skill lines above), there was no out-of-skill work. The full sha-by-sha chain older than UI.7 is in the ledger below and in `git log`; it is not re-derived here.
- **current wave:** **WAVE 4 (Authoring 3.0) IS CLOSED — A3.1–A3.15 ✓** (A3.15 = docs-sync + `scripts/smoke-authoring3.ts`). Waves 0–4 all closed (RU/AX/RL/NT/HV/GR/A3). **WAVE 5 (Settings moat + polish) is OPEN — UI.2 ✓, UI.3 ✓, UI.4 ✓, UI.5 ✓, UI.6 ✓, UI.7 ✓ (the registry lane is DONE — 14 groups / 53 knobs, 17 mirrored); UI.11–UI.17 remain `[ ]`, all of them UI work.** No locked files anywhere. **The migration journal is FREE; the next migrating task owns `0024`** (never run two migrating tasks in parallel — journal conflicts). **The D9 `content.ts` chain is COMPLETE** [AX.3✓→…→A3.3✓]; UI.7 took it once more for the band blob read, and UI.16's token polish is now the only future claimant. **D5 FULLY DISCHARGED** (dm + article registered; no open task binds it). **D2 RESOLVED at UI.2 (Option B)** — the 3 niche-owned band keys `x.doctrine.{replyTargetMin,replyTargetMax,weekReplyTargetPct}` were DROPPED from `app_settings` (the niche is the sole owner via `loadDoctrine`); the surviving `doctrine` group = the cadence ladder (`anchors3/anchors4/ladderSwitchAt`), plus the NEW `quests`+`display` groups, now drive `routes/brief.ts`. See **D128**. **D93a standing for UI.17:** phase entries → `docs/PHASE-HISTORY.md`, never CLAUDE.md. **The Wave-4 forward-binders for Wave 5 are consolidated in the Wave-4 carry-forward gotcha below** (browser-verify debt + which UI.12–UI.16 task owns each A3 surface + docs re-reads).
- **next-up:** **UI.11** (Settings tab rebuild — the crowded cockpit, **xhigh**, D6+D8-bound) is the next `[ ]` Wave-5 ledger entry, then the UI.12–UI.16 polish passes and UI.17 (docs-sync + the archive pass). **The registry lane is closed** — UI.11 consumes what UI.2–UI.7 built rather than adding to it, so four things bind it: **(1)** the tab renders from `GET /x/settings` (the extension NEVER imports the server registry — §5 build isolation; `SettingEntry` etc. are hand-synced mirrors in `extension/src/shared/types.ts`), and its saves go through `settingsClient`, which already fires `stratus/settings-sync` so a mirrored change reaches the page without a reload. **(2)** It must surface the **two-tier AI story** (`x.ai.*` per-surface defaults vs the global `ai` blob — D131a) and **link the niche-owned knobs** (reply band min/max, week reply %, 2–10× multipliers) to the Niche card rather than showing them as settings — they were deliberately dropped from `app_settings` three times (D128a/D129a). The `band` group UI.7 added is the ADJACENT-but-different thing: those twelve are store-owned, and sitting them beside the Niche band link is the honest layout. **(3)** `appliesOn:'restart'` is on the wire and needs the "takes effect on restart" hint (only `x.workers.dailyMetricsHourUtc`/`publisherIntervalSec`). **(4)** D6 (keep feature Settings UI minimal, UI.11 absorbs it) and D8 (prune the "coming soon" manifest to features still unbuilt) both discharge here; `docs/settings-tab.md` still says **four toggles** in two places and now understates the group count badly. **The full `scope:'mirrored'` set is 17 keys** — `x.doctrine.anchors3/anchors4/ladderSwitchAt`, the twelve `x.band.*`, `x.gates.bestTimeMinN`, `x.mentions.panelRefreshCap` — all consumed as of UI.7; verify with `GET /x/settings/values?scope=mirrored` rather than trusting this list. **The UI.1 store pattern is now proven by UI.2–UI.7** (D128/D129/D130/D131/D132/D133): pure modules take an opts object (or a trailing defaulted param) defaulted to today's constant — **on BOTH sides of the wire now, the panel's pure modules included** — and only routes/workers (server) or components (panel) read the store/blob; EXCEPT `people/store.ts::recomputePerson`, which reads the `people` group directly because `computeStage` runs from every ingest path (D129b, the one sanctioned impure-DB store read); a knob whose real owner is elsewhere (a niche field) is DROPPED, never duplicated (targetBand keys dropped a 3rd time); a knob with two legitimate consumers gets ONE key read twice (digest+followups at UI.3, brief+metrics at UI.4, mentions-route+daily-pass at UI.5, Composer+Calendar at UI.6, reply-gate+timeline-funnel at UI.7); and when the owner is a shared PURE module, the registry defaults ARE that module's constant and the group is its whole shape, never a subset (D133a). Env-backed defaults use the D130c helper `envNumberDefault` so an out-of-range env value can't become an invalid default — the `every registry default validates against its own def` test catches it. `appliesOn:'restart'` is only for knobs that arm a timer (D130b); **no money knob is ever restart-scoped** (UI.5). **One pending CA.2 verification rides ahead and is NOT a masterplan task:** grep the prod journal for `[cost-audit]` — if `includes.tweets` ≈ the console-vs-`/cost/today` gap, do CA.2 step 2 (drop `referenced_tweets.id` from the metrics-read expansions in `fields.ts`) + a `docs/PHASE-HISTORY.md` one-liner; else write the refute one-liner and remove the instrumentation. **Gate reminders:** `bun run test` (bare `bun test` targets the file DB); biome forbids `delete process.env.X`/`: any` in `scripts/`, sorts imports case-sensitively, collapses short chained calls, and rejects a `console.log` string-concat unless every operand interpolates (run `bunx biome check --write`, `--fix --unsafe` for the string rules). Extension-touching tasks add `cd extension && bun run build` **and** `typecheck`. **Whole-repo `bun run lint` is now 0 errors** — the two long-standing `Replies.tsx` a11y errors are gone, so a new error is yours.
- **prior (UI.7 — xhigh, dep UI.6✓, $0 [a stricter band SAVES Grok spend; the gate still runs before it], server + extension, no schema/migration/route/MCP change, 12 files [1 new]; suite 1518→1534 [+16]):** the first knob BOTH sides of the wire act on. `classifyBand(signals, thresholds = BAND)` — the 8 `BAND` numbers plus the four literals hoisted out of the function body (`tooSmallAgeMin/Views/Vpm`, `watchReplyCeiling`) became the exported `BandThresholds`; the shared module stays dependency-free and only its CALLERS differ. NEW registry group `band`: **all 12 keys, every one `mirrored`**, defaults taken from `BAND` itself, each description carrying the §7.19 ">=100 measured replies" line (14 groups / 53 knobs; MCP unchanged at 23). NEW `src/x/settings/bandThresholds.ts` is the single server resolver — `routes/replies.ts`'s gate (unmoved in the refuse-before-spend ladder, still before the niche load) and `routes/playbook.ts`'s timeline funnel read it, so the two can't drift onto different subsets. Extension: `serverSettings.ts` gained `band` + `readBand()` (per-key fallback to `BAND` — a half-corrupt blob must not make the badge disagree with the gate) and now value-imports the `../replyBand.ts` shim; `content.ts` resolves the blob itself (`initBandThresholds()` in `start()`, one `storage.local` read + `onChanged` on area `'local'`) and passes it into both `classifyBand` call sites. `dist/content.js` re-verified as a **zero-import IIFE**. **Browser-verify owed** (content-script convention): PATCH `x.band.bigViews` → after one sync a ~500-view tweet's badge downgrades, matching the 422 the route test already proves. See **D133**.
- **prior (GR.10 — closed the GR sub-plan + Wave 3):** docs-sync + `scripts/smoke-guardrails.ts` (the template for smoking a surface whose READS write — snapshot/restore what you can't namespace). Full detail in codemap §9 + the **Wave-3 carry-forward** gotcha below.
- **prior (CA.1 + CA.2 step 1, 2026-07-23, out-of-masterplan, $0):** `pullMentions` now pages to `MENTIONS_HARD_CAP=800` (killed the silent 50/day inbox loss since Jul 14); `xFetch` logs `[cost-audit]` on `includes`-carrying reads. **CA.2 step 2 is still unpaid — see the next-up verification.** Detail in `docs/PHASE-HISTORY.md`.
- **earlier priors (UI.6 back through HV.1):** moved verbatim to `STATE-ARCHIVE.md` (UI.6→UI.2 + A3.15 archived at UI.11; A3.1–A3.14 at A3.15; GR.9→HV.1 at GR.10). Their forward-binding content survives here as the three carry-forward gotchas below.

## Ledger

Status: `[ ]` todo · `[~]` in progress (lane claimed) · `[x]` done (sha + date) · `[s]` skipped (reason in deviations).

**Waves 0–4 are CLOSED — all 105 tasks `[x]`.** Per-task entries (shas, parents, dates, notes) moved verbatim to `STATE-ARCHIVE.md` §"Ledger — Waves 0–4" at UI.11; grep it by task id. Summary:

- **Wave 0 — Foundations (13/13 ✓)**: UI.1, UI.8, UI.9, UI.10, ST.1–ST.9. Closed by `d1902e5` ST.9 studio docs+smoke.
- **Wave 1 — Prompt & identity core (32/32 ✓)**: RU.1–RU.3, N.1–N.9, ME.1–ME.7, AI.1–AI.13. Closed by `f86e06a` AI.13 AI docs+smoke.
- **Wave 2 — Reply machine & on-page surfaces (29/29 ✓)**: RU.4–RU.10, AX.1–AX.7, RL.1–RL.8, NT.1–NT.7. Closed by `5d4a1a4` NT.7 NT docs+smoke.
- **Wave 3 — Ambient data & guardrails (16/16 ✓)**: HV.1–HV.6, GR.1–GR.10. Closed by `docs(guardrails): GR docs-sync + $0 smoke-guardrails.ts (GR.10)`, parent `7adfbca`.
- **Wave 4 — Authoring 3.0 (15/15 ✓)**: A3.1–A3.15. Took migration `0023_slimy_night_thrasher` (`articles`) — the journal is FREE from `0024`. Closed by `docs(authoring): Authoring 3.0 docs-sync + $0 smoke-authoring3.ts (A3.15)`, parent `819c61d`.

### Wave 5 — Settings moat + polish
- [x] UI.2 Doctrine/quests knobs — D2 (xhigh) — subject `feat(settings): doctrine + quest knobs read from registry (UI.2)`, parent `859a923`, 2026-07-24 (no sha by design — D97). **D2 resolved Option B — the 3 niche-owned band keys dropped from `app_settings`; see D128.**
- [x] UI.3 People/followups knobs (high) — subject `feat(settings): people/followups/stage/targets/pinned knobs (UI.3)`, parent `13dd18e`, 2026-07-24 (no sha by design — D97). **targetBand keys DROPPED (niche-owned, 3rd D2 application); see D129.**
- [x] UI.4 Gates/workers knobs + MCP (high) — subject `feat(settings): gate/radar/worker knobs + x_settings MCP tools (UI.4)`, parent `28e0840`, 2026-07-24 (no sha by design — D97). **MCP is now 23 tools (3/15/5); the `src/mcp.test.ts` exact count + `docs/s2-mcp-server.md`'s three counts moved with it.**
- [x] UI.5 Money/AI knobs + ceilings (xhigh) — subject `feat(settings): budget/AI/mention knobs with hard ceilings (UI.5)`, parent `7b3f5aa`, 2026-07-24 (no sha by design — D97). **13 groups / 41 knobs; MCP unchanged at 23. See D131.**
- [x] UI.6 Extension mirror (xhigh) — subject `feat(extension): mirrored server settings blob + first consumers (UI.6)`, parent `398e546`, 2026-07-24 (no sha by design — D97). **The 5 mirrored keys now cross the wire; `DoNext.tsx` deliberately untouched (its keys are UI.12's). See D132.**
- [x] UI.7 Band thresholds e2e (xhigh) — subject `feat(band): configurable thresholds shared server + badge (UI.7)`, parent `b378293`, 2026-07-24 (no sha by design — D97). **12 `x.band.*` keys, all mirrored → the mirrored set is 17; 14 groups / 53 knobs; MCP unchanged at 23. See D133.**
- [ ] UI.11 Settings tab rebuild — D6, D8 (xhigh)
- [ ] UI.12 Today polish (high)
- [ ] UI.13 Composer/Calendar polish (high)
- [ ] UI.14 People/Channels/Voice/Ideas polish (high)
- [ ] UI.15 Replies/Playbook/Harvest/Studio polish (high)
- [ ] UI.16 Content-script + explorer tokens (high)
- [ ] UI.17 UI docs+smoke (high)

## Hot-file locks

A lane claims a file before starting, releases on commit. `owner: —` = free.
**Everything is free — Wave 3 released the last of it.** Rows below are kept only where
the note binds a task still marked `[ ]`; a closed lane's file with nothing to say to
Wave 4/5 has been dropped (grep `STATE-ARCHIVE.md` for its lane if you need the history).

| File | Owner |
|---|---|
| `extension/src/shared/serverSettings.ts` + `sidepanel/serverSettingsHook.ts` + `background.ts::syncServerSettings` | — (UI.6 built them, UI.7 added `band`. **The rule is "nothing that can't be inlined into the content-script IIFE"** (§7.26) — no React, no sidepanel module, nothing with its own deps; "zero imports" was the old proxy for it and UI.7 retired it by value-importing `BAND` from `../replyBand.ts`, the module content.ts already inlines (D133b). The check is `grep -cE '^\s*import[ {]' extension/dist/content.js` == 0 after `bun run build`. The background is the only fetcher AND writer of `settings:server` (§7.24/7.25) — a panel or content-script write is the bug this exists to prevent. Adding a mirrored key = registry def + `ServerConfig` field + `SERVER_DEFAULTS` fallback + the exact-list test in `src/x/settings/registry.test.ts`, now **17 keys**) |
| `extension/src/sidepanel/composerLogic.ts` + `calendarLogic.ts` | — (UI.6 released them. Both now take a trailing defaulted `cfg: CadenceConfig` — `pickAnchors`/`suggestSlotDate`/`suggestBestSlotDate`/`buildWeekBoard`, plus `slotHint(…, minN)`. **`ANCHORS_3`/`ANCHORS_4`/`BEST_TIME_MIN_N`/`CADENCE_DEFAULTS` are projections of `SERVER_DEFAULTS`** — never re-literal a baked number here. **Whatever passes `cfg` to one must pass it to the other**: the Composer's Best-time and the Calendar board's ghost anchors are adjacent surfaces on the same ladder. UI.13 polishes both components) |
| `extension/src/content.ts` | — (**the D9 chain is COMPLETE at A3.3**; **UI.7 released it** — `initBandThresholds()` is the fourth `storage.onChanged` listener in `start()` and every one of them filters `area !== 'local'` first; the module-level `bandThresholds` starts at `SERVER_DEFAULTS.band` and feeds BOTH `classifyBand` call sites (`applyBand`'s badge + the reply-master capture stamp) — a new call site must take it too or the badge and the server gate fork. **UI.16's token polish is the only remaining claimant.** Both ambient-capture gate orders are perf contracts: passive harvest enabled → `/home` → `!isHarvestActive()` → one `findPermalink` → throttle → only THEN `extractArticle`; active-times pathname → `passiveCaptureEnabled` → done/in-flight flags → only THEN `extractActiveTimesSection`) |
| `extension/src/harvester.ts` | — (GR.2 released it; `runHarvest`'s ingest param is a `ship(profile, store, outcome)` step and the mode dispatch lives in `startRun` — **a fourth mode is one branch there**, plus `MODE_NOUN`/`isHarvestMode`/`targetPath`/`startRun` staying exhaustive over the 3-union) |
| `src/db/migrations/` journal (any migration task) | — (A3.11 took `0023_slimy_night_thrasher` (`articles`); next owns `0024`. **Wave 4 has NO migrating tasks left; the journal is free for Wave 5 — but never run two migrating tasks in parallel sessions (journal conflicts)**) |
| `src/x/playbook.ts` + `src/x/routes/playbook.ts` | — (**UI.7** released them: `buildTimelineFunnel`/`deriveTimelineBand` gained a trailing defaulted `thresholds` and `loadTimelineFunnel(minN, thresholds)` passes `bandThresholdsFromSettings()`. **`scoreReplyOutcome`'s fallback classifier deliberately stays on baked `BAND`** — the band crosstab is recalibration EVIDENCE and mostly capture-time stamps, so don't "unify" the two classifiers (D133c; the reasons are comments in the file). UI.4 notes still hold: the route's default gate is now `x.gates.minCellN` from the store [`?minN=` still wins per read] and the guidance loaders read it too — `DEFAULT_MIN_CELL_N` survives only as the registry default + the pure builders' param default. HV.5 note still holds: the pure file's sections run to **§12** — the next `build*` is §13, and any plan text counting sections is off by one from AI.12 onward. UI.15 polishes the panel) |
| `src/x/routes/brief.ts` | — (UI.4 released it: `annotateGaps` gained a 5th trailing param `minN = BEST_TIME_MIN_N` and the route passes `x.gates.bestTimeMinN` — the same key `/metrics/best-times` reads. A3.4 notes still hold: the big `Promise.all` now ends with `loadCommitmentsWithDebt` + `loadGoalsWithPacing` + a $0 `audienceActivity` newest-row select, and the response carries `monitor` / `goals` / `commitments` plus each gap's `audienceScore`. `annotateGaps` takes an optional `audience` grid — own-score-first ordering is unchanged, audience is display-only. **A3.5:** the today select is `['pending','manual','posted','failed']` — a manual slot fills its anchor. **The goals loader WRITES**, so `GET /brief` settles goal statuses on every read — A3.8 and A3.14 both build on this payload; keep it out of any polling loop) |
| `extension/src/sidepanel/Today.tsx` | — (GR.8 released it; `AccountHealthCard` between `PinnedWatchCard` and `TodayPlan`, `GoalsCard` right after the quests block, `DebtLine` inside `QuestsSection`. **A3.8 added the manual-post card here (documented as §2 at A3.15); UI.12 polishes it** and owes `docs/today-tab.md` — now numbered §1–§18 — a re-read) |
| `src/x/routes/calendar.ts` | — (A3.5 released it; `manual` is in `STATUSES`/`WRITABLE_STATUSES`, `mark-posted` sits between PATCH and DELETE, and the URL guard is `pending`-only at both sites (decision 5 — don't "fix" it). **A3.7:** `scheduleWarnings` now fires for `pending` AND `manual` rows and counts both as neighbors (`inArray(['pending','manual'])`), still computed **after** the insert inside a try/catch. **PATCH still does NOT warn** — A3.14 did NOT touch this file (Task 14 is extension-only, D126d); the D105 PATCH-warn item is retired as an A3.14 follow-up and is now just an unclaimed server nicety (the board's ghost-click/→best-slot PATCH gets no schedule-cluster advisory, same as the Composer's edit-schedule). The URL guard stays the only content 4xx) |
| `extension/src/sidepanel/Composer.tsx` | — (GR.6 released it; a `warnings: string[]` state renders amber under the notice line and is set **after** `reset()` because reset clears it — **A3.7 kept that order** (the toggle's `setManualMode(false)` is inside `reset()`, warnings still set after). A3.7 added the API|Manual toggle + `manualMode` state + the module-level pure `nextEditStatus`; **UI.6 added `const server = useServerSettings()` at the top and threads it into `suggestSlotDate`/`suggestBestSlotDate`/`topCellsForWeekday`/`slotHint`/`bestTimeCellScore`** — `Calendar.tsx` does the same into `buildWeekBoard` + `suggestBestSlotDate`; UI.13 polishes both files next and must keep both passing it) |
| `extension/src/sidepanel/Settings.tsx` | — (GR.8 released it; the General view renders `<CommitmentsCard/>` between `NicheCard` and the settings form — a separate `.panel` div, NOT inside the form, because the per-key Save is its own `PUT`. D6 still binds: UI.11 absorbs it) |
| `src/x/routes/replies.ts` + `src/x/replies/prompt.ts` | — (**UI.5** released `replies.ts`: the three `MAX_OUTPUT_TOKENS`/`DEFAULT_TEMPERATURE`/`DEFAULT_REASONING` constants are GONE — the exported `replyLlmDefaults()` reads `x.ai.reply*` per request and both paths use it (`{...replyLlmDefaults(), maxOutputTokens}` in batch, whose cap stays computed). `parseBatchTweets` gained a trailing `maxTweets` param. **UI.7 released it** — the gate is now `classifyBand(gateSignals, bandThresholdsFromSettings())`, still the first thing after body validation and still ahead of the niche/relationship/me/guidance loads; `prompt.ts` was untouched) |
| `src/shared/replyBand.ts` + `src/x/settings/bandThresholds.ts` | — (UI.7 built the pair. `classifyBand`/`deriveTimelineBand`/`buildTimelineFunnel` all take a **trailing defaulted** `thresholds`, so every pre-UI.7 call site and fixture is byte-valid — that default is the regression proof, don't make the param required. The shared module must stay dependency-free (it is inlined into the IIFE): thresholds arrive as an argument, never a store or `chrome.storage` read inside it. `bandThresholdsFromSettings()` is the ONLY server resolver — a second consumer imports it rather than assembling its own subset) |
| `src/x/routes/articles.ts` + `src/x/db/schema.ts::articles` | — (A3.11 built the CRUD + table; A3.12 added `POST /articles/:id/assist` to this SAME route file; **A3.13's Writer page CONSUMES both (shipped)** — it did NOT edit this file (a new `public/writer.html` + a `GET /writer` route on the `explorer` router in `data.ts`). D123 + D124 + D125) |
| `src/x/routes/data.ts` + `public/*.html` | — (A3.13 added `GET /writer` to the `explorer` Hono router [already `app.route('/', explorer)`] + `public/writer.html`; both public shells lazy `Bun.file`-cache. A rename/move of the `explorer` router moves BOTH pages; UI.16's content-script+explorer token polish is the only future claimant on `explorer.html`, and it should re-token `writer.html` too. D125) |
| `post prompt.md` / `reply prompt.md` / `thread prompt.md` / **`article prompt.md`** (+ TS literals) | — (**A3.12 added `article prompt.md`** ↔ `ARTICLE_PROMPT_TEMPLATE`, byte-synced + the `article` registry key via the RL.4 4-file move → 13 keys. All four .md↔literal pairs are byte-synced the same way; edit both together — D124) |
| `src/x/routes/harvest.ts` | — (HV.4 released it; POST runs/rows/passive + GET runs/affinity, still **no `:param` route** — keep it that way. `scripts/smoke-passive-harvest.ts` mounts it over the real DB, so a route rename breaks the smoke as well as the suite) |
| `extension/src/shared/harvest.ts` + `sidepanel/Harvest.tsx` | — (GR.2 released them; nothing before UI.15's polish pass claims them again. Sibling `shared/userCell.ts` is free too) |
| `extension/src/sidepanel/People.tsx` | — (GR.4 released it; the panel opens with a `SubTabs` **Roster \| Following** switch that renders only when no dossier is open — a new People surface goes inside one of the two views, not above the switch. **A3.10 landed `DmBox` ("Draft DM" section after Openers in the dossier); UI.14** polishes the file and owes `docs/people-tab.md` a re-read — the Draft-DM surface was documented at A3.15) |
| `src/x/goals.ts` + `src/x/routes/goals.ts` · `src/x/monitor.ts` + `src/x/routes/monitor.ts` · `src/x/connections.ts` | — (the three GR pure/route pairs, all closed. **Import from them, never re-derive**: `loadGoalsWithPacing`/`loadCommitmentsWithDebt`/`loadFlowCurrents`, `loadMonitorInputs`/`runMonitor`/`worstOf`/`shingleJaccard`, `DAILY_CEILING`/`MARK_LOOKBACK_MS`. Every threshold is an exported opening guess — a recalibration is a one-line diff in the pure file and moves every surface at once) |
| `src/x/digest.ts` + `src/x/routes/digest.ts` · `src/x/quests.ts` · `src/x/routes/me.ts` + `src/x/me/profile.ts` | — (closed at GR.7–GR.9. The scorecard is computed **inside** `buildDigestFacts` (D112a); `QuestInputs.originalsTarget?` is optional so pre-GR.8 streaks stay valid; `GOAL_KINDS` is a 5-union with `missed` in `GOAL_STATUSES`, and `POST /me/goals` is the single goal writer) |

## Deviations & decisions register

Pre-seeded from cross-plan analysis (see MASTERPLAN "Cross-plan adaptations" for full text).
Append D11+ as work reveals divergences from plan text.

**Closed-lane D-entries (137 of them: Waves 0–3, the Wave-4 closer D127, and the whole registry lane D128–D133) live in `STATE-ARCHIVE.md`.** What stays below is only what still binds an OPEN task: the three undischarged pre-seeded adaptations (**D6**/**D8**, both discharging at UI.11; **D7**, standing over every polish pass), the two standing infra notes (**D11** lint-ignore, **D13** store layering), the bookkeeping rule (**D97**), and the closing Wave-3 entry (**D113**/**D113a**) whose (d) and (e) bind every future smoke. D1–D5/D9/D10 were discharged and archived at UI.11. The forward-binding content of the archived lanes survives as the three **carry-forward** gotchas below (Wave-3, Wave-4, Wave-5-registry).

- **D6** (binds all Settings.tsx tasks pre-UI.11): keep feature Settings UI minimal; UI.11 rebuilds and absorbs.
- **D7** (standing): all NEW UI from Wave 1 on uses UI.10 primitives + `--strat-*` tokens; Wave-5 polish passes touch only pre-masterplan tabs.
- **D8** (binds UI.11): prune the "coming soon" manifest to features still unbuilt at that time.
- **D11** (UI.8, standing): `biome.json` `files.ignore` now includes `Stratus Design System` — the DS reference folder (Decision 9, plan out-of-scope) was ~112 of the baseline lint errors. Not in UI.8's plan edit-list but a correct, universal fix; whole-repo lint dropped 119→7. The remaining 7 are pre-existing and NOT in any changed file: 2 a11y in `extension/src/sidepanel/Replies.tsx` (UI.15 territory) + 5 biome-format on generated `src/db/migrations/meta/*.json` (a future infra task could ignore `src/db/migrations/meta`).
- **D13** (UI.1, binds UI.2): the **doctrine group is seeded in full by UI.1** — the 6 keys `x.doctrine.{replyTargetMin,replyTargetMax,weekReplyTargetPct,anchors3,anchors4,ladderSwitchAt}`, with the anchors' sorted-unique+range numberArray validation already in place (Task 2's plan attributed that hook to itself; landed early because UI.1 seeds the anchors so it must validate them). **UI.2 must NOT re-declare these doctrine keys** — it only adds the `quests`+`display` groups and wires the consumers (brief quota/ratio/gaps, composer ladder). Store layering (also binds AI.2/D1): `src/settings/store.ts` is platform-agnostic and never imports `src/x/*`; the X registry `src/x/settings/registry.ts` implements the store's `SettingsRegistry` interface and exposes the bound `getSetting`/`resolveSetting`/`getAllValues`/`setSettings`/`resetSettings` — **UI.2+ consumers import those from `../settings/registry.ts`, never the store directly**. biome.json now also ignores `src/db/migrations` (fulfils D11's anticipated infra ignore; the 5 generated-JSON lint errors are gone → whole-repo lint = 2, both pre-existing `Replies.tsx` a11y).
- **D97** (HV.5, bookkeeping — supersedes the sha half of D93a/D94a/D95a): **the ledger records the commit SUBJECT LINE, not a sha.** Three tasks in a row wasted a Step-0 investigation proving a recorded sha was an amend-orphan, and every time the resolution was "the subject line is the identity". A sha can only be written into the commit that changes it by amending, which changes it again — the churn is structural, not carelessness. From HV.5 on: ledger entries carry the subject line + the PARENT sha (stable, already in history), the codemap header stamps `<parent>+<TASK>`, and Step 0 is `git log -1 --format='%h %s'` compared against the last ticked subject. No reconciliation, no orphan hunts.
- **D113** (GR.10): **four places Task 10's text and the repo disagreed, all resolved in the repo's favour, plus one thing the plan could not have known.** **(a) The phase entry went to `docs/PHASE-HISTORY.md`, not CLAUDE.md** (D93a — Task 10 still says CLAUDE.md), and the `PLAN.md` note is a **top blockquote** above §"Product, in one paragraph each", not a §"Phased build" edit (D98b, the RU.10/HV.6 exemplar). **(b) The docs bill was nine items, not the plan's four.** Task 10 lists people/today/harvest/"settings doc if present"; GR.6 also changed the Composer, GR.7 changed the Me tab's goal kinds, and `docs/s2-mcp-server.md` had been wrong about the tool count since **N0.2** (16 in three places; it is 21, and both tier counts were stale too — curated 10→14, write 3→4). A docs-sync task inherits every stale count in the files it touches, not just its own lane's. **(c) `docs/today-tab.md` needed a RENUMBER, not an append.** The Goals card renders after the quests block and Account health after Pinned watch, so the doc's numbered §1–§15 became §1–§17 with two inserts. Verified against `Today.tsx`'s actual JSX order rather than the plan's prose. **(d) The smoke could NOT follow `smoke-passive-harvest.ts` as written.** D98c's rule is namespace-then-delete-what-we-made, which assumes every write is attributable. Two Guardrails writes are **global**: a complete-run reconcile marks EVERY unseen live handle `gone`, and `GET /following/queue` releases rows at read time. So the smoke additionally **snapshots and restores** every foreign `following.status` (from the success path and from `fail()`), plus `commitments` and the seeded `streaks`/`digests` keys — and, because its burst fixtures sit inside today's window while `GET /x/brief` writes the C9 diary, it closes by **re-reading the brief** so the diary is recomputed rather than left describing fixtures. **Any future smoke that exercises a read-that-writes must copy this, not D98c alone.** **(e) A fixture fact the plan's sequence hides:** "unseen" in the reconcile is `last_run_id != runId`, i.e. per **run**, not per batch — a handle shipped in batch 1 and omitted from batch 2 of the SAME run is still seen. The smoke needs **two** runs to demonstrate `gone`/`confirmed`.
- **D113a** (GR.10, bookkeeping): the archive pass moved **20 D-entries (D93–D112, D97 excepted)**, **16 gotchas (HV.1 → GR.9)** and **7 header priors (GR.4 → GR.9)** into `STATE-ARCHIVE.md` verbatim. STATE.md went ~128 KB → **~50 KB** (one `Read` again); the archive is now ~350 KB (grep-only by design). What was deliberately kept live out of the closed lanes: the **carry-forward gotcha** below (the four HV/GR facts that still bind an open task) and nothing else.

## Gotchas log

Things the next implementer must know that aren't obvious from the code. Append-only,
one line each, newest last.

**Closed-lane gotchas (154 of them: Waves 0–3, plus the eleven archived at UI.11) live in `STATE-ARCHIVE.md` — grep it by task id or filename before touching a file a closed lane built.** What stays below: standing test/lint/typecheck traps, the browser-verification debt Wave 5 must clear, and **three** carry-forward entries (Wave-3, Wave-4, Wave-5-registry) holding the closed lanes' facts that still bind an open task — consolidated rather than left as forty separate lines.

- **UI.9 light theme is an OPENING-GUESS palette, unverified in a browser.** Automated gates pass (632 pass / 0 fail incl. 6 new storage tests, typecheck + build + changed-file lint green) but the 12-tabs × {dark,light} × {cozy,compact} visual QA matrix is explicitly manual and deferred to the Wave-5 polish passes (UI.12–16 done-whens). Known trap (plan risk note): AA contrast on warn/ok/band **text over tinted fills** in light — the fill tokens and semantic ink were re-authored dark-enough to double as text (`--strat-warn: #9a6a10`, band-hot `rgb(0,150,100)`, band-warm-text `#8a6100`), but spot-check before trusting. Adjust `:root[data-theme='light']` values, not the consuming rules.
- **Settings types are extension-local mirrors** — `SettingEntry`/`SettingsGroup`/`SettingsResponse`/`SettingsPatchResult`/`SettingsResetResult` in `extension/src/shared/types.ts` (re-exported via api.ts), mirroring the `GET /x/settings` JSON. The extension MUST NOT import `src/x/settings/registry.ts` (§5 build-isolation). **Binds UI.2/UI.11:** if the server `SettingDef` shape gains a field the editor needs, hand-sync it into `SettingEntry`.
- **`exactOptionalPropertyTypes` pass-through pattern (UI.10):** primitives forwarding a maybe-undefined optional (`Slider.unit`, `SettingRow.onReset`, `GearPopover.onReset`/`label`) type the prop as `T | undefined` explicitly — else TS rejects `prop={maybeUndefined}`. Reuse this for new primitives that relay optionals.
- **UI.10 rail regrouping is UNVERIFIED IN A BROWSER** (like UI.9's palette). Extension typecheck + Vite build + changed-file lint are green and the CSS compiles, but the visual QA (grouped rail spacing, brandmark, EmptyState, popover) rides the Wave-5 polish passes (UI.12–16 done-whens), per D7/D14.
- **`inspect.test.ts` fails 2/2 under bare `bun test`, GREEN under `bun run test` (the canonical, package.json script `SQLITE_PATH=:memory: bun test`).** Introduced by `906f2d3` ("eval 1", out-of-skill Wave-0 audit): `src/db/client.ts` defaults to `:memory:` when `NODE_ENV==='test'` (Bun sets it), but `src/x/data/inspect.ts:101`'s readonly connection still defaults to `./stratus.db` when `SQLITE_PATH` is unset — so under bare `bun test` the primary writes `:memory:` while the inspect readonly reads the file → seeded `people` rows invisible → the 2 LIKE-query tests get `[]`. **NOT RU.3 (bisected: 0 fail at `d1902e5`/ST.9, 2 fail from `906f2d3` onward; RU.3 touches none of inspect.ts/client.ts/the people table).** Full suite is **732/0 under `bun run test`** — always gate with `bun run test`, not bare `bun test`. One-line fix for a data-core/S1 task: make `inspect.ts` honor the same `NODE_ENV==='test'`→`:memory:` default as `client.ts` (or `client.ts` should also `process.env.SQLITE_PATH ??=` set it so both agree).
- **RL.4: adding a registry key is a 4-file move.** `PROMPT_KEYS` + `PROMPT_SPECS` in `prompts/registry.ts`, the default exported from a **PURE** module (a route import cycles — AI.5's `voice/extractPrompt.ts` move exists for that reason), `registry.test.ts`'s key-list + defaultBody assertions, and `docs/settings-tab.md`'s prompt-count copy (now **11**). The extension needs nothing — the Prompts panel renders whatever `GET /x/prompts` returns. CLAUDE.md's AI-layer entry still says "10 keys"; that is a historical phase record and stays — **RL.8's phase entry is where the 11th key gets documented.**
- **RL.5 is UNVERIFIED IN A BROWSER**, like every panel component since UI.9 — no tests were added (repo convention: panels are untested, logic lives server-side). Automated gates only: root+extension typecheck, `cd extension && bun run build`, changed-file lint, full suite 1087/0. The plan's live done-whens (create → add → test-render → generate → Overwrite) ride RL.8's smoke + the Wave-5 QA pass. The one server-behavior assumption the UI leans on was **not** left to faith — the `{humanizer:{}}` → defaults round-trip was proven with a throwaway route test (D81b).
- **NT.1: extension typecheck does NOT cover `*.test.ts`** — `extension/tsconfig.app.json` has `"exclude": ["src/**/*.test.ts"]`, and root `tsconfig.json` only includes `src/**/*` + `scripts/**/*` + `drizzle.config.ts`. So an extension test file is checked by **`bun run test` alone**; a type error in one is invisible to both typechecks. (Same for `earlyReplies.test.ts` — pre-existing, just never written down.)
- **NT.7: `app.request(...)` is typed `Response | Promise<Response>`.** A helper declared `function f(): Promise<Response> { return app.request(...) }` fails root typecheck; make the helper `async`. `bun run typecheck` covers `scripts/**`, so this bites in smoke scripts, not in route suites (which `await` inline).
- **CLAUDE.md was slimmed to guardrails-only (2026-07-23, out-of-skill docs refactor).** The full phase-entry ledger moved **verbatim** to `docs/PHASE-HISTORY.md`. **Every docs-sync task (MASTERPLAN global rule 6, codemap §7 rule 29 — updated) now appends its phase entry to `docs/PHASE-HISTORY.md`, NOT CLAUDE.md**; CLAUDE.md changes only when a guardrail (invariant, workflow, stack quirk) changes. Plan text still saying "CLAUDE.md phase entry" means the history file.
- **STATE.md was compacted 2026-07-23 (out-of-skill housekeeping, no code touched).** It had reached **339 KB** — past the `Read` tool's 256 KB cap, so Step 0's "read STATE.md fully" silently became a chunked-read investigation every session. 96 closed-wave D-entries + 127 closed-wave gotchas + the header's HV/GR prior paragraphs moved **verbatim** into the new `STATE-ARCHIVE.md` (frozen, 269 KB, grep-only — bigger than one `Read` by design); the live file is **74 KB**. The split was generated by line number, not retyped, and verified as a set operation: every non-empty original line lands in **exactly one** of the two files (438/438, zero duplicated). **What this means for you:** a task touching a file Waves 0–2 built should `grep STATE-ARCHIVE.md` for the task id or filename before assuming the live file tells the whole story — e.g. the RL `/use` contract (D79), the NT.5 icon prefixes (D89), the ST registry contract. SKILL.md gained the matching archive rule in Step 6 (a lane's docs-sync task archives that lane) and its Step-0 staleness guard now compares the **subject line**, matching D97 — it still said `--format=%h` five sessions after shas stopped being recorded.

- **Wave-3 carry-forward (the only HV/GR facts that still bind an open task — everything else is in the archive):** **(1) MCP tool count is asserted exactly.** `src/mcp.test.ts` asserts **23** (UI.4 took it 21→23 with `x_settings`+`x_update_setting`); every future tool bumps that number *and* `docs/s2-mcp-server.md`'s three counts (total / curated tier / write tier) **in the same commit** — that doc was silently 5 tools stale for four phases. **(2) Docs now assert user-visible strings, and two Wave-5 tasks own them.** `docs/harvest-tab.md`, `docs/playbook-tab.md`, `docs/people-tab.md`, `docs/today-tab.md`, `docs/composer-tab.md` and `docs/settings-tab.md` quote real copy (the both-off warn line, `Passive: N rows today`, `insufficient data (n=…)`, `Nobody has shown up often enough yet.`, the three schedule-warning sentences, the debt-tier wording, the `Nd left · need X/day · doing Y/day` line). **UI.14 (People/Channels/Voice/Ideas) and UI.15 (Replies/Playbook/Harvest/Studio) must re-read the matching `docs/*-tab.md` in their own task**, and `docs/today-tab.md` is now numbered §1–§17 — an inserted card renumbers, it does not append. `docs/settings-tab.md` still says **four toggles** in two places: UI.2/UI.11's settings rebuild has to move that count, not just add a row. **(3) Real-DB fixture rules that outlive their lane:** a seeded `posts_published` row is written **`retired: true`** (NT.7 — otherwise it is a candidate for the daily *billed* pass), a `following` fixture needs a `following_runs` row first (`last_run_id` is a notNull FK), people-layer handles must be **≤15 chars** (longer ones are silently skipped and the assertions go vacuous), and any suite touching `scheduled_posts`/`posts_published` inside the monitor's windows must delete its rows in `afterAll` — `monitor.test.ts` asserts `clusterCount === baseline + 1` and a leaked pending pair breaks a different file. **(4) Five reads WRITE and none of them may be polled:** `/radar/drafts` (48h expiry), `/following/queue` (release + revoke), `/x/goals`, `/x/brief` and `/x/digest` (all three settle `active → achieved|missed`). A3.8's Today card and A3.14's week board both sit on `/x/brief` — a refresh loop there silently advances goal statuses.
- **GR.10: the smoke that closes Wave 3 is the template for smoking a surface whose READS write.** `scripts/smoke-guardrails.ts` ($0, no `--live`, rerunnable, verified to leave 0 rows) mounts following + monitor + goals + me + calendar + brief + digest over the real `./stratus.db`. **Copy its safety shape, not `smoke-passive-harvest.ts`'s, whenever a route mutates rows it did not create:** namespaced ids cover only what you wrote, so it *additionally* snapshots every foreign `following.status` before the first write and restores it from the success path **and** from `fail()` (a complete-run reconcile marks every unseen live handle `gone`; a queue read releases rows), snapshot-restores `commitments` and the seeded `streaks`/`digests` keys rather than deleting them, and **closes with one more `GET /x/brief`** so today's C9 streak row is recomputed from clean data instead of describing the burst fixtures it just deleted. **Three things it discovered that no plan text says:** (a) reconcile "unseen" is per **run** (`last_run_id`), not per batch — proving `gone`/`confirmed` needs TWO runs; (b) the queue needs ≥3 slots of budget for its assertions, so the script fails loudly naming `windowUsed/windowCap` + `dailyUsed/dailyCeiling` rather than through a confusing count mismatch; (c) `?factsOnly=true` returns **before** the digest cache write, so a scorecard read caches nothing — but it still settles goals. Assertions over shared/real data are **baseline-relative** where they must be (`nearDuplicate.pairCount`, `scheduleCluster.clusterCount`) and exact where the fixture owns the whole population (`questAdherence`, the queue batch, the ledger statuses).

- **Wave-4 carry-forward (the A3 facts that still bind an open Wave-5 task — the per-task A3 gotchas + D114–D126 + the A3.1–A3.14 header priors are in `STATE-ARCHIVE.md` §"Wave 4", grep by task id):** **(1) Browser-verify debt owed, all panel-convention 2-minute user checks (deploy + chrome://extensions reload), cleared by the Wave-5 polish passes:** A3.3 active-times end-to-end row-landing + the passiveCapture-off gate; A3.4 the Composer audience-peak/staleness/blended-Best-time render; A3.7/A3.8 the manual-publish UI (Composer API|Manual toggle, Calendar overdue tint + Mark posted, the alarm→notification→card round-trip); A3.10 the dossier Draft-DM flow + the 422 thin-dossier line; A3.13 the Writer page (create→outline→section→autosave-survives-reload→**Copy-for-X rich-vs-plain**, the risk item — record which clipboard path the prod host accepts); A3.14 the week board (7 columns, draft→slot in two clicks, chips/overdue). **(2) Wave-5 polish ownership:** UI.12 (Today) owns the A3.8 manual card + a `docs/today-tab.md` (now §1–§18) re-read; UI.13 (Composer/Calendar) owns the A3.7 toggle + the A3.14 board (**`Calendar.tsx` is a thin renderer over the pure `calendarLogic.ts` — put board logic THERE**; it reuses the Composer's cadence/best-times helpers by import) + `docs/composer-tab.md`/`docs/calendar-tab.md`; UI.14 (People) owns the A3.10 Draft-DM box + `docs/people-tab.md`; UI.16 (content-script + explorer tokens) owns the A3.3 active-times capture in `content.ts` AND must re-token `public/writer.html` in lockstep with the explorer's `stratus_explorer_token` key (A3.13 — the two public pages share it). **(3) Standing A3 facts:** `article prompt.md`↔`ARTICLE_PROMPT_TEMPLATE` byte-sync joins post/reply/thread/dm as an edit-both-together pair (13-key registry); `mark-posted`/manual reminders render off `status`, NEVER `postedTweetId` (the daily reconcile links the tweet id); the calendar URL guard is `pending`-only (manual = the $0 link path) and articles never hit the API; `dm_drafts`/`articles` PATCH is SELECT-then-guard with terminal-status 409s; `suggestBestSlotDate`/`annotateGaps` take `audience` as a trailing optional arg (no grid ⇒ byte-identical to pre-A3.4); calendar-fixture day bands 90–130d (GR.6) / 150–160d (A3.5) / ~200d (brief) / ~300d (A3.7) are spoken for — a new suite picks a fresh band ≥45 min clear.

- **Wave-5 registry carry-forward (the UI.2–UI.7 facts that still bind an open task — the five per-task gotcha blocks + D128–D133 are in `STATE-ARCHIVE.md` §"Archived at UI.11", grep by task id):** **(1) The store is process-global across test files.** One in-memory `app_settings`, one module cache — any test that `setSettings(...)` MUST reset in `afterEach`/`afterAll` (`resetSettings({group})` or `{keys}`), and keeping a suite's PATCH assertions inside ONE registry group means a single group-reset cleans them. **(2) A knob's blast radius can exceed its caller.** PATCHing a knob whose consumer is a global read-that-writes (the `/radar/drafts` TTL expires *every* stale row) means seeding your own fixture and only ever tightening the window; a smoke does it by snapshot-restoring `resolveSetting(key)` by `isDefault`, never `resetSettings` (which DELETES an override the operator set on purpose) — and hangs the restore off `fail()` too, since `fail()` exits. **(3) An env-backed registry default is read ONCE, at registry import** — a runtime `process.env.X = …` in a test or script is a silent no-op that still looks like it works. Steer through `setSettings`. **(4) `appliesOn` is behavior, not copy:** `'restart'` **only** where reading later can't take effect (the knob armed a timer — just `x.workers.dailyMetricsHourUtc`/`publisherIntervalSec`); everything else reads per request/run even inside a worker, and **no money knob is ever restart-scoped**. Getting this backwards makes a knob look broken. **(5) The `?param=` vs knob contract:** a query param overrides the configured baseline **per read**, in both directions (`?minN=20` re-tightens a store-loosened gate) — assert both, as `playbook.test.ts` does. **(6) A knob whose LOOSENING direction spends can only be route-tested on the refusal side** — `/x/replies/generate` mounts whenever any LLM key is configured and `.env` has one, so "PATCH → it gets through" runs a real paid Grok call inside `bun run test`. Assert a tightening that 422s, and prove the store was read by picking a context that refuses under BOTH settings so the reported `band` changes; the permissive direction belongs in the pure suite. **(7) Adding a `scope:'mirrored'` key is a 2-file move** — the registry def, then a `ServerConfig` field + `SERVER_DEFAULTS` fallback in `extension/src/shared/serverSettings.ts`; `registry.test.ts` asserts the mirrored list EXACTLY (**17 keys**) so the second file can't be forgotten (without it the key ships in the blob and is never read — no type error, no test failure). **(8) A cap arriving asynchronously must be applied to a SPENT counter, not snapshotted as REMAINING** — any mirrored knob feeding an initial `useState` has the `Conversations.tsx` trap. **(9) The AI layer has TWO legitimate tiers and they are not duplicates:** `x.ai.*` (per-surface house default) and the `ai` blob from Settings → AI (global override, `null` = defer). "Unifying" them silently drops the per-surface knobs out of the merge. **(10) Two infra facts:** the `.claude/skills/stratus/scripts/` files are linted but NOT typechecked (root `tsconfig.json` covers the repo-root `scripts/`, not the skill's) — verified only by running them; and Bun auto-loads `.env` into `process.env`, so `env -u API_TOKEN bun run …` does NOT unset it (the skill script's `STRATUS_API_TOKEN ?? API_TOKEN` mapping exists for that).

## Planning-error log

Corrections to MASTERPLAN.md itself (wrong dep, wrong order discovered live).

- **2026-07-16-niche.md Task 3 done-when** — the "`Pitești` hits nothing" grep contradicts the same task's bit-for-bit equivalence requirement (post prompt §6/§9/§0 carry builder biography outside the §1/§5 extraction scope; no niche field exists to hold them). Resolved at N.3 in favor of equivalence; see D28b for the full resolution + what N.9 must scope differently. Source plans are never edited — this entry is the correction.
- **2026-07-17-ai-layer.md — no task owns the post drafter's askLLM migration.** Task 5's edit list names replies/pillars/playbook/voiceExtract; Task 6 only flips drafter's MOUNT gate; Task 7 adds a sibling route. Yet the plan's Why-section + Done-when #1 spirit require every draft surface to honor the provider setting, and the Task-6 gate flip would 502 `/posts/draft` on OpenRouter-only setups. Resolved at AI.5 by migrating `drafter.ts` there (D47b) — the call-site-migration task was the right home.

