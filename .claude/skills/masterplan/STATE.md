# Masterplan execution state

> Dynamic memory for `/masterplan`. Updated after EVERY task, in the task's own commit.
> Plan: `plans/MASTERPLAN.md` (static — order, reasoning levels, waves, D1–D10).
> Codemap: `.claude/skills/plan-feature/references/codemap.md` (updated per task too).
> History: `.claude/skills/masterplan/STATE-ARCHIVE.md` (frozen, grep-only — closed-wave deviations + gotchas).

**Size discipline (added 2026-07-23 at 339 KB; re-applied at GR.10, 128 KB → ~47 KB):** STATE.md must stay loadable in a single `Read`. What lives here is (a) standing rules, (b) entries for the OPEN lane / current wave, (c) unpaid verification debt, (d) the ledger + locks. When a sub-plan closes, its D-entries and gotchas move verbatim to `STATE-ARCHIVE.md` — that move is part of the sub-plan's docs-sync task, not a separate chore.

- **last-commit:** **identity is the SUBJECT LINE — no sha is recorded any more (D97).** HEAD should read `feat(extension): manual-post alarms, notification and Today publish card (A3.8)`, whose parent is `49a05e7` = A3.7 `feat(extension): manual publish mode in Composer + calendar/today chips (A3.7)`, whose parent is `03a3b7a` = A3.6 `feat(metrics): manual-post reconcile links pasted tweets to calendar rows (A3.6)`, whose parent is `1053f5d` = A3.5 `feat(calendar): manual publish status + mark-posted transition (A3.5)`, whose parent is `8d499fb` = A3.4 `feat(composer): audience-blended best-slot ranking + staleness note (A3.4)`, whose parent is `5bb88e2` = A3.3 `feat(extension): passive active-times capture on X Analytics (A3.3)`, whose parent is `cf2f312` = A3.2 `feat(analytics): audience_activity capture table + /x/analytics/active-times routes (A3.2)`, whose parent is `d4869ab` = A3.1 `feat(extension): active-times heatmap parser (shared, fixture-tested) (A3.1)`, whose parent is `49fcd1d` **`wave 3 done`** — an out-of-skill user commit (post-GR.10 fixup: `POST /me/goals` gained `deadline_in_past` + `target_not_above_baseline` 400 guards, +goals-route tests, smoke-me/smoke-guardrails tweaks, one docs/me-tab.md line; no ledger drift — reconciled at A3.1's Step 0). Behind it: `0e891b0` = GR.10 `docs(guardrails): GR docs-sync + $0 smoke-guardrails.ts (GR.10)`, whose parent is `7adfbca` **`feat(goals): weekly scorecard in digest facts + card (GR.9)`**. Behind that, in order: `b59a564` **`ideas`** (an out-of-skill commit adding ONLY `crazy_ideas.md` — no code, no ledger drift), GR.8, GR.7, GR.6, GR.5, GR.4, `55c6d19` **`chore(masterplan): compact STATE.md into a frozen STATE-ARCHIVE.md`** (out-of-skill housekeeping), then the two out-of-masterplan **CA** commits (plan `plans/2026-07-23-cost-audit-followups.md`, executed 2026-07-23 at explicit user direction and deployed the same day), whose parent is `30b0583` (= GR.3). **Step 0 is one command:** `git log -1 --format='%h %s'` — if the subject names the last ticked task (or one of the two out-of-skill lines above), there was no out-of-skill work.
- **current wave:** **WAVE 4 (Authoring 3.0) IS OPEN — A3.1 ✓, A3.2 ✓, A3.3 ✓, A3.4 ✓, A3.5 ✓, A3.6 ✓, A3.7 ✓, A3.8 ✓.** Waves 0–3 closed (RU.1–10✓ AX.1–7✓ RL.1–8✓ NT.1–7✓ HV.1–6✓ GR.1–10✓). No locked files anywhere. **The migration journal is FREE** — A3.2 took `0021_square_iron_monger`; the next migrating task owns `0022`, and Wave 4 has **two** migrating tasks left (A3.9 DM drafts, A3.11 articles) that **must serialize** — never run two of them in parallel sessions. **The D9 `content.ts` chain is COMPLETE** [AX.3✓→AX.5✓→RU.7✓→RU.8✓→NT.5✓→HV.2✓→A3.3✓]; nothing before UI.16's token polish claims the file again. **The A3.2→A3.4 audience chain is now consumed** — `audienceScoreFor` reads a stored capture in both `brief.ts` (gap `audienceScore`) and the Composer (blended best-slot); A3.14's week board will lean on the same brief payload. **D2 (UI.2, Wave 5) still pending decision** — the 5 doctrine knobs are owned by `niches.doctrine` since N.5; UI.2 must not duplicate them in `app_settings` (D30c). **D5 binds A3.9 + A3.12** (new LLM calls go through `askLLM` + `llmConfigured()`, and each new prompt registers a registry key — `dm`, `article`; see the RL.4 gotcha for the 4-file move that entails). **D93a is standing for A3.15 and UI.17:** phase entries go to `docs/PHASE-HISTORY.md`, never CLAUDE.md (codemap §7.29).
- **next-up:** **A3.9** (DM drafts — table, grounding reuse, routes; xhigh, dep-free, **migration `0022`**, D5-bound) OR **A3.11** (Articles CRUD, migration) — **the two remaining Wave-4 migrating tasks MUST serialize on the journal; never run them in parallel sessions**, and each registers a registry key (`dm`/`article`, the RL.4 4-file move). **A3.14** (Calendar week board, xhigh) is ALSO unblocked now that its deps A3.4✓+A3.5✓+A3.7✓ are all in — it owns `calendar.ts`/`Calendar.tsx` next, sits on the **same `GET /brief` payload** (do NOT poll it — the goals loader writes), and reuses A3.7's `.badge-manual`/`.overdue`/`.mark-posted-btn` + A3.8's `manual:due` Today card affordances. **A3.8 shipped** the C7-mirrored manual-alarm machinery ($0, extension-only, 8 files): `background.ts` alarms a `manual` row at its slot (no grace/retry — a sibling `syncManualAlarms()` beside `syncLaunchAlarms()`, own `manualChain`), a `manual:due` session entry drives the new `ManualPostCard` in Today (**renders off `status`, never `postedTweetId`** — the A3.5 gotcha), and Mark posted goes through `api.markPosted` + `stratus/manual-dismiss`; **its alarm/notification/card round-trip is browser-verify owed** (panel convention, same debt shape as A3.7's chips — see D120). **D117 was DISCHARGED at A3.7** — it shipped the server follow-through (`scheduleWarnings` + `loadMonitorInputs` both treat `manual` as a first-class scheduled slot; see D119). **Migration lane starters still pending & mutually exclusive on the journal: A3.9 (DM drafts, migration `0022`) and A3.11 (articles, migration) must never run in parallel sessions** (D5 binds both — each registers a registry key `dm`/`article`, the RL.4 4-file move). A3.14 (week board, xhigh, dep A3.4+A3.5+**A3.7**✓) comes last of the calendar surfaces and sits on the **same `GET /brief` payload** — do not poll it (the goals loader writes); it also owns `calendar.ts`/`Calendar.tsx` next and can lean on A3.7's `.badge-manual`/`.overdue`/mark-posted affordances. **Two pieces of browser-verify debt are owed (both 2-minute user checks, not code risk):** (a) **A3.3** end-to-end row-landing (visit Audience → row in `audience_activity`; revisit <12h → no new row; toggle passiveCapture off → no capture) needs `./scripts/deploy.sh` + a chrome://extensions reload; (b) **A3.4** the Composer's audience-peak line, staleness nudge, and blended "Best time" pick need the loaded extension against a server that has an `audience_activity` row — the ranking/label logic is fully unit-tested, only the on-screen render is unverified. **One pending CA.2 verification still rides ahead of everything and is NOT a masterplan task:** grep the prod journal for `[cost-audit]` (`journalctl -u stratus | grep cost-audit`) — if the `includes.tweets` counts ≈ the console-vs-`/cost/today` gap, execute **CA.2 step 2** (preferred: drop `referenced_tweets.id` from the metrics-read expansions in `fields.ts`; fallback: bill `data.length + includes.tweets.length` in `itemCount`) and write the confirm one-liner into `docs/PHASE-HISTORY.md`; if refuted, write the refute one-liner and remove the instrumentation. **Gate reminders (unchanged):** canonical test command is `bun run test` (bare `bun test` targets the file DB); biome forbids `delete process.env.X` and `: any` in `scripts/`; biome **sorts imports case-sensitively**, **collapses short chained calls onto one line**, and rejects string concatenation in a `console.log` unless **every** operand is a template literal that actually interpolates (a plain-quoted half trips `useTemplate`/`noUnusedTemplateLiteral`; `smoke-passive-harvest.ts` is the shape that passes) — run `bunx biome check --write` (and `--fix --unsafe` for the string-style rules) on changed files rather than hand-formatting. Extension-touching tasks add `cd extension && bun run build` **and** `cd extension && bun run typecheck` to the bar.
- **prior (A3.8 — $0, extension-only, no schema/migration/route/MCP change; suite 1397→1411, 8 files):** the manual-publish reminder. **The plan's 6-file Task 8 became 8** — `api.ts` was a **no-op** (A3.7 already landed `api.markPosted` top-level), and the count picked up `messages.test.ts` (guard) + `styles.css` (the card). Pure core in `extension/src/shared/launch.ts` beside the C7 launch core: `computeManualAlarms(posts, nowMs)` (filter `status==='manual'`+valid `scheduledFor`, fire AT the minute — **no `LAUNCH_GRACE_MS`, no retry alarms**, there's no publisher tick to wait on; a ≤`MANUAL_MISS_GRACE_MS`=30 min miss clamps to `now+1s`, older drops → the Calendar/Today overdue chip owns it), `parseManualAlarm`/`MANUAL_ALARM_PREFIX='stratus-manual:'` (**no collision** with `stratus-launch:`/`stratus-launch-retry:`), `mergeManualDue(existing, entry, cap=5)` (prepend newest-first, **dedup by postId** so a re-fire refreshes-not-stacks), `manualCardVisible` (`MANUAL_CARD_TTL_MS`=60 min), `manualNotificationText` (`Time to post: «…»` ≤60), `ManualDue`+`isManualDueList`, `MANUAL_DUE_KEY='manual:due'`, `MANUAL_DUE_CAP`. `background.ts`: **a sibling `syncManualAlarms()`** (NOT an extension of `syncLaunchAlarms`) fetching `?status=manual&from=now−30min&to=now+24h` with a **scoped-prefix** alarm clear, called alongside the launch sync at all three triggers (module-load, `stratus/launch-sync`, the 15-min `LAUNCH_SYNC_ALARM`); its **own `manualChain`/`enqueueManual`** (a distinct session-key family — the radar/launch/openPerson per-family-chain pattern, §7.24); `handleManualFire(postId)` re-GETs the row and **only if still `manual`** writes the due entry (`mergeManualDue`) + fires a `stratus-manual-note:<id>` notification — posted/cancelled/edited-away drops silently, a failed GET is best-effort (next sync re-creates within the miss window); `parseManualAlarm` dispatched in `onAlarm` **before** `parseLaunchAlarm`; the notification-click handler widened to open the panel for the manual prefix too. `messages.ts`: `ManualDismiss {type:'stratus/manual-dismiss', postId}` + `isManualDismiss` (carries a postId — several cards can be due, unlike the param-less `LaunchDismiss`). Panel: NEW `ManualPostCard.tsx` (`ManualPostCardSection`) mounted right after `<LaunchRoomSection>` in `Today.tsx`, **reader-only** (§7.24 — it never writes `manual:due`; Mark posted/dismiss route through the background): one `.manual-card` (warn-toned, launch-room shape) per visible entry with full text, an amber `media_note` line, Copy, "Open X compose" (`x.com/compose/post`), and **Mark posted** → `api.markPosted` then `stratus/manual-dismiss`. Tests: +12 `launch.test.ts` (manual alarm matrix, merge dedup/cap, visibility, notif text, `parseManualAlarm`, `isManualDueList`) + 2 `messages.test.ts`. **Owed:** the alarm/notification/card round-trip is panel-convention browser-verify (schedule a manual row ~2 min out → notification fires → card Copy → Mark posted clears it); needs `./scripts/deploy.sh` + a chrome://extensions reload.
- **prior (A3.7 — $0, no schema/migration/MCP change; suite 1395→1397, 11 files):** manual publishing reaches the UI. **The plan (Task 7) is extension-only 5 files; D117 expanded it to 11** by adding the server follow-through it earmarked (D119). Pure `composerLogic.estimatePostCostUsd` gained a `manual?` short-circuit (`{usd:0, note:'manual paste'}`) + a co-located test. Composer: an **API|Manual segmented toggle** (`isSinglePost` only — the toggle NEVER renders in either thread branch, so manual+thread is structurally impossible; threads reject manual server-side anyway, decision 7), `manualMode` state auto-on when editing a `manual` row OR one carrying a `media_note` (a Studio visual must ship by hand), a hard **scheduled-time requirement** (guarded in `submit` before the round-trip AND folded into `canSubmit`), `status:'manual'` on create, and the new **module-level pure `nextEditStatus(cur, manual, hasTime)`** for edit transitions (only draft/pending/manual participate; posted/publishing are edit-locked, cancelled/failed keep status unless re-scheduled the API way; returns `'draft'|'pending'|'manual'|null`); the cost line becomes `$0 · you paste it` and the URL warning is suppressed in manual mode. **`warnings`-after-`reset()` order preserved** (GR.6 lock — untouched). `api.markPosted(id)` is **top-level on `api`, NOT the plan's `posts.markPosted`** (there is no `posts` namespace — the scheduled-post methods hang off `api` directly). Calendar: styled `.badge-manual` pill (title=paste hint) + an `.overdue` tint (`manual && scheduledFor<now`) + a **Mark posted** button rendered as a **sibling** to the row `<button>` (can't nest buttons) → `api.markPosted` then reload (status drives chips server-side; the reconcile links the tweet id later). Today TodayPlan: paste-hint title on the manual pill — **no second chip**, because A3.5 already gave `.badge-manual` CSS + widened the brief today-select, so the styled status pill IS the chip (adding a "manual" text chip beside a "manual" status pill would double the word). **D117 server follow-through:** `calendar.ts::scheduleWarnings` now fires for `manual` as well as `pending` and counts manual neighbors (`inArray(['pending','manual'])`, wording `pending post`→`scheduled post`); `monitor.ts::loadMonitorInputs`' slot select widened identically, `scheduleCluster` message `pending pair`→`scheduled pair`. Both wording changes were safe — no test asserted the old strings (`calendar.test.ts` matches `within`/`20 min away`; `brief.test.ts` checks only `severity`/`typeof message`). +1 calendar route test (a manual slot 20 min after a pending one warns — proves both fire and see each other; fixtures ~300 d out, a fresh band clear of GR.6 90–130 d / A3.5 150–160 d / brief ~200 d). CSS: `.publish-mode`/`.segmented`, `.post-row.overdue`, `.mark-posted-btn`. **Owed:** Composer/Calendar/Today browser-verify (panel convention — build + click-through: schedule a manual URL post → chip + overdue tint + Mark posted in Calendar).
- **prior (A3.6 — $0, no schema/migration/route/MCP/extension change; suite 1385→1395, 3 files):** the manual-post reconcile in the daily pass. NEW pure `src/x/posts/manualReconcile.ts` `matchManualRows(manual, published)` (+ its co-located test, +10) links hand-pasted tweets to their calendar rows: candidates are published ORIGINALS (`isReply===false`) not yet linked (`scheduledPostId===null`); a link needs collapsed-whitespace text equality (`replace(/\s+/g,' ').trim()`) AND `postedAt ∈ [scheduledFor−1h, +7d]` (edges **inclusive**); a **global-greedy** pass over `|postedAt−scheduledFor|` (nearest first, stable-sort tie = manual-row order) makes each tweet AND each row link at most once — the harvest `matchUnlinkedDraft` discipline. Exports window consts `RECONCILE_WINDOW_BEFORE_MS`/`AFTER_MS`; empty-collapsed text never matches. `workers/dailyMetrics.ts` gains impure `reconcileManualPosts(result)`, run **try/caught right after `discover`, before `snapshotDue`** (a reconcile failure never fails the run — the `pullMentions` discipline): two 30-d-bounded selects, one **sync txn per match** (§7.13) stamping `scheduled_posts.posted_tweet_id` + `status='posted'` (manual→posted; an already-posted row stays posted, same value) + `posts_published.scheduled_post_id`; new `RunResult.manualLinked`, logged `manualLinked=n`. **It inserts NOTHING into `posts_published`** — only stamps the FK on rows discovery already created — so the since_id checkpoint (decision 6 trap) is untouched. `POST /posts/reconcile` reruns it for free — **no `posts.ts` edit** (confirmed). **Two deltas from plan text:** (a) plan assumed a numeric `scheduled_posts.id`; it's a **text uuid**, so the matcher keys are strings (D118); (b) plan said "`.getTime()` in raw sql" but I used typed `gte(col, Date)` on the timestamp_ms columns — the file's own `snapshotAccount`/`rereadWinners` idiom, no raw sql (D118).
- **prior (A3.5 — $0, no schema/migration/MCP change — status is free text; suite 1372→1385, 5 files):** the manual-publish server core, all in `calendar.ts` + one line-family in `brief.ts` + type-only extension mirrors. `manual` joined `STATUSES` and `WRITABLE_STATUSES`; create + PATCH accept it with scheduledFor required (same `scheduled_for_required_when_pending` error — deliberately NOT a new error string); the single-route create-status error renamed `create_status_must_be_draft_pending_or_manual` (threads keep the old name — accurate there; no client matched either). URL guard stays `pending`-only at BOTH sites with decision-5 why-comments; manual→pending promotion re-checks structurally (`finalStatus==='pending'`). `POST /posts/scheduled/:id/mark-posted` (registered between PATCH and DELETE; extra segment, no `:id` shadowing): 400/404/**409 `not_manual`**, flips to `posted` + `updatedAt`, reads NO body, **never writes `posts_published`** — the test asserts `postedTweetId` stays null (linking is A3.6's reconcile). Threads reject manual at both doors (create 400 `manual_threads_unsupported` AND thread-member PATCH — the D117 plan-gap close). `brief.ts` today select is now `['pending','manual','posted','failed']` (manual = filled slot; anchor leaves the gap list; `slotted`'s `!== 'failed'` filter needed no change). `publisher.ts` untouched (`git diff` clean — the claim predicate is the whole double-post story). Extension: `PostStatus`/`CreateBody.status`/`UpdateBody.status` widened, `CreateThreadBody` deliberately not; UI is A3.7. Tests: +12 calendar (`manual publish (A3.5)` describe, fixtures 150–160d out, clear of the GR.6 advisory band 90–130d and the brief monitor pair ~200d) + 1 brief (`brief manual slots (A3.5)` — the file's ONE deliberate today-window calendar fixture; status `manual` is invisible to the monitor's pending-only rules and it deletes itself in `afterAll`). the audience-blending consumer. `composerLogic.suggestBestSlotDate` took a trailing `audience?: ActiveTimesGrid | null` (6th arg, after `rand`) → **three-tier** ranking: own gated best-times score desc (the measured authority — two measured slots NEVER reorder by audience, §7.19/decision 10) → `audienceScoreFor` intensity desc among null-own-score candidates → earliest. **No grid ⇒ every audienceScore null ⇒ byte-identical to pre-A3.4** (regression-tested with an explicit `null`-vs-omitted assertion). New pure `slotHint(cell, audienceScore)` → `'measured'|'audience'|null` and `audiencePeakHours(grid, weekday, topN=2)` (zero-intensity hours excluded). `brief.ts` `annotateGaps` gained the same optional `audience` → each gap carries `audienceScore: number|null`; **its sort is untouched** (own-score-first; audience is display data only), and `GET /brief`'s big `Promise.all` gained one $0 select of the newest `audience_activity` row — the **full DB row is a structural superset of `ActiveTimesGrid`**, so it feeds `audienceScoreFor` with no cast. `api.ts` `analytics.activeTimes()` → `ActiveTimesResponse {capture: AudienceCapture|null}`; new `AudienceCapture`/`ActiveTimesResponse` mirrors in `shared/types.ts` (`capturedAt` an ISO string — a `timestamp_ms` Date over JSON). Composer: `audience` state loaded once on mount (silent null on 404/unconfigured), passed to `suggestBestSlotDate`, the winning slot labeled via `slotHint`, plus two `.best-times muted` lines — `Audience peak <day>: HH:xx, HH:xx` and a `>28d`/absent staleness nudge. **The BriefGap extension mirror + Today's rendering of `audienceScore` were deliberately NOT built** (no consumer in A3.4's edit list — a later Today-polish/A3.14 task adds them; the server field exists now). D114 held: the server `audienceScoreFor` import needed no file move. The brief test landed in `src/test.test.ts` (`brief annotateGaps (S0.4)` block), **not** the plan's stated `brief.test.ts` (D116).
- **prior (A3.3 — $0, extension-only, no schema/route/MCP change, suite 1364→1366):** the capture half. `content.ts` gained one `scan()` hook `syncActiveTimesCapture()` gated pathname (`/i/account_analytics`, re-read per scan; the off-page branch is the per-visit reset) → `passiveCaptureEnabled` (the sightings toggle, NOT `passiveHarvest`) → done/in-flight flags → DOM read; POST over ApiRequest, `unconfigured` = done-for-visit, failure = warn once + retry next scan; 12h/`gridsEqual` suppression in `chrome.storage.local['activeTimes:lastSent']` (client-side ONLY — the route never dedupes). **The A3.1 live pass happened and forced two adjustments (D115):** the live palette is an opacity ramp over one constant `hsl(var(--color-blue-500))` color, so the DOM half's `cellColor` now folds element opacity into an rgba alpha and resolves `var()` via computed style (+2 fixtures); and the metric combobox sits outside the heading's ≤8-hop walk, so `readActiveTimesMetric` gained a page-level fallback. Parsed grid eyeball-matched the on-page heatmap (row-major confirmed; Sa hottest).
- **prior (A3.2 — $0, server-only, no extension/MCP change, suite 1352→1364):** table `audience_activity` (migration **`0021_square_iron_monger`**, DDL-only: 1 CREATE TABLE + 1 `captured_at` index, no seed — inspected clean) + always-mounted `src/x/routes/analytics.ts` (mounted right after `metrics` in `index.ts`, static path only) + 12 route tests. POST validates before any DB touch (metric trim ≤40 / tz |x|≤840 / `cols===ACTIVE_TIMES_COLS` / rows in MIN..MAX / grid cols×rows finite numbers) with the bounds **imported from `src/shared/activeTimes.ts`** (D114 honored); grid values are **clamped** [0,1] server-side, not rejected; `captured_at` server-stamped. GET → newest `{capture}`\|`{capture: null}`; `?history=n` (positive int, clamp 30, else 400 `invalid_history`) → `{count, captures}` newest-first with a same-ms tie broken by `id` desc (append order — two same-request-cycle inserts genuinely collide under test).
- **prior (A3.1 — opens Wave 4, $0, no schema/route/MCP change):** two new pure modules + 25 tests, 5 files. **Canonical/shim split, not the extension-only file Task 1's block names (D114):** `src/shared/activeTimes.ts` holds the pure core (`ActiveTimesGrid` [col][row] col 0 = Mon; `parseHeatColors` — grid-level alpha mode, >20% unparseable → null, all-equal → 0.5; `audienceScoreFor` — `(jsWeekday+6)%7` column mapping, proportional hour rows; `gridsEqual` ε0.02; exported bounds 7/12/96) and `extension/src/shared/activeTimes.ts` re-exports it + adds the DOM-only `extractActiveTimesSection` (heading-anchored ≤8-hop ancestor walk; 7 same-height size-filtered parent groups as columns OR one flat container row-major; all selectors named constants). `extension/tsconfig.app.json` include gained the out-of-tree canonical (third shim after replyBand/channelSuggest).
- **prior (GR.10 — closes the GR sub-plan AND Wave 3):** docs-sync + smoke — **$0, no code/schema/route/MCP/test change** (suite stays **1325/0**). One new file, `scripts/smoke-guardrails.ts`, plus nine docs. **The smoke is the whole task.** It mounts following + monitor + goals + me + calendar + brief + digest over the REAL DB and drives all three slices; it is rerunnable back-to-back and verified to leave 0 rows. **Its real-DB safety needed a shape `smoke-passive-harvest.ts` did not:** namespacing (`gr10_*` handles, 889-prefixed tweet ids, a `gr10 smoke` text prefix) covers only the rows it creates, but a **complete-run reconcile is GLOBAL** (every unseen live handle → `gone`) and **reading the queue releases rows** — so it snapshots every FOREIGN `following` status before the first write and restores it from both the success path and `fail()`, and does the same for `commitments` and the seeded `streaks`/`digests` keys. Because the seeded burst posts sit inside today's window while `GET /x/brief` writes the C9 diary, the run **ends with one more brief read** so today's streak row is recomputed from clean data rather than left describing fixtures. Docs: people (Following subtab) / harvest (third mode) / today (**three** surfaces + a renumber to 17 sections) / composer (schedule warnings) / me (five goal kinds + the ratchet) / settings (Commitments card) tabs, `docs/s2-mcp-server.md` corrected **16 → 21 tools**, the `docs/PHASE-HISTORY.md` phase entry and the `PLAN.md` blockquote.
- **prior (CA.1 + CA.2 step 1, 2026-07-23, out-of-masterplan URGENT jump — kept live because CA.2 step 2 is still unpaid):** two commits, both server-only, **$0**, no migration/route/extension change, suite 1224→**1228** (+4 in NEW `src/x/mentions.test.ts`). **CA.1:** `getUserMentions` gained `maxTotal` (TOTAL ceiling across pages; per-request `max_results` clamp untouched — invariant #5) and `pullMentions` incremental pulls now page to `MENTIONS_HARD_CAP = 800` — before this, prod silently dropped every mention past 50/day since Jul 14 (the tail fell below the next pull's checkpoint forever). **CA.2 step 1 (instrument-only):** `xFetch` logs `[cost-audit] <method> <endpoint>: data=N includes.tweets=N includes.users=N` on 2xx bodies carrying `includes` — the expansions drag reply parents into `includes.tweets`, which `itemCount` never counted (suspected mechanism of the console $0.60 vs ledger $0.33 divergence). **No `cost_usd` change was made** — step 2 is gated on a real 03:00 pass (see next-up).
- **earlier priors (GR.9 back through HV.1):** moved verbatim to `STATE-ARCHIVE.md`. Their forward-binding content survives here as the carry-forward gotchas below.

## Ledger

Status: `[ ]` todo · `[~]` in progress (lane claimed) · `[x]` done (sha + date) · `[s]` skipped (reason in deviations).

### Wave 0 — Foundations
- [x] UI.1 Settings platform (xhigh) — 226368c (code+format+state+codemap; D12 absorbed into 61a04e7) 2026-07-18
- [x] UI.8 Design tokens dark refactor (high) — 1299e43 (styles.css in 61a04e7 per D12) 2026-07-18
- [x] UI.9 Light theme + Appearance (high) — f9ff346 2026-07-18 (sha corrected from orphaned 8c89950)
- [x] UI.10 UI primitives + grouped rail (xhigh) — c37d504 2026-07-18 (sha corrected from orphaned 50948c9 — see D17)
- [x] ST.1 Engine layers + PRNG (high) — 61a04e7 2026-07-18
- [x] ST.2 Studio shell refactor (high) — dffd60a 2026-07-18 (sha corrected from orphaned 860725f — see D16)
- [x] ST.3 Cloud mascot (high) — b82058b 2026-07-18 (sha corrected from orphaned 292603d — see D17)
- [x] ST.4 Patterns + presets (high) — 15f2563 2026-07-18 (sha corrected from orphaned c1b0314 — see D18)
- [x] ST.5 Milestone + streak cards (high) — 1f6b20f 2026-07-18 (sha corrected from orphaned bc3f1e8 — see D19)
- [x] ST.6 Code card + mono font (high) — 00b9615 2026-07-18 (sha corrected from orphaned b65266f — see D19/D20)
- [x] ST.7 Thread cover + list card (high) — acd95ec 2026-07-18 (sha corrected from orphaned 2104f46 — see D21)
- [x] ST.8 Chart card (high) — 289ec2c 2026-07-18 (sha corrected from orphaned d38bbc5 — see D22)
- [x] ST.9 Studio docs+smoke (high) — d1902e5 2026-07-18 (sha corrected from orphaned 47b10d2 — see D23)

### Wave 1 — Prompt & identity core
- [x] RU.1 Reply prompt → 3 variants (xhigh) — 63df913 2026-07-18 (sha corrected from orphaned 9c05464 — see D24)
- [x] RU.2 Migration: source/variants/model columns (high) — ddecb5f 2026-07-18
- [x] RU.3 Batch → 3 variants (xhigh) — d8ecc9a 2026-07-18
- [x] N.1 Niche schema + defaults + store (xhigh) — 0bbca12 2026-07-18 (sha corrected from orphaned da97495 — see D27)
- [x] N.2 Niche CRUD + activation + MCP (high) — c76a29b 2026-07-18 (sha corrected from orphaned 4312a0e — see D28)
- [x] N.3 Post prompt persona extraction (MAX) — 3b1547c 2026-07-18 (sha corrected from orphaned b06770c — see D29)
- [x] N.4 Reply prompt persona extraction (MAX) — 0668451 2026-07-18
- [x] N.5 Doctrine knobs threaded (high) — 7e9213d 2026-07-18 (sha corrected from orphaned 20faa44 — see D31)
- [x] N.6 Pillar/channel niche ownership (xhigh) — 730a009 2026-07-18
- [x] N.7 Settings Niche card (high) — 25a8c99 2026-07-18
- [x] N.8 Niche wizard (high) — 5c1031d 2026-07-18 (sha corrected from orphaned 527ad21 — see D35)
- [x] N.9 Niche docs+smoke (high) — 78d1d93 2026-07-18
- [x] ME.1 me_entries/me_goals + profile module (high) — `0545185` 2026-07-18 (sha corrected from orphaned f0c91f4 — see D37; N.9 sha reconciled e452f7c — see D36)
- [x] ME.2 /x/me routes + loader + smoke (high) — `43e6bc9` 2026-07-18 (sha corrected from orphaned a3a94bb — see D38)
- [x] ME.3 Inject me-context at prompt tails (xhigh) — `82f0291` 2026-07-18
- [x] ME.4 Me tab (high) — `5a4b427` 2026-07-18 (sha corrected from orphaned 92499ab — see D40)
- [x] ME.5 Playbook lift + digest goals fact (high) — `9df22ff` 2026-07-18 (sha corrected from orphaned d611dce — see D41)
- [x] ME.6 MCP x_me + x_add_me_entry tools (high) — `d4ef3dc` 2026-07-18 (sha corrected from orphaned 8686d79 — see D42)
- [x] ME.7 Me docs-sync (high) — `f010bf3` 2026-07-18 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.1 OpenRouter client (xhigh) — `fc5b3c4` 2026-07-18 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.2 askLLM + /llm routes — D1 (xhigh) — `3d3598c` 2026-07-18 (sha corrected from orphaned 87f3bd7 — see D45a)
- [x] AI.3 Prompt registry + overrides (MAX) — `a087a66` 2026-07-18 (sha corrected from orphaned 9f33fb2 — see D45b)
- [x] AI.4 /x/prompts routes (high) — `3c649ce` 2026-07-18 (sha corrected from orphaned 466b090 — see D47a)
- [x] AI.5 Batch/extract/pillar-draft via registry — D3 (MAX) — `7d0a786` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.6 Digest/icebreaker via registry + LLM gate (xhigh) — `e2c91ac` 2026-07-19 (sha corrected from orphaned b82f4ef — see D49)
- [x] AI.7 Thread drafter (xhigh) — `b932a06` 2026-07-19 (sha corrected from orphaned 995a08b — see D50)
- [x] AI.8 Rewrite assist (high) — `b9fe98d` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.9 Idea generator (high) — `8e53621` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.10 Settings AI subtab (high) — `a91387f` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.11 Prompts editor panel (high) — `e7d5be9` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.12 Model-effectiveness cell (high) — `9c59183` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.13 AI docs+smoke (high) — `f86e06a` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles) — **closes Wave 1**

### Wave 2 — Reply machine & on-page surfaces
- [x] RU.4 Buffer/panel variants (high) — `22cf0b1` 2026-07-20 (sha corrected from orphaned a3664af — see D58)
- [x] RU.5 Confirm endpoint (xhigh) — `5935107` 2026-07-20 (sha corrected to `983fbea` — see D59; recorded sha was an amend-orphan)
- [x] RU.6 Panel confirm wiring (high) — `1ed57bd` 2026-07-20 (sha corrected from orphaned 6261673 — see D60)
- [x] AX.1 Glance endpoint (high) — `9467220` 2026-07-20 (sha corrected from orphaned f1d85c2 — see D61)
- [x] AX.2 Glance chip view-model (high) — `e29b629` 2026-07-20 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AX.4 tweetContext view-model (high) — `7ecb8df` 2026-07-21 (sha corrected from orphaned 7abec86 — see D64)
- [x] AX.3 Timeline chips, pill removal (xhigh) — `39fecb6` 2026-07-21 (sha corrected from orphaned bc2c9fe — see D65)
- [x] AX.5 Status-page context panel (xhigh) — `5e31b8d` 2026-07-21 (sha corrected from orphaned 0eb5319 — see D66)
- [x] AX.6 Dossier click-through (high) — `b3fd7b3` 2026-07-21 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] RU.7 On-page variant chips (xhigh) — `f90084b` 2026-07-21 (sha corrected from orphaned bfa2441 — see D69)
- [x] RU.8 Manual add ⊕ (xhigh) — `9ba6fba` 2026-07-21 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] RU.9 Playbook source attribution (high) — `f78b48c` 2026-07-21 (sha corrected from orphaned 899e126 — see D72)
- [x] RU.10 RU docs+smoke (high) — `22099a3` 2026-07-21 (sha stamped post-commit; amend re-orphans it, next session reconciles) — **closes the RU sub-plan**
- [x] AX.7 AX docs+smoke (high) — `00ec8a8` 2026-07-21 (sha stamped post-commit; amend re-orphans it, next session reconciles) — **closes the AX sub-plan (AX.1–AX.7)**
- [x] RL.1 Reply-list engine (xhigh) — `ffbdb3f` 2026-07-22 (sha corrected from orphaned 58903fc — see D77)
- [x] RL.2 Schema + CRUD (high) — `9c0d97b` 2026-07-22 (sha corrected from orphaned a29ffbe — see D79a)
- [x] RL.3 /use route (high) — `c3f44d2` 2026-07-23 (sha corrected from orphaned 0ea02a3 — see D80a)
- [x] RL.4 AI generator — D5 (high) — `99eef6b` 2026-07-23 (sha stamped post-commit; amend re-orphans it, next session reconciles by subject)
- [x] RL.5 Lists subtab UI (high) — `17a530f` 2026-07-23 (sha stamped post-commit; amend re-orphans it, next session reconciles by subject)
- [x] RL.6 QuickReplyPicker (high) — `aae4665` 2026-07-23 (sha stamped post-commit; amend re-orphans it, next session reconciles by subject)
- [x] RL.7 Playbook canned bucket (high) — `3a4ae56` 2026-07-23 (sha corrected from orphaned b2543e7 — see D84a)
- [x] RL.8 RL docs+smoke (high) — `a120207` 2026-07-23 (sha corrected from orphaned 7ac1b5e — see D85c) — **closes the RL sub-plan (RL.1–RL.8)**
- [x] NT.1 Notification parser (high) — `2063b86` 2026-07-23 (sha corrected from orphaned 7f3d2fd — see D86a)
- [x] NT.2 Engagement events + ingest (xhigh) — `4e4d9fc` 2026-07-23
- [x] NT.3 Engagements route (high) — `2b763b1` 2026-07-23 (sha stamped post-commit; the amend re-orphans it, next session reconciles by subject)
- [x] NT.4 Background notif-context (high) — `fe0f6aa` 2026-07-23
- [x] NT.5 Notifications content script (xhigh) — `69784da` 2026-07-23 (sha corrected from orphaned 3a94830 — see D90a; NT.4 sha reconciled to `68039f2` — see D89a)
- [x] NT.6 Fans engagement count (high) — `0c0ab5d` 2026-07-23 (sha corrected from orphaned 532f87d — see D91a)
- [x] NT.7 NT docs+smoke (high) — `5d4a1a4` 2026-07-23 (sha corrected from orphaned 75f15e7 — see D93a) — **closes Wave 2**

### Wave 3 — Ambient data & guardrails
- [x] HV.1 Passive harvest endpoint (xhigh) — `5ea3495` 2026-07-23 (sha corrected from orphaned bf5e7b5 — see D93a)
- [x] HV.2 Passive capture + toggle (xhigh) — `661d039` 2026-07-23 (sha corrected from orphaned 090a8ac — see D94a)
- [x] HV.3 Harvest tab options (high) — `4c2adcf` 2026-07-23 (HV.2 sha reconciled to `661d039` — see D94a)
- [x] HV.4 Timeline affinity (high) — `3132f10` 2026-07-23 (HV.3 sha reconciled to `5cd316a` — see D95a)
- [x] HV.5 Funnel playbook cell (high) — subject `feat(playbook): timeline opportunity-capture funnel over passive harvest rows (HV.5)`, parent `855b424`, 2026-07-23 (no sha by design — D97)
- [x] HV.6 HV docs+smoke (high) — subject `docs(harvest): Harvest 2.0 docs-sync + $0 smoke-passive-harvest.ts (HV.6)`, parent `3416f14`, 2026-07-23 (no sha by design — D97). **Closes the HV sub-plan.**
- [x] GR.1 Following ledger (xhigh) — `feat(following): ledger schema + ingest/ratchet routes (GR.1)` 2026-07-23
- [x] GR.2 Harvester following mode (xhigh) — `feat(following): harvester following-mode scrape (GR.2)` 2026-07-23
- [x] GR.3 Curation queue (xhigh) — `feat(following): curation queue with jittered cadence caps (GR.3)` 2026-07-23
- [x] GR.4 Following subtab (high) — `feat(following): People tab Following subtab (GR.4)` 2026-07-23
- [x] GR.5 Monitor rules + route + MCP (xhigh) — `feat(monitor): activity-pattern rules + /x/monitor + MCP tool (GR.5)` 2026-07-23
- [x] GR.6 Monitor surfacing (high) — `feat(monitor): brief block, Today card, schedule-time warnings (GR.6)` 2026-07-23
- [x] GR.7 Goals + commitments — D4 (xhigh) — `feat(goals): goals + commitments over me_goals, pacing/debt/scorecard core (GR.7)` 2026-07-24
- [x] GR.8 Accountability surfacing (high) — `feat(goals): brief pacing + commitment-driven quests + Today Goals card (GR.8)` 2026-07-24
- [x] GR.9 Digest scorecard (high) — `feat(goals): weekly scorecard in digest facts + card (GR.9)` 2026-07-24
- [x] GR.10 GR docs+smoke (high) — `docs(guardrails): GR docs-sync + $0 smoke-guardrails.ts (GR.10)`, parent `7adfbca`, 2026-07-24. **Closes the GR sub-plan AND Wave 3.**

### Wave 4 — Authoring 3.0
- [x] A3.1 Active-times parser (xhigh) — subject `feat(extension): active-times heatmap parser (shared, fixture-tested) (A3.1)`, parent `49fcd1d`, 2026-07-24 (no sha by design — D97)
- [x] A3.2 audience_activity + routes (high) — subject `feat(analytics): audience_activity capture table + /x/analytics/active-times routes (A3.2)`, parent `d4869ab`, 2026-07-24 (no sha by design — D97)
- [x] A3.3 Analytics capture (xhigh) — subject `feat(extension): passive active-times capture on X Analytics (A3.3)`, parent `cf2f312`, 2026-07-24 (no sha by design — D97). **Completes the D9 chain.**
- [x] A3.4 Audience-blended slots (xhigh) — subject `feat(composer): audience-blended best-slot ranking + staleness note (A3.4)`, parent `5bb88e2`, 2026-07-24 (no sha by design — D97)
- [x] A3.5 Manual publish core (MAX) — subject `feat(calendar): manual publish status + mark-posted transition (A3.5)`, parent `8d499fb`, 2026-07-24 (no sha by design — D97)
- [x] A3.6 Manual reconcile (xhigh) — subject `feat(metrics): manual-post reconcile links pasted tweets to calendar rows (A3.6)`, parent `1053f5d`, 2026-07-24 (no sha by design — D97)
- [x] A3.7 Composer toggle + chips (high) — subject `feat(extension): manual publish mode in Composer + calendar/today chips (A3.7)`, parent `03a3b7a`, 2026-07-24 (no sha by design — D97)
- [x] A3.8 Manual alarms + Today card (xhigh) — subject `feat(extension): manual-post alarms, notification and Today publish card (A3.8)`, parent `49a05e7`, 2026-07-24 (no sha by design — D97)
- [ ] A3.9 DM drafts — D5 (xhigh)
- [ ] A3.10 DM UI (high)
- [ ] A3.11 Articles CRUD (high)
- [ ] A3.12 Article prompt + assist — D5 (xhigh)
- [ ] A3.13 Writer page (xhigh)
- [ ] A3.14 Calendar week board (xhigh)
- [ ] A3.15 A3 docs+smoke (high)

### Wave 5 — Settings moat + polish
- [ ] UI.2 Doctrine/quests knobs — D2 (xhigh)
- [ ] UI.3 People/followups knobs (high)
- [ ] UI.4 Gates/workers knobs + MCP (high)
- [ ] UI.5 Money/AI knobs + ceilings (xhigh)
- [ ] UI.6 Extension mirror (xhigh)
- [ ] UI.7 Band thresholds e2e (xhigh)
- [ ] UI.11 Settings tab rebuild — D6, D8 (xhigh)
- [ ] UI.12 Today polish (high)
- [ ] UI.13 Composer/Calendar polish (high)
- [ ] UI.14 People/Channels/Voice/Ideas polish (high)
- [ ] UI.15 Replies/Playbook/Harvest/Studio polish (high)
- [ ] UI.16 Content-script + explorer tokens (high)
- [ ] UI.17 UI docs+smoke (high)

## Hot-file locks

A lane claims a file before starting, releases on commit. `owner: —` = free.
**Everything is free — Wave 3 released the last of it.** Rows below are kept only where
the note binds a task still marked `[ ]`; a closed lane's file with nothing to say to
Wave 4/5 has been dropped (grep `STATE-ARCHIVE.md` for its lane if you need the history).

| File | Owner |
|---|---|
| `extension/src/content.ts` | — (**the D9 chain is COMPLETE at A3.3**; UI.16's token polish is the only future claimant. Both ambient-capture gate orders are perf contracts: passive harvest enabled → `/home` → `!isHarvestActive()` → one `findPermalink` → throttle → only THEN `extractArticle`; active-times pathname → `passiveCaptureEnabled` → done/in-flight flags → only THEN `extractActiveTimesSection`) |
| `extension/src/harvester.ts` | — (GR.2 released it; `runHarvest`'s ingest param is a `ship(profile, store, outcome)` step and the mode dispatch lives in `startRun` — **a fourth mode is one branch there**, plus `MODE_NOUN`/`isHarvestMode`/`targetPath`/`startRun` staying exhaustive over the 3-union) |
| `src/db/migrations/` journal (any migration task) | — (A3.2 took `0021_square_iron_monger`; next owns `0022`. **Wave 4 has two migrating tasks left — A3.9 / A3.11 — which must never run in parallel sessions**) |
| `src/x/playbook.ts` + `src/x/routes/playbook.ts` | — (HV.5 released them; the pure file's sections run to **§12** — the next `build*` is §13, and any plan text counting sections is off by one from AI.12 onward. UI.15 polishes the panel) |
| `src/x/routes/brief.ts` | — (A3.4 released it; the big `Promise.all` now ends with `loadCommitmentsWithDebt` + `loadGoalsWithPacing` + a $0 `audienceActivity` newest-row select, and the response carries `monitor` / `goals` / `commitments` plus each gap's `audienceScore`. `annotateGaps` takes an optional `audience` grid — own-score-first ordering is unchanged, audience is display-only. **A3.5:** the today select is `['pending','manual','posted','failed']` — a manual slot fills its anchor. **The goals loader WRITES**, so `GET /brief` settles goal statuses on every read — A3.8 and A3.14 both build on this payload; keep it out of any polling loop) |
| `extension/src/sidepanel/Today.tsx` | — (GR.8 released it; `AccountHealthCard` between `PinnedWatchCard` and `TodayPlan`, `GoalsCard` right after the quests block, `DebtLine` inside `QuestsSection`. **A3.8 adds the manual-post card here; UI.12 polishes it** and owes `docs/today-tab.md` — now numbered §1–§17 — a re-read) |
| `src/x/routes/calendar.ts` | — (A3.5 released it; `manual` is in `STATUSES`/`WRITABLE_STATUSES`, `mark-posted` sits between PATCH and DELETE, and the URL guard is `pending`-only at both sites (decision 5 — don't "fix" it). **A3.7:** `scheduleWarnings` now fires for `pending` AND `manual` rows and counts both as neighbors (`inArray(['pending','manual'])`), still computed **after** the insert inside a try/catch. **PATCH still does NOT warn** (D105) — the one remaining A3.14 follow-up on this file. The URL guard stays the only content 4xx) |
| `extension/src/sidepanel/Composer.tsx` | — (GR.6 released it; a `warnings: string[]` state renders amber under the notice line and is set **after** `reset()` because reset clears it — **A3.7 kept that order** (the toggle's `setManualMode(false)` is inside `reset()`, warnings still set after). A3.7 added the API|Manual toggle + `manualMode` state + the module-level pure `nextEditStatus`; UI.13 polishes this file next) |
| `extension/src/sidepanel/Settings.tsx` | — (GR.8 released it; the General view renders `<CommitmentsCard/>` between `NicheCard` and the settings form — a separate `.panel` div, NOT inside the form, because the per-key Save is its own `PUT`. D6 still binds: UI.11 absorbs it) |
| `src/x/routes/replies.ts` + `src/x/replies/prompt.ts` | — |
| `post prompt.md` / `reply prompt.md` / `thread prompt.md` (+ TS literals) | — (**A3.12 adds `article prompt.md`** — byte-synced the same way, and D5 says it registers a registry key; see the RL.4 gotcha for the 4-file move) |
| `src/x/routes/harvest.ts` | — (HV.4 released it; POST runs/rows/passive + GET runs/affinity, still **no `:param` route** — keep it that way. `scripts/smoke-passive-harvest.ts` mounts it over the real DB, so a route rename breaks the smoke as well as the suite) |
| `extension/src/shared/harvest.ts` + `sidepanel/Harvest.tsx` | — (GR.2 released them; nothing before UI.15's polish pass claims them again. Sibling `shared/userCell.ts` is free too) |
| `extension/src/sidepanel/People.tsx` | — (GR.4 released it; the panel opens with a `SubTabs` **Roster \| Following** switch that renders only when no dossier is open — a new People surface goes inside one of the two views, not above the switch. **A3.10's DM UI lands in the dossier; UI.14** polishes the file and owes `docs/people-tab.md` a re-read) |
| `src/x/goals.ts` + `src/x/routes/goals.ts` · `src/x/monitor.ts` + `src/x/routes/monitor.ts` · `src/x/connections.ts` | — (the three GR pure/route pairs, all closed. **Import from them, never re-derive**: `loadGoalsWithPacing`/`loadCommitmentsWithDebt`/`loadFlowCurrents`, `loadMonitorInputs`/`runMonitor`/`worstOf`/`shingleJaccard`, `DAILY_CEILING`/`MARK_LOOKBACK_MS`. Every threshold is an exported opening guess — a recalibration is a one-line diff in the pure file and moves every surface at once) |
| `src/x/digest.ts` + `src/x/routes/digest.ts` · `src/x/quests.ts` · `src/x/routes/me.ts` + `src/x/me/profile.ts` | — (closed at GR.7–GR.9. The scorecard is computed **inside** `buildDigestFacts` (D112a); `QuestInputs.originalsTarget?` is optional so pre-GR.8 streaks stay valid; `GOAL_KINDS` is a 5-union with `missed` in `GOAL_STATUSES`, and `POST /me/goals` is the single goal writer) |

## Deviations & decisions register

Pre-seeded from cross-plan analysis (see MASTERPLAN "Cross-plan adaptations" for full text).
Append D11+ as work reveals divergences from plan text.

**Closed-wave D-entries (116 of them, Waves 0–3) live in `STATE-ARCHIVE.md`.** What stays below: the pre-seeded cross-plan adaptations (D1–D11, D13), the standing bookkeeping rule (D97), and the closing Wave-3 entry (D113/D113a) whose (d) and (e) bind every future smoke. The HV/GR entries D93–D112 were archived at GR.10; their forward-binding content survives as the single **Wave-3 carry-forward** gotcha below.

- **D1** (binds AI.2): `app_settings` created once by UI.1; AI.2 reuses it — no second table/migration.
- **D2** (binds UI.2): the 5 doctrine knobs are owned by `niches.doctrine` after N.5; UI.2 must not duplicate them in `app_settings` — read/write through the active niche or drop the keys. **Decision pending at UI.2.**
- **D3** (binds N.4, AI.5): `VOICE_BLOCK_END` renamed by RU.1 to `'## The three variants'`; N.4 substitutes `{{REPLY_PERSONA}}` inside the slice; AI.5 retires slicing — its parity fixture is post-RU/post-N output, and its anti-drift test asserts against defaults containing the niche placeholder.
- **D4** (binds GR.7): no separate `goals` table — extend `me_goals` (baseline fields, `posted_replies`/`originals` metric kinds, `missed` status). One goals system, one digest fact. **Confirm merge shape at GR.7.**
- **D5** (binds RL.4, A3.9, A3.12): after AI.2/AI.6, new LLM calls use `askLLM` + `llmConfigured()`; new prompts register in the registry (`dm`, `article`) so the editor covers them. Refusal-ladder order unchanged.
- **D6** (binds all Settings.tsx tasks pre-UI.11): keep feature Settings UI minimal; UI.11 rebuilds and absorbs.
- **D7** (standing): all NEW UI from Wave 1 on uses UI.10 primitives + `--strat-*` tokens; Wave-5 polish passes touch only pre-masterplan tabs.
- **D8** (binds UI.11): prune the "coming soon" manifest to features still unbuilt at that time.
- **D9** (standing): content.ts task order is fixed: AX.3 → AX.5 → RU.7 → RU.8 → NT.5 → HV.2 → A3.3.
- **D10** (standing): registry knob groups (UI.2–5) run in Wave 5 so they catalog the final constant set, including RU/HV/GR/A3 constants.
- **D11** (UI.8, standing): `biome.json` `files.ignore` now includes `Stratus Design System` — the DS reference folder (Decision 9, plan out-of-scope) was ~112 of the baseline lint errors. Not in UI.8's plan edit-list but a correct, universal fix; whole-repo lint dropped 119→7. The remaining 7 are pre-existing and NOT in any changed file: 2 a11y in `extension/src/sidepanel/Replies.tsx` (UI.15 territory) + 5 biome-format on generated `src/db/migrations/meta/*.json` (a future infra task could ignore `src/db/migrations/meta`).
- **D13** (UI.1, binds UI.2): the **doctrine group is seeded in full by UI.1** — the 6 keys `x.doctrine.{replyTargetMin,replyTargetMax,weekReplyTargetPct,anchors3,anchors4,ladderSwitchAt}`, with the anchors' sorted-unique+range numberArray validation already in place (Task 2's plan attributed that hook to itself; landed early because UI.1 seeds the anchors so it must validate them). **UI.2 must NOT re-declare these doctrine keys** — it only adds the `quests`+`display` groups and wires the consumers (brief quota/ratio/gaps, composer ladder). Store layering (also binds AI.2/D1): `src/settings/store.ts` is platform-agnostic and never imports `src/x/*`; the X registry `src/x/settings/registry.ts` implements the store's `SettingsRegistry` interface and exposes the bound `getSetting`/`resolveSetting`/`getAllValues`/`setSettings`/`resetSettings` — **UI.2+ consumers import those from `../settings/registry.ts`, never the store directly**. biome.json now also ignores `src/db/migrations` (fulfils D11's anticipated infra ignore; the 5 generated-JSON lint errors are gone → whole-repo lint = 2, both pre-existing `Replies.tsx` a11y).
- **D97** (HV.5, bookkeeping — supersedes the sha half of D93a/D94a/D95a): **the ledger records the commit SUBJECT LINE, not a sha.** Three tasks in a row wasted a Step-0 investigation proving a recorded sha was an amend-orphan, and every time the resolution was "the subject line is the identity". A sha can only be written into the commit that changes it by amending, which changes it again — the churn is structural, not carelessness. From HV.5 on: ledger entries carry the subject line + the PARENT sha (stable, already in history), the codemap header stamps `<parent>+<TASK>`, and Step 0 is `git log -1 --format='%h %s'` compared against the last ticked subject. No reconciliation, no orphan hunts.
- **D113** (GR.10): **four places Task 10's text and the repo disagreed, all resolved in the repo's favour, plus one thing the plan could not have known.** **(a) The phase entry went to `docs/PHASE-HISTORY.md`, not CLAUDE.md** (D93a — Task 10 still says CLAUDE.md), and the `PLAN.md` note is a **top blockquote** above §"Product, in one paragraph each", not a §"Phased build" edit (D98b, the RU.10/HV.6 exemplar). **(b) The docs bill was nine items, not the plan's four.** Task 10 lists people/today/harvest/"settings doc if present"; GR.6 also changed the Composer, GR.7 changed the Me tab's goal kinds, and `docs/s2-mcp-server.md` had been wrong about the tool count since **N0.2** (16 in three places; it is 21, and both tier counts were stale too — curated 10→14, write 3→4). A docs-sync task inherits every stale count in the files it touches, not just its own lane's. **(c) `docs/today-tab.md` needed a RENUMBER, not an append.** The Goals card renders after the quests block and Account health after Pinned watch, so the doc's numbered §1–§15 became §1–§17 with two inserts. Verified against `Today.tsx`'s actual JSX order rather than the plan's prose. **(d) The smoke could NOT follow `smoke-passive-harvest.ts` as written.** D98c's rule is namespace-then-delete-what-we-made, which assumes every write is attributable. Two Guardrails writes are **global**: a complete-run reconcile marks EVERY unseen live handle `gone`, and `GET /following/queue` releases rows at read time. So the smoke additionally **snapshots and restores** every foreign `following.status` (from the success path and from `fail()`), plus `commitments` and the seeded `streaks`/`digests` keys — and, because its burst fixtures sit inside today's window while `GET /x/brief` writes the C9 diary, it closes by **re-reading the brief** so the diary is recomputed rather than left describing fixtures. **Any future smoke that exercises a read-that-writes must copy this, not D98c alone.** **(e) A fixture fact the plan's sequence hides:** "unseen" in the reconcile is `last_run_id != runId`, i.e. per **run**, not per batch — a handle shipped in batch 1 and omitted from batch 2 of the SAME run is still seen. The smoke needs **two** runs to demonstrate `gone`/`confirmed`.
- **D113a** (GR.10, bookkeeping): the archive pass moved **20 D-entries (D93–D112, D97 excepted)**, **16 gotchas (HV.1 → GR.9)** and **7 header priors (GR.4 → GR.9)** into `STATE-ARCHIVE.md` verbatim. STATE.md went ~128 KB → **~50 KB** (one `Read` again); the archive is now ~350 KB (grep-only by design). What was deliberately kept live out of the closed lanes: the **carry-forward gotcha** below (the four HV/GR facts that still bind an open task) and nothing else.

- **D114** (A3.1): **Task 1's edit list (extension-only `extension/src/shared/activeTimes.ts`) was overridden by the same plan's Task 4 line 221 + §7.27** — the pure core (grid types, `parseHeatColors`, `audienceScoreFor`, `gridsEqual`, bounds) lives canonically in `src/shared/activeTimes.ts`, the extension file is the DOM extractor + re-export shim (replyBand arrangement; root tsconfig has no DOM lib, so the split is also forced by typecheck). **Binds A3.2** (import `ACTIVE_TIMES_COLS/MIN_ROWS/MAX_ROWS` from `src/shared/activeTimes.ts` for route validation), **A3.3** (import path is `./shared/activeTimes.ts`, unchanged from plan text) and **A3.4** (the server-side `audienceScoreFor` import the plan asks for needs NO file move — it already lives server-side). Also an interpretation call inside `parseHeatColors`: the plan's per-cell "luminance-or-alpha scalar" became **grid-level alpha mode** (one translucent cell ⇒ alpha carries the signal for ALL cells, so the a=1 peak of an alpha ramp scores 1 instead of flipping to its hue's ~0.53 luminance) — plan intent ("relative intensity is what we need") over letter.

- **D116** (A3.4): **two plan-vs-repo divergences, both resolved in the repo's favour.** (a) Task 4's Read-first + Edit list name `src/x/routes/brief.test.ts` for the `annotateGaps` test, but `annotateGaps` is tested in **`src/test.test.ts`** (the grab-bag pure-function suite, codemap §3.1) — the audienceScore case landed there beside the existing S0.4 block, not in the route suite. (b) The plan calls `audienceScore` "display data for the TodayPlan row", implying Today renders it — but Task 4's edit list contains no `Today.tsx` and no `BriefGap` extension-mirror change, and nothing consumes the field yet. Both were **deliberately left unbuilt** (masterplan "don't pre-stub API surface"): the server now returns `AnnotatedGap.audienceScore`, and whichever later task first renders it (a Today polish, or **A3.14**'s week board, which already sits on this `/brief` payload) adds the `BriefGap` mirror field + the JSX then. No forward task is blocked — a mirror gaining a field it now needs is a one-line hand-sync (the standing Settings-mirror gotcha).
- **D115** (A3.3): **Task 3's edit list said 1 file (content.ts); the mandated live pass (the A3.1 gotcha) forced 3.** The live x.com/i/account_analytics/audience palette carries intensity as ELEMENT `opacity` (0.2..1) over one constant `background-color: hsl(var(--color-blue-500))` — the shipped `cellColor` returned that unresolvable `var()` string for every cell (100% unparseable → capture could never fire), and even the computed color is the same constant `rgb(30,156,241)` for all cells. Fix landed in the DOM half `extension/src/shared/activeTimes.ts` (the file whose header says "adjust live in A3.3"): `cellColor` resolves `var()` backgrounds via computed style and folds element opacity into an rgba alpha (`withOpacity`), which the pure core's existing alpha mode reads correctly — `src/shared/activeTimes.ts` untouched, +2 fixtures. Second live divergence: the metric combobox (`BUTTON[role="combobox"]`, the Audience tab's only one) sits OUTSIDE every ≤8-hop ancestor of the section heading, so `readActiveTimesMetric` in content.ts walks first, then falls back to a page-level `document.querySelector`. **Affects no future task's contract** — the wire shape and the pure core are unchanged.

- **D117** (A3.5): **three implementation decisions the plan text under-specified, all recorded here because two bind future tasks.** **(a) The grep-verify found two `'pending'` enumerations that POST-DATE the plan** (written 2026-07-17; GR.5/GR.6 shipped 2026-07-23/24): `monitor.ts::loadMonitorInputs`' scheduleCluster slot select and `calendar.ts::scheduleWarnings` (fires only for `pending` rows and reads only `pending` neighbors). Both **deliberately stay pending-only at A3.5**: nothing can create a `manual` row before A3.7 ships the Composer toggle, so there is zero regression window — and STATE already earmarks calendar advisory polish to A3.7/A3.14 (the D105 note). **Binds A3.7:** when the toggle lands, extend `scheduleWarnings` to treat `manual` as first-class (fire for manual rows AND include manual neighbors in its select) and consider the one-line `inArray` widening in `loadMonitorInputs` (message wording says "pending" — check `monitor.ts`'s cluster message text); a cadence warning that can't see the manual half of the calendar is wrong the moment manual rows exist. **(b) Plan gap closed: the PATCH promotion path could mint a manual THREAD member** (threads-create rejects manual, but PATCHing a head/tail to `manual` would strand the tails — mark-posted flips one row and the publisher drives tails off a `pending` head only). `PATCH {status:'manual'}` on a row with a `threadId` now 400s `manual_threads_unsupported`, same string as the create door. **(c) `scheduled_for_required_when_pending` is reused verbatim for manual** (no new error string): no client matches it, the semantics are identical, and a second string would double the guard surface for zero information.

- **D118** (A3.6): **two plan-vs-repo divergences in the reconcile, both resolved in the repo's favour.** **(a) `scheduled_posts.id` is a text uuid, not a number** — the plan's matcher signature `{id, ...}` implied a numeric id; `matchManualRows`'s `id`/`scheduledPostId` and `ReconcileLink.scheduledPostId` are therefore **strings**. Any future caller of the matcher passes string ids. **(b) Typed `gte(col, Date)`, not "`.getTime()` in raw sql"** — the plan (Task 6 + this task's earlier next-up) prescribed the raw-sql `.getTime()` bind (§7.13). But §7.13's raw-sql-Date ban only applies to `` sql`` `` templates; the 30-d bounds here are plain Drizzle operators (`inArray`/`isNull`/`isNotNull`/`gte`) on `timestamp_ms` columns, and dailyMetrics.ts's own `snapshotAccount`/`rereadWinners` (and channels.ts) already bind Date objects through the typed operators, which Drizzle converts via the column mapper. No raw sql was written, so no `.getTime()` was needed. **Binds nothing forward** — a note that "the plan said raw sql" is not a defect to chase.

- **D119** (A3.7, discharges the A3.5-half of D117): **the plan's extension-only Task 7 (5 files) grew to 11 by executing the server follow-through D117 earmarked.** A3.5 deliberately left `calendar.ts::scheduleWarnings` and `monitor.ts::loadMonitorInputs` `pending`-only because no manual row could exist yet; **A3.7 is where the toggle mints them, so both were widened here** — `scheduleWarnings` fires for `manual` and includes manual neighbors (`inArray(['pending','manual'])`), `loadMonitorInputs`' slot select does the same, and both user-facing strings moved off "pending" (`pending post`→`scheduled post`, `pending pair`→`scheduled pair`). **No test asserted the old wording** (calendar matches `within`/`20 min away`; brief checks only `severity`/`typeof message`) — a future reword is likewise free of string-assertion drag. **Two smaller plan-vs-repo calls, both resolved in the repo's favour:** (a) the plan's **`posts.markPosted(id)`** has no home — the scheduled-post client methods hang off `api` directly, so it landed as **top-level `api.markPosted`** next to `remove`; any A3.8/A3.14 caller uses `api.markPosted`, not `api.posts.*`. (b) the plan's **"Today renders a manual chip next to the visual chip"** collapsed to a **title on the existing styled `.badge-manual` status pill** — A3.5 shipped that CSS and widened the brief today-select, so the status pill already IS the manual chip; a second "manual" text chip beside a "manual" status pill would double the word. **Binds A3.8/A3.14:** the Calendar/Today manual affordances (`.badge-manual` pill, `.overdue` tint, the sibling `.mark-posted-btn`, `api.markPosted`) exist now — reuse them, don't re-invent; A3.14's week board owns `calendar.ts`+`Calendar.tsx` next and inherits the manual-first `scheduleWarnings`.

- **D120** (A3.8): **the plan's 6-file Task 8 landed as 8, plus three implementation shapes the plan under-specified.** **(a) `api.ts` was a NO-OP** — Task 8's edit list says "reuse `posts.markPosted` from Task 7 (add here if Task 7 hasn't landed)", but A3.7 landed it top-level as `api.markPosted` (D119), so no api.ts edit; the touched set dropped api.ts and picked up `messages.test.ts` (the guard test) + `styles.css` (the `.manual-card` block). **(b) A SIBLING `syncManualAlarms()`, not an extension of `syncLaunchAlarms()`** — the plan says "the existing launch sync additionally fetches manual rows"; implemented as an independent function (fetch `status=manual`, clear only `stratus-manual:`-prefixed alarms) called alongside the launch sync at all three triggers, with its **own `manualChain`/`enqueueManual`** (the established per-session-key-family chain pattern — `manual:due` is a distinct key from `launch:active`/`launch:replies`, so a shared chain would be false coupling). **(c) No grace, no retry, and a re-fire is DELIBERATE** — `computeManualAlarms` fires at `scheduledFor` exactly (unlike launch's +90 s) and `handleManualFire` schedules no retry alarm; but because a still-`manual` recently-missed row re-enters the sync's `wanted` set and re-fires, `mergeManualDue` dedups by postId (refresh-not-stack) and the notification id `stratus-manual-note:<id>` replaces rather than stacks — so an unposted slot re-nudges at most ~once more inside the 30-min miss window, then the overdue chip takes over. **Binds A3.14** (the week board reuses A3.8's `manual:due`/`ManualPostCard` affordances alongside A3.7's chips) and any future extension alarm work (the two alarm prefixes are namespaced siblings — a third family adds its own prefix + parse fn + an `onAlarm` branch BEFORE the `parseLaunchAlarm` fall-through, never widening `parseLaunchAlarm`).

## Gotchas log

Things the next implementer must know that aren't obvious from the code. Append-only,
one line each, newest last.

**Closed-wave gotchas (143 of them, Waves 0–3) live in `STATE-ARCHIVE.md` — grep it by task id or filename before touching a file a closed lane built.** What stays below: standing test/lint/typecheck traps, the browser-verification debt Wave 5 must clear, and the four HV/GR facts that still bind an open task (consolidated into ONE carry-forward entry at the end rather than sixteen).

- **`bun run lint` was 114–119 pre-existing errors at baseline** — mostly the `Stratus Design System/` reference folder. **UI.8 (D11) added that folder to biome ignore → whole-repo now 7 errors**, all pre-existing and outside any changed file (2 a11y `Replies.tsx`, 5 format on generated `migrations/meta/*.json`). Per-task gate stays "the changed files lint clean" (`bunx biome check <files>`), not "whole-repo green"; don't chase the remaining 7 unless your task owns those files.
- **UI.9 light theme is an OPENING-GUESS palette, unverified in a browser.** Automated gates pass (632 pass / 0 fail incl. 6 new storage tests, typecheck + build + changed-file lint green) but the 12-tabs × {dark,light} × {cozy,compact} visual QA matrix is explicitly manual and deferred to the Wave-5 polish passes (UI.12–16 done-whens). Known trap (plan risk note): AA contrast on warn/ok/band **text over tinted fills** in light — the fill tokens and semantic ink were re-authored dark-enough to double as text (`--strat-warn: #9a6a10`, band-hot `rgb(0,150,100)`, band-warm-text `#8a6100`), but spot-check before trusting. Adjust `:root[data-theme='light']` values, not the consuming rules.
- **Settings types are extension-local mirrors** — `SettingEntry`/`SettingsGroup`/`SettingsResponse`/`SettingsPatchResult`/`SettingsResetResult` in `extension/src/shared/types.ts` (re-exported via api.ts), mirroring the `GET /x/settings` JSON. The extension MUST NOT import `src/x/settings/registry.ts` (§5 build-isolation). **Binds UI.2/UI.11:** if the server `SettingDef` shape gains a field the editor needs, hand-sync it into `SettingEntry`.
- **`exactOptionalPropertyTypes` pass-through pattern (UI.10):** primitives forwarding a maybe-undefined optional (`Slider.unit`, `SettingRow.onReset`, `GearPopover.onReset`/`label`) type the prop as `T | undefined` explicitly — else TS rejects `prop={maybeUndefined}`. Reuse this for new primitives that relay optionals.
- **UI.10 rail regrouping is UNVERIFIED IN A BROWSER** (like UI.9's palette). Extension typecheck + Vite build + changed-file lint are green and the CSS compiles, but the visual QA (grouped rail spacing, brandmark, EmptyState, popover) rides the Wave-5 polish passes (UI.12–16 done-whens), per D7/D14.
- **`inspect.test.ts` fails 2/2 under bare `bun test`, GREEN under `bun run test` (the canonical, package.json script `SQLITE_PATH=:memory: bun test`).** Introduced by `906f2d3` ("eval 1", out-of-skill Wave-0 audit): `src/db/client.ts` defaults to `:memory:` when `NODE_ENV==='test'` (Bun sets it), but `src/x/data/inspect.ts:101`'s readonly connection still defaults to `./stratus.db` when `SQLITE_PATH` is unset — so under bare `bun test` the primary writes `:memory:` while the inspect readonly reads the file → seeded `people` rows invisible → the 2 LIKE-query tests get `[]`. **NOT RU.3 (bisected: 0 fail at `d1902e5`/ST.9, 2 fail from `906f2d3` onward; RU.3 touches none of inspect.ts/client.ts/the people table).** Full suite is **732/0 under `bun run test`** — always gate with `bun run test`, not bare `bun test`. One-line fix for a data-core/S1 task: make `inspect.ts` honor the same `NODE_ENV==='test'`→`:memory:` default as `client.ts` (or `client.ts` should also `process.env.SQLITE_PATH ??=` set it so both agree).
- **RL.4: adding a registry key is a 4-file move.** `PROMPT_KEYS` + `PROMPT_SPECS` in `prompts/registry.ts`, the default exported from a **PURE** module (a route import cycles — AI.5's `voice/extractPrompt.ts` move exists for that reason), `registry.test.ts`'s key-list + defaultBody assertions, and `docs/settings-tab.md`'s prompt-count copy (now **11**). The extension needs nothing — the Prompts panel renders whatever `GET /x/prompts` returns. CLAUDE.md's AI-layer entry still says "10 keys"; that is a historical phase record and stays — **RL.8's phase entry is where the 11th key gets documented.**
- **RL.5 is UNVERIFIED IN A BROWSER**, like every panel component since UI.9 — no tests were added (repo convention: panels are untested, logic lives server-side). Automated gates only: root+extension typecheck, `cd extension && bun run build`, changed-file lint, full suite 1087/0. The plan's live done-whens (create → add → test-render → generate → Overwrite) ride RL.8's smoke + the Wave-5 QA pass. The one server-behavior assumption the UI leans on was **not** left to faith — the `{humanizer:{}}` → defaults round-trip was proven with a throwaway route test (D81b).
- **RL.8: `cellOf` counts `n` only for MEASURED rows** (those with a `metrics_snapshots` row); `posted` counts every attributed reply. The smoke seeds a snapshot so both move by +1 — a future assertion that publishes a reply without a snapshot must assert `canned.posted`, not `canned.n`, or it will read as a silent failure.
- **NT.1: extension typecheck does NOT cover `*.test.ts`** — `extension/tsconfig.app.json` has `"exclude": ["src/**/*.test.ts"]`, and root `tsconfig.json` only includes `src/**/*` + `scripts/**/*` + `drizzle.config.ts`. So an extension test file is checked by **`bun run test` alone**; a type error in one is invisible to both typechecks. (Same for `earlyReplies.test.ts` — pre-existing, just never written down.)
- **NT.2: a test handle longer than 15 chars is silently skipped and the whole test goes vacuous.** `normalizePersonHandle` enforces `/^[A-Za-z0-9_]{1,15}$/`, so `nt2_mutual_person` (17) was dropped by `recordEngagements` while every assertion around it still "passed" (the person row had been inserted by hand). Cost a debug round-trip. Two rules for any people-layer test: keep seeded handles **≤15 chars**, and assert `{processed, skipped}` on the ingest result so a skip can never hide behind downstream assertions.
- **NT.2: seeding `posts_published` in the shared in-memory DB is safe only with an out-of-window date and no metrics row.** The engagements suite needs own posts for target resolution; it uses a **January `postedAt`** and writes no `metrics_snapshots`, which keeps the rows out of every windowed or measured aggregation other suites assert over (playbook medians, reup 14–60d, brief/digest/quest windows, pinned watch). Files run sequentially so the only real hazard is a stray — cleanup runs inline *and* in `afterAll`. Reuse this shape rather than inventing a new seed.
- **NT.3 test-handle rule (again):** the route suite uses `nt3_route_eng` (13 chars). The NT.2 gotcha applies unchanged — any people-layer test handle over 15 chars is silently skipped by `normalizePersonHandle`, and asserting `{processed, skipped}` is what keeps that from hiding.
- **NT.7: a real-DB smoke that seeds `posts_published` must write the row `retired: true`.** `scripts/smoke-notifications.ts` needs an own post for prefix target resolution; a non-retired one left behind by an aborted run is a candidate for the daily 03:00 UTC snapshot pass, i.e. a **real billed X read** for a tweet id that doesn't exist (invariant #7). `retired: true` takes it out of the candidate set permanently and costs the smoke nothing (it never reads metrics). Reuse this whenever a smoke seeds own posts against the real DB — `smoke-followups.ts` predates the rule and gets away with it only because its rows are deleted synchronously.
- **NT.7: `app.request(...)` is typed `Response | Promise<Response>`.** A helper declared `function f(): Promise<Response> { return app.request(...) }` fails root typecheck; make the helper `async`. `bun run typecheck` covers `scripts/**`, so this bites in smoke scripts, not in route suites (which `await` inline).
- **CLAUDE.md was slimmed to guardrails-only (2026-07-23, out-of-skill docs refactor).** The full phase-entry ledger moved **verbatim** to `docs/PHASE-HISTORY.md`. **Every docs-sync task (MASTERPLAN global rule 6, codemap §7 rule 29 — updated) now appends its phase entry to `docs/PHASE-HISTORY.md`, NOT CLAUDE.md**; CLAUDE.md changes only when a guardrail (invariant, workflow, stack quirk) changes. Plan text still saying "CLAUDE.md phase entry" means the history file.
- **STATE.md was compacted 2026-07-23 (out-of-skill housekeeping, no code touched).** It had reached **339 KB** — past the `Read` tool's 256 KB cap, so Step 0's "read STATE.md fully" silently became a chunked-read investigation every session. 96 closed-wave D-entries + 127 closed-wave gotchas + the header's HV/GR prior paragraphs moved **verbatim** into the new `STATE-ARCHIVE.md` (frozen, 269 KB, grep-only — bigger than one `Read` by design); the live file is **74 KB**. The split was generated by line number, not retyped, and verified as a set operation: every non-empty original line lands in **exactly one** of the two files (438/438, zero duplicated). **What this means for you:** a task touching a file Waves 0–2 built should `grep STATE-ARCHIVE.md` for the task id or filename before assuming the live file tells the whole story — e.g. the RL `/use` contract (D79), the NT.5 icon prefixes (D89), the ST registry contract. SKILL.md gained the matching archive rule in Step 6 (a lane's docs-sync task archives that lane) and its Step-0 staleness guard now compares the **subject line**, matching D97 — it still said `--format=%h` five sessions after shas stopped being recorded.




- **Wave-3 carry-forward (the only HV/GR facts that still bind an open task — everything else is in the archive):** **(1) MCP tool count is asserted exactly.** `src/mcp.test.ts` asserts **21**; every future curated tool bumps that number *and* `docs/s2-mcp-server.md`'s three counts (total / curated tier / write tier) **in the same commit** — that doc was silently 5 tools stale for four phases. **(2) Docs now assert user-visible strings, and two Wave-5 tasks own them.** `docs/harvest-tab.md`, `docs/playbook-tab.md`, `docs/people-tab.md`, `docs/today-tab.md`, `docs/composer-tab.md` and `docs/settings-tab.md` quote real copy (the both-off warn line, `Passive: N rows today`, `insufficient data (n=…)`, `Nobody has shown up often enough yet.`, the three schedule-warning sentences, the debt-tier wording, the `Nd left · need X/day · doing Y/day` line). **UI.14 (People/Channels/Voice/Ideas) and UI.15 (Replies/Playbook/Harvest/Studio) must re-read the matching `docs/*-tab.md` in their own task**, and `docs/today-tab.md` is now numbered §1–§17 — an inserted card renumbers, it does not append. `docs/settings-tab.md` still says **four toggles** in two places: UI.2/UI.11's settings rebuild has to move that count, not just add a row. **(3) Real-DB fixture rules that outlive their lane:** a seeded `posts_published` row is written **`retired: true`** (NT.7 — otherwise it is a candidate for the daily *billed* pass), a `following` fixture needs a `following_runs` row first (`last_run_id` is a notNull FK), people-layer handles must be **≤15 chars** (longer ones are silently skipped and the assertions go vacuous), and any suite touching `scheduled_posts`/`posts_published` inside the monitor's windows must delete its rows in `afterAll` — `monitor.test.ts` asserts `clusterCount === baseline + 1` and a leaked pending pair breaks a different file. **(4) Five reads WRITE and none of them may be polled:** `/radar/drafts` (48h expiry), `/following/queue` (release + revoke), `/x/goals`, `/x/brief` and `/x/digest` (all three settle `active → achieved|missed`). A3.8's Today card and A3.14's week board both sit on `/x/brief` — a refresh loop there silently advances goal statuses.
- **GR.10: the smoke that closes Wave 3 is the template for smoking a surface whose READS write.** `scripts/smoke-guardrails.ts` ($0, no `--live`, rerunnable, verified to leave 0 rows) mounts following + monitor + goals + me + calendar + brief + digest over the real `./stratus.db`. **Copy its safety shape, not `smoke-passive-harvest.ts`'s, whenever a route mutates rows it did not create:** namespaced ids cover only what you wrote, so it *additionally* snapshots every foreign `following.status` before the first write and restores it from the success path **and** from `fail()` (a complete-run reconcile marks every unseen live handle `gone`; a queue read releases rows), snapshot-restores `commitments` and the seeded `streaks`/`digests` keys rather than deleting them, and **closes with one more `GET /x/brief`** so today's C9 streak row is recomputed from clean data instead of describing the burst fixtures it just deleted. **Three things it discovered that no plan text says:** (a) reconcile "unseen" is per **run** (`last_run_id`), not per batch — proving `gone`/`confirmed` needs TWO runs; (b) the queue needs ≥3 slots of budget for its assertions, so the script fails loudly naming `windowUsed/windowCap` + `dailyUsed/dailyCeiling` rather than through a confusing count mismatch; (c) `?factsOnly=true` returns **before** the digest cache write, so a scorecard read caches nothing — but it still settles goals. Assertions over shared/real data are **baseline-relative** where they must be (`nearDuplicate.pairCount`, `scheduleCluster.clusterCount`) and exact where the fixture owns the whole population (`questAdherence`, the queue batch, the ledger statuses).
- **A3.1: the luminance DIRECTION of an opaque Active-times palette is theme-dependent and unverified.** `parseHeatColors` assumes higher scalar = more activity. Alpha ramps (the likely live palette — X blue at varying opacity) are immune: alpha unambiguously rises with intensity. But an OPAQUE light-theme palette runs pale→dark (more activity = LOWER luminance), which would invert the grid. **A3.3's live pass must eyeball one real capture against the on-page heatmap** and, if inverted, flip the luminance branch in `src/shared/activeTimes.ts` (one line) — do not "fix" it by re-normalizing downstream. Also live-verify the two structural guesses in `extractActiveTimesSection`: cells are inline-background LEAF divs, and the layout is either 7 parent groups or one flat rows-of-7 container — every selector/hop is a named constant at the top of `extension/src/shared/activeTimes.ts`.
- **A3.1: `extension/tsconfig.app.json` include is now a 4-entry array** (`src/**/*` + the three out-of-tree canonicals). A future shared module needs its canonical path appended there or `cd extension && bun run typecheck` fails on the out-of-tree import — the error names the file, but only if you run that gate (root typecheck won't see it).
- **A3.2: the `/x/analytics/active-times` GET returns the FULL DB row (incl. `id`) under `capture`, and `?history=` returns `{count, captures}` newest-first** — A3.4's Composer blending and any extension type mirror should model `{capture: AudienceCapture | null}` off that, not the plan's field list. A3.3 needs NO server-side suppression: the route is append-only and a duplicate POST is harmless by design; the 12h/`gridsEqual` gate lives client-side in `chrome.storage.local`. Also: whole-repo `bun run lint` is now **0 errors** (the two `Replies.tsx` a11y stragglers noted at D11/D13 are gone — fixed somewhere in Wave 3; per-task gate stays "changed files lint clean").
- **GR.10 lint trap (new, and it cost a round-trip):** biome's `useTemplate` + `noUnusedTemplateLiteral` reject a `console.log` built as `` `a ${x}` + 'b' `` or `` `plain` + `b ${x}` `` — a concatenation is only accepted when **every** operand is a template literal that actually interpolates (that is why `smoke-passive-harvest.ts` passes with the same shape). `bunx biome check --write` will NOT fix it; the fixes are classed unsafe. Run `bunx biome check --fix --unsafe <file>` on a new script, then re-check.
- **A3.3: the live Active-times DOM, verified 2026-07-24 (all via in-page probes, x.com/i/account_analytics/audience):** the grid is a FLAT rows-of-7 row-major CSS grid, 7×24 (hourly rows — not the plan's guessed 48; `audienceScoreFor`'s 24-row passthrough covers it), H2 heading, found at ancestor hop 1; it renders TWICE — a hidden responsive twin PRECEDES the visible grid in DOM order and `collectGrid` picks it, which is safe because both twins carry identical per-cell opacities (verified equal, all 168). The visible grid's container has 8 grid tracks (7 day columns + a 32.5px hour-rail track) but the rail labels aren't background-styled leaves, so the 168-group stays clean. Sub-tab navigation rewrites `location.pathname` under the same `/i/account_analytics` prefix (`/overview`, `/audience`) without reloads.
- **A3.3: the `parseHeatColors` luminance branch is still direction-unverified** — the live palette is an opacity ramp (alpha mode), so the luminance path is only reachable if X ships an OPAQUE palette someday; the A3.1 warning (pale→dark light-theme palette would invert the grid) transfers to whoever first sees a capture that disagrees with the on-page heatmap. The alpha half of the A3.1 gotcha is RESOLVED — direction confirmed correct against the live page (Sa hottest column reproduced).
- **A3.3 verification debt (2-minute user check, not a code risk):** the loaded-extension end-to-end (visit Audience → one row in `audience_activity`; revisit <12h → no new row; Settings passiveCapture off → no capture) needs `./scripts/deploy.sh` (prod predates A3.2's route) + a chrome://extensions reload of the unpacked extension — neither is automatable from a session. Everything upstream of the POST (path gate, extraction, palette parse, payload shape, metric read) was verified live in-page; the transport is the byte-tested flushSightings idiom.
- **A3.4: `suggestBestSlotDate`'s `audience` is the SIXTH arg, after `rand`.** A caller that wants blending must pass `rand` explicitly — the Composer does `suggestBestSlotDate(now, slotted, bestCells, SLOT_HORIZON_DAYS, Math.random, audience)`. Every pre-A3.4 5-arg call is untouched and behaves identically (audience defaults undefined → all audienceScores null → tier 2 collapses to earliest). Same shape on the server: `annotateGaps(gapHours, cells, localWeekday, audience?)`.
- **A3.4: the full `audience_activity` DB row is fed straight to `audienceScoreFor` as an `ActiveTimesGrid`** — it is a structural superset (extra `id`/`capturedAt` are ignored), so `brief.ts` does `const audienceGrid: ActiveTimesGrid | null = audienceRows[0] ?? null` with no cast and the Composer passes its `AudienceCapture` state directly. If a future change narrows the select's columns, keep `cols`/`rows`/`grid`/`tzOffsetMin`/`metric` or the assignment stops typechecking. `AudienceCapture.capturedAt` is an **ISO string** over the wire (a `timestamp_ms` Date serialized by `c.json`), which is why the Composer's staleness check is `new Date(audience.capturedAt).getTime()`.
- **A3.5: `mark-posted` returns the row with `postedTweetId` still null** — linking is the A3.6 reconcile's job (decision 6); a client wanting the tweet id after marking waits for the daily pass. A3.7's Calendar "mark posted" action and A3.8's Today card should render off `status`, never off `postedTweetId`.
- **A3.5/A3.7: calendar-fixture day bands are now spoken for:** GR.6 advisory suite 90–130d out, A3.5 manual suite 150–160d, brief monitor pair ~200d, **A3.7 manual-warns-near-pending pair ~300d** (`calendar.test.ts`'s second describe). A future suite seeding `scheduled_posts` with far-future `scheduledFor` picks a fresh band ≥45 min (SCHEDULE_CLUSTER_MS) clear of all four, and any TODAY-window calendar fixture must either be non-`pending`/non-`manual` or clean up before brief/monitor assertions run (brief.test's A3.5 manual fixture is the sanctioned shape). **Note since A3.7:** `scheduleWarnings` now counts BOTH `pending` and `manual` neighbors — a `manual` fixture within 45 min of a `pending` one now cross-warns where it wouldn't before, and `nearDuplicate`/pendingTwin has NO time window (identical text in any band cross-matches). The A3.5 suite's two `URL_TEXT` manual rows (at 150 d + 152 d) now produce a harmless pendingTwin warning on the second create; no assertion checks it, so it's fine — but a NEW manual fixture reusing an existing text will warn.
- **A3.7: `nextEditStatus` is a Composer-local pure helper with NO unit test** — the plan only mandated the cost-estimate test, and the extension convention is panels-are-browser-verified. Its edit-transition table (manual↔API↔draft↔pending over a single row) is covered by typecheck + the browser-verify debt below, not bun:test. If a later task needs to assert it, extract it or exercise it through a route round-trip (there's no composerLogic home for it — it references `PostStatus`, which lives in `shared/types.ts`).
- **A3.7 browser-verify debt (2-minute user check over the LOADED extension, not a code risk):** the whole manual-publish UI is panel-convention unverified — (a) Composer: the API|Manual toggle appears for single posts only (hidden in thread mode), Manual auto-selects when editing a `media_note` row, the cost line reads `$0 · you paste it`, the URL warning is gone, submit is blocked until a time is set, and a manual create lands a `manual` row; (b) Calendar: the manual row shows the pill + an amber `.overdue` tint once its slot passes, and **Mark posted** flips it to `posted` then the chip updates on reload; (c) Today: the manual pill's paste-hint title. Needs `./scripts/deploy.sh` (prod predates A3.5's route) + a chrome://extensions reload. Everything server-side (warnings over manual, mark-posted transition, cost math) is unit-tested.
- **A3.6: `reconcileManualPosts` runs on EVERY daily pass and on every `POST /posts/reconcile`, and it is a WRITE that never re-fires per row.** It selects `scheduled_posts` `status IN ('manual','posted')` with `posted_tweet_id IS NULL` — so once a row is linked (its `posted_tweet_id` stamped) it drops out of the candidate set permanently; a smoke seeding a manual row + a matching `posts_published` original and calling `reconcileManualPosts` directly (A3.15's plan) sees the link happen ONCE. The matcher is exported pure from `src/x/posts/manualReconcile.ts`; the impure wrapper lives in `dailyMetrics.ts` and is NOT exported (unit-tested via the matcher; the wrapper is smoke-covered per plan). **`RunResult` gained `manualLinked`** — the only consumer is `POST /posts/reconcile` (returns the whole result as JSON), so it's a safe additive field; no test asserts the result shape.
- **A3.6: the reconcile deliberately never touches `posts_published.retired`/`pollCount`/`isReply`, only `scheduled_post_id`** — so it slots between `discover` and `snapshotDue` with zero interaction (both later steps key off `retired`/`pollCount`/`isReply` for their candidate sets). If a future daily-pass step starts reading `scheduled_post_id` as a candidate filter, re-examine the ordering.
- **A3.8: manual reminders are a SIBLING of the C7 launch machinery, not a fork.** `background.ts` now has TWO alarm families with namespaced prefixes (`stratus-launch:`/`stratus-launch-retry:` vs `stratus-manual:`) and TWO session-key chains (`launchChain` for `launch:*`, `manualChain` for `manual:due`); `syncManualAlarms()` runs beside `syncLaunchAlarms()` at every trigger (module-load, `stratus/launch-sync`, the 15-min `LAUNCH_SYNC_ALARM`). A future alarm family copies the shape — its own prefix + `parseX` + an `onAlarm` branch placed **before** the `parseLaunchAlarm` fall-through — and must **never widen `parseLaunchAlarm`** (it would swallow the new family's names). There is deliberately NO retry: `handleManualFire` fires once per alarm; a still-unposted slot re-fires only because the sync re-creates the clamped alarm inside the 30-min miss window, and `mergeManualDue`'s postId dedup keeps the card single.
- **A3.8: the pure core in `extension/src/shared/launch.ts` is fully unit-tested; `ManualPostCard.tsx` is browser-verify-only** (panel convention — no bun test, like every panel since UI.9). The card READS `manual:due` from `chrome.storage.session` and **must not write it** (§7.24 single-writer): Copy is local, but Mark posted and dismiss both send `stratus/manual-dismiss {postId}` and let the background mutate the key. It renders off the stored entry / `status`, **never `postedTweetId`** (still null until the A3.6 daily reconcile links the pasted tweet). `computeManualAlarms`/`mergeManualDue` reuse the existing `SchedulablePost`/`LaunchAlarm` types (not new ones) — a manual alarm IS just `{name, when}`.

## Planning-error log

Corrections to MASTERPLAN.md itself (wrong dep, wrong order discovered live).

- **2026-07-16-niche.md Task 3 done-when** — the "`Pitești` hits nothing" grep contradicts the same task's bit-for-bit equivalence requirement (post prompt §6/§9/§0 carry builder biography outside the §1/§5 extraction scope; no niche field exists to hold them). Resolved at N.3 in favor of equivalence; see D28b for the full resolution + what N.9 must scope differently. Source plans are never edited — this entry is the correction.
- **2026-07-17-ai-layer.md — no task owns the post drafter's askLLM migration.** Task 5's edit list names replies/pillars/playbook/voiceExtract; Task 6 only flips drafter's MOUNT gate; Task 7 adds a sibling route. Yet the plan's Why-section + Done-when #1 spirit require every draft surface to honor the provider setting, and the Task-6 gate flip would 502 `/posts/draft` on OpenRouter-only setups. Resolved at AI.5 by migrating `drafter.ts` there (D47b) — the call-site-migration task was the right home.

