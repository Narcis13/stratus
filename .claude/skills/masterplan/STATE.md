# Masterplan execution state

> Dynamic memory for `/masterplan`. Updated after EVERY task, in the task's own commit.
> Plan: `plans/MASTERPLAN.md` (static — order, reasoning levels, waves, D1–D10).
> Codemap: `.claude/skills/plan-feature/references/codemap.md` (updated per task too).
> History: `.claude/skills/masterplan/STATE-ARCHIVE.md` (frozen, grep-only — closed-wave deviations + gotchas).

**Size discipline (added 2026-07-23, when this file hit 339 KB and stopped fitting in one `Read`):** STATE.md must stay loadable in a single `Read`. What lives here is (a) standing rules, (b) entries for the OPEN lane / current wave, (c) unpaid verification debt, (d) the ledger + locks. When a sub-plan closes, its D-entries and gotchas move verbatim to `STATE-ARCHIVE.md` — that move is part of the sub-plan's docs-sync task, not a separate chore.

- **last-commit:** **identity is the SUBJECT LINE — no sha is recorded any more (D97).** HEAD should read `feat(cost): log expanded includes on X reads to confirm ledger gap (CA.2 step 1)` — the second of two **out-of-masterplan CA commits** (plan `plans/2026-07-23-cost-audit-followups.md`), executed 2026-07-23 at explicit user direction and deployed to prod the same day so the next 03:00 UTC pass runs with them; parent = CA.1 `fix(mentions): page inbox pull past 50 without widening max_results (CA.1)`, whose parent is `30b0583` (= GR.3). **Step 0 is one command:** `git log -1 --format='%h %s'` — if the subject names the last ticked task (or the CA line above), there was no out-of-skill work. `git log --oneline -4` should show CA.2 ← CA.1 ← GR.3 ← the out-of-band cost fix. Earlier parent chain (GR.3 back through HV.4) and the D93a/D94a/D95a sha-orphan saga that produced D97 → archive.
- **current wave:** **WAVE 3 — the HV sub-plan is CLOSED (HV.1–HV.6 all ✓); the GR lane is OPEN (GR.1✓ GR.2✓ GR.3✓, GR.4–GR.10 left).** Waves 0–2 closed (RU.1–10✓ AX.1–7✓ RL.1–8✓ NT.1–7✓). No locked files — GR.1 touched only new files plus one mount line. **The migration journal is FREE again** (GR.1 took `0019_messy_blue_shield`; the next migrating task owns `0020` — GR.7's `me_goals` extension is the only GR task that still needs one). **The D9 `content.ts` chain is DONE through HV.2✓** [AX.3✓→AX.5✓→RU.7✓→RU.8✓→NT.5✓→HV.2✓→**A3.3**]; the file is RELEASED and A3.3 (Wave 4) is its last claimant. **D2 (UI.2, Wave 5) still pending decision** — the 5 doctrine knobs are owned by `niches.doctrine` since N.5; UI.2 must not duplicate them in `app_settings` (D30c). **D93a is binding on every remaining docs-sync task:** phase entries go to `docs/PHASE-HISTORY.md`, no longer to CLAUDE.md (codemap §7.29).
- **next-up:** **GR.4** is the next masterplan task, but **one pending CA.2 verification rides ahead of everything on the first session after a 03:00 UTC pass ran in prod (i.e. any session on/after 2026-07-24):** grep the prod journal for `[cost-audit]` (`journalctl -u stratus | grep cost-audit`) — if the includes.tweets counts ≈ the console-vs-`/cost/today` gap, execute **CA.2 step 2** (preferred: drop `referenced_tweets.id` from the metrics-read expansions in `fields.ts` — `in_reply_to_tweet_id` survives on the tweet's own `referenced_tweets` field; fallback: bill `data.length + includes.tweets.length` in `itemCount`) and write the confirm one-liner into `docs/PHASE-HISTORY.md`; if refuted, write the refute one-liner and remove the instrumentation. **CA.1 + CA.2 step 1 are DONE and deployed** (see last-commit). Then the GR lane: **GR.4** (People-tab Following subtab, deps GR.2✓+GR.3✓ — `extension/src/sidepanel/People.tsx`, imitate the Voice.tsx Roster|Following subtab; `api.ts` gains `following.queue()/list()/patch()`), then GR.5 (monitor, its OWN file `src/x/routes/monitor.ts`), GR.6–GR.10. GR.3 was the only thing gating the lane. **D4 stays binding on GR.7.** **Gate reminders (unchanged):** canonical test command is `bun run test` (bare `bun test` targets the file DB); biome forbids `delete process.env.X` and `: any` in `scripts/`; biome **sorts imports case-sensitively** and **collapses short chained calls onto one line** — run `bunx biome check --write` on changed files rather than hand-formatting. Extension-touching tasks add `cd extension && bun run build` **and** `cd extension && bun run typecheck` to the bar.
- **prior (CA.1 + CA.2 step 1, 2026-07-23, out-of-masterplan URGENT jump):** two commits, both server-only, **$0**, no migration/route/extension change, suite 1224→**1228** (+4 in NEW `src/x/mentions.test.ts`). **CA.1:** `getUserMentions` gained `maxTotal` (TOTAL ceiling across pages; per-request `max_results` clamp untouched — invariant #5) and `pullMentions` incremental pulls (live since_id) now page to `MENTIONS_HARD_CAP = 800` — before this, prod silently dropped every mention past 50/day since Jul 14 (the tail fell below the next pull's checkpoint forever). Caller-supplied `maxResults` stays a TOTAL cap (route ≤200 + smoke `--live` ≤10 promises hold); a cold pull keeps the bounded 50. The test stubs `globalThis.fetch` with 9.2e18-prefixed multi-page fixtures (above all other suites' ids in the shared in-memory DB) and omits `includes.users` so no people rows are written. **CA.2 step 1 (instrument-only):** `xFetch` logs `[cost-audit] <method> <endpoint>: data=N includes.tweets=N includes.users=N` on 2xx bodies carrying `includes` — the expansions drag reply parents into `includes.tweets`, which `itemCount` never counted (suspected mechanism of the console $0.60 vs ledger $0.33 divergence; timing matches the Jul-17 reply 10×). **No `cost_usd` change was made** — step 2 is gated on a real 03:00 pass (see next-up).
- **prior (GR.3):** the following-curation queue — **server-only, $0, no schema/migration/CSS/extension change** (suite 1211→**1224**, +13). New pure `src/x/connections.ts` (followups.ts style — no DB/clock/`Math.random`): `GRACE_DAYS=7`, `UNFOLLOW_WINDOW_MS=6h`, `WINDOW_CAP_MIN/MAX=15/18`, `DAILY_CEILING=40`, `MARK_LOOKBACK_MS=24h`; `eligibleForUnfollow` (active + `!followsBack` + `!keep` + `∉whitelist` + past-grace, `rankForUnfollow` = oldest first / `listPosition` desc / handle asc), `reviewQueued` → `{held, revoked}`, `releaseBudget` (jittered cap − window marks, daily-ceiling zero). `GET /following/queue` (registered ABOVE `/:handle`) is fully read-time (§7.12): whitelist = people mutual/ally non-retired + `loadTargetHandles()`; `keep`/`followsBack` enforced as columns; releases `active→queued` and revokes `queued→active` in one sync txn; **idempotent by topping up to `budget`** (D101a). Payload `{batch, eligibleTotal, releasedNow, windowUsed, windowCap, dailyUsed, dailyCeiling, lastCompleteRunAt}`. `following.ts` is RELEASED; `connections.ts` is NEW (GR.5's churn monitor imports its `DAILY_CEILING`). **Code-verified only** — the ledger it reads is still browser-UNVERIFIED (GR.2's tail).
- **prior (out-of-band cost fix, 2026-07-23):** the 2026-07-23 cost audit shipped **fix 1 + fix 2** directly into `src/x/workers/dailyMetrics.ts` (not a numbered masterplan task — a money leak caught mid-session). **The discovery timeline pull now IS the snapshot read:** it requests `ownedPrivate`, and new pure `ingestPulledTweet(tweet, now)` writes row + `metrics_snapshots` + retire in ONE idempotent sync txn keyed off the existing row. Before this, `discover()` pulled cheap fields and `snapshotDue()` re-read the SAME ids minutes later — every own tweet billed twice/day (~$0.09/day dupe at 90 tweets/day). `snapshotDue()` is now the **leftover** backstop (rows below the checkpoint / failed batches); steady-state it reads nothing. `fullScan` keeps the old two-phase seed-then-read (30-day private-field window). Replies dropped from the winner re-read (`isReply=false`) — their snapshot rides free on the pull, but a day-7 re-read would be a real extra call and "which content compounds" is about posts. `discover()` wrapped in try/catch so a pull failure degrades to the old shape instead of skipping snapshot+reread+mentions. New `RunResult.pullSnapshotted`. Tests +4 (`ingestPulledTweet` idempotency/adopt/reply), suite 1207→**1211**. Full history in `docs/PHASE-HISTORY.md`. **The two DEFERRED findings are CA.1/CA.2 above.**
- **earlier priors (GR.2, GR.1, HV.6 … HV.1):** moved verbatim to `STATE-ARCHIVE.md`. Their forward-binding content survives here as the HV/GR gotchas below.

## Ledger

Status: `[ ]` todo · `[~]` in progress (lane claimed) · `[x]` done (sha + date) · `[s]` skipped (reason in deviations).

### Wave 0 — Foundations
- [x] UI.1 Settings platform (xhigh) — 226368c (code+format+state+codemap; D12 absorbed into 61a04e7) 2026-07-18
- [x] UI.8 Design tokens dark refactor (high) — 1299e43 (styles.css in 61a04e7 per D12) 2026-07-18
- [x] UI.9 Light theme + Appearance (high) — f9ff346 2026-07-18 (sha corrected from orphaned 8c89950)
- [x] UI.10 UI primitives + grouped rail (xhigh) — c37d504 2026-07-18 (sha corrected from orphaned 50948c9 — see D17)
- [x] ST.1 Engine layers + PRNG (high) — 61a04e7 2026-07-18
- [x] ST.2 Studio shell refactor (high) — dffd60a 2026-07-18 (sha corrected from orphaned 860725f — see D16)
- [x] ST.3 Cloud mascot (high) — b82058b 2026-07-18 (sha corrected from orphaned 292603d — see D17)
- [x] ST.4 Patterns + presets (high) — 15f2563 2026-07-18 (sha corrected from orphaned c1b0314 — see D18)
- [x] ST.5 Milestone + streak cards (high) — 1f6b20f 2026-07-18 (sha corrected from orphaned bc3f1e8 — see D19)
- [x] ST.6 Code card + mono font (high) — 00b9615 2026-07-18 (sha corrected from orphaned b65266f — see D19/D20)
- [x] ST.7 Thread cover + list card (high) — acd95ec 2026-07-18 (sha corrected from orphaned 2104f46 — see D21)
- [x] ST.8 Chart card (high) — 289ec2c 2026-07-18 (sha corrected from orphaned d38bbc5 — see D22)
- [x] ST.9 Studio docs+smoke (high) — d1902e5 2026-07-18 (sha corrected from orphaned 47b10d2 — see D23)

### Wave 1 — Prompt & identity core
- [x] RU.1 Reply prompt → 3 variants (xhigh) — 63df913 2026-07-18 (sha corrected from orphaned 9c05464 — see D24)
- [x] RU.2 Migration: source/variants/model columns (high) — ddecb5f 2026-07-18
- [x] RU.3 Batch → 3 variants (xhigh) — d8ecc9a 2026-07-18
- [x] N.1 Niche schema + defaults + store (xhigh) — 0bbca12 2026-07-18 (sha corrected from orphaned da97495 — see D27)
- [x] N.2 Niche CRUD + activation + MCP (high) — c76a29b 2026-07-18 (sha corrected from orphaned 4312a0e — see D28)
- [x] N.3 Post prompt persona extraction (MAX) — 3b1547c 2026-07-18 (sha corrected from orphaned b06770c — see D29)
- [x] N.4 Reply prompt persona extraction (MAX) — 0668451 2026-07-18
- [x] N.5 Doctrine knobs threaded (high) — 7e9213d 2026-07-18 (sha corrected from orphaned 20faa44 — see D31)
- [x] N.6 Pillar/channel niche ownership (xhigh) — 730a009 2026-07-18
- [x] N.7 Settings Niche card (high) — 25a8c99 2026-07-18
- [x] N.8 Niche wizard (high) — 5c1031d 2026-07-18 (sha corrected from orphaned 527ad21 — see D35)
- [x] N.9 Niche docs+smoke (high) — 78d1d93 2026-07-18
- [x] ME.1 me_entries/me_goals + profile module (high) — `0545185` 2026-07-18 (sha corrected from orphaned f0c91f4 — see D37; N.9 sha reconciled e452f7c — see D36)
- [x] ME.2 /x/me routes + loader + smoke (high) — `43e6bc9` 2026-07-18 (sha corrected from orphaned a3a94bb — see D38)
- [x] ME.3 Inject me-context at prompt tails (xhigh) — `82f0291` 2026-07-18
- [x] ME.4 Me tab (high) — `5a4b427` 2026-07-18 (sha corrected from orphaned 92499ab — see D40)
- [x] ME.5 Playbook lift + digest goals fact (high) — `9df22ff` 2026-07-18 (sha corrected from orphaned d611dce — see D41)
- [x] ME.6 MCP x_me + x_add_me_entry tools (high) — `d4ef3dc` 2026-07-18 (sha corrected from orphaned 8686d79 — see D42)
- [x] ME.7 Me docs-sync (high) — `f010bf3` 2026-07-18 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.1 OpenRouter client (xhigh) — `fc5b3c4` 2026-07-18 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.2 askLLM + /llm routes — D1 (xhigh) — `3d3598c` 2026-07-18 (sha corrected from orphaned 87f3bd7 — see D45a)
- [x] AI.3 Prompt registry + overrides (MAX) — `a087a66` 2026-07-18 (sha corrected from orphaned 9f33fb2 — see D45b)
- [x] AI.4 /x/prompts routes (high) — `3c649ce` 2026-07-18 (sha corrected from orphaned 466b090 — see D47a)
- [x] AI.5 Batch/extract/pillar-draft via registry — D3 (MAX) — `7d0a786` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.6 Digest/icebreaker via registry + LLM gate (xhigh) — `e2c91ac` 2026-07-19 (sha corrected from orphaned b82f4ef — see D49)
- [x] AI.7 Thread drafter (xhigh) — `b932a06` 2026-07-19 (sha corrected from orphaned 995a08b — see D50)
- [x] AI.8 Rewrite assist (high) — `b9fe98d` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.9 Idea generator (high) — `8e53621` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.10 Settings AI subtab (high) — `a91387f` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.11 Prompts editor panel (high) — `e7d5be9` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.12 Model-effectiveness cell (high) — `9c59183` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.13 AI docs+smoke (high) — `f86e06a` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles) — **closes Wave 1**

### Wave 2 — Reply machine & on-page surfaces
- [x] RU.4 Buffer/panel variants (high) — `22cf0b1` 2026-07-20 (sha corrected from orphaned a3664af — see D58)
- [x] RU.5 Confirm endpoint (xhigh) — `5935107` 2026-07-20 (sha corrected to `983fbea` — see D59; recorded sha was an amend-orphan)
- [x] RU.6 Panel confirm wiring (high) — `1ed57bd` 2026-07-20 (sha corrected from orphaned 6261673 — see D60)
- [x] AX.1 Glance endpoint (high) — `9467220` 2026-07-20 (sha corrected from orphaned f1d85c2 — see D61)
- [x] AX.2 Glance chip view-model (high) — `e29b629` 2026-07-20 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AX.4 tweetContext view-model (high) — `7ecb8df` 2026-07-21 (sha corrected from orphaned 7abec86 — see D64)
- [x] AX.3 Timeline chips, pill removal (xhigh) — `39fecb6` 2026-07-21 (sha corrected from orphaned bc2c9fe — see D65)
- [x] AX.5 Status-page context panel (xhigh) — `5e31b8d` 2026-07-21 (sha corrected from orphaned 0eb5319 — see D66)
- [x] AX.6 Dossier click-through (high) — `b3fd7b3` 2026-07-21 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] RU.7 On-page variant chips (xhigh) — `f90084b` 2026-07-21 (sha corrected from orphaned bfa2441 — see D69)
- [x] RU.8 Manual add ⊕ (xhigh) — `9ba6fba` 2026-07-21 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] RU.9 Playbook source attribution (high) — `f78b48c` 2026-07-21 (sha corrected from orphaned 899e126 — see D72)
- [x] RU.10 RU docs+smoke (high) — `22099a3` 2026-07-21 (sha stamped post-commit; amend re-orphans it, next session reconciles) — **closes the RU sub-plan**
- [x] AX.7 AX docs+smoke (high) — `00ec8a8` 2026-07-21 (sha stamped post-commit; amend re-orphans it, next session reconciles) — **closes the AX sub-plan (AX.1–AX.7)**
- [x] RL.1 Reply-list engine (xhigh) — `ffbdb3f` 2026-07-22 (sha corrected from orphaned 58903fc — see D77)
- [x] RL.2 Schema + CRUD (high) — `9c0d97b` 2026-07-22 (sha corrected from orphaned a29ffbe — see D79a)
- [x] RL.3 /use route (high) — `c3f44d2` 2026-07-23 (sha corrected from orphaned 0ea02a3 — see D80a)
- [x] RL.4 AI generator — D5 (high) — `99eef6b` 2026-07-23 (sha stamped post-commit; amend re-orphans it, next session reconciles by subject)
- [x] RL.5 Lists subtab UI (high) — `17a530f` 2026-07-23 (sha stamped post-commit; amend re-orphans it, next session reconciles by subject)
- [x] RL.6 QuickReplyPicker (high) — `aae4665` 2026-07-23 (sha stamped post-commit; amend re-orphans it, next session reconciles by subject)
- [x] RL.7 Playbook canned bucket (high) — `3a4ae56` 2026-07-23 (sha corrected from orphaned b2543e7 — see D84a)
- [x] RL.8 RL docs+smoke (high) — `a120207` 2026-07-23 (sha corrected from orphaned 7ac1b5e — see D85c) — **closes the RL sub-plan (RL.1–RL.8)**
- [x] NT.1 Notification parser (high) — `2063b86` 2026-07-23 (sha corrected from orphaned 7f3d2fd — see D86a)
- [x] NT.2 Engagement events + ingest (xhigh) — `4e4d9fc` 2026-07-23
- [x] NT.3 Engagements route (high) — `2b763b1` 2026-07-23 (sha stamped post-commit; the amend re-orphans it, next session reconciles by subject)
- [x] NT.4 Background notif-context (high) — `fe0f6aa` 2026-07-23
- [x] NT.5 Notifications content script (xhigh) — `69784da` 2026-07-23 (sha corrected from orphaned 3a94830 — see D90a; NT.4 sha reconciled to `68039f2` — see D89a)
- [x] NT.6 Fans engagement count (high) — `0c0ab5d` 2026-07-23 (sha corrected from orphaned 532f87d — see D91a)
- [x] NT.7 NT docs+smoke (high) — `5d4a1a4` 2026-07-23 (sha corrected from orphaned 75f15e7 — see D93a) — **closes Wave 2**

### Wave 3 — Ambient data & guardrails
- [x] HV.1 Passive harvest endpoint (xhigh) — `5ea3495` 2026-07-23 (sha corrected from orphaned bf5e7b5 — see D93a)
- [x] HV.2 Passive capture + toggle (xhigh) — `661d039` 2026-07-23 (sha corrected from orphaned 090a8ac — see D94a)
- [x] HV.3 Harvest tab options (high) — `4c2adcf` 2026-07-23 (HV.2 sha reconciled to `661d039` — see D94a)
- [x] HV.4 Timeline affinity (high) — `3132f10` 2026-07-23 (HV.3 sha reconciled to `5cd316a` — see D95a)
- [x] HV.5 Funnel playbook cell (high) — subject `feat(playbook): timeline opportunity-capture funnel over passive harvest rows (HV.5)`, parent `855b424`, 2026-07-23 (no sha by design — D97)
- [x] HV.6 HV docs+smoke (high) — subject `docs(harvest): Harvest 2.0 docs-sync + $0 smoke-passive-harvest.ts (HV.6)`, parent `3416f14`, 2026-07-23 (no sha by design — D97). **Closes the HV sub-plan.**
- [x] GR.1 Following ledger (xhigh) — `feat(following): ledger schema + ingest/ratchet routes (GR.1)` 2026-07-23
- [x] GR.2 Harvester following mode (xhigh) — `feat(following): harvester following-mode scrape (GR.2)` 2026-07-23
- [x] GR.3 Curation queue (xhigh) — `feat(following): curation queue with jittered cadence caps (GR.3)` 2026-07-23
- [ ] GR.4 Following subtab (high)
- [ ] GR.5 Monitor rules + route + MCP (xhigh)
- [ ] GR.6 Monitor surfacing (high)
- [ ] GR.7 Goals + commitments — D4 (xhigh)
- [ ] GR.8 Accountability surfacing (high)
- [ ] GR.9 Digest scorecard (high)
- [ ] GR.10 GR docs+smoke (high)

### Wave 4 — Authoring 3.0
- [ ] A3.1 Active-times parser (xhigh)
- [ ] A3.2 audience_activity + routes (high)
- [ ] A3.3 Analytics capture (xhigh)
- [ ] A3.4 Audience-blended slots (xhigh)
- [ ] A3.5 Manual publish core (MAX)
- [ ] A3.6 Manual reconcile (xhigh)
- [ ] A3.7 Composer toggle + chips (high)
- [ ] A3.8 Manual alarms + Today card (xhigh)
- [ ] A3.9 DM drafts — D5 (xhigh)
- [ ] A3.10 DM UI (high)
- [ ] A3.11 Articles CRUD (high)
- [ ] A3.12 Article prompt + assist — D5 (xhigh)
- [ ] A3.13 Writer page (xhigh)
- [ ] A3.14 Calendar week board (xhigh)
- [ ] A3.15 A3 docs+smoke (high)

### Wave 5 — Settings moat + polish
- [ ] UI.2 Doctrine/quests knobs — D2 (xhigh)
- [ ] UI.3 People/followups knobs (high)
- [ ] UI.4 Gates/workers knobs + MCP (high)
- [ ] UI.5 Money/AI knobs + ceilings (xhigh)
- [ ] UI.6 Extension mirror (xhigh)
- [ ] UI.7 Band thresholds e2e (xhigh)
- [ ] UI.11 Settings tab rebuild — D6, D8 (xhigh)
- [ ] UI.12 Today polish (high)
- [ ] UI.13 Composer/Calendar polish (high)
- [ ] UI.14 People/Channels/Voice/Ideas polish (high)
- [ ] UI.15 Replies/Playbook/Harvest/Studio polish (high)
- [ ] UI.16 Content-script + explorer tokens (high)
- [ ] UI.17 UI docs+smoke (high)

## Hot-file locks

A lane claims a file before starting, releases on commit. `owner: —` = free.

| File | Owner |
|---|---|
| `extension/src/content.ts` | — |
| `extension/src/harvester.ts` | — (GR.2 released it; `runHarvest`'s ingest param is now a `ship(profile, store, outcome)` step and the mode dispatch lives in `startRun` — a fourth mode is one branch there) |
| `extension/src/sidepanel/Settings.tsx` | — |
| `extension/src/sidepanel/Composer.tsx` | — |
| `src/x/routes/replies.ts` + `src/x/replies/prompt.ts` | — |
| `post prompt.md` / `reply prompt.md` (+ TS literals) | — |
| `src/x/playbook.ts` + `src/x/routes/playbook.ts` | — (HV.5 released them; the pure file's sections now run to **§12** — the next `build*` is §13) |
| `src/db/migrations/` journal (any migration task) | — (GR.1 took `0019`; next migrating task owns `0020`) |
| `src/x/routes/following.ts` | — (GR.3 released it; the file now holds runs/rows/GET/**queue**/PATCH with `/following/queue` ABOVE `/:handle` (§7.20). GR.5's `GET /monitor` goes in its OWN file `src/x/routes/monitor.ts`, not here) |
| `src/x/connections.ts` | — (NEW at GR.3; the pure curation module. GR.5's `unfollowChurn` monitor rule imports `DAILY_CEILING` + reads `following.unfollow_marked_at` from here — don't re-derive the constant) |
| `src/x/routes/brief.ts` | — |
| `src/x/routes/harvest.ts` | — (HV.4 released it; the router is now POST runs/rows/passive + GET runs/affinity — still no `:param` route, keep it that way. **HV.6 added no route; `scripts/smoke-passive-harvest.ts` now mounts this router + playbook over the real DB, so a route rename breaks the smoke as well as the suite.**) |
| `extension/src/shared/harvest.ts` + `sidepanel/Harvest.tsx` | — (GR.2 released them; nothing before UI.15's polish pass claims them again. New sibling `shared/userCell.ts` is free too) |

## Deviations & decisions register

Pre-seeded from cross-plan analysis (see MASTERPLAN "Cross-plan adaptations" for full text).
Append D11+ as work reveals divergences from plan text.

**Closed-wave D-entries (96 of them, Waves 0–2 + the HV sha reconciliations) live in `STATE-ARCHIVE.md`.** What stays below: the pre-seeded cross-plan adaptations (D1–D11, D13), the standing bookkeeping rule (D97), and every entry that binds a task still marked `[ ]` (D93 docs-sync, D96a–c + D98 playbook/docs shape, D99–D101 the open GR lane).

- **D1** (binds AI.2): `app_settings` created once by UI.1; AI.2 reuses it — no second table/migration.
- **D2** (binds UI.2): the 5 doctrine knobs are owned by `niches.doctrine` after N.5; UI.2 must not duplicate them in `app_settings` — read/write through the active niche or drop the keys. **Decision pending at UI.2.**
- **D3** (binds N.4, AI.5): `VOICE_BLOCK_END` renamed by RU.1 to `'## The three variants'`; N.4 substitutes `{{REPLY_PERSONA}}` inside the slice; AI.5 retires slicing — its parity fixture is post-RU/post-N output, and its anti-drift test asserts against defaults containing the niche placeholder.
- **D4** (binds GR.7): no separate `goals` table — extend `me_goals` (baseline fields, `posted_replies`/`originals` metric kinds, `missed` status). One goals system, one digest fact. **Confirm merge shape at GR.7.**
- **D5** (binds RL.4, A3.9, A3.12): after AI.2/AI.6, new LLM calls use `askLLM` + `llmConfigured()`; new prompts register in the registry (`dm`, `article`) so the editor covers them. Refusal-ladder order unchanged.
- **D6** (binds all Settings.tsx tasks pre-UI.11): keep feature Settings UI minimal; UI.11 rebuilds and absorbs.
- **D7** (standing): all NEW UI from Wave 1 on uses UI.10 primitives + `--strat-*` tokens; Wave-5 polish passes touch only pre-masterplan tabs.
- **D8** (binds UI.11): prune the "coming soon" manifest to features still unbuilt at that time.
- **D9** (standing): content.ts task order is fixed: AX.3 → AX.5 → RU.7 → RU.8 → NT.5 → HV.2 → A3.3.
- **D10** (standing): registry knob groups (UI.2–5) run in Wave 5 so they catalog the final constant set, including RU/HV/GR/A3 constants.
- **D11** (UI.8, standing): `biome.json` `files.ignore` now includes `Stratus Design System` — the DS reference folder (Decision 9, plan out-of-scope) was ~112 of the baseline lint errors. Not in UI.8's plan edit-list but a correct, universal fix; whole-repo lint dropped 119→7. The remaining 7 are pre-existing and NOT in any changed file: 2 a11y in `extension/src/sidepanel/Replies.tsx` (UI.15 territory) + 5 biome-format on generated `src/db/migrations/meta/*.json` (a future infra task could ignore `src/db/migrations/meta`).
- **D13** (UI.1, binds UI.2): the **doctrine group is seeded in full by UI.1** — the 6 keys `x.doctrine.{replyTargetMin,replyTargetMax,weekReplyTargetPct,anchors3,anchors4,ladderSwitchAt}`, with the anchors' sorted-unique+range numberArray validation already in place (Task 2's plan attributed that hook to itself; landed early because UI.1 seeds the anchors so it must validate them). **UI.2 must NOT re-declare these doctrine keys** — it only adds the `quests`+`display` groups and wires the consumers (brief quota/ratio/gaps, composer ladder). Store layering (also binds AI.2/D1): `src/settings/store.ts` is platform-agnostic and never imports `src/x/*`; the X registry `src/x/settings/registry.ts` implements the store's `SettingsRegistry` interface and exposes the bound `getSetting`/`resolveSetting`/`getAllValues`/`setSettings`/`resetSettings` — **UI.2+ consumers import those from `../settings/registry.ts`, never the store directly**. biome.json now also ignores `src/db/migrations` (fulfils D11's anticipated infra ignore; the 5 generated-JSON lint errors are gone → whole-repo lint = 2, both pre-existing `Replies.tsx` a11y).
- **D93** (HV.2): **four things.** **(a) Sha reconciliation + the first out-of-skill commit since Wave 0.** HV.1's recorded `bf5e7b5` was an amend-orphan (`git merge-base --is-ancestor` → false, same `(HV.1)` subject); real HV.1 = **`5ea3495`**, whose parent `5d4a1a4` is the real NT.7 (recorded `75f15e7` was likewise an orphan). Both corrected in the ledger. **But HEAD at HV.2 start was `e49f705 "refactor claude"` — genuine out-of-skill work**: CLAUDE.md slimmed by ~215 lines with its phase block moved to the NEW `docs/PHASE-HISTORY.md` (+97), plus a codemap touch (+5) and a STATE touch (+1). **No ledger drift** (no task implemented), and codemap §7.29 already reflects it: **phase entries now go to `docs/PHASE-HISTORY.md`, NOT CLAUDE.md** — every remaining docs-sync task (HV.6, GR.10, A3.15, UI.17) must target that file. **(b) The pure module type-only-imports `Extracted` from `../harvester.ts`.** The plan writes `toPassiveIngestRow(x: Extracted)` without saying how the type crosses the layer. Options were duplicating a 10-field structural interface in `shared/` or importing the real one; `verbatimModuleSyntax: true` guarantees `import type` is erased, so the import costs nothing at runtime (IIFE or bun:test) and cannot drift. `Extracted` is therefore exported. **(c) Gate order is tighter than the plan's.** The plan says the record gate is `enabled && isHomeTimelinePath && !isHarvestActive()`. That is necessary but not sufficient: `applyBand` runs for EVERY article on EVERY rAF-coalesced mutation burst, so `recordPassiveHarvest` also checks the pending map and the 30-min throttle — off ONE `findPermalink` querySelector — **before** calling `extractArticle` (~8 querySelectors). After a tweet's first sighting the throttle is the hot path for the next 30 minutes. Don't reorder these to "simplify". **(d) Two permalink readers, deliberately.** The throttle/pending key is `findPermalink(article).tweetId` (content.ts) while the shipped row's `tweetId` comes from `extractArticle`'s `idFrom` (harvester.ts). Both take the article's first `a[href*="/status/"]`, so they agree; they are separate because the cheap gate must not pay for the full extract. A later task that unifies them must keep the cheap reader on the gate path.
- **D96a** (HV.5): the plan's Task-5 said "section 11" in `src/x/playbook.ts` — **§11 is AI.12's `buildModelEffectiveness`**, so the funnel landed as **§12**. Any later plan text counting playbook sections is off by one from AI.12 onward; the next `build*` is §13.
- **D96b** (HV.5): the plan's return shape was `{hot: {...}, warm: {...}}`. Shipped instead as **`{cells: FunnelCell[], totalSeen, totalReplied}`** in `TIMELINE_BAND_ORDER` (`hot, warm, skip, null, 'unknown'`), matching every other section in the file (latency/model/band cells) and, more importantly, keeping the honest denominator visible: `skip`/`null`/`unknown` rows ARE the tweets the algorithm showed me and I passed on. A `{hot, warm}` object would have hidden them and made the funnel a tautology. The panel and any future digest fact read `cells.find(c => c.band === 'hot')`.
- **D96c** (HV.5): the plan said "null tweetTime → null band = excluded". **Not excluded — bucketed as `'unknown'`**, its own key in the `TimelineBand` union, never folded into the real `null` band (which means "classified, not worth a reply"). Same discipline as `LATENCY_BUCKETS`' `unknown` and `ROSTER_BANDS`' `unknown`. Silently dropping those rows would shrink the denominator by exactly the articles whose `<time>` didn't render — a systematic, invisible bias.
- **D97** (HV.5, bookkeeping — supersedes the sha half of D93a/D94a/D95a): **the ledger records the commit SUBJECT LINE, not a sha.** Three tasks in a row wasted a Step-0 investigation proving a recorded sha was an amend-orphan, and every time the resolution was "the subject line is the identity". A sha can only be written into the commit that changes it by amending, which changes it again — the churn is structural, not carelessness. From HV.5 on: ledger entries carry the subject line + the PARENT sha (stable, already in history), the codemap header stamps `<parent>+<TASK>`, and Step 0 is `git log -1 --format='%h %s'` compared against the last ticked subject. No reconciliation, no orphan hunts.
- **D99** (GR.1): **three judgement calls the plan's Task-1 text left open, all resolved toward "a broken scrape must not be able to lie".** **(a) Revive.** The plan enumerates five statuses and five edges, none of which covers a handle that is `confirmed`/`gone` and then appears in a later scrape (I re-followed them). Left alone, that row is invisible to the queue forever while its `follows_back`/`last_seen_at` keep refreshing — half-live, half-dead. Shipped: presence in any batch **revives `confirmed|gone` → `active`**. But `first_seen_at` is **NOT** reset (it stays fill-only exactly as the plan says) — it is the only follow-age signal that exists and a bad reconcile must not be able to erase it; the cost is that a genuinely re-followed person gets no fresh 7-day grace, which is cheap because the queue only ever nudges and `keep` is one click. `unfollow_marked_at` is likewise **never cleared anywhere** (not on revive, not on requeue): it is churn history that the 6h release budget and the monitor's 24h churn rule both count in a trailing window, so a re-follow or a failed tick must not hand budget back. **(b) The empty-run guard.** Decision 9 says partial runs never reconcile; generalized here — a run closed with `rowsSeen === 0` is stamped `completed_at` but **`complete` stays false**, so it neither reconciles nor counts as `lastCompleteRunAt`. Without this, a DOM drift that extracts nothing (X renames `UserCell`) closes a run seeing zero handles and flips the ENTIRE ledger to `gone` in one txn. **(c) Empty batches are legal when `done:true`.** The plan lists `rows_required` as an unconditional 400; a scrape whose row count lands on an exact multiple of the batch size then has nothing left to ship and can never close its run — so the run would never reconcile. `rows: []` is refused only when `done` is absent/false. **Also (minor, not deviations):** `status: 'banana'` → 400 `invalid_status` while a real-but-forbidden status → 409 `invalid_transition` (radar.ts idiom); PATCH accepts `status` and `keep` together, neither → 400 `empty_patch`.
- **D100** (GR.2): **five divergences from the plan's Task-2 text, four additive and one a straight disagreement; nothing in its checklist was skipped.** **(a) `extractUserCell` shipped as pure `parseUserCell` in a NEW `extension/src/shared/userCell.ts`, not in `harvester.ts`.** The plan puts it in harvester.ts and then asks for happy-dom fixture tests of it — but §7.27 and NT.1's `shared/notifications.ts` already answer that shape: the scroll engine is browser-verified by convention, the DOM *parse* is the half that silently rots when X renames a testid, so it lives in `shared/` where it is testable and gets inlined into the IIFE anyway. Cost: a 5th file. `handleFromAvatarTestid` is exported from it and reused by the harvester's logged-in probe, so the `UserAvatar-Container-` suffix rule exists once. **(b) `runHarvest`'s `toIngest` param became `ship(profile, store, outcome)`.** The plan says "reuse the `runHarvest<R>` generic", but that generic hardcoded `shipToStratus` → `/x/harvest/*` with a `HarvestIngestRow[]`. Swapping the one param (arity unchanged) keeps posts/replies byte-identical while letting following mode own its own transport AND see how the run ended. The mode ternary moved into a `startRun(opts, emit, shouldCancel)` function — a 3-way nested ternary in the port handler was the alternative. **(c) `complete` is computed, never assumed.** The plan's "final batch `done: true` only when the scroll hit natural bottom, not on cancel/max" needed a fact the loop didn't record: **five** exits existed (cancel, row cap, day window, `HARD_STEP_CAP`, stable-height bottom) and only the last means "I saw everything". New `reachedBottom` flag set at that break; `complete = reachedBottom && !cancelled`. **(d) NEW: the not-my-list guard the plan doesn't mention.** A complete run over SOMEONE ELSE's `/following` would insert their followees as mine and flip my entire real ledger to `gone` in one txn — the exact failure D99(b) built the zero-row guard against, reachable here by nothing more than a stale handle in the input. Two layers: the panel targets `ctx.handle` of the OPEN following page (the handle input is frozen, never navigated away from), and `loggedInHandle()` (left-nav account switcher's `UserAvatar-Container-` suffix) aborts with `not_own_following` — **on a positive mismatch only**, since a null read is "unknown" (§7.11) and refusing a legitimate run over a collapsed sidebar is worse than the risk it removes. **(e) The Following mode chip is NOT disabled off the context probe.** The plan says "enabled only when the context probe reports `onFollowing`", but `HarvestForm` persists the mode, so a restore on a non-following page would render the chip **selected and disabled** — which reads as a broken panel and explains nothing. Shipped: the chip always selects, **Start** is what's blocked, and a warn line says exactly what to open. Same safety, discoverable.
- **D100a** (GR.2, calls the plan doesn't pin down): **(a) `followsBack` is sticky-true and `displayName` fill-only WITHIN a run, `listPosition` is first-sighting.** The list virtualizes, so the same handle is re-parsed on later screens and a cell's DOM index restarts every screen. Presence of the badge is evidence, absence on a re-render is more likely a paint gap than a same-minute unfollow — GR.1's own asymmetry at DOM scale. Across runs nothing is sticky: the server always takes the newest batch's value, so a real unfollow lands on the next scrape. **(b) `parseUserCell` requires ONLY the handle.** Every optional field is null-tolerant on purpose: a row dropped for a missing display name would count as *unseen* to a complete run and be marked `gone`. Anything that makes the scrape MISS a row is the expensive failure here, not a stale name. **(c) Display name = first *innermost* `[dir]` block that is neither `@…` nor the badge's own text.** Skipping blocks that wrap another `[dir]` is what stops a container from reading as "Alice @alice Follows you"; comparing against the badge's OWN text (never a hardcoded "Follows you") is what keeps it locale-proof, and is fixture-tested with a Romanian badge. **(d) Batch size 200, not the server's 500 cap** — matches the file's existing `INGEST_CHUNK` and keeps a failed batch small; a mid-run failure simply leaves the run open (never `complete`), so the ledger is untouched. **(e) `harvestTargetUrl`/`isAtTarget` now share a private `targetPath(handle, mode)`** — they duplicated the path spelling, which is how a new mode navigates somewhere its own already-there check disagrees with (an infinite re-navigate). **(f) `HarvestIngest`'s success arm gained optional `followsBack`/`complete`** rather than a new event field; both are absent for posts/replies, so the panel's existing rendering is untouched and an older panel just shows less.
- **D101** (GR.3): **three calls the plan's Task-3 text left open, all resolved toward "the window cap governs NEW releases; already-queued rows survive until ticked".** **(a) The batch tops up to the budget, it does not stack on it.** `releasedNow = max(0, budget − held.length)`, so `batch = held + released` never exceeds `budget` by new releases and a re-read with `held ≥ budget` releases nothing — that IS the idempotence the plan asked for. The plan's phrase "already-queued rows consume no new budget" is about the *budget number* (`releaseBudget` counts `unfollow_marked_at`, i.e. completed unfollows, never releases — a queued-never-done row can't shrink the window), NOT a licence for the batch to grow unbounded across reads. Got this backwards and every read would pile another 15–18 onto the batch until the user started ticking. **(b) `eligibleTotal = held.length + eligible.length`** — the full manual-unfollow backlog (everyone released + everyone still eligible), deliberately stable across reads because a released row just moves from the `eligible` pool into `held`. NOT "active-eligible only", which would jump down by `releasedNow` on the first read and confuse the UI's backlog line. **(c) `keep` is enforced as a COLUMN inside the pure fns, never added to the whitelist set.** The read-time whitelist is only people at stage mutual/ally (non-retired) + `loadTargetHandles()`; `keep` and `followsBack` live on the `following` row, so `eligibleForUnfollow`/`reviewQueued` check them directly. A `queued` row that becomes `keep`/mutual/ally/follows-back **after** release is **revoked** `queued→active` in the same read (GR.1 hand-off #1: kept rows must be filtered out of the re-listed queued batch, not only at first release). **Also (not a deviation, a trap for the next test author):** the marks query is GLOBAL over `following.unfollow_marked_at` (trailing 24h), so the route suite `db.delete(following)` first — the GR.1 lifecycle fixture leaves recent marks on CAROL/EVE that would otherwise perturb the jittered budget assertions.
- **D98** (HV.6): **three places the plan's Task-6 text and the current repo disagree, all resolved in the repo's favour.** **(a) The phase entry went to `docs/PHASE-HISTORY.md`, not CLAUDE.md** — Task 6 literally says "CLAUDE.md: one phase-style entry", but D93a moved the whole ledger out of CLAUDE.md on 2026-07-23; CLAUDE.md now changes only when a guardrail changes, and Harvest 2.0 changed none. **(b) `PLAN.md` got a top blockquote, not a §"Phased build" edit.** The plan said "PLAN.md status updated"; §"Phased build" is the frozen Phases-1–10 ledger, and every post-Phase-10 phase since Niche N0 has landed as a `>` blockquote in the stack above §"Product, in one paragraph each" (RU.10 is the exemplar). Same place, same style, `See docs/PHASE-HISTORY.md §"Harvest 2.0"` instead of RU.10's `See CLAUDE.md §…`. **(c) The smoke does NOT "delete every seeded run/row on exit" literally.** It cannot: `POST /harvest/passive` find-or-creates *today's* passive run, which on a live box is the user's real one holding real browsing rows. Deleting it would destroy live data. Shipped instead as: every written row carries an **888-prefixed tweet id** (no real snowflake starts there) and every directly-seeded run carries an **`hv6_*` handle** (no real harvest produces one), so cleanup's `like`/`inArray` are exact; a run the route *created* for the smoke is deleted, a **pre-existing** run is kept with its `row_count` **recomputed from the surviving rows** (self-healing after an aborted run — better than restoring a remembered number). Any future smoke that writes into a table live browsing also writes must copy this shape.

## Gotchas log

Things the next implementer must know that aren't obvious from the code. Append-only,
one line each, newest last.

**Closed-wave gotchas (127 of them) live in `STATE-ARCHIVE.md` — grep it by task id or filename before touching a file Waves 0–2 built.** What stays below: standing test/lint/typecheck traps, the browser-verification debt Wave 5 must clear, and every HV/GR entry (current wave).

- **`bun run lint` was 114–119 pre-existing errors at baseline** — mostly the `Stratus Design System/` reference folder. **UI.8 (D11) added that folder to biome ignore → whole-repo now 7 errors**, all pre-existing and outside any changed file (2 a11y `Replies.tsx`, 5 format on generated `migrations/meta/*.json`). Per-task gate stays "the changed files lint clean" (`bunx biome check <files>`), not "whole-repo green"; don't chase the remaining 7 unless your task owns those files.
- **UI.9 light theme is an OPENING-GUESS palette, unverified in a browser.** Automated gates pass (632 pass / 0 fail incl. 6 new storage tests, typecheck + build + changed-file lint green) but the 12-tabs × {dark,light} × {cozy,compact} visual QA matrix is explicitly manual and deferred to the Wave-5 polish passes (UI.12–16 done-whens). Known trap (plan risk note): AA contrast on warn/ok/band **text over tinted fills** in light — the fill tokens and semantic ink were re-authored dark-enough to double as text (`--strat-warn: #9a6a10`, band-hot `rgb(0,150,100)`, band-warm-text `#8a6100`), but spot-check before trusting. Adjust `:root[data-theme='light']` values, not the consuming rules.
- **Settings types are extension-local mirrors** — `SettingEntry`/`SettingsGroup`/`SettingsResponse`/`SettingsPatchResult`/`SettingsResetResult` in `extension/src/shared/types.ts` (re-exported via api.ts), mirroring the `GET /x/settings` JSON. The extension MUST NOT import `src/x/settings/registry.ts` (§5 build-isolation). **Binds UI.2/UI.11:** if the server `SettingDef` shape gains a field the editor needs, hand-sync it into `SettingEntry`.
- **`exactOptionalPropertyTypes` pass-through pattern (UI.10):** primitives forwarding a maybe-undefined optional (`Slider.unit`, `SettingRow.onReset`, `GearPopover.onReset`/`label`) type the prop as `T | undefined` explicitly — else TS rejects `prop={maybeUndefined}`. Reuse this for new primitives that relay optionals.
- **UI.10 rail regrouping is UNVERIFIED IN A BROWSER** (like UI.9's palette). Extension typecheck + Vite build + changed-file lint are green and the CSS compiles, but the visual QA (grouped rail spacing, brandmark, EmptyState, popover) rides the Wave-5 polish passes (UI.12–16 done-whens), per D7/D14.
- **`inspect.test.ts` fails 2/2 under bare `bun test`, GREEN under `bun run test` (the canonical, package.json script `SQLITE_PATH=:memory: bun test`).** Introduced by `906f2d3` ("eval 1", out-of-skill Wave-0 audit): `src/db/client.ts` defaults to `:memory:` when `NODE_ENV==='test'` (Bun sets it), but `src/x/data/inspect.ts:101`'s readonly connection still defaults to `./stratus.db` when `SQLITE_PATH` is unset — so under bare `bun test` the primary writes `:memory:` while the inspect readonly reads the file → seeded `people` rows invisible → the 2 LIKE-query tests get `[]`. **NOT RU.3 (bisected: 0 fail at `d1902e5`/ST.9, 2 fail from `906f2d3` onward; RU.3 touches none of inspect.ts/client.ts/the people table).** Full suite is **732/0 under `bun run test`** — always gate with `bun run test`, not bare `bun test`. One-line fix for a data-core/S1 task: make `inspect.ts` honor the same `NODE_ENV==='test'`→`:memory:` default as `client.ts` (or `client.ts` should also `process.env.SQLITE_PATH ??=` set it so both agree).
- **RL.4: adding a registry key is a 4-file move.** `PROMPT_KEYS` + `PROMPT_SPECS` in `prompts/registry.ts`, the default exported from a **PURE** module (a route import cycles — AI.5's `voice/extractPrompt.ts` move exists for that reason), `registry.test.ts`'s key-list + defaultBody assertions, and `docs/settings-tab.md`'s prompt-count copy (now **11**). The extension needs nothing — the Prompts panel renders whatever `GET /x/prompts` returns. CLAUDE.md's AI-layer entry still says "10 keys"; that is a historical phase record and stays — **RL.8's phase entry is where the 11th key gets documented.**
- **RL.5 is UNVERIFIED IN A BROWSER**, like every panel component since UI.9 — no tests were added (repo convention: panels are untested, logic lives server-side). Automated gates only: root+extension typecheck, `cd extension && bun run build`, changed-file lint, full suite 1087/0. The plan's live done-whens (create → add → test-render → generate → Overwrite) ride RL.8's smoke + the Wave-5 QA pass. The one server-behavior assumption the UI leans on was **not** left to faith — the `{humanizer:{}}` → defaults round-trip was proven with a throwaway route test (D81b).
- **RL.8: `cellOf` counts `n` only for MEASURED rows** (those with a `metrics_snapshots` row); `posted` counts every attributed reply. The smoke seeds a snapshot so both move by +1 — a future assertion that publishes a reply without a snapshot must assert `canned.posted`, not `canned.n`, or it will read as a silent failure.
- **NT.1: extension typecheck does NOT cover `*.test.ts`** — `extension/tsconfig.app.json` has `"exclude": ["src/**/*.test.ts"]`, and root `tsconfig.json` only includes `src/**/*` + `scripts/**/*` + `drizzle.config.ts`. So an extension test file is checked by **`bun run test` alone**; a type error in one is invisible to both typechecks. (Same for `earlyReplies.test.ts` — pre-existing, just never written down.)
- **NT.2: a test handle longer than 15 chars is silently skipped and the whole test goes vacuous.** `normalizePersonHandle` enforces `/^[A-Za-z0-9_]{1,15}$/`, so `nt2_mutual_person` (17) was dropped by `recordEngagements` while every assertion around it still "passed" (the person row had been inserted by hand). Cost a debug round-trip. Two rules for any people-layer test: keep seeded handles **≤15 chars**, and assert `{processed, skipped}` on the ingest result so a skip can never hide behind downstream assertions.
- **NT.2: seeding `posts_published` in the shared in-memory DB is safe only with an out-of-window date and no metrics row.** The engagements suite needs own posts for target resolution; it uses a **January `postedAt`** and writes no `metrics_snapshots`, which keeps the rows out of every windowed or measured aggregation other suites assert over (playbook medians, reup 14–60d, brief/digest/quest windows, pinned watch). Files run sequentially so the only real hazard is a stray — cleanup runs inline *and* in `afterAll`. Reuse this shape rather than inventing a new seed.
- **NT.3 test-handle rule (again):** the route suite uses `nt3_route_eng` (13 chars). The NT.2 gotcha applies unchanged — any people-layer test handle over 15 chars is silently skipped by `normalizePersonHandle`, and asserting `{processed, skipped}` is what keeps that from hiding.
- **NT.7: a real-DB smoke that seeds `posts_published` must write the row `retired: true`.** `scripts/smoke-notifications.ts` needs an own post for prefix target resolution; a non-retired one left behind by an aborted run is a candidate for the daily 03:00 UTC snapshot pass, i.e. a **real billed X read** for a tweet id that doesn't exist (invariant #7). `retired: true` takes it out of the candidate set permanently and costs the smoke nothing (it never reads metrics). Reuse this whenever a smoke seeds own posts against the real DB — `smoke-followups.ts` predates the rule and gets away with it only because its rows are deleted synchronously.
- **NT.7: `app.request(...)` is typed `Response | Promise<Response>`.** A helper declared `function f(): Promise<Response> { return app.request(...) }` fails root typecheck; make the helper `async`. `bun run typecheck` covers `scripts/**`, so this bites in smoke scripts, not in route suites (which `await` inline).
- **HV.1: `POST /x/harvest/passive` is live and Wave 3 is open — read this before HV.2/HV.3/HV.4 touch the harvest lane.** The route is server-only, **$0**, and adds **no column and no migration** (see D92 — the journal is still `0018` and FREE). Shape the rest of the phase must build against: the extension posts `{rows: HarvestIngestRow[]}` (**≤100/batch**, validated by the *existing* `parseIngestRow`, so the content script must ship the same row shape the active harvester already ships) and always gets back **`201 {runId, inserted, skippedRecent, skippedCap}`** — HV.2's flush should treat a non-201 as warn-and-drop and must NOT retry (a dropped sighting is a missing longitudinal point, never a lost user action). Four in-file constants are the contract and all four are **opening guesses**: `MAX_PASSIVE_BATCH=100`, `PASSIVE_DAILY_CAP=2000`, `PASSIVE_RECAPTURE_MS=30min`, `PASSIVE_RETENTION_DAYS=60`. The client's per-tweet resend throttle should mirror the server's 30 min so the wire isn't full of rows that will only be counted as `skippedRecent`. **Three things that will bite a later task:** (1) `MODES` is still `['posts','replies']` **on purpose** — `POST /harvest/runs` 400s `mode:'timeline'`, so nothing client-side may try to create a passive run (decision 7, asserted in the suite); (2) **`mode` is the ONLY discriminator** between ambient rows and hand-run harvests, and today the only other reader (`scripts/backfill-people.ts`) filters `mode='replies'` — **HV.4's affinity SQL and HV.5's funnel MUST filter `mode='timeline'`**, and any *future* query over `harvest_rows` has to declare a mode or it will silently mix corpora; (3) **band is never stored** (§7.12) — HV.5 recomputes it from `views`/`comments`/`tweet_time`/`captured_at`/`text` through the shared `replyBand.ts`, which is why capturing `tweetTime` matters (a null `tweet_time` makes `ageMin` and therefore vpm underivable, so the row can only ever land in an unknown bucket).
- **HV.1 test-suite traps for anyone editing `src/x/routes/harvest.test.ts`** (the file's first route suite; the pure `parseIngestRow`/`matchUnlinkedDraft` tests stay in `src/test.test.ts`): there is exactly **one passive run per UTC day**, so tests share it and **order is load-bearing** — the cap test forces `rowCount` to 1999 and **restores it** (`before + 1`) or every later test in the file gets capped to zero inserts; the prune test seeds its stale rows *after* the earlier posts because prune fires on **every** passive request; and each test needs **fresh tweet ids** (`77000xxx` here) or the 30-min recapture gate silently turns an expected insert into a `skippedRecent`. `afterAll` deletes every `mode='timeline'` run + rows plus the one seeded `posts`-mode keeper — no other suite touches these tables today, so that is exact. Suite total moved **1130 → 1139**.
- **CLAUDE.md was slimmed to guardrails-only (2026-07-23, out-of-skill docs refactor).** The full phase-entry ledger moved **verbatim** to `docs/PHASE-HISTORY.md`. **Every docs-sync task (MASTERPLAN global rule 6, codemap §7 rule 29 — updated) now appends its phase entry to `docs/PHASE-HISTORY.md`, NOT CLAUDE.md**; CLAUDE.md changes only when a guardrail (invariant, workflow, stack quirk) changes. Plan text still saying "CLAUDE.md phase entry" means the history file.
- **HV.2: the passive tap is OPEN — x.com/home now writes to `harvest_rows` while you scroll, and HV.3/4/5 finally have a corpus to read.** One line in `applyBand` (`if (sig) recordPassiveHarvest(article)`) drives it, for **every band including `skip`** — HV.5's funnel needs the denominator, and ads/promoted rows carry no metrics label so `sig === null` filters them for free. **What a later task must not casually change:** (1) **gate order is the perf contract** (D93c) — enabled → `/home` → `!isHarvestActive()` → `findPermalink` → pending/throttle, and only THEN `extractArticle`; the hook runs per-article per-mutation-burst, so the throttle is the hot path after a tweet's first sighting; (2) `isHomeTimelinePath(location.pathname)` is re-read **per call**, never resolved once at `start()` — X navigates without reloading (same reason NT.5's `onNotificationsPage()` is per-scan); (3) the flush is **warn-and-drop with NO retry** (a lost sighting is a missing point on a view curve, not a lost user action) and it ignores `unconfigured`; (4) `time: x.time || null` in `toPassiveIngestRow` — shipping `''` would be an invalid_row_time 400, and a null `tweet_time` strands the row in HV.5's unknown bucket, so an article whose `<time>` didn't render is worth less than it looks. **Three constants are opening guesses** and they mirror the server's, not each other's: `PASSIVE_RESEND_MS=30min` ↔ server `PASSIVE_RECAPTURE_MS`, `PASSIVE_BATCH_MAX=100` ↔ server `MAX_PASSIVE_BATCH`; change one side and the wire fills with rows the server only counts as `skippedRecent`. `PASSIVE_FLUSH_MS=5000` and the >5000 `passiveSentAt` reset are client-only. **The toggle is a SECOND key** — `passiveHarvest`, not C6's `passiveCapture` (one grows the people roster from hover cards, one grows the corpus from the timeline); both are default-ON/opt-out with `absent key = enabled`, and both are read directly by content.ts as well as through `sidepanel/storage.ts`. Adding it to the `Settings` interface forced the object literal in `storage.test.ts` — and surfaced that **extension test files are typechecked by NEITHER tsconfig** (root `include` stops at `src/`; `tsconfig.app.json` excludes `src/**/*.test.ts`), so `bun run test` is the only thing that ever executes them and a type error in one is invisible. **Browser-UNVERIFIED by convention** (content.ts is never unit-tested): the live done-when tail is scrolling x.com/home with the unpacked extension and watching POSTs to `/x/harvest/passive` in the network tab + rows landing under one `mode='timeline'` run, toggling the setting off stopping them **without a reload** (the `onChanged` listener), and starting a Harvest-tab run suspending them. Gates: `bun run test` **1147/0** (+8, all in the new pure suite), root+extension typecheck clean, `vite build` green (the IIFE pass proves the inlining — `isHomeTimelinePath` appears as a plain function in `dist/content.js`), `bun run lint` clean. **Docs are deliberately NOT updated** — plan Task 2 defers `docs/settings-tab.md` and the new harvest doc to **HV.6**, which owes: the `passiveHarvest` toggle row, the /home-only scope, the cap/retention/recapture numbers, and (per D93a) its phase entry in **`docs/PHASE-HISTORY.md`, not CLAUDE.md**. **Releases `extension/src/content.ts`, `harvester.ts` and `Settings.tsx`.** The D9 chain now stands at HV.2✓ — **A3.3 (Wave 4) is the last claimant** and nothing before it touches content.ts. HV.3 (Harvest tab options, `Harvest.tsx` + `harvester.ts` — note it re-claims harvester.ts), HV.4 (`GET /harvest/affinity`, re-claims `src/x/routes/harvest.ts`) and HV.5 (playbook lock) are all unblocked and disjoint.
- **HV.3: the Harvest tab is done and the harvester's option surface is now four flags — read this before HV.4/HV.5/HV.6 or any later harvester work.** `HarvestOptions` = `{mode, scope, pace, max?, sendToStratus?, downloadCsv?, minViews?}`; both new flags are **absent-means-on/off-by-default** (`downloadCsv !== false` downloads, an absent/0/NaN `minViews` filters nothing), so an older side panel driving a newer content script behaves exactly as before. **Three things that will bite a later task:** (1) **the min-views filter sits after `noteSeen` and before the store write** — moving it above `noteSeen` would stop filtered items from counting as scrolled-past, which defeats the day-window exhaustion break (`win && ctx.oldestSeenMs < win.startMs`) and makes every scoped run scroll to `HARD_STEP_CAP`; it also reads the harvested item's OWN views (`reply.metrics.views` in replies mode, never `orig`'s); (2) **`done.filename === ''` now means two different things** — zero rows (as before) *or* CSV-off; the panel disambiguates by `rows > 0`, and anything else consuming the event should too; (3) **`passiveRowsToday`'s day boundary is UTC on purpose** and must stay pinned to the server's `utcDayStart` — if HV.4/HV.5 ever add a "today" read over `harvest_rows`, use the same boundary or the two surfaces will disagree for hours a day. The form now lives in **one** `chrome.storage.local` key `harvestForm` (mode/scope/pace/maxStr/minViewsStr/downloadCsv, restored through the lenient `parseHarvestForm`) while `harvestSendToStratus` deliberately keeps its own legacy slot; the restore→mirror pair is guarded by a `formLoaded` flag, so **don't add a form field to the write effect's deps without also adding it to the read** or the first render will stamp a default over stored state. Start is blocked with a warn line when CSV **and** send-to-stratus are both off. The status line renders **nothing** while unloaded or after a failed `GET /x/harvest/runs` (it never claims 0) — the `limit:20` lookback is the route's own default and could in principle miss today's passive run behind 20+ same-day hand harvests; raise the limit if that ever happens rather than filtering server-side. **Browser-UNVERIFIED** (panel React is untested by convention): the live tail is a CSV-off DB-only run landing rows with no download, `min views 1000` thinning both the CSV and the ingest, the form surviving a panel close, and the status line matching what `x.com/home` scrolling actually captured. Gates: `bun run test` **1162/0** (+15), root + extension typecheck clean, `vite build` green (`ApiError` verified **absent** from `dist/content.js` — the new `import type { HarvestRun }` in `shared/harvest.ts` is fully erased), whole-repo `bun run lint` clean. **HV.4 and HV.5 remain unblocked and disjoint; HV.6 owes the docs** — a new/updated harvest doc covering the four options + the passive tap, `docs/settings-tab.md`'s `passiveHarvest` row (deferred from HV.2), and (per D93a) its phase entry in **`docs/PHASE-HISTORY.md`, not CLAUDE.md**.
- **HV.4: the ambient corpus has its first READER — and the `mode='timeline'` rule is now load-bearing in two places, not one.** `GET /x/harvest/affinity?days=&limit=&minDays=` → `{days, minDays, authors:[{handle, distinctDays, sightings, lastSeenAt, avgViews, stage, inRoster}]}`, $0, defaults 30 / 20 / ≥3 — **all four affinity constants are opening guesses** to revisit with HV.1's four (`DEFAULT_AFFINITY_DAYS`, `MIN/MAX_AFFINITY_DAYS` 7–90, `DEFAULT_AFFINITY_LIMIT` 20 / `MAX` 50, `DEFAULT_AFFINITY_MIN_DAYS` 3). **Four things that will bite a later task:** (1) the UTC day bucket is `strftime('%Y-%m-%d', captured_at/1000, 'unixepoch')` — the THIRD place the UTC-day boundary is written (server `utcDayStart`, panel `passiveRowsToday`, now this); any fourth reader must use the same one or two surfaces disagree for hours a day; (2) the `people`/`voice_authors` join runs **in TS off the ≤`limit` handles after the group-by**, with an early return when the group-by is empty — never let it reach drizzle as `in ()` (the NT.6/D90a rule); (3) `intParam` is now shared by three params and is the place to add a fourth — its contract is clamp-on-range, **400-on-garbage**, and the 400 is deliberate; (4) the route is still the only reader that isn't `mode='timeline'`-aware by accident — **HV.5's funnel must carry the same filter**, and `scripts/backfill-people.ts` (`mode='replies'`) remains the only other consumer. **Test-suite trap:** the affinity describe must stay **LAST** in `src/x/routes/harvest.test.ts` — the ingest describe's first test asserts **zero** `mode='timeline'` runs exist, and the affinity `beforeAll` creates one. Its rows are seeded **directly** (a multi-day history is unbuildable through the 30-min recapture gate) under a run created **now** with **backdated rows**, so the 60-day prune on the passive path can't delete them mid-suite; `afterAll` now also deletes the seeded `people` + `voice_authors` rows (shared in-memory DB). **Panel side is deliberately minimal:** a `Section`+`EmptyState` drawer under the People roster, **collapsed and unfetched until clicked** (one manual Refresh, no polling), and each row calls the SAME `setSelected` the roster uses — so an unknown handle rides the existing `NotYetKnown` "start the file" path. **No new message type, no storage key, no CSS rule** (it reuses `.people-list`/`.people-row-main`/`.people-counts`/`.people-ago`/`.stage-chip`). **Browser-UNVERIFIED** (panel React is untested by convention): the live tail is opening People → Show and seeing real handles from actual x.com/home scrolling, with a genuinely unknown one clicking through to the create-file screen. Gates: `bun run test` **1167/0** (+5), root + extension typecheck clean, `vite build` green, whole-repo `bun run lint` clean. **HV.6 now also owes `docs/people-tab.md`** (the affinity drawer) on top of the harvest doc + `docs/settings-tab.md`'s `passiveHarvest` row. **HV.5 is unblocked and disjoint** (playbook lock, untouched here).
- **HV.5: the ambient corpus now has TWO readers and the funnel is the one that needs `skip` rows to exist.** `GET /x/playbook` → `timelineFunnel: {cells, totalSeen, totalReplied}`, 30-day window, `mode='timeline'`, $0, **no schema/migration/CSS/api.ts change**. **Five things that will bite a later task:** (1) **HV.2 capturing every band including `skip` is load-bearing here** — the funnel's denominator IS "tweets the algorithm showed me"; if anyone ever "optimizes" passive capture to only store hot/warm rows, this cell silently becomes `replied/replied = 100%`. (2) The first-sighting rule is enforced in TWO places on purpose — `min(captured_at)` with bare columns in `loadTimelineFunnel` (SQLite's documented behavior: with exactly ONE min/max aggregate, every bare column comes from the matching input row — add a second aggregate like `max(views)` to that select and the guarantee evaporates) **and** an earliest-`capturedAtMs` dedupe inside the pure `buildTimelineFunnel`, so the pure fn is correct on any row set. (3) `'unknown'` ≠ `null` band (D96c) — and `unknown` is the bucket that grows if `content.ts` ever ships `time: ''`/omits it (HV.2 gotcha 4), so a sudden unknown spike is a capture regression, not a timeline change. (4) The window is **30d while retention is 60d** and the select is **unlimited** — deliberate, because HV.1's 2,000/day cap bounds it structurally (≤60k rows) and a `limit` would truncate the denominator without saying so. If the daily cap is ever raised, revisit the limit BEFORE the window. (5) `replied` reads posted `reply_drafts.sourceTweetId` (unwindowed; the ∩ with seen ids bounds it) — **canned replies and hand-typed replies leave no draft row**, so the capture rate reads LOW by construction. Don't "fix" it by widening to `posts_published.in_reply_to_tweet_id` without a decision: the plan's own risk note says refine the population in a follow-up, not pre-filter now. **Test-suite traps:** the funnel describe must stay **LAST** in `src/x/routes/playbook.test.ts` (it seeds a posted `reply_drafts` row, which moves the earlier describes' angle/latency/model/batch cells) and it must delete its `mode='timeline'` run + rows in `afterAll` — `src/x/routes/harvest.test.ts` asserts that **zero** timeline runs exist. Multi-sighting histories are seeded **directly**, not through `POST /harvest/passive` (its 30-min recapture gate makes them unbuildable). **Browser-UNVERIFIED** (panel React is untested by convention): the live tail is the Playbook tab showing a real seen/replied split after some days of x.com/home scrolling — and note **every cell will read `insufficient data` until 20 tweets land in a band**, which is hours of scrolling, not minutes. Gates: `bun run test` **1176/0** (+9: 6 pure, 3 route), root + extension typecheck clean, `vite build` green, whole-repo `bun run lint` clean. **HV.6 is the only HV task left** and now also owes `docs/playbook-tab.md`.
- **HV.6 CLOSES the HV sub-plan — Wave 3's only remaining lane is GR (GR.1–GR.10).** No code, schema, route or test changed (suite stays **1176/0**); the task was one new smoke + seven docs. **What a later task inherits:** (1) **`scripts/smoke-passive-harvest.ts` is the reusable template for smoking a table the user's live browsing also writes** — prefix-namespaced ids + a handle no real path produces, delete-what-we-made, recompute-not-restore for anything we merely touched (D98c). It is rerunnable back-to-back and verified to leave 0 rows; it will FAIL loudly (not silently skip) if today's passive run is already at the 2,000-row cap, with a message naming the cap. (2) **The `unknown`-band assertion is the funnel's canary** — the smoke seeds 6 rows with `tweetTime: null` on purpose and asserts a `≥6` `unknown` cell; if a future capture change starts shipping `time: ''` the smoke keeps passing but the *live* unknown cell balloons, which is the documented capture-regression signal (`docs/playbook-tab.md` says so in user words). (3) **Docs now assert user-visible strings** that a UI polish pass must keep or update together: the both-off warn line, `Passive: N rows today` / `Passive capture off`, `saved N rows to stratus only (no CSV)`, `Nobody has shown up often enough yet.`, `Start their file →`, `insufficient data (n=…)`, and `Nothing captured yet — passive harvest fills this while you scroll x.com/home.` **UI.15 (Wave 5) polishes Replies/Playbook/Harvest/Studio and UI.14 polishes People — both must re-read the matching `docs/*-tab.md` in the same task.** (4) `docs/settings-tab.md` now says **four** toggles in two places (the section intro and the troubleshooting bullet); UI.2/UI.11's settings rebuild has to move that count, not just add a row. (5) The HV phase's **live "done when" tails are still open** and no code task owns them — an unpacked-extension session on x.com/home proving rows land under one `mode='timeline'` run, the toggle stopping capture without a reload, a Harvest-tab run suspending it, the first ≥30-min re-sighting, and the first affinity author promoted to a dossier. Every volume constant (2,000/day, 30-min, 60-day, minDays 3, affinity defaults) stays an **opening guess** until ~30 days of real scrolling.
- **GR.1 OPENS the GR lane and hands the rest of it four things.** Suite 1176→**1190**; migration **`0019_messy_blue_shield`** (2 CREATE TABLEs + 1 index, DDL-only, no seed) — **the journal is FREE again**, next migrating task owns `0020`. **(1) `keep` moves no status.** `PATCH /following/:handle {keep:true}` pins a row but leaves a `queued` row queued — so **GR.3's `GET /following/queue` must filter `keep = false` on the already-`queued` rows it re-lists**, not only inside `eligibleForUnfollow`; otherwise a kept person stays in the batch forever. Same for the read-time whitelist (mutual/ally stages + `loadTargetHandles()`): it has to be applied to the re-listed `queued` rows too, since eligibility ran before those stages existed. **(2) The index `following_status_seen_idx` on `(status, first_seen_at)` is exactly GR.3's eligibility query** — `status='active'` ordered by oldest `first_seen_at`; keep the ORDER BY on `first_seen_at` (with `list_position` desc as tie-break) and it stays covered. **(3) The route file is `:param`-free until GR.3 adds `GET /following/queue`** — it must register above `PATCH/GET /following/:handle` (§7.20); the current ordering already is the safe one, and there is a comment saying so. **(4) `FollowingIngestRow` is exported from `src/x/routes/following.ts`** with `{handle, displayName, followsBack, listPosition}` — GR.2's extension-side type must match it field for field, and `followsBack` is **required**: a harvester build that can't read `[data-testid="userFollowIndicator"]` gets an indexed 400 `invalid_follows_back` instead of silently marking the whole roster as non-reciprocating. GR.2 should ship `done: true` on its final batch (an empty final batch is legal for exactly this) and must NOT set it on a cancelled or capped scroll. **The 7-day grace, the 6h/15–18 cap and the 40/day ceiling are still unwritten** — GR.1 stores `first_seen_at`/`unfollow_marked_at`, GR.3 owns the constants.
- **GR.2 gives the ledger its client half — read this before GR.3/GR.4 or any later harvester work.** Extension-only, **$0**, **no server/schema/migration/CSS/api.ts change**; suite 1190→**1207** (+17: 11 fixture tests in the NEW `extension/src/shared/userCell.test.ts`, 6 URL/form cases in `shared/harvest.test.ts`). **Five things that will bite a later task:** (1) **`runHarvest<R>` no longer takes `toIngest` — it takes `ship(profile, store, outcome)`**, and the mode dispatch lives in `startRun(opts, emit, shouldCancel)`; a fourth mode is one branch there plus a ship closure, NOT another ternary in the port handler. (2) **`outcome.complete` is the whole ballgame and it is computed from ONE loop exit** — `reachedBottom` is set only at the stable-height break; cancel, the `max` row cap, the day window and `HARD_STEP_CAP` all leave a tail unread and must stay excluded. `done: true` rides the last batch only when it is true, so **a capped following run silently never reconciles** — the panel's partial-pass notice is the only thing that tells the user, and the persisted `maxStr` carries over from posts harvests. If following runs start looking stale, check Max rows before suspecting the server. (3) **`parseUserCell` returning null means "nothing seen here", never "a row with gaps"** — a dropped handle reads as *unseen* to a complete run and gets marked `gone`, so every optional field is null-tolerant and only the handle is required; within a run `followsBack` is sticky-true and `displayName` fill-only (D100a). (4) **The not-my-list guard is `loggedInHandle()`** in harvester.ts, off `[data-testid="SideNav_AccountSwitcher_Button"] [data-testid^="UserAvatar-Container-"]` — it fires `not_own_following` on a POSITIVE mismatch only, and the panel additionally targets the OPEN page's handle rather than the (frozen) handle input. Both exist because a complete run over someone else's list would insert their followees and flip my whole ledger to `gone`. (5) **`HarvestMode` is now a 3-union** — `MODE_NOUN`, `isHarvestMode`, `targetPath` and `startRun` are the four places that must stay exhaustive. **Browser-UNVERIFIED, and this is the task's real tail** (the plan's own done-when #1): nobody has run this against x.com yet. The selectors to watch on the first live run are `[data-testid="UserCell"]`, `[data-testid="userFollowIndicator"]` (the whole follows-back half rides on it — if it never appears, every row lands `followsBack:false` and GR.3's queue would offer up mutuals), the `UserAvatar-Container-` suffix, and the account-switcher nesting the self-check depends on. Verify with a **cancelled** run first (partial ⇒ no reconcile ⇒ nothing can be marked `gone`), check `GET /x/following` for sane `followsBack` flags, and only then let one scroll to the bottom. **GR.10 owes the docs** — `docs/harvest-tab.md` (the third mode + the complete-run rule) and a `docs/people-tab.md` note once GR.4 lands; per D93a the phase entry goes to `docs/PHASE-HISTORY.md`. Gates: `bun run test` **1207/0**, root + extension typecheck clean, `cd extension && bun run build` green (`UserCell`/`userFollowIndicator`/`/x/following/rows` verified present in `dist/content.js`, zero bare imports — the new shared module inlines), whole-repo `bun run lint` clean.
- **GR.3 closes the last gate before GR.4 — read this before GR.4/GR.5 or touching the queue.** Server-only, **$0**, **no schema/migration/CSS/extension change**; suite 1211→**1224** (+13: 10 pure in NEW `src/x/connections.test.ts`, 3 route in `following.test.ts`). **`following.ts` is released** (the file now holds runs/rows/GET/**queue**/PATCH; `/following/queue` sits ABOVE `/:handle`, keep it there). **Five things a later task must know:** (1) **The queue is idempotent by topping up to `budget`, not by stacking** — `releasedNow = max(0, budget − held.length)` (D101a). If you add a "release N more" button, decrement against `held`, don't add a second release call. (2) **`eligibleTotal` includes the held/queued rows** (D101b) — it is the whole backlog, not just what's still active; the UI's "N eligible" reads this. (3) **`releaseBudget`'s marks are GLOBAL and 24h-trailing** — GR.5's `unfollowChurn` monitor rule must read the SAME `following.unfollow_marked_at` column and the SAME `DAILY_CEILING(40)` constant (both exported from `connections.ts`) so the alert can only fire when the user out-runs the queue by hand; don't re-derive either. (4) **Whitelist = people mutual/ally (non-retired) + `loadTargetHandles()` only; `keep`/`followsBack` are enforced as columns** (D101c). GR.4's subtab keep-toggle is a `PATCH /following/:handle {keep}` (GR.1) — the very next queue read revokes that handle from the batch, no queue-side plumbing needed. (5) **The constants (7d grace, 6h/15–18 cap, 40/day) are opening guesses** (plan decisions 6–7) — revisit after ~30 days of real queue use, all in `connections.ts`, mirrored nowhere. **The queue is code-verified only** (10 pure + 3 route tests) — it has never run against a real populated ledger because the ledger itself is still browser-UNVERIFIED (GR.2's tail): the first live following scrape is what will first put real rows through `GET /following/queue`. Gates: `bun run test` **1224/0**, root typecheck clean, whole-repo `bun run lint` clean; no extension file touched so no build step. **⚠ Built on an uncommitted out-of-band cost fix in the same tree (dailyMetrics + CA.1/CA.2 plan) — see next-up + the commit note in last-commit.**
- **STATE.md was compacted 2026-07-23 (out-of-skill housekeeping, no code touched).** It had reached **339 KB** — past the `Read` tool's 256 KB cap, so Step 0's "read STATE.md fully" silently became a chunked-read investigation every session. 96 closed-wave D-entries + 127 closed-wave gotchas + the header's HV/GR prior paragraphs moved **verbatim** into the new `STATE-ARCHIVE.md` (frozen, 269 KB, grep-only — bigger than one `Read` by design); the live file is **74 KB**. The split was generated by line number, not retyped, and verified as a set operation: every non-empty original line lands in **exactly one** of the two files (438/438, zero duplicated). **What this means for you:** a task touching a file Waves 0–2 built should `grep STATE-ARCHIVE.md` for the task id or filename before assuming the live file tells the whole story — e.g. the RL `/use` contract (D79), the NT.5 icon prefixes (D89), the ST registry contract. SKILL.md gained the matching archive rule in Step 6 (a lane's docs-sync task archives that lane) and its Step-0 staleness guard now compares the **subject line**, matching D97 — it still said `--format=%h` five sessions after shas stopped being recorded.

## Planning-error log

Corrections to MASTERPLAN.md itself (wrong dep, wrong order discovered live).

- **2026-07-16-niche.md Task 3 done-when** — the "`Pitești` hits nothing" grep contradicts the same task's bit-for-bit equivalence requirement (post prompt §6/§9/§0 carry builder biography outside the §1/§5 extraction scope; no niche field exists to hold them). Resolved at N.3 in favor of equivalence; see D28b for the full resolution + what N.9 must scope differently. Source plans are never edited — this entry is the correction.
- **2026-07-17-ai-layer.md — no task owns the post drafter's askLLM migration.** Task 5's edit list names replies/pillars/playbook/voiceExtract; Task 6 only flips drafter's MOUNT gate; Task 7 adds a sibling route. Yet the plan's Why-section + Done-when #1 spirit require every draft surface to honor the provider setting, and the Task-6 gate flip would 502 `/posts/draft` on OpenRouter-only setups. Resolved at AI.5 by migrating `drafter.ts` there (D47b) — the call-site-migration task was the right home.

