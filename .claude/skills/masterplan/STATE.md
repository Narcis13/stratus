# Masterplan execution state

> Dynamic memory for `/masterplan`. Updated after EVERY task, in the task's own commit.
> Plan: `plans/MASTERPLAN.md` (static — order, reasoning levels, waves, D1–D10).
> Codemap: `.claude/skills/plan-feature/references/codemap.md` (updated per task too).

- **last-commit:** HV.6 — **identity is the subject line** `docs(harvest): Harvest 2.0 docs-sync + $0 smoke-passive-harvest.ts (HV.6)`, parent `3416f14` (= HV.5). No drift at HV.6 start (HEAD was `3416f14` with the HV.5 subject, exactly as expected). **No sha is recorded any more (D97):** three consecutive tasks (D93a/D94a/D95a) burned a Step-0 investigation reconciling an amend-orphaned sha, and the header already said the subject line was authoritative — so it is now the ONLY thing recorded. Step 0 stays one command: `git log -1 --format='%h %s'`; if the subject names the last ticked task, there was no out-of-skill work. No drift at HV.5 start (HEAD was `855b424` = HV.4, exactly as expected).
- **current wave:** **WAVE 3 — the HV sub-plan is CLOSED (HV.1–HV.6 all ✓).** Waves 0–2 closed (RU.1–10✓ AX.1–7✓ RL.1–8✓ NT.1–7✓). Remaining in Wave 3: **the GR lane only** (GR.1–GR.10). No locked files — HV.6 touched no code. **The migration journal is still FREE** (`0018` latest; the whole HV sub-plan needed none — D92) so the next task that actually adds a column owns `0019`. **The D9 `content.ts` chain is DONE through HV.2✓** [AX.3✓→AX.5✓→RU.7✓→RU.8✓→NT.5✓→HV.2✓→**A3.3**]; the file is RELEASED and A3.3 (Wave 4) is its last claimant. **D2 (UI.2, Wave 5) still pending decision** — the 5 doctrine knobs are owned by `niches.doctrine` since N.5; UI.2 must not duplicate them in `app_settings` (D30c). **D93a is binding on every remaining docs-sync task:** phase entries go to `docs/PHASE-HISTORY.md`, no longer to CLAUDE.md (codemap §7.29).
- **next-up:** **GR.1 — Following ledger (xhigh, no deps)**, the first task of the last Wave-3 lane. Source plan **`plans/2026-07-17-guardrails.md`** Task 1 (the GR sub-plan = following curation queue, activity monitor, goals/commitments/scorecard; GR.1–GR.10; **D4 is binding on GR.7**). It is the first task in a while that likely **adds a table** → it owns migration **`0019`** (journal free since `0018`; `bun run db:generate` then INSPECT the SQL — drizzle-kit drops seed INSERTs, codemap §4). **Gate reminders (unchanged):** canonical test command is `bun run test` (bare `bun test` targets the file DB); biome forbids `delete process.env.X` and `: any` in `scripts/`; biome **sorts imports case-sensitively** and **collapses short chained calls onto one line** — run `bunx biome check --write` on changed files rather than hand-formatting. Extension-touching tasks add `cd extension && bun run build` **and** `cd extension && bun run typecheck` to the bar.
- **prior (HV.6):** the HV sub-plan's closer — **no code, schema, route or test change** (suite stays **1176/0**): one new `scripts/smoke-passive-harvest.ts` + seven docs (`harvest-tab`, `settings-tab`, `people-tab`, `playbook-tab`, `README`, `PLAN.md` blockquote, `docs/PHASE-HISTORY.md` entry) + codemap §9/§11/header. The smoke drives ingest → recapture → oversized-400 → 61-day prune → affinity → `timelineFunnel` in-process over the **real** DB and is safe there by construction (888-prefixed rows, `hv6_*` run handles, keep-and-recompute for a pre-existing passive run — D98c). Wave 3 now has only the GR lane left.
- **prior (HV.5):** the ambient corpus's second reader and the HV sub-plan's last code task — `GET /x/playbook` now carries **`timelineFunnel`** ($0, **no schema/migration/CSS/api.ts change**, 6 files, +9 tests, 1167→**1176**). Pure `buildTimelineFunnel(rows, repliedTweetIds, minN)` is playbook **§12** (the plan said §11 — AI.12 took that — D96a); `deriveTimelineBand` recomputes the band at read time through the shared `classifyBand` (§7.12, never stored) and returns `TimelineBand = Band | 'unknown'`, where **`'unknown'` (no `tweet_time`) is deliberately NOT the `null` band** (`null` = judged not worth replying to). First sighting wins twice over: deduped by earliest `capturedAtMs` in the pure fn AND by `min(captured_at)`+bare columns in `loadTimelineFunnel`'s `group by tweet_id`. `replied` = posted `reply_drafts.sourceTweetId` ∩ seen ids. 30d window, `mode='timeline'` filter, no row limit. Panel: a "Timeline funnel" section after Model effectiveness.
- **prior (HV.4):** the ambient corpus's first reader — `GET /x/harvest/affinity?days=&limit=&minDays=` (defaults 30/20/≥3, $0, **no schema/migration/CSS change**): one grouped select over `harvest_rows` filtered **`mode='timeline'`** + `capturedAt >= now−days`, `having count(distinct UTC day) >= minDays`, ordered **distinctDays desc then sightings desc** (a viral thread re-scrolled 20× in one afternoon is not affinity), then the `people`/`voice_authors` join **in TS** off the ≤`limit` handles. A **retired** person reads `stage: null` but `inRoster: true` — known, deliberately dropped, never re-nagged (D95(b)). New shared `intParam` (absent → default, out of range → clamped, garbage → **400**). Panel: a `Section`+`EmptyState` "Timeline affinity" drawer under the People roster, **collapsed and unfetched until clicked**, rows reusing the roster's own `setSelected` so an unknown handle rides the existing `NotYetKnown` create-file flow. 5 files, +5 tests (1162→**1167**).
- **prior (HV.3):** the Harvest tab's four options — a **Download CSV** checkbox (Start blocked with a warn line when it and Send-to-stratus are both off), a **Min views** floor applied at store time in `harvestPosts`/`harvestReplies` (after `noteSeen`, so filtered items still count as scrolled-past — else the day-window break dies), the whole form persisted in one `harvestForm` key through the lenient `parseHarvestForm`, and a **`Passive: N rows today`** line off `api.harvest.runs` + the new UTC-boundary `passiveRowsToday` (`Passive capture off` when the toggle is off, nothing at all while unloaded/failed). `HarvestOptions` gained `downloadCsv?`/`minViews?`, both absent-means-old-behaviour; a DB-only run reports `done.filename: ''`. New `shared/harvest.test.ts` (+15, 1147→**1162**), new `HarvestRun` type + `api.harvest.runs`, `HarvestPanel` now takes `settings`. **Drive-bys:** `useSettings`'s onChanged filter was missing `KEY_PASSIVE_HARVEST` (HV.2 gap) and `.harvest-tuning .field` needed `min-width: 0`. No server/schema/migration/route change. $0.
- **prior (HV.2):** the client half of ambient ingest — scrolling **x.com/home** streams every tweet whose metrics label parses into `POST /x/harvest/passive`. One line in `applyBand` (`if (sig) recordPassiveHarvest`), **every band including `skip`**; gate order enabled → `/home` (re-read per call) → `!isHarvestActive()` → `findPermalink` → pending/throttle → **only then** `extractArticle` (now exported from `harvester.ts` alongside `Extracted` + `isHarvestActive()`). `flushPassiveHarvest` = the `flushSightings` transport, warn-and-drop, **never retried**. NEW pure `extension/src/shared/passiveHarvest.ts` (+8 tests, 1139→**1147**) + the `passiveHarvest` Settings toggle (its own key, default ON). **No server/schema/migration/route change.** $0.
- **prior (HV.1):** `POST /x/harvest/passive` — ambient home-timeline rows into the SAME `harvest_rows` under `mode='timeline'`, hung off one **server-created** run per UTC day (`{handle:'timeline', mode:'timeline', scope:'passive'}`; `POST /harvest/runs` still 400s that mode, so clients can't forge one). Five ordered gates: 60d prune scoped to timeline runs → find-or-create today's run (exported pure `utcDayStart`) → in-batch dedupe + 30-min recapture (both → `skippedRecent`) → 2000/day cap (`skippedCap`) → one sync txn insert + `rowCount` bump. **Zero schema change, no migration** (D92). No band stored (§7.12), no person events (decision 6), unreachable from `xFetch` — **$0**. New `src/x/routes/harvest.test.ts` (9 tests; 1130→1139).

## Ledger

Status: `[ ]` todo · `[~]` in progress (lane claimed) · `[x]` done (sha + date) · `[s]` skipped (reason in deviations).

### Wave 0 — Foundations
- [x] UI.1 Settings platform (xhigh) — 226368c (code+format+state+codemap; D12 absorbed into 61a04e7) 2026-07-18
- [x] UI.8 Design tokens dark refactor (high) — 1299e43 (styles.css in 61a04e7 per D12) 2026-07-18
- [x] UI.9 Light theme + Appearance (high) — f9ff346 2026-07-18 (sha corrected from orphaned 8c89950)
- [x] UI.10 UI primitives + grouped rail (xhigh) — c37d504 2026-07-18 (sha corrected from orphaned 50948c9 — see D17)
- [x] ST.1 Engine layers + PRNG (high) — 61a04e7 2026-07-18
- [x] ST.2 Studio shell refactor (high) — dffd60a 2026-07-18 (sha corrected from orphaned 860725f — see D16)
- [x] ST.3 Cloud mascot (high) — b82058b 2026-07-18 (sha corrected from orphaned 292603d — see D17)
- [x] ST.4 Patterns + presets (high) — 15f2563 2026-07-18 (sha corrected from orphaned c1b0314 — see D18)
- [x] ST.5 Milestone + streak cards (high) — 1f6b20f 2026-07-18 (sha corrected from orphaned bc3f1e8 — see D19)
- [x] ST.6 Code card + mono font (high) — 00b9615 2026-07-18 (sha corrected from orphaned b65266f — see D19/D20)
- [x] ST.7 Thread cover + list card (high) — acd95ec 2026-07-18 (sha corrected from orphaned 2104f46 — see D21)
- [x] ST.8 Chart card (high) — 289ec2c 2026-07-18 (sha corrected from orphaned d38bbc5 — see D22)
- [x] ST.9 Studio docs+smoke (high) — d1902e5 2026-07-18 (sha corrected from orphaned 47b10d2 — see D23)

### Wave 1 — Prompt & identity core
- [x] RU.1 Reply prompt → 3 variants (xhigh) — 63df913 2026-07-18 (sha corrected from orphaned 9c05464 — see D24)
- [x] RU.2 Migration: source/variants/model columns (high) — ddecb5f 2026-07-18
- [x] RU.3 Batch → 3 variants (xhigh) — d8ecc9a 2026-07-18
- [x] N.1 Niche schema + defaults + store (xhigh) — 0bbca12 2026-07-18 (sha corrected from orphaned da97495 — see D27)
- [x] N.2 Niche CRUD + activation + MCP (high) — c76a29b 2026-07-18 (sha corrected from orphaned 4312a0e — see D28)
- [x] N.3 Post prompt persona extraction (MAX) — 3b1547c 2026-07-18 (sha corrected from orphaned b06770c — see D29)
- [x] N.4 Reply prompt persona extraction (MAX) — 0668451 2026-07-18
- [x] N.5 Doctrine knobs threaded (high) — 7e9213d 2026-07-18 (sha corrected from orphaned 20faa44 — see D31)
- [x] N.6 Pillar/channel niche ownership (xhigh) — 730a009 2026-07-18
- [x] N.7 Settings Niche card (high) — 25a8c99 2026-07-18
- [x] N.8 Niche wizard (high) — 5c1031d 2026-07-18 (sha corrected from orphaned 527ad21 — see D35)
- [x] N.9 Niche docs+smoke (high) — 78d1d93 2026-07-18
- [x] ME.1 me_entries/me_goals + profile module (high) — `0545185` 2026-07-18 (sha corrected from orphaned f0c91f4 — see D37; N.9 sha reconciled e452f7c — see D36)
- [x] ME.2 /x/me routes + loader + smoke (high) — `43e6bc9` 2026-07-18 (sha corrected from orphaned a3a94bb — see D38)
- [x] ME.3 Inject me-context at prompt tails (xhigh) — `82f0291` 2026-07-18
- [x] ME.4 Me tab (high) — `5a4b427` 2026-07-18 (sha corrected from orphaned 92499ab — see D40)
- [x] ME.5 Playbook lift + digest goals fact (high) — `9df22ff` 2026-07-18 (sha corrected from orphaned d611dce — see D41)
- [x] ME.6 MCP x_me + x_add_me_entry tools (high) — `d4ef3dc` 2026-07-18 (sha corrected from orphaned 8686d79 — see D42)
- [x] ME.7 Me docs-sync (high) — `f010bf3` 2026-07-18 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.1 OpenRouter client (xhigh) — `fc5b3c4` 2026-07-18 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.2 askLLM + /llm routes — D1 (xhigh) — `3d3598c` 2026-07-18 (sha corrected from orphaned 87f3bd7 — see D45a)
- [x] AI.3 Prompt registry + overrides (MAX) — `a087a66` 2026-07-18 (sha corrected from orphaned 9f33fb2 — see D45b)
- [x] AI.4 /x/prompts routes (high) — `3c649ce` 2026-07-18 (sha corrected from orphaned 466b090 — see D47a)
- [x] AI.5 Batch/extract/pillar-draft via registry — D3 (MAX) — `7d0a786` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.6 Digest/icebreaker via registry + LLM gate (xhigh) — `e2c91ac` 2026-07-19 (sha corrected from orphaned b82f4ef — see D49)
- [x] AI.7 Thread drafter (xhigh) — `b932a06` 2026-07-19 (sha corrected from orphaned 995a08b — see D50)
- [x] AI.8 Rewrite assist (high) — `b9fe98d` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.9 Idea generator (high) — `8e53621` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.10 Settings AI subtab (high) — `a91387f` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.11 Prompts editor panel (high) — `e7d5be9` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.12 Model-effectiveness cell (high) — `9c59183` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AI.13 AI docs+smoke (high) — `f86e06a` 2026-07-19 (sha stamped post-commit; amend re-orphans it, next session reconciles) — **closes Wave 1**

### Wave 2 — Reply machine & on-page surfaces
- [x] RU.4 Buffer/panel variants (high) — `22cf0b1` 2026-07-20 (sha corrected from orphaned a3664af — see D58)
- [x] RU.5 Confirm endpoint (xhigh) — `5935107` 2026-07-20 (sha corrected to `983fbea` — see D59; recorded sha was an amend-orphan)
- [x] RU.6 Panel confirm wiring (high) — `1ed57bd` 2026-07-20 (sha corrected from orphaned 6261673 — see D60)
- [x] AX.1 Glance endpoint (high) — `9467220` 2026-07-20 (sha corrected from orphaned f1d85c2 — see D61)
- [x] AX.2 Glance chip view-model (high) — `e29b629` 2026-07-20 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] AX.4 tweetContext view-model (high) — `7ecb8df` 2026-07-21 (sha corrected from orphaned 7abec86 — see D64)
- [x] AX.3 Timeline chips, pill removal (xhigh) — `39fecb6` 2026-07-21 (sha corrected from orphaned bc2c9fe — see D65)
- [x] AX.5 Status-page context panel (xhigh) — `5e31b8d` 2026-07-21 (sha corrected from orphaned 0eb5319 — see D66)
- [x] AX.6 Dossier click-through (high) — `b3fd7b3` 2026-07-21 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] RU.7 On-page variant chips (xhigh) — `f90084b` 2026-07-21 (sha corrected from orphaned bfa2441 — see D69)
- [x] RU.8 Manual add ⊕ (xhigh) — `9ba6fba` 2026-07-21 (sha stamped post-commit; amend re-orphans it, next session reconciles)
- [x] RU.9 Playbook source attribution (high) — `f78b48c` 2026-07-21 (sha corrected from orphaned 899e126 — see D72)
- [x] RU.10 RU docs+smoke (high) — `22099a3` 2026-07-21 (sha stamped post-commit; amend re-orphans it, next session reconciles) — **closes the RU sub-plan**
- [x] AX.7 AX docs+smoke (high) — `00ec8a8` 2026-07-21 (sha stamped post-commit; amend re-orphans it, next session reconciles) — **closes the AX sub-plan (AX.1–AX.7)**
- [x] RL.1 Reply-list engine (xhigh) — `ffbdb3f` 2026-07-22 (sha corrected from orphaned 58903fc — see D77)
- [x] RL.2 Schema + CRUD (high) — `9c0d97b` 2026-07-22 (sha corrected from orphaned a29ffbe — see D79a)
- [x] RL.3 /use route (high) — `c3f44d2` 2026-07-23 (sha corrected from orphaned 0ea02a3 — see D80a)
- [x] RL.4 AI generator — D5 (high) — `99eef6b` 2026-07-23 (sha stamped post-commit; amend re-orphans it, next session reconciles by subject)
- [x] RL.5 Lists subtab UI (high) — `17a530f` 2026-07-23 (sha stamped post-commit; amend re-orphans it, next session reconciles by subject)
- [x] RL.6 QuickReplyPicker (high) — `aae4665` 2026-07-23 (sha stamped post-commit; amend re-orphans it, next session reconciles by subject)
- [x] RL.7 Playbook canned bucket (high) — `3a4ae56` 2026-07-23 (sha corrected from orphaned b2543e7 — see D84a)
- [x] RL.8 RL docs+smoke (high) — `a120207` 2026-07-23 (sha corrected from orphaned 7ac1b5e — see D85c) — **closes the RL sub-plan (RL.1–RL.8)**
- [x] NT.1 Notification parser (high) — `2063b86` 2026-07-23 (sha corrected from orphaned 7f3d2fd — see D86a)
- [x] NT.2 Engagement events + ingest (xhigh) — `4e4d9fc` 2026-07-23
- [x] NT.3 Engagements route (high) — `2b763b1` 2026-07-23 (sha stamped post-commit; the amend re-orphans it, next session reconciles by subject)
- [x] NT.4 Background notif-context (high) — `fe0f6aa` 2026-07-23
- [x] NT.5 Notifications content script (xhigh) — `69784da` 2026-07-23 (sha corrected from orphaned 3a94830 — see D90a; NT.4 sha reconciled to `68039f2` — see D89a)
- [x] NT.6 Fans engagement count (high) — `0c0ab5d` 2026-07-23 (sha corrected from orphaned 532f87d — see D91a)
- [x] NT.7 NT docs+smoke (high) — `5d4a1a4` 2026-07-23 (sha corrected from orphaned 75f15e7 — see D93a) — **closes Wave 2**

### Wave 3 — Ambient data & guardrails
- [x] HV.1 Passive harvest endpoint (xhigh) — `5ea3495` 2026-07-23 (sha corrected from orphaned bf5e7b5 — see D93a)
- [x] HV.2 Passive capture + toggle (xhigh) — `661d039` 2026-07-23 (sha corrected from orphaned 090a8ac — see D94a)
- [x] HV.3 Harvest tab options (high) — `4c2adcf` 2026-07-23 (HV.2 sha reconciled to `661d039` — see D94a)
- [x] HV.4 Timeline affinity (high) — `3132f10` 2026-07-23 (HV.3 sha reconciled to `5cd316a` — see D95a)
- [x] HV.5 Funnel playbook cell (high) — subject `feat(playbook): timeline opportunity-capture funnel over passive harvest rows (HV.5)`, parent `855b424`, 2026-07-23 (no sha by design — D97)
- [x] HV.6 HV docs+smoke (high) — subject `docs(harvest): Harvest 2.0 docs-sync + $0 smoke-passive-harvest.ts (HV.6)`, parent `3416f14`, 2026-07-23 (no sha by design — D97). **Closes the HV sub-plan.**
- [ ] GR.1 Following ledger (xhigh)
- [ ] GR.2 Harvester following mode (xhigh)
- [ ] GR.3 Curation queue (xhigh)
- [ ] GR.4 Following subtab (high)
- [ ] GR.5 Monitor rules + route + MCP (xhigh)
- [ ] GR.6 Monitor surfacing (high)
- [ ] GR.7 Goals + commitments — D4 (xhigh)
- [ ] GR.8 Accountability surfacing (high)
- [ ] GR.9 Digest scorecard (high)
- [ ] GR.10 GR docs+smoke (high)

### Wave 4 — Authoring 3.0
- [ ] A3.1 Active-times parser (xhigh)
- [ ] A3.2 audience_activity + routes (high)
- [ ] A3.3 Analytics capture (xhigh)
- [ ] A3.4 Audience-blended slots (xhigh)
- [ ] A3.5 Manual publish core (MAX)
- [ ] A3.6 Manual reconcile (xhigh)
- [ ] A3.7 Composer toggle + chips (high)
- [ ] A3.8 Manual alarms + Today card (xhigh)
- [ ] A3.9 DM drafts — D5 (xhigh)
- [ ] A3.10 DM UI (high)
- [ ] A3.11 Articles CRUD (high)
- [ ] A3.12 Article prompt + assist — D5 (xhigh)
- [ ] A3.13 Writer page (xhigh)
- [ ] A3.14 Calendar week board (xhigh)
- [ ] A3.15 A3 docs+smoke (high)

### Wave 5 — Settings moat + polish
- [ ] UI.2 Doctrine/quests knobs — D2 (xhigh)
- [ ] UI.3 People/followups knobs (high)
- [ ] UI.4 Gates/workers knobs + MCP (high)
- [ ] UI.5 Money/AI knobs + ceilings (xhigh)
- [ ] UI.6 Extension mirror (xhigh)
- [ ] UI.7 Band thresholds e2e (xhigh)
- [ ] UI.11 Settings tab rebuild — D6, D8 (xhigh)
- [ ] UI.12 Today polish (high)
- [ ] UI.13 Composer/Calendar polish (high)
- [ ] UI.14 People/Channels/Voice/Ideas polish (high)
- [ ] UI.15 Replies/Playbook/Harvest/Studio polish (high)
- [ ] UI.16 Content-script + explorer tokens (high)
- [ ] UI.17 UI docs+smoke (high)

## Hot-file locks

A lane claims a file before starting, releases on commit. `owner: —` = free.

| File | Owner |
|---|---|
| `extension/src/content.ts` | — |
| `extension/src/harvester.ts` | — |
| `extension/src/sidepanel/Settings.tsx` | — |
| `extension/src/sidepanel/Composer.tsx` | — |
| `src/x/routes/replies.ts` + `src/x/replies/prompt.ts` | — |
| `post prompt.md` / `reply prompt.md` (+ TS literals) | — |
| `src/x/playbook.ts` + `src/x/routes/playbook.ts` | — (HV.5 released them; the pure file's sections now run to **§12** — the next `build*` is §13) |
| `src/db/migrations/` journal (any migration task) | — |
| `src/x/routes/brief.ts` | — |
| `src/x/routes/harvest.ts` | — (HV.4 released it; the router is now POST runs/rows/passive + GET runs/affinity — still no `:param` route, keep it that way. **HV.6 added no route; `scripts/smoke-passive-harvest.ts` now mounts this router + playbook over the real DB, so a route rename breaks the smoke as well as the suite.**) |
| `extension/src/shared/harvest.ts` + `sidepanel/Harvest.tsx` | — (HV.3 released them; nothing before UI.15's polish pass claims them again) |

## Deviations & decisions register

Pre-seeded from cross-plan analysis (see MASTERPLAN "Cross-plan adaptations" for full text).
Append D11+ as work reveals divergences from plan text.

- **D1** (binds AI.2): `app_settings` created once by UI.1; AI.2 reuses it — no second table/migration.
- **D2** (binds UI.2): the 5 doctrine knobs are owned by `niches.doctrine` after N.5; UI.2 must not duplicate them in `app_settings` — read/write through the active niche or drop the keys. **Decision pending at UI.2.**
- **D3** (binds N.4, AI.5): `VOICE_BLOCK_END` renamed by RU.1 to `'## The three variants'`; N.4 substitutes `{{REPLY_PERSONA}}` inside the slice; AI.5 retires slicing — its parity fixture is post-RU/post-N output, and its anti-drift test asserts against defaults containing the niche placeholder.
- **D4** (binds GR.7): no separate `goals` table — extend `me_goals` (baseline fields, `posted_replies`/`originals` metric kinds, `missed` status). One goals system, one digest fact. **Confirm merge shape at GR.7.**
- **D5** (binds RL.4, A3.9, A3.12): after AI.2/AI.6, new LLM calls use `askLLM` + `llmConfigured()`; new prompts register in the registry (`dm`, `article`) so the editor covers them. Refusal-ladder order unchanged.
- **D6** (binds all Settings.tsx tasks pre-UI.11): keep feature Settings UI minimal; UI.11 rebuilds and absorbs.
- **D7** (standing): all NEW UI from Wave 1 on uses UI.10 primitives + `--strat-*` tokens; Wave-5 polish passes touch only pre-masterplan tabs.
- **D8** (binds UI.11): prune the "coming soon" manifest to features still unbuilt at that time.
- **D9** (standing): content.ts task order is fixed: AX.3 → AX.5 → RU.7 → RU.8 → NT.5 → HV.2 → A3.3.
- **D10** (standing): registry knob groups (UI.2–5) run in Wave 5 so they catalog the final constant set, including RU/HV/GR/A3 constants.
- **D11** (UI.8, standing): `biome.json` `files.ignore` now includes `Stratus Design System` — the DS reference folder (Decision 9, plan out-of-scope) was ~112 of the baseline lint errors. Not in UI.8's plan edit-list but a correct, universal fix; whole-repo lint dropped 119→7. The remaining 7 are pre-existing and NOT in any changed file: 2 a11y in `extension/src/sidepanel/Replies.tsx` (UI.15 territory) + 5 biome-format on generated `src/db/migrations/meta/*.json` (a future infra task could ignore `src/db/migrations/meta`).
- **D12** (UI.8, historical): UI.8's `styles.css` and UI.1's whole settings platform were absorbed into ST.1's commit `61a04e7` (15 files, 3894 ins) by a concurrent lane's broad `git add` before either owner committed. Not reverted — `61a04e7` is shared HEAD and other lanes built on it; rewriting shared history mid-run is more dangerous than the mislabel. UI.8's `styles.css` content is correct there (0 literals outside `:root`, build green). **Process lesson: parallel masterplan lanes in ONE worktree must stage explicit paths (`git add <files>`), never `git add -A`/`commit -am`.**
- **D14** (UI.9): two things. **(a) Sha reconciliation** — the git history was rewritten (rebase/squash) after UI.1 wrote STATE, so `7e07dd5`/`40c718e`/`95b9fff` are gone; real Wave-0 shas corrected in the ledger (UI.1 `226368c`, UI.8 `1299e43`, ST.1 `61a04e7`). All ticked work verified present in the tree — status was accurate, only shas stale. **(b) Density plumbing divergence** — the plan assumed density = "override ~6 spacing vars", but UI.8 did NOT variableize spacing (`.panel` hardcoded `padding:14px;gap:12px`, `.row` `gap:8px`). UI.9 introduced three semantic vars `--ui-panel-pad`/`--ui-section-gap`/`--ui-row-gap` (cozy defaults in `:root`, compact overrides in `[data-density='compact']`) and wired `.panel`/`.row` to them; `--ui-root-size` (13px, overridden by `[data-scale='12'|'14']`) drives `body` font-size. **Binds UI.11–16 polish:** density currently only tightens `.panel`/`.row`; a polish task wanting finer compaction adds more consumers of the `--ui-*` vars (don't remap the generic `--strat-space-*` tokens — they're used everywhere).
- **D15** (ST.2, binds ST.3–ST.8): the Studio shell landed at **554 lines, not the plan's "< ~300"**. The plan's explicit split (registry.ts + fields.tsx + KitEditor.tsx) was done in full AND the AI-background section + library rail were also extracted into `fields.tsx` as `BackgroundFields`/`LibraryRail` (beyond the 4 named field components) — but the shell's irreducible logic (all state, 6 effects incl. the debounced render loop, and the async handlers generateBackground/saveToLibrary/reopenAsset/loadStatData/loadBannerData/stampVisualMade) can't be moved without a `useStudio` hook the plan didn't ask for. The **actual goal is met**: a new template = 1 `registry.ts` row + 1 field section + 1 `buildSpec` case, zero shell edits beyond those. ST.3/ST.5/ST.7/ST.8 register new templates through `TEMPLATES`/`buildSpec`; ST.3 also appends mascot layers in `templates.ts` (not the shell). **Do NOT add per-template render branches back into `Studio.tsx`** — go through the registry.
- **D16** (ST.3): two things. **(a) Sha reconciliation** — ST.2's recorded `860725f` was an amend-orphan (`git merge-base --is-ancestor 860725f HEAD` → false); real ST.2 commit is `dffd60a` (= HEAD at ST.3 start). Corrected in the ledger, `last-commit`, codemap header + §11. Same sha-stamp trap as UI.9/UI.10 — status was accurate, only the sha was stale. **(b) Mascot placement diverges from the plan's loose anchors** (all QA-adjustable at ST.9, browser-verified here so they're de-risked): quote card = happy mascot at **x:96, y:598, scale:0.55** — the plan said "bottom-left" but the handle occupies the natural bottom-left slot, so the mascot sits *below* the handle (y:598, card h:675). Stat = celebrating/happy at **x:1020, y:334, scale:0.6** (plan's ≈1020/330). Banner = thinking at **x:1190, y:140, scale:1.5**, guarded `kit.mascot && !data.background && data.followers === null`. When no milestone the headline box still spans w:1340, so a very long headline *could* run under the banner mascot — didn't in the test render, flagged for ST.9. Confetti seed default 7; body strokeWidth `max(1.5, 3*scale)`, face `max(1.2, 2.4*scale)` (scale-proportional, since `drawPath` normalizes strokeWidth to output px).
- **D17** (ST.4): three things. **(a) Sha reconciliation** — ST.3's recorded `292603d` and UI.10's `50948c9` were both amend-orphans (`git merge-base --is-ancestor` → false); real commits `b82058b` (ST.3) and `c37d504` (UI.10), corrected in the ledger + codemap header + §11. Status was accurate, only shas stale — same trap as D16/D14a. **(b) `patternSeed?` added beyond the plan** — the plan said thread only `patternKind?` onto the card-data interfaces, but reroll determinism needs the seed too, so `QuoteCardData`/`BannerData`/`TemplateState` carry `patternKind?`+`patternSeed?` (seed default 7, only `blobs` reads it). **(c) KitEditor prop shape changed** — the S5.2 `kit`/`onReplace` props became `bundle` (a `BrandKits`) + preset callbacks (`onSelectPreset`/`onSaveAs`/`onRename`/`onDelete`/`onImport`/`onResetActive`); the shell's single-kit `replaceKit` is gone. **Binds ST.7** (touches `Studio.tsx`/`StudioSeed`): the shell now holds `bundle` (not `kit`) — read the active kit via `activeKit(bundle)`; `StudioSeed` still lives in `Studio.tsx` (add `template?` there per plan).
- **D18** (ST.5): three things. **(a) Sha reconciliation** — ST.4's recorded `c1b0314` was an amend-orphan (`git merge-base --is-ancestor c1b0314 HEAD` → false); real ST.4 commit is `15f2563` (= HEAD at ST.5 start). Corrected in the ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16/D17. **(b) Confetti is a fixed-seed built-in, NOT the S5.4 pattern picker** — milestone/streak cards are `supportsAiBackground:false`, so they don't get the background segmented control; their `blobs` confetti is authored directly in the spec via `confettiLayer(kit,w,h)` at `withAlpha(kit.accent,0.25)`, seed **11** (module const `CONFETTI_SEED`). Deterministic (a fixed seed satisfies the "confetti deterministic" test); no reroll UI. **Binds ST.8** if the chart card wants confetti — reuse `confettiLayer`, don't thread patternSeed. **(c) `metrics.account()` client + types shipped** — `api.metrics.account()` over `GET /x/metrics/account` returning `MetricsAccountResponse{count,latest,series}` with `AccountSeriesPoint` (both new in `shared/types.ts`, `snapshotAt` is the ISO string of the server's Date). **ST.8 reuses this exact client** (MASTERPLAN dep ST.5) — don't re-add it. The milestone auto-detect maps `series → {date: snapshotAt, followers: followersCount}` into `latestCrossed`.
- **D19** (ST.6): three things. **(a) Sha reconciliation** — ST.5's recorded `bc3f1e8` was an amend-orphan (`git merge-base --is-ancestor bc3f1e8 HEAD` → false); real ST.5 commit is `1f6b20f` (= HEAD at ST.6 start). Corrected in the ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16/D17/D18. **(b) `STUDIO_MONO_STACK` lives in `templates.ts`, not `brandKit.ts`** — the plan named `brandKit.ts:32`'s `STUDIO_FONT_STACK` only as a *precedent to follow*, not a location mandate; the mono stack is code-card-only and belongs with its consumer. The code card font is independent of `kit.fontFamily` (code is always mono) via a local `monoFont(weight,sizePx)` helper. **(c) `assets.ts` needed no edit** — ST.5 pre-whitelisted `code` (and `thread`/`list`/`chart`) in `ASSET_KINDS`, so the plan's "kind:'code' degrades to 'other'" fallback never had to fire. No server change in ST.6 at all (Studio-sealed lane, as designed).
- **D20** (ST.7): four things. **(a) Sha reconciliation** — ST.6's recorded `b65266f` was an amend-orphan (`git merge-base --is-ancestor b65266f HEAD` → false); real ST.6 commit is `00b9615` (= HEAD at ST.7 start). Corrected in the ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16/D17/D18/D19. **(b) `App.tsx` needed NO edit** — the plan's edit-list included `App.tsx` ("prop type only"), but `StudioSeed` is imported by App and `openStudio` is generic over it, so widening `StudioSeed` in `Studio.tsx` (adding `template?: 'quote'|'thread'`) is structurally sufficient; typecheck green with App untouched. `Composer.tsx`'s inline `onMakeVisual` prop type was widened to `{text; postId?; template?: 'quote'|'thread'}` and `onMakeVisual={openStudio}` still typechecks (same shape). **(c) Thread cover + list card are BOTH `supportsAiBackground:true`** — the plan said "both honor kit.mascot/patterns via the Task 4 `baseLayers` signature", and patterns only reach templates whose TEMPLATES row is bg-capable (ST.4 gotcha), so both carry `background?`/`patternKind?`/`patternSeed?` and get the full gradient/pattern/AI segmented control for free. **(d) The list card has NO mascot** (the plan describes the thinking mascot only on the thread cover; the list's ≤6 rows fill the card). The thread cover's thinking mascot is bottom-right, suppressed under an AI background like the quote card. **Binds ST.9:** the thread mascot coords (x:1030,y:548,scale:0.55) and both cards' layouts are QA-adjustable in the browser walk (same de-risking spirit as D16's mascot placements).
- **D21** (ST.8): three things. **(a) Sha reconciliation** — ST.7's recorded `2104f46` was an amend-orphan (`git merge-base --is-ancestor 2104f46 HEAD` → false); real ST.7 commit is `acd95ec` (= HEAD at ST.8 start). Corrected in the ledger + `last-commit` + codemap header + §11 (the §11 ST.7 line had a literal `→**ST.7 sha**` placeholder — now `acd95ec`). Same sha-stamp trap as D16–D20. **(b) `heatmapCells(cells, minN=3)` takes minN** — the plan wrote `heatmapCells(cells)`, but `BestTimeCell` carries no `sufficient` field; the S0.4 gate is `BestTimesResponse.minN`, so the signature needs it. Default `HEATMAP_MIN_N=3` (the server also gates its `top` at 3). The shell passes `best.minN`. `heatmapCells` emits the FULL 7×24 grid (168 cells) so the template just places each — a cell below `minN` OR with no `avgViewsPerDay ?? avgViews` value is `sufficient:false`/`intensity:null` (renders the muted ink wash). Intensity = min→max over the sufficient set; a flat set → uniform 0.5 (guarded span, never all-invisible). **(c) Chart card is NOT bg-capable** (`supportsAiBackground:false`, like the stat card) — the plan was silent; an AI image behind a data chart would fight it, so no `patternKind`/`patternSeed` threading and no bg segmented control. `growthSeries` returns RAW follower points (the `sparkline` layer does its own min/max fit via `sparklineCoords`) — no normalization in chartData for growth. **Binds ST.9:** the chart card's two live-data fills (account series → growth, best-times → heatmap) + below-gate muting are the browser-walk items; both are $0 reads.
- **D13** (UI.1, binds UI.2): the **doctrine group is seeded in full by UI.1** — the 6 keys `x.doctrine.{replyTargetMin,replyTargetMax,weekReplyTargetPct,anchors3,anchors4,ladderSwitchAt}`, with the anchors' sorted-unique+range numberArray validation already in place (Task 2's plan attributed that hook to itself; landed early because UI.1 seeds the anchors so it must validate them). **UI.2 must NOT re-declare these doctrine keys** — it only adds the `quests`+`display` groups and wires the consumers (brief quota/ratio/gaps, composer ladder). Store layering (also binds AI.2/D1): `src/settings/store.ts` is platform-agnostic and never imports `src/x/*`; the X registry `src/x/settings/registry.ts` implements the store's `SettingsRegistry` interface and exposes the bound `getSetting`/`resolveSetting`/`getAllValues`/`setSettings`/`resetSettings` — **UI.2+ consumers import those from `../settings/registry.ts`, never the store directly**. biome.json now also ignores `src/db/migrations` (fulfils D11's anticipated infra ignore; the 5 generated-JSON lint errors are gone → whole-repo lint = 2, both pre-existing `Replies.tsx` a11y).

- **D22** (ST.9): two things. **(a) Sha reconciliation** — ST.8's recorded `d38bbc5` was an amend-orphan (`git merge-base --is-ancestor d38bbc5 HEAD` → false); real ST.8 commit is `289ec2c` (= HEAD at ST.9 start). Corrected in the ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16–D21 — status was accurate, only the sha was stale. **(b) Browser verification = the S3 harness discipline, not the full unpacked-extension walk.** The plan asked to "load the unpacked extension, walk every template in the gallery." Loading unpacked needs a native "Load unpacked" file-picker (not scriptable) + a live-token server for the live-data cards, so the walk was done the way ST.3's mascot was verified: a throwaway Bun-served harness (`extension/src/studio/__harness_entry.ts`, **deleted before commit**) bundled the real `compose`/`templates`/`registry` modules and rendered every template onto a canvas in Chrome. Verified: all 10 templates × 3 presets render with bundled Inter+JetBrains Mono; mascot poses correct + kit-colored; confetti; chart growth+heatmap with below-gate muting; **preset re-skin** (Midnight→Paper→Neon); **byte-identical determinism** across two renders for every spec + the seeded blobs pattern; **Copy-PNG** wrote a 174KB image/png ClipboardItem from a real click. This **clears the "unverified in a browser" flags** ST.1/2/4/5/6/7/8 accrued. Live-data fills (stat/milestone/chart) used representative sample data of the exact `/x/brief`·`/x/metrics/*` shapes, not a live server — the "first real card pasted into a live tweet" tail is the user's, same spirit as prior "done when needs a live launch" entries. **No binding constraint on future tasks** — Studio lane is closed.

- **D23** (RU.1): two things. **(a) Sha reconciliation + an out-of-skill commit** — ST.9's recorded `47b10d2` was an amend-orphan (`git merge-base --is-ancestor 47b10d2 HEAD` → false); real ST.9 = `d1902e5`. Additionally `906f2d3` ("eval 1") was committed OUTSIDE the skill between ST.9 and RU.1 — it's the Wave-0 audit fix already documented as RESOLVED in the gotchas (`src/db/client.ts` defaults `:memory:` under `NODE_ENV==='test'`, + a `stratus.db.backup-*` artifact). Touches nothing in RU.1's edit-scope; no status drift. Corrected in ledger `last-commit` + codemap header + §11. **(b) D3 confirmed, not diverged** — RU.1 renamed `VOICE_BLOCK_END` to `'## The three variants'` exactly as D3 predicted; N.4 substitutes `{{REPLY_PERSONA}}` inside that slice, AI.5 retires slicing. No change to D3.

- **D25** (RU.3): four things. **(a) Sha reconciliation** — RU.2's recorded `ddecb5f` was an amend-orphan (`git merge-base --is-ancestor ddecb5f HEAD` → false, though `git cat-file -t` still finds it); real RU.2 = `182716a` (= HEAD at RU.3 start, subject clearly "(RU.2)"). Corrected in ledger `last-commit` + codemap header + §11. Same sha-stamp trap as D16–D24; status accurate, only the sha stale. **(b) `variants?` made OPTIONAL in `buildRadarDraftRows`/`persistRadarDrafts` (beyond the plan's literal "stores full variants JSON")** — the plan didn't specify the no-variants case, and the smoke script (`scripts/smoke-radar-drafts.ts:39-56`) + any CLI caller pass only `{tweetId,text,angle}`. New private `BatchReplyRow` type in `radar.ts` with `variants?`; stored value = `r.variants && r.variants.length>0 ? r.variants : null` (RU.2 "null=unknown" semantics; RU.5's confirm reconstructs `[{text:replyText,angle}]` from the primary at read time). Smoke script unbroken, no edit. The batch route always supplies all 3, so real radar rows carry variants. **(c) Slice-guard test sentinel swapped** — `src/test.test.ts:503` `not.toContain('## The three variants')` collided with the new batch heading "## The three variants for each post"; changed to `not.toContain('not three paraphrases')` (unique to the single-reply variants section, `prompt.ts` L91) + kept the `{{TWEET_CONTEXT}}`/`{{IDEA}}` placeholder checks (the definitive VOICE_BLOCK_END desync evidence). **(d) Pre-existing red, NOT RU.3:** see gotchas — `inspect.test.ts` fails only under bare `bun test`; green under canonical `bun run test`. **No binding constraint on future tasks** except RU.4/RU.5 which now consume the populated `radar_drafts.variants` (RU.4 rehydrates them into the buffer; RU.5's confirm reads them with the primary fallback).
- **D24** (RU.2): three things. **(a) Sha reconciliation** — RU.1's recorded `9c05464` was an amend-orphan; real RU.1 = `63df913` (= HEAD at RU.2 start). Corrected in ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16–D23; status accurate, only the sha stale. **(b) Migration is `0014`, NOT the plan's "0013"** — `0013_silent_nova` was already taken by UI.1's `app_settings`. Per Step-4 (ignore hardcoded plan migration numbers), `bun run db:generate` produced `0014_swift_rhodey.sql` — four nullable `ADD COLUMN` ALTERs, DDL-only, **no seed INSERT dropped** (none exist for these tables). **(c) `buildRadarDraftRows`/`persistRadarDrafts` now take a 3rd `model: string \| null` arg** — the call site in `replies.ts` passes `result.model`; `variants` stays `null` on every radar row until **RU.3** lands the 3-variant batch response (RU.3 must set `variants` in `buildRadarDraftRows` and thread the batch reply's variants through). Binds **RU.5** (confirm endpoint): it reads `radar_drafts.model` (fallback `'radar-batch'`) to satisfy `reply_drafts.model` NOT NULL, and stamps `reply_draft_id` + status→`clicked` in one txn. The new columns are otherwise **inert** — nothing reads them yet.

- **D26** (N.1): five things. **(a) Sha reconciliation** — RU.3's recorded `d8ecc9a` was an amend-orphan (`git merge-base --is-ancestor d8ecc9a HEAD` → false, `cat-file` still finds the object); real RU.3 = `c81046a` (= HEAD at N.1 start). Corrected in ledger `last-commit` + codemap header + §11. Same sha-stamp trap as D16–D25; status accurate, only the sha stale. **(b) Migration is `0015_faulty_blazing_skull.sql`, NOT the plan's "0013"** — 0013 (app_settings) + 0014 (RU.2) already taken; per Global Rule #1 `bun run db:generate` produced the DDL, then the seed `INSERT OR IGNORE INTO niches` (byte-exact from `DEFAULT_NICHE`) + the two `UPDATE … SET niche='builder'` backfills were **hand-appended** (drizzle drops DML — codemap §4 trap). **(c) `loadDoctrine()` calls `loadActiveNicheSafe()`, not the plan's literal `loadActiveNiche()`** — strictly more robust and consistent with the module's own §7.8 rationale (a niche-layer read must never 500 a serving path); happy-path identical. **Binds N.5** — its brief/targets/etc. consumers call `loadDoctrine()`, which now can't throw. **(d) Introduced beyond the plan's field list: a `Niche` interface + `isValidNicheSlug` helper.** `Niche` mirrors the row but with `createdAt?`/`updatedAt?` **optional**, so `DEFAULT_NICHE` omits them and the N.3/N.4 cache key `niche.updatedAt?.getTime() ?? 0` reads **0** for the fallback (stable, not process-start-dependent). `isValidNicheSlug` mirrors pillars' `isValidPillarSlug`. **Binds N.2** (CRUD reuses `NICHE_SLUG_RE`/`isValidNicheSlug`) and **N.3/N.4** (they read `niche.updatedAt?.getTime() ?? 0`). **(e) Builder niche `label` = `'The 51-year-old builder'`** (plan left it unspecified) — N.7's Settings card + any label assertion should reference this string. `DEFAULT_NICHE.doctrine = null` (builder uses `DEFAULT_DOCTRINE`); the seed omits the doctrine column → NULL.

- **D27** (N.2): four things. **(a) Sha reconciliation** — N.1's recorded `da97495` was an amend-orphan (`git merge-base --is-ancestor da97495 HEAD` → false, `cat-file -t` still finds the object); real N.1 = `0bbca12` (= HEAD at N.2 start, subject clearly "(N.1)"). Corrected in ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16–D26; status accurate, only the sha stale. **(b) Route-test bearer case deliberately NOT duplicated** — the plan's Task-2 test list includes "bearer 401", but the `/x` bearer middleware is a shared app.ts-level guard already covered by `app.test.ts` + `mcp.test.ts`; `niche.test.ts` mounts `nicheRouter` on a bare Hono (the `channels.test.ts` pattern) so the CRUD guards run without an API_TOKEN gate. Auth is not niche-specific — no value in a third copy. **(c) Doctrine PATCH is REPLACE, not merge** — a supplied `doctrine` object overwrites the stored JSON wholesale (only when `doctrine` is present in the body; absent → untouched); `null` clears to defaults. The plan said "reject when a supplied field is present but non-numeric"; `readDoctrine` does exactly that (finite positive wins, else 400 `invalid_doctrine`) and stores the validated partial. **Binds N.7** (Settings card): send the full 5-knob doctrine object on each save so replace-semantics don't drop a previously-set knob. **(d) `x_niche` is curated tier** (in-process `GET /x/niche`, forwarded bearer), placed after `x_digest`; MCP is now **17 tools** and `mcp.test.ts` asserts `names.size === 17` as a firm 16→17 regression guard.

- **D28** (N.3): five things. **(a) Sha reconciliation** — N.2's recorded `4312a0e` was an amend-orphan (`git merge-base --is-ancestor 4312a0e HEAD` → false, `cat-file -t` still finds the object); real N.2 = `c76a29b` (= HEAD at N.3 start). Corrected in ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16–D27; status accurate, only the sha stale. **(b) PLANNING ERROR — Task 3's done-when grep is unsatisfiable as written.** "`grep -rn "Pitești" src/x/posts src/x/routes` hits nothing" collides with the bit-for-bit equivalence requirement: **§6 of the post prompt (specificity palette) also carries builder biography** (Pitești/386/ANAF/hospital/med-school), as do §9's hook examples and §0's "51-year-old builder" phrase — but the plan's extraction scope (Design Decision 2, Task 3 edit list) is §1+§5 only, the N.1 schema has no "specifics" field, and DEFAULT_NICHE is frozen (already seeded byte-exact into the real DB). Equivalence wins (it's the task's MAX criterion): §1/§5 extracted exactly, §6/§9/§0 left byte-identical. The `Pitești` grep now hits exactly ONE line (`posts/prompt.ts` §6 Career). **Binds N.9:** the smoke's "zero builder strings in buildPostDraftInput" assertion must scope to §1/§5 markers (`Alteramens`, `Naval-derived` — both fully gone) not `Pitești`/`386`/`hospital`; a nutrition niche's post prompt WILL still carry §6/§9 builder texture until a future task adds a niche `specifics` field (schema change — deliberately not smuggled into N.3). Logged in the Planning-error log too. **(c) Pillar-draft cache key also niche-suffixed** — the plan's Task 3 text only names `drafter.ts`'s `PROMPT_CACHE_KEY`, but the Design section's blanket "cache keys gain a niche suffix" covers `PILLAR_DRAFT_CACHE_KEY` (the persona sits at the TOP of that prompt, so a niche edit changes its prefix); both now append `:${slug}:${updatedAt?.getTime() ?? 0}`. **(d) `buildPillarDraftInput.persona` is optional-with-default** (`DEFAULT_NICHE.persona`), not the plan's literal required `persona: string` — consistent with `BuildPostDraftOptions.persona?`, and a missed call site degrades to grounded-default instead of a compile error. The route composes `niche.persona + '\n\n' + description` and passes it. **(e) The niche load lives in `generateAndInsert`**, not per-route — both /posts/draft and /posts/reup funnel through it, so one load covers both (the plan's "both routes call loadActiveNicheSafe()" is satisfied by the shared pipeline).

- **D29** (N.4): four things. **(a) Sha reconciliation** — N.3's recorded `b06770c` was an amend-orphan (`git merge-base --is-ancestor b06770c HEAD` → false, `cat-file -t` still finds the object); real N.3 = **`3b1547c`** (= HEAD at N.4 start). Corrected in ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16–D28; status accurate, only the sha stale. **(b) The plan's "route test: generated draft's contextSnapshot.niche.slug === 'builder'" is not implementable in CI** — it requires a successful Grok call, and no route test in the repo mocks askGrok (bun runs all test files in one process; `mock.module` would leak into other suites). Same gap N.3's "drafter route test" had. Implemented instead: the stamp is code-reviewed one-liner (`ctx.niche = {slug}` before the insert), a **band-gate 422 route test** was ADDED to `app.test.ts` (dead-post context → 422 `band_gate`, pre-network — proves the refusal path is unchanged and sits before the niche read), and the live `contextSnapshot.niche.slug === 'nutrition'` assertion is N.9's smoke (already in the plan's Done-when). **(c) Substitution is via a shared `substituteReplyPersona(template, replyPersona?)` helper** (not inline in each builder) — token-tolerant (a template without `{{REPLY_PERSONA}}` passes through, the {{IDEA}} tolerance), used by both `buildGrokInput` (on the full template, FIRST — before context/idea, blocking token injection) and `buildBatchGrokInput` (on the head after `sharedVoiceBlock()` slicing — the slice carries the token, so batch substitutes post-slice; D3's `'## The three variants'` bound untouched). **(d) `GET /x/replies/default-prompt` now returns the template with the raw `{{REPLY_PERSONA}}` token** — deliberate: a user copying it into `systemPromptOverride` gets live niche substitution via the token-tolerant path (an override predating the token keeps its hardcoded persona). Binds RU/AI panel work that renders the default prompt: the token is expected, don't "fix" it.

- **D30** (N.5): three things. **(a) Sha reconciliation** — N.4's recorded `0668451` was an amend-orphan (`git merge-base --is-ancestor 0668451 HEAD` → false, `cat-file -t` still finds the object); real N.4 = **`943a8d0`** (= HEAD at N.5 start). Corrected in ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16–D29; status accurate, only the sha stale. **(b) `targetBand` signature took a `{minX,maxX}` band arg with a DEFAULT `{minX:2,maxX:10}`, not a required param** — exactly as the plan's "defaults keep every existing suite green untouched" asked; `src/test.test.ts:1465` and all non-passing callers stay on the default (regression), the 6 route call sites (voice ×2, brief, followups, playbook, digest) pass the active niche's `targetBandMinX/MaxX`. Doctrine is loaded per call site (one sync SELECT, no caching) — `loadDoctrine()` never throws (D26c). **(c) D2 (binds UI.2) is now DECIDABLE.** The 5 doctrine keys are owned by `niches.doctrine` and consumed via `loadDoctrine()` — UI.2 must NOT re-declare `x.doctrine.{replyTargetMin,replyTargetMax,weekReplyTargetPct,anchors3,anchors4,ladderSwitchAt}` in `app_settings` for the 5 niche-owned knobs (the anchors/ladderSwitchAt are NOT niche doctrine — those stay `app_settings`, seeded by UI.1 per D13). Read/write the 5 through the active niche (`GET /x/niche` / `PATCH /x/niches/:slug` doctrine), not the settings store.

- **D31** (N.6): six things. **(a) Sha reconciliation** — N.5's recorded `20faa44` was an amend-orphan (`git merge-base --is-ancestor 20faa44 HEAD` → false, `cat-file -t` still finds it, same subject as HEAD); real N.5 = **`7e9213d`** (= HEAD at N.6 start). Corrected in ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16–D30; status accurate, only the sha stale. **(b) Used `loadActiveNicheSafe()` not the plan's literal `loadActiveNiche()`** — the D26c convention (a niche-layer read must never 500 a serving path); happy-path identical. All four new call sites (pillars getActivePillars/GET/POST/guards, channels GET/POST/room, drafter refusal) use the safe variant. **(c) DELETE last-active guard scoped to target-in-set beyond the plan's literal "counts within the active niche's set"** — the guard only runs when `existing.niche === active.slug || existing.niche === null`, else a false 409 fires when the active niche has exactly 1 pillar and you delete a DIFFERENT (inactive) niche's pillar. PATCH already had the equivalent `.some(r=>r.slug===slug)` gate; DELETE needed the explicit niche check added. Correctness refinement, not a behavior the plan forbids. **(d) Last-active per-niche TEST framed via nutrition, not the plan's "deactivate builder's last pillar"** — builder has 3 seed pillars (mutating them would corrupt the shared seed other files read); instead the test makes nutrition the single-pillar ACTIVE niche (builder inactive with 3) and deactivates/deletes nutrition's only pillar → 409, proving the count is per-niche (a global count would see builder's 3 and NOT 409). Semantically identical guarantee, non-destructive. **(e) `people.ts:554` (icebreaker channel-overlap grounding) reads active channels directly and is NOT niche-filtered** — out of Task 6's scope (plan scoped it to pillars.ts/channels.ts/drafter.ts/calendar.ts). Harmless in v1 single-niche (all channels are `builder`, builder active); a future per-niche partition task should filter it. **(f) One cross-cutting test file `niche-ownership.test.ts` instead of two** — the plan said "Route tests for both files"; one file covering pillars + channels filtering + drafter refusal + calendar validation keeps all niche mutation + cleanup in a single afterAll (the N.2 shared-DB discipline). It is now the **FOURTH** niche-mutating test file (after `niche/niche.test.ts`, `routes/niche.test.ts`, `routes/doctrine.test.ts`) — all restore builder-active in afterAll.

- **D32** (N.7): two things. **(a) Sha reconciliation** — N.6's recorded `730a009` was an amend-orphan (`git merge-base --is-ancestor 730a009 HEAD` → false, `cat-file -t` still finds it, same subject as HEAD); real N.6 = **`3ecaebd`** (= HEAD at N.7 start). Corrected in ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16–D31; status accurate, only the sha stale. **(b) The Settings Niche card self-loads `getSettings()`** rather than taking a `settings` prop like `PillarsPanel` — `SettingsPanel` is rendered propless in App.tsx, and the api client ignores its `_s` arg anyway (the background owns the transport/bearer). Not a divergence that binds anything; just a note that `NicheCard` is drop-anywhere self-contained.
- **D33** (N.7, PLAN vs CODE): the plan's "+ new niche (slug + label → create inactive)" is **not achievable** — `POST /x/niches` (N.2 code) rejects empty `persona`/`beliefs`/`replyPersona` with 400 (`invalid_persona`/`invalid_beliefs`/`invalid_reply_persona`). Current code wins: the create form includes those three required textareas (optional description/doctrine left to the editor/wizard). **Binds N.8** — the wizard's proposal must fill persona/beliefs/replyPersona so a wizard→create round-trip passes the same validation; a future slug+label-only quick-add would need the route to seed placeholder persona text (out of N.7 scope). Not a planning error worth patching MASTERPLAN — the create form is honest about what the server needs.
- **D34** (N.8): two things. **(a) Sha reconciliation** — N.7's recorded `25a8c99` was an amend-orphan (`git merge-base --is-ancestor 25a8c99 HEAD` → false, `cat-file -t` still finds it, same subject "Settings niche editor + activation UI (N0.7)"); real N.7 = **`3d80988`** (= HEAD at N.8 start). Corrected in ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16–D32; status accurate, only the sha stale. **(b) Wizard flow landed in `Niche.tsx`, not `Settings.tsx`** — the plan's Task-8 edit-list named `Settings.tsx`, but N.7 (D33) already moved the whole niche card into `Niche.tsx`; the `NicheWizard` component was added there, `Settings.tsx` untouched. Current-code-wins (Step-4). The wizard satisfies D33: its proposal fills persona/beliefs/replyPersona so a wizard→create round-trip passes `POST /x/niches`'s non-empty validation. **Binds nothing downstream** — N.9's smoke drives `POST /x/niche/draft` directly (server), not the panel. Also fixed here: a duplicate `invalid_description` key in `Niche.tsx`'s `ERR` map (N.7 had one for the CRUD "too long", my wizard addition collided) — dropped mine, kept N.7's; the wizard's only UI-reachable 400 is oversize (Generate is disabled on empty input), for which "Description is too long." fits.

- **D35** (N.9): two things. **(a) Sha reconciliation** — N.8's recorded `527ad21` was an amend-orphan (`git merge-base --is-ancestor 527ad21 HEAD` → false, `cat-file -t` still finds it, same subject "AI niche wizard — prose to proposed niche (N0.8)"); real N.8 = **`5c1031d`** (= HEAD at N.9 start). Corrected in ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16–D34. **(b) Smoke follows D28b/D29b precisely, no new divergence.** `scripts/smoke-niche.ts`'s "biography gone" assertion scopes to the §1/§5-exclusive markers `Alteramens`/`Naval-derived` (verified absent from the post prompt template — extracted into the niche) and deliberately does NOT assert absence of §6's `Pitești` (still present, D28b); the reply-side absence marker is `build in public` (reply-persona-exclusive, verified 0 in `reply prompt.md`). The plan's Task-9 line 304 `--live` adds exactly ONE wizard call — the live `contextSnapshot.niche.slug === 'nutrition'` proof (top-level done-when, line 86) genuinely needs a Grok reply-generate call not in the Task-9 step list (D29b), so it stays the observable-in-production tail; the $0 `buildGrokInput` assembly assertion is its proxy. **Niche N0 (N.1–N.9) is complete — no niche task binds anything downstream except the standing D2 (UI.2 must not re-own the 5 doctrine knobs).**

- **D36** (ME.1): three things. **(a) Sha reconciliation** — N.9's recorded `78d1d93` was an amend-orphan (`git merge-base --is-ancestor 78d1d93 HEAD` → false, `cat-file -t` still finds it, same subject as HEAD); real N.9 = **`e452f7c`** (= HEAD at ME.1 start). Corrected in ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16–D35. **(b) Migration is `0016_high_miek.sql`, NOT the plan's "0013"** — 0013 (app_settings) / 0014 (RU.2) / 0015 (N0.1) already taken; per Step-4 `bun run db:generate` produced DDL-only for `me_entries`+`me_goals` (two CREATE TABLE + two CREATE INDEX, **no seed INSERT** — the plan says none is needed, and migration 0000's pillar seed was untouched, verified). **(c) Introduced beyond the plan's exact export list: `RenderGoal` type + `resolveGoals(goals, latestFollowers, now)` helper + `isEntryInWindow(e, now)` + `MeSelection` shape.** The plan's `renderMeContext(selection, goals, now)` couldn't take raw `MeGoal[]` because the followers lookup is a separate concern (the render has no `latestFollowers` arg) — so **`renderMeContext`/`renderMeBrief` take `RenderGoal[]`** (a goal already paired with computed `progress`), and the route/loader calls `resolveGoals` (thin map over `goalProgress`) FIRST. `isEntryInWindow` is exported so **ME.2's `GET /x/me` `inWindow` flag reads it, not re-derives it** (plan Design: "the UI must never fork the window logic"). **Binds ME.2** (loader: load active entries + active goals + latest followers → `selectEntriesForPrompt` + `resolveGoals` → `renderMeContext`/`renderMeBrief`; the `inWindow` per-entry flag = `isEntryInWindow`; each goal's `progress` = `goalProgress`) and **ME.3** (the three tail call sites feed the rendered STRING, so no signature change there — but if a call site wants to build the block itself, it must `resolveGoals` before `renderMeContext`). No niche/prompt/byte-sync surface touched — templates untouched (injection is ME.3).

- **D37** (ME.2): three things. **(a) Sha reconciliation** — ME.1's recorded `f0c91f4` was an amend-orphan (`git merge-base --is-ancestor f0c91f4 HEAD` → false, `cat-file -t` still finds it, same subject as HEAD); real ME.1 = **`0545185`** (= HEAD at ME.2 start). Corrected in ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16–D36; status accurate, only the sha stale. **(b) One shared `parseDate` helper + one internal `renderFor(mode)`** (beyond the plan's literal separate `renderFor` mention) — `parseDate` handles BOTH `happenedAt` and `deadline` (ISO string → Date | null | invalid); `renderFor` is the single source shared by `GET /me/context` and `loadMeContextSafe` (loads `active` entries + `status='active'` goals + latest followers → render), so the preview and the injected block can never diverge. **(c) `GET /me` returns ALL goals (no status filter), each with `progress`** — the plan's contract only filters entries by `kind`/`active`; goals come back regardless of status so the Me tab (ME.4) can show achieved/dropped goals too. `renderFor` (the injected block) still loads `status='active'` only — an achieved goal shows in the tab but not in a draft. **Binds ME.4** (render all goals, group by status client-side) and **ME.3** (the loader is the entry point — feed its returned STRING to the tails, no signature change).

- **D38** (ME.3): three things. **(a) Sha reconciliation** — ME.2's recorded `a3a94bb` was an amend-orphan (`git merge-base --is-ancestor a3a94bb HEAD` → false, `cat-file -t` still finds it, same subject as HEAD); real ME.2 = **`43e6bc9`** (= HEAD at ME.3 start). Corrected in ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16–D37; status accurate, only the sha stale. **(b) Batch signature diverged from the plan's literal — `meBrief` rides the opts BAG, not a 6th positional arg.** The plan wrote `buildBatchGrokInput(tweets, idea, systemOverride, pillarDefs, guidance, meBrief?)`, but N0.4 already made positional arg 6 the options bag `opts?: {replyPersona?}`. Widened it to `opts?: {replyPersona?; meBrief?}` (cleaner than a 7th positional, matches the file's current style); the call site passes `{replyPersona: niche.replyPersona, ...(meBrief !== undefined ? {meBrief} : {})}` (conditional spread for `exactOptionalPropertyTypes`). **Binds AI.5** (retires the batch-slice, touches `buildBatchGrokInput`): the signature is now `(tweets, idea?, override?, pillars?, guidance?, opts?: {replyPersona?; meBrief?})` — read it as it is, not as the AI-layer plan quotes it. **(c) Single-tail order is relationship → me → pillars → guidance.** The plan said "relationship → me → guidance", but the §8.6 pillars opt-in block (usually off) sits between relationship and guidance; `me` goes immediately after relationship (before pillars), guidance stays last — satisfies both "between relationship and guidance" and the stated order. `parseContext`/`parseBatchTweets` were NOT touched (strict allowlists already drop a client `me`; a new test locks it). Templates + both `.md` byte-untouched — byte-sync + N0.3/N0.4 equivalence tests green. **Binds nothing downstream except AI.5 (the signature) + ME.5** (the me-lift cell reads `contextSnapshot.me`, which the single reply path now stamps before the insert).

- **D39** (ME.4): three things. **(a) Sha reconciliation** — ME.3's recorded `82f0291` was an amend-orphan (`git merge-base --is-ancestor 82f0291 HEAD` → false, `cat-file -t` still finds it, same subject "feat(me): inject personal context at the variable tail of post + reply prompts (M1.3)" as HEAD); real ME.3 = **`aec637b`** (= HEAD at ME.4 start). Corrected in ledger + `last-commit` + codemap header (sha-trap list) + §11. Same sha-stamp trap as D16–D38; status accurate, only the sha stale. **(b) The `me` tab lives in the Operate group (after People), NOT its own group** — the plan Design said "wired in App.tsx after `people`"; the rail is grouped (`TAB_GROUPS`), so it joins the Operate group's tab list after People rather than adding a new eyebrow group. No structural change to `TAB_GROUPS`. **(c) Create/PATCH responses return the RAW row without the server-computed `inWindow`/`progress`** (the route returns the Drizzle row on POST/PATCH; only `GET /me` attaches them) — so the tab **reloads after every mutation** (`api.me.get` + both `context` modes in one `Promise.all`) and NEVER reads the computed fields off a mutation response. The api client still types create/patch as `Promise<MeEntry>`/`Promise<MeGoal>` (the computed field is just absent at runtime, unread — harmless, matches the Ideas/Pillars precedent). **Binds nothing downstream** except: ME.5/ME.6 that touch the extension should reuse `api.me.*` (don't re-add). **(d) Pre-existing lint baseline** — `bun run lint` reports 2 a11y errors in `extension/src/sidepanel/Replies.tsx` (last changed C6 commit `61bf692`, untouched by ME.4). They predate this session; ME.4's changed files lint clean in isolation. A future RU-lane task touching Replies.tsx should fix them; not ME.4's to own.
- **D40** (ME.5): four things. **(a) Sha reconciliation** — ME.4's recorded `92499ab` was an amend-orphan (`git merge-base --is-ancestor 92499ab HEAD` → false, `cat-file -t` still finds it, same subject "feat(me): Me tab — goals, quick log, entries, AI-view preview (M1.4)" as HEAD); real ME.4 = **`5a4b427`** (= HEAD at ME.5 start). Corrected in ledger + `last-commit` + codemap header (sha-trap list) + §11. Same sha-stamp trap as D16–D39; status accurate, only the sha stale. **(b) `buildMeEffectiveness` is a hybrid clone** — the plan named `buildRelationshipLift` as the exemplar (a `{withX, withoutX, viewsLift, profileVisitsLift}` two-bucket cell, NO `totalMeasured`), but the plan's own test spec requires the partition invariant `with.n + without.n === totalMeasured`. So the interface adds a `totalMeasured` field (lifted from `buildMediaEffectiveness`): `{withMe, withoutMe, totalMeasured, viewsLift, profileVisitsLift}`. Two buckets only — every reply's `contextSnapshot.me` is present or absent, no "unknown" third bucket (unlike media's null). **Binds nothing** — a self-contained playbook cell. **(c) Digest goals follow the `rosterCoverage` passthrough shape** — new `DigestGoal` interface; `DigestFactInputs.goals`/`DigestFacts.goals` are `DigestGoal[] | null` (REQUIRED on the server type, so `BASE_INPUTS` needed `goals: null`), the route's `loadGoals(now)` computes them (active `me_goals` × **all-time-latest** `account_snapshots` followers via `resolveGoals` — the plan's "latest account_snapshots", NOT the week-end value), `buildDigestFacts` passes through. The extension mirror `DigestFacts.goals?` is **optional** (cached-digest contract like `imageSpendUsd?`/`mediaVsText?` — digests cached before ME.5 lack the key). The digest narration prompt is UNCHANGED (facts-only rule already covers a new fact). **(d) Gate = canonical `bun run test` (815/0)** — bare `bun test` shows 2 `inspect.test.ts` fails (the documented D25d/RU.3 file-vs-`:memory:` trap: bare `bun test` opens the real `./stratus.db`, the canonical script sets `SQLITE_PATH=:memory:`); NOT a regression, and the 4 ME.5-touched test files are 75/0 either way. **Binds ME.6/ME.7:** gate with `bun run test`, don't panic at bare-`bun test` inspect reds.

- **D41** (ME.6): four things. **(a) Sha reconciliation** — ME.5's recorded `d611dce` was an amend-orphan (`git merge-base --is-ancestor d611dce HEAD` → false, `cat-file -t` still finds it, same subject "feat(me): playbook personal-context lift + digest goals fact (M1.5)" as HEAD); real ME.5 = **`9df22ff`** (= HEAD at ME.6 start). Corrected in ledger + `last-commit` + codemap header (sha-trap list) + §11. Same sha-stamp trap as D16–D40. **(b) PLAN + STATE tool-count both wrong — it's 17→19, not "16→18"/"17→18".** The ME plan's Task 6 said "16→18" (a base of 16); STATE's next-up said "17→18". Reality: the base was **17** (N.2's `x_niche`, D27d), and ME.6 adds **TWO** tools (`x_me` curated + `x_add_me_entry` write) → **19**. `mcp.test.ts` now asserts `names.size === 19` (was 17); the comment reads "3 schema + 12 curated (incl. x_niche, x_me) + 4 write (incl. x_add_me_entry)". **Binds ME.7:** the docs-sync must say 19 tools, not the plan's 18. **(c) Cross-test pollution → mandatory cleanup in `mcp.test.ts`.** bun runs every test file in ONE `:memory:` process; the `x_add_me_entry` round-trip inserts an active in-window `emotion` row into `me_entries`, and `me.test.ts:164` asserts an EMPTY profile renders a null context block — so the round-trip test **DELETEs its row via `app.request('/x/me/entries/:id', {method:'DELETE'})`** after the read-back. Only surfaces when `API_TOKEN` is set (the MCP tests are `describe.if(authed)`), which the canonical gate script does NOT set → `bun run test` = **815/0** without token, **817/0** with token. The existing `x_add_idea`/`x_add_person_note` writes don't need cleanup because no suite asserts `ideas`/`people` emptiness; `me_entries` is the one count-sensitive table. **Do NOT remove that DELETE.** **(d) Added the previously-missing `x_niche` to `scripts/smoke-mcp.ts`'s expected list** — N.2 registered `x_niche` in `mcp.ts`/`mcp.test.ts` but never added it to the smoke's `expected` array (a subset-presence check + `expected.length` log, so it was incomplete-not-failing). Corrected in the same edit since I was touching that exact array. **x_me** has an EMPTY inputSchema (no kind/active filters — mirrors `x_niche`/`x_followups`; the single-user profile is small, return it all); **no `x_add_me_goal`** by design (plan How: goals are deliberate tab decisions, a bad target steers every draft). **Binds nothing downstream except ME.7** (docs must reflect 19 tools).

- **D42** (ME.7): three things. **(a) Sha reconciliation** — ME.6's recorded `8686d79` was an amend-orphan (`git merge-base --is-ancestor 8686d79 HEAD` → false, `cat-file -t` still finds it, same subject "feat(me): x_me + x_add_me_entry MCP tools (M1.6)" as HEAD); real ME.6 = **`d4ef3dc`** (= HEAD at ME.7 start). Corrected in ledger + `last-commit` + codemap header (sha-trap list) + §11. Same sha-stamp trap as D16–D41; status accurate, only the sha stale. **(b) Docs-sync was a verification pass, with ONE catch-up.** §3.3/§3.4/§4/§5/§6/§9 codemap rows + migration `0016` + the 19-tool count were all already landed by the ME.1–ME.6 per-task updates, so ME.7 only added the §11 log line + header re-stamp there. The lone catch-up: **ME.5 shipped the "Personal context" Playbook code + UI section but never wrote the `docs/playbook-tab.md` prose** — ME.7 added it (after "Relationship lift", mirroring that section's shape) + "personal context" into the "Lift lines silent" states list. `docs/me-tab.md` (ME.4) was already complete. **(c) PLAN.md placement mirrors N.9's** — the plan checklist said "a line in the phased-build section", but the Niche N0 sibling put its pointer as a `> **…:**` blockquote in the Goal section (NOT the frozen Phase 1–10 list, which keeps the original 3-goal numbering); ME.7 followed that established precedent. **Binds nothing downstream** — the ME lane is closed. The plan's own tool-count "18" is superseded by **19** (D41b) in every doc written here.

- **D43** (AI.1): five things. **(a) Sha reconciliation** — ME.7's recorded `f010bf3` was an amend-orphan (`git merge-base --is-ancestor f010bf3 HEAD` → false, `cat-file -t` still finds it, same subject "docs(me): Me layer M1 docs-sync … (ME.7)" as HEAD); real ME.7 = **`1cd60d8`** (= HEAD at AI.1 start, parent `d4ef3dc` = ME.6). Reconciled per Step-0; no outside work landed. Same sha-stamp trap as D16–D42. **(b) `src/db/shared-schema.ts` touched though NOT in the edit list** — AI.1's "How" says "update the platform union comment"; there was NO such comment (bare `text('platform').notNull()`), so one was ADDED (`'x'|'grok'|'xai'|'openrouter'`). **Comment-only → NO migration** (drizzle ignores comments; `bun run db:generate` deliberately NOT run). Binds nothing; AI.2 REUSES the same file's existing `app_settings` (D1) — no table/migration. **(c) `fetchImpl` test seam added to `AskOpenRouterOptions` beyond askGrok's shape** — the task sanctions "injected/mocked fetch"; an injectable fetch (defaults to global `fetch`) is safer than monkeypatching `globalThis.fetch` in bun's shared-process runner. Optional → flows into AI.2's `askLLM` opts harmlessly (AI.2 constructs openrouter opts explicitly anyway). **(d) `DEFAULT_MODEL='anthropic/claude-sonnet-4.5'` in the client + `promptCacheKey` OMITTED** — the client default is a standalone/test fallback; AI.2's settings own the real default (same string) and always pass an explicit model. `promptCacheKey` has no OpenAI-compat field, so it's not in the options — AI.2's askLLM passes it only on the grok path. **(e) Watchdog is soft/log-only, sync `.get()` after the cost row lands** — mirrors `costTracker.checkBudget` but sync (matches the file's own `logCost` `.run()`); `OPENROUTER_DAILY_BUDGET_USD` default $1.00, `0` disables. **Binds AI.2** (its edit-list "add `appSettings` + migration `0013`" is stale — both shipped in UI.1; skip them per D1).

- **D44** (AI.2): five things. **(a) Sha reconciliation** — AI.1's recorded `fc5b3c4` was an amend-orphan (`git merge-base --is-ancestor fc5b3c4 HEAD` → false, `cat-file -t` still finds it, same subject "feat(llm): OpenRouter client with usage-accounted cost logging (AI.1)" as HEAD); real AI.1 = **`85e11e2`** (= HEAD at AI.2 start, parent `1cd60d8` = ME.7). Reconciled per Step-0; no outside work landed. Same sha-stamp trap as D16–D43. **(b) NO schema/migration — the plan's edit-list is stale (D1/D43e binding honored).** The plan's Task-2 edit list says "add `appSettings` table + migration `0013_*`" but UI.1 shipped both (`app_settings`, `0013_silent_nova`) and AI.1 added the `openrouter` platform-union comment; AI.2 REUSES the table, skips `db:generate`. `src/llm/settings.ts` reads/writes `app_settings` under key `'ai'` **directly** (NOT through the UI.1 `src/settings/store.ts` — that store is registry-bound; `resolveSetting`/`getAllValues`/`resetSettings` only touch keys in the X registry, so key `'ai'` is invisible to it — verified no collision). Read-through, **NO cache** (loadDoctrine discipline — a PATCH shows on the next `getAiSettings`); the UI.1 store's module-level cache may hold a stale `'ai'` entry but never surfaces it (not a registry key), so no invalidation needed. **(c) Added `src/llm/ask.test.ts` beyond the plan's edit-list** (it named only `settings.test.ts`+`routes.test.ts`) — Done-when #2 needs an askLLM dispatch assertion. The grok branch is proved "by shape" (`resolveProvider` returns grok + `LlmNotConfiguredError` fires for the resolved provider — askGrok has no fetch seam so no network dispatch test); the openrouter branch is end-to-end via the injected `fetchImpl` (asserts endpoint + response_format mapping + the opts>settings>defaults merge). **(d) `AskLlmResult.provider` added** (beyond the plan's "same as askGrok result") — informational, harmless; callers store `model` for the §7.19 measurement, `provider` is a convenience. **(e) `mountLlm` ALWAYS mounts** (unlike `mountGrok`'s `XAI_API_KEY` gate) — `/llm/settings` must be reachable to configure the provider before a key exists; `/llm/models` 503s at runtime without the openrouter key. **Binds AI.3/5/6:** import `askLLM`/`llmConfigured`/`llmProviderReady` from `src/llm/index.ts`; the §7.22 mount/503 gate flips `XAI_API_KEY`→`llmConfigured()` at AI.6; `askLLM(opts, {defaults})` is the ONLY new-Grok-call entry point (D5). **Binds nothing on the schema** — AI.3's `prompt_overrides` is the next migration (0017).

- **D45** (AI.3): six things. **(a) Sha reconciliation** — AI.2's recorded `87f3bd7` was an amend-orphan (`git merge-base --is-ancestor 87f3bd7 HEAD` → false, `cat-file -t` still finds it, same subject as HEAD); real AI.2 = **`3d3598c`** (= HEAD at AI.3 start, parent `85e11e2` = AI.1). Corrected in ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16–D44. **(b) `template` rides the opts bag / options object, NOT the plan's positional** — the plan wrote "`buildGrokInput(ctx, idea, template)`", but the current signature (post-N.4/ME.3) is `(ctx, override?, idea?, pillars?, opts?)`; `template?` joined the opts bag (`{replyPersona?; meBrief?…}` pattern, D38 precedent) and `BuildPostDraftOptions.template?` on the post side. **Precedence: per-request `systemPromptOverride` > registry template (DB override or default) > code constant** — the Decision-4 explicit>DB>default philosophy applied to prompt bodies; locked by a test. **(c) `PromptSpec.optional` added beyond the plan's `required`-only spec** — the niche placeholders (`{{REPLY_PERSONA}}` / `{{PERSONA}}`+`{{BELIEFS}}`) are KNOWN but droppable (token-tolerant substitution; an override hardcoding its persona is the D29d-sanctioned contract), so they must neither be required nor flag as unknown. Also **post has FIVE required placeholders** (`{{PILLARS}}/{{MY_WINNERS}}/{{REMIX}}/{{PILLAR}}/{{IDEA}}`), not the plan's "four" — the count predates §8.6/N.3; the render is split/join (a missing token silently drops that content), so all five content placeholders are required. **Binds AI.4** (PATCH validation surfaces these sets) and **AI.5** (append later keys with both sets). **(d) `loadPromptSafe` added** (loadActiveNicheSafe/§7.8 pattern — a prompt-layer DB failure degrades to the shipped default, never 500s a draft); both routes use the safe variant. **(e) Cache key COMPOSES with the niche suffix** — the plan's `sha256(body)` key alone is insufficient post-N.3/N.4 (persona substitutes IN the template, so the rendered prefix varies by niche too): `promptCacheKey = ${prompt.cacheKey}:${niche.slug}:${niche.updatedAt?.getTime() ?? 0}`. The static `PROMPT_CACHE_KEY` consts in replies.ts/drafter.ts are GONE; `BATCH_PROMPT_CACHE_KEY` survives until AI.5. **(f) `GET /x/replies/default-prompt` now returns `loadPromptSafe('reply').body`** — the plan's Design listed this under Task 4's realm, but it's a one-liner in a file AI.3 already edits and makes the override observable; AI.4's lifecycle test can assert through it. Migration is **`0017_misty_zeigeist.sql`**, not the plan's "0013" (0013–0016 taken; single CREATE TABLE, no seed by design — Decision 1).

- **D46** (AI.4): four things. **(a) Sha reconciliation** — AI.3's recorded `9f33fb2` was an amend-orphan (`git merge-base --is-ancestor 9f33fb2 HEAD` → false, `cat-file -t` still finds it, same subject "feat(prompts): DB overrides + registry, wired into reply/post drafting (AI.3)" as HEAD); real AI.3 = **`a087a66`** (= HEAD at AI.4 start, parent `3d3598c` = AI.2). Corrected in ledger + `last-commit` + codemap header (sha-trap list) + §11. Same sha-stamp trap as D16–D45. **(b) Router mounts on a bare Hono in tests + the bearer 401 is deliberately NOT duplicated** — the `/x` bearer middleware is a shared app.ts-level guard (app.test/mcp.test cover it), so `prompts.test.ts` mounts `promptsRouter` on a bare Hono like niche.test/channels.test. The plan's Task-4 test list named "401 without bearer"; skipped for the same reason N.2 skipped it (D27b) — no value in a third copy. **(c) Reset/restore responses chosen (plan left them unspecified):** `POST /prompts/:key/reset` → `{customized:false}` (idempotent — deleting a non-existent override is a no-op, still 200); `POST /prompts/restore-defaults` → `{restored:n}` (count via a pre-delete `select().all().length`, driver-agnostic). PATCH cap uses `TextEncoder().encode(body).length > 32*1024` (bytes, not UTF-16 length) → 413 `body_too_large` with `maxBytes`. Empty/whitespace/absent `body` → 400 `invalid_body_field` (before the size + placeholder checks). **(d) `restore-defaults` static path registered BEFORE `:key/reset`** in the router for clarity, though they can't collide (1-segment `restore-defaults` vs 2-segment `:key/reset`, and `GET /prompts/:key` with key=`restore-defaults` → 404 via `isPromptKey`). No schema/migration (`prompt_overrides` from AI.3's `0017`). **Binds nothing downstream** — AI.5 appends registry keys (they auto-appear in `GET /prompts` since the list maps `PROMPT_KEYS`); AI.10/AI.11 build the Settings editor UI against these routes.

- **D47** (AI.5): six things. **(a) Sha reconciliation** — AI.4's recorded `466b090` was an amend-orphan (`git merge-base --is-ancestor 466b090 HEAD` → false, `cat-file -t` still finds it, same subject as HEAD); real AI.4 = **`3c649ce`** (= HEAD at AI.5 start, parent `a087a66` = AI.3). Corrected in ledger + `last-commit` + codemap header + §11. Same sha-stamp trap as D16–D46. **(b) `drafter.ts` migrated to askLLM though NOT in the plan's Task-5 edit list** — an audit of Tasks 5–13 shows NO task migrates the post drafter's `askGrok` call (Task 6 only flips its mount gate; Task 7 adds a sibling route), yet the plan's Why-section promises "any draft surface can run on Claude/GPT/Gemini" and AI.6's gate flip would strand OpenRouter-only setups with a 502 on `/posts/draft`. Migrated here (parseCommon gains `provider`, house defaults via the defaults tier, `llmErrorPayload` catch) — mechanical, same shape as replies. Logged in the Planning-error log too. **(c) New pure module `src/x/voice/extractPrompt.ts` beyond the edit list** — the plan said "render via registry" for voiceExtract, but the registry importing `routes/voiceExtract.ts` for `EXTRACT_PROMPT_TEMPLATE` while that route imports the registry is a real TDZ cycle (PROMPT_SPECS initializes at module load). Template/schema/parse/`TEMPLATE_EXTRACT_MAX_OUTPUT_TOKENS` moved to the pure module; importers (playbook.ts, test.test.ts) updated; NO legacy re-exports left in voiceExtract.ts. **Binds AI.6+:** registry defaults must come from PURE modules — check digest.ts/icebreakers.ts don't import the registry before wiring their keys. **(d) `provider` body param scope** — added to replies generate + generate-batch, drafter draft + reup (via parseCommon), pillars/draft, and playbook/extract-winners; deliberately NOT to voiceExtract's two routes (their bodies carry no LLM params today — extraction is a backfill operation; the DB AI setting decides). The plan's "keep accepting model/reasoningEffort, add provider" is satisfied where those params exist. **(e) Shared `llmErrorPayload` in `src/llm/ask.ts`** (+ index re-export) instead of per-route error branches — `LlmNotConfiguredError`→503 `llm_not_configured`, `GrokApiError`→`grok_upstream_error` (contract unchanged), `OpenRouterApiError`→`openrouter_upstream_error` (new, same shape), else null (routes keep their generic `generate_failed`/`draft_failed` branch). extractOne/extract-winners failures classify to strings (`llm_not_configured`/`grok_<status>`/`openrouter_<status>`) instead. **(f) reasoningEffort semantics changed at the migrated routes** — the route-level `DEFAULT_REASONING` fallback moved from "always passed in the call" to the askLLM defaults tier, so a stored AI-settings `reasoningEffort` (or temperature/maxOutputTokens) now applies when the body doesn't override — this IS Decision 4's intended precedence, but note the batch's computed `maxOutputTokens=min(9000,200+n*420)` is also a defaults-tier value: a stored global maxOutputTokens overrides it and can truncate large batches (comment at the call site; clear the setting if batches truncate).

- **D48** (AI.6): seven things. **(a) Sha reconciliation** — AI.5's recorded `7d0a786` was an amend-orphan (`git merge-base --is-ancestor 7d0a786 HEAD` → false, `cat-file -t` still finds it, same subject "refactor(prompts): reply-batch/voice-extract/pillar-draft via registry + askLLM (AI.5)" as HEAD); real AI.5 = **`a579e75`** (= HEAD at AI.6 start, parent `3c649ce` = AI.4). Reconciled per Step-0; no outside work landed. Same sha-stamp trap as D16–D47. **(b) String-rename scope is DELIBERATELY narrow — only digest's degrade field.** The plan's parenthetical named ONLY "digest's degrade string `grok_not_configured` → `llm_not_configured`"; the icebreaker/pillars-draft/extract-winners **pre-flight 503s KEEP `grok_not_configured`** (flip the `!process.env.XAI_API_KEY` condition → `!llmConfigured()` but NOT the string). Reason: those strings are stable error codes the extension maps to messages (`Pillars.tsx`/`Studio.tsx`/`Niche.tsx` matchers) and their tests assert them (`icebreakers.test.ts:59`, `playbook.test.ts`); the plan renamed only digest because its `narrativeError` is prose the panel renders. `Digest.tsx:64` matcher + message + `digest.test.ts:97` updated to `llm_not_configured` in the same commit (plan's "update the matcher" branch, chosen over "emit both"). **(c) Digest catch classifies to STRINGS (D47e pattern), NOT `llmErrorPayload`** — digest degrades to facts (never a 5xx), so it needs a string not an HTTP payload: `LlmNotConfiguredError`→`llm_not_configured` / `GrokApiError`→`grok_<status>` / `OpenRouterApiError`→`openrouter_<status>` / else `llm_failed` (was `grok_failed`); the parse-null path renamed `grok_parse_error`→`llm_parse_error`. Icebreaker uses `llmErrorPayload` (it returns a 502/503) and **KEEPS its own `grok_parse_error`** at line 613 (untested, no consumer — left untouched, minimal). **(d) BOTH env keys unset in three no-key tests** — the gate is `llmConfigured()` (checks XAI OR OR) now, so `digest.test.ts` (plan-named), `icebreakers.test.ts` (plan-named), AND `playbook.test.ts` extract-winners (NOT plan-named but a required consequence — with only XAI deleted, an OR-configured dev machine would let the pre-flight pass and reach askLLM) all save+unset+restore `OPENROUTER_API_KEY` alongside `XAI_API_KEY`. **(e) reasoningEffort/maxOutputTokens/temperature ride the `defaults` tier** for digest (low/700/0.7) + icebreaker (low/400/0.7), matching D47f — a stored global AI setting may now override them (Decision 4 intended precedence). No `provider` body param added to either route (digest is GET, icebreaker is handle-only POST — provider comes from the DB AI setting via askLLM). **(f) `promptCacheKey` now `prompt.cacheKey`** (the registry per-body sha bucket) — the static `DIGEST_CACHE_KEY = 'stratus-digest'` const deleted; icebreaker's `'stratus-icebreaker'` literal replaced. One-time xAI cache miss on deploy, by design (AI.3 pattern). No niche suffix (neither prompt is persona-based). **(g) `niche.ts` wizard + `images.ts` NOT flipped** — the niche wizard (`POST /x/niche/draft`) still uses `askGrok` + `process.env.XAI_API_KEY` (never migrated to askLLM at AI.5, out of Task 6 scope; a future task owns it); `images.ts` stays xAI-only (Decision 6). Both keep `grok_not_configured` correctly. **Binds AI.7+** (thread drafter): a new LLM surface uses `askLLM`+`loadPromptSafe`+`llmConfigured()`+`llmErrorPayload`, registers its prompt key (D5), and its no-key test must unset BOTH keys.
- **D49** (AI.7): four things. **(a) Sha reconciliation** — AI.6's recorded `b82f4ef` was an amend-orphan (`git merge-base --is-ancestor b82f4ef HEAD` → false, `cat-file -t` still finds it, same subject "refactor(prompts): digest/icebreaker via registry; XAI gate becomes LLM gate (AI.6)" as HEAD); real AI.6 = **`e2c91ac`** (= HEAD at AI.7 start, parent `a579e75` = AI.5). Corrected in ledger + `last-commit` + codemap header (sha-trap list) + §11. Same sha-stamp trap as D16–D48. **(b) Thread schema drops the plan's `minItems 3 / maxItems 9`** — the plan's Task-7 "How" specified array bounds, but strict xAI/OpenRouter structured-outputs + array `minItems`/`maxItems` is a documented rejection risk (N.8's `NICHE_WIZARD_SCHEMA` gotcha, RU.3's `REPLY_VARIANTS_SCHEMA` gotcha, and `buildPostDraftsSchema` all deliberately omit bounds). `buildThreadDraftSchema`'s `tweets` array is unbounded; the 4–8 count is steered by the prompt job section and floored (≥2) in `parseThreadDraft` (→ null below 2). **(c) No `{{PILLAR}}` token in the thread prompt** — the post prompt conveys a user-chosen pillar via a separate `<pillar>{{PILLAR}}</pillar>` tail token; the thread prompt's required set is only `{{PILLARS}}/{{FEW_SHOT}}/{{IDEA}}` (per the plan's Design table), so a user-chosen pillar + tweetCount fold into the single `{{IDEA}}` steer via `renderSteer` (`Serve the "X" content pillar.` / `Write exactly N tweets.` prepended). Soft steering like the post prompt — the schema enum still carries all active slugs; `parseThreadDraft` validates the returned pillar against them (bad → null). **(d) `THREAD_SCHEMA`→`buildThreadDraftSchema(slugs)`** — the plan named a static `THREAD_SCHEMA`, but the pillar enum is the live active-slug set (dynamic), so it's built per-call like `buildPostDraftsSchema`. **Binds AI.8/AI.9** (rewrite/ideas): they share the `parseCommon` provider/model validation shape and the registry-key-from-a-pure-module rule; AI.8 shares `Composer.tsx` with AI.7 (serialize).
- **D50** (AI.7 sha, recorded at AI.8): AI.7's recorded `995a08b` was an amend-orphan (`git merge-base --is-ancestor 995a08b HEAD` → false, `cat-file -t` still finds it, same subject "feat(threads): AI thread drafter — prompt, route, Composer button (AI.7)" as HEAD) — real AI.7 = **`b932a06`** (= HEAD at AI.8 start, parent `e2c91ac` = AI.6). Corrected in ledger + `last-commit` + codemap header (sha-trap list) + §11. Same sha-stamp trap as D16–D49; status accurate, only the sha stale.
- **D51** (AI.8): four things. **(a) NO `.md`/byte-sync for `rewritePrompt.ts`** — Decision 9 keeps the `.md`↔TS byte-sync convention only for the three big authoring prompts (post/reply/thread); the six smaller templates (rewrite + ideas/voice-extract/pillar-draft/digest/icebreaker) live as TS constants and their editing surface is the DB prompt editor. So `src/x/posts/rewritePrompt.ts` has NO sibling `.md` and NO byte-sync test (unlike AI.7's thread). **(b) `MAX_VARIANT_LENGTH=560` variant cap** (plan-specified) — a rewrite variant over 560 chars is DROPPED by `parseRewrite`, not truncated (a wall of text signals the model changed substance). Tension worth revisiting: the prompt allows "longer than 280 if the draft itself is longer", but a long-form draft's legit rewrite could exceed 560 and get dropped — an opening choice from the plan, revisit if long-form drafts lose all variants and 502. **(c) NO one-auto-regenerate** — the thread route burns one retry on an over-280 segment, but the plan's rewrite spec says only "0 survivors → 502" (no retry). Kept minimal; `parseRewrite` null → 502 `rewrite_parse_error`, `[]` → 502 `rewrite_invalid`. **(d) The rewrite route uses INLINE body validation, not `parseCommon`** — `parseCommon` is pillar/idea/ideaId/tweetId-shaped (post/thread bodies); rewrite's body is `{text, instruction, model, provider}` (different fields), so it validates inline (same `invalid_provider`/`invalid_model` codes, `invalid_text` [1–2000], `invalid_instruction` [≤500]). No `reasoningEffort` in the rewrite body (plan's signature omits it — the DB AI setting still decides via askLLM's defaults tier). Cache key = bare `prompt.cacheKey` (no niche suffix — the rewrite prompt has no persona token, so a niche edit can't change its render). **Binds AI.9** (idea generator): same TS-only-no-.md rule, same inline-or-parseCommon body-validation shape, same registry-key-from-pure-module rule; but AI.9 lands on the ideas router (`src/x/routes/ideas.ts`), reuses `topWinners()`/`getActivePillars()`, and should mirror the `no_pillars_for_niche` 409 refuse-before-spend.
- **D52** (AI.9): five things. **(a) Sha reconciliation** — AI.8's recorded `b9fe98d` was an amend-orphan (`git merge-base --is-ancestor b9fe98d HEAD` → false, `cat-file -t` still finds it, same subject "feat(drafter): rewrite assist — prompt, route, Composer variants UI (AI.8)" as HEAD); real AI.8 = **`7a5fe9e`** (= HEAD at AI.9 start, parent `b932a06` = AI.7). Corrected in ledger + `last-commit` + codemap header (sha-trap list) + §11. Same sha-stamp trap as D16–D51. **(b) `ideas.ts` ↔ `drafter.ts` is a deliberate two-way import cycle, and it's SAFE** — `ideas.ts` imports `topWinners` from `drafter.ts` (reuse the drafter's few-shot loader, per the plan/STATE), while `drafter.ts` already imports `consumeIdeaSafe` from `ideas.ts`. Both references are DEFERRED to request time (function calls inside handlers, never module-top-level), so ESM live-bindings resolve them after both modules finish evaluating — no TDZ. Do NOT "fix" this by inlining a winners query (the plan mandates reuse) or by extracting `topWinners` to a third module (unnecessary — the cycle is inert). **(c) `IDEAS_SCHEMA.pillar` is a FREE string, NOT enum-bound to the live slugs** — unlike `buildThreadDraftSchema`/`buildPostDraftsSchema` (dynamic pillar enum), the ideas schema is STATIC and `parseIdeaProposals` NULLS an out-of-set pillar rather than the model being forced to pick a valid one. Reason: the plan's "How" says "proposals with a pillar outside the active slug set get pillar nulled, not dropped" — an idea is a loose seed; a mis-tagged pillar shouldn't force-drop the idea. So the schema stays static (like `REWRITE_SCHEMA`) and the route passes `slugs` only to `parseIdeaProposals` for the null-check. **(d) No niche cache-key suffix + always-mounted router (no mount-gate flip)** — the ideas prompt carries NO persona/beliefs token (pillars sit at the variable tail), so the cacheable prefix is niche-independent → `promptCacheKey = prompt.cacheKey` (bare, like rewrite). `ideasRouter` was ALREADY always-mounted (C6), so AI.9 adds NO §7.22 mount-gate change — the runtime 503 comes purely from `askLLM` throwing `LlmNotConfiguredError` → `llmErrorPayload` (like the draft-thread route, no explicit `llmConfigured()` pre-check). The `count` body param folds into the `{{STEER}}` block (`Return exactly N ideas.`) like threadPrompt folds `tweetCount`, and `parseIdeaProposals` clamps the output to `count`. **(e) Route order** — `POST /ideas/generate` registered after `POST /ideas` (distinct static paths, no trie collision; the PATCH/DELETE `:id` param routes are a different method so no shadow). **Binds AI.12** (model-effectiveness cell reads `reply_drafts.model`/the drafter's `model` — the ideas route returns `model` in its response but writes no row, so it contributes nothing to that cell — a generated idea's eventual post is measured only after the human saves+schedules it). **Binds nothing else** — the AI backend is complete; AI.10–AI.13 are extension UI + docs.
- **D54** (AI.11): four things. **(a) Sha reconciliation** — AI.10's recorded `a91387f` was an amend-orphan (`git merge-base --is-ancestor a91387f HEAD` → false, `cat-file -t` still finds it, same subject "feat(extension): Settings AI subtab + restore default prompts (AI.10)" as HEAD); real AI.10 = **`9be4b0a`** (= HEAD at AI.11 start, parent `bc49503` = AI.9). Same sha-stamp trap as D16–D53. **(b) Fixed a pre-existing AI.8 bug NOT in AI.11's scope** — `RewriteVariant` (defined in `shared/types.ts`, imported by `Composer.tsx` from `./api.ts` since AI.8) was never re-exported from `api.ts`, so `cd extension && bun run typecheck` (`tsc --build --noEmit`) was RED from AI.8 onward. It hid because the mandated extension gate is `bun run build` (vite/esbuild strips types without checking) — the root `bun run typecheck` (tsc over `src/`) never sees extension files. Since AI.11 already edits api.ts's import + re-export blocks (adding the prompt types), I added `RewriteVariant` to both there — the correct one-line fix, unblocks the extension typecheck for AI.11 and every later extension task. Verified it's genuinely pre-existing: committed `api.ts` has 0 `RewriteVariant`, committed `Composer.tsx` imports it. **(c) File count** — the plan says "2 files" (`Prompts.tsx` + `api.ts`), but I touched 4: `shared/types.ts` (the 4 prompt response types belong there — every prior AI type lives in shared/types.ts, the AI.10/D53 precedent) and `Settings.tsx` (swap the `<PromptsTab>` placeholder for `<PromptsPanel>` — the next-up hint explicitly directs this; without it the panel is unreachable), plus `styles.css` for the `.prompt-*` block. No new tests (the `missingPlaceholders` `includes` mirror is inline, not extracted — the task said extract only if reused). **(d) Editor UX choices** — Save is disabled while any required placeholder is missing (client mirror of `validatePromptBody`) so the server's `missing_placeholder` 400 is a backstop, not the primary UX; `api.prompts.patch` returns only `{customized, unknownPlaceholders}` (no body/updatedAt), so after Save I set `customized:true` locally and refetch the list on Back to keep the chip + edited-at accurate; Reset reloads (`GET /x/prompts/:key`) to pull the shipped default text back into the textarea. **Binds AI.12/AI.13:** the AI extension UI is now complete through the editor; AI.13 docs-sync should mention the Prompts editor + the D54 RewriteVariant fix.
- **D55** (AI.12): two things. **(a) Sha reconciliation** — AI.11's recorded `e7d5be9` was an amend-orphan (`git merge-base --is-ancestor e7d5be9 HEAD` → false, `cat-file -t` still finds it, same subject "feat(extension): prompts editor panel (AI.11)" as HEAD); real AI.11 = **`a8316d6`** (= HEAD at AI.12 start, parent `9be4b0a` = AI.10). Same sha-stamp trap as D16–D54. **(b) File count** — the plan says "4 files" (`playbook.ts`, `routes/playbook.ts`, `Playbook.tsx`, the two test files), but I touched 7: also `shared/types.ts` (`PlaybookModelCell` + `Playbook.modelEffectiveness` — the extension type plumbing every prior playbook cell needs) and `api.ts` (re-export `PlaybookModelCell`, the `PlaybookLatencyCell` precedent). Not a design deviation — the plan's "4 files" undercounts the extension type wiring exactly as the AI.10/D53 and AI.11/D54 precedents. **(c) Design fidelity to the exemplar** — `buildModelEffectiveness` is the S0.5 `buildLatencyEffectiveness` copied EXACTLY minus the parts the plan says to drop: buckets are dynamic (a `Map` over model strings, not `LATENCY_BUCKETS.filter`), sorted most-sampled-first (`b.n - a.n || b.posted - a.posted || model.localeCompare` — latency's fixed chronological order has no analogue), and there is NO `early`/`late` pooling and NO `viewsLift`/`profileVisitsLift` (no canonical baseline pair — buckets only). `totalMeasured` kept for the partition-invariant test. **Binds AI.13:** the AI-layer docs-sync should list this model-effectiveness cell alongside the other AI surfaces.
- **D56** (AI.13): four things. **(a) Sha reconciliation** — AI.12's recorded `9c59183` was an amend-orphan (`git merge-base --is-ancestor 9c59183 HEAD` → false, `cat-file -t` still finds it, same subject "feat(playbook): model effectiveness cell over reply outcomes (AI.12)" as HEAD); real AI.12 = **`852dcac`** (= HEAD at AI.13 start, parent `a8316d6` = AI.11). Same sha-stamp trap as D16–D55. **(b) Smoke tests routers in-process against the REAL DB, with snapshot/restore** — `scripts/smoke-ai-layer.ts` mounts `llm`/`promptsRouter`/`drafter`/`ideasRouter` on a bare Hono (the drafter/replies routers are `llmConfigured()`-gated in `mountX`, but a direct `app.route()` mounts them unconditionally, which is what lets the smoke test the 503 path). Because `POST /x/prompts/restore-defaults` DELETEs every `prompt_overrides` row and the settings test writes `app_settings` 'ai', the smoke **snapshots the 'ai' row + ALL `prompt_overrides` up front and restores them on exit** — a real customization is never wiped (verified pristine after a run). The no-key 503 and openrouter-409 paths **unset `process.env` keys in-process** (`Reflect.deleteProperty`) and restore in `cleanup()`/inline — `askLLM` reads env at call time, so no network/no spend. **(c) draft-thread/ideas 503-OR-409 tolerance** — with keys unset, `rewrite` (no pillar dep) definitively 503s, but `draft-thread`/`ideas` check `getActivePillars()` first, so on a DB whose active niche has zero pillars they 409 `no_pillars_for_niche` instead; both are $0 refuse-before-spend, so the smoke accepts either (on the current builder-niche DB it observed 503). **(d) Script-lint gotcha** — see the next-up note (c): biome's `noDelete`/`noExplicitAny` apply to `scripts/` (not test files); use `Reflect.deleteProperty` + the `biome-ignore` precedent, and annotate `.map`/`.find` params concretely so tsc's `noImplicitAny` doesn't fire against the `any`-typed `req` body. **Binds Wave 2+:** none — the AI layer is complete; future smoke scripts that mutate shared singleton tables (`app_settings`, `prompt_overrides`) should copy this snapshot/restore discipline.
- **D50** (AI.7, testing): the `POST /x/posts/draft-thread` route tests live in a NEW `src/x/routes/drafter.test.ts` (the drafter had no route test before — only app.test.ts pre-DB guards). It mounts `drafter`+`calendar` on a bare Hono (no bearer — shared `/x` middleware, prompts.test discipline). The pre-spend guards (`invalid_tweet_count`, `invalid_provider`) need no LLM key; the **no-key 503 test force-unsets BOTH `XAI_API_KEY`+`OPENROUTER_API_KEY`** (`process.env.X = ''`, restored in a `finally`) so it never spends even on a keyed dev machine (N.8 wizard discipline). `insertThreadDraft` is tested directly (no LLM call). **prompts.test.ts changed:** its three "unknown key → 404" fixtures used `thread` as the unknown slug — now `thread` is a valid PROMPT_KEY, so they were swapped to `not-a-prompt`. Any future test using a fake prompt key must use a slug NOT in `PROMPT_KEYS` (rewrite/ideas will be added soon).
- **D57** (RU.4, Wave 2 opener): five things. **(a) Out-of-skill work reconciliation** — at RU.4 start HEAD was `3fc2bbc` ("fixes mici"), 6 commits past the real AI.13 `f003e8f` (STATE's recorded `f86e06a` was an amend-orphan, GC'd — `cat-file -t` MISSING, not just non-ancestor). The 6 outside commits: `b920507` (Wave-1 verification pass — the `Replies.tsx` `<dialog>` a11y + `inspect.ts`/`client.ts` `:memory:`-under-test fix STATE already references), a **metrics-backfill feature** (`d376ffc`+`aff3bb9`+`dc58567` — new `src/x/backfill.ts` + `POST /x/posts/backfill` in-process single-tweet ingest, `dailyMetrics` discovery-checkpoint decouple, `scripts/backfill-tweet.ts`/`stratus_claude_mcp.sh`), evals docs `cbeecf1`, and two Studio-template tweaks `6bce793`+`3fc2bbc` (`templates.ts`/`fields.tsx`/`registry.ts` + `docs/assets/banner-billboard.png`). **None touch RU.4's files; ledger has no status drift** (all done tasks in HEAD ancestry). The codemap §3.4/§3.3 are now slightly stale re: the backfill route/file — NOT updated here (outside-author's scope; kept the RU.4 commit focused) but flagged in §11 + this entry so the next session isn't surprised. **(b) `bun install` was mandatory** — `@modelcontextprotocol/sdk`+`zod` (root) and `happy-dom` (extension) are declared in package.json but were UNINSTALLED in this env; a fresh `bun run test` had 4 pre-existing dep-resolution fails (`Cannot find module '@modelcontextprotocol/sdk/...'`, `Cannot find package 'happy-dom'`, + a cascade `GET /x/settings 401` because app.ts imports the failing mcp.ts) — identical on clean HEAD (stash-verified). After `bun install` (root + extension): 964/0. **Any session hitting these MCP/happy-dom "Cannot find" fails should `bun install` first, not assume a regression.** **(c) Reused the strict `ReplyVariant` type, not the plan's inline `{text,angle:string}[]`** — `extension/src/shared/types.ts` already exports `ReplyVariant {text, angle: ReplyAngle}` (the strict union, matching `BatchReplyItem.angle`); imported it into radar.ts/messages.ts/background.ts. No circular import: types.ts imports ONLY `../replyBand.ts`. **(d) `RadarDraftRow.variants` is required-nullable (`ReplyVariant[] | null`), not optional** — matches the server row shape (`GET /x/radar/drafts` does `db.select()`, so the RU.2 `variants` column always rides on the wire as null-or-array), consistent with the sibling `url`/`band`/`signals` required-nullable fields; the test fixture gained `variants: null` and `draftRowToSighting` skips the key when null/empty so the pre-existing `toEqual` assertion stays green. **(e) Scope held to variants ONLY** — the `manual` band widening (band type stays `'hot'|'warm'`), `draftId`, and the confirm flow are explicitly RU.5/RU.6/RU.8, NOT RU.4. **Binds RU.5/RU.6/RU.7:** RU.7's on-page picker reads `RadarSighting.variants` from the buffer; RU.5's confirm endpoint reconstructs `[{text: replyText, angle}]` for pre-variant rows (the `draftRowToSighting` null-variants path is the same "unknown" semantics).

- **D58** (RU.5): five things. **(a) Sha reconciliation** — RU.4's recorded `a3664af` was an amend-orphan (`git merge-base --is-ancestor a3664af HEAD` → false, `cat-file -t` still finds it, same "(RU.4)" subject as HEAD); real RU.4 = **`22cf0b1`** (= HEAD at RU.5 start). Corrected in the ledger + `last-commit` + codemap header/§11. Same sha-stamp trap as D16–D57; **no out-of-skill work landed between RU.4 and RU.5** (HEAD *was* RU.4), ledger no drift. **(b) `GET /radar/drafts?tweetId=` default is non-expired, beyond the Task-edit's terse "combinable with status"** — the Task-5 edit line only said the filter is "combinable with status", but the Design says it "returns that tweet's non-expired rows"; so status handling now branches: an explicit `status` query wins (`eq`), else a `tweetId` query returns the tweet's whole non-expired history (`ne(status,'expired')`), else the bare list keeps its `ready` default. Backward-compatible (the no-tweetId path is unchanged; smoke-radar-drafts passes explicit statuses). Rationale: RU.7's on-page variants-get server fallback (`GET …?tweetId=`) must find a **confirmed (clicked)** tweet's variants after a deep-link, which a ready-only default would miss. **(c) HTTP codes: 201 fresh-create / 200 idempotent** — the plan specified `200` only for the idempotent (already-linked) return; I return **201** for a fresh `reply_drafts` creation (a resource was created) and 200 for the idempotent path. The route test asserts both. **(d) `contextSnapshot.signals.band` merges the `band` COLUMN with the `signals` JSON** — `radar_drafts.signals` is a `TweetSignals` (no `band` field; `band` lives in its own column), so the confirm builds `PostSignals = {band: row.band, ...signals}` (band cast to `PostSignals['band']`, preserving a future `'manual'` verbatim). When `signals` is null (CLI rows): NO `signals` key on the snapshot (conditional spread for `exactOptionalPropertyTypes`), metrics zeros, `sourcePostedAt: null`, `contextSnapshot.postedAt = draftedAt` ISO (best-effort — only ever read as a last-resort age fallback by `scoreReplyOutcome`; `resolveAgeMin`/`buildReplyOutcomes` handle null signals natively). **(e) `copied → posted` already exists in `ALLOWED_TRANSITIONS`** (replies.ts:86) — the created row is PATCHable to posted (Done-when #1) with **no replies.ts change** in RU.5. **Binds RU.6** (panel click→confirm wiring): the confirm returns the full `reply_drafts` row; stamp its `id` as the sighting's `draftId`. **Binds RU.7** (paste→posted): `variants-get` fallback maps `row.variants ?? [{text: replyText, angle}]`; `variant-pasted` POSTs confirm idempotently (covers deep links) then PATCHes `/replies/:draftId {status:'posted', replyTextEdited: text≠primary ? text : undefined}`. **Binds RU.9** (`classifyReplyOrigin`): confirmed radar rows carry `source='radar'` for the exact batch-vs-single split. No new hot-file lock held (radar.ts wasn't in the table).
- **D59** (RU.6): **Sha reconciliation** — RU.5's recorded `5935107` was an amend-orphan (`git merge-base --is-ancestor 5935107 HEAD` → false, `cat-file -t` still finds it, same "(RU.5)" subject as HEAD); real RU.5 = **`983fbea`** (= HEAD at RU.6 start). Corrected in the ledger + `last-commit` + codemap header/§11. Same sha-stamp trap as D16–D58; **no out-of-skill work landed between RU.5 and RU.6** (HEAD *was* RU.5), ledger no drift. **No plan divergence otherwise** — RU.6 followed Task 6 exactly (new `radar-confirm` message + guard, `RadarSighting.draftId?` surviving merges, background `confirmDraft`→`stampDraftId`, `Radar.tsx onOpen` fires it). The only additive choice: created a NEW `extension/src/shared/messages.test.ts` for the guard test (the plan said "message guard unit test"; no messages.test.ts existed — this is the first). No new hot-file lock held (all four RU.6 files were free).
- **D60** (AX.1): **Sha reconciliation** — RU.6's recorded `6261673` was an amend-orphan (`git merge-base --is-ancestor 6261673 HEAD` → false, `cat-file -t` still finds it, same "(RU.6)" subject as HEAD); real RU.6 = **`1ed57bd`** (= HEAD at AX.1 start). Corrected in the ledger + `last-commit` + codemap header/§11. Same sha-stamp trap as D16–D59; **no out-of-skill work landed between RU.6 and AX.1** (HEAD *was* RU.6, the 3 prior commits are RU.5/RU.4/`3fc2bbc`[D57]), ledger no drift. **No plan divergence otherwise** — AX.1 followed Task 1 exactly (`GET /people/glance` after rankmap/before `:handle`, three-query map, target backfill, ISO dates, route describe + smoke). Additive choices, all in-spec: (a) a local non-exported `GlanceEntry` interface in `people.ts` for map typing (the test/extension keep their own copies — the rankmap test's inline-type precedent); (b) `isNotNull(mentions.authorUsername)` on the open-loop query so a null-author mention can't create a `null`-keyed group (defensive — mentions always carry authorUsername from includes.users); (c) backfilled targets use `openLoops[h] ?? 0` (spec said literal `0`, but a handle with an unanswered mention always has a people row so it reduces to 0 — the `?? 0` is strictly more correct, never wrong); (d) glance's route-test `beforeAll` seeds its OWN `accountSnapshots` row (1000 followers, band 2000–10000) so it doesn't depend on rankmap's describe ordering — both are 1000 so the band is identical regardless. No new hot-file lock held (`src/x/routes/people.ts` wasn't in the table; AX.2 touches `extension/src/shared/glance.ts`, not people.ts, so nothing to hand off). **Binds AX.2:** the `GlanceEntry` shape is now fixed as the client-type contract — `{stage, isTarget, openLoops, lastOutboundAt:string|null, lastInboundAt:string|null, followersCount:number|null}`, map keyed lowercased.
- **D62** (AX.4): **Sha reconciliation** — AX.2's recorded `e29b629` was an amend-orphan (`git merge-base --is-ancestor e29b629 HEAD` → false, same "(AX.2)" subject `feat(extension): glance chip view-model core` as HEAD); real AX.2 = **`6c46a56`** (= HEAD at AX.4 start). Corrected in the ledger + `last-commit` + codemap header/§11. Same sha-stamp trap as D16–D61; **no out-of-skill work landed between AX.2 and AX.4** (HEAD *was* AX.2), ledger no drift.
- **D63** (AX.4): **Two in-spec design choices in `buildTweetContextModel`, both forced by what the dossier route actually emits** (the plan's model text is intent; current code is ground truth). (a) **Relationship inbound/outbound COUNTS from `events`** (their_mention+their_reply_to_me vs my_reply — the plan's explicit sourcing), but **last-exchange DATES from the `people` watermarks `lastInboundAt`/`lastOutboundAt`**, not from scanning events — the model field names ARE the watermark column names, the watermarks are authoritative + null-safe, and the dossier caps `events` at `MAX_EVENTS=500` so watermarks can't be missed by a cap. The plan's "counts from events, last dates" reads either way; this is the cleaner reading. (b) **`outcomes[].angle` is OPTIONAL and reads null in production** — the dossier route's `buildReplyOutcomes` has **no per-outcome angle field** (angle is computed internally only for the aggregate `angles` crosstab, never attached to an outcome). Task 4 is view-model-only + parallel-ok (must NOT touch the route), so the type is `angle?: string|null` read `?? null`; the fixture exercises the passthrough for forward-compat (a later route enhancement, e.g. RU.9-adjacent, could attach it). **Neither affects AX.5** — AX.5 is DOM plumbing over this exact model; if per-outcome angle is ever wanted on the panel, adding it to the dossier `outcomes` map + the `DossierOutcome` type flows through with zero view-model change. No new hot-file lock (both files are new, extension-only).
- **D64** (AX.3): **Sha reconciliation** — AX.4's recorded `7abec86` was an amend-orphan (`git merge-base --is-ancestor 7abec86 HEAD` → false, same "(AX.4)" subject as HEAD); real AX.4 = **`7ecb8df`** (= HEAD at AX.3 start). Corrected in the ledger + `last-commit` + codemap header/§11. Same sha-stamp trap as D16–D63; **no out-of-skill work landed between AX.2 (`6c46a56`) and AX.4** (only AX.4 sits between AX.2 and AX.3), ledger no drift. **In-spec design choices in `content.ts` (the plan's Task-3 `content.ts` line numbers matched current code exactly — no delta drift):** (a) **chips inserted via `userName.insertAdjacentElement('afterend', span)`**, not appended to `User-Name.parentElement` — the plan named `parentElement` as "the header row" but flagged it "verify live"; `afterend` places chips between the name/handle/time block and the ⋯ menu (right of the name line, before the menu) and is the ONE live-verify item (the plan's Risk note). (b) **`getGlanceMap()` is SYNC** — `scan()` is synchronous, so it returns the cached-or-empty map immediately and kicks a background `refreshGlanceMap()` (in-flight-guarded); the first scan renders no chips, the next does (X emits mutations constantly). `unconfigured`/error → silent, keeps the stale map (channels-cache discipline). (c) **`data-sig` skip-guard** — chips recompute every scan (pure, cheap) into a `kind:label:tone` signature; the DOM write is skipped unless `data-handle` OR `data-sig` changed, so recycled nodes self-correct and the once-a-day neglect-age tick re-renders. (d) **chip `<button>`s carry `data-handle`** already (+ a no-op `stopPropagation` click) so **AX.6** is a one-line handler swap (`stratus/open-person {handle}`). (e) **6 tone→CSS classes** added to `injectStyles` (ally/mutual/responded/engaged/target/warn, theme-neutral rgba on 0.10 fills) per the AX.2 gotcha's "the 6 tone values become CSS classes AX.3 adds"; cap 4 chips + `overflow:hidden` so the header line never wraps. **kept byte-identical** (radar/Reply-Master invariant): `readTweetSignals`, `article.dataset.stratusBand` (border/dim), `recordRadarSighting`. **No new tests** (content.ts untested by convention; the pure chip logic is AX.2's bun-tested `glance.ts`). **Binds AX.5** (same content.ts lane, next in the D9 chain): the glance cache, chip styles, `applyPersonChips`, and `data-handle` stamping are all in place; AX.5 adds the status-page context panel + the `#reply-master-btn { display:none !important; }` kill rule (remind the user to uninstall the legacy `~/newme/clipx/reply-master` extension at ship). content.ts lock released on commit (AX.5 isn't running immediately).
- **D65** (AX.5): **Sha reconciliation** — AX.3's recorded `bc2c9fe` was an amend-orphan (`git merge-base --is-ancestor bc2c9fe HEAD` → false, `cat-file -t` still finds it, same "(AX.3)" subject as HEAD); real AX.3 = **`39fecb6`** (= HEAD at AX.5 start, parent `7ecb8df` = AX.4). Corrected in the ledger + `last-commit` + codemap header/§11. Same sha-stamp trap as D16–D64; no out-of-skill work landed between AX.4 and AX.3, ledger no drift. **In-spec design choices in `content.ts` (the plan's Task-5 pointers matched current code — no delta drift):** (a) **panel inserted as the article's NEXT SIBLING** (`focusedArticle.insertAdjacentElement('afterend', panel)`), not inside the article after the action-row group — the plan's two phrasings ("after the action-row group" + "insert into the article's parent flow so X's virtualisation doesn't clone it") conflict unless read as *visually* after; the action row is the primary article's last block, so a sibling-after-article reads as "below the action row" AND satisfies the no-clone constraint (a child of a recycled article node would be carried; a sibling isn't). (b) **`refreshDossier`'s `.finally` calls `scheduleScan()`** — the ONE deviation from the sync glance cache (`getGlanceMap` relies on constant timeline mutations to re-render). A `/status/` page can be mutation-quiet, so nothing would re-render the panel once the async dossier arrives; the rescan can't loop because the fetch result is cached fresh BEFORE the rescan fires (getDossier won't re-fetch a fresh entry). (c) **Three cached terminal states `ready|missing|unavailable`** (not dossier|null): `missing` (HTTP 404) → the muted "No stratus file" line, `unavailable` (unconfigured `status:0`/network) → render NOTHING but STILL cached — caching the negative is what breaks the fetch-loop the `.finally`-scheduleScan would otherwise create on an unconfigured extension. (d) **The header stage chip reuses AX.3's `.stratus-person-chip[data-tone]`** but AX.3's tone classes only cover engaged/responded/mutual/ally; `stageTone()` maps a sub-engaged stage (stranger/noticed) → the neutral gray `engaged` tone so a chip always renders (a known person always gets a panel, even at `noticed`). (e) **Collapse toggle is a dedicated chevron button** (`▾`/`▸`, geometric glyphs, §7.31 no-emoji) whose click `stopPropagation`s; the header→dossier **click-through is AX.6's** (`title.dataset.handle` stamp + a future handler), left exactly as AX.3 left the chips. **Binds RU.7** (next content.ts task, D9): the panel + kill rule + dossier cache are all in place; RU.7 adds on-page variant chips to the focused-article action row + the paste→posted flow — it does NOT touch `syncContextPanel`. **Binds AX.6** (parallel-ok, NOT in the D9 content.ts chain — it edits messages/background/App): the `data-handle` on chip buttons (AX.3) and the panel's `title` (AX.5) are the click-through hooks; wire `stratus/open-person {handle}`. content.ts lock released on commit.
- **D66** (AX.6): **Sha reconciliation** — AX.5's recorded `0eb5319` was an amend-orphan (`git merge-base --is-ancestor 0eb5319 HEAD` → false, `cat-file -t` still finds it, same "(AX.5)" subject `feat(extension): stratus context panel on tweet pages` as HEAD); real AX.5 = **`5e31b8d`** (= HEAD at AX.6 start, parent `39fecb6` = AX.3). Corrected in the ledger + `last-commit` + codemap header/§11. Same sha-stamp trap as D16–D65; **no out-of-skill work landed between AX.3 and AX.5** (HEAD *was* AX.5), ledger no drift. **No plan divergence otherwise** — AX.6 followed Task 6 exactly (new `OpenPerson`/`OpenPersonClear` messages + guards; background `sidePanel.open({tabId})` best-effort + `stratus:openPerson` session write via its own `enqueueOpenPerson` chain; `App.tsx` mount+onChanged consumer that routes then clears; chip button + panel `title` click both fire `sendOpenPerson`). **Additive/in-spec choices worth keeping:** (a) `App.tsx`'s effect **inlines openPerson's body** (`setPersonHandle`+`setTab`) rather than calling `openPerson`, so the empty dep array references only stable setState setters — biome `useExhaustiveDependencies` (recommended set) stays green without re-registering the storage listener each render. (b) The `enqueueOpenPerson` chain is **separate from `enqueueRadar`/`enqueueLaunch`** — the open-person key is an independent session cell; serializing it through its own chain keeps the background-single-writer discipline without coupling to the radar/launch write order. (c) `sidePanel.open` is guarded `if (tabId !== undefined)` (passing `{tabId: undefined}` throws); renamed the onMessage listener's `_sender`→`sender` (now used). (d) The panel `.stratus-ctx-title` already had `cursor:pointer` from AX.5 — no CSS change needed; the collapse toggle is a header **sibling** of `title`, so a toggle click never bubbles to the title's open handler. **No new tests** (guards trivially typed — the task block says skip; no message-guard suite exists for these, unlike RU.6's `isRadarConfirm`). **Live done-when tail (not scriptable):** in the loaded unpacked extension, a timeline chip click (panel closed → opens on the dossier; open → tab switches) and a status-page panel-header click both land on the right person; replayed opens don't recur. **Binds RU.7** (next D9 content.ts task — on-page variant chips; unrelated to the click-through). All four AX.6 files were free (no hot-file lock held).

- **D67** (RU.7): **Sha reconciliation** — AX.6's recorded `b3fd7b3` was an amend-orphan (`git merge-base --is-ancestor b3fd7b3 HEAD` → false, `cat-file -t` still finds it, same "(AX.6)" subject `feat(extension): open dossier from timeline chips and context panel` as HEAD); real AX.6 = **`0e08af9`** (= HEAD at RU.7 start, parent `5e31b8d` = AX.5). Corrected in the ledger + `last-commit` + codemap header/§11. Same sha-stamp trap as D16–D66; **no out-of-skill work landed between AX.5 and AX.6** (HEAD *was* AX.6), ledger no drift.
- **D68** (RU.7): five in-spec choices, all faithful to Task 7's edit list but sharper against the current code (the plan predates AX.5/RU.5/RU.6). **(a) Injection point = `actionRow.appendChild`, NOT article-afterend.** The plan says "same injection discipline as `attachReplyMasterButton`" (which appends inside the action row) and separately "under the action row". AX.5's `syncContextPanel` already owns `focusedArticle.insertAdjacentElement('afterend', panel)`; injecting the chip strip at the article's afterend would collide (my strip becomes the article's `nextElementSibling`, which flips AX.5's `nextElementSibling===existing` anchor check → the panel thrash-rebuilds every scan). So the strip is appended INSIDE the action row via a SEPARATE WeakSet (`variantChipsHandled`, distinct from `replyMasterHandled` so both inject) — it dies with the recycled action row (no orphan), and never touches the article-sibling slot. The exact visual placement (a compact 3-chip column pinned to the right of the action icons) is **browser-unverified by convention** (content.ts DOM behavior is manual-verified per the plan's Tests section + the D64/D65 tail) and a candidate for Wave-5 polish. **(b) The primary comes off the confirm response, not the message.** The plan's `variant-pasted` PATCH is `replyTextEdited: text≠primary ? text : undefined` but left "primary" unspecified. RU.5's confirm returns the FULL `reply_drafts` row, so `pasteVariant` reads `row.replyText` (= the primary) and compares — no need to thread the primary through the message. Cleaner + always correct. **(c) `variant-pasted` does NOT stamp `draftId` into the buffer.** confirmDraft (RU.6) stamps it on the panel-click path; the paste path re-confirms (idempotent) to GET the id and PATCH, and nothing reads the buffer `draftId` client-side, so stamping would be vestigial. Minimal, matches the plan's edit list (which omits stamping here). **(d) `GET /x/radar/drafts` returns `{count, drafts}`, not a bare array** — the background fallback reads `.drafts[0]` (newest-first, RU.5's non-expired-when-tweetId filter), mirroring the existing `rehydrateFromServer`. **(e) Pure helpers live in a NEW `variantChips.ts`** (`variantChipPreview`/`isReplyVariants`) — content.ts is untested by convention, so the two testable bits get their own bun-tested module; `isReplyVariants` is a REAL consumer (background server-fallback validation + content response validation), not dead test-only code. Message guards ride `messages.test.ts` (RU.6 pattern). No server/schema/route change — RU.7 is a pure consumer of RU.5's confirm + tweetId filter + the `copied→posted` transition. **Binds RU.8** (next content.ts task): it widens `RadarSighting.band` to `'manual'`, adds the ⊕ button in the same action row (a THIRD WeakSet), and widens the server band on `parseBatchTweets`/`RadarBatchTweet`/`RadarDraftInsert`.

- **D70** (RU.9): **Sha reconciliation** — RU.8's recorded `9ba6fba` was an amend-orphan (`git merge-base --is-ancestor 9ba6fba HEAD` → false, `cat-file -t` still finds it, same "(RU.8)" subject `feat(radar): manual add-to-queue button on any tweet` as HEAD); real RU.8 = **`065c382`** (= HEAD at RU.9 start, parent `f90084b` = RU.7). Corrected in the ledger + `last-commit` + codemap header/§11. Same sha-stamp trap as D16–D69; **no out-of-skill work landed between RU.7 and RU.8** (HEAD *was* RU.8), ledger no drift.
- **D71** (RU.9): **`classifyReplyOrigin` signature — merged Set into the source map, not "added a 4th arg".** The plan (Task 9 Edit) said "`classifyReplyOrigin` gains a source map arg." Taken literally that adds a 4th param `draftSourceById` alongside the existing `draftPostedIds: ReadonlySet<string>` — but the Set is *exactly* the map's keys (both built from the same `status='posted' AND postedTweetId NOT NULL` reply_drafts query), so keeping both is a redundant param. Instead the 2nd arg CHANGED type `ReadonlySet<string>` → `ReadonlyMap<string, string|null>` (`draftSourceByPostedId`); `.has(tweetId)` still gates the draft-linked branch, `.get(tweetId)==='radar'` picks radar-vs-single. Behavior is identical to the plan's intent (source beats the radar text-match heuristic, which survives only as the no-draft-link fallback). Sole non-test caller was `routes/playbook.ts::loadOriginRows` (updated) + the pure tests (Set→Map). **No downstream binding** — `classifyReplyOrigin`/`buildBatchVsSingle` are RU-terminal; RL.7 (`canned` attribution, Wave 2) will ADD a bucket to `buildBatchVsSingle`/the origin type but doesn't re-read this signature. **Route-test isolation choice:** the new `describe('radar source-exact attribution (RU.9)')` seeds into the shared `:memory:` DB and cleans up in afterAll — placed AFTER the permanent-seed main describe so pb_r1/pb_r2 (single) + pb_r3 (radar-fallback) exist during its run, giving the deterministic `single.n=2, radar.n=2` (old code: 3/1). Did NOT extend the main beforeAll (a radar-sourced measured reply would ripple into angle/band/model/latency/roster aggregations and break their exact-count assertions).
- **D69** (RU.8): **Sha reconciliation** — RU.7's recorded `bfa2441` was an amend-orphan (`git merge-base --is-ancestor bfa2441 HEAD` → false, `cat-file -t` still finds it, same "(RU.7)" subject `feat(extension): on-page variant chips paste chosen radar reply` as HEAD); real RU.7 = **`f90084b`** (= HEAD at RU.8 start, parent `0e08af9` = AX.6). Corrected in the ledger + `last-commit` + codemap header/§11. Same sha-stamp trap as D16–D68; **no out-of-skill work landed between AX.6 and RU.7** (HEAD *was* RU.7), ledger no drift. **RU.8 in-spec choices vs the plan (the plan predates RU.4–RU.7):** **(a — D69a) `band:'manual'` is the SOLE manual marker, not a separate `manual?: true` flag.** The Design §33 text mentions both a `manual?: true` flag AND band widening, but Task 8's edit list + Decisions 5/7 key everything (merge/rank/evict/rehydrate) off `band==='manual'`. I dropped the redundant flag — cleaner, and every consumer already branches on band. **(b — D69b) "6 files" resolved to include `extension/src/shared/types.ts`** (`BatchReplyTweet.band` widened): `Radar.tsx:147 draftReplies` forwards `band: s.band` (now `RadarBand`) into a `BatchReplyTweet`, so without widening the extension typecheck breaks. The plan listed 5 files; the 6th is types.ts (plus the 3 test files touched). **(c — D69c) The confirm endpoint (radar.ts:285) coerces `band:'manual'`→`null`** in the rebuilt `contextSnapshot.signals.band` — this is BEYOND the literal edit list (which only says "type widened on RadarBatchTweet/RadarDraftInsert") but is MANDATED by Task 8's "How" ("never 'manual'; PostSignals.band is the classifier's type"): once band widens, a confirmed manual radar draft would otherwise leak `'manual'` into the Playbook's §7.19 band-calibration cells. Added `row.band === 'manual' ? null : (row.band as PostSignals['band'])` + a regression test (`T_MANUAL` seed → confirm → `signals.band` toBeNull). `PostSignals['band']` = `Band` = `'hot'|'warm'|'skip'|null`, so null is type-valid. **(d — D69d) ⊕ injected into the action row (`actionRow.appendChild`), not "next to the band badge"** — AX.3 removed the pill, so the plan's "next to the band badge" is moot; mirrored `attachButton`. `onRadarAddClick` sends `stratus/radar-report` IMMEDIATELY (one deliberate click, not the 2s-batched `pendingRadar` path) so the background (single §7.24 writer) merges it right away; `recordRadarSighting` left byte-identical (feeds the hot/warm stream + Reply-Master signals). content.ts lock released on commit — the next content.ts task NT.5 is gated behind its server deps.

- **D72** (RU.10): **Sha reconciliation** — RU.9's recorded `899e126` was an amend-orphan (`git merge-base --is-ancestor 899e126 HEAD` → false, `cat-file -t` still finds it, same "(RU.9)" subject `feat(playbook): exact radar attribution via reply source column` as HEAD); real RU.9 = **`f78b48c`** (= HEAD at RU.10 start, parent `065c382` = RU.8). Corrected in the ledger + `last-commit` + codemap header/§11. Same sha-stamp trap as D16–D71; **no out-of-skill work landed between RU.8 and RU.9** (HEAD *was* RU.9), ledger no drift. **RU.10 followed Task 10 exactly** — new `scripts/smoke-radar-reply-flow.ts`, CLAUDE.md §"Overhaul 7.7" phase entry, PLAN.md top blockquote, `docs/today-tab.md` (Radar) + `docs/replies-tab.md` (two→three variants + on-page chips); **codemap §3.3/§3.4/§4/§5 were ALREADY accurate from the incremental RU.1–RU.9 updates** (confirm route in §3.4, messages/radar.ts/Radar.tsx/content.ts chips+⊕ in §5, `reply_drafts.source`/`radar_drafts.variants`/`model`/`reply_draft_id` in §4), so only §9 (smoke-list), §11 (log line), and the header re-stamp were touched. **In-spec smoke choices:** (a) the origin assertion calls the pure `classifyReplyOrigin` directly with a `postedTweetId→source` map rebuilt exactly as `loadOriginRows` does (the PATCH route doesn't itself run the classifier — the Playbook loader does) and passes an **empty text-match fallback** map so a pass proves attribution is by SOURCE, not the reconstructed heuristic; the `replyTextEdited` deliberately matches no variant to make that airtight. (b) The seed uses `band:'hot'` and asserts it's PRESERVED in `contextSnapshot.signals.band` — the manual→null coercion is already locked by the server `radar.test.ts` (D69c), so testing 'hot' exercises the more common shape. **This closes the RU sub-plan (RU.1–RU.10).** No hot-file lock held (script + docs only).
- **D73** (AX.7): **Sha reconciliation** — RU.10's recorded `22099a3` was an amend-orphan, this time **GC'd/MISSING** (`git merge-base --is-ancestor 22099a3 HEAD` → false; `cat-file -t 22099a3` → MISSING); real RU.10 = **`84ce82b`** (= HEAD at AX.7 start, subject `docs(radar): radar↔reply unification docs-sync + $0 smoke (RU.10)`, parent `f78b48c` = RU.9). Corrected in the ledger + `last-commit` + codemap header/§11. Same sha-stamp trap as D16–D72; **no out-of-skill work landed between RU.9 and RU.10** (HEAD *was* RU.10), ledger no drift.
- **D74** (AX.7): **Naming deviation — plan's "Surfaces S5" is stale; shipped as "Surfaces S6".** The AX plan (`plans/2026-07-16-augmented-x-ui.md` Task 7) instructs a CLAUDE.md entry `"Surfaces S5 — Augmented X UI"` and a `SURFACES-PLAN.md` "S5 section" + `docs/s5-augmented-ui.md`. But **S5 is already taken by Studio 2.0** (CLAUDE.md §"Surfaces S5 — Studio 2.0"; `SURFACES-PLAN.md` §5b "Phase S5"). Per Step 4 (current code wins over stale plan text), AX shipped as **S6**: CLAUDE.md §"Surfaces S6 — Augmented X UI", `SURFACES-PLAN.md` §5c "Phase S6 — Augmented X UI" (SURFACES-PLAN numbers phases S0–S5, so S6 is the next free number), and **`docs/s6-augmented-ui.md`** (no `s5-*.md` file ever existed — Studio 2.0's doc is `s3-studio.md`, so the doc-file numbering and the CLAUDE "Surfaces SX" numbering already diverged; s6 is a clean new file). AX.7 was **docs-only, no code/schema/route/test change** — codemap §3.4/§4/§5/§9 were already accurate from AX.1–6 (verification only), so the codemap touches were §2 `docs/` row + §11 line + header re-stamp. Gates docs-only: `bun run test` 1020/0 unchanged, typecheck clean, `smoke-glance` SMOKE OK $0, changed-file lint 0-error. **Closes the AX sub-plan (AX.1–AX.7). Wave 2 = RU.1–10✓ AX.1–7✓.**
- **D75** (RL.1): **Sha reconciliation + 3 out-of-skill commits.** AX.7's recorded `00ec8a8` was an amend-orphan, this time **GC'd/MISSING** (`git cat-file -t 00ec8a8` → *Not a valid object name*); real AX.7 = **`83f8c4d`** (in HEAD's ancestry, same "(AX.7)" subject, parent `84ce82b`=RU.10) — corrected in the ledger + `last-commit` + codemap header/§11. Then **3 commits landed OUTSIDE the skill**: `912a9cb` ("reply ammo" — `reply-ammo.md`), `1b2a474` ("analysises" — `evals/i_mika_el_{analysis.md,posts,replies}.csv` + `plans/2026-07-22-mika-growth-tactics.md`), `2ffca50` ("future iterations plans" — `evals/x-builder-{judge-generate,static-engine}-analysis.md` + `plans/2026-07-22-llm-judge.md` + `plans/2026-07-22-static-coach.md`). All **docs/plans/evals only — zero source files, zero masterplan files**; ledger no drift, no RL.1 file touched. **Note for future sessions:** those 3 new `plans/2026-07-22-*.md` docs are NOT in `plans/MASTERPLAN.md` — they are not masterplan tasks and `/masterplan next` must not pick from them.
- **D76** (RL.1, binds RL.2/RL.3/RL.5): **engine contract additions beyond the plan text.** (a) **`parseHumanizerConfig` returns `HumanizerConfig | null`, not always-a-config** — null ONLY when the value isn't a plain object (brandKit's exact contract). The plan's Task-1 line says "lenient, field-by-field fallback to defaults", but Task 2's done-when requires `400 invalid_humanizer`, which is impossible if every input parses; a partially-bad object still falls back field-by-field. **RL.2:** `humanizer === null` in the body = clear-to-defaults, a non-null value that parses to null = 400. **RL.3:** use `resolveHumanizer(list.humanizer)` (exported, = `parse ?? DEFAULT_HUMANIZER`). (b) **`HUMANIZE_DRAWS`=10 rng draws are taken up front on EVERY `humanize` call, fired or not** (0 prefix chance · 1 prefix pick · 2 suffix chance · 3 suffix pick · 4 lowercase · 5 drop-period · 6 typo chance · 7 typo word · 8 typo kind · 9 typo position) — the plan asked for "independent rolls in fixed order so stubbed sequences are predictable"; a fixed count makes a stub array map to steps by position regardless of what fires. Don't make a step's draws conditional without updating the tests + the exported constant. (c) **Extra exports the plan didn't list** (all used by later RL tasks): `MAX_REPLY_LENGTH` (280 — RL.2 item validation should reuse it), `TEMPLATE_VARS`/`TemplateVar`, `templateVars(text)` (which vars an item needs — RL.5 can show them per item), `availableVarsFor(vars)` (build the `pickItem` arg from the target), `stripEmoji`, `resolveHumanizer`. (d) **`humanize` tracks a `bodyStart` offset** so an applied prefix doesn't shield the item's first letter from the (independent) lowercase roll. (e) **`pickItem` is generic** `<T extends PickableItem>` — the route gets its own row type back; it takes `ReadonlySet<string>`, not a `ReplyVars`. (f) **Deprioritization outranks recency**: when only var-free items are usable, the pool collapses to them and one may repeat — never blocking the use (Decision 7) — locked by a test. (g) **RL.2's migration is NOT `0013`** (the plan's number is long taken by UI.1/AI): run `bun run db:generate` against the current journal and inspect the SQL (codemap §4 seed-drop trap).

- **D77** (RL.2, binds RL.3/RL.5/RL.7): **two things. (a) Sha reconciliation** — RL.1's recorded `58903fc` was an amend-orphan (`git merge-base --is-ancestor 58903fc HEAD` → false, `cat-file -t` still finds it); real RL.1 = **`ffbdb3f`** (= HEAD at RL.2 start, same "(RL.1)" subject `feat(x): reply-list engine — template render, anti-repeat pick, humanizer`, parent `2ffca50`). No out-of-skill work landed between RL.1 and RL.2; ledger no drift — RL.1's ledger sha corrected. Same sha-stamp trap as D16–D76. **(b) Migration is `0018_flimsy_captain_universe.sql`, NOT the plan's "0013"** (long taken by UI.1; 0014–0017 too) — per Global Rule #1 `bun run db:generate` produced it and the SQL was inspected: 3 CREATE TABLEs + 2 CREATE INDEXes, **DDL-only, no seed to drop, no ALTER/DROP on an existing table**. Additionally verified on a *copy* of the real `stratus.db` (36 tables after, `content_pillars` seed still 3 rows, FK cascade live under `PRAGMA foreign_keys=ON`) — the plan's "lands on a fresh boot without touching existing tables" done-when, proven on real data rather than only the in-memory suite.
- **D78** (RL.2, binds RL.3/RL.4/RL.5): **route-contract decisions the plan left open.** (a) **`POST /:id/items` returns the list's WHOLE current item set**, not just the written rows — the plan's `→ {items}` is ambiguous and one shape for both modes is what the RL.5 editor wants. (b) **Item `createdAt` is stamped explicitly** (`base = max(now, list's max createdAt + 1)`, then `+i` per row) inside the txn: a batch insert shares one `unixepoch()` default millisecond, and an append in the same ms as the previous batch tied with it and sorted into the middle (caught by a test). Don't revert to the column default. (c) **Items must be `{text}` objects** — a bare string 400s `invalid_items`, matching the plan's contract and RL.4's generator shape. (d) **An empty `items: []` is legal**: with `mode:'replace'` it clears the list (the panel's "start over"), with `append` it's a no-op. (e) **`source` is per-CALL, not per-item** (`'manual'`|`'ai'`, default manual) — RL.4's "Overwrite list" sends `source:'ai'` once. (f) Item PATCH/DELETE match `id` AND `listId` (wrong-list → 404); malformed uuid → 400 `invalid_id`, absent → 404. (g) `MAX_ITEMS_PER_CALL=100` is per call, **not a per-list ceiling** — repeated appends can exceed it by design. (h) No `?active=` filter on the collection GET (the plan's contract has none; RL.5 filters client-side).

- **D79** (RL.3, binds RL.4/RL.5/RL.6/RL.7): **the `/use` contract, and one correctness fix the plan didn't call for.** (a) **`lastUsedAt` is stamped MONOTONICALLY past the list's current max**, not from `Date.now()` — the plan says only "stamps last_used_at". The anti-repeat window (`pickItem` excludes the `min(n-1, floor(n/2))` most-recently-used) only guarantees no immediate repeat while the just-used item is *strictly* the most recent; uses land ~0.5ms apart, so `Date.now()` ties them and the recency sort can exclude the wrong item. **Measured, not theorized:** a throwaway 500-use stress showed **241/500 uses sharing a millisecond and 4 immediate repeats** without the stamp, and 0/0 with it — the plan's own 10-use done-when test would have been ~8%-flaky. Same trap RL.2 hit on item `createdAt` (D78b); now codemap **§7.13a**. Any future column an ordering rule reads needs this. (b) **`composeReply` has NO `protectedValues` argument** — the plan's "`composeReply` with … `protectedValues = [vars.name, vars.handle]`" is stale; RL.1's signature is `(itemText, vars, config, rng)` and it derives name/first_name/handle protection internally. (c) **The route passes ALL items to `pickItem`, not just enabled ones** — the engine owns the enabled + var-availability rules, so the filtering lives in exactly one place. (d) **`updatedAt` is NOT bumped on use** (it means "the text was edited" — RL.5's item editor can show it truthfully); only `lastUsedAt`/`useCount` move. (e) **An `active:false` list is still usable.** `active` is presentation state (D78h: RL.5 filters client-side) and the contract lists only 404/409, so no new error code was invented — RL.5/RL.6 must not offer inactive lists in the picker if that's the intent. (f) **A body-less POST is a legal "use with no target"** (`c.req.json().catch(() => undefined)` → `{}`); only a body that parses to a non-object 400s `invalid_body`. Malformed JSON therefore degrades to an un-personalized use rather than a 400 — accepted because the text is copied to the clipboard and read by a human before pasting (§7.28). (g) **`targetHandle` is normalized the repo way** (`@`-stripped, lowercased, `USERNAME_RE` = `/^[A-Za-z0-9_]{1,15}$/`) and **400s `invalid_target_handle`** when malformed — it's audit metadata that later joins people/voice rows, so storing garbage is worse than refusing. `targetTweetId` uses the plan's `/^\d{1,32}$/` (note: laxer than launch.ts's `TWEET_ID_RE` = `{1,25}`). `vars.name`/`vars.handle` are only length-bounded (≤120) — never USERNAME_RE'd, because they are rendered as display text. (h) **Error slugs:** `invalid_id` / `invalid_body` / `invalid_vars` / `invalid_target_tweet_id` / `invalid_target_handle` / `invalid_preview` / `not_found` (404) / `no_enabled_items` (409). (i) **`reply_list_uses` never cascades** (FK-free by design) — the route's test `afterAll` deletes those rows explicitly, and **RL.7's playbook suite must expect stray rows if any future suite forgets**.

- **D79a** (RL.3 bookkeeping): RL.2's recorded `a29ffbe` was an amend-orphan; real RL.2 = **`9c0d97b`** (= HEAD at RL.3 start, same "(RL.2)" subject, parent `ffbdb3f` = RL.1). No out-of-skill work landed; ledger no drift. The two orphaned shas embedded in the **codemap** (`a29ffbe` for RL.2, `58903fc` for RL.1) were corrected there too. **Housekeeping:** STATE.md's `last-commit`/`current wave`/`next-up` lines had grown into ~40KB of historical recital; RL.3 compacted them to the actionable present (260KB → ~223KB). The per-task history lives in the D-entries and the codemap §11 log — do not re-grow these three lines. The sha trap in one sentence: a commit cannot contain its own sha, so the stamp is always pre-amend and **HEAD's subject line is the authoritative identity**.

- **D80** (RL.4, binds RL.5/RL.6/RL.8): **the plan's Task-4 text predates the AI layer; three instructions were overridden.** (a) **`askGrok` + `promptCacheKey:'stratus-reply-list-gen'` → `askLLM` + `loadPromptSafe('reply-list')`** (D5): the generator prompt is now an editable registry key, so `PROMPT_KEYS` went **10 → 11** (appended after `icebreaker`; required placeholders `{{COUNT}}`/`{{REQUEST}}`/`{{EXISTING_ITEMS}}`, no optional ones) and the cache key is the registry sha — **bare, no niche suffix**, because the prompt carries no persona token (the `ideas`/`rewrite` precedent). `registry.test.ts`'s key list and `docs/settings-tab.md`'s "10 prompts" copy were updated in the same commit; the extension's Prompts panel is data-driven (`api.prompts.list`) and needed no change. (b) **No pre-flight `XAI_API_KEY` check → 503 `grok_not_configured`.** Following ideas.ts (AI.9, the newest new-surface precedent), the route has NO pre-flight gate: `askLLM` enforces the resolved provider's key and `llmErrorPayload` maps it to **503 `llm_not_configured`**. Two reasons this is the right call and not laziness: an OpenRouter-only setup must be able to generate, and a pre-flight check would shadow the **404 for an unknown list** (which now fires before any spend regardless of keys). **RL.5's error matcher must expect `llm_not_configured`, not `grok_not_configured`.** (c) **`count` 400s past 30 instead of clamping** (ideas.ts clamps) — the plan's own test asks for a 400 on 31, and silently returning 12 of a requested 40 reads as a broken generator. Also: `parseGeneratedItems` dedupes on a **lowercased + whitespace-collapsed** key rather than the plan's "exact texts" (casing-only twins defeat the anti-repeat pick they would feed), and it distinguishes **`null` = parse error** from **`[]` = nothing survived** — the route 502s `items_parse_error` vs `items_invalid`. Response is the plan's contract plus `count`/`requested` (ideas.ts shape). **Nothing is persisted** — RL.5's "Overwrite/Append" goes through the existing `POST /items` with `source:'ai'` (D78e).
- **D80a** (RL.4 bookkeeping): RL.3's recorded `0ea02a3` was an amend-orphan; real RL.3 = **`c3f44d2`** (= HEAD at RL.4 start, same "(RL.3)" subject `feat(x): POST /x/reply-lists/:id/use — anti-repeat pick + humanized render`, parent `9c0d97b` = RL.2). No out-of-skill work landed; ledger no drift. The orphaned sha embedded in the codemap (header + §11 RL.3 line) was corrected there too. Same one-command check as always: `git log -1 --format='%h %s'` — HEAD's subject is the authoritative identity.

- **D81** (RL.5, binds RL.6/RL.8): **four divergences from the plan's Task-5 text, three of them substantive.** (a) **The subtab uses the `SubTabs` UI.10 primitive, not a copied `radar-tabs` block.** The plan says "copy `Voice.tsx:19`'s `'tweets'|'pillars'` switch + its subtab header markup" — but `SubTabs` IS the formalized version of exactly that control (UI.10 shipped it saying "Voice migrates onto this in UI.14"), and **D7 binds every NEW Wave-1+ surface to the primitives**. Using it cost one import and zero CSS; copying `radar-tabs` would have added a third hand-rolled copy for UI.14 to unpick. The AI.10 Settings shell's `radar-tabs` is the older idiom — don't read it as the standard for new surfaces. `Replies.tsx`'s `<h2>` also changed "Reply Master" → "Replies" (the subtab chip now carries that name). (b) **The humanizer editor keeps NO client-side copy of `DEFAULT_HUMANIZER`.** The plan's "pools + chance sliders, Reset to defaults" implies the panel knows the defaults to seed the form; duplicating them in TS is exactly the drift the repo avoids elsewhere (and the extension MUST NOT import `src/x/replyLists/engine.ts` — §5 build isolation, and it isn't a `src/shared/` shim). Instead **"Customize for this list" PATCHes `{humanizer:{}}`**: `parseHumanizerConfig({})` is non-null and lenient-parses every absent field to its default, so the response row IS the full engine config and the form renders from server truth. "Reset to defaults" PATCHes `null` (back to the "using engine defaults" card). **Verified against the real route** with a throwaway `bun:test` (create → `humanizer:null`; PATCH `{}` → deep-equals `DEFAULT_HUMANIZER`; PATCH `null` → null; plus an items append + a `preview:true` `/use` asserting `{name}` got filled) — deleted after the run, not committed. (c) **Pools are one-per-line textareas, NOT the plan's "comma-separated inputs"** — a planning error: `DEFAULT_HUMANIZER.prefixes` is `['honestly,','man,','ngl','tbh','yeah,','ok so']`, so a comma split would shred half the defaults into fragments the first time anyone saved. The same one-per-line convention is used for the items add box (bulk paste → one item per line, which is also how a swipe file arrives). (d) **Per-item template-var chips were dropped.** D76c offered `templateVars(text)` "so RL.5 can show which vars an item needs", but reaching it needs either a `src/shared/` move of the engine (out of scope, touches RL.1–4) or a duplicated regex; the raw template text already shows `{name}` literally, and Test render's server-returned `missingVars` is the authoritative answer. Revisit only if a real user asks.

- **D81a** (RL.5 bookkeeping): RL.4's recorded `99eef6b` was an amend-orphan; real RL.4 = **`f353f6a`** (= HEAD at RL.5 start, same "(RL.4)" subject `feat(x): reply-list AI item generator (proposal-only askLLM call)`, parent `c3f44d2` = RL.3). No out-of-skill work landed; ledger no drift. The orphaned sha embedded in the codemap (header + §11 RL.4 line) was corrected there too.
- **D82** (RL.6, binds RL.8): **three divergences from the plan's Task-6 text.** (a) **The `onUsed?` callback prop was dropped.** The plan lists `{vars, targetTweetId, targetHandle, onUsed?}`, but the same task's How forbids the only thing a caller could do with it ("don't add new status flips; the user still clicks Done after pasting"), so neither call site had a use — an optional callback nobody passes is dead weight. Re-add it the day a caller genuinely needs to react to a use. (b) **The popover keeps the composed text visible instead of only flashing "Copied ✓".** A `writeText` rejection after the round-trip is the one failure mode that *loses* work — the item is already spent server-side (`lastUsedAt` stamped, `reply_list_uses` row written), so a swallowed clipboard error would burn an item and hand back nothing. The result view therefore renders the text (selectable) with "Copied ✓ — paste it in X" or "Copy this by hand:", plus a `missingVars` hint line and a "Pick another" button. Same instinct as Studio's `copyPng` catch. (c) **Two files beyond the plan's three:** `ReplyLists.tsx` gained a 1-line unmount cleanup (`useEffect(() => invalidateReplyListsCache, [])` — that panel is the ONLY place lists are mutated, so one line beats six invalidate calls sprinkled through its child components; the ChannelTags precedent invalidates from the editing tab too), and `styles.css` gained the `.quick-reply*` block. **No UI.10 primitive fits** — there is no Menu/Popover primitive (GearPopover is settings-specific), so the picker re-uses its outside-`mousedown` close *pattern* with its own `--strat-*`-only CSS; D7 is honored on tokens, and a future UI.x can extract a Popover primitive from these two consumers.

- **D83** (RL.7, binds RL.8): **three divergences from the plan's Task-7 text, plus one drive-by.** (a) **`cannedTexts` is a REQUIRED 4th param, not optional.** The plan says "gains a `cannedTexts: Set<string>` param"; defaulting it to an empty set would have spared 5 test call sites and `scripts/smoke-radar-reply-flow.ts` — and let any future caller silently misattribute every canned reply as unattributed. Explicit beats convenient here (the same reasoning RU.9 used for the source map). (b) **The route test asserts a DELTA, not `canned.n === 1`.** The plan's test spec says "seeded use row + posts_published reply → `canned.n === 1`", but D79i is explicit that `reply_list_uses` is FK-free and a suite that forgets its cleanup leaves strays in the shared `:memory:` DB. The describe therefore snapshots `batchVsSingle` AFTER seeding the published reply but BEFORE inserting the use row, then asserts canned +1 / unattributed −1 (the median is pinned only when `before.canned === 0`). That also proves the *direction* — the use row is what moves the reply out of unattributed. (c) **A behavior-neutral refactor inside `classifyReplyOrigin`**: `normalizeReplyText(reply.text)` is now computed once at the top (both the radar branch and the canned check need it) instead of inside the radar `if`. Note the knock-on in the pure suite: the existing "wrong radar target" case (`tweetId 904`, text `'Ship it. Then fix it.'`) **now returns `'canned'` instead of `null`** because that same text is in the test's canned set — deliberate, and the clearest available demonstration that canned is a genuine fallback tier rather than dead code. (d) **Drive-by:** `src/x/playbook.ts` contained two literal NUL bytes (the `${r.pillar ?? '\0'}` key sentinel in `buildPillarRegisterScorecard`), written as raw bytes rather than escapes. `file` reported the file as `data`, so **every plain `grep` over it returned nothing** — I lost a round-trip concluding `classifyReplyOrigin` didn't exist before trying `grep -a`. Replaced with `'\0'` escapes (byte-different, runtime-identical); codemap §7.32 records the audit one-liner. `src/x/playbook.ts` was the only such file in the repo.

- **D84** (RL.8, closes the RL sub-plan): **four divergences from the plan's Task-8 text, none structural.** (a) **The phase shipped as "Surfaces S7", not the plan title's "(S5)"** — S5 is Studio 2.0 and S6 the Augmented X UI, both already in CLAUDE.md/SURFACES-PLAN; AX.7 hit the identical collision and resolved it the same way (a parenthetical noting the plan text is stale). SURFACES-PLAN gets §5d, matching §5b/§5c. (b) **The smoke covers the Playbook `canned` bucket, which the plan's Task-8 spec doesn't mention** — RL.7 landed after the plan was written and the RL.7 next-up explicitly asked for it. It asserts a **delta** (`canned.posted`/`canned.n` +1, `unattributed` unmoved) around its own seed, for D83b's reason: `reply_list_uses` is FK-free, so an absolute is never safe. It seeds **both** sides (a `posts_published` row whose text IS a use's `renderedText`, plus a `metrics_snapshots` row) rather than calling `/use` and waiting on the 03:00 discovery. (c) **Two guards beyond the plan's walk, both $0:** the empty-list `409 no_enabled_items`, and a `/generate` refusal check that **branches on `llmConfigured()`** — no key → assert `503 llm_not_configured` (D80b's error code, not the plan's `grok_not_configured`); key present → assert the unknown-list **404 fires before any spend** (so the script stays $0 on a configured machine, which is the only way it was actually run here). The plan's "vars rendered" assertion is done **per item template** via an `itemId → template` map, so it holds whichever item the shuffle picks — a blanket "the text contains Ana Pop" would fail the moment the shuffle picks the var-less item. (d) **Docs went wider than the plan's one-liner:** `docs/replies-tab.md` gained a full "The Lists subtab" section (templates + missing-var degradation, the anti-repeat shuffle, the humanizer table with its two guarantees, proposal-only generate, the `canned ▾` surfaces, and the two honest measurement caveats) plus a Launch-Room canned workflow; `docs/today-tab.md` got three touches, not two (both picker surfaces + the "which clicks cost money" tip, where canned belongs on the $0 side). **Drive-by:** three stale "two variants/two replies" lines in `docs/replies-tab.md` corrected to three (RU.1 shipped 3 variants; the body sections were already right, only the intro and the tip lagged).
- **D84a** (RL.8 bookkeeping): RL.7's recorded `b2543e7` was an amend-orphan; real RL.7 = **`3a4ae56`** (= HEAD at RL.8 start, same "(RL.7)" subject `feat(x): playbook canned-reply attribution bucket`, parent `299595f` = RL.6). No out-of-skill work landed; ledger no drift. The orphaned sha embedded in the codemap (header + §11 RL.7 line) was corrected there too. Same one-command check as always: `git log -1 --format='%h %s'` — HEAD's subject is the authoritative identity.

- **D85c** (NT.1 bookkeeping): RL.8's recorded `7ac1b5e` was an amend-orphan; real RL.8 = **`a120207`** (= HEAD at NT.1 start, same "(RL.8)" subject `docs(reply-lists): Surfaces S7 docs-sync + $0 smoke`, parent `3a4ae56` = RL.7). No out-of-skill work landed; ledger no drift. The orphaned sha in the codemap header + §11 RL.8 line was corrected there too. Same one-command check as always: `git log -1 --format='%h %s'`.
- **D85** (NT.1): **the icon `d` prefixes could not be captured from the live DOM.** The plan's How says "capture the real `d` strings into the fixtures while building them" — that assumes a browser session on x.com/notifications, which this environment doesn't have. Three consequences, all deliberate: (a) `LIKE_ICON_PREFIXES` (2 entries — outline + filled heart), `REPOST_ICON_PREFIXES`, `FOLLOW_ICON_PREFIXES` hold **best-effort recollections of X's glyph paths**, exported and comment-flagged as unverified; (b) `detectKindFromIcon` returns **`null` on an unrecognised glyph** (falls through to keywords) rather than deciding `'other'`, so wrong prefixes cost accuracy-on-locale, never correctness; (c) the test fixtures **interpolate the exported constants** into their `<path d>` instead of hardcoding paths, so correcting a prefix after the live walk needs no test edit. **NT.5's browser walk MUST verify/correct these three constants** — that is the task where a real notifications page is open anyway.
- **D85a** (NT.1): **three small divergences from the plan's How, all additive.** (1) `parseNotificationCell` returns `null` when the element's `data-testid !== 'notification'` — the plan declares the `| null` return but never says when it fires; giving it that meaning lets NT.5 pass articles blindly. (2) Keyword matching runs against the **first `[dir]` block** (the header line) and only falls back to the whole cell text — the plan says "header text keywords" without saying how the header is found; whole-text-only would let a quoted post containing "liked" vote. (3) `targetText` is **forced null for `kind === 'follow'`** rather than left to the verb filter — X sometimes renders the new follower's bio in a follow cell, and NT.2's `resolveTargetTweetId` would then try to prefix-match a bio against own `posts_published`. **Binds NT.2:** it may assume `targetText === null` for every follow engagement it receives.
- **D85b** (NT.1): **`RESERVED_HANDLES` was deliberately NOT reused.** The plan's Read-first points at `content.ts:79-100` for it, but handles here come from `UserAvatar-Container-*` testid suffixes (never a URL path segment), so route names can't appear. The one real placeholder X emits is `UserAvatar-Container-unknown`, which is filtered by name; everything else is regex-validated `/^[A-Za-z0-9_]{1,15}$/`. Importing the content.ts set would have broken the §7.26 dependency-free rule for no gain.

- **D86a** (NT.2 bookkeeping): NT.1's recorded `7f3d2fd` was an amend-orphan; real NT.1 = **`2063b86`** (= HEAD at NT.2 start, same "(NT.1)" subject `feat(extension): notification cell parser (shared, fixture-tested)`, parent `a120207` = RL.8). No out-of-skill work landed; ledger no drift. The orphaned sha in the codemap header + §11 NT.1 line was corrected there too. Same one-command check as always: `git log -1 --format='%h %s'`.
- **D86** (NT.2): **four divergences from the plan's Task-2 text, all additive or forced.** (a) **`resolveTargetTweetId` was split into a pure matcher + a DB wrapper.** The plan names one export `resolveTargetTweetId(targetText)` *and* separately demands "cache the post list once per batch" — those cannot both be one function. `matchTargetTweetId(targetText, posts)` is the pure prefix match (unit-testable with zero DB), `loadRecentOwnPosts()` is module-private, and `resolveTargetTweetId(targetText)` remains as the single-shot wrapper the plan names (exercised by the test, so it is not dead code). `recordEngagements` loads the window **lazily, at most once per batch** — a follow-only batch issues zero target SQL. (b) **`recomputePerson` runs once per distinct handle (at that handle's max seenAt), not once per row** as the plan's "per row:" list reads. A handle can legitimately carry several engagements in one batch (three likes), and `recomputePerson` reloads the entire event history on every call; once-per-handle is provably identical (stage goes through `maxStage` and these types are in no stage set; the watermarks are min/max over all events) and strictly cheaper. (c) **`engagementSummary` is exported** (the plan implies an inline expression) so the timeline wording is pinned by a test — and an unresolved like reads `liked a post` rather than a quoted empty snippet. (d) **The `people.source` schema comment was corrected beyond adding `notification`** — it listed `mention | voice | reply | harvest | manual` and had never been updated for C6 `hover` or C7 `launch`, both of which have been written for months. **Not a divergence but worth stating:** the extension's `PersonEventType` union and the **exhaustive** `EVENT_ICONS: Record<PersonEvent['type'], string>` in `People.tsx` were left alone — NT.6's edit list owns them, and because the Record is exhaustive TypeScript will *force* the three labels the moment the union grows (no risk of a silent gap). NT.2 is server-only; nothing imports `engagements.ts` until NT.3.

- **D87a** (NT.3 bookkeeping): NT.2's recorded `4e4d9fc` was an amend-orphan; real NT.2 = **`ba2c531`** (= HEAD at NT.3 start, same "(NT.2)" subject `feat(people): engagement events (their_like/their_repost/their_follow) + ingest module`, parent `2063b86` = NT.1). No out-of-skill work landed; ledger no drift. The orphaned sha in the codemap header + §11 NT.2 line was corrected there too. Same one-command check as always: `git log -1 --format='%h %s'`.
- **D87** (NT.3, binds NT.5/NT.7): **three divergences from the plan's Task-3 text, all small.** (a) **Tests were colocated in `src/x/people/engagements.test.ts`, not `src/x/routes/people.test.ts`** — the plan's Edit list names the latter, but its own Read-first says "find where sightings route tests live and colocate", and `POST /x/people/sightings` is tested in `people/sightings.test.ts`. Following the precedent also keeps the C10 ingest (module + route) in one readable file. `routes/people.test.ts` was not touched. (b) **A third file:** the `isEngagementKind` guard + `ENGAGEMENT_KINDS` tuple were added to `people/engagements.ts` beside the union rather than inlined in the route, matching `isStage` (stage.ts) and `isFollowupKind` (followups.ts) — the guard belongs with the type it narrows. The plan's "the route's kind whitelist is the enforcement point" is unchanged: the route is still the only caller. (c) **The route re-forces `targetText: null` when `kind === 'follow'`**, which the plan doesn't ask for. NT.1 already guarantees it (D85a), but the guarantee lives in a *client* the server can't version-pin, and a follow cell's prose is the new follower's **bio** — feeding that to `matchTargetTweetId` is exactly the wrong-attribution the day-bucket fallback exists to avoid. This is §7.3's "recompute the gate server-side so a stale extension build can't spend/corrupt" applied to a $0 path. It is observable (two follow cells with different `targetText` collapse to `processed: 1` instead of 2) and pinned by a test. **For NT.5:** the wire contract is `{engagements: [{kind: 'like'|'repost'|'follow', handle, targetText?: string|null, seenAt: ISO}]}`, ≤50/batch; drop `'other'` cells and handle-less cells client-side (they'd 400 the whole batch on kind, or waste a slot); a bad handle inside an otherwise-valid batch is safe — it comes back in `skipped`.

- **D88** (NT.4, binds NT.5): **four divergences from the plan's Task-4 text, all additive.** (a) **Three result types were exported beyond the plan's "message + guard"** — `NotifContextEntry`, `NotifContextMap`, `NotifContextResponse` in `shared/messages.ts`. The repo convention is to document a message's response in a comment (LaunchGet, RadarVariantsGet), but here the map shape is consumed by *both* ends (the background builds it, NT.5's DOM code reads it per cell), so a shared type beats a structural cast written twice; `MentionStatus` is reused from `shared/types.ts` rather than the plan's loose `status: string`, which is what makes NT.5's `status === 'answered'` chip check type-safe. (b) **`force` drains any in-flight refresh BEFORE invalidating**, where the plan says only "if `force` → drop cache". Without the drain, a scan-triggered pull that started *before* the sync click can resolve after it, stamp `notifContextAt = Date.now()` and pin the pre-sync rows for the full 5-minute TTL — exactly what the (already spent, capped) `POST /x/mentions/refresh` was clicked to avoid. Costs at most one extra $0 GET, and only on a deliberate human click. (c) **A fetch failure keeps the last-good map instead of the plan's literal `{ok:true, mentions:{}, rankMap}`** — that empty-map response is now only the thrown-handler path. The stamp advances *only* on success, so a failed pull retries on the next ask rather than caching emptiness for 5 min; before the first successful pull the map IS `{}`, so the plan's degradation contract holds either way. (d) **Two guard tests were added** though the plan said "none required beyond typecheck" — `extension/src/shared/messages.test.ts` didn't exist when the plan was written (RU.6 created it) and now every message guard is tested there; the cache logic itself stays untested (no background harness), as the plan intends.

- **D89a** (NT.5 bookkeeping): NT.4's recorded `fe0f6aa` was an amend-orphan; real NT.4 = **`68039f2`** (= HEAD at NT.5 start, same "(NT.4)" subject `feat(extension): background notif-context cache (mentions map + rankmap)`, parent `638d600` = NT.3). No out-of-skill work landed; ledger no drift. The orphaned sha in the codemap header + §11 NT.4 line was corrected there too. Same one-command check as always: `git log -1 --format='%h %s'`.

- **D89** (NT.5, binds NT.6/NT.7): **five divergences from the plan's Task-5 text, plus the D85 verification finally done.** (a) **The plan's (b) said tier chips go on "both tweet-articles and notification cells"; they go on notification cells ONLY.** The plan predates AX.3 (written 2026-07-16, AX.3 shipped 2026-07-21), which already decorates every `article[data-testid="tweet"]` — including the ones on this page — with the strictly richer glance chips (stage + `◎` target + `↩` owed + `Nd` neglect). Following the plan literally would have double-decorated the same line with a weaker vocabulary. Aggregated cells have no `User-Name` row and up to ~8 handles, so they keep the compact `personTierFor` chip the plan designed, which is also what keeps NT.4's `rankMap` half of the payload from being dead weight. (b) **The parent-context line renders on `status === 'answered'` alone, even when `parentText` is null.** The plan gates the whole line on `parentText`. A standalone @-mention that the inbox already settled is precisely the "don't reply to this twice" case the surface exists for, and it costs one chip. (c) **The context throttle stamps on settle, not on success.** The plan says only "60s throttle". Stamping on success alone would let a failing ask re-fire on every animation frame; the background already owns success-vs-failure caching (D88c keeps the last-good map), so the client throttle only governs *how often we ask*. (d) **`force` deliberately bypasses the in-flight guard.** `syncNotifContext()` returns early when a bare ask is in flight, but the sync chip calls `fetchNotifContext(true)` directly — routing it through the guard would silently drop the forced refetch the (already spent, capped) pull was clicked for, which is the exact failure D88b hardened the background against. (e) **`'other'` and empty-handle cells are left OUT of the `engagementHandled` WeakSet** rather than marked processed. The plan says "WeakSet per cell"; NT.1's gotcha says a skeleton cell can legitimately parse to `handles: []`. Marking those would burn the cell permanently on a render X hadn't finished — so only a cell that actually produced engagements is marked, exactly as `capturePassiveHoverCards` treats skeleton hover cards. **The D85 icon prefixes are now VERIFIED against live x.com/notifications** (this was the task that was supposed to do it): like `M20.884 13.19` confirmed on **191** cells (it is the *filled* heart; the second guess `M16.697 5.5` turned out to be the real *outline* heart X uses on the action-row like button, so both stay), repost `M4.5 3.88` confirmed by asset identity with the live `[data-testid="retweet"]` icon (**no repost notification was in the window — this one is verified by shared asset, not by a live repost cell**, and the like pair is what proves X reuses these assets), and follow **CORRECTED**: the guessed `M12 11.816` was wrong, the real glyph is `M17.863 13.44` (14 live cells, single + aggregated). Because NT.1's fixtures interpolate the exported constants (D85's design), the correction needed **no test edit**. X's bell glyph `M11.996 2c` is deliberately left unmapped so "New post notifications for … and 6 others" cells fall to `'other'` and get dropped — verified, and note those cells DO carry avatars, so a mis-mapping there would silently harvest bogus engagements. **Also live-validated, so NT.6/NT.7 can trust it:** `findPermalink` on a notification tweet-article yields the REPLY's own tweet id (the `NotifContextMap` key — the whole (a) path depends on it); a 21-cell page classified 19 like / 1 follow / 1 other; every follow's `targetText` was null (D85a holds on live DOM); and the load-bearing placement claim — chips injected **after** the header `[dir]` block changed **0 of 21** re-parses, so the augmentation provably cannot feed its own text back into the harvest. The DOM anchors themselves (`[data-testid="primaryColumn"] h2` for the chip, `tweetText`-afterend for the line, header-`[dir]`-afterend for the chips) were all exercised on the live page.
- **D90a** (NT.6 bookkeeping): NT.5's recorded `3a94830` was an amend-orphan; real NT.5 = **`69784da`** (= HEAD at NT.6 start, same "(NT.5)" subject `feat(extension): notifications tab — parent-post context, tier chips, engagement capture, sync chip`, parent `68039f2` = NT.4). No out-of-skill work landed; ledger no drift. The orphaned sha in the codemap header + §11 NT.5 line was corrected there too. Same one-command check as always: `git log -1 --format='%h %s'`.
- **D90** (NT.6, binds NT.7): **three divergences from the plan's Task-6 text, all small.** (a) **The engagement query is scoped to the ranked page, not to every handle in the window.** The plan says only "a separate query keyed by handle"; running it unbounded would aggregate every engager the notifications harvest ever produced (dozens/day by design — decision 2) to then throw all but ≤`limit` rows away. `loadFanEngagementCounts(handles, cutoff)` takes the post-`rankFans`, post-`slice` handle list, and **returns an empty Map on an empty list** so drizzle can never emit `in ()`. This is only correct *because* the count is display-only — if engagement ever became a ranking input the scoping would have to move before the ranking, and at that point decision 1 has been broken anyway. (b) **The types come from `Object.values(ENGAGEMENT_EVENT_TYPE)`** (NT.2's map) rather than a new `ENGAGEMENT_TYPES` tuple in `stage.ts`. stage.ts carries the load-bearing comment "their_like/their_repost/their_follow deliberately appear in no set below" — adding a set there, even a display-only one, is exactly the ambiguity that comment exists to prevent. Derived-not-hardcoded also means the three strings live in one place. (c) **The plan's dossier-label bullet was a no-op and stays one.** `People.tsx` renders `{e.summary ?? e.type}` and NT.2's `recordEngagements` already stores `engagementSummary(kind, targetText)` (`liked: "…"` / `liked a post` / `reposted…` / `followed you`), so the timeline reads correctly with zero label mapping. What the union widening *did* force is `EVENT_ICONS` — the exhaustive `Record<PersonEvent['type'], string>` NT.2 deliberately left alone (see D86's closing note); the icons chosen are ♥ / ⟳ / ✚. **`rankFans` was not touched at all** — decision 1 is enforced structurally (the count is attached in the response `.map`, after `slice`), and the route test re-ranks the returned page through the real `rankFans` with the counts stripped to prove the order is inbound-only.

- **D91a** (NT.7 bookkeeping): NT.6's recorded `532f87d` was an amend-orphan; real NT.6 = **`0c0ab5d`** (= HEAD at NT.7 start, same "(NT.6)" subject `feat(fans): display-only engagement counts from notification events`, parent `69784da` = NT.5). No out-of-skill work landed; ledger no drift. The orphaned sha in the codemap header + §11 NT.6 line was corrected there too. Same one-command check as always: `git log -1 --format='%h %s'`.
- **D91** (NT.7): **three additions beyond the plan's Task-7 checklist, all additive; nothing in the checklist was skipped.** (a) **`docs/README.md` gained an "On x.com itself" table.** The plan lists only the new `docs/notifications-surface.md` + one `today-tab.md` line, but the docs index has a tab table and an S1–S4 surfaces table and no home for a page-level surface — so `notifications-surface.md` would have shipped unreachable, exactly as **`s6-augmented-ui.md` already had** (AX.7 wrote it and never linked it). The new table links both; the S6 link is the drive-by fix. (b) **The smoke asserts four things the plan's spec doesn't name**, each earning its line: the seeded own post is written **`retired: true`** (invariant #7 — a non-retired synthetic own post left behind by an aborted run would be picked up and *billed* by the next 03:00 pass; this is the one real hazard of a real-DB smoke that seeds `posts_published`); a **fill-only** proof (the fan is pre-seeded enriched with `source='mention'` + a displayName, so the harvest has something it could wrongly clobber — and it is also why the plan's "assert `source='notification'`" is asserted on the *second*, harvest-created person); **the likes-only person never ranks** in `/x/people/fans` (a structural proof of decision 1 that costs one line and can't rot, unlike a re-ranking assertion which the route test already owns); and `kind:'other'` → **400** (the route whitelist is where the parser's client-side drop is re-enforced — D87c). (c) **A §7 codemap bullet** was added for the checkpoint trap (the plan asked for the note in §7; §4's `mentions` row already carried the two-precedent version, so the §7 bullet is the discoverable-from-the-patterns-list half). **No code, schema, route, test or extension file was touched** — `bun run test` is 1130/0, byte-identical to NT.6.

- **D92** (HV.1): **the plan's "no migration" beat STATE's "migration task" hint — and it was right.** STATE's next-up for HV.1 said to run `db:generate` and watch the dropped-seed trap; the source plan's Design block and decision 1 both say **zero schema change**. Verified against the code, not the prose: `harvest_runs.mode`/`.scope` and `harvest_rows.mode` are plain `text().notNull()` (the `MODES`/`SCOPES` whitelists are *route* validation, not column constraints), every passive field already exists (orig\*/`group_position`/`matched_draft_id` simply stay null), and `harvest_rows_tweet_captured_idx` **already existed** to serve the 30-min recapture query. **No migration was generated; the journal is still at `0018` and stays FREE for the next task that needs it.** Standing lesson: a next-up hint is a hint — the source plan's decision block plus the actual schema outrank it.
- **D92a** (HV.1, implementation choices the plan text doesn't pin down): (a) **in-batch duplicates are counted as `skippedRecent`.** The plan's test list names "batch-internal dupe" separately but the response shape it specifies has only `inserted`/`skippedRecent`/`skippedCap` — "already captured recently" is the honest bucket for "twice in the same 5s flush", so both fold there. (b) **The cap is computed OUTSIDE the txn** (from the run row read a few statements earlier), mirroring `/harvest/rows` — two tabs flushing concurrently can therefore overshoot 2000 by up to one batch, and the same race can double-write one longitudinal point. Deliberate and commented: the cap is a **volume guard, not an invariant** (no money is on this path — §8 can't be reached from here), and moving it inside the txn would buy nothing a single-user process needs. (c) `utcDayStart` is **exported and unit-tested** (the plan said "if not trivial inline") — it is the one piece of date math a wrong answer would silently fork a second run per day over. (d) Two extra in-file constants beyond the plan's five (`PASSIVE_SCOPE`, `PASSIVE_HANDLE`) so the synthetic run's identity is stated once. (e) `findOrCreatePassiveRun` **throws** `passive_run_insert_failed` on the impossible empty `.returning()` — `noUncheckedIndexedAccess` forces a branch and a throw is truthful where a cast would lie. (f) One test beyond the plan's list — **"accepts a re-capture once the window has passed"** (ages a stored row 31 min, re-posts, asserts a 2-point series): the plan's *phase* done-when #2 requires exactly this and the gate is only half-proven by the skip case.

- **D93** (HV.2): **four things.** **(a) Sha reconciliation + the first out-of-skill commit since Wave 0.** HV.1's recorded `bf5e7b5` was an amend-orphan (`git merge-base --is-ancestor` → false, same `(HV.1)` subject); real HV.1 = **`5ea3495`**, whose parent `5d4a1a4` is the real NT.7 (recorded `75f15e7` was likewise an orphan). Both corrected in the ledger. **But HEAD at HV.2 start was `e49f705 "refactor claude"` — genuine out-of-skill work**: CLAUDE.md slimmed by ~215 lines with its phase block moved to the NEW `docs/PHASE-HISTORY.md` (+97), plus a codemap touch (+5) and a STATE touch (+1). **No ledger drift** (no task implemented), and codemap §7.29 already reflects it: **phase entries now go to `docs/PHASE-HISTORY.md`, NOT CLAUDE.md** — every remaining docs-sync task (HV.6, GR.10, A3.15, UI.17) must target that file. **(b) The pure module type-only-imports `Extracted` from `../harvester.ts`.** The plan writes `toPassiveIngestRow(x: Extracted)` without saying how the type crosses the layer. Options were duplicating a 10-field structural interface in `shared/` or importing the real one; `verbatimModuleSyntax: true` guarantees `import type` is erased, so the import costs nothing at runtime (IIFE or bun:test) and cannot drift. `Extracted` is therefore exported. **(c) Gate order is tighter than the plan's.** The plan says the record gate is `enabled && isHomeTimelinePath && !isHarvestActive()`. That is necessary but not sufficient: `applyBand` runs for EVERY article on EVERY rAF-coalesced mutation burst, so `recordPassiveHarvest` also checks the pending map and the 30-min throttle — off ONE `findPermalink` querySelector — **before** calling `extractArticle` (~8 querySelectors). After a tweet's first sighting the throttle is the hot path for the next 30 minutes. Don't reorder these to "simplify". **(d) Two permalink readers, deliberately.** The throttle/pending key is `findPermalink(article).tweetId` (content.ts) while the shipped row's `tweetId` comes from `extractArticle`'s `idFrom` (harvester.ts). Both take the article's first `a[href*="/status/"]`, so they agree; they are separate because the cheap gate must not pay for the full extract. A later task that unifies them must keep the cheap reader on the gate path.

- **D94a** (HV.3 bookkeeping): HV.2's recorded `090a8ac` was an amend-orphan; real HV.2 = **`661d039`** (= HEAD at HV.3 start, same `(HV.2)` subject `feat(extension): passive home-timeline harvest capture with settings opt-out`, parent `e49f705` = the out-of-skill docs refactor D93a already covers). **No out-of-skill work landed since; ledger no drift.** The orphaned sha in the codemap header + §11's HV.2 line stays as written (it names the same commit by subject) — only the ledger and `last-commit` were corrected. Same one-command check as always: `git log -1 --format='%h %s'`.
- **D94** (HV.3): **four divergences from the plan's Task-3 text, all additive; nothing in its checklist was skipped.** **(a) The task grew from 4 files to 9, because the status line needs the typed api client and the panel had no `Settings`.** The plan says "prefer the existing `api.ts` request path", but every `api.*` method takes `Settings` and `HarvestPanel` was the one tab component App.tsx rendered **prop-less**. So: `HarvestRun` added to `shared/types.ts` (server-response types live there — the `SettingEntry` precedent — not in `shared/harvest.ts`), `api.harvest.runs(s,{limit?})` added next to `api.voice`, `HarvestPanel({settings})` + the one-line App.tsx call site. The alternative (a bare `chrome.runtime.sendMessage` ApiRequest, as `harvester.ts::apiSend` does) would have kept the file count but put a second untyped client in a panel; not worth it. `harvestClient.ts` needed **no** edit at all (the plan's "only if" clause) — it forwards `options` verbatim over the port. **(b) Two drive-by fixes outside the edit list.** `useSettings`'s `chrome.storage.onChanged` filter in `sidepanel/storage.ts` was **missing `KEY_PASSIVE_HARVEST`** — HV.2 added the key to the `Settings` interface, `getSettings`, `saveSettings` and `patchSettings` but not to the listener's early-return, so toggling it in Settings never re-fired `getSettings` and the App-level `settings` object stayed stale until a reload. HV.3's status line is the first consumer that would have shown the bug, so it is fixed here (one line, D11-style universal correctness). And `.harvest-tuning .field` got `min-width: 0` — the tuning row went from two columns to three and flex children won't shrink below an `<input type=number>`'s intrinsic width without it. **(c) Three pure exports, not the plan's "if extracted" one.** The plan offered `passesMinViews` as a minimal option and treated the form parse as optional; both were extracted into `shared/harvest.ts` with its first-ever test suite, and `passiveRowsToday` was added beside them — the UTC-day boundary must match the server's `utcDayStart`, and a local-midnight window would silently read yesterday's run for part of every day, which is exactly the kind of wrong that never announces itself. 15 tests. **(d) The numeric form fields persist as raw digit STRINGS (`maxStr`/`minViewsStr`), not numbers.** That is the shape the panel actually holds; storing numbers would make a blank Min-views box round-trip as `0`, which `passesMinViews` treats as "no floor" today but would be a live landmine the moment anyone reads the stored form as a number. `parseHarvestForm` therefore keeps only `/^\d*$/` strings and blanks anything else.

- **D95a** (HV.4 bookkeeping): HV.3's recorded `4c2adcf` was an amend-orphan; real HV.3 = **`5cd316a`** (= HEAD at HV.4 start, same `(HV.3)` subject `feat(extension): harvest tab options — csv toggle, min-views, persisted form, passive status`, parent `661d039` = HV.2). **No out-of-skill work landed since; ledger no drift.** Corrected in the ledger, `last-commit`, and BOTH codemap places (header + §11's HV.3 line — unlike D94a, which left them; naming the same commit by a dead sha is still a sha a future session has to re-derive). Same one-command check as always: `git log -1 --format='%h %s'`.
- **D95** (HV.4): **four calls the plan's Task-4 text doesn't pin down, plus one it got backwards.** **(a) Ranking is distinctDays desc THEN sightings desc — and that ordering is the product decision, not a detail.** The plan states it, but the reason belongs in the record: one viral thread re-scrolled twenty times in an afternoon would otherwise out-rank an author the algorithm fed you on four separate days, which is the exact opposite of "affinity". The floor (`minDays`) and the sort key are the same idea applied twice. **(b) A retired person reads `stage: null` but `inRoster: true`.** The plan says "people rows by `lower(handle)` (stage, skip `retired`)" — skipping them outright would make every retired handle read as an unknown discovery and re-nag "Start their file" forever, which is precisely the outcome retiring someone expresses a preference against. So the stage is dropped (a retired person has no *current* stage) but membership is kept; the drawer then renders neither a chip nor a CTA for them. `inRoster` answers "does stratus already know this handle", and a retired row is a yes. **(c) The `lower(handle)` join wrapper was dropped as dead weight** — `parseIngestRow`, `normalizePersonHandle` and the voice scrape all lowercase on write, so both sides are already lowercase and a `lower()` would only cost the index. Commented in-file so nobody re-adds it. **(d) `intParam` is a new shared helper, not three inline blocks.** `days`/`limit`/`minDays` all want the same rule the `GET /harvest/runs` limit already implements — absent → default, out of range → **clamped**, anything that isn't a positive integer → **400** with the caller's own code. The 400 (rather than a silent fall-back to the default) is the load-bearing half: a typo'd `?days=1O` must not answer with a confident 30-day roster. **(e) No `count` field.** The plan's response shape is `{days, minDays, authors}` and it shipped exactly that — `authors.length` is the count, and the two echoed params are what make a clamped request self-describing. **Also worth stating:** own tweets are NOT filtered out of the corpus (decision 8 — filtering is analysis-time), so your own handle can appear in the affinity list if x.com/home feeds you your own posts; nothing stores a self-handle server-side today, so a filter would have cost a new lookup for a cosmetic win. Revisit only if it actually annoys.

- **D96a** (HV.5): the plan's Task-5 said "section 11" in `src/x/playbook.ts` — **§11 is AI.12's `buildModelEffectiveness`**, so the funnel landed as **§12**. Any later plan text counting playbook sections is off by one from AI.12 onward; the next `build*` is §13.
- **D96b** (HV.5): the plan's return shape was `{hot: {...}, warm: {...}}`. Shipped instead as **`{cells: FunnelCell[], totalSeen, totalReplied}`** in `TIMELINE_BAND_ORDER` (`hot, warm, skip, null, 'unknown'`), matching every other section in the file (latency/model/band cells) and, more importantly, keeping the honest denominator visible: `skip`/`null`/`unknown` rows ARE the tweets the algorithm showed me and I passed on. A `{hot, warm}` object would have hidden them and made the funnel a tautology. The panel and any future digest fact read `cells.find(c => c.band === 'hot')`.
- **D96c** (HV.5): the plan said "null tweetTime → null band = excluded". **Not excluded — bucketed as `'unknown'`**, its own key in the `TimelineBand` union, never folded into the real `null` band (which means "classified, not worth a reply"). Same discipline as `LATENCY_BUCKETS`' `unknown` and `ROSTER_BANDS`' `unknown`. Silently dropping those rows would shrink the denominator by exactly the articles whose `<time>` didn't render — a systematic, invisible bias.
- **D97** (HV.5, bookkeeping — supersedes the sha half of D93a/D94a/D95a): **the ledger records the commit SUBJECT LINE, not a sha.** Three tasks in a row wasted a Step-0 investigation proving a recorded sha was an amend-orphan, and every time the resolution was "the subject line is the identity". A sha can only be written into the commit that changes it by amending, which changes it again — the churn is structural, not carelessness. From HV.5 on: ledger entries carry the subject line + the PARENT sha (stable, already in history), the codemap header stamps `<parent>+<TASK>`, and Step 0 is `git log -1 --format='%h %s'` compared against the last ticked subject. No reconciliation, no orphan hunts.

- **D98** (HV.6): **three places the plan's Task-6 text and the current repo disagree, all resolved in the repo's favour.** **(a) The phase entry went to `docs/PHASE-HISTORY.md`, not CLAUDE.md** — Task 6 literally says "CLAUDE.md: one phase-style entry", but D93a moved the whole ledger out of CLAUDE.md on 2026-07-23; CLAUDE.md now changes only when a guardrail changes, and Harvest 2.0 changed none. **(b) `PLAN.md` got a top blockquote, not a §"Phased build" edit.** The plan said "PLAN.md status updated"; §"Phased build" is the frozen Phases-1–10 ledger, and every post-Phase-10 phase since Niche N0 has landed as a `>` blockquote in the stack above §"Product, in one paragraph each" (RU.10 is the exemplar). Same place, same style, `See docs/PHASE-HISTORY.md §"Harvest 2.0"` instead of RU.10's `See CLAUDE.md §…`. **(c) The smoke does NOT "delete every seeded run/row on exit" literally.** It cannot: `POST /harvest/passive` find-or-creates *today's* passive run, which on a live box is the user's real one holding real browsing rows. Deleting it would destroy live data. Shipped instead as: every written row carries an **888-prefixed tweet id** (no real snowflake starts there) and every directly-seeded run carries an **`hv6_*` handle** (no real harvest produces one), so cleanup's `like`/`inArray` are exact; a run the route *created* for the smoke is deleted, a **pre-existing** run is kept with its `row_count` **recomputed from the surviving rows** (self-healing after an aborted run — better than restoring a remembered number). Any future smoke that writes into a table live browsing also writes must copy this shape.

## Gotchas log

Things the next implementer must know that aren't obvious from the code. Append-only,
one line each, newest last.

- **`bun run lint` was 114–119 pre-existing errors at baseline** — mostly the `Stratus Design System/` reference folder. **UI.8 (D11) added that folder to biome ignore → whole-repo now 7 errors**, all pre-existing and outside any changed file (2 a11y `Replies.tsx`, 5 format on generated `migrations/meta/*.json`). Per-task gate stays "the changed files lint clean" (`bunx biome check <files>`), not "whole-repo green"; don't chase the remaining 7 unless your task owns those files.
- **`bun test` has 2 pre-existing failures** in `src/x/routes/brief.test.ts` ("brief quests (C9)") — full suite 624 pass / 2 fail, identical before and after UI.8. Unrelated to CSS/settings; a Wave-1 or brief-owning task should investigate. Don't read "2 fail" as a regression. **RESOLVED (2026-07-18 Wave-0 audit):** the failures were never flaky tests — bare `bun test` (without `bun run test`'s `SQLITE_PATH=:memory:`) opened the REAL `./stratus.db`, so `onConflictDoNothing` seeds kept stale dates from prior runs. `src/db/client.ts` now defaults to `:memory:` when `NODE_ENV === 'test'` (Bun sets it under `bun test`), and the local stratus.db was purged of accumulated test junk (backup `stratus.db.backup-20260718-121416`; tokens table was empty — no real data existed locally). 730/730 green both ways now.
- **UI.8 design-token conventions:** `styles.css` `:root` = `--strat-*` set + legacy short aliases (`--bg`,`--accent`,`--status-*`…, all mapping to `--strat-*`) — the 2.4k-line sheet still resolves through the aliases; don't rip them out until a full migration pass. Alpha fills use `color-mix(in srgb, var(--strat-*) N%, transparent)` (exact-equal to the old rgba, tracks the base for UI.9's light theme) — a pre-existing `color-mix` at ~line 2051 already used the pattern, so it's sanctioned. `#fff`→`white` keyword. Fonts load from absolute `/fonts/Inter-*.woff2` (public/ → dist root; same files Studio uses) — no `sidepanel.html` change needed. `color-scheme: dark` stays until UI.9 adds `:root[data-theme='light']`.
- **ST.1 / compose.ts pattern engine:** `patternCoords(pattern,w,h,spacing,seed)` returns box-local coords; `dots|grid|diagonal|plus` share one top-left-first lattice (marks at `spacing/2 + k·spacing`, `< w/h`), `blobs` returns seeded-random `{x,y,r}` (count = `floor(w/spacing)·floor(h/spacing)`, 3 PRNG draws/blob in x,y,r order). `mulberry32` is the only sanctioned RNG in the Studio — Math.random is banned (determinism = the preview-IS-artifact contract). `path` layers author SVG in a 100×100 viewbox scaled into `box`; strokes divide lineWidth by mean scale. Downstream S5 tasks (ST.3 mascot, ST.4 patterns) build on these.
- **UI.1 settings platform landed INERT.** The routes (`GET /x/settings`, `GET /x/settings/values?scope=`, `PATCH /x/settings`, `POST /x/settings/reset`) are always-mounted (`settingsRouter` next to `data`/`explorer` in `mountX`) and fully working, but **no consumer reads the store yet** — pure modules stay pure and take params (Decision 6); UI.2–UI.7 wire brief/quests/people/gates/band/budgets through `getSetting`. Only the `doctrine` group is seeded (D13). Store cache is a module-level `Map` invalidated on every write; PATCH validates ALL keys before a one-txn upsert (all-or-nothing). Value column is JSON (`text {mode:'json'}`) so numbers/booleans/strings/arrays round-trip uniformly. `app_settings` = DB table #28; migration `0013_silent_nova.sql` (no seed INSERT — defaults live in the registry).
- **Sha-stamp trap (learned at UI.9, applies to EVERY task):** the codemap header + STATE `last-commit` embed the task's commit sha, but a commit can't contain its own hash — if you `commit --amend` to inject the sha, the amend produces a *new* sha and orphans the one you just wrote (this is precisely why UI.1's recorded `7e07dd5` vanished and forced a sync). **Don't chase convergence.** Stamp the sha of the just-created commit, amend once, and STOP — accept a possible one-commit lag between the stamp and the true post-amend HEAD. The Step-0 staleness guard runs `sync` on any mismatch, so a one-off lag is harmless by design. Prefer: commit → read sha → stamp → amend → done (no re-verify loop).
- **UI.9 light theme is an OPENING-GUESS palette, unverified in a browser.** Automated gates pass (632 pass / 0 fail incl. 6 new storage tests, typecheck + build + changed-file lint green) but the 12-tabs × {dark,light} × {cozy,compact} visual QA matrix is explicitly manual and deferred to the Wave-5 polish passes (UI.12–16 done-whens). Known trap (plan risk note): AA contrast on warn/ok/band **text over tinted fills** in light — the fill tokens and semantic ink were re-authored dark-enough to double as text (`--strat-warn: #9a6a10`, band-hot `rgb(0,150,100)`, band-warm-text `#8a6100`), but spot-check before trusting. Adjust `:root[data-theme='light']` values, not the consuming rules.
- **UI.9 `uiScale` only affects inherited/`em` sizes, not px tokens.** The sheet is px-heavy (no rem), so scale is implemented as `body { font-size: var(--ui-root-size) }` — it shifts base body text and anything relative, but the many explicit-px component font-sizes don't scale. True global scaling would need a rem migration (out of scope; QA-adjust if the effect feels too weak). Theme resolution + normalizers are pure exports in `storage.ts` (`resolveTheme(pref, prefersLight)`, `normalizeTheme/Density/Scale`) — reuse them; `main.tsx` owns the `<html>` stamping (sync matchMedia best-guess to avoid flash, then storage-corrected, + storage.onChanged + matchMedia-change listeners).
- **`bun test` ran 626 pass / 0 fail at UI.1 finish** (HEAD `1299e43` + UI.1). The 2 `brief.test.ts` failures the UI.8 gotcha flags are shared-in-memory-DB **order-dependent flakiness** (§9), not a hard failure — they did not manifest here. Still worth a brief-owning task's attention, but "2 fail" ≠ regression.
- **UI.10 rail is now GROUPED.** App.tsx uses `TAB_GROUPS` (Operate/Author/Library/Learn/System eyebrow clusters), not the old flat `TABS` — a later task adding a tab adds it to a group. The vertical 104px rail's layout rules live at styles.css `.app`/`.topbar`/`.brand`/`.tab-group` **~L265+** (the plan's "rail/topbar ~L44-100" pointer is STALE — L44-100 is the UI.8 token block). `.topbar` is still the class name despite being a sidebar.
- **UI.10 primitives are INERT except `EmptyState`** (wired into App.tsx's not-configured branch — the "used by the shell" done-when). `Section`/`SubTabs`/`SettingRow`/`Slider`/`GearPopover` + `settingsClient.{loadSettingGroups,flattenSettings,patchSetting,resetGroup,resetKeys}` + `api.settings.{get,patch,reset}` are exported but UNWIRED — first real consumers are UI.11 (Settings rebuild) + the Wave-5 inline gears (UI.12–15). Per D7 this is foundational, not dead code; don't delete on an "unused" pass.
- **UI.10 brandmark reuses `/icons/icon128.png`** (byte-identical to DS `assets/logo.png`, md5 `d06f9c20…`) — no separate logo asset added; the plan's "copy logo.png → public/icons" was a no-op since icon128 already sits there and serves from dist root.
- **Settings types are extension-local mirrors** — `SettingEntry`/`SettingsGroup`/`SettingsResponse`/`SettingsPatchResult`/`SettingsResetResult` in `extension/src/shared/types.ts` (re-exported via api.ts), mirroring the `GET /x/settings` JSON. The extension MUST NOT import `src/x/settings/registry.ts` (§5 build-isolation). **Binds UI.2/UI.11:** if the server `SettingDef` shape gains a field the editor needs, hand-sync it into `SettingEntry`.
- **`exactOptionalPropertyTypes` pass-through pattern (UI.10):** primitives forwarding a maybe-undefined optional (`Slider.unit`, `SettingRow.onReset`, `GearPopover.onReset`/`label`) type the prop as `T | undefined` explicitly — else TS rejects `prop={maybeUndefined}`. Reuse this for new primitives that relay optionals.
- **UI.10 rail regrouping is UNVERIFIED IN A BROWSER** (like UI.9's palette). Extension typecheck + Vite build + changed-file lint are green and the CSS compiles, but the visual QA (grouped rail spacing, brandmark, EmptyState, popover) rides the Wave-5 polish passes (UI.12–16 done-whens), per D7/D14.
- **ST.2 registry contract (the S5 template-authoring API):** `extension/src/sidepanel/studio/registry.ts` owns `TemplateId` (union), `TEMPLATES` (metadata rows), `templateMeta(id)`/`supportsAiBackground(id)` helpers, `EMPTY_STAT`, the `TemplateState` interface (every per-template input the shell holds), and `buildSpec(id, state, kit) → RenderSpec` (a switch, exhaustive on the union). To add a template downstream: (1) extend the `TemplateId` union + add a `TEMPLATES` row (set `supportsAiBackground`), (2) add the template's inputs to `TemplateState` + a `case` in `buildSpec`, (3) add a field component to `fields.tsx` + one `{template === 'x' && <XFields .../>}` line in the shell's JSX. `supportsAiBackground(id)` **replaced the old `BG_TEMPLATES` set** — use it, it reads the metadata row. Card size is now `{w,h}` (object), not the `"1200×675"` string — display is `${size.w}×${size.h}`; save/download read `.w`/`.h` directly (always finite, so the old `Number.isFinite` guards are gone). **ST.2 is behavior-neutral & UNVERIFIED IN A BROWSER** — extension build+typecheck+lint green, `templates.test.ts`/`brandKit.test.ts` untouched (142 ext tests pass); pixel-identical rendering is asserted by construction, not yet eyeballed (rides ST.9's browser walk).
- **`StudioSeed` stays exported from `Studio.tsx`** (the shell) post-ST.2 — App.tsx's `import { StudioPanel, type StudioSeed } from './Studio.tsx'` is unchanged. ST.7 adds `template?: 'quote' | 'thread'` to `StudioSeed` there (still in the shell), not in registry.ts.
- **ST.3 mascot is BROWSER-VERIFIED** (unlike ST.1/ST.2 which deferred to ST.9): rendered the real `mascotLayers` output onto a canvas via a throwaway HTTP harness — all 4 poses draw a clean lobed cloud, the stat pose-flip is visible (celebrating has confetti+arm-puffs, idle is a plain happy cloud), quote/stat/banner placements are collision-free. The hand-authored `CLOUD_PATH` + face arcs are validated. ST.9's full-Studio browser walk still owns the in-panel toggle/preset re-skin check, but the geometry is no longer a risk.
- **ST.3 pattern — mascot is `path`-only + one shared box (S5.3):** every mascot feature (body, eyes, mouth, arms, confetti, thought-trail) is a `path` layer sharing the SAME `box: {x, y, w:100*scale, h:80*scale}` 100×100 viewbox — `circlePath(cx,cy,r)` builds filled dots as SVG arcs, so there's no per-feature canvas math and the whole thing is one coordinate system. Sleeping adds one `text` "zzz" (plain text — repo no-emoji rule §7.31). Colours are ALL `shade(kit.accent, …)` (body fill 0.85, face ink −0.55), so the mascot re-skins for free when ST.4's presets change the accent — **do not hardcode a mascot hex.** Templates guard with `if (kit.mascot && !data.background …)` — the byte-identity regression tests (`mascot:false` === pre-task) lock this; keep it when ST.5/ST.7/ST.8 add cards that should carry the mascot.
- **`BrandKit.mascot` is a REQUIRED boolean field now (default true).** ST.4 rewrites `brandKit.ts` for multi-preset storage (`studio:brandKits`) + built-in starter presets (`Midnight`/`Paper`/`Neon`) — each preset constant and the lenient multi-parse MUST carry `mascot` (fallback true), or a preset load drops the field and typecheck breaks. The 3 full-kit test literals (`templates.test.ts` mascot:false, `brandKit.test.ts` mascot:false, `mascot.test.ts` mascot:true) are the only other places a kit is spelled out in full.
- **ST.4 landed: `loadBrandKit`/`saveBrandKit` (single-kit) are GONE.** Storage is now the bundle API: `loadBrandKits()`/`saveBrandKits(bundle)` + `activeKit(bundle)` for the current kit. `loadBrandKits` migrates a legacy `studio:brandKit` value into `kits.default` on first load, else seeds `STARTER_KITS`, and keeps writing the legacy key for the active kit (rollback safety). The pure preset mutations (`patchActiveKit`/`setActivePreset`/`savePresetAs`/`renamePreset`/`deletePreset`/`canDeletePreset`) never touch chrome.* — the shell wraps them in `applyBundle(fn)` which persists. `parseBrandKitsFile` (import) accepts BOTH the multi bundle AND a legacy single-kit JSON.
- **ST.5 celebration cards suppress the mascot on null data.** `milestoneCardSpec`/`streakCardSpec` render a graceful placeholder ("no milestone crossed yet" / "no streak yet — show up today") when the value is null AND drop the celebrating mascot (nothing to cheer). The `templates.test.ts` blocks lock this (null → no `path` layer). Both cards' data comes resolved from the shell: **override beats auto** (a typed number wins over the account/streak read), and the milestone's `dateLabel` ("reached YYYY-MM-DD") only shows when the auto value is in effect (an override has no crossing date). `milestoneData`/`streakData` are `useMemo`-stabilized in `Studio.tsx` so their fresh-object identity doesn't retrigger the debounced render effect every render.
- **ST.5 streak start-date is derived, not stored.** `brief.quests.streak` only carries `{current, todayComplete}` — no start date — so `Studio.tsx` computes "since" as `today − (current−1) days`. If a future task adds a real streak-start to the brief, prefer it over this derivation.
- **ST.6 code card is measure-free by design — `MONO_ADVANCE = 0.6` is exact, not a guess.** JetBrains Mono is 600 advance units on a 1000 em, so a token at column `c` sits at `codeLeft + c·fontSizePx·0.6` with zero canvas `measureText`. That's WHY the mono font is bundled (the ratio must be identical across machines). `codeCardSpec` caps at 18 lines / 62 cols (`CODE_FONT_PX=22`, panel `{72,44,1056,588}`); over-cap → `⌄ trimmed` footer (U+2304, a symbol not an emoji — the repo no-emoji rule is unaffected). Token boxes are given a generous width (`text.length·fontSizePx+8`, ≫ 0.6 advance) so a single-line left-aligned token can never wrap/ellipsize even if the real glyph advance drifts slightly from 0.6. **ST.6 is UNVERIFIED IN A BROWSER** (like ST.1/2/4/5) — the in-panel pixel/column check rides ST.9's Studio walk; the layout math itself is asserted in `templates.test.ts` (column x-diff === colDiff·size·MONO_ADVANCE).
- **ST.6 fonts came from fontsource (jsDelivr `jetbrains-mono@latest` latin-400/700 woff2, ~21KB each), not the official JetBrains release.** They are the same OFL-1.1 JetBrains Mono, latin-subset (smaller = fine for code cards). If ST.9/QA finds a needed glyph missing, swap for the full official woff2. The two files are `extension/public/fonts/JetBrainsMono-{Regular,Bold}.woff2`; Vite copies `public/` → dist root, so they serve at `/fonts/…` and `chrome.runtime.getURL('fonts/…')` resolves (same path as Inter — no manifest/`web_accessible_resources` change; the side panel is an extension page).
- **ST.7 thread cover + list card are the two NEW background-capable templates** (bringing the bg-capable set to quote/banner/thread/list). `parseListItems(raw)` lives in `templates.ts` (exported): trim → drop blanks → strip a leading `\d+[.)]` or `-*•` marker **followed by whitespace** (`/^\s*(?:\d+[.)]|[-*•])\s+/`, so "3 lessons" with no punctuation stays whole) → cap 6. The list card's accent number disc is a `ring` with `width = 2r` (r=11,width=22 → filled disc radius 22), the same trick the pfp monogram uses; the digit ink is `contrastOn(kit.accent)`. The thread cover badge is `['a thread', '1/N']` (plain text, §7.31; N = `max(1, round(count))`). Both cards call `baseLayers` so patterns/AI-bg compose; the thread's thinking mascot is guarded `kit.mascot && !data.background`. **ST.7 is UNVERIFIED IN A BROWSER** (like ST.1/2/4/5/6) — the Composer→thread-cover seed handoff, the list render, and both cards' pixels ride ST.9's Studio walk; the pure spec math + `parseListItems` are bun-tested (55 tests in templates.test.ts). Seed routing lives in `Studio.tsx`'s effect (`seed.template === 'thread' ? 'thread' : 'quote'`), not the registry — component-level, covered by ST.9's browser check per the plan.
- **ST.8 chart card is the 9th and last S5 template — Lane C is code-complete but UNVERIFIED IN A BROWSER** (like ST.1/2/4/5/6/7). Pure spec + `chartData` gate/normalization are bun-tested (148 studio tests, 239 ext total, typecheck+build+changed-file lint green); the live 30-day follower curve, the real best-times heatmap, and the below-gate muting all ride ST.9's Studio walk. `heatmapCells` intensity uses **min→max** normalization over the sufficient set (NOT absolute 0..max) — the least-active measured cell reads at the 0.15 alpha floor, the most-active at 0.9; a flat measured set is uniform 0.5. If ST.9's browser check finds the heatmap too washed-out on a thin single-user sample, that's the knob to revisit (raise the 0.15 floor or switch to absolute normalization), QA-adjustable like the other ST card placements.
- **Studio browser-QA harness pattern (ST.9, reusable):** a `Bun.build({target:'browser'})` of a tiny entry that imports the real `compose`/`templates`/`registry` modules, served (HTML + bundle + `extension/public/fonts`) by a `Bun.serve`, driven via claude-in-chrome. Shim `chrome.runtime.getURL = p => '/'+p` so `fonts.ts` loads the served WOFF2s. `window.STUDIO.{gallery,determinism,copyPng}` on the page. **Clipboard write needs a real click + focused document** — a `javascript_tool` eval fails "Document is not focused"; inject a button and click it with the `computer` tool (the extension's Copy PNG is a click handler, so this reproduces the real path). The `.gallery(preset)`/`.determinism(id)` helpers were enough to verify fonts/mascot/presets/pixel-determinism without the unpacked extension. Recreate the harness for future Studio visual QA; do not commit it.
- **`bun test` ran 730 pass / 0 fail at ST.9** (HEAD `289ec2c` + ST.9). The 2 `brief.test.ts` "brief quests (C9)" failures were later root-caused at the Wave-0 audit (see the RESOLVED note above): bare `bun test` vs `bun run test` DB targeting, fixed in `src/db/client.ts`.
- **ST.4 patterns only reach background-capable templates (quote, banner).** `baseLayers(kit,w,h,bg,scrimAlpha,pattern?)` is the only pattern injection point and only quote/banner use it (stat/pfp call `background(kit)` directly). A new bg-capable card (ST.5/ST.8) gets the background segmented control **for free** if its `TEMPLATES` row sets `supportsAiBackground:true` — BUT its data interface must carry `patternKind?`/`patternSeed?`, its `buildSpec` case must forward `state.patternKind`/`state.patternSeed` (conditional-spread — `exactOptionalPropertyTypes`), and its spec must call `baseLayers(..., patternArg(data.patternKind, data.patternSeed))`. The bg segmented control (`gradient|dots|grid|diagonal|plus|blobs|ai`) is inline in `Studio.tsx` and gates on `supportsAiBackground(template)`; `bgMode` is the single source of truth, `patternKind` derives from it (don't add a second patternKind state — biome exhaustive-deps flagged it).

- **RU.1: the reply prompt now asks for THREE variants, one per angle** (extends/contrarian/debate each appear exactly once) — heading `## The three variants`. **The batch-slice trap is live:** `sharedVoiceBlock()` slices `REPLY_PROMPT_TEMPLATE` from `## Who I am` to `VOICE_BLOCK_END = '## The three variants'`; both moved together. N.4 (which substitutes `{{REPLY_PERSONA}}` inside that slice per D3) and AI.5 (retires slicing) MUST read the heading/constant as they are NOW, not as their plan quotes them.
- **`sharedVoiceBlock`/`batchReplyHead` are NOT exported** from `src/x/replies/prompt.ts` — the RU.1 slice-guard test asserts through the exported `buildBatchGrokInput([tweet])[0].content` (contains `Forbidden openers` + `solopreneur`, does NOT contain `## The three variants`/`{{TWEET_CONTEXT}}`/`{{IDEA}}` — a VOICE_BLOCK_END desync would leak all three). Reuse that indirection if N.4 needs to test the slice, or export the internals then.
- **`MAX_OUTPUT_TOKENS` is 520** (was 350) in `src/x/routes/replies.ts` — headroom for the 3rd variant; xAI doesn't count reasoning tokens against it (verified live at 350 for two). `REPLY_VARIANTS_SCHEMA` is unchanged (array already unbounded — no min/maxItems), so the parser accepts whatever count comes back; the prompt + enum do the steering (angle-count enforcement stays soft, per plan).
- **Stale comment left on purpose:** `src/x/db/schema.ts:319` still says "structured two-variant call" for the `variants` column — out of RU.1's edit-scope (schema.ts is RU.2/RU.3 hot-file territory). RU.2/RU.3 should fix it when they touch that file. **RESOLVED (RU.2):** now reads "three-variant call".
- **RU.3: the batch endpoint now returns 3 variants/tweet.** `BatchReply` = `{tweetId, variants: ReplyVariant[]}` (was `{tweetId,text,angle}`); `parseBatchReplies` requires ≥1 variant/entry (missing/empty `variants` → null; old flat `{id,text,angle}` shape → null). The `/replies/generate-batch` response items are `{tweetId, text, angle, variants}` where `text`/`angle` = `variants[0]` (primary, back-compat for un-updated panels — **RU.4** widens the extension's `BatchReplyResponse` + buffer to carry `variants`). `radar_drafts.variants` is now POPULATED (`buildRadarDraftRows` stores `r.variants`) — **RU.5**'s confirm reads it (falls back to `[{text:replyText, angle}]` when null). `maxOutputTokens=min(9000,200+n*420)`. **No band gate / no reply_drafts write** in the batch path (decision 6) — still deliberately lightweight. `buildRadarDraftRows`/`persistRadarDrafts` accept an optional `variants?` (private `BatchReplyRow` type in radar.ts); a caller passing only the primary stores `variants: null`.
- **`inspect.test.ts` fails 2/2 under bare `bun test`, GREEN under `bun run test` (the canonical, package.json script `SQLITE_PATH=:memory: bun test`).** Introduced by `906f2d3` ("eval 1", out-of-skill Wave-0 audit): `src/db/client.ts` defaults to `:memory:` when `NODE_ENV==='test'` (Bun sets it), but `src/x/data/inspect.ts:101`'s readonly connection still defaults to `./stratus.db` when `SQLITE_PATH` is unset — so under bare `bun test` the primary writes `:memory:` while the inspect readonly reads the file → seeded `people` rows invisible → the 2 LIKE-query tests get `[]`. **NOT RU.3 (bisected: 0 fail at `d1902e5`/ST.9, 2 fail from `906f2d3` onward; RU.3 touches none of inspect.ts/client.ts/the people table).** Full suite is **732/0 under `bun run test`** — always gate with `bun run test`, not bare `bun test`. One-line fix for a data-core/S1 task: make `inspect.ts` honor the same `NODE_ENV==='test'`→`:memory:` default as `client.ts` (or `client.ts` should also `process.env.SQLITE_PATH ??=` set it so both agree).
- **RU.2: `radar_drafts` + `reply_drafts` columns are INERT.** `reply_drafts.source` is stamped `'reply_master'` on the generate insert (radar rows will carry `'radar'` at RU.5 confirm); `radar_drafts.{variants,model,reply_draft_id}` are added but only `model` is written so far (via `persistRadarDrafts(…, result.model)`). ~~`variants` is `null` on every radar row — RU.3 must populate it~~ **RESOLVED (RU.3):** `buildRadarDraftRows` now stores `r.variants` from the 3-variant batch (null only when a caller supplies just the primary). `reply_draft_id` stays null until **RU.5**'s confirm endpoint. `classifyReplyOrigin` (RU.9) will read `reply_drafts.source` for the exact batch-vs-single split. Migration `0014_swift_rhodey.sql`.
- **N.1: `niches` table + `DEFAULT_NICHE`/store exist but are INERT** — no consumer wired (N.3 substitutes `{{PERSONA}}`/`{{BELIEFS}}` in the post prompt, N.4 `{{REPLY_PERSONA}}` in the reply prompt, N.5 threads doctrine). The rollback story: nothing behaves differently until those land. `src/x/niche/{defaults,store}.ts`, migration `0015_faulty_blazing_skull.sql`.
- **N.1: the persona/beliefs/replyPersona/description in `DEFAULT_NICHE` are STILL ALSO in the `.md` templates right now** — N.1 only *copied* them into `defaults.ts` (byte-exact); it did NOT remove them from `post prompt.md`/`reply prompt.md`. **N.3/N.4 do the removal** (replace the §1/§5/"Who I am" bodies with the placeholder tokens). ⚠️ **After N.3/N.4, extracting "§1 body" from the .md yields `{{PERSONA}}`, not the text** — so N.3/N.4's equivalence-test fixtures MUST be built from a copy of the original text (or from `DEFAULT_NICHE`), never by re-reading the .md. Do NOT add a permanent "defaults.ts == .md slice" test in N.1 for the same reason (it'd break at N.3).
- **N.1: the byte-exact defaults + seed were GENERATED, not hand-typed** (throwaway scripts, deleted). `defaults.ts`'s persona/beliefs/replyPersona were extracted from the `.md` (collect-lines-until-`---`, trim blank edges); the migration seed `INSERT` was generated from `DEFAULT_NICHE` with `'`→`''` escaping. To regenerate after an edit, reuse the same technique — do NOT hand-retype (smart quotes `’`, em/en-dashes, arrows `→`, Romanian diacritics `ș ț` are all live in the text).
- **N.1: the migration seed embeds real newlines + one in-string `;`** (in the description: "…for an audience; you build…"). Applies fine because the bun-sqlite migrator splits on `--> statement-breakpoint`, NOT `;`, and sqlite respects quotes — verified by the migration-boot byte-exact round-trip test (`niche.test.ts`). If you hand-edit the seed, keep the value single-quoted and rely on the breakpoint markers, never introduce a bare statement-terminating `;`.
- **N.1: `niche.test.ts` describe order is load-bearing.** The `migration boot: seed + backfill` describe reads the *pristine migrated* seed (builder row byte-exact + content_pillars backfilled `niche='builder'`); the later `loadActiveNiche / loadDoctrine` describe's `beforeEach` deletes+reseeds `niches` (afterAll restores builder). The boot describe MUST stay declared BEFORE the mutating one, and no other test file touches `niches`. `niches` now shows in the explorer/`x_list_tables` whitelist (not sensitive; no count assertion broke — `inspect.test.ts` checks names, not length).
- **N.3: the post prompt's §1/§5 bodies are GONE from the .md/TS literal** — extracting "§1 body" from either now yields `{{PERSONA}}`. The equivalence fixtures live in `src/test.test.ts` (`ORIGINAL_SECTION_1_BODY`/`ORIGINAL_SECTION_5_BODY`, independent copies that also pin `DEFAULT_NICHE.persona/beliefs` against drift) — N.4 must build its reply-persona fixture the same way (copy the original "Who I am" 3-bullet body from `DEFAULT_NICHE.replyPersona` or git history, never the .md). §1/§5-only builder markers for "biography gone" assertions: `Alteramens` and `Naval-derived`; do NOT use `Pitești`/`386`/`hospital` (§6/§9 still carry them — D28b).
- **N.3: persona/beliefs substitution is FIRST in `buildPostDraftInput`** — before PILLARS/winners/remix/pillar/idea — so later user-content substitutions can't inject an expandable `{{PERSONA}}` token. Keep that order if adding placeholders. The bit-for-bit proof technique (reusable at N.4/ME.3/AI.5): render fixed inputs to scratchpad before editing, re-render after, `diff` — byte-identical or you broke equivalence.
- **N.4: the reply prompt's "Who I am" body is GONE from the .md/TS literal** — extracting it from either now yields `{{REPLY_PERSONA}}`. The equivalence fixture is `ORIGINAL_WHO_I_AM_BODY` in `src/test.test.ts` (independent copy that also pins `DEFAULT_NICHE.replyPersona` against drift — never re-read the .md). AI.5 (retires slicing per D3) must build its parity fixture from post-RU/post-N *rendered output*, and its anti-drift test asserts against defaults containing the placeholder. `buildGrokInput`/`buildBatchGrokInput` both end in a trailing `opts?: {replyPersona?: string}` — new call sites (AI.5, ME.3-adjacent) thread niche/persona content through there, never by editing the template.
- **N.4: the batch bit-for-bit probe technique extends N.3's** — 5 fixed-input renders (single default/rich/override + batch default/rich) written to scratchpad pre-edit, re-rendered post-edit, `diff` all byte-identical. The single-render probe used `postedAt = now − 90min` so `relativeTime` stays "1h ago" across the two runs; reuse that trick for any prompt-equivalence task touching `renderContext`.
- **N.2: TWO test files now mutate the shared in-memory `niches` table** — the N.1 store suite (`src/x/niche/niche.test.ts`) AND the N.2 route suite (`src/x/routes/niche.test.ts`). Both create/activate/delete rows and BOTH restore the single active `builder` row in `afterAll` (bun runs test files sequentially, so file order doesn't matter as long as every mutator restores builder-active). The route suite uses throwaway slugs `nutrition-test-n2`/`fitness-test-n2`. A future niche-touching test (N.3/N.4 equivalence, N.6 pillar-ownership) MUST also delete its rows + reactivate builder in cleanup, or it can leave a non-builder niche active and skew `GET /x/niche` for a later file. `x_niche`/`mcp.test.ts` is robust regardless (GET /niche falls back to `DEFAULT_NICHE`, and the seed/restore is always builder), but keep the discipline.
- **N.5: a THIRD test file now mutates the shared in-memory `niches` table** — `src/x/routes/doctrine.test.ts` sets the active (builder) niche's `doctrine` JSON and restores it to `null` in `afterAll` (same discipline as N.2's two files). It also seeds a strictly-latest `account_snapshots` row (`2999-12-01` — later than playbook.test's `2999-01-01` so the `desc(snapshotAt) limit 1` picks 100 followers whatever the file order) + two throwaway `voice_authors` (`doctrine_in_test` 400 / `doctrine_out_test` 1000), all deleted in `afterAll`. **Assertion design worth reusing:** `GET /voice/targets` returns ALL in-band voice authors (incl. other files' rows), so assert `handles.toContain(mine)`/`.not.toContain(myOut)`, never exact length; `GET /brief`'s `replyQuota.target`/`week.targetReplyPct` depend ONLY on doctrine (not on snapshots), so those assertions are robust regardless of shared-DB state. **`targetBand`'s doctrine band is NOT cached** — each consumer calls `loadDoctrine()` at its call site; a test PATCHing doctrine sees the change on the very next request (no cache to bust), unlike the settings store's module-level Map.
- **N.6: pillars + channels are now niche-scoped.** `getActivePillars()`, `GET /pillars`, `GET /channels`, and the `GET /channels/:slug` room all filter to `ownedByNiche(activeSlug)` = `niche = active.slug OR niche IS NULL` (the `ownedByNiche` helper is duplicated — one copy in `pillars.ts`, one in `channels.ts`; keep them in sync). **`getActivePillars()` returns `[]`** (not `DEFAULT_PILLARS`) when a **non-`builder`** niche is active and has zero active pillars — that empty return is the ONLY trigger for the drafter's `409 no_pillars_for_niche` (`/posts/draft` + `/posts/reup`, before any Grok spend). `POST /pillars/draft` does NOT refuse on empty (it's how you make the first pillar). A NULL-niche active pillar would sneak into EVERY niche's set (via `OR IS NULL`) and break the drafter refusal — no code creates one today (POST stamps the active niche; the only direct `content_pillars` insert is the POST route + the migration seed which backfills `builder`), but a future direct-insert test must set `niche`.
- **N.6: `niche-ownership.test.ts` is the FOURTH niche-mutating test file** (with `niche/niche.test.ts`, `routes/niche.test.ts`, `routes/doctrine.test.ts`). It activates `nutrition-test-n6`, creates `nutri-pillar-n6`/`builder-chan-n6`/`nutri-chan-n6` + a calendar draft, and restores builder-active + deletes all its rows in afterAll. bun runs files sequentially and each mutator restores builder-active, so order doesn't matter — but any NEW niche-touching test MUST keep the discipline or it leaves a non-builder niche active and skews `getActivePillars()`/`GET /pillars`/`GET /channels`/`GET /niche` for later files.
- **N.6: `people.ts:554` icebreaker channel-overlap is NOT niche-filtered** (out of Task 6 scope, D31e). Reads all active channels directly for grounding — harmless in v1 single-niche; a future per-niche partition should filter it (add `ownedByNiche` there or route through a niche-aware channel loader).
- **N.6: `calendar.ts` POST `/posts/scheduled` pillar validation inherits the niche filter with NO code change** — it validates via `(await getActivePillars()).map(p=>p.slug)`, so a builder pillar slug is `invalid_pillar` while a custom niche is active. Locked by a test in `niche-ownership.test.ts`, not by calendar.ts code.
- **N.7: the Settings Niche card is UNVERIFIED IN A BROWSER** (same class of deferral as UI.9/UI.10 opening-guess palettes). All automated gates green (ext typecheck+build, changed-file lint, root typecheck, `bun run test` 774/0), but the plan's live done-when — create+activate `nutrition` → the Composer pillar dropdown empties (N.6 filter observable e2e), switch back → restores — needs an unpacked extension + live-token server, not scriptable here. N.9's `smoke-niche.ts` exercises the same server round-trip live; the panel wiring rides Wave-5 QA. **No new CSS** — reuses `.pillar-*` classes + inline flex-wrap for the 5 doctrine inputs (D6: minimal until UI.11 rebuild). The 5 doctrine inputs render the RESOLVED values from `GET /x/niche`'s `doctrine` (all present), and Save sends the full object only when any changed (D27c replace-semantics).
- **N.8: the wizard's 503-no-key test FORCE-UNSETS `XAI_API_KEY` (save→delete→restore in a finally)** so it NEVER makes a real ~$0.01 Grok call, even on a dev machine that has the key set. Any future test that exercises a Grok-gated route with a *valid* body must do the same, or `bun test` spends money locally. The two 400 tests (empty/oversize description) are pre-network by construction (validation before the key check), so they're safe regardless.
- **N.8: `POST /x/niche/draft` validates the description (400 `invalid_description`) BEFORE the runtime XAI key check (503)** — deliberate ordering so the $0 guards test without a key (§7.4 only requires *spend* to be last, which it is). If you add fields to the wizard body, keep all validation ahead of the `askGrok` call.
- **N.8: `parseNicheProposal` is strict on the niche/pillars (bad niche slug that can't coerce, empty required text, pillars≠3, over-length → null) but LENIENT on channels (a malformed channel is dropped, not fatal).** The single-slug coerce (`coerceSlug`) works for niche/pillar/channel slugs because `NICHE_SLUG_RE`===`PILLAR_SLUG_RE`. `NICHE_WIZARD_SCHEMA` deliberately carries NO `minItems`/`maxItems` (repo convention — the batch-reply schema comment says the same; strict xAI structured-outputs + array bounds is a rejection risk); the "exactly 3 / ≤5" enforcement lives in the parser + prompt steering. If a real niche wants more/fewer pillars, change the parser AND the schema description AND the extension's 3-pillar assumption together.
- **N.8: the wizard Create order is load-bearing — niche.create → ACTIVATE → pillars.create × N → channels.create × N.** The pillar/channel POST routes stamp `niche = loadActiveNicheSafe().slug`, so the new niche MUST be active before its pillars/channels are created or they'd be stamped onto the *previously* active niche (and the new niche would draft-refuse `no_pillars_for_niche`, N.6). Activating is a real app-wide side effect; the wizard captures the prior active slug and offers "switch back to {prev}". Don't reorder.
- **N.9: `scripts/smoke-niche.ts` ACTIVATES a niche against the real `./stratus.db`** (`bun run scripts/smoke-niche.ts` targets the file DB, not `:memory:`). Its sync `cleanup()` (a `niches` txn: deactivate-all → activate `builder`, then delete the smoke row + any seeded account snapshot) runs at start (leftover sweep), at every `fail()`, and at success — so a mid-run abort can't leave a non-builder niche active (which would silently skew every other route/suite reading the active niche). Any future smoke that activates a niche MUST copy this restore-builder-on-fail discipline. Verified non-destructive: builder was sole-active before AND after a full real-DB run. The smoke mounts 5 routers on one Hono at `/x` (niche/pillars/drafter/brief/voice) — Hono merges same-path `.route()` calls; no path collisions. Gate with `bun run test` (782/0), not bare `bun test` (the inspect.test.ts file-vs-:memory: trap still applies — see the RU.3 gotcha).
- **ME.1: `src/x/me/profile.ts` is PURE and INERT** — no DB import, no consumer wired (the loader is ME.2, the three prompt-tail injections are ME.3). Rollback story: nothing behaves differently until those land; an empty profile makes every render `''`. **The renderers take `RenderGoal[]`, not `MeGoal[]`** (goal + precomputed `progress`) — ME.2's loader must `resolveGoals(goals, latestFollowers, now)` before `renderMeContext`/`renderMeBrief`; `latestFollowers` comes from the latest `account_snapshots.followersCount` (null until the first daily pass → followers-goal progress is null). The `GET /x/me` per-entry `inWindow` flag = the exported `isEntryInWindow(e, now)` — reuse it, don't recompute the 7d/30d windows in the route or UI. `selectEntriesForPrompt` is active-only (filters `active` internally) + windowed + per-kind capped (pinned overrides both). Freshness windows (emotion 7d / event 30d, boundaries inclusive) + caps are opening guesses — revisit after ~30 days like the C1 stage thresholds. Gate with `bun run test` (802/0); the pure suite `profile.test.ts` needs no DB.

- **ME.2: `src/x/routes/me.ts` is wired (always-mounted, $0) but its OUTPUT is still INERT** — `loadMeContextSafe('post'|'reply')` exists and works, but NO draft pipeline calls it yet (ME.3 adds the three tail injections: `BuildPostDraftOptions.meContext?` in drafter, `PostContext.me?` server-stamped in `/replies/generate`, `meBrief?` arg in `buildBatchGrokInput`). So an empty profile changes nothing; a populated profile changes nothing until ME.3. **The me_* tables are exclusive to this feature** — `me.test.ts` wipes both in `afterAll` and seeds a year-3000 `account_snapshots` row (later than doctrine.test's 2999-12-01) deleted by exact-date match; any future me-touching test (ME.4/5/6) MUST clean up its rows + any snapshot it seeds, same shared-in-memory-DB discipline as the niche suites. **`GET /me/context?mode=` and `loadMeContextSafe` share `renderFor` — never fork the render** (ME.4's preview must call the endpoint, not rebuild the block). Gate with `bun run test` (805/0); `bun run scripts/smoke-me.ts` is the $0 real-DB check (surgical by-id cleanup — safe to rerun).

- **ME.3: the Me/profile block is now LIVE at all three draft-prompt tails** — post drafter (`BuildPostDraftOptions.meContext?` before guidance), reply single (`PostContext.me?` after relationship, before pillars/guidance), reply batch (`buildBatchGrokInput` opts bag `meBrief?` after guidance, once per batch). ALL server-stamped/server-loaded via `loadMeContextSafe`; `parseContext`/`parseBatchTweets` never accept `me` from a client (locked by a test). The single reply path stamps `ctx.me` BEFORE the `contextSnapshot: ctx` insert — so **ME.5's me-lift cell reads `contextSnapshot.me` present/absent** (posts have no control group — the drafter always injects — so replies are the measured surface, plan Decision 6). **Byte-identity is proven additively**, not by a stored pre-feature fixture: the tail injections are conditional appends skipped on empty, and the test asserts `warm === cold + '\n\n' + block` + `empty → cold` (templates untouched, so byte-sync + N0.3/N0.4 equivalence tests carry the .md side). **The live `contextSnapshot.me` stamp on a real draft is unverified in CI** (needs a Grok call — same gap as N0.4's `contextSnapshot.niche`, D29b); ME.5's smoke or production is the observable proof. Gate with `bun run test` (811/0), not bare `bun test` (the `inspect.test.ts` file-vs-`:memory:` trap still applies — RU.3 gotcha).

- **ME.4: the Me tab ships (`extension/src/sidepanel/Me.tsx`, `MePanel`).** `api.me.*` (get/context/addEntry/patchEntry/deleteEntry/addGoal/patchGoal/deleteGoal) is the client surface — **ME.5/ME.6 reuse it, don't re-add**. The tab **reloads after every mutation** (`api.me.get` + both `context` modes in one `Promise.all`) and drives the "not injected anymore" dashed dimming off the server `inWindow` flag — **never re-derive the 7d/30d windows client-side** (D39c, §7.27). Followers goals show "auto · from daily snapshot" (no value editor); mrr/custom PATCH `currentValue` on blur. Extension-only: **NO new tests** (Task-4 spec — no shared pure module; behavioral check is manual over the loaded-unpacked extension), suite stays **811/0**. Gates: root+extension typecheck, extension `vite build`, changed-file lint clean — but the **2 pre-existing `Replies.tsx` a11y lint errors** (C6 commit `61bf692`, untouched) make bare `bun run lint` non-green; scope lint to changed files, or a future RU-lane task fixes Replies.tsx (D39d). **Unverified in a browser** — no live-token server / unpacked-extension walk is scriptable here; the render is standard React over `api.me.*`, typecheck+build green. The plan's own "done when" (add an emotion → appears in preview → fades after 7d; followers auto-progress) needs the user's live session.

- **ME.5: the Me/profile layer is now MEASURED.** `GET /x/playbook` carries `meEffectiveness` (`buildMeEffectiveness` — posted+measured replies split by `contextSnapshot.me` present/absent; `withMe`/`withoutMe`/`totalMeasured`, lift gated n≥20/side; D40b), fed by `ReplyRow.hasMe` (mirrors `hasRelationship`, no new query). The digest facts carry `goals` (`loadGoals` — active `me_goals` × all-time-latest `account_snapshots` followers via `resolveGoals`, null when none; D40c). Extension: `Playbook.meEffectiveness` + `DigestFacts.goals?` types + a "Personal context" Playbook-tab section (silent-until-gated). **The first gated `meEffectiveness` cell is MONTHS away** (needs ≥20 measured replies per side, and the with/without split only accrues as drafts post through the ME.3-live pipeline — same as every lift in this repo). **Live proof still pending** (needs real posted+measured replies): today the cell renders with real n's but no lift number; the digest `goals` fact needs an active `me_goals` row + a getMe snapshot to show a non-null followers value. Suite **815/0** under canonical `bun run test`; the 4 ME.5-touched test files (playbook/digest, both pure+route) are 75/0 either gate. **ME.6/ME.7 note:** ME.6 adds `x_me`/`x_add_me_entry` MCP tools (17→18, `mcp.test.ts` asserts the count); ME.7 docs-sync should flag the still-pending live proofs above + add the `docs/playbook-tab.md` "Personal context" section.

- **ME.6: MCP is 19 tools now (NOT the plan's 18).** ME.6 adds `x_me` (curated, empty schema → `GET /x/me`) + `x_add_me_entry` (write, zod-enum `kind` → `POST /x/me/entries`, no goal write) from a base of 17 (N.2's `x_niche`), so `mcp.test.ts` asserts `names.size === 19` and `smoke-mcp.ts` expects all 19 (its list had been missing `x_niche` — fixed here). The ME.5 gotcha above says "17→18"; that undercount is superseded — it's **17→19** (D41b). **General lesson for MCP/route write-tool tests:** bun runs every `*.test.ts` in ONE `:memory:` process, so a write tool that inserts into a table another suite counts/empties needs cleanup — the `x_add_me_entry` round-trip `app.request`-DELETEs its `me_entries` row because `me.test.ts` asserts an empty profile. `ideas`/`people`/`scheduled_posts` writes are harmless (no emptiness assert). The MCP tests are `describe.if(authed)`, so this only bites when `API_TOKEN` is set: canonical `bun run test` = **815/0 without token, 817/0 with token** (both green). Gate with `bun run test`, not bare `bun test` (inspect.test.ts trap unchanged).

- **ME.7: the Me lane is CLOSED (docs-synced).** CLAUDE.md §"Me layer M1", the PLAN.md "Me layer M1" blockquote (Goal section, after Niche N0), `docs/me-tab.md` (ME.4) + the `docs/playbook-tab.md` "Personal context" section, and `plans/2026-07-16-me-profile.md` Status → shipped all live. `scripts/smoke-me.ts` re-verified green against the real `./stratus.db` (`SMOKE OK` — 4 entries, window flag, mrr 16% / followers null, both context blocks, surgical cleanup). **No source/schema/route/test touched** — a pure docs-sync, so `bun run test` is unchanged at **815/0** (817/0 with `API_TOKEN`); the D40d/D41c gate discipline (canonical `bun run test`, not bare `bun test`) still stands. The next Wave-1 work is the **AI-layer lane starting at AI.1** (OpenRouter, dep-free, **xhigh**).

- **AI.1: `src/openrouter/` exists but is INERT** — nothing imports it until AI.2 wires `askLLM`/`/llm` routes/AI settings. `askOpenRouter(opts)` mirrors `askGrok`: OpenAI chat-completions shape, structured outputs via `response_format.json_schema` (never xAI's `text.format` — §7.17), `usage:{include:true}` + `provider:{require_parameters:true}` ALWAYS on the body, `reasoning:{effort}` only for low/med/high. **Cost is `usage.cost` off the response — there is NO price table** (a 2xx with tokens but no `usage.cost` logs $0 + `console.warn`); `cost_events` platform `'openrouter'`, endpoint `/v1/chat/completions`. Soft `OPENROUTER_DAILY_BUDGET_USD` watchdog (default $1.00, log-only, `0` disables). `AskOpenRouterOptions.fetchImpl?` is a TEST SEAM (defaults to global fetch) — AI.2's askLLM may spread it through harmlessly; NO `promptCacheKey` (no OpenAI-compat field — grok-only). `DEFAULT_MODEL`/settings default both `anthropic/claude-sonnet-4.5`.
- **AI.1 lint trap (biome `useTemplate` + `noUnusedTemplateLiteral`):** do NOT split a long `console.warn/error` message across `+` with a plain-string tail — `useTemplate` flags the `+` and `noUnusedTemplateLiteral` flags a backtick part that has no `${}`. `costTracker`'s `template + template` (both interpolated) is fine; the clean fix for a >100-char message is ONE template literal on a single long line (biome won't force-break an un-splittable string past `lineWidth`). Same pattern will bite AI.2–AI.9's new console messages.
- **AI.1: AI.2's plan edit-list is STALE on the schema** — it says "add `appSettings` table + migration `0013_*`", but UI.1 already shipped both (`app_settings`, migration `0013_silent_nova`). Per D1, AI.2 REUSES them: no new table, no new migration, read/write via `src/settings/store.ts` (or a thin key-`'ai'` reader). AI.2 still adds `src/llm/{settings,ask,routes,index}.ts` + mounts `/llm/*` under the bearer guard in `app.ts` after `mountGrok` (bearer wiring in `app.ts` covers `/x/*`,`/cost/*`,`/grok/*`,`/mcp` — add `/llm`). Gate with canonical `bun run test` (**827/0** at AI.1; the bare-`bun test` `inspect.test.ts` file-vs-`:memory:` trap still applies — RU.3 gotcha).
- **AI.2: `src/llm/` exists — `askLLM` is the AI-layer entry point, INERT until a call site imports it.** `src/llm/ask.ts::askLLM(opts, {defaults})` dispatches grok↔openrouter (NOT through `src/settings/store.ts` — it reads its OWN blob via `src/llm/settings.ts::getAiSettings` over `app_settings` key `'ai'`, read-through no cache). Merge precedence **opts > DB AI settings > call-site defaults** lives ONLY here — a call site passes `{defaults: {temperature, maxOutputTokens, reasoningEffort, model}}` as its house floor; do NOT re-implement precedence per route. Grok default stays `grok-4.3` (askGrok owns it; askLLM passes no model on grok unless opts/defaults set one — Decision 10); openrouter model = `opts.model ?? settings.openrouterModel`. `LlmNotConfiguredError` (routes → 503) on a missing key — **NEVER silent-fallback to the other provider.** `fetchImpl` forwards ONLY to openrouter (test seam), `promptCacheKey` ONLY to grok. **The AI settings blob has NO key field — API keys stay in env** (`app_settings` is explorer/MCP-visible; §7.16). **AI.6 flips the §7.22 gate** `if (process.env.XAI_API_KEY)` → `if (llmConfigured())` for replies/drafter/voiceExtract and the pillars/digest/playbook runtime 503s → `llmProviderReady`-style; images stay xAI-only. **`/llm/settings` PATCH is a partial merge** (only present keys applied; `null` clears temperature/maxOutputTokens/reasoningEffort); picking openrouter without its key → 409 `provider_not_configured`. **Env-unset in tests = `process.env.X = ''`** (falsy), NEVER `delete` (biome `noDelete`) — the AI.1 client.test.ts pattern; the `''`-vs-absent distinction is invisible to the `Boolean(...)` readiness checks. Gate with canonical `bun run test` (**868/0**; bare `bun test` inspect.test.ts trap unchanged — RU.3 gotcha).

- **AI.3: the prompt registry is LIVE on reply-single + post drafting** — `loadPromptSafe('reply'|'post')` feeds the builders via `template` and the sha-8 cache key replaced the static consts (one-time xAI cache miss on deploy, by design). `registry.test.ts` **afterEach-wipes ALL `prompt_overrides` rows** — it's the only suite touching the table today, but any future suite (AI.4 routes) inserting overrides must clean up too, or prompt-rendering suites (test.test.ts equivalence, app.test.ts) could see a customized body. `PROMPT_SPECS` is a `Record<PromptKey, …>` over a const-tuple union — appending a key without its spec is a compile error (AI.5's safety net). The registry imports BOTH prompt modules for defaults; never import the registry FROM a prompt module (cycle). `bun run test` = **881/0** at AI.3.

- **AI.4: the prompt editor routes are LIVE** (`src/x/routes/prompts.ts`, `promptsRouter`, always mounted next to `pillars`). `GET /x/prompts` maps `PROMPT_KEYS`, so **AI.5's appended keys (`reply-batch`/`voice-extract`/`pillar-draft`) auto-appear in the list with no route change** — the editor covers a new prompt the moment its `PROMPT_SPECS` entry exists. `customized` is row-presence in `prompt_overrides` (structural, never a diff). `prompts.test.ts` **afterEach+afterAll-wipe ALL `prompt_overrides` rows** — like registry.test, it's a shared-`:memory:`-DB discipline (test.test equivalence / app.test / registry.test render prompts and must see defaults); any AI.5+ suite inserting overrides must clean up too. The bearer 401 case is NOT in this suite (shared `/x` middleware, app.test/mcp.test own it — D27b/D46b). `bun run test` = **893/0** at AI.4.

- **AI.5: the batch-slicing machinery is GONE from `replies/prompt.ts`** — `sharedVoiceBlock`/`batchReplyHead`/`VOICE_BLOCK_START`/`VOICE_BLOCK_END` deleted; `REPLY_BATCH_PROMPT_TEMPLATE` is a standalone literal generated byte-exact from the old assembly (it keeps the doubled `---\n\n---` seam the old head+trimEnd produced — do NOT "fix" that seam without regenerating xAI cache expectations; it's the production wire format). Editing the reply voice block now requires mirroring the edit into the batch template — the anti-drift test in `src/test.test.ts` fails loudly if you forget. `buildBatchGrokInput`'s opts bag is `{replyPersona?; meBrief?; template?}`; token-missing fallbacks preserve pre-token override behavior (posts appended with the `**The posts I'm replying to:**` header, steer appended with its header). The 7-fixture pre/post parity probe technique (scratchpad `pre.ts`/`post.ts`, diff) is the reusable proof for any future prompt-shape refactor.
- **AI.5: `bun run test` = 900/0.** New assertions ride existing files (registry.test.ts 5-key block, test.test.ts anti-drift + template overrides + extract tail render, app.test.ts 4 provider guards — the replies-route guards live in `describe.if(authed)` blocks that ALSO need `XAI_API_KEY` at run time for the mount, matching the pre-existing batch-guards pattern). No new test file, no new smoke (AI.13 owns `smoke-ai-layer.ts`). The bare-`bun test` inspect.test.ts trap is unchanged.
- **AI.5: all §7.22 gates still XAI-only on purpose** — `src/x/index.ts` mounts replies/drafter/voiceExtract under `if (XAI_API_KEY)`, pillars/draft + extract-winners still 503 `grok_not_configured` on a missing XAI key. With XAI set but settings provider=openrouter and no OR key, the migrated routes now 503 `llm_not_configured` via `llmErrorPayload` (correct refuse-before-spend). AI.6 owns the flip to `llmConfigured()` — don't half-flip earlier.

- **AI.6: the §7.22 gate is now `llmConfigured()` — mount for replies/drafter/voiceExtract on EITHER provider.** `src/x/index.ts`'s `if (process.env.XAI_API_KEY)` block → `if (llmConfigured())` (import from `../llm/index.ts`); runtime `!llmConfigured()` on pillars-draft (`pillars.ts`), extract-winners (`playbook.ts`), digest + icebreaker. **The pre-flight check is only "is ANY provider available"** — `askLLM` enforces the RESOLVED provider's key per request (throws `LlmNotConfiguredError` → 503 `llm_not_configured` before any network spend). So a stored provider=grok with only OR set: pre-flight `llmConfigured()` passes (OR set) but the call throws → caught. Verified: `app.test.ts` passes with ONLY `OPENROUTER_API_KEY` set (the `describe.if(authed)` batch/band guards need replies mounted → they'd 404 if the flip were wrong). `images.ts` deliberately still `XAI_API_KEY`-only (Decision 6). **The niche wizard (`niche.ts` `POST /x/niche/draft`) was NOT migrated** (still askGrok + XAI-only) — out of Task 6 scope; a future task owns it.
- **AI.6: registry now has 7 keys — `digest`+`icebreaker` defaults come from `src/x/digest.ts`/`src/x/people/icebreakers.ts` (both pure-ish).** Those modules export `DIGEST_PROMPT_TEMPLATE` (`{{FACTS}}` tail) / `ICEBREAKER_PROMPT_TEMPLATE` (`{{GROUNDING}}` tail) and substitute the token via a LOCAL `.split().join()` — they must NEVER import `renderPrompt` from the registry (the registry imports THEM = cycle; same rule as AI.5's `voice/extractPrompt.ts` and `replies/prompt.ts`). Verified digest.ts pulls only `conversion.ts`(runtime, no registry)/type-playbook/type-grok, icebreakers.ts only types. `buildDigestInput(facts, template?)` / `buildIcebreakerInput(grounding, template?)` default the template to the export → byte-identical to the old concatenation (parity tests in `digest.test.ts`/`icebreakers.test.ts` assert `.endsWith('\n\nFACTS:\n{{FACTS}}')` etc. + a token-less-template append fallback). **registry.test.ts:60 asserts `v.unknown===[]` for every default** — a new prompt's default body must have NO stray `{{A_Z}}` token (the JSON examples `{"narrative":...}` use SINGLE braces, so they're clean; a `{{…}}`-looking literal in a new prompt body would fail this).
- **AI.6: three no-key tests unset BOTH env keys now** (`digest.test.ts`, `icebreakers.test.ts`, `playbook.test.ts` extract-winners) — `save+set-''`/`delete` + restore for `OPENROUTER_API_KEY` alongside `XAI_API_KEY`, because `llmConfigured()` checks both. A future test that forces "LLM not configured" MUST unset both, else an OR-configured machine slips past the pre-flight into `askLLM`. **Digest degrade string is `llm_not_configured`** (renamed from `grok_not_configured`, + `Digest.tsx` matcher); the icebreaker/pillars/playbook pre-flight 503s KEEP `grok_not_configured` (stable extension matchers, plan renamed only digest). `bun run test` = **902/0** (900+2 parity tests); `bun run scripts/smoke-c9.ts` (both keys off) = SMOKE PASS. Gate with `bun run test`, not bare `bun test` (inspect.test.ts file-vs-`:memory:` trap unchanged — RU.3 gotcha).

- **AI.7: the thread drafter is LIVE at `POST /x/posts/draft-thread`** (on the existing `drafter` router → inherits the AI.6 `llmConfigured()` mount gate; **no index.ts change**). New pure module `src/x/posts/threadPrompt.ts` (`THREAD_PROMPT_TEMPLATE` byte-synced with `thread prompt.md`, `buildThreadDraftSchema`, `parseThreadDraft`, `buildThreadDraftInput`) imports ONLY types/consts from `posts/prompt.ts` — no cycle, so the registry can import it for the `thread` default. **Registry is 8 keys now** (`thread` inserted at index 3, after `post`) — `registry.test.ts` pins the exact `PROMPT_KEYS` array + `isPromptKey('thread')===true`; **`prompts.test.ts` swapped its `thread` "unknown key" fixtures → `not-a-prompt`** (D50). **The thread schema has NO minItems/maxItems** (D49b — strict-structured-output rejection risk); count floor (≥2) is in `parseThreadDraft` (null below 2), and a bad pillar → null (not fallback — the plan's "bad pillar → error", unlike `parsePostDrafts` which falls back). **`insertThreadDraft(pillar,tweets,ideaId?)` is exported** for the route test (calendar thread insert shape: head `draft`+tails `segment`, shared threadId, `source='drafter'`, unscheduled). One auto-regenerate on a >280 segment (cost summed, still-long → 502 `thread_invalid`). Extension: `api.drafts.thread` + the Composer drafter-button `threadMode` branch → `onEdit(head.id)`. **Live done-when tails unverified** (need a browser + a real LLM call): the "Draft thread with AI" → thread-edit load, and a real Grok/OpenRouter thread draft that lands rows — same class of deferral as N.7/ME.4 UI. `bun run test` = **914/0**; extension typecheck + `vite build` + changed-file lint green. Gate with `bun run test`, not bare `bun test` (inspect.test.ts file-vs-`:memory:` trap unchanged — RU.3 gotcha). **Binds AI.8** (rewrite): it shares `Composer.tsx` — serialize; the button pattern (`threadMode ? draftThread() : ...`) is the exemplar for adding "Improve with AI".

- **AI.8: the rewrite assist is LIVE at `POST /x/posts/rewrite`** (on the `drafter` router → inherits AI.6's `llmConfigured()` mount gate; no index.ts change). New TS-only pure module `src/x/posts/rewritePrompt.ts` (`REWRITE_PROMPT_TEMPLATE` — **NO `.md`/byte-sync**, Decision 9 for the 6 smaller templates; `REWRITE_SCHEMA` static `{variants:[{text,kind}]}` kind enum `tightened|rehooked|restructured` **no minItems/maxItems**; `parseRewrite(raw, maxLen=560)→RewriteVariant[]|null`; `buildRewriteInput`). **Registry is 9 keys now** (`rewrite` at index 4, after `thread`) — `registry.test.ts` pins the exact `PROMPT_KEYS` array (8→9) + `isPromptKey('rewrite')===true`. The route uses **INLINE body validation** (not `parseCommon` — different body shape `{text,instruction,model,provider}`): `invalid_text` [1–2000], `invalid_instruction` [≤500], `invalid_provider`/`invalid_model`, all pre-spend; `parseRewrite` null → 502 `rewrite_parse_error`, `[]` → 502 `rewrite_invalid`. **No DB rows, no niche suffix on the cache key** (bare `prompt.cacheKey` — the prompt has no persona token). **No one-auto-regenerate** (D51c — plan doesn't ask; unlike the thread route). `MAX_VARIANT_LENGTH=560` cap DROPS over-long variants (D51b — revisit if long-form drafts lose all variants). Extension: `api.drafts.rewrite`, `shared/types.ts` `RewriteBody`/`RewriteVariant`/`RewriteKind`/`RewriteResponse`, an "Improve with AI" button under the single-post textarea → variant cards reusing `.draft-card` → `applyRewrite` sets the textarea. **Live done-when tails unverified** (browser + real LLM call). `bun run test` = **926/0**; extension typecheck + `vite build` + changed-file lint green. **`bun run lint` is 2-red on clean HEAD** (pre-existing `Replies.tsx` a11y, verified via `git stash` — NOT AI.8). Gate with `bun run test`, not bare `bun test` (inspect.test.ts trap unchanged). **Binds AI.9** (idea generator): same TS-only-no-.md prompt-module pattern (`rewritePrompt.ts` is the exemplar) + inline-body-validation; but AI.9 lands on the ideas router, reuses `topWinners()`/`getActivePillars()`, mirrors the `no_pillars_for_niche` 409.

- **AI.9: the idea generator is LIVE at `POST /x/ideas/generate`** (on the ALWAYS-mounted `ideasRouter` — **no §7.22 mount-gate change**, unlike thread/rewrite which ride the drafter's `llmConfigured()` mount gate; the runtime 503 comes purely from `askLLM`→`llmErrorPayload`, no explicit `llmConfigured()` pre-check, like `/posts/draft-thread`). New TS-only pure module `src/x/posts/ideasPrompt.ts` (`IDEAS_PROMPT_TEMPLATE` NO `.md`/byte-sync; `IDEAS_SCHEMA` static; `parseIdeaProposals`; `buildIdeasInput`). **Registry is 10 keys now — the full set** (`ideas` at index 5, after `rewrite`) — `registry.test.ts` pins the exact `PROMPT_KEYS` array (9→10) + `isPromptKey('ideas')===true`. **Two non-obvious calls:** (1) `ideas.ts` imports `topWinners` from `drafter.ts` while `drafter.ts` imports `consumeIdeaSafe` from `ideas.ts` — a deliberate two-way cycle that's SAFE because both are deferred function calls (never module-top-level); do NOT "fix" it. (2) `IDEAS_SCHEMA.pillar` is a FREE string (not a slug enum) so `parseIdeaProposals` NULLS an out-of-set pillar instead of the model force-picking — the idea survives its mis-tag (D52c). **Writes NOTHING** — the panel saves picked proposals via `POST /ideas` `tags:['ai']`; `no_pillars_for_niche` 409 before spend; `parseIdeaProposals` null → 502 `ideas_parse_error`, `[]` → 502 `ideas_invalid`. Bare `prompt.cacheKey` (no niche suffix — persona-free prompt). `count` folds into `{{STEER}}` + clamps the parse output. Extension: `api.ideas.generate`, `shared/types.ts` `IdeaGenerateBody`/`IdeaProposal`/`IdeaGenerateResponse`/`IdeaAngle`, a "Generate ideas with AI" section in `Ideas.tsx` → proposal cards (pillar+angle badges) → Save-to-inbox / Skip. **Live done-when tails unverified** (browser + real LLM call). `bun run test` = **935/0**; extension typecheck + `vite build` + changed-file lint green. **`bun run lint` is 2-red on clean HEAD** (pre-existing `Replies.tsx` a11y — NOT AI.9). Gate with `bun run test`, not bare `bun test` (inspect.test.ts trap unchanged). **Binds AI.10** (Settings AI subtab): the backend it configures (`/llm/*`, `/x/prompts/*`) is complete; AI.10 is extension-only UI.

- **AI.10: the Settings AI subtab is LIVE** — `Settings.tsx` now opens with a `radar-tabs` `General | AI | Prompts` shell (Voice.tsx `radar-tab` pattern; the tab bar is one thin `.panel` above the active view). **General is byte-unchanged** (`<NicheCard/>` + the old form, just wrapped in `view === 'general'`). **`<AiPanel>`, `<PromptsTab>`, `<RestoreDefaultPrompts>` are sub-components IN `Settings.tsx`** (not separate files) — AI.11 should replace `<PromptsTab>`'s placeholder body with `<PromptsPanel settings={settings}/>` and KEEP the `<RestoreDefaultPrompts>` mount (the user wanted restore "in settings", reachable from both the AI footer and the Prompts tab). **The api transport ignores the `Settings` arg** (background owns Authorization, §7.25) — `AiPanel` still needs a Settings object for the signatures, so `SettingsPanel` passes a `useMemo`'d `currentSettings` (memoized over the 8 primitive fields, else the on-mount `getSettings` effect refires every render → reload loop). **`/llm/models` is fetched only when `providers.openrouter` is true** (503 otherwise); a fetch error just leaves the `<datalist>` empty — free-text model entry still works. New pure `extension/src/shared/aiSettings.ts` (`aiSettingsToForm`/`aiFormToPatch`/`pricePerMillion`, 11 bun tests): **form fields are all strings** so blank is representable → `aiFormToPatch` maps blank numeric/effort to explicit `null` (clears the override) and rejects non-numeric temp/non-integer tokens client-side with the SAME `invalid_temperature`/`invalid_max_output_tokens` codes the server uses. `api.llm.{getSettings,patchSettings,models}` + `api.prompts.restoreDefaults` added (AI.11 extends `api.prompts` with list/get/patch/reset). 8 new `shared/types.ts` types (`AiSettings`/`AiSettingsResponse`/`AiSettingsPatchBody`/`LlmModel`/`LlmModelsResponse`/`LlmProvider`/`LlmReasoningEffort`/`PromptsRestoreResult`). **NO server change** — pure consumer of AI.2 `/llm/*` + AI.4 `/x/prompts/*`. **Live done-when tails unverified** (need a browser + running server): provider/model round-trip + a real Restore-Default-Prompts click. `bun run test` = **946/0** (+11 aiSettings); root+extension typecheck + `vite build` + changed-file lint green (**biome auto-sorted the import blocks** in Settings.tsx/api.ts — expected). **`bun run lint` is 2-red on clean HEAD** (pre-existing `Replies.tsx` a11y — NOT AI.10). Gate with `bun run test`, not bare `bun test` (inspect.test.ts trap unchanged). **Binds AI.11** (Prompts editor): same lane (owns Settings.tsx/api.ts), swaps the `<PromptsTab>` placeholder, extends `api.prompts`; `Pillars.tsx` is the edit-list exemplar.
- **AI.11: the Prompts editor is LIVE** — new `extension/src/sidepanel/Prompts.tsx` (`<PromptsPanel>` list → `<PromptEditor>`); `Settings.tsx`'s `<PromptsTab>` now renders `<PromptsPanel settings={settings}/>` + a footer `<RestoreDefaultPrompts>` (both AI-footer and Prompts-tab still reachable). `api.prompts` gained `list/get/patch/reset`; `shared/types.ts` gained `PromptSummary`/`PromptDetail`/`PromptPatchResult`/`PromptResetResult`; `styles.css` gained a `.prompt-*` block (list/row/key/token/body/meta — tokens only, no literals). **No new tests** — the required-placeholder mirror is an inline `missingPlaceholders(body, required)` (`required.filter(t => !body.includes(t))`, exactly `validatePromptBody`'s check); Save greys out while any required token is missing, the server still 400s `missing_placeholder`. **`api.prompts.patch` returns only `{customized, unknownPlaceholders}`** (no body/updatedAt) — the editor sets `customized:true` locally after Save and refetches the list on Back so the chip + edited-at stay accurate. **RESOLVED an AI.8 bug (D54):** `RewriteVariant` was imported by `Composer.tsx` from `./api.ts` but never re-exported there → `cd extension && bun run typecheck` (`tsc --build`) was RED from AI.8 on (hidden because the mandated extension gate is `bun run build`, which strips types); added `RewriteVariant` to api.ts's import + re-export blocks — **extension typecheck is green again for AI.12+**. Gates: `bun run test` **946/0** (no new tests), root+extension typecheck clean, extension `vite build` green, changed-file lint clean (the 2 `Replies.tsx` a11y errors are still pre-existing, NOT AI.11; my `Prompts.tsx` had 1 format error, biome-autofixed). **Live done-when unverified** (needs a browser + running server): edit `reply` → Save → chip appears → `GET /x/replies/default-prompt` reflects → Reset reverts. **Binds AI.12** (model-effectiveness cell — different lane: `src/x/playbook.ts` + `routes/playbook.ts` + `Playbook.tsx`, no Settings/prompts/Composer overlap; the extension typecheck is now clean so AI.12 starts from green).

- **AI.12: the Playbook model-effectiveness cell is LIVE** — `buildModelEffectiveness(rows: ModelRow[], minN=DEFAULT_MIN_CELL_N)` is §11 of `src/x/playbook.ts` (after idea payoff, before "feedback into generation"). It's the S0.5 `buildLatencyEffectiveness` copied but with **dynamic buckets** (a `Map` over the raw `reply_drafts.model` string — `grok-4.3`, `anthropic/claude-sonnet-4.5`; the `/` reads as provider, **never parsed**), **no lift line** (no baseline pair — buckets only, each independently gated n≥20), cells sorted most-sampled-first (`b.n - a.n || b.posted - a.posted || model.localeCompare`), `totalMeasured` kept for the partition invariant. `routes/playbook.ts`: `model` added to `ReplyRow` + the `loadReplyRows` select (`replyDrafts.model`, `notNull` — every posted reply has one) + the row map; new `toModelRows(rows)` beside `toLatencyRows`; wired into `GET /x/playbook` as `modelEffectiveness` (the `minN` knob flows through); `buildModelEffectiveness`/`type ModelRow` imported. Extension: `shared/types.ts` `PlaybookModelCell extends PlaybookCell {model}` + `Playbook.modelEffectiveness: {cells, totalMeasured}`, re-exported via `api.ts` (import + re-export blocks); a "Model effectiveness" section in `Playbook.tsx` (after latency, before RosterCoverage) — `cellSummary` table (model/posted/result), silent-until-gated `.pb-note`, "No posted replies yet" empty state. Tests: `playbook.test.ts` `buildModelEffectiveness` (bucketing / provider-slash-kept / most-sampled-first / partition Σn=totalMeasured & Σposted=rows / per-bucket gate / default-20-silent) + `routes/playbook.test.ts` (both seeded replies `model:'test'` → one bucket n=2 medianViews 250, partition invariant). Gates: canonical `bun run test` **952/0** (+6), root+extension typecheck clean, extension `vite build` green, changed-file lint clean (**the 2 `Replies.tsx` a11y errors are still pre-existing on clean HEAD, NOT AI.12** — I touched only Playbook.tsx/types.ts/api.ts on the extension side, no dialogs). **Live done-when unverified** (data-dependent, months away): a model bucket reaching n≥20 to render a real median — every test reply seeds `model:'test'`, and non-Grok rows only start existing once AI.10's Settings picks OpenRouter. **Binds AI.13** (the final Wave-1 docs+smoke task): the AI-layer docs-sync should list this cell.

- **RU.5: the radar confirm endpoint is LIVE at `POST /x/radar/drafts/:tweetId/confirm`** ($0, always-mounted on the `radar` router). It promotes the newest non-expired `radar_drafts` row for a tweet into a real `reply_drafts` row (`source='radar'`, `status='copied'`) in ONE sync txn (`db.transaction`, `.all()`/`.run()`, no await inside — §7.13) that also stamps `reply_draft_id` + ratchets radar status→`clicked`. **Idempotent** via the `reply_draft_id` soft link (linked row → 200; a dangling link rebuilds); fresh create → 201. `model` falls back to `'radar-batch'` (reply_drafts.model is NOT NULL); `variants` falls back to `[{text: replyText, angle}]` for pre-variant/CLI rows; `contextSnapshot` is a `PostContext` rebuilt from the row (band-column merged with the signals JSON, `postedAt = draftedAt − ageMin`, metrics from signals + reposts/likes 0, topComments []). **`GET /radar/drafts` now takes `?tweetId=`** (explicit status wins, else tweetId→non-expired, else ready default). **No `my_reply` event here** — that stays on the `PATCH /replies/:id` posted flip (RU.7); `copied→posted` is already an allowed transition. New route suite **`src/x/routes/radar.test.ts`** (7 tests) — DISTINCT from the extension's `extension/src/shared/radar.test.ts` (RU.4 buffer tests); both are named `radar.test.ts`, so reference them by full path. It seeds `radar_drafts` rows in `beforeAll` (tweet ids `991…`) and cleans up radar + created `reply_drafts` rows in `afterAll` (shared `:memory:` discipline). **`sourcePostedAt` serializes to an ISO string over `c.json`** (it's a `timestamp_ms` Date column) — a route test asserting it must expect a string, not a number. Gate with canonical `bun run test` (**971/0**), not bare `bun test` (the `inspect.test.ts` file-vs-`:memory:` trap still applies — RU.3 gotcha). **Binds RU.6/RU.7** — see D58's binds.
- **RU.6: the Radar panel row-click now confirms the draft server-side.** `Radar.tsx`'s `RadarRow.onOpen` fires `stratus/radar-confirm {tweetId}` (new `RadarConfirm` message + `isRadarConfirm` guard in `shared/messages.ts`) alongside the existing `markClicked` + clipboard copy of the primary. The background's `confirmDraft(tweetId)` POSTs `/x/radar/drafts/:id/confirm` (RU.5, idempotent) then `stampDraftId` writes the returned reply_drafts `id` onto the sighting through `enqueueRadar` (single writer, markClicked pattern — an evicted sighting is skipped). `RadarSighting.draftId?` survives `mergeSightings` like `reply`/`variants`/`clickedAt`. **Best-effort §7.8:** a failed confirm logs a warn (ignores `unconfigured`), the UX proceeds — the reply was already copied. The confirm returns the **full reply_drafts row directly** (`c.json(created,201)`/`c.json(existing,200)`), so `res.data.id` is top-level (no wrapper). `markDraftsOnServer([id],'clicked')` stays (a no-op PATCH on the already-clicked confirmed row). **First message-guard test suite** — new `extension/src/shared/messages.test.ts` (`isRadarConfirm`); none existed before. **Binds RU.7** (paste→posted): the stamped `draftId` is the row RU.7 PATCHes to `posted`; RU.7 also needs its own confirm-if-needed for deep links (D58 bind). Gate with canonical `bun run test` (**974/0**), not bare `bun test` (the `inspect.test.ts` file-vs-`:memory:` trap still applies — RU.3 gotcha).

- **AX.2: `extension/src/shared/glance.ts` is PURE and INERT** — `buildPersonChips`/`GlanceEntry`/`GlanceMap`/`NEGLECT_DAYS`/`GLANCE_TTL_MS` exist but NO consumer imports them yet (**AX.3** injects chips into the timeline via `applyPersonChips` in `content.ts` + fetches `GET /people/glance` with a `GLANCE_TTL_MS` module cache, channels pattern). **The chip contract AX.3 renders against:** `PersonChip = {kind:'stage'|'target'|'owed'|'neglected', label, tooltip, tone:'ally'|'mutual'|'responded'|'engaged'|'target'|'warn'}`, ordered stage/target/owed/neglected — the 6 `tone` values are meant to become CSS classes AX.3 adds in `injectStyles` (theme-neutral rgba, like the old badge). **Deliberate design choices worth keeping:** (1) `GlanceEntry.stage` is `string` not a `Stage` union — the radar `RankMapEntry` tolerance pattern (a looser client cache survives a server that grows a stage); an unknown stage → `indexOf` -1 → no stage chip, harmless. (2) The 6-element `STAGE_ORDER` is hardcoded — the plan FORBIDS importing server `stage.ts` into the IIFE; if the server ladder ever changes, hand-sync this const. (3) Neglect uses a **strict `<` timestamp cutoff** (`Date.parse(lastOutbound) < now − 7d`) mirroring `followups.ts` — exactly-7d is NOT neglected, 8d is (the boundary test locks this). (4) The null-outbound neglect branch requires `lastInboundAt` present (a dropped inbound) — a never-touched target with no inbound is deliberately NOT neglected, else the whole roster lights up. **Freshness `NEGLECT_DAYS=7` is an opening guess** (= `NEGLECTED_TARGET_DAYS`), revisit with the Targets amber. Extension-only, 13 bun tests; gate with `bun run test` (**988/0**), not bare `bun test` (inspect.test.ts file-vs-`:memory:` trap unchanged — RU.3 gotcha). **Binds AX.3** (timeline revamp — xhigh, opens the D9 content.ts chain) and is parallel-independent of AX.4 (tweetContext view-model).

- **AX.4: `extension/src/shared/tweetContext.ts` is PURE and INERT** — `buildTweetContextModel(dossier, tweetId, nowMs)` + the `Dossier`/`TweetContextModel` types exist but NO consumer imports them yet (**AX.5** does the status-page DOM plumbing: one dossier fetch per handle, 5-min module cache, renders this model into `div.stratus-context-panel`, collapse flag `chrome.storage.local['augment:contextCollapsed']`). **The model contract AX.5 renders against:** `{header:{handle,displayName,stage,sinceDays,followersCount,momentumPerDay,tags[]}, relationship:{inbound,outbound,lastInboundAt,lastOutboundAt}, alreadyReplied:{postedTweetId,ageMin}|null, openLoops:{tweetId,text,ageDays}[], outcomes:{text,views,profileVisits,angle,postedAt}[], anglePreference:{angle,measured}|null, notes}`. **The module consumes the C1 dossier as HTTP JSON — every timestamp is an ISO string, never a Date** (the fixture test literal encodes this; AX.5 passes `Date.now()` as `nowMs`, all ages derive from it). **Design choices (D63, worth keeping):** (1) relationship COUNTS come from `events` (their_mention+their_reply_to_me vs my_reply), last DATES from the `people` watermark columns; (2) `outcomes[].angle` is null in production today — the dossier route does NOT emit per-outcome angle (only the aggregate `angles` crosstab), so it's typed optional + read `?? null`; if AX.5 wants it on the panel, add it to the dossier `outcomes` map, no view-model change needed; (3) `anglePreference` re-implements `pickAnglePreference` inline (gate ≥3 = `MIN_MEASURED_FOR_ANGLE_PREFERENCE`, best median profileVisits, views tie-break) to keep the IIFE free of server imports — hand-sync if the server pick ever changes; (4) `momentumPerDay` mirrors `authorMomentum`'s `Math.max(days,1)` clamp so same-day snapshots don't read as explosive, null <2 points. Extension-only, 15 bun tests; gate with `bun run test` (**1003/0**), not bare `bun test` (inspect.test.ts file-vs-`:memory:` trap unchanged — RU.3 gotcha). **Binds AX.5** (status-page context panel — xhigh, deps AX.3+AX.4, part of the D9 content.ts chain).

- **AX.5: the tweet-page context panel is LIVE in `content.ts`** (`syncContextPanel`/`renderContextPanel`, once/scan beside `syncAuthorButton`). `dossierCache` is a `Map<handle,{state,dossier,at}>` — **all 3 terminal states are cached** (`ready`/`missing`/`unavailable`) precisely so a status page never fetch-loops through the `.finally`→`scheduleScan()` re-render (the ONE deviation from the timeline glance cache, which relies on constant mutations). The panel goes **as the focused article's next sibling** (`insertAdjacentElement('afterend')` — parent flow, survives article-node recycling), keyed `data-renderKey`=`state:tweetId:handle:cachedAt` + a `nextElementSibling`-anchor check so a rebuild fires on data refresh / SPA nav / lost anchor and a stale panel from a previous tweet is torn down. Collapse state = `chrome.storage.local['augment:contextCollapsed']` (read once into `contextCollapsed` at `start()` via `initContextCollapsed`; NEVER re-read per render — apply the module var, D39c-style discipline). The `#reply-master-btn{display:none!important}` kill rule is DEFENSIVE only — the real fix is uninstalling the legacy `~/newme/clipx/reply-master` unpacked extension in chrome://extensions (**remind the user at ship**). **NO new tests** — content.ts is untested by convention; the logic is AX.4's bun-tested `tweetContext.ts`. The whole panel is **UNVERIFIED IN A BROWSER** (D64/D65 tail — the unpacked-extension walk isn't scriptable): panel renders/collapses/survives SPA nav, kill rule with the legacy ext enabled-then-removed, and unknown-author muted line + no 404 fetch loop are the live done-when items. **Whole-repo `bun run lint` is now 0 errors** — the 2 pre-existing `Replies.tsx` a11y errors the ME/AI gotchas flagged were fixed by the Wave-1 verification pass `b920507` (`<dialog>` a11y). Gate with canonical `bun run test` (**1003/0**), not bare `bun test` (inspect.test.ts file-vs-`:memory:` trap unchanged — RU.3 gotcha). **Binds RU.7** (next D9 content.ts task — on-page variant chips + paste→posted; does NOT touch the panel) and **AX.6** (parallel — wires the AX.3 chip `data-handle` + the AX.5 panel `title` `data-handle` to `stratus/open-person`).

- **AX.6: the dossier click-through is LIVE across timeline chips + the tweet-page panel.** A chip button (`applyPersonChips`) or the panel header (`.stratus-ctx-title`, `renderContextPanel`) fires `sendOpenPerson(handle)` → the `OpenPerson {type:'stratus/open-person', handle}` message. The **background** is the only writer of the `stratus:openPerson` session key (`{handle, at}`, via its own `enqueueOpenPerson` chain) and does the best-effort `chrome.sidePanel.open({tabId: sender.tab?.id})` (user-gesture credit survives one hop on Chrome ≥116; `.catch()` if not — the panel is one action-click away). **`App.tsx` is the consumer + clearer:** a `useEffect` (empty deps — inlines `setPersonHandle`+`setTab('people')` so only stable setters are referenced, biome-clean) reads the key on mount + `chrome.storage.onChanged` (session area), routes to the dossier, then fires `stratus/open-person-clear` so the handoff cell can't replay on a later panel open. The chip/panel handlers `preventDefault`+`stopPropagation` so X never navigates; the collapse toggle is a header sibling of the title, so a toggle click never doubles as an open. **NO new tests** (guards trivially typed — the task block said skip; there's no message-guard suite for `isOpenPerson`/`isOpenPersonClear`, unlike RU.6's `isRadarConfirm`). **UNVERIFIED IN A BROWSER** (not scriptable): the two click paths landing on the right dossier + no replayed opens are the live done-when items. Gate with canonical `bun run test` (**1003/0** — unchanged), not bare `bun test` (inspect.test.ts file-vs-`:memory:` trap unchanged — RU.3 gotcha). **Completes the on-page click-through**; the D9 content.ts chain continues at **RU.7** (on-page variant chips + paste→posted).

- **RU.7: on-page radar variant chips + paste→posted are LIVE in `content.ts`.** `syncVariantChips(focusedId)` runs once/scan (after `syncContextPanel`): for the focused article it asks the background `stratus/radar-variants-get` ONCE per tweetId (`variantCache` Map + `variantFetchInFlight` Set; `requestVariants` calls `scheduleScan()` on resolve so the chips inject on the next scan — a MISS is cached too, so it won't re-fetch; a page reload clears the Map). Chips inject via `actionRow.appendChild(strip)` behind a SEPARATE `variantChipsHandled` WeakSet (distinct from `replyMasterHandled`, so both inject) — **NOT** article-afterend (that slot is AX.5's panel; colliding would thrash its `nextElementSibling` anchor). The strip dies with the recycled action row (no orphan). Each chip: angle label + `variantChipPreview(text)`, `title`=full text; click → `clearReplyEditor` (selectAll+delete only when non-empty) + `typeTextInto(findReplyEditor())` (clipboard + hint span fallback), `data-active` marker, then `stratus/radar-variant-pasted`. Posting stays MANUAL (§7.28) — the chip fills the composer; the user hits Reply. Background: `getVariants` (buffer `isReplyVariants(hit.variants)` → `GET /x/radar/drafts?tweetId=` `.drafts[0]` fallback, `variants ?? [{text:replyText,angle}]`) and `pasteVariant` (POST idempotent confirm → read `id`+`replyText` off the returned reply_drafts row → PATCH `/x/replies/:id {status:'posted', ...(text!==primary?{replyTextEdited}:{})}`; 2nd chip click no-ops the status but re-PATCHes replyTextEdited; best-effort §7.8). **No server/schema/route change** — pure consumer of RU.5's confirm + tweetId filter + the `copied→posted` transition (`ALLOWED_TRANSITIONS`, replies.ts). Pure helpers in NEW `shared/variantChips.ts` (`variantChipPreview` truncation + `isReplyVariants` non-empty shape guard, angle a loose string) — 7 bun tests; message guards `isRadarVariantsGet`/`isRadarVariantPasted` +4 in `messages.test.ts`. **DOM behavior UNVERIFIED IN A BROWSER** (content.ts convention + D64/D65 tail): the 3-chips-render + chip-click-types-and-flips-posted + deep-link-works-via-idempotent-confirm are the live done-when items. Gate with canonical `bun run test` (**1014/0**, +11), not bare `bun test` (inspect.test.ts file-vs-`:memory:` trap unchanged — RU.3 gotcha); root+extension typecheck clean, extension `vite build` green, `bun run lint` 0-error (3 changed files biome-formatted). **Binds RU.8** (next D9 content.ts task): a THIRD action-row WeakSet for the ⊕ button; widens `RadarSighting.band`→`'manual'` and the server band on `parseBatchTweets`/`RadarBatchTweet`/`RadarDraftInsert`.

- **RU.8: manual add-to-radar ⊕ is LIVE in `content.ts`, and `'manual'` is now a first-class `RadarBand`.** `attachRadarAddButton(article)` runs from `scan` (its own `radarAddHandled` WeakSet — the THIRD action-row button after save + reply-master) on EVERY tweet (timeline + status, band-null included); `onRadarAddClick(btn)` resolves the article from `btn.closest`, builds a `band:'manual'` sighting via `readTweetSignals(article) ?? synthManualSignals(article)` (zeros + `time[datetime]`-derived ageMin when there's no metrics label), flips the button ✓ for 1.5s, and sends `stratus/radar-report` with a single sighting IMMEDIATELY (NOT the 2s-batched `pendingRadar` path — one deliberate click). The background stays the single §7.24 buffer writer; `recordRadarSighting`/`flushRadar` are byte-untouched. **`'manual'` semantics across the stack:** it is QUEUE/UX METADATA, never a classifier verdict — `mergeSightings` never downgrades an existing manual band on a hot/warm re-sight (`prev.band==='manual' ? 'manual' : s.band`), eviction sorts non-manual to the front (dropped first, `am - bm`) so a human pin outlives fresher auto-captures, `rankSightings` has a leading manual-first comparison ABOVE roster tier, `isRadarSightings` accepts it. Server: `parseBatchTweets` (replies.ts) accepts `band:'manual'`, `RadarBatchTweet`/`RadarDraftInsert` (radar.ts) widened, so a pinned tweet drafted via "Draft replies" persists `radar_drafts.band='manual'`. **THE ONE TRAP (D69c):** the confirm endpoint (radar.ts ~L285) rebuilds `contextSnapshot.signals.band` from `row.band` — it now **coerces `'manual'`→`null`** (`row.band === 'manual' ? null : (row.band as PostSignals['band'])`) so a confirmed manual reply never lands in the Playbook's §7.19 hot/warm band cells; DON'T remove that coercion. `Radar.tsx` renders `.radar-band-manual` (accent-tinted) + a "manually added · Nd" whyLine for zero-signal pins; `BatchReplyTweet.band` (shared/types.ts, the 6th file) is widened so `draftReplies` can forward `s.band`. Tests +4 (radar.test.ts merge/evict/rank, src/test.test.ts parseBatchTweets, server radar.test.ts confirm-coerce). content.ts DOM behavior UNVERIFIED IN A BROWSER (convention): ⊕ lands the tweet top of the Radar queue with a `manual` chip + next "Draft replies" drafts it are the live done-when items. Gate with canonical `bun run test` (**1018/0**, +4), not bare `bun test` (inspect.test.ts file-vs-`:memory:` trap — RU.3 gotcha). **Completes the D9 content.ts chain through RU.8✓**; NT.5 is next in that lane but gated behind NT.1/NT.3/NT.4 (server, none done) — RU.9 (server, dep RU.2✓) is the readiest unblocked task.

- **RU.9: Playbook batch-vs-single attribution is now source-EXACT, not text-heuristic-first.** `classifyReplyOrigin(reply, draftSourceByPostedId, radarRepliesByTarget)` — the 2nd arg is now a `ReadonlyMap<string, reply_drafts.source|null>` (was a `ReadonlySet<string>`; D71). A reply linked to a posted `reply_drafts` row is `radar` iff that row's `source==='radar'` (RU.5's confirm stamps it), else `single`; the old collapsed-whitespace radar text-match survives ONLY as the fallback for replies with NO draft link (pre-tooling history). If you add a new drafting surface, stamp `reply_drafts.source` at insert AND teach this classifier + `loadOriginRows` (routes/playbook.ts, which selects `replyDrafts.source`). **Route-test trap:** the main `describe('playbook route')` beforeAll seeds pb_r1/pb_r2 (single) + pb_r3 (radar) PERMANENTLY (no afterAll) — they persist for the whole file. A test needing a specific batch-vs-single count must run in its OWN describe with afterAll cleanup (like `radar source-exact attribution (RU.9)`), never extend the main beforeAll (a measured reply ripples into angle/band/model/latency/roster cells and breaks their exact-count asserts). Gate with canonical `bun run test` (**1020/0**, +2), not bare `bun test` (inspect.test.ts file-vs-`:memory:` trap — RU.3 gotcha). **Shell/grep trap (root cause found):** `grep` silently returns NO matches on `src/x/playbook.ts` — git also flags it `Bin` in `--stat`. Cause: **2 literal NUL bytes** (0x00) at `playbook.ts:160`, a PRE-EXISTING deliberate sentinel — `` `${r.pillar ?? '\x00'}|${r.register ?? '\x00'}` `` uses NUL as the "null-slug" separator in the pillar×register composite key (impossible in a real slug). A NUL in the first 8KB makes git+grep treat the file as binary and suppress line output. tsc/biome/bun read it fine. Use `awk`/absolute-path `Read` on this file, not `grep`. Also relative-path `ls`/`find` were flaky this session — prefer absolute paths.

- **RU.10 CLOSES the RU sub-plan (RU.1–RU.10).** The whole radar↔reply unification is shipped: 3-variant single+batch drafting, the `POST /x/radar/drafts/:tweetId/confirm` endpoint (radar draft → measured `reply_drafts` row), on-page variant chips (paste=posted), the ⊕ manual add, and source-exact Playbook attribution. `scripts/smoke-radar-reply-flow.ts` is the rerunnable $0 end-to-end proof (confirm→idempotent→PATCH-posted→origin=radar-by-source). CLAUDE.md §"Overhaul 7.7" is the phase entry. **Live done-when tails not yet observed** (need real X use): a variant chip typed into a live reply box → the confirmed `source='radar'` draft → after the next harvest it gains `postedTweetId` and shows in the Playbook radar bucket without text-matching; ⊕ on a cold tweet top-ranks it with a `manual` chip. **Wave 2 remaining:** AX.7 (AX docs-sync — the AX.1–6 augmented-X-UI surfaces), then the NT server lane (NT.1/NT.2/NT.4, none-dep, parallel-ok) and RL.1 (reply-list pure engine, none-dep). The D9 content.ts chain's next task NT.5 stays gated behind NT.1/NT.3/NT.4 (server, none done).

- **RL.1: the reply-list engine is IN but INERT** — `src/x/replyLists/engine.ts` has no consumer until RL.2 (tables + CRUD) and RL.3 (`/use` route: pick → compose → stamp `lastUsedAt`/`useCount` + insert `reply_list_uses`, all in ONE sync txn §7.13). It is pure by contract: **no DB, no clock, no `Math.random`** — the route injects `rng` (server-side `Math.random` is fine here; the Studio's PRNG ban is a determinism contract that does NOT apply to this module). The anti-repeat promise ("no immediate repeats over 10 consecutive uses") only holds if **RL.3 actually stamps `lastUsedAt` on every non-preview use** — the exclusion window is the only mechanism. Two lint/impl traps found here: biome `lint/suspicious/noMisleadingCharacterClass` **forbids a ZWJ (`\u200D`) inside a `[...]` class** — the emoji strip had to become an alternation (`/[\p{Extended_Pictographic}\p{Emoji_Modifier}\p{Regional_Indicator}]|\u200D|\uFE0F/gu`); and with `noUncheckedIndexedAccess` every pool/word index needs a `?? null` fallback (no `!` assertions — biome `noNonNullAssertion`). Verified beyond the unit tests with a throwaway 10-use simulation (not committed): no immediate repeats, vars filled, emoji/`@` stripped, jitter rates plausible.

- **RL.2: the tables are in, the anti-repeat state has a home, nothing writes it yet.** `reply_list_items.lastUsedAt`/`useCount` and the whole `reply_list_uses` table stay untouched until **RL.3's `/use`** stamps them in ONE sync txn — the anti-repeat promise is exactly that stamp plus `pickItem`'s exclusion window, nothing else. Three contracts RL.3–RL.5 should not re-derive: (1) **`humanizer` is already normalized in the DB** (parsed through `parseHumanizerConfig` at write time), so `/use` just calls `resolveHumanizer(list.humanizer)` — no re-validation, and a stored value is always a FULL config; (2) **`POST /:id/items` always answers with the list's whole current set**, append and replace alike (RL.5's UI can render the response directly); (3) **item `createdAt` is stamped explicitly** (per-row offset starting past the list's current max) because a batch insert shares one `unixepoch()` millisecond — don't "simplify" back to the column default or insertion order goes non-deterministic (this bit once: an append landing in the same ms as the prior batch sorted into the middle). Item PATCH/DELETE match on `id` AND `listId` — a 404 there means wrong list, not missing item. `reply_list_uses` has **no FK** by design (RL.7's `canned` bucket must still see uses from a deleted list); `reply_list_items` DOES cascade. Malformed uuid → 400 `invalid_id`, valid-but-absent → 404 (ideas.ts idiom) — RL.5's client should treat them differently.

- **RL.3: `/use` is the route that makes the anti-repeat promise real** — before it, `reply_list_items.lastUsedAt` was never written by anything, so `pickItem`'s exclusion window was reading a column of nulls. Any surface that composes a canned reply MUST go through `POST /reply-lists/:id/use` (or `preview:true` when it's only rendering a sample) — a client that picks locally re-introduces the exact drift Decision 1 rejects.
- **RL.3: every `/use` text assertion needs a jitter-free list.** The route calls `Math.random` for real (server-side; the no-`Math.random` rule is a Studio/extension rule, not a server one), so `humanize` can prepend "ngl", drop a period or inject a typo at any time. The test helper `seedList()` creates lists with an all-zero-chance humanizer (`{prefixes:[],suffixes:[],prefixChance:0,…}`) — copy it for RL.5/RL.6/RL.8 assertions. The jitter itself is the engine suite's job (seeded rng).
- **RL.3: `reply_list_uses` rows do NOT cascade** (the table is FK-free on purpose, D77/RL.2), so `replyLists.test.ts`'s `afterAll` deletes them explicitly by `listId`. **RL.7 reads this table for the Playbook `canned` bucket** — any new suite or smoke that calls `/use` must clean up its use rows or it will show up as phantom canned attribution in a shared-DB run.
- **RL.3 opening guesses worth revisiting with real use:** `MAX_VAR_LEN` 120 (display name + handle bound) and the decision to allow `/use` on an inactive list. Neither is load-bearing; both are one-line changes if the panel wants different behavior.

- **RL.4: the generator has never called an LLM — not once, by design.** Every test is either a pure-function assertion or a pre-network guard (the 503 case force-unsets BOTH provider keys, ideas.test.ts pattern). The first real spend happens when **RL.5** builds the "Generate items" button; budget ~$0.003–$0.01 per click (12 items, temp 0.9, 1500 max tokens, reasoning low). RL.8's smoke stays $0 by default — a `--live` flag is the only place a real call belongs.
- **RL.4: `/generate` writes NOTHING, and that is load-bearing.** The proposal is returned, previewed, and applied by the human through `POST /reply-lists/:id/items` (`mode:'replace'|'append'`, `source:'ai'` — per-call, not per-item, D78e). RL.5 must not "helpfully" persist the proposal on arrival: the destructive overwrite is the one step Decision 3 keeps as an explicit click. The response `items` array is already the exact `{text}[]` shape `/items` accepts.
- **RL.4: adding a registry key is a 4-file move.** `PROMPT_KEYS` + `PROMPT_SPECS` in `prompts/registry.ts`, the default exported from a **PURE** module (a route import cycles — AI.5's `voice/extractPrompt.ts` move exists for that reason), `registry.test.ts`'s key-list + defaultBody assertions, and `docs/settings-tab.md`'s prompt-count copy (now **11**). The extension needs nothing — the Prompts panel renders whatever `GET /x/prompts` returns. CLAUDE.md's AI-layer entry still says "10 keys"; that is a historical phase record and stays — **RL.8's phase entry is where the 11th key gets documented.**
- **RL.5: the panel is now the ONLY place a reply list can be managed, and `api.replyLists.use()` is the only sanctioned way to compose a canned reply.** RL.6's QuickReplyPicker must call it (with `preview` absent/false so the anti-repeat state actually advances) and copy inside the same click handler — a local pick, or a `preview:true` "real" use, both silently break the anti-repeat promise (Decision 1 / RL.3's stamp). The ReplyLists panel deliberately uses `preview:true` everywhere because it is only *sampling*.
- **RL.5: `GET /x/reply-lists` has no `?active=` filter** (D78h) — the panel shows every list and dims the inactive ones. RL.6's picker menu is the place that should *offer* only `active` lists (client-side filter), but note an inactive list is still perfectly usable server-side (D79e) — "inactive" is presentation state, not a lock.
- **RL.5 is UNVERIFIED IN A BROWSER**, like every panel component since UI.9 — no tests were added (repo convention: panels are untested, logic lives server-side). Automated gates only: root+extension typecheck, `cd extension && bun run build`, changed-file lint, full suite 1087/0. The plan's live done-whens (create → add → test-render → generate → Overwrite) ride RL.8's smoke + the Wave-5 QA pass. The one server-behavior assumption the UI leans on was **not** left to faith — the `{humanizer:{}}` → defaults round-trip was proven with a throwaway route test (D81b).
- **RL.5 opening guesses worth revisiting with real use:** the Test-render sample vars default to `Ana Pop`/`anapop` (arbitrary, just needs to be obviously fake), the generate count defaults to 12 (the server's `DEFAULT_GENERATED_ITEMS`), and the items add box splits on newlines with no escape hatch for a genuinely multi-line canned reply — if one is ever wanted, add a "paste as single item" toggle rather than changing the split.

- **RL.6: the picker normalizes client-side ON PURPOSE, mirroring the route's own guards.** `targetHandle` and `targetTweetId` are DROPPED (not sent) unless they match `USERNAME_RE` / `/^\d{1,32}$/`, and `vars.name`/`vars.handle` are trimmed to 120 chars — because RL.3 400s on a malformed target (D79g) and losing the whole use over a scraped display quirk would be absurd, while a missing var just degrades the template. If those route regexes ever change, `QuickReplyPicker.tsx`'s copies change with them (they're commented with the route constant names).
- **RL.6: `await api…use()` THEN `navigator.clipboard.writeText()` works in the side panel** — the panel document stays focused, which is what Chrome's clipboard-write gate actually checks; transient activation is not required for `writeText` there (Studio's `copyPng` already awaits a canvas `toBlob` before `clipboard.write`). It is still wrapped in try/catch, and the composed text stays on screen on refusal (D82b). If a future surface copies from a NON-focused context, this assumption is the first thing to re-check.
- **RL.6 is UNVERIFIED IN A BROWSER** (panel convention, no tests added). The live done-whens RL.8 should watch: a Launch Room row's picker putting a name-filled humanized reply on the clipboard in one click, and two rapid uses of the same list on different rows yielding different items (the server shuffle, observable end-to-end for the first time).
- **RL.6 cache staleness is bounded by 60 s + the Lists-subtab unmount.** A list created in the Lists subtab appears in pickers as soon as the user leaves that subtab; created any other way (MCP, curl) it takes up to a minute. If a picker ever shows a deleted list, the click 404s and says so inline — no crash path.

- **RL.7: canned attribution is TEXT-MATCH ONLY, and that is the whole risk surface.** A canned reply has no `reply_drafts` row and no id link — the only evidence it ever existed is `reply_list_uses.renderedText` matching the published reply's text under `normalizeReplyText` (whitespace collapsed, nothing else). This works *because* the humanizer's typos are stored in `renderedText` exactly as they were copied (RL.3), so the paste is byte-faithful. It breaks the moment the user edits the text after pasting — that reply silently falls back to `unattributed`, never to a wrong bucket. **Undercount, never overcount** is the intended failure mode; don't 'fix' it with fuzzy matching.
- **RL.7: the `canned` bucket will read 0 until a canned reply is both pasted AND discovered.** The chain is: picker click → `reply_list_uses` row → user pastes manually → the reply ships → the 03:00 UTC dailyMetrics discovery writes it into `posts_published` → only then does the join see it. So a same-day check of `GET /x/playbook` after the first canned use legitimately shows nothing. RL.8's smoke should seed both sides rather than wait for discovery.
- **RL.7: `grep` over `src/x/playbook.ts` silently returned nothing before this commit** (two raw NUL bytes → `file` says `data`). Fixed to `'\0'` escapes (D83d). If a future session ever again finds a symbol that 'doesn't exist' in a file it can see with `Read`, try `grep -a` and then run the audit in codemap §7.32.
- **RL.7 opening guess worth revisiting:** the canned set is loaded **unwindowed** (every use row ever), mirroring `radarRows`. Fine at single-user scale; if `reply_list_uses` ever grows into the tens of thousands, bound it by the oldest `postedAt` in the published window rather than adding a fixed row cap.

- **RL.8: `smoke-reply-lists.ts` cleans up by fixed TARGET tweet id + the three list names, NOT by the created uuid.** `reply_list_uses` is FK-free, so a `/use` call in that script that omits `targetTweetId` would leak a row the cleanup can't find (and it would then sit in the Playbook's canned set forever). If you extend the script, either keep passing `TARGET` on every real use or widen `cleanup()` to delete by `listId` as well. Same reason the empty-list 409 check uses a *second* named list rather than disabling items on the first.
- **RL.8: `cellOf` counts `n` only for MEASURED rows** (those with a `metrics_snapshots` row); `posted` counts every attributed reply. The smoke seeds a snapshot so both move by +1 — a future assertion that publishes a reply without a snapshot must assert `canned.posted`, not `canned.n`, or it will read as a silent failure.
- **RL.8: the smoke's "≥3 distinct items in 10 uses" is deterministic, not luck.** With n=5 the exclusion window is `min(n-1, floor(n/2)) = 2`, so picks 1–3 are necessarily three different items. Shrink the seeded list below 3 items and that assertion becomes a coin flip — keep it at 5.
- **RL.8 was run on a machine WITH an LLM key**, so the `/generate` → 503 branch of the smoke was not exercised live (the unknown-list 404 branch was). Verify the 503 path the first time you run it keyless — it is the exact code path a fresh clone hits.

- **NT.1: `extension/src/shared/notifications.ts` is PURE and INERT** — nothing imports it until NT.5. **The shape NT.2's ingest and NT.5's DOM code build against:** `parseNotificationCell(article) → {kind: 'like'|'repost'|'follow'|'other', handles: string[] (lowercased, deduped, DOM order), targetText: string|null} | null`. Callers drop `'other'` client-side and never send it (plan Decision 6); `handles` may legitimately be `[]` on a skeleton cell — a batch entry with no handle has nothing to upsert and should be dropped too.
- **NT.1: the keyword-check ORDER is load-bearing, not stylistic.** `like → repost → follow` is what makes the deliberately loose Romanian stem `urmăre` safe: a follow cell carries no post text, so by the time the follow stems are tested the like/repost phrases have already had their chance at the cell. Reordering these three `if`s (or adding a looser stem above them) reintroduces the false positives the ordering was chosen to prevent.
- **NT.1: extension typecheck does NOT cover `*.test.ts`** — `extension/tsconfig.app.json` has `"exclude": ["src/**/*.test.ts"]`, and root `tsconfig.json` only includes `src/**/*` + `scripts/**/*` + `drizzle.config.ts`. So an extension test file is checked by **`bun run test` alone**; a type error in one is invisible to both typechecks. (Same for `earlyReplies.test.ts` — pre-existing, just never written down.)
- **NT.1: happy-dom parses inline `<svg><path d="…">` from `innerHTML` and `querySelector('svg path')` resolves it** — verified live in this suite, so an icon-first DOM parser is fixture-testable without a real browser. Useful for NT.5's fixtures if it wants any.
- **NT.1 opening guess worth revisiting:** `targetText` picks the **longest** `[dir="auto"]` block that contains no verb stem. A post whose own text contains "liked"/"apreciat" is therefore skipped and the cell may resolve `targetText: null` — which NT.2 handles (day-bucket fallback event id), so it degrades to a coarser event rather than a wrong one. If that turns out to be common in real data, the fix is structural (exclude the header block by position) rather than a longer stop-word list.

- **NT.2: a test handle longer than 15 chars is silently skipped and the whole test goes vacuous.** `normalizePersonHandle` enforces `/^[A-Za-z0-9_]{1,15}$/`, so `nt2_mutual_person` (17) was dropped by `recordEngagements` while every assertion around it still "passed" (the person row had been inserted by hand). Cost a debug round-trip. Two rules for any people-layer test: keep seeded handles **≤15 chars**, and assert `{processed, skipped}` on the ingest result so a skip can never hide behind downstream assertions.
- **NT.2: seeding `posts_published` in the shared in-memory DB is safe only with an out-of-window date and no metrics row.** The engagements suite needs own posts for target resolution; it uses a **January `postedAt`** and writes no `metrics_snapshots`, which keeps the rows out of every windowed or measured aggregation other suites assert over (playbook medians, reup 14–60d, brief/digest/quest windows, pinned watch). Files run sequentially so the only real hazard is a stray — cleanup runs inline *and* in `afterAll`. Reuse this shape rather than inventing a new seed.
- **NT.2 for NT.3:** the route imports `MAX_ENGAGEMENTS_PER_BATCH`, `EngagementKind`, `EngagementInput`, `recordEngagements` from `src/x/people/engagements.ts`. Server-side `EngagementKind` is **`like|repost|follow` only** — the parser's `'other'` is deliberately not representable, so the route's kind whitelist is the enforcement point for it. `recordEngagements` already skips (never throws on) an invalid handle, so the route may pass handles through and report `skipped`.
- **NT.2 opening guesses worth revisiting after real data:** `MIN_TARGET_SNIPPET_CHARS = 20` (below it a prefix is not evidence) and `TARGET_POST_WINDOW = 300` recent own posts. If dossiers fill with day-bucket events instead of tweet-anchored ones, the snippet is failing the prefix match (emoji/link rendering is the likely cause) — that is a parser-side fix, not a looser threshold.

- **NT.3: the whole-batch-400 vs per-item-skip split is deliberate, and NT.5 must respect it.** A malformed *shape* (bad kind, non-string handle, unparseable `seenAt`, oversize batch) rejects the ENTIRE batch with an indexed error — that is a client bug, and failing loudly is how it gets found. A *valid-shaped but unusable* handle (`not!a!handle`, >15 chars) is passed to `recordEngagements` and comes back in `skipped`. So a scroll that produces one weird cell still lands the other 49; a scroll that produces one `'other'` cell that the client forgot to drop lands nothing. Drop `'other'` and empty-`handles` cells client-side.
- **NT.3: `POST /x/people/engagements` is still unreachable from any UI** — NT.5 is its only planned caller. Until then the endpoint is exercised only by its test suite and (later) `scripts/smoke-notifications.ts` (NT.7). If NT.7 runs before NT.5 lands, that smoke IS the only end-to-end proof the route works against the real DB.
- **NT.3 test-handle rule (again):** the route suite uses `nt3_route_eng` (13 chars). The NT.2 gotcha applies unchanged — any people-layer test handle over 15 chars is silently skipped by `normalizePersonHandle`, and asserting `{processed, skipped}` is what keeps that from hiding.
- **NT.4: the notifications augmentation payload is ONE message, `{type:'stratus/notif-context', force?}` → `NotifContextResponse {ok: true, mentions: NotifContextMap, rankMap}`** (both types exported from `shared/messages.ts` — import and cast, don't re-declare the shape). `NotifContextMap` is keyed by the **REPLY's tweet id** (the `mentions` row's own `tweetId`, which is what `findPermalink` yields on a notification cell), valued `{parentText: string|null, status: MentionStatus}`. One ask covers BOTH augmentations: it refreshes the 5-min mentions cache and the 10-min S0.3 rank map (the same `rankMap` the radar tier stamp uses) in parallel.
- **NT.4: the response is ALWAYS `ok: true` — empty maps are the failure mode, not an error.** An unconfigured extension (`handleApiRequest` → `unconfigured`), a network failure, a non-JSON body, or a thrown handler all resolve to `{ok:true, mentions:{}, rankMap}` / the last-good map. NT.5 must render nothing extra in that case and never surface an error to the page — this augments a page it doesn't own (same discipline as AX.3's glance cache).
- **NT.4: `force: true` has exactly one legitimate call site** — immediately after the injected sync-replies chip's `POST /x/mentions/refresh` resolves. It drains any in-flight refresh BEFORE invalidating (D88b), so a scan-triggered pull that started pre-sync can't stamp the cache and pin pre-sync rows for the full 5 min. Sending it on page load / per scan would defeat the TTL and re-fetch 200 mentions on every scroll burst.
- **NT.4: only a SUCCESSFUL pull advances `notifContextAt`.** A failed refresh leaves the stamp where it was, so the next ask retries instead of going quiet for 5 minutes — the same rule `refreshRankMap` follows. If you ever add a "cache is warm" indicator, read the stamp, not the map (an empty map is a legitimate warm state on an account with no mentions).

- **NT.5: the notifications surface is LIVE in `content.ts`, and the D85 icon prefixes are now VERIFIED (one was wrong).** `FOLLOW_ICON_PREFIXES` is **`M17.863 13.44`** — the old `M12 11.816` guess was wrong; like (`M20.884 13.19` filled + `M16.697 5.5` outline) and repost (`M4.5 3.88`) are confirmed. Fixtures interpolate the constants, so no test moved. **Everything hangs off one `onNotificationsPage()` gate computed ONCE per `scan()`** (`location.pathname.startsWith('/notifications')`) — X navigates without reloads, so it is per-scan, never once at `start()`. Four behaviors: (a) `applyNotifParentContext(article)` runs **inside** the existing article loop (so it rides the same `root.querySelectorAll`), (b)+(c) `scanNotificationCells()` parses each `article[data-testid="notification"]` **once per scan** and serves both the tier chips and the harvest, (d) `syncNotifSyncChip()`. **Three placement rules a later task must not casually change:** (1) tier chips go **after** the header `[dir]` block, never inside it and never inside a `[dir="auto"]` — the parser reads the first `[dir]` as the header and the longest `[dir="auto"]` as the target post, so nesting would feed our own chip text back into the next parse (live-verified: 0 of 21 re-parses changed); (2) the parent line anchors `tweetText`-afterend with an article-append fallback (media-only replies); (3) the sync chip anchors after `[data-testid="primaryColumn"] h2`. **Do NOT add tier chips to tweet-articles** — AX.3's `applyPersonChips` already decorates them with the strictly richer glance vocabulary (D89a). `engagementSent` is a plain **Set** keyed `kind:handle:targetSlice` (not the radar's time-windowed Map): the server's deterministic ids make a re-send a no-op, so it is a chatter guard only. A single screen produced **28** engagements from 21 cells, so the ≤50 batch cap + overflow re-arm is a real path, not theoretical. **Nothing here writes `mentions`** — that table's max tweet_id IS the since_id checkpoint (§4); the freshness gap is closed by the human sync chip only, and `POST /x/mentions/refresh` keeps its 6/day server cap with **no client bypass and no retry loop**. **No new tests by design** (plan Task-5: the parser is NT.1's, the transport mirrors `flushSightings`, content.ts DOM is untested by convention) — `bun run test` stays **1129/0**. **What is still browser-UNVERIFIED:** the harness that validated the DOM ran the logic transcribed into the live page, not the unpacked extension — so the message round-trips (`stratus/notif-context` actually answering, the engagement POST actually landing rows, the sync chip's 429 path) are the live done-when tail. Load unpacked and watch: parent lines on known replies, a tier chip on a known ally/target, `people` rows with `source='notification'` appearing while scrolling, and one refresh per chip click. **Binds NT.6** (fans engagement count — different lane, server + `People.tsx`; its `EVENT_ICONS` Record is exhaustive over the extension `PersonEventType` union, so adding the three types forces the three labels) and **NT.7** (docs+smoke — `docs/notifications-surface.md` should carry the three verified glyphs and the "chips go after the header block" rule). **Releases `extension/src/content.ts`;** the D9 chain now stands at NT.5✓ with **HV.2** next in that lane.

- **NT.6: `engagementCount` on a fan row is DISPLAY-ONLY and must stay that way.** C10 decision 1 (user-confirmed, do not re-litigate): likes/reposts/follows are not reciprocity, so they move neither `computeStage` nor `rankFans` nor C5 followup classification. The route enforces it structurally — `loadFanEngagementCounts` runs *after* `rankFans` and *after* `.slice(0, limit)`, and its result is merged in the response `.map`, so there is no code path where a count can reach the comparator. `followups.test.ts` re-ranks the returned page through the real `rankFans` with counts stripped and asserts the order is unchanged; if a future task ever wants engagement-weighted fans, it needs a new field and a new decision, not an edit to this one.
- **NT.6: widening `PersonEventType` is what forces the dossier icons — that is the designed forcing function, not an obstacle.** `EVENT_ICONS` in `extension/src/sidepanel/People.tsx` is an exhaustive `Record<PersonEvent['type'], string>`; adding a type to the union in `extension/src/shared/types.ts` breaks the build until the icon exists. Note the *text* side needs nothing: `People.tsx` renders `{e.summary ?? e.type}` and the server already writes a readable `engagementSummary`. Root `bun run typecheck` does **not** cover the extension — `cd extension && bun run typecheck` (`tsc --build --noEmit`) is the one that catches this, and it is not part of `bun run build` (vite only). Extension-touching tasks should run both.
- **NT.6: the `· N engagements` line has never been rendered against real data.** No `their_like`/`their_repost`/`their_follow` rows exist outside tests — they only appear once someone scrolls x.com/notifications with the unpacked extension loaded and `passiveCapture` on. So the count reads 0 for every fan today, and the UI hides it. A pre-NT.6 server returns `undefined` for the field, which also reads as hidden — the panel degrades cleanly against an un-deployed backend.

- **NT.7: a real-DB smoke that seeds `posts_published` must write the row `retired: true`.** `scripts/smoke-notifications.ts` needs an own post for prefix target resolution; a non-retired one left behind by an aborted run is a candidate for the daily 03:00 UTC snapshot pass, i.e. a **real billed X read** for a tweet id that doesn't exist (invariant #7). `retired: true` takes it out of the candidate set permanently and costs the smoke nothing (it never reads metrics). Reuse this whenever a smoke seeds own posts against the real DB — `smoke-followups.ts` predates the rule and gets away with it only because its rows are deleted synchronously.
- **NT.7: `app.request(...)` is typed `Response | Promise<Response>`.** A helper declared `function f(): Promise<Response> { return app.request(...) }` fails root typecheck; make the helper `async`. `bun run typecheck` covers `scripts/**`, so this bites in smoke scripts, not in route suites (which `await` inline).
- **NT.7: the whole C10 phase is documented in `docs/notifications-surface.md`** — the four on-page behaviors, the deterministic-id table, the passiveCapture gate, the sync-chip caps, the verified glyph prefixes, the after-the-header chip-placement rule, and the never-write-`mentions` rule. A future task touching the notifications surface updates that file (and the CLAUDE.md C10 entry), not `docs/*-tab.md`. It is reachable from `docs/README.md`'s new "On x.com itself" table.
- **NT.7 closes Wave 2.** Everything from here is Wave 3: HV.1 (server, migration, xhigh) → HV.2 (which re-claims `extension/src/content.ts`, next in the D9 chain) → HV.3–6, then the GR lane. The NT sub-plan's remaining obligations are **live-observation tails only** (an unpacked-extension session proving the message round-trips + the first `· N engagements` against real data); no code task remains for them.

- **HV.1: `POST /x/harvest/passive` is live and Wave 3 is open — read this before HV.2/HV.3/HV.4 touch the harvest lane.** The route is server-only, **$0**, and adds **no column and no migration** (see D92 — the journal is still `0018` and FREE). Shape the rest of the phase must build against: the extension posts `{rows: HarvestIngestRow[]}` (**≤100/batch**, validated by the *existing* `parseIngestRow`, so the content script must ship the same row shape the active harvester already ships) and always gets back **`201 {runId, inserted, skippedRecent, skippedCap}`** — HV.2's flush should treat a non-201 as warn-and-drop and must NOT retry (a dropped sighting is a missing longitudinal point, never a lost user action). Four in-file constants are the contract and all four are **opening guesses**: `MAX_PASSIVE_BATCH=100`, `PASSIVE_DAILY_CAP=2000`, `PASSIVE_RECAPTURE_MS=30min`, `PASSIVE_RETENTION_DAYS=60`. The client's per-tweet resend throttle should mirror the server's 30 min so the wire isn't full of rows that will only be counted as `skippedRecent`. **Three things that will bite a later task:** (1) `MODES` is still `['posts','replies']` **on purpose** — `POST /harvest/runs` 400s `mode:'timeline'`, so nothing client-side may try to create a passive run (decision 7, asserted in the suite); (2) **`mode` is the ONLY discriminator** between ambient rows and hand-run harvests, and today the only other reader (`scripts/backfill-people.ts`) filters `mode='replies'` — **HV.4's affinity SQL and HV.5's funnel MUST filter `mode='timeline'`**, and any *future* query over `harvest_rows` has to declare a mode or it will silently mix corpora; (3) **band is never stored** (§7.12) — HV.5 recomputes it from `views`/`comments`/`tweet_time`/`captured_at`/`text` through the shared `replyBand.ts`, which is why capturing `tweetTime` matters (a null `tweet_time` makes `ageMin` and therefore vpm underivable, so the row can only ever land in an unknown bucket).
- **HV.1 test-suite traps for anyone editing `src/x/routes/harvest.test.ts`** (the file's first route suite; the pure `parseIngestRow`/`matchUnlinkedDraft` tests stay in `src/test.test.ts`): there is exactly **one passive run per UTC day**, so tests share it and **order is load-bearing** — the cap test forces `rowCount` to 1999 and **restores it** (`before + 1`) or every later test in the file gets capped to zero inserts; the prune test seeds its stale rows *after* the earlier posts because prune fires on **every** passive request; and each test needs **fresh tweet ids** (`77000xxx` here) or the 30-min recapture gate silently turns an expected insert into a `skippedRecent`. `afterAll` deletes every `mode='timeline'` run + rows plus the one seeded `posts`-mode keeper — no other suite touches these tables today, so that is exact. Suite total moved **1130 → 1139**.
- **CLAUDE.md was slimmed to guardrails-only (2026-07-23, out-of-skill docs refactor).** The full phase-entry ledger moved **verbatim** to `docs/PHASE-HISTORY.md`. **Every docs-sync task (MASTERPLAN global rule 6, codemap §7 rule 29 — updated) now appends its phase entry to `docs/PHASE-HISTORY.md`, NOT CLAUDE.md**; CLAUDE.md changes only when a guardrail (invariant, workflow, stack quirk) changes. Plan text still saying "CLAUDE.md phase entry" means the history file.

- **HV.2: the passive tap is OPEN — x.com/home now writes to `harvest_rows` while you scroll, and HV.3/4/5 finally have a corpus to read.** One line in `applyBand` (`if (sig) recordPassiveHarvest(article)`) drives it, for **every band including `skip`** — HV.5's funnel needs the denominator, and ads/promoted rows carry no metrics label so `sig === null` filters them for free. **What a later task must not casually change:** (1) **gate order is the perf contract** (D93c) — enabled → `/home` → `!isHarvestActive()` → `findPermalink` → pending/throttle, and only THEN `extractArticle`; the hook runs per-article per-mutation-burst, so the throttle is the hot path after a tweet's first sighting; (2) `isHomeTimelinePath(location.pathname)` is re-read **per call**, never resolved once at `start()` — X navigates without reloading (same reason NT.5's `onNotificationsPage()` is per-scan); (3) the flush is **warn-and-drop with NO retry** (a lost sighting is a missing point on a view curve, not a lost user action) and it ignores `unconfigured`; (4) `time: x.time || null` in `toPassiveIngestRow` — shipping `''` would be an invalid_row_time 400, and a null `tweet_time` strands the row in HV.5's unknown bucket, so an article whose `<time>` didn't render is worth less than it looks. **Three constants are opening guesses** and they mirror the server's, not each other's: `PASSIVE_RESEND_MS=30min` ↔ server `PASSIVE_RECAPTURE_MS`, `PASSIVE_BATCH_MAX=100` ↔ server `MAX_PASSIVE_BATCH`; change one side and the wire fills with rows the server only counts as `skippedRecent`. `PASSIVE_FLUSH_MS=5000` and the >5000 `passiveSentAt` reset are client-only. **The toggle is a SECOND key** — `passiveHarvest`, not C6's `passiveCapture` (one grows the people roster from hover cards, one grows the corpus from the timeline); both are default-ON/opt-out with `absent key = enabled`, and both are read directly by content.ts as well as through `sidepanel/storage.ts`. Adding it to the `Settings` interface forced the object literal in `storage.test.ts` — and surfaced that **extension test files are typechecked by NEITHER tsconfig** (root `include` stops at `src/`; `tsconfig.app.json` excludes `src/**/*.test.ts`), so `bun run test` is the only thing that ever executes them and a type error in one is invisible. **Browser-UNVERIFIED by convention** (content.ts is never unit-tested): the live done-when tail is scrolling x.com/home with the unpacked extension and watching POSTs to `/x/harvest/passive` in the network tab + rows landing under one `mode='timeline'` run, toggling the setting off stopping them **without a reload** (the `onChanged` listener), and starting a Harvest-tab run suspending them. Gates: `bun run test` **1147/0** (+8, all in the new pure suite), root+extension typecheck clean, `vite build` green (the IIFE pass proves the inlining — `isHomeTimelinePath` appears as a plain function in `dist/content.js`), `bun run lint` clean. **Docs are deliberately NOT updated** — plan Task 2 defers `docs/settings-tab.md` and the new harvest doc to **HV.6**, which owes: the `passiveHarvest` toggle row, the /home-only scope, the cap/retention/recapture numbers, and (per D93a) its phase entry in **`docs/PHASE-HISTORY.md`, not CLAUDE.md**. **Releases `extension/src/content.ts`, `harvester.ts` and `Settings.tsx`.** The D9 chain now stands at HV.2✓ — **A3.3 (Wave 4) is the last claimant** and nothing before it touches content.ts. HV.3 (Harvest tab options, `Harvest.tsx` + `harvester.ts` — note it re-claims harvester.ts), HV.4 (`GET /harvest/affinity`, re-claims `src/x/routes/harvest.ts`) and HV.5 (playbook lock) are all unblocked and disjoint.

- **HV.3: the Harvest tab is done and the harvester's option surface is now four flags — read this before HV.4/HV.5/HV.6 or any later harvester work.** `HarvestOptions` = `{mode, scope, pace, max?, sendToStratus?, downloadCsv?, minViews?}`; both new flags are **absent-means-on/off-by-default** (`downloadCsv !== false` downloads, an absent/0/NaN `minViews` filters nothing), so an older side panel driving a newer content script behaves exactly as before. **Three things that will bite a later task:** (1) **the min-views filter sits after `noteSeen` and before the store write** — moving it above `noteSeen` would stop filtered items from counting as scrolled-past, which defeats the day-window exhaustion break (`win && ctx.oldestSeenMs < win.startMs`) and makes every scoped run scroll to `HARD_STEP_CAP`; it also reads the harvested item's OWN views (`reply.metrics.views` in replies mode, never `orig`'s); (2) **`done.filename === ''` now means two different things** — zero rows (as before) *or* CSV-off; the panel disambiguates by `rows > 0`, and anything else consuming the event should too; (3) **`passiveRowsToday`'s day boundary is UTC on purpose** and must stay pinned to the server's `utcDayStart` — if HV.4/HV.5 ever add a "today" read over `harvest_rows`, use the same boundary or the two surfaces will disagree for hours a day. The form now lives in **one** `chrome.storage.local` key `harvestForm` (mode/scope/pace/maxStr/minViewsStr/downloadCsv, restored through the lenient `parseHarvestForm`) while `harvestSendToStratus` deliberately keeps its own legacy slot; the restore→mirror pair is guarded by a `formLoaded` flag, so **don't add a form field to the write effect's deps without also adding it to the read** or the first render will stamp a default over stored state. Start is blocked with a warn line when CSV **and** send-to-stratus are both off. The status line renders **nothing** while unloaded or after a failed `GET /x/harvest/runs` (it never claims 0) — the `limit:20` lookback is the route's own default and could in principle miss today's passive run behind 20+ same-day hand harvests; raise the limit if that ever happens rather than filtering server-side. **Browser-UNVERIFIED** (panel React is untested by convention): the live tail is a CSV-off DB-only run landing rows with no download, `min views 1000` thinning both the CSV and the ingest, the form surviving a panel close, and the status line matching what `x.com/home` scrolling actually captured. Gates: `bun run test` **1162/0** (+15), root + extension typecheck clean, `vite build` green (`ApiError` verified **absent** from `dist/content.js` — the new `import type { HarvestRun }` in `shared/harvest.ts` is fully erased), whole-repo `bun run lint` clean. **HV.4 and HV.5 remain unblocked and disjoint; HV.6 owes the docs** — a new/updated harvest doc covering the four options + the passive tap, `docs/settings-tab.md`'s `passiveHarvest` row (deferred from HV.2), and (per D93a) its phase entry in **`docs/PHASE-HISTORY.md`, not CLAUDE.md**.

- **HV.4: the ambient corpus has its first READER — and the `mode='timeline'` rule is now load-bearing in two places, not one.** `GET /x/harvest/affinity?days=&limit=&minDays=` → `{days, minDays, authors:[{handle, distinctDays, sightings, lastSeenAt, avgViews, stage, inRoster}]}`, $0, defaults 30 / 20 / ≥3 — **all four affinity constants are opening guesses** to revisit with HV.1's four (`DEFAULT_AFFINITY_DAYS`, `MIN/MAX_AFFINITY_DAYS` 7–90, `DEFAULT_AFFINITY_LIMIT` 20 / `MAX` 50, `DEFAULT_AFFINITY_MIN_DAYS` 3). **Four things that will bite a later task:** (1) the UTC day bucket is `strftime('%Y-%m-%d', captured_at/1000, 'unixepoch')` — the THIRD place the UTC-day boundary is written (server `utcDayStart`, panel `passiveRowsToday`, now this); any fourth reader must use the same one or two surfaces disagree for hours a day; (2) the `people`/`voice_authors` join runs **in TS off the ≤`limit` handles after the group-by**, with an early return when the group-by is empty — never let it reach drizzle as `in ()` (the NT.6/D90a rule); (3) `intParam` is now shared by three params and is the place to add a fourth — its contract is clamp-on-range, **400-on-garbage**, and the 400 is deliberate; (4) the route is still the only reader that isn't `mode='timeline'`-aware by accident — **HV.5's funnel must carry the same filter**, and `scripts/backfill-people.ts` (`mode='replies'`) remains the only other consumer. **Test-suite trap:** the affinity describe must stay **LAST** in `src/x/routes/harvest.test.ts` — the ingest describe's first test asserts **zero** `mode='timeline'` runs exist, and the affinity `beforeAll` creates one. Its rows are seeded **directly** (a multi-day history is unbuildable through the 30-min recapture gate) under a run created **now** with **backdated rows**, so the 60-day prune on the passive path can't delete them mid-suite; `afterAll` now also deletes the seeded `people` + `voice_authors` rows (shared in-memory DB). **Panel side is deliberately minimal:** a `Section`+`EmptyState` drawer under the People roster, **collapsed and unfetched until clicked** (one manual Refresh, no polling), and each row calls the SAME `setSelected` the roster uses — so an unknown handle rides the existing `NotYetKnown` "start the file" path. **No new message type, no storage key, no CSS rule** (it reuses `.people-list`/`.people-row-main`/`.people-counts`/`.people-ago`/`.stage-chip`). **Browser-UNVERIFIED** (panel React is untested by convention): the live tail is opening People → Show and seeing real handles from actual x.com/home scrolling, with a genuinely unknown one clicking through to the create-file screen. Gates: `bun run test` **1167/0** (+5), root + extension typecheck clean, `vite build` green, whole-repo `bun run lint` clean. **HV.6 now also owes `docs/people-tab.md`** (the affinity drawer) on top of the harvest doc + `docs/settings-tab.md`'s `passiveHarvest` row. **HV.5 is unblocked and disjoint** (playbook lock, untouched here).

- **HV.5: the ambient corpus now has TWO readers and the funnel is the one that needs `skip` rows to exist.** `GET /x/playbook` → `timelineFunnel: {cells, totalSeen, totalReplied}`, 30-day window, `mode='timeline'`, $0, **no schema/migration/CSS/api.ts change**. **Five things that will bite a later task:** (1) **HV.2 capturing every band including `skip` is load-bearing here** — the funnel's denominator IS "tweets the algorithm showed me"; if anyone ever "optimizes" passive capture to only store hot/warm rows, this cell silently becomes `replied/replied = 100%`. (2) The first-sighting rule is enforced in TWO places on purpose — `min(captured_at)` with bare columns in `loadTimelineFunnel` (SQLite's documented behavior: with exactly ONE min/max aggregate, every bare column comes from the matching input row — add a second aggregate like `max(views)` to that select and the guarantee evaporates) **and** an earliest-`capturedAtMs` dedupe inside the pure `buildTimelineFunnel`, so the pure fn is correct on any row set. (3) `'unknown'` ≠ `null` band (D96c) — and `unknown` is the bucket that grows if `content.ts` ever ships `time: ''`/omits it (HV.2 gotcha 4), so a sudden unknown spike is a capture regression, not a timeline change. (4) The window is **30d while retention is 60d** and the select is **unlimited** — deliberate, because HV.1's 2,000/day cap bounds it structurally (≤60k rows) and a `limit` would truncate the denominator without saying so. If the daily cap is ever raised, revisit the limit BEFORE the window. (5) `replied` reads posted `reply_drafts.sourceTweetId` (unwindowed; the ∩ with seen ids bounds it) — **canned replies and hand-typed replies leave no draft row**, so the capture rate reads LOW by construction. Don't "fix" it by widening to `posts_published.in_reply_to_tweet_id` without a decision: the plan's own risk note says refine the population in a follow-up, not pre-filter now. **Test-suite traps:** the funnel describe must stay **LAST** in `src/x/routes/playbook.test.ts` (it seeds a posted `reply_drafts` row, which moves the earlier describes' angle/latency/model/batch cells) and it must delete its `mode='timeline'` run + rows in `afterAll` — `src/x/routes/harvest.test.ts` asserts that **zero** timeline runs exist. Multi-sighting histories are seeded **directly**, not through `POST /harvest/passive` (its 30-min recapture gate makes them unbuildable). **Browser-UNVERIFIED** (panel React is untested by convention): the live tail is the Playbook tab showing a real seen/replied split after some days of x.com/home scrolling — and note **every cell will read `insufficient data` until 20 tweets land in a band**, which is hours of scrolling, not minutes. Gates: `bun run test` **1176/0** (+9: 6 pure, 3 route), root + extension typecheck clean, `vite build` green, whole-repo `bun run lint` clean. **HV.6 is the only HV task left** and now also owes `docs/playbook-tab.md`.

- **HV.6 CLOSES the HV sub-plan — Wave 3's only remaining lane is GR (GR.1–GR.10).** No code, schema, route or test changed (suite stays **1176/0**); the task was one new smoke + seven docs. **What a later task inherits:** (1) **`scripts/smoke-passive-harvest.ts` is the reusable template for smoking a table the user's live browsing also writes** — prefix-namespaced ids + a handle no real path produces, delete-what-we-made, recompute-not-restore for anything we merely touched (D98c). It is rerunnable back-to-back and verified to leave 0 rows; it will FAIL loudly (not silently skip) if today's passive run is already at the 2,000-row cap, with a message naming the cap. (2) **The `unknown`-band assertion is the funnel's canary** — the smoke seeds 6 rows with `tweetTime: null` on purpose and asserts a `≥6` `unknown` cell; if a future capture change starts shipping `time: ''` the smoke keeps passing but the *live* unknown cell balloons, which is the documented capture-regression signal (`docs/playbook-tab.md` says so in user words). (3) **Docs now assert user-visible strings** that a UI polish pass must keep or update together: the both-off warn line, `Passive: N rows today` / `Passive capture off`, `saved N rows to stratus only (no CSV)`, `Nobody has shown up often enough yet.`, `Start their file →`, `insufficient data (n=…)`, and `Nothing captured yet — passive harvest fills this while you scroll x.com/home.` **UI.15 (Wave 5) polishes Replies/Playbook/Harvest/Studio and UI.14 polishes People — both must re-read the matching `docs/*-tab.md` in the same task.** (4) `docs/settings-tab.md` now says **four** toggles in two places (the section intro and the troubleshooting bullet); UI.2/UI.11's settings rebuild has to move that count, not just add a row. (5) The HV phase's **live "done when" tails are still open** and no code task owns them — an unpacked-extension session on x.com/home proving rows land under one `mode='timeline'` run, the toggle stopping capture without a reload, a Harvest-tab run suspending it, the first ≥30-min re-sighting, and the first affinity author promoted to a dossier. Every volume constant (2,000/day, 30-min, 60-day, minDays 3, affinity defaults) stays an **opening guess** until ~30 days of real scrolling.

## Planning-error log

Corrections to MASTERPLAN.md itself (wrong dep, wrong order discovered live).

- **2026-07-16-niche.md Task 3 done-when** — the "`Pitești` hits nothing" grep contradicts the same task's bit-for-bit equivalence requirement (post prompt §6/§9/§0 carry builder biography outside the §1/§5 extraction scope; no niche field exists to hold them). Resolved at N.3 in favor of equivalence; see D28b for the full resolution + what N.9 must scope differently. Source plans are never edited — this entry is the correction.
- **2026-07-17-ai-layer.md — no task owns the post drafter's askLLM migration.** Task 5's edit list names replies/pillars/playbook/voiceExtract; Task 6 only flips drafter's MOUNT gate; Task 7 adds a sibling route. Yet the plan's Why-section + Done-when #1 spirit require every draft surface to honor the provider setting, and the Task-6 gate flip would 502 `/posts/draft` on OpenRouter-only setups. Resolved at AI.5 by migrating `drafter.ts` there (D47b) — the call-site-migration task was the right home.

