# Masterplan execution state

> Dynamic memory for `/masterplan`. Updated after EVERY task, in the task's own commit.
> Plan: `plans/MASTERPLAN.md` (static — order, reasoning levels, waves, D1–D10 + Wave-6 seeds D141–D144).
> Codemap: `.claude/skills/plan-feature/references/codemap.md` (updated per task too).
> History: `.claude/skills/masterplan/STATE-ARCHIVE.md` (frozen, grep-only — closed-lane deviations, gotchas, priors, ledger rows).
> Debt: `.claude/skills/masterplan/VERIFY-DEBT.md` (**unpaid**, not history — browser-verify + CA.2. Read it when you have Chrome open; skip it otherwise).

## ✅ Waves 0–5 COMPLETE — 106/106, closed by UI.17 on 2026-07-25 · WAVE 6 IS OPEN (added the same day)

**The next task is in Wave 6.** Waves 0–5 are all closed (106/106); the three plans that were backlog-after-the-masterplan (`static-coach`, `llm-judge`, `mika-growth-tactics`) were scheduled as **Wave 6** on 2026-07-25 — MASTERPLAN's Wave-6 table + seeds **D141–D144** are the order/deps/reasoning source, the ledger below is the state. `/masterplan next` picks from the Wave-6 `[ ]` rows. **The GT lane is CLOSED at GT.9 (2026-07-27), the wave's first sub-plan to finish**; SC and JD remain. Unpaid verification debt lives in its own file now — `.claude/skills/masterplan/VERIFY-DEBT.md`, not the archive, because it is still owed.

**✓ Archive passes paid: GR.10, UI.11, UI.17, and GT.9 (2026-07-27, the Wave-6 one).** GT.9 took **156 KB → 94 KB** in five moves, all by line number and never retyped, each verified as a set operation (the D113a precedent): (i) the GT lane's 4 header priors + **D147/D148/D150/D151/D157/D158/D159** + the 7 GT gotcha blocks → `STATE-ARCHIVE.md` §"Archived at GT.9"; (ii) **Wave 5's 13 per-task ledger rows** → §"Ledger — Wave 5", the rule Waves 0–4 got at UI.11 and the one thing UI.17's own closing pass missed; (iii) the **verification-debt matrix** → its own file, `VERIFY-DEBT.md` — it is *unpaid debt*, so it could not be archived, but it had become the single largest block in a file whose whole job is to load in one `Read`, and nothing in it binds an open task; (iv) three **hot-file-locks rows** whose own text said no open task binds the file, plus **D140** (UI.17's closing deviation, discharged — its rule is restated in D160(d)) and the Wave-4 carry-forward's discharged half; (v) the discharged narration in this header (the UI.11/UI.17 pass detail, the last-commit sha archaeology D97 itself says not to re-derive, and the Wave-6 recap of tasks that are now in the archive). What survives below is the standing entries, the two OPEN lanes (SC, JD), and the ledger — **and it is still two `Read`s, honestly; see the budget in the size-discipline note.**

**Size discipline (added 2026-07-23 at 339 KB; paid at GR.10, UI.11, UI.17, and GT.9):** STATE.md must stay loadable in a **single `Read`**, and the limit that bites is the tool's **token** cap (~25k), not the 256 KB byte cap — "well under 256 KB" is not the test; whether one `Read` returns the whole file is. What lives here: (a) standing rules, (b) entries for the OPEN lanes, (c) the ledger + locks. When a sub-plan closes, its D-entries, gotchas, header priors and ledger rows move verbatim to `STATE-ARCHIVE.md` — that move is part of the closing task, not a separate chore. **Two lessons the four passes have now taught, both of which cost real sessions:** (1) *the breach is never one big entry, it is six good ones that each looked affordable* — Wave 6 went 48 KB → 156 KB in fifteen tasks, ~7 KB each, and Step 0 was a two-`Read` job from GT.3 onward; (2) *a closing pass archives the lane in front of it and misses the structural weight behind it* — UI.17 archived the polish lane but left Wave 5's own ledger rows, and every session since paid for them. **So: when you close a lane, also ask what stopped binding anything two waves ago.** **The budget, finally measured instead of guessed (GT.9): this file at 94 KB reports 37.9k tokens against the 25k cap, ≈ 400 tokens/KB — so one `Read` is roughly 62 KB.** GT.9 got it to **94 KB and stopped, honestly: everything left binds an OPEN task.** The two open lanes carry ~19 KB (SC: six D-entries + six gotchas) and ~5 KB (JD) of live content, plus ~9 KB of standing carry-forwards. **So Step 0 is a two-`Read` job until SC.9 closes the SC lane (−19 KB, ~75 KB → still two) and JD.8 closes JD (−5 KB, and the three carry-forwards become archivable) — the file gets under 62 KB when Wave 6 ends, not before.** Plan for two `Read`s; do not "fix" it by deleting something an open task needs. If a pass slips, pay it as a standalone housekeeping commit (the `55c6d19`/`c9c8ade` precedent) rather than letting every session keep paying the tax.

- **last-commit:** **identity is the SUBJECT LINE — no sha is recorded (D97).** HEAD should read `feat(coach): fitted reach band from own corpus (SC.8)`, parent `cdd2632` = GT.9. **Step 0 is one command:** `git log -1 --format='%h %s'`, compared against the last ticked subject in the ledger below. **Do not hunt shas.** The per-task chain is in the ledger (parent shas, live and archived) and in `git log`; re-deriving it here is what made this line 1.8 KB of archaeology. **Out-of-skill commits are NOT ledger drift and are already reconciled** — thirteen of them, all before SC.1: the eight between UI.17 and SC.1 (wave-5 wrap-up, the Wave-6 plan + masterplan edits, a registry fix, a version bump, PR #4's canned-reply fixes, and `db34235` RD.1–RD.3 Radar-own-tab, which carries its own codemap §11 entry), plus `wave 3 done`, `b59a564` `ideas`, the two `chore(masterplan)` compaction commits, and the two **CA** commits (`plans/2026-07-23-cost-audit-followups.md`, run 2026-07-23 at explicit user direction). If Step 0 finds a subject that names none of the above and no ticked task, run the `sync` reconciliation.
- **current wave:** **WAVE 6 (Coach, judge & growth tactics) — OPEN, 17/25 (+ GT.5 `[s]` by design).** **The GT lane is CLOSED at GT.9** — the whole mika-growth-tactics sub-plan (GT.1–GT.8 + its docs-sync) has shipped, its D-entries/gotchas/priors are in `STATE-ARCHIVE.md` §"Archived at GT.9", and its generalizable half is codemap §7 rules **4a/4b/4c** (measure a membership rule before you gate on it · a counter over a set the counted act can join needs a time qualifier · a client that re-decides a server rule reproduces it with a twin comment and gets diffed on live data). **Three GT facts survive as gotchas below rather than in the archive, because they bind code, not tasks:** `src/x/people/reciprocity.ts` is the ONE definition of "my people" (import it, never re-derive it — a Radar chip that disagrees with the money gate is the UI.7 fork with a new coat); one concept deliberately carries **three names** (`reciprocity` the module + quest, `roster` the stored band + `gateBypass` value, **"your circle"** everything the user reads) and renaming the band rewrites history in `radar_drafts.band`; and the doctrine now has **6** knobs, with D157(d)'s three-place registration trap. **What is left: SC (1 task — the closer) and JD (7).** SC.8 done — the D141 Composer lane's third link, and `Composer.tsx` is released to JD.6, its last claimant; NEW `src/x/coach/reach.ts` + `GET /coach/reach` folded onto SC.7's router, one line inside the existing `.coach` box, **no shim, no `tsconfig.app.json` entry, no registry knob** (the gate reuses `x.gates.minCellN`). **Read D161 before any future aggregate over `metrics_snapshots`: both house readers the plan named — latest-snapshot-per-tweet and `views*1440/age` — were measured and rejected, and the second snapshot existing only on winner re-reads is now codemap §7 rule 34.** The band ships GATED and silent: zero formats clear n>=20 today (the only bucket with the sample is the exempt `substance`), which is SC decision 9 working as designed. Browser-verify owed. SC.6 ✓ — the D141 Composer lane's second link is done and `Composer.tsx` is released to SC.8; NEW `src/shared/postCooldown.ts` + `GET /x/posts/cooldowns` on the calendar router, one line inside the existing `.coach` box, **no shim and no tsconfig entry** (the panel reads cells, never builds them); the plan's Jaccard idea-clustering was DROPPED on evidence — read D156(a) before trusting any ported threshold — and D146(b)'s fallback question is answered by exempting `substance`/`one_liner`/`other`; browser-verify owed; SC.7 ✓ — the coach is no longer niche-less: NEW `routes/coach.ts` + `sidepanel/coachLexicon.ts`, and because `CoachChip` reads the hook itself all four SC.4 pickers inherit the vocabulary with zero call-site edits; `Composer.tsx` took 2 lines out of lane, D141 unaffected; browser-verify owed; SC.5 ✓ — the `playbook.ts` + `routes/playbook.ts` pair is now the SC.5→JD.7 lane's, released; the `postFormat` extension shim landed with it so SC.6 inherits it; browser-verify owed; **its prod eyeball found the coach band axis is a near-constant, 97% `top` — read D154(a) before SC.8 or JD.7**; SC.4 ✓ — four `.reply-variant`-family pickers scored through the new `sidepanel/CoachChip.tsx`, which now owns the coach's tone maps; `Composer.tsx` was touched for two import lines only and its `.coach` JSX is byte-untouched, so the D141 lane is unaffected and SC.6 is still next on it; browser-verify owed; SC.3 ✓ — the D141 Composer lane is OPEN and its first link is done, so SC.6 is now the next Composer task and `Composer.tsx` is released; SC.1, SC.2 ✓ — both `src/shared/` cores in; JD.1 ✓ — the D143 `src/x/replies/*` lane is FREE. **No locked files anywhere** — only the D141 Composer chain (SC.8 → JD.6) and the `playbook.ts` pair (JD.7) still serialize anything. Behind it, closed: Waves 0–5 = **106/106** (see the ledger). **The migration journal is FREE; the next migrating task owns `0024`** — JD.4 is the wave's only migration and never runs in a parallel session (journal conflicts). Registry: **14 groups / 61 knobs, 27 mirrored**; MCP **23 tools** (`src/mcp.test.ts` asserts it exactly + `docs/s2-mcp-server.md`'s three counts); suite **1719**; whole-repo lint **0 errors**.
- **next-up:** **the Wave-6 ledger below is the source** — `/masterplan next` takes the first `[ ]` whose deps are `[x]`: **SC.9** (SC docs-sync + `smoke-coach.ts`, high — it CLOSES the SC lane, so it also owns the archive pass for D145/D146/D152-D156 + D161 and the six SC gotchas, which is ~19 KB off this file; read D156f and the SC.8 gotcha for what the smoke should actually assert — the exempt invariant and the first-vs-latest contract, never a raw count over a shared corpus), then **JD.2** (`shared/judge.ts`, pure, no deps, disjoint from everything). All touch disjoint files and **there are no locked files left anywhere**. **The verification debt that used to live here moved to `.claude/skills/masterplan/VERIFY-DEBT.md` at GT.9** — twelve items (nine GT/SC browser-verifies, the UI.11–UI.16 panel matrix, and CA.2 step 2), unpaid, none of them a masterplan task, none of them binding on any open task. It is not in the archive because it is not history. Read it when you next have Chrome open on the panel; otherwise skip it, which is exactly why it is no longer in the file every session must read. **Standing gate reminders for any future work:** `bun run test` (bare `bun test` targets the file DB and `inspect.test.ts` fails 2/2); biome forbids `delete process.env.X` / `: any` in `scripts/`, **non-null assertions (`x!`) anywhere**, and a `let` assigned once — it also sorts imports case-sensitively, collapses short chained calls, and rejects a `console.log` string-concat unless every operand interpolates (run `bunx biome check --write`). Extension-touching work adds `cd extension && bun run build` **and** its own `typecheck`, then `grep -cE '^\s*import[ {]' extension/dist/content.js` == 0.
- **prior (SC.8 — fitted reach band; xhigh, deps SC.2✓+SC.5✓, **$0 — two indexed SQL reads, no X call, no LLM**, 9 files [2 new], server + extension + docs, no schema/migration/registry/mirror/MCP change; suite 1719→1733 (+14)):** NEW `src/x/coach/reach.ts` (pure, server-only) — `buildReachFit(rows, minN)` → one cell per format (all 14, cascade order) carrying `weightSource: 'fitted'|'insufficient'`, `stallRange` (p25–p75 of the non-escape outcomes × base), `escapeThreshold` (3× base) and `escapeProbability`; **no seed table anywhere**, so an under-gate format is `null` in every numeric field. `GET /coach/reach` folded onto SC.7's `routes/coach.ts` (second static path), gate = the shared `x.gates.minCellN`. `Composer.tsx` renders ONE line inside the existing `.coach` box under the cooldown line; `canSubmit` byte-untouched. **The task's real output is a measurement that killed both house readers the plan named to copy** — `latestOutcomes`' latest-snapshot is a SELECTED sample (the 2nd snapshot exists only on winner re-reads: 25/135 rows, median first-views 1,059 vs 122) and `views*1440/age` is falsified by the corpus (<12h and 12–27h posts carry the same raw count, 129 vs 121, while their normalized rates differ 170%; one 4-minute read minted a 696× multiplier). Ships as FIRST snapshot + raw views inside the daily pass's own 3–27h window; codemap §7 gained rule **34** from it. **At the gate the honest answer is zero fitted formats** — the only bucket with the sample is `substance`, exempt for SC.6's reason (D156b) — so the band renders the insufficient line today and arms itself as audience_cta (17) / list (16) / story (16) cross 20. See **D161** + the SC.8 gotcha. Browser-verify owed (`VERIFY-DEBT.md` item 0k).
- **prior (GT.9 — GT docs-sync + `scripts/smoke-growth-tactics.ts`; high, deps all GT✓, **$0 and structurally unable to spend**, 9 files [2 new + the archive pass], docs + smoke, no src/extension/schema/migration/registry/mirror/route/MCP change; suite 1719 unchanged by design — a smoke is not a test file):** **closes the GT lane and Wave 6's first sub-plan.** NEW `scripts/smoke-growth-tactics.ts`: the gate carve-out proven by *how far a keyless request gets* (503 for my people, 422 for a stranger AND for a retired person, `parseContext` dropping a client `gateBypass`, then a re-read proving the block wrote zero rows), `buildMilestoneWatch` on four synthetic series, the reciprocity quest as a **delta** plus its **qualifier** (re-stamp the person to now → the same posted reply stops counting), and a `band:'roster'` row confirming to a null snapshot band. **Docs were mostly a VERIFICATION pass** — `git log --name-only -- docs/` showed GT.3/GT.4/GT.6/GT.7/GT.8 each paid their own tab doc (D140c working as designed), so the owed pieces were the two **prompt** tasks, which had shipped with no user-facing docs at all: `docs/replies-tab.md` (echo anchor, + a stale "two variants in one call" → three) and `docs/composer-tab.md` (the four skeletons, the one-of-three cap, the not-bait reconciliation). Plus `docs/PHASE-HISTORY.md`, PLAN.md, two CIRCLES-PLAN cross-refs (C3 gate half / C9 quest half), and `evals/i_mika_el_analysis.md`'s "Shipped as GT.1–GT.8" — which records the **three source-study numbers that did not survive our own corpus**, because that is the reusable half of the study. Housekeeping: the Wave-6 archive pass (156 KB → ~76 KB, four moves — see the header). See **D160** + the GT.9 gotcha. **No browser-verify owed** (nothing user-visible changed).
- **prior (SC.6 — format cooldown, supersedes GT.5 per D142; high, deps SC.2✓+SC.3✓, **$0 — one indexed SQL read, no X call, no LLM**, 8 files [2 new], server + extension + docs, no schema/migration/registry/mirror/MCP change; suite 1673→1690 (+17)):** NEW `src/shared/postCooldown.ts` — `buildCooldowns(posts, now, windowDays = 7)` → one `{format, count, status, exempt, lastPostedAt, exampleText}` cell per format present in the window, in `POST_FORMATS` cascade order; 4 = cooldown, 2 = warming. NEW `GET /x/posts/cooldowns?days=` on the calendar router ($0 over own non-reply `posts_published`, `MAX_COOLDOWN_DAYS` 90, `invalid_days` 400, registered before `/posts/scheduled/:id` per §7.20). `Composer.tsx` renders ONE line inside the existing `.coach` box, above the check rows because it is about the week rather than the draft — one fetch per mount, `classifyFormat(coachInput)` matched against the cells, amber only at the cooldown bar, `canSubmit` byte-untouched. **The plan's Jaccard idea-clustering was dropped on measured evidence (max same-format 7-day pair similarity 0.238 vs its 0.45 bar over 137 real originals) and D146(b)'s fallback question is answered by exempting `substance`/`one_liner`/`other`** — see **D156** + the SC.6 gotcha; codemap §7 gained rule **33** from it. Browser-verify owed (next-up item 0g).
- **prior (SC.7 — niche coach lexicon; high, deps SC.1✓+SC.3✓, **$0 — pure SQL, no X read, no LLM**, 9 files [3 new], server + extension + docs, no schema/migration/registry/mirror/MCP change; suite 1667→1673 (+6)):** NEW `src/x/routes/coach.ts` — `GET /coach/lexicon` → `{niche, specificTerms, tribeTerms}`, mounted after `channelsRouter`, pure `assembleCoachLexicon(...)` + never-throwing `loadActiveCoachLexicon()` (the `loadActiveNicheSafe` discipline; the niche slug is stamped even on the default). The two lists are derived by opposite rules — see D155(a). NEW `extension/src/sidepanel/coachLexicon.ts`: the `loadActiveChannels` cache idiom (module value + 60 s TTL + one in-flight promise) plus a listener set behind `useCoachLexicon()`, which **`CoachChip` calls itself** — so Replies/Conversations/LaunchRoom/Radar inherit the vocabulary with **zero call-site edits**. `Composer.tsx` took 2 lines (hook + `scoreDraft(coachInput, {lexicon})`); `shared/types.ts` + `api.ts` the `coach.lexicon` pair; `docs/composer-tab.md` + `docs/replies-tab.md`. See **D155** + the SC.7 gotcha; browser-verify owed (next-up item 0f).
- **prior (SC.5 — Playbook format + coach-score cells; high, deps SC.1✓+SC.2✓, **$0 — pure SQL over already-billed rows, no X read, no LLM**, 9 files [1 new], server + extension + docs, no schema/migration/registry/mirror/MCP change; suite 1654→1667 (+13)):** `src/x/playbook.ts` §13 `buildFormatEffectiveness` (own originals bucketed by `classifyFormat`, cells in `POST_FORMATS` cascade order, no lift line) + §14 `buildCoachScoreEffectiveness` (the SAME rows keyed two ways — all four `CoachBand`s worst→best AND `clean`/`flagged` on `counts.fix>0`, per D152(b) — with `spread`/`spreadBands` naming the two gated bands it compared and `fixSpread` on the both-sides discipline). `routes/playbook.ts`: `loadMediaRows` widened to select `text` and renamed **`loadOriginalPostRows`** — one load, three cells (`digest.ts` updated). NEW `extension/src/postFormat.ts` shim + the 5th `tsconfig.app.json` `include`; `Playbook.tsx` two sections between media and idea-payoff; `shared/types.ts` + `api.ts` the two payload shapes. `docs/playbook-tab.md` gained both sections (its headings are NOT numbered, so this appends rather than renumbers). See **D154** + the SC.5 gotcha; browser-verify owed (next-up item 0e).
- **prior (SC.4 — coach chips on reply variants; high, deps SC.1✓+SC.3✓, **$0 — client-side, no fetch/LLM/X read**, 11 files [2 new], extension + docs, no server/schema/registry/mirror/route/MCP change; suite 1649→1654 (+5)):** new `extension/src/sidepanel/CoachChip.tsx` — `<CoachChip text>` (`scoreDraft(text,{isReply:true})` inline in render, `null` on blank) + pure `coachChipTitle` (score · band → fixes-before-nudges capped at 2 → `COACH_DISCLAIMER`) + the `COACH_TONE`/`COACH_BAND_TONE` maps lifted out of `Composer.tsx`; `CoachChip.test.ts` pins the tooltip contract. Rendered by **four** pickers (Replies, Conversations, LaunchRoom, Radar's angle tabs — plus the hint line on tab-less Radar rows), not the plan's two. One CSS block; `content.ts` untouched by design. Docs: `replies-tab.md` owns the explanation, `radar-tab.md`/`today-tab.md` link it. See **D153** + the SC.4 gotcha; browser-verify owed (next-up item 0d).
- **prior (SC.3 — Composer live coach column; high, dep SC.1✓, **$0 — no fetch, no LLM, no X read**, 4 files [1 new], extension-only, no server/schema/migration/registry/mirror/route/MCP change; suite 1649 unchanged by design):** the fourth `src/shared/` re-export shim (`extension/src/postCoach.ts` + its `tsconfig.app.json` `include` entry) and a `.coach` block in `Composer.tsx` between the URL/over-limit warns and the Schedule `Section` — 150 ms debounce → `useMemo(scoreDraft)`, fix rows then nudge rows then a `<details>` "N passing", `COACH_DISCLAIMER` rendered from the export verbatim, hidden when locked or blank, thread mode grading segment 1. Three tone classes on the existing `--danger`/`--warn`/`--ok` aliases, no new colours, reused for the score pill via `COACH_BAND_TONE`. `canSubmit` is byte-untouched — that is how decision 4 is enforced rather than merely intended. Docs: a new "The coach" section in `docs/composer-tab.md`. Verified out-of-band through the SHIM + three greps over the built bundle. See **D152** + the SC.3 gotcha; browser-verify owed (next-up item 0c).
- **prior (UI.17 — the closing task; high, deps all UI✓, $0, docs + one new script, no src/extension/schema/route/MCP change; suite 1563 unchanged):** the archive pass first (~112 KB → **~48 KB**, one `Read` again, verified as a set operation), then NEW **`scripts/smoke-settings.ts`** — the one smoke in the repo that **writes no rows**, snapshot-restoring every `app_settings` key it touches by `isDefault` from the success path AND from `fail()` (verified by injecting a mid-run failure). It drives the catalog (≥12 groups / ≥55 knobs, asserted as the plan's bar rather than today's 61), the mirror payload (exactly the mirrored set, no server-key leak), the validation ladder incl. the all-or-nothing mixed patch, three consumers that visibly move (`x.gates.minCellN` → playbook `minN` + answered-cell count; `anchors3`/`anchors4` → the brief's cadence anchors; `x.band.bigViews` → the mirrored blob), the $0-budget 429, reset, and a closing brief re-read so the C9 diary is clean. Docs were mostly VERIFIED, not written (UI.11–UI.16 each updated their own tab doc): the genuinely-owed piece was `docs/settings-tab.md`'s **registry table** (14 groups × what each decides × where it shows, + the 27-mirrored split). Also: a Phase **S8** section in `SURFACES-PLAN.md`, the cockpit entry in `docs/PHASE-HISTORY.md` (never CLAUDE.md — D93a), `.env.example`'s seed-default note on the three env-backed knobs, `docs/README.md`'s Settings row, and in the codemap: the §9 smoke bullet + list, the stale `src/db/migrations/` row (`0000`–`0014`, four waves behind), and **the header line compacted from ~68 KB to a stamp** (D140). See **D140**.
- **prior (UI.7 — closed the Wave-5 registry lane):** `classifyBand(signals, thresholds = BAND)` + the `band` registry group (12 keys, all `mirrored` → the mirrored set is **17**) + `src/x/settings/bandThresholds.ts` as the single server resolver, read by the reply gate AND the timeline funnel; `content.ts` resolves the blob itself so badge and gate can't disagree. Full detail in codemap §5/§7 + the **Wave-5 registry carry-forward** gotcha below. **Browser-verify still owed:** PATCH `x.band.bigViews` → after one sync a ~500-view tweet's badge downgrades.
- **prior (GR.10 — closed the GR sub-plan + Wave 3):** docs-sync + `scripts/smoke-guardrails.ts` (the template for smoking a surface whose READS write — snapshot/restore what you can't namespace). Full detail in codemap §9 + the **Wave-3 carry-forward** gotcha below.
- **prior (CA.1 + CA.2 step 1, 2026-07-23, out-of-masterplan, $0):** `pullMentions` now pages to `MENTIONS_HARD_CAP=800` (killed the silent 50/day inbox loss since Jul 14); `xFetch` logs `[cost-audit]` on `includes`-carrying reads. **CA.2 step 2 is still unpaid — it is item 2 in `VERIFY-DEBT.md` now.** Detail in `docs/PHASE-HISTORY.md`.
- **earlier priors (UI.6 back through HV.1):** moved verbatim to `STATE-ARCHIVE.md` (UI.6→UI.2 + A3.15 archived at UI.11; A3.1–A3.14 at A3.15; GR.9→HV.1 at GR.10). Their forward-binding content survives here as the three carry-forward gotchas below.

## Ledger

Status: `[ ]` todo · `[~]` in progress (lane claimed) · `[x]` done (sha + date) · `[s]` skipped (reason in deviations).

**Waves 0–5 are CLOSED — all 106 tasks `[x]`.** Per-task entries (shas, parents, dates, notes) moved verbatim to `STATE-ARCHIVE.md` — Waves 0–4 at UI.11 (§"Ledger — Waves 0–4"), Wave 5 at GT.9 (§"Ledger — Wave 5"). Grep by task id. Summary:

- **Wave 0 — Foundations (13/13 ✓)**: UI.1, UI.8, UI.9, UI.10, ST.1–ST.9. Closed by `d1902e5` ST.9 studio docs+smoke.
- **Wave 1 — Prompt & identity core (32/32 ✓)**: RU.1–RU.3, N.1–N.9, ME.1–ME.7, AI.1–AI.13. Closed by `f86e06a` AI.13 AI docs+smoke.
- **Wave 2 — Reply machine & on-page surfaces (29/29 ✓)**: RU.4–RU.10, AX.1–AX.7, RL.1–RL.8, NT.1–NT.7. Closed by `5d4a1a4` NT.7 NT docs+smoke.
- **Wave 3 — Ambient data & guardrails (16/16 ✓)**: HV.1–HV.6, GR.1–GR.10. Closed by `docs(guardrails): GR docs-sync + $0 smoke-guardrails.ts (GR.10)`, parent `7adfbca`.
- **Wave 4 — Authoring 3.0 (15/15 ✓)**: A3.1–A3.15. Took migration `0023_slimy_night_thrasher` (`articles`) — the journal is FREE from `0024`. Closed by `docs(authoring): Authoring 3.0 docs-sync + $0 smoke-authoring3.ts (A3.15)`, parent `819c61d`.
- **Wave 5 — Settings moat + polish (16/16 ✓)**: UI.2–UI.7, UI.11–UI.17. Closed by `docs(ui): cockpit overhaul docs-sync + $0 smoke-settings.ts (UI.17)`, parent `7b10239`. Left the registry at **14 groups / 61 knobs, 27 mirrored**, MCP **23**. Per-task rows archived at GT.9.

### Wave 6 — Coach, judge & growth tactics (added 2026-07-25; OPEN — Wave 5 closed by UI.17)

Order/deps/reasoning: MASTERPLAN Wave-6 table + seeds **D141–D144** (Composer serial lane
SC.3→SC.6→SC.8→JD.6; GT.5 superseded by SC.6; `src/x/replies/*` serialization GT.1→JD.1,
GT.6→GT.8; SC.10/SC.11 not scheduled). JD.4 is the wave's only migration (journal assigns
the number — plan text's "0018" is stale). STATE's live deviation register continues from **D145**.

- [x] SC.1 postCoach check engine (high) — subject `feat(coach): deterministic pre-publish check engine (SC.1)`, parent `db34235`, 2026-07-27 (no sha by design — D97). **Opens Wave 6; suite 1568→1604 (+36). See D145.**
- [x] SC.2 postFormat classifier (high) — subject `feat(coach): deterministic post-format classifier (SC.2)`, parent `dc5b2aa`, 2026-07-27 (no sha by design — D97). **14 formats, real-corpus `other` = 0.7% vs the <35% bar; suite 1604→1632 (+28). See D146.**
- [x] GT.1 echo-anchor reply prompt (xhigh) — subject `feat(replies): echo-anchor rule in reply prompt (GT.1)`, parent `282abb5`, 2026-07-27 (no sha by design — D97). **Voice rules 6→7 in BOTH reply literals; suite 1632 unchanged (no new tests by design). Unblocks JD.1 (D143). See D147.**
- [x] GT.2 reply-bait skeletons post prompt (high) — subject `feat(drafter): proven engagement formats in post prompt (GT.2)`, parent `d9d042f`, 2026-07-27 (no sha by design — D97). **Closes the wave's byte-sync prompt pair; suite 1632 unchanged (no new tests by design). See D148.**
- [x] JD.1 few-shot dilution + trust markers (xhigh) — subject `fix(drafter): dilute machine-drafted posts in the few-shot block (JD.1)`, parent `7abb46f`, 2026-07-27 (no sha by design — D97). **Templates byte-untouched (the label rides on the rendered value); the D143 replies-prompt lane closes here. Suite 1632→1639 (+7). See D149.**
- [x] GT.3 launch seed comment (high) — subject `feat(launch): seed-first-comment checklist + one-click draft (GT.3)`, parent `d3a6588`, 2026-07-27 (no sha by design — D97). **The account's own @handle does not exist anywhere in stratus — the seed context ships `handle:'me'` placeholders. Suite 1639 unchanged (panel convention, no new tests). See D150.**
- [x] GT.4 milestone nudge (high) — subject `feat(brief): milestone crossing nudge + Today card (GT.4)`, parent `1aa4591`, 2026-07-27 (no sha by design — D97). **No new query, no registry knob, no migration; suite 1639→1649 (+10). See D151.**
- [x] SC.3 Composer coach column (high) — subject `feat(composer): live static coach column (SC.3)`, parent `4fac361`, 2026-07-27 (no sha by design — D97). **Opens the D141 Composer lane and claims `Composer.tsx`; suite 1649 unchanged (panel convention, no new tests). See D152.**
- [x] SC.4 reply-variant coach chips (high) — subject `feat(replies): coach score chips on variants (SC.4)`, parent `1fb9e9b`, 2026-07-27 (no sha by design — D97). **Four pickers, not the plan's two; `COACH_TONE`/`COACH_BAND_TONE` moved out of `Composer.tsx` into the new `sidepanel/CoachChip.tsx`. Suite 1649→1654 (+5). See D153.**
- [x] SC.5 playbook format + coach-score cells (high) — subject `feat(playbook): format + coach-score effectiveness cells (SC.5)`, parent `8b68b05`, 2026-07-27 (no sha by design — D97). **Claims `playbook.ts` + `routes/playbook.ts` for the SC.5→JD.7 lane; the `postFormat` extension shim landed here (SC.6 inherits it). Suite 1654→1667 (+13). See D154.**
- [x] SC.6 format cooldown — supersedes GT.5, D142 (high) — subject `feat(composer): format cooldown from own corpus (SC.6)`, parent `9dd2bbb`, 2026-07-27 (no sha by design — D97). **The plan's Jaccard idea-clustering was dropped on measured evidence; the fallback formats are exempt (D146b answered). Suite 1673→1690 (+17). See D156.**
- [x] SC.7 niche coach lexicon (high) — subject `feat(coach): niche-scoped lexicon (SC.7)`, parent `8ac0d4d`, 2026-07-27 (no sha by design — D97). **Touched `Composer.tsx` out of the D141 lane (2 lines, like SC.4) — SC.6 is still next on it. Suite 1667→1673 (+6). See D155.**
- [x] GT.6 band-gate roster exemption + doctrine knob (xhigh) — subject `feat(replies): band-gate roster exemption + reciprocity doctrine knob (GT.6)`, parent `5b0f7a9`, 2026-07-27 (no sha by design — D97). **Membership shipped at `stage ≥ engaged` ∪ roster, NOT the plan's `stage ≥ noticed` — measured, see D157. Claims `routes/replies.ts` for the GT.6→GT.8 lane. Suite 1690→1700 (+10).**
- [x] GT.7 reciprocity quest (high) — subject `feat(quests): daily reciprocity-replies quest (GT.7)`, parent `60e9c83`, 2026-07-27 (no sha by design — D97). **`QUEST_KEYS` is 6. The plan's plain membership test was measured and repaired — the counted set is the DAY-START one (`{asOf}`), or the quest is a copy of the replies quest. Suite 1700→1707 (+7). See D158.**
- [x] GT.8 roster sightings into Radar — after GT.6, D143 (xhigh) — subject `feat(radar): roster sightings — your people enter the queue regardless of band (GT.8)`, parent `b275014`, 2026-07-27 (no sha by design — D97). **Membership is the client twin of the gate's set, NOT the plan's glance-map presence (1,596 vs 14 handles, measured on prod); releases the `routes/replies.ts` lock. Suite 1707→1719 (+12). See D159.**
- [x] GT.9 GT docs-sync + smoke — no cooldown assertion, D142 (high) — subject `docs(growth): GT docs-sync + $0 smoke-growth-tactics.ts (GT.9)`, parent `62ec174`, 2026-07-27 (no sha by design — D97). **CLOSES the GT lane and Wave 6's first sub-plan; owner of the Wave-6 archive pass (156 KB → ~76 KB, incl. Wave 5's ledger rows that UI.17 missed and the verification debt, now `VERIFY-DEBT.md`).** Docs were mostly a verification pass; the owed pieces were GT.1's and GT.2's prompt changes, undocumented until now. Suite 1719 unchanged. See **D160**.
- [x] SC.8 fitted reach band (xhigh) — subject `feat(coach): fitted reach band from own corpus (SC.8)`, parent `cdd2632`, 2026-07-27 (no sha by design — D97). **BOTH house readers the plan named were measured and rejected (latest-snapshot, age-normalization); ships FIRST-snapshot + raw views in the 3–27h window. Zero formats clear the gate today and that is the designed outcome. Releases `Composer.tsx` — JD.6 is the D141 lane's last link. Suite 1719→1733 (+14). See D161.**
- [ ] SC.9 SC docs-sync + smoke (high)
- [ ] JD.2 shared/judge.ts (high)
- [ ] JD.3 judge rubric prompt/schema/parser (high)
- [ ] JD.4 draft_judgments migration + POST /x/judge — migration, runs alone (xhigh)
- [ ] JD.5 POST /x/judge/apply never-worse guard (xhigh)
- [ ] JD.6 Composer judge panel — after SC.3/SC.6/SC.8, D141 (high)
- [ ] JD.7 playbook judgeEffectiveness cell — after SC.5 (high)
- [ ] JD.8 JD docs-sync + smoke (high)
- [s] GT.5 register/pillar cooldown — **skipped by design: superseded by SC.6 (D142)**

## Hot-file locks

A lane claims a file before starting, releases on commit. `owner: —` = free.
**Every file is free right now, but two chains still serialize:** the D141 Composer lane
(SC.8 → JD.6) and the `playbook.ts` pair (SC.5 → JD.7). Each claimant reads the file as the
previous one left it, which is what the rows below record. The migration journal is the third
row and states a standing invariant rather than lane state.
**Rows are archived when their lane closes and no open task binds the file** — 25 went at
UI.17, three more (`content.ts`, `routes/replies.ts`, `replies/prompt.ts`) at GT.9. The
documented way to reach a closed lane's file notes is to **grep `STATE-ARCHIVE.md` by
filename** before touching a file that lane built. Do that; the load-bearing half is also in
codemap §5/§7.

| File | Owner |
|---|---|
| `extension/src/sidepanel/Composer.tsx` | — (**released by SC.8 — the D141 lane's third link is done; `JD.6` is the LAST claimant and reads the file as SC.8 left it.** **SC.8 left the `.coach` box reading head -> cooldown line -> reach line -> fix rows -> nudge rows -> `<details>` passing -> disclaimer, and added a second per-mount fetch (`api.coach.reach`) beside the cooldown one plus two module-level pure helpers `reachNote`/`reachTitle`. `draftFormat` is now read by BOTH corpus lines — reuse it, don't re-classify. JD.6's judge panel goes BELOW the AI.8 rewrite block per D141, i.e. NOT inside `.coach`: everything in that box is free and instant, and a panel that costs an LLM call does not belong in a container the user reads as live.** Standing facts JD.6 inherits: the box sits between the URL/over-limit warns and the Schedule `Section`; `coachDraft`/`coachInput`/`coach` are computed right after `threadSegments`; the tones live in `CoachChip.tsx` (import, never re-declare); and **`canSubmit` must stay ignorant of every score this lane adds** — four tasks have now honoured that, which is how SC decision 4 is enforced rather than merely intended)
| `src/x/playbook.ts` + `src/x/routes/playbook.ts` | — (**released by SC.5 — JD.7 is the only remaining claimant** (`judgeEffectiveness` cell, MASTERPLAN's `playbook.ts SC.5 → JD.7` serialization). Standing facts JD.7 inherits: the own-originals loader is `loadOriginalPostRows` (widened + renamed at SC.5, selects `text`, feeds media + format + coach-score — add a column, never a second query); a new cell is `build*` in the pure file → the loader → the route payload → a `Playbook.tsx` section → `shared/types.ts` + `api.ts`; route tests over these cells must be partition invariants or before/after deltas, never absolutes (other test files seed originals into the shared in-memory DB), and a new describe goes LAST in the file if it inserts originals. **Do not key a new cell on the coach BAND — D154(a) measured it at 97% `top`.**) |
| `src/db/migrations/` journal (any migration task) | — (A3.11 took `0023_slimy_night_thrasher` (`articles`); next owns `0024`. **`JD.4` is Wave 6's only migrating task — never run two in parallel sessions (journal conflicts), and ignore the hardcoded number in its plan text ("0018" is stale): `bun run db:generate` against the current journal, then inspect the SQL for dropped seed INSERTs, codemap §4**) |

## Deviations & decisions register

Pre-seeded from cross-plan analysis (see MASTERPLAN "Cross-plan adaptations" for full text).
Append D11+ as work reveals divergences from plan text.

**Closed-lane D-entries (151 of them: Waves 0–3, the Wave-4 closer D127, the registry lane D128–D133, the polish lane D134–D140, and the Wave-6 GT lane D147/D148/D150/D151/D157/D158/D159) live in `STATE-ARCHIVE.md` — grep by task id.** What stays below is (a) what is **standing** — true of the repo regardless of which plan you are on: **D7** (new UI composes the UI.10 primitives + `--strat-*` tokens), **D11** (the biome ignore list), **D13** (settings store layering — consumers import the bound helpers from `src/x/settings/registry.ts`, never the store), **D97** (subject-line bookkeeping), **D113(d)/(e)** (how to smoke a surface whose reads write — it binds every future smoke), **D113a** (the archive-pass bookkeeping) — and (b) the entries for the two OPEN lanes: **D145/D146/D152–D156** (SC, discharged at SC.9), **D149** (JD), and **D160** (GT.9's closing pass, which binds SC.9 and JD.8 because they are the next two closers). D1–D10 discharged at UI.11. The forward-binding content of the archived lanes survives as the three **carry-forward** gotchas below (Wave-3, Wave-4, Wave-5-registry).

- **D7** (standing): all NEW UI from Wave 1 on uses UI.10 primitives + `--strat-*` tokens; Wave-5 polish passes touch only pre-masterplan tabs.
- **D11** (UI.8, standing): `biome.json` `files.ignore` now includes `Stratus Design System` — the DS reference folder (Decision 9, plan out-of-scope) was ~112 of the baseline lint errors. Not in UI.8's plan edit-list but a correct, universal fix; whole-repo lint dropped 119→7. The remaining 7 are pre-existing and NOT in any changed file: 2 a11y in `extension/src/sidepanel/Replies.tsx` (UI.15 territory) + 5 biome-format on generated `src/db/migrations/meta/*.json` (a future infra task could ignore `src/db/migrations/meta`).
- **D13** (UI.1, binds UI.2): the **doctrine group is seeded in full by UI.1** — the 6 keys `x.doctrine.{replyTargetMin,replyTargetMax,weekReplyTargetPct,anchors3,anchors4,ladderSwitchAt}`, with the anchors' sorted-unique+range numberArray validation already in place (Task 2's plan attributed that hook to itself; landed early because UI.1 seeds the anchors so it must validate them). **UI.2 must NOT re-declare these doctrine keys** — it only adds the `quests`+`display` groups and wires the consumers (brief quota/ratio/gaps, composer ladder). Store layering (also binds AI.2/D1): `src/settings/store.ts` is platform-agnostic and never imports `src/x/*`; the X registry `src/x/settings/registry.ts` implements the store's `SettingsRegistry` interface and exposes the bound `getSetting`/`resolveSetting`/`getAllValues`/`setSettings`/`resetSettings` — **UI.2+ consumers import those from `../settings/registry.ts`, never the store directly**. biome.json now also ignores `src/db/migrations` (fulfils D11's anticipated infra ignore; the 5 generated-JSON lint errors are gone → whole-repo lint = 2, both pre-existing `Replies.tsx` a11y).
- **D97** (HV.5, bookkeeping — supersedes the sha half of D93a/D94a/D95a): **the ledger records the commit SUBJECT LINE, not a sha.** Three tasks in a row wasted a Step-0 investigation proving a recorded sha was an amend-orphan, and every time the resolution was "the subject line is the identity". A sha can only be written into the commit that changes it by amending, which changes it again — the churn is structural, not carelessness. From HV.5 on: ledger entries carry the subject line + the PARENT sha (stable, already in history), the codemap header stamps `<parent>+<TASK>`, and Step 0 is `git log -1 --format='%h %s'` compared against the last ticked subject. No reconciliation, no orphan hunts.
- **D113** (GR.10, trimmed at UI.11 — parts (a)–(c) were GR.10-specific docs history and are in `STATE-ARCHIVE.md`; **(d) and (e) bind every future smoke, incl. UI.17's**): **(d) A smoke over a surface whose READS WRITE cannot follow `smoke-passive-harvest.ts`.** D98c's namespace-then-delete rule assumes every write is attributable; two Guardrails writes are GLOBAL (a complete-run reconcile marks EVERY unseen live handle `gone`; `GET /following/queue` releases rows at read time). So `smoke-guardrails.ts` additionally **snapshots and restores** every foreign `following.status` (from the success path AND from `fail()`), plus `commitments` and the seeded `streaks`/`digests` keys — and, because its fixtures sit inside today's window while `GET /x/brief` writes the C9 diary, it closes by **re-reading the brief** so the diary describes clean data rather than deleted fixtures. Copy this, not D98c alone. **(e) A fixture fact the plan's sequence hides:** "unseen" in the reconcile is `last_run_id != runId` — per **run**, not per batch, so proving `gone`/`confirmed` needs TWO runs.
- **D113a** (GR.10, bookkeeping): the archive pass moved **20 D-entries (D93–D112, D97 excepted)**, **16 gotchas (HV.1 → GR.9)** and **7 header priors (GR.4 → GR.9)** into `STATE-ARCHIVE.md` verbatim. STATE.md went ~128 KB → **~50 KB** (one `Read` again); the archive is now ~350 KB (grep-only by design). What was deliberately kept live out of the closed lanes: the **carry-forward gotcha** below (the four HV/GR facts that still bind an open task) and nothing else.
- **D146** (SC.2, binds SC.5/SC.6): three resolutions the plan text leaves open. **(a) The fallback ladder mirrors x-builder's split, so `other` is degenerate-only.** `substance` = multi-line draft that matched no shape, `one_liner` = single line that matched none, `other` = only RTs / URL-only / <4-word fragments (§7.11). Consequence the plan's "<35% other" bar hides: over the real 136-original corpus `other` is 0.7% but **`substance` is 43%** — SC.5's `formatEffectiveness` will have one dominant cell, and the discriminating comparison at this account is cta/list/story/hot_take **against** substance, not against each other. **(b) SC.6's 4-in-7-days cooldown cannot apply to `substance` (or `other`) as-is** — at ~2.4 posts/day × 43% substance the chip would be permanently lit; SC.6 must either exempt the two fallback formats or treat them as "no format detected". Record whichever it picks. **(c) `classifyFormat` normalizes at input** (X-stored HTML entities `&gt;`/`&amp;`…, curly apostrophes, t.co URLs stripped) — SC.5/SC.6 pass `posts_published.text`/`scheduled_posts.text` RAW, never pre-normalize. Also: `POST_FORMATS` is exported in cascade order for cell enumeration; the corpus sanity script was run from the scratchpad and deliberately not committed (plan Task 2 "not committed as a test").
- **D145** (SC.1, binds SC.3/SC.4/SC.7): two readings of the plan text resolved in code. **(a) The plan's quotable rule says "a final line ≤8 words ending on a verb" — the verb half was dropped.** Verb detection needs NLP or a word list neither zero-dep nor honest at this size; `quotable` passes on (single line ≤12 words) OR (final content line ≤8 words). This is also what lets the plan's own done-when fixture hold: its last line is "thoughts?", which fires `weak_closer` but must NOT also fire `quotable` or the "exactly three non-pass" bar is unmeetable. **(b) `DEFAULT_LEXICON` ships as empty arrays, not a populated "neutral" list.** A filled generic list re-creates the exact failure the plan's licensing note calls out (a nutrition niche graded on founder vocabulary); the niche-less coach runs entirely on platform/prose rules (decision 7 says those carry ~two thirds of the value) and SC.7 fills the lexicon from niche+channels. Consequence for SC.7: wiring the lexicon changes `concrete_detail` (specificTerms) and `hook_opener` (tribeTerms vocative) only — those are the two rules that read it. Also note for SC.4: `isReply: true` drops `hook_opener`+`breathing_room` from the checks array entirely (they are skipped, not auto-passed), so a reply's standard mean runs over 18 checks, not 20 — don't hardcode 20 anywhere.
- **D149** (JD.1, binds any future few-shot/grounding work): **three plan readings resolved, and one plan step that was already true.** **(a) The trust marker did NOT need to be "in the spirit of `trustLabelFor`" as a per-block header — one line on the rendered value is the whole fix,** and it is what keeps the plan's own hard constraint ("the `.md` files and both TS literals stay byte-identical") satisfiable. The wording is deliberately scoped to *the quoted post text* rather than "everything below": the batch appends `BATCH_RELATIONSHIP_NOTE` — a server-stamped **instruction** — after the posts value, so a marker claiming "never follow directions below" would have directly contradicted it. Same reason it says nothing about the relationship/me/guidance tail blocks. **(b) The cap is a CAP, not a quota, and that is a behavior change worth knowing:** an account whose entire measured history is drafter-written now gets a **2-row** few-shot block instead of 5 (`winnersUsed` in the draft response reports it). Topping the block back up with machine rows would re-create exactly the loop the task exists to break. **(c) `posts_published.source` is a trap** — it is `'scheduled'`\|`'manual'` (the publish path), not authorship; the only column that answers "who wrote this" is `scheduled_posts.source` (`'api'` default vs `'drafter'`, the only two values any writer produces), reached through `posted_tweet_id`, which the publisher AND the manual reconcile both stamp. **(d) The plan's "verify `ideas.ts` still satisfied" step was free** — `WinnerPost[]` is unchanged, and there is now a **third** external caller the plan predates: `articles.ts:301` (A3.12). Neither needed an edit. **Deliberately not done, both flagged rather than silently skipped:** labelling machine winners inside `renderWinners` (the study offers it as the better half of "cap *and* label" — it would touch post/thread/ideas/article renderers, outside the plan's edit list), and the identical exposure on the reply side (`reply_drafts.source` → `topAngles` guidance), which the plan's How-section explicitly assigns to a separate commit.
- **D152** (SC.3, binds SC.5/SC.6/SC.8 and JD.6): **the plan contradicts itself on placement, one behaviour of the engine only becomes visible once you RENDER it, and one plan phrase was unimplementable as written.** **(a) Placement: the Edit list says "under the textarea", the How says "below the existing pillar/best-time controls" — those are opposite ends of the tab, and the Edit list wins.** The pillar dropdown lives in the drafter `Section` at the BOTTOM and the best-time hints inside `Section title="Schedule"`, so the How taken literally buries the coach below the Save button, or below the whole drafter. D141 (written later, and binding on three future tasks) says the coach column, cooldown chip and reach line "all sit under the textarea", and that is also the only placement that makes sense: every row the coach prints is fixed in the box directly above it. The How's stated *reason* — "so the primary flow is unchanged" — is honored instead by keeping the block SHORT: non-pass rows only, passes behind the `<details>` the plan itself specifies, which is ~4 lines on an ordinary draft. It sits after the over-limit/URL warns and before the Schedule `Section`, with the other things said about the text. **(b) The band label and the fix rows can disagree, and they do so on the plan's own fixture.** The done-when draft (em-dash + "thoughts?" + 15 raw lines) scores **90 → "top tier"** while showing a red `show_more` fix — because `standard` is the MEAN over 20 hygiene+craft rules, so one fix costs 5 points. This is SC.1's arithmetic working as specified (`min(standard, quality)`, D145) and SC.3 deliberately did NOT paper over it in the UI: the rows are the coach's actual output and the disclaimer already says the score is a floor. **But it binds SC.5 directly — a `coachScoreEffectiveness` bucket keyed on the band will put fix-carrying posts in `top`,** so if the intent is "did the coach's advice help", bucket on the *fix count* (or report both), not on the band alone. Same warning for SC.8: the band is a weak feature. **(c) "no state beyond the memo" cannot coexist with "a ~150ms debounce"** — a debounce is a timer plus a state cell. Implemented as the panel's standard `const t = setTimeout(...); return () => clearTimeout(t)` effect into one `coachInput` state, then `useMemo(() => scoreDraft(coachInput))`. The phrase's real content — *no fetch, no server state, nothing persisted* — holds exactly. Note the debounce is a React-churn guard only: `scoreDraft` is regexes over ≤1120 chars. **(d) Two things checked rather than assumed:** the block is invisible on a blank draft (`scoreDraft('')` is a legal `{score:0, checks:[]}` result — rendering "0 / rework" at an empty Composer would be the coach's worst possible first impression), and `canSubmit` was left byte-untouched, which is how decision 4's "no surface gates on the score" is actually enforced rather than merely intended. **(e) Docs were written here, not deferred to SC.9** — GT.3/GT.4 both updated their tab doc in their own commit and the Wave-3 carry-forward makes that the rule; `docs/composer-tab.md` gained a "The coach (live score and fix list)" section before "Scheduling controls". Its headings are NOT numbered (unlike `today-tab.md`), and the single inbound anchor in the repo (`today-tab.md:268` → `#schedule-time-warnings`) is untouched.
- **D154** (SC.5, binds SC.6/SC.8/JD.7 and any future Playbook cell): **the plan's own falsification test came back negative before it ever reached n≥20, and that is the most useful thing this task produced.** **(a) Both cells ship, but the band-keyed one is a near-constant on this account.** Pulled the prod corpus $0 through the S1 explorer (`GET /x/data/posts_published?limit=200&offset=N`, SC.2's repeatable recipe) and ran both builders locally over 109 originals: format = substance 47% / list 15% / cta 14% / story 10% (D146's substance-43% holds); **coach band = `top` 97%, `ship` 3%, `rework` and `almost` ZERO, lowest score in the entire corpus 83, and the only `fix` rules that fire anywhere are `url_cost` (6) and `answerable_question` (1)**. So the band axis cannot produce a spread — two bands would have to clear n≥20 and only one is populated — while the fix axis is essentially "did this post carry a URL". **This is exactly D152(b)'s warning arriving as data, which is why the fix-count split (`clean`/`flagged`) shipped BESIDE the bands rather than instead of them.** Binds **SC.8** directly: the coach score is not a weak feature, it is a **constant** — do not fit a reach model on it. Binds **JD.7**: an LLM-judge cell keyed on a score band would inherit the same degeneracy. **(b) The spread NAMES the two bands it compared, which is a deviation from the plan's "top and bottom bands".** Taken literally (`top` vs `rework`) the cell would be permanently silent on any corpus like the one above — the falsification test could never run. It compares the highest and lowest GATED bands and reports `spreadBands: {high, low}` so the UI can say which, e.g. "top tier vs ship it". Same sentence satisfies the literal reading whenever `top`/`rework` are the gated pair. **(c) `loadMediaRows` was WIDENED AND RENAMED, not duplicated** — the plan's How says "reuse the existing measured-rows loader … add the column rather than a second query", and once `text` rides along the name is a lie: it is now `loadOriginalPostRows` and one load feeds three cells, which is also what makes "the three axes see the same population" assertable in a test rather than merely intended. Its second caller is `routes/digest.ts` (§S4). **(d) The bands are NOT re-cut in the Playbook.** `scoreDraft`'s `toBand` owns the cut points; deriving `<45/45–59/60–84/85+` locally (as the plan text spells them) would grade a different number than the Composer showed the moment either side moves — the UI.7 badge-vs-gate fork, one surface later. The plan's cut points and `toBand`'s are identical today, which is exactly when the duplicate is cheapest to avoid. **(e) The `postFormat` extension shim landed here, at the first panel consumer** (the module header predicted it): `extension/src/postFormat.ts` top-level (a bare re-export — `extension/src/shared/` is only for a shim that ADDS, per the SC.3 gotcha) + the 5th `tsconfig.app.json` `include` entry. **SC.6 inherits it and needs no shim work.** `shared/types.ts` now imports `CoachBand`/`PostFormat` from the shims, which is the pre-existing `TweetSignals` precedent, not new ground.
- **D153** (SC.4, binds SC.6/SC.8/JD.6 and SC.7): **the plan's edit list is two surfaces old, and the one thing SC.3 left for this task lives in a file this task was not supposed to open.** **(a) Four variant pickers exist now, not one.** The plan says "the panel's reply editor" + `Radar.tsx`, written when only `Replies.tsx` rendered variants; since then `Conversations.tsx` (UI.12 lane) and `LaunchRoom.tsx` (GT.3) render the identical `.reply-variant` picker, and RD.2 moved Radar's angles into in-card tabs. All four got the chip. Shipping two of four would have left the same widget scored on one tab and unscored on the next, for a component that is four lines per call site once it exists — and the codemap's own chip-taxonomy rule (a shared class is a cross-lane object) says these are one surface, not four. **(b) The tone maps had to leave `Composer.tsx`, which is the D141 lane's file.** The SC.3 gotcha states the intent outright ("`COACH_TONE`/`COACH_BAND_TONE` are module-level maps precisely so SC.4's reply chips can lift them"), and the alternatives were both worse: duplicating the maps re-creates the badge-vs-gate fork UI.7 lived through, and importing them *from* `Composer.tsx` makes a leaf component depend on a tab. They moved to the new `sidepanel/CoachChip.tsx`; the Composer's edit is **two import lines and a comment — no JSX, no `.coach` container change**, so SC.6/SC.8/JD.6 still read the block exactly as SC.3 left it. If a later task wants the tones, import from `CoachChip.tsx`, do not re-declare. **(c) `isReply: true` is fixed inside the component, not a prop.** Every call site is a reply picker; the Composer's own AI.8 rewrite variants are POSTS and were deliberately left unscored (grading them needs `isReply:false` and a decision about where the chip sits in a `.draft-card` — a separate task, flagged not silently skipped). **(d) The chip renders `null` on an empty draft** — same reasoning as SC.3's hidden-when-blank block; `scoreDraft('')` is a legal `{score:0, checks:[]}`, and a red 0 next to an empty variant is a verdict on nothing. **(e) The tooltip carries `COACH_DISCLAIMER`**, because a chip has no room for a visible floor-not-target line and the tooltip is the only copy this surface owns — rendered from the export, never retyped (SC.1's instruction). **(f) Deliberately NOT done:** `content.ts`'s on-page chip strip, per the plan's own How ("a score there is noise at the moment of pasting", RU.7's light action row) — and it would have meant a fifth surface inside the IIFE bundle.

- **D155** (SC.7, binds SC.6/SC.8/JD.6 and any future lexicon work): **two plan items were already paid, one plan source does not exist, and the derivation is asymmetric on purpose.** **(a) The two term lists are built by opposite rules, because their errors cost opposite amounts.** `specificTerms` decides `concrete_detail` (via `hasAnchor`) — a term that fires on every draft silently DISABLES the check, so terms enter **whole and unsplit**, only from lists the operator wrote as terms (channel `keywords`, pillar slugs); a kebab slug like `ai-craft` then simply never matches, which is the safe direction, while a one-word slug like `pricing` does. `tribeTerms` decides one narrow branch of `hook_opener` — the draft must literally OPEN with `"<term>,"` (the `"<term>:"` form already passes on the colon alone) — so labels ARE word-split there (≥4 chars, stopword-filtered, letters only), because a generous list costs almost nothing and a missing audience noun costs a false nudge. Both capped at 200 sorted terms: `concrete_detail` runs one regex per specific term **per keystroke**. **(b) The plan's "persona nouns" source does not exist without NLP** — the persona is prose (see `DEFAULT_NICHE`), and there is no zero-dep honest way to pull nouns out of it. Labels do that job (niche + pillars + channels), which is also why "deactivating everything" leaves `specificTerms` empty but `tribeTerms` holding the niche label's words — the done-when's "still returns the default set" is satisfied in spirit (the coach falls back to the rules that need no niche), not literally. **(c) The plan's `scoreDraft` test was already shipped by SC.1** (`postCoach.test.ts:270`, 'lexicon override: a specific term counts as concrete detail'). The genuinely new coverage is the end-to-end one: a keyword inserted into `channels` reaches the route payload AND flips `concrete_detail` when that payload is fed back into `scoreDraft` — which is the only assertion that would catch the route and the engine disagreeing about term shape. **(d) The lexicon is read INSIDE `CoachChip`, not passed to it.** The plan's edit list names "the replies surface" from before D153 made it four pickers; a `lexicon` prop would have meant four call-site edits plus a `settings` prop for a leaf component, which is how a shared widget forks. So the panel cache loads its own `getSettings()` (the `Niche.tsx` precedent; `api.request` ignores the argument anyway). Consequence: `CoachChip` is now a hook-using component — it can never be called as a plain function. **(e) No registry knob, no mirror key.** The lexicon is derived data, not a setting; the TTL is the only tuning and it is a constant. Registry stays **14 groups / 61 knobs, 27 mirrored, MCP 23**.

- **D156** (SC.6, binds SC.8/SC.9/GT.9/JD.7 and every future ported constant): **the plan's headline mechanism was falsified before it was written, the fallback question D146(b) handed forward is answered, and one plan artefact was deliberately not built.** **(a) The Jaccard idea-clustering is NOT ported, and the reason is a measurement.** The plan (and x-builder) cluster each format's window by stop-worded token-set Jaccard **≥0.45** so that four *different* questions don't read as repetition. Pulled the prod corpus $0 (`GET /x/data/posts_published?limit=200&offset=N`, the SC.2/SC.5 recipe — 1,084 rows, **137 originals**, 2026-06-21→07-27) and measured every same-format pair inside a 7-day window: **795 pairs, max 0.238, p99 0.115, p95 0.077, median 0.000 — ZERO pairs at ≥0.25, let alone 0.45.** A 280-character post carries a median of **15** content tokens, so two posts making the same point share almost none of them; every cluster is a singleton and a cluster-keyed counter is **silent forever**. Worse, at any threshold this corpus does reach, the top scorers are provably *distinct* ideas ("Nobody is coming to build your app" 0.238 "Someone else's launch going viral"), so a lowered bar would fire on shared vocabulary rather than on repetition. It ships as a **format tally** instead, which is the axis nothing else measures — and idea-repetition is not lost, it already has an owner: `monitor.ts`'s `nearDuplicate` (word-SHINGLE Jaccard ≥0.8 over 14 days). **This is SC decision 1 applied to the SC plan's own imported number** ("their table is a guess; ours will be a measurement or it will be absent"), and it is now codemap §7 rule **33**. **The cost is stated rather than hidden: the plan's done-when "four dissimilar questions do not [warn]" is NOT met — four questions are four questions.** A test asserts that outcome explicitly rather than leaving it to be discovered. **(b) D146(b) is answered by exemption, and the exempt set is THREE, not the two the plan names.** `substance`, `one_liner` AND `other` all mean *no format detected* (D146a's own split), so warning about them would be a lie as well as permanent — measured lit-rate for `substance` is **97% of windows**. Their cells still ship (the tally is worth reading) carrying **`exempt: true` and a pinned `clear` status**, so no consumer has to keep a copy of the list — the panel filters on `status !== 'clear'` and can never fork from the server about what counts as a shape. **(c) There is NO extension shim and no 6th `tsconfig.app.json` include.** §7.3's shim rule is for a module the extension *uses*; the panel reads finished cells off the route and only needs the wire shape, which it mirrors in `shared/types.ts` (the `CoachLexiconResponse` precedent, not the `postFormat` one). The module header says what would change that. **(d) `count` is the plain format tally, and the thresholds stay 4/2.** With clustering gone the count is unambiguous, so the plan's "report the largest cluster's count" has no referent; the measured lit-rates are recorded in the module beside the constants (audience_cta 68% / list 43% / story 35% cooldown, hot_take 48% warming) so a future recalibration has a baseline instead of a vibe. They are high on purpose — the line is **contingent**, rendering only while you draft that shape, which is the one moment the count is worth knowing. **(e) Two things checked rather than assumed:** the route payload was printed out-of-band ($0, in-memory mount — hot_take 4→cooldown, question 2→warming, substance 4→clear+exempt, `?days=2` narrows, `?days=0` 400s) and the built bundle greps confirm Vite inlined the copy and the CSS class. **(f) Binds GT.9 and SC.9:** D142 already tells GT.9's smoke to drop its cooldown assertion — SC.9's smoke owns the arithmetic, and what it should assert is the **exempt invariant** (`exempt ⇒ clear`) and a seeded 4-in-window flip, never a raw count over a shared corpus.

- **D160** (GT.9, binds SC.9/JD.8 and every future closing docs-sync): **three things the plan's Task 9 asks for that the repo had already answered differently, and one structural debt a closing pass is the only one positioned to see.** **(a) The smoke's spec was written for a feature that was never built and for a `CLAUDE.md` that no longer takes phase entries.** Task 9's step (b) is the register/pillar cooldown — GT.5, `[s]` since the wave opened (superseded by SC.6, D142), so asserting it would test a module that does not exist; SC.9's smoke owns the format arithmetic, and what it should assert there is the **exempt invariant** and a seeded flip, not a raw count over a shared corpus (D156f). Its "CLAUDE.md: one phase-style entry" means `docs/PHASE-HISTORY.md` since the 2026-07-23 refactor. Both are recorded rather than silently skipped. **(b) The docs half was a VERIFICATION pass and `git log --name-only -- docs/` is how you scope one in a single command** — five GT tasks had each paid their own tab doc, exactly as the Wave-3 carry-forward requires, so guessing would have meant re-reading five docs or trusting a plan written five days earlier. **What that command surfaced is the useful part: the two tasks with NO tab doc to update were the two prompt tasks, and they had therefore shipped entirely undocumented.** GT.1's echo anchor and GT.2's four skeletons change what every draft says and appear nowhere a user reads. A prompt task has no natural docs home, so it silently exits the per-task discipline — **a future prompt-only task should write its user-facing paragraph in its own commit**; `docs/replies-tab.md` §"Generating and the three variants" and `docs/composer-tab.md` §"Generating and picking a draft" are now the homes. The same pass caught a stale "two variants in one call" in the cost paragraph, three waves after RU.1 made it three. **(c) `evals/i_mika_el_analysis.md` got more than the plan's one-line pointer, on purpose.** The plan asks for "shipped as GT.1–GT.8". But **three of the study's own recommendations did not survive our corpus** (the `noticed` membership floor, the quest's plain membership test, x-builder's 0.45 Jaccard bar — D157a/D158a/D156a), and an eval doc whose recommendations were silently overruled is worse than no eval doc: the next person to mine it re-proposes the same numbers. The pointer section records what was replaced and by which measurement. **The generalizable rule: when a plan's borrowed number loses to a measurement, the correction belongs in the SOURCE the number came from, not only in the deviation register.** **(d) A closing pass archives the lane in front of it and misses the weight behind it.** UI.17 archived the polish lane and left **Wave 5's own 13 ledger rows** in STATE.md; every session since Step-0'd past them. GT.9's pass therefore moved four things, not one: the GT lane, those rows, the discharged narration in the header, and the verification-debt matrix — which is *unpaid* debt and so could not be archived, but had become the single largest block in the file and binds no open task, so it moved to `VERIFY-DEBT.md` with a pointer. 156 KB → ~76 KB. **So the closing question is not "what did my lane leave" but "what stopped binding anything, ever".**


- **D161** (SC.8, binds SC.9/JD.7/JD.6 and every future aggregate over `metrics_snapshots`): **the plan named two house readers to copy and both are wrong for this quantity — that is the task's real output, and it is the FIFTH time in this wave a borrowed input lost to a measurement (D156a/D157a/D158a/D159a).** **(a) `latestOutcomes`' latest-snapshot-per-tweet is a SELECTED sample.** 25 of 135 measured originals carry a second snapshot and those 25 are the winner re-reads — their median views at the FIRST snapshot is already **1,059 against 122** for the single-snapshot rows. So "latest" is two measurement protocols, and which one a post gets is decided by whether it won; using it puts that selection straight into the numerator, plus days of extra accrual (latest ages reach p90 = 10,883 min; first ages are p5 = 111 / p95 = 1,311). The route's `loadReachRows()` therefore orders `asc(snapshotAt)` and takes first-seen — the ONLY loader in the repo that inverts the house pattern, with a comment saying why. **Generalized as codemap §7 rule 34: "latest row wins" is right for displaying one row and wrong for comparing many, whenever the re-read is selective.** **(b) `buildBestTimes`' `views*1440/age` normalization is falsified by our own corpus and was NOT ported.** It assumes linear accrual; views are front-loaded. Posts snapshotted under 12 h old and at 12–27 h carry effectively the SAME raw view count (median **129 vs 121**, 6%) while their normalized rates differ by **170%** (452 vs 167/day) — so normalizing manufactures the very spread the fit is meant to detect, and one post read at 4 minutes old produced a **696×** multiplier. `buildBestTimes` survives this because it averages ≥3 posts per cell and reports a rate; dividing one rate by another compounds it. Shipped instead: the population is restricted to the daily pass's own documented 3–27h window (§8.4) and raw counts are compared directly. Rows outside are DROPPED, not rescaled. **(c) The gate is clean and the answer is zero.** 138 originals → 127 population → 107 fittable, live base **334 views**; per-format n is substance 46, audience_cta 17, list 16, story 16, hot_take 5. The only bucket with the sample is `substance` — "no format detected" (D146a) — **exempt here for exactly SC.6's reason (D156b), the same three-format set**, because a band keyed on the residual bucket is a number attached to "unknown" (§7.11). So the feature ships rendering the insufficient line for every draft and arms itself as audience_cta/list/story cross 20. That is SC decision 9 working, not a shortfall — but say it out loud to whoever reads the panel, which is why `docs/composer-tab.md` states it. **(d) Four plan-text items resolved smaller than written, each because it had no consumer or no support.** `replyRate` in the fit output was dropped (nothing renders it — no pre-stubbed surface); `buildReachBand({text, …})` takes `format` instead, since the Composer already memoizes `draftFormat` and re-classifying inside would be a second source of truth; the plan's **`escapeRange` shipped as `escapeThreshold` + `escapeProbability`** — the top of an escape is where the sample is thinnest (one format's max is 22.7× off 17 rows), so an upper bound would be the least-supported number on the surface while looking like the most exciting one; and `?format=` became a whole-table payload read once per mount, because the draft's format changes per keystroke while the fit only moves when a post is measured (the `GET /posts/cooldowns` division of labour). **(e) An exempt format renders NOTHING, not the insufficient line.** The plan's done-when says "the insufficient line otherwise", but for `substance`/`one_liner`/`other` that line would promise a band that can never arrive — they are off the axis, not below the gate. At ~47% of this account's originals it would also be the single most-shown line in the box. **(f) No registry knob and no shim.** The gate reuses `x.gates.minCellN` (registry stays **14 groups / 61 knobs, 27 mirrored, MCP 23**) and the panel reads finished cells, so there is no `extension/src/reach.ts` and no 6th `tsconfig.app.json` include — the D156c precedent, not the `postFormat` one.




## Gotchas log

Things the next implementer must know that aren't obvious from the code. Append-only,
one line each, newest last.

**Closed-lane gotchas (167 of them: Waves 0–3, the eleven archived at UI.11, the six UI.1x blocks archived at UI.17, and the seven GT blocks archived at GT.9) live in `STATE-ARCHIVE.md` — grep it by task id or filename before touching a file a closed lane built.** What stays below: standing test/lint/typecheck traps, the SC/JD lanes' own gotchas, GT.9's (it binds the next two closers), and **three** carry-forward entries (Wave-3, Wave-4, Wave-5-registry) holding the closed lanes' facts that still bind an open task — consolidated rather than left as forty separate lines. **Browser-verification debt is no longer here — it is `VERIFY-DEBT.md`.**

- **UI.9 light theme is an OPENING-GUESS palette, unverified in a browser.** Automated gates pass (632 pass / 0 fail incl. 6 new storage tests, typecheck + build + changed-file lint green) but the 12-tabs × {dark,light} × {cozy,compact} visual QA matrix is explicitly manual and deferred to the Wave-5 polish passes (UI.12–16 done-whens). Known trap (plan risk note): AA contrast on warn/ok/band **text over tinted fills** in light — the fill tokens and semantic ink were re-authored dark-enough to double as text (`--strat-warn: #9a6a10`, band-hot `rgb(0,150,100)`, band-warm-text `#8a6100`), but spot-check before trusting. Adjust `:root[data-theme='light']` values, not the consuming rules.
- **Settings types are extension-local mirrors** — `SettingEntry`/`SettingsGroup`/`SettingsResponse`/`SettingsPatchResult`/`SettingsResetResult` in `extension/src/shared/types.ts` (re-exported via api.ts), mirroring the `GET /x/settings` JSON. The extension MUST NOT import `src/x/settings/registry.ts` (§5 build-isolation). **Binds UI.2/UI.11:** if the server `SettingDef` shape gains a field the editor needs, hand-sync it into `SettingEntry`.
- **`exactOptionalPropertyTypes` pass-through pattern (UI.10):** primitives forwarding a maybe-undefined optional (`Slider.unit`, `SettingRow.onReset`, `GearPopover.onReset`/`label`) type the prop as `T | undefined` explicitly — else TS rejects `prop={maybeUndefined}`. Reuse this for new primitives that relay optionals.
- **UI.10 rail regrouping is UNVERIFIED IN A BROWSER** (like UI.9's palette). Extension typecheck + Vite build + changed-file lint are green and the CSS compiles, but the visual QA (grouped rail spacing, brandmark, EmptyState, popover) rides the Wave-5 polish passes (UI.12–16 done-whens), per D7/D14.
- **`inspect.test.ts` fails 2/2 under bare `bun test`, GREEN under `bun run test`** (the canonical `SQLITE_PATH=:memory: bun test`). `src/db/client.ts` defaults to `:memory:` when `NODE_ENV==='test'`, but `src/x/data/inspect.ts`'s readonly connection still defaults to `./stratus.db` when `SQLITE_PATH` is unset — the primary writes memory, the readonly reads the file, seeded rows are invisible. **Always gate with `bun run test`.** One-line fix for a future data-core task: make `inspect.ts` honor the same `NODE_ENV==='test'` default (or have `client.ts` `process.env.SQLITE_PATH ??=` set it so both agree).
- **RL.4: adding a registry key is a 4-file move.** `PROMPT_KEYS` + `PROMPT_SPECS` in `prompts/registry.ts`, the default exported from a **PURE** module (a route import cycles — AI.5's `voice/extractPrompt.ts` move exists for that reason), `registry.test.ts`'s key-list + defaultBody assertions, and `docs/settings-tab.md`'s prompt-count copy (now **11**). The extension needs nothing — the Prompts panel renders whatever `GET /x/prompts` returns. CLAUDE.md's AI-layer entry still says "10 keys"; that is a historical phase record and stays — **RL.8's phase entry is where the 11th key gets documented.**
- **RL.5 is UNVERIFIED IN A BROWSER**, like every panel component since UI.9 — no tests were added (repo convention: panels are untested, logic lives server-side). Automated gates only: root+extension typecheck, `cd extension && bun run build`, changed-file lint, full suite 1087/0. The plan's live done-whens (create → add → test-render → generate → Overwrite) ride RL.8's smoke + the Wave-5 QA pass. The one server-behavior assumption the UI leans on was **not** left to faith — the `{humanizer:{}}` → defaults round-trip was proven with a throwaway route test (D81b).
- **NT.1: extension typecheck does NOT cover `*.test.ts`** — `extension/tsconfig.app.json` has `"exclude": ["src/**/*.test.ts"]`, and root `tsconfig.json` only includes `src/**/*` + `scripts/**/*` + `drizzle.config.ts`. So an extension test file is checked by **`bun run test` alone**; a type error in one is invisible to both typechecks. (Same for `earlyReplies.test.ts` — pre-existing, just never written down.)
- **NT.7: `app.request(...)` is typed `Response | Promise<Response>`.** A helper declared `function f(): Promise<Response> { return app.request(...) }` fails root typecheck; make the helper `async`. `bun run typecheck` covers `scripts/**`, so this bites in smoke scripts, not in route suites (which `await` inline).
- **CLAUDE.md was slimmed to guardrails-only (2026-07-23, out-of-skill docs refactor).** The full phase-entry ledger moved **verbatim** to `docs/PHASE-HISTORY.md`. **Every docs-sync task (MASTERPLAN global rule 6, codemap §7 rule 29 — updated) now appends its phase entry to `docs/PHASE-HISTORY.md`, NOT CLAUDE.md**; CLAUDE.md changes only when a guardrail (invariant, workflow, stack quirk) changes. Plan text still saying "CLAUDE.md phase entry" means the history file.
- **STATE.md was compacted 2026-07-23 (out-of-skill housekeeping, no code touched).** It had reached **339 KB** — past the `Read` tool's 256 KB cap, so Step 0's "read STATE.md fully" silently became a chunked-read investigation every session. 96 closed-wave D-entries + 127 closed-wave gotchas + the header's HV/GR prior paragraphs moved **verbatim** into the new `STATE-ARCHIVE.md` (frozen, 269 KB, grep-only — bigger than one `Read` by design); the live file is **74 KB**. The split was generated by line number, not retyped, and verified as a set operation: every non-empty original line lands in **exactly one** of the two files (438/438, zero duplicated). **What this means for you:** a task touching a file Waves 0–2 built should `grep STATE-ARCHIVE.md` for the task id or filename before assuming the live file tells the whole story — e.g. the RL `/use` contract (D79), the NT.5 icon prefixes (D89), the ST registry contract. SKILL.md gained the matching archive rule in Step 6 (a lane's docs-sync task archives that lane) and its Step-0 staleness guard now compares the **subject line**, matching D97 — it still said `--format=%h` five sessions after shas stopped being recorded.

- **SC.7: the coach's lexicon arrives ASYNC, and two rules change under you.** Anything grading a draft in the panel must take the lexicon from `useCoachLexicon()` — never `DEFAULT_LEXICON` and never a locally fetched copy, or two surfaces will disagree about whether the same sentence has a concrete detail. The first paint of a cold panel legitimately grades with the empty default and re-renders ~1 request later; that is by design (the score sharpens, it doesn't appear), so a test or a screenshot taken at t=0 sees the pre-lexicon score. Server-side callers use `loadActiveCoachLexicon()` from `routes/coach.ts` (never re-derive). And the payload is only as good as the operator's curation: with no channel keywords and multi-word pillar slugs, `specificTerms` is legitimately EMPTY — that is not a bug, and `concrete_detail` then behaves exactly as it did before SC.7.

- **Wave-3 carry-forward (the only HV/GR facts that still bind an open task — everything else is in the archive):** **(1) MCP tool count is asserted exactly.** `src/mcp.test.ts` asserts **23** (UI.4 took it 21→23 with `x_settings`+`x_update_setting`); every future tool bumps that number *and* `docs/s2-mcp-server.md`'s three counts (total / curated tier / write tier) **in the same commit** — that doc was silently 5 tools stale for four phases. **(2) Docs now assert user-visible strings, and two Wave-5 tasks own them.** `docs/harvest-tab.md`, `docs/playbook-tab.md`, `docs/people-tab.md`, `docs/today-tab.md`, `docs/composer-tab.md` and `docs/settings-tab.md` quote real copy (the both-off warn line, `Passive: N rows today`, `insufficient data (n=…)`, `Nobody has shown up often enough yet.`, the three schedule-warning sentences, the debt-tier wording, the `Nd left · need X/day · doing Y/day` line). **UI.14 and UI.15 did re-read the matching `docs/*-tab.md` in their own tasks and UI.17 verified all twelve — the standing half is the RULE, not the assignment: reword a quoted string and you owe the doc in the same commit** (`docs/today-tab.md` is numbered §1–§18, so an inserted card renumbers rather than appends; the settings doc's toggle count was fixed at UI.11 and its registry table added at UI.17). **(3) Real-DB fixture rules that outlive their lane:** a seeded `posts_published` row is written **`retired: true`** (NT.7 — otherwise it is a candidate for the daily *billed* pass), a `following` fixture needs a `following_runs` row first (`last_run_id` is a notNull FK), people-layer handles must be **≤15 chars** (longer ones are silently skipped and the assertions go vacuous), and any suite touching `scheduled_posts`/`posts_published` inside the monitor's windows must delete its rows in `afterAll` — `monitor.test.ts` asserts `clusterCount === baseline + 1` and a leaked pending pair breaks a different file. **(4) Five reads WRITE and none of them may be polled:** `/radar/drafts` (48h expiry), `/following/queue` (release + revoke), `/x/goals`, `/x/brief` and `/x/digest` (all three settle `active → achieved|missed`). A3.8's Today card and A3.14's week board both sit on `/x/brief` — a refresh loop there silently advances goal statuses.
- **GR.10: the smoke that closes Wave 3 is the template for smoking a surface whose READS write.** `scripts/smoke-guardrails.ts` ($0, no `--live`, rerunnable, verified to leave 0 rows) mounts following + monitor + goals + me + calendar + brief + digest over the real `./stratus.db`. **Copy its safety shape, not `smoke-passive-harvest.ts`'s, whenever a route mutates rows it did not create:** namespaced ids cover only what you wrote, so it *additionally* snapshots every foreign `following.status` before the first write and restores it from the success path **and** from `fail()` (a complete-run reconcile marks every unseen live handle `gone`; a queue read releases rows), snapshot-restores `commitments` and the seeded `streaks`/`digests` keys rather than deleting them, and **closes with one more `GET /x/brief`** so today's C9 streak row is recomputed from clean data instead of describing the burst fixtures it just deleted. **Three things it discovered that no plan text says:** (a) reconcile "unseen" is per **run** (`last_run_id`), not per batch — proving `gone`/`confirmed` needs TWO runs; (b) the queue needs ≥3 slots of budget for its assertions, so the script fails loudly naming `windowUsed/windowCap` + `dailyUsed/dailyCeiling` rather than through a confusing count mismatch; (c) `?factsOnly=true` returns **before** the digest cache write, so a scorecard read caches nothing — but it still settles goals. Assertions over shared/real data are **baseline-relative** where they must be (`nearDuplicate.pairCount`, `scheduleCluster.clusterCount`) and exact where the fixture owns the whole population (`questAdherence`, the queue batch, the ledger statuses).

- **Wave-4 carry-forward (the A3 facts that still bind an open Wave-5 task — the per-task A3 gotchas + D114–D126 + the A3.1–A3.14 header priors are in `STATE-ARCHIVE.md` §"Wave 4", grep by task id):** **(3) Standing A3 facts:** `article prompt.md`↔`ARTICLE_PROMPT_TEMPLATE` byte-sync joins post/reply/thread/dm as an edit-both-together pair (13-key registry); `mark-posted`/manual reminders render off `status`, NEVER `postedTweetId` (the daily reconcile links the tweet id); the calendar URL guard is `pending`-only (manual = the $0 link path) and articles never hit the API; `dm_drafts`/`articles` PATCH is SELECT-then-guard with terminal-status 409s; `suggestBestSlotDate`/`annotateGaps` take `audience` as a trailing optional arg (no grid ⇒ byte-identical to pre-A3.4); calendar-fixture day bands 90–130d (GR.6) / 150–160d (A3.5) / ~200d (brief) / ~300d (A3.7) are spoken for — a new suite picks a fresh band ≥45 min clear. **Item (1), the A3 browser-verify debt, moved to `VERIFY-DEBT.md` at GT.9 (it is debt, not a fact); item (2) assigned it to Wave-5 polish tasks that have all shipped and is discharged.**

- **Wave-5 registry carry-forward (the UI.2–UI.7 facts that still bind an open task — the five per-task gotcha blocks + D128–D133 are in `STATE-ARCHIVE.md` §"Archived at UI.11", grep by task id):** **(1) The store is process-global across test files.** One in-memory `app_settings`, one module cache — any test that `setSettings(...)` MUST reset in `afterEach`/`afterAll` (`resetSettings({group})` or `{keys}`), and keeping a suite's PATCH assertions inside ONE registry group means a single group-reset cleans them. **(2) A knob's blast radius can exceed its caller.** PATCHing a knob whose consumer is a global read-that-writes (the `/radar/drafts` TTL expires *every* stale row) means seeding your own fixture and only ever tightening the window; a smoke does it by snapshot-restoring `resolveSetting(key)` by `isDefault`, never `resetSettings` (which DELETES an override the operator set on purpose) — and hangs the restore off `fail()` too, since `fail()` exits. **(3) An env-backed registry default is read ONCE, at registry import** — a runtime `process.env.X = …` in a test or script is a silent no-op that still looks like it works. Steer through `setSettings`. **(4) `appliesOn` is behavior, not copy:** `'restart'` **only** where reading later can't take effect (the knob armed a timer — just `x.workers.dailyMetricsHourUtc`/`publisherIntervalSec`); everything else reads per request/run even inside a worker, and **no money knob is ever restart-scoped**. Getting this backwards makes a knob look broken. **(5) The `?param=` vs knob contract:** a query param overrides the configured baseline **per read**, in both directions (`?minN=20` re-tightens a store-loosened gate) — assert both, as `playbook.test.ts` does. **(6) A knob whose LOOSENING direction spends can only be route-tested on the refusal side** — `/x/replies/generate` mounts whenever any LLM key is configured and `.env` has one, so "PATCH → it gets through" runs a real paid Grok call inside `bun run test`. Assert a tightening that 422s, and prove the store was read by picking a context that refuses under BOTH settings so the reported `band` changes; the permissive direction belongs in the pure suite. **(7) Adding a `scope:'mirrored'` key is a 2-file move** — the registry def, then a `ServerConfig` field + `SERVER_DEFAULTS` fallback in `extension/src/shared/serverSettings.ts`; `registry.test.ts` asserts the mirrored list EXACTLY (**17 keys when this was written; 27 as of UI.15** — and `extension/src/shared/serverSettings.test.ts` separately asserts `Object.keys(ServerConfig)` plus the full-blob round-trip, so one knob is a 2-file move that reddens 3 assertions until both halves land) so the second file can't be forgotten (without it the key ships in the blob and is never read — no type error, no test failure). **(8) A cap arriving asynchronously must be applied to a SPENT counter, not snapshotted as REMAINING** — any mirrored knob feeding an initial `useState` has the `Conversations.tsx` trap. **(9) The AI layer has TWO legitimate tiers and they are not duplicates:** `x.ai.*` (per-surface house default) and the `ai` blob from Settings → AI (global override, `null` = defer). "Unifying" them silently drops the per-surface knobs out of the merge. **(10) Two infra facts:** the `.claude/skills/stratus/scripts/` files are linted but NOT typechecked (root `tsconfig.json` covers the repo-root `scripts/`, not the skill's) — verified only by running them; and Bun auto-loads `.env` into `process.env`, so `env -u API_TOKEN bun run …` does NOT unset it (the skill script's `STRATUS_API_TOKEN ?? API_TOKEN` mapping exists for that).

- **UI.17 — five facts for whoever opens the next plan.** **(1) A closing docs-sync is mostly a VERIFICATION pass, and the way to scope it is `git log --name-only -- docs/`.** Six polish tasks had already rewritten twelve tab docs in their own commits; guessing would have meant either re-reading all twelve or trusting a plan written eight days earlier. One command answered it, and the residue was a single owed section. **(2) Assert a plan's BAR, not today's number.** The smoke checks "≥12 groups / ≥55 knobs" (the plan's done-when) and prints the real 14/61. Had it asserted 61, the next knob would redden a script that has nothing to do with knobs — the same trap the exact-count tests in `registry.test.ts` accept **on purpose** (there the exactness IS the point: a mirrored key with no `ServerConfig` field must fail). Know which kind of count you are writing. **(3) A monotone assertion plus a printed denominator beats a fixture, when the fixture would drag invariants along.** `sufficient` counts across three gates prove the gate is applied without seeding a single row — and printing "0 of 21 gated cells" is what keeps a zero honest. Reach for this shape whenever the thing under test is a *parameter* and the data belongs to someone else's smoke. **(4) A settings knob whose loosening direction spends can still be smoked safely from the tightening side** — `x.budgets.imageDailyUsd: 0` makes `spent >= budget` true at $0 spend, so the 429 fires with no `cost_events` fixture at all (`smoke-studio.ts` seeds one because it needs a *nonzero* cap for a different assertion). The XAI key is faked **only when absent** and restored either way; biome forbids `delete process.env.X` in `scripts/`, so the restore assigns `?? ''`. **(5) `bun run <script>` buffers stdout when it is not a TTY.** A run that hangs writes an EMPTY output file, so "no output" tells you nothing about where it stopped — `console.error` (or `2>&1 | head`) is how you bisect a stuck script here. Cost twenty minutes on a hang that turned out to be transient.


- **SC.2 — facts for SC.5/SC.6:** the classifier's branches carry one why-comment each and the ORDER is the contract — `postFormat.test.ts` pins 8 ordering regressions (hot_take beats question, poll_list beats binary_choice, milestone beats audience_cta, data_comparison beats story, story beats list, RT beats everything), so "fixing" a misclassification by reordering reddens those first: add/tighten a predicate instead. Fixture rows are REAL posts stored verbatim (entities escaped, truncated texts kept as stored) — don't "clean them up". The corpus pull is repeatable: `GET /x/data/posts_published?limit=200&offset=N` (S1 explorer, $0), filter `is_reply=0`, 136 originals as of 2026-07-27. See D146 for the substance-43% consequence and the cooldown exemption SC.6 owes.
- **JD.1 — facts for JD.3/JD.6 and any DB-fixture suite:** the reply prompts now render a **one-line trust label above the quoted tweet text** (`UNTRUSTED_CONTEXT_MARKER`, exported) — JD.3's judge prompt renders a draft, not scraped text, so it does NOT want this; anything that renders *observed* text does. **`drafter.test.ts` is now a DB-fixture file** and it documents the shape a shared-in-memory-DB fixture needs: seeds 400 days back (clear of brief/digest/monitor windows and of the spoken-for calendar bands), view counts ~9M so they outrank every other suite's rows deterministically, `retired: true` on every `posts_published` row (NT.7), teardown in a **`try/finally` inside the test** as well as `afterAll` — files share one in-memory DB, so `afterAll` alone leaves your rows visible to every test that runs after yours *in the same file*. Where a foreign row could legitimately take a freed slot, the assertion filters to the seeds (`t.startsWith('JD1')`) instead of comparing whole arrays; where the seeds provably fill every slot, exact equality is safe. **Also:** `bun -e` out-of-band verification (the GT.1 recipe) caught nothing this time but took ten seconds and is what makes "byte-sync is untouched" a checked claim rather than an inference from a green suite — keep doing it on any task that touches a prompt module.
- **SC.3 — facts for SC.4 (the next `postCoach` consumer), SC.6/SC.8 (the same file) and SC.9's smoke:** **the $0 way to verify a panel that renders a pure module is to push a fixture through the SHIM, not the canonical module** — a `bun -e` importing from `extension/src/postCoach.ts` and printing the exact rows the component will map over. It proves the re-export, the tsconfig `include`, and the rendered shape in one second, and it is what turned up D152(b). Pair it with three greps over the BUILT bundle (`dist/sidepanel.js` for a copy string + a tone class, `dist/assets/sidepanel-*.css` for the class) — that is the only check that proves **Vite inlined the out-of-tree module**, which neither typecheck nor `bun test` can tell you. **Shim mechanics:** the fourth out-of-tree module is a 4th `extension/tsconfig.app.json` `include` entry; forgetting it fails the extension typecheck loudly, so it cannot ship half-done. `extension/src/postCoach.ts` sits at the top level (the `replyBand.ts`/`channelSuggest.ts` form), not under `extension/src/shared/` — that directory is for a shim that ADDS to its re-export (`activeTimes.ts` bolts on the DOM extractor); a bare re-export goes top-level. **Component facts:** `Composer.tsx` has no test file (panel convention) so SC.3 added none and the suite is 1649 either way — automated coverage is root+extension typecheck, `cd extension && bun run build`, lint, plus SC.1's 36 engine tests; **the browser-verify is owed (see next-up).** `COACH_TONE`/`COACH_BAND_TONE` are module-level maps precisely so SC.4's reply chips can lift them instead of re-deriving a colour scale — a second tone vocabulary for the same engine is how the badge and the gate forked in UI.7's world. And `COACH_DISCLAIMER`/`COACH_BAND_LABEL` are rendered from the exports verbatim, never retyped (SC.1's instruction): reword the floor-not-target sentence in ONE place and every surface moves.
- **SC.4 — facts for SC.6/SC.7/SC.9 and any future coach surface:** **a `.tsx` in `extension/src/sidepanel/` IS importable from a `bun:test` file** — bun transpiles the JSX and resolves `react/jsx-runtime` from `extension/node_modules` by walking up from the file, so a pure helper that lives beside a component does not have to be exiled to a `.ts` to be tested (`CoachChip.test.ts` imports `./CoachChip.tsx` and the suite is green). That is new ground: every other extension test imports a `.ts`. It does NOT make the component testable — there is no DOM renderer here — so the split stays "pure helper tested, JSX manually verified", and NT.1 still applies (an extension `.test.ts` is checked by `bun run test` alone). **The $0 verification recipe, applied to a component:** import the module in `bun -e`, feed it a fixture, print the string the user will hover — that is how the fix-before-nudge ordering was checked before writing the assertion rather than after, and it is what turned up that a reply grades over **27** checks (29 minus `hook_opener`+`breathing_room`, D145b). Then three greps over the BUILT bundle (`dist/sidepanel.js` for `coach-chip` + a tone class, `dist/assets/sidepanel-*.css` for the class) — the only check that proves Vite kept it. **Copy facts:** the chip's tooltip is `N/100 · band` + at most two labels + `COACH_DISCLAIMER`; `CoachChip.test.ts` asserts the two-issue cap and `toEndWith(COACH_DISCLAIMER)`, so adding a third line or retyping the disclaimer reddens it. **For SC.7:** filling `DEFAULT_LEXICON` changes what these chips say on every reply picker at once — there is no per-surface lexicon and the chips pass no `lexicon` argument, so wiring SC.7 into `scoreDraft`'s default is the only lever, and it moves four surfaces plus the Composer in one commit.
- **SC.5 — facts for SC.6/SC.8/JD.7 and the next Playbook cell:** **the $0 way to check a new Playbook cell against reality is to pull the corpus, not to deploy** — a `bun -e` that fetches `GET /x/data/posts_published?limit=200&offset=N` from prod with the `.env` bearer (S1 explorer, $0, SC.2's recipe), filters `is_reply=0`, and runs the new `build*` locally at `minN=1`. It answered the plan's "eyeball the distribution" step in one command and produced D154(a). **Note the pull is capped by your loop bound** — 600 rows gave 109 originals vs SC.2's 136, so a share is comparable but a count is not. **Local `./stratus.db` has ZERO published originals** (prod data lives on the Hetzner host), so mounting the router against it only proves the empty-corpus path; it is still worth doing — that path is what a fresh install sees. **Route-test facts:** the two new cells are partitions over the WHOLE `posts_published` corpus, and other test files seed originals into the shared in-memory DB, so route assertions are **partition invariants** (Σposted = totalPosted, Σn = totalMeasured, clean+flagged = the same totals) plus **before/after deltas** — never absolutes, the way the media test's exact `n: 2` can be. The SC.5 describe is declared **LAST in the file on purpose**: it inserts originals and the media test above asserts exact bucket counts over the same table, and describes run in declaration order. Its seeds are `retired: true` (NT.7). **Panel facts:** the two sections have no test file (panel convention); coverage is root+extension typecheck, `cd extension && bun run build`, lint, and the 13 new pure/route tests — **browser-verify owed (next-up item 0e)**. The bundle greps that prove Vite inlined the new shim: `dist/sidepanel.js` for `Would-you-rather` (a `FORMAT_LABELS` value) and for the section copy.
- **SC.6 — facts for SC.8/SC.9 and anything that ships a counter:** **the $0 way to tell a useful counter from a permanently-lit one is a ROLLING-WINDOW REPLAY over the real corpus, and it is three lines on top of the SC.5 pull** — for every day of history, take the window ending that day, bucket it, and print the share of windows in each bucket that would have warned. A fixture proves arithmetic; only the replay proves *calibration*, and it is what produced both halves of D156 (the 0.238-vs-0.45 kill and the 68/43/35% lit-rates now recorded beside the constants). Run it BEFORE the thresholds reach a test file. **Route facts:** `GET /x/posts/cooldowns` lives on the calendar router but its api-client home is `api.coach.cooldowns` — namespacing there follows the FEATURE, not the path (`drafts.generate` → `/x/posts/draft` is the same shape), so don't go looking for a `posts` namespace. Its population is `posts_published`, so on `./stratus.db` locally it returns `{cells: []}` — prod data lives on the Hetzner host (the SC.5 fact, still true), and the empty-corpus path is exactly what a fresh install sees. **Test facts:** the route describe is in `calendar.test.ts`, seeds `posts_published` with `retired: true` (NT.7) and cleans up in a **try/finally inside each test** as well as the file's `afterAll` (which now drains a shared `publishedIds` array rather than the single `DUPE_ID`) — the window is "the last N days from now", which is precisely where other suites' fixtures live, so every count assertion is a **delta against a baseline read**, never an absolute. The pure tests assert their own premises first (`classifyFormat(fixture) === 'hot_take'`), so a classifier change reddens the premise instead of silently re-labelling the arithmetic under it. **Panel facts:** no test file (panel convention); coverage is root+extension typecheck, `cd extension && bun run build`, lint, the 14 pure + 3 route tests, and the two bundle greps (`dist/sidepanel.js` for `A count of what went out`, `dist/assets/sidepanel-*.css` for `coach-cooldown`). **Browser-verify owed (next-up item 0g).** The `.coach` box now reads, in order: head → cooldown line → fix rows → nudge rows → `<details>` passing → disclaimer. SC.8's reach line appends INSIDE it (D141), and `canSubmit` is STILL byte-untouched — three tasks in this lane have now added a number to this file without one of them being allowed to gate on it.
- **SC.1 — facts for SC.3/SC.4/SC.5:** `scoreDraft` returns checks in fixed rule order (hygiene→craft→signal) with a label on EVERY status — pass labels exist because the Composer column collapses passes but still lists them; render non-pass first by filtering, not by re-sorting ids. `COACH_DISCLAIMER` and `COACH_BAND_LABEL` are exported precisely so SC.3's UI copy is not retyped (decision 4's floor-not-target sentence must be identical everywhere). The `url_cost` fix-label contains the literal `$0.20` — `postCoach.test.ts` asserts `toContain('$0.20')`, so rewording that copy reddens the suite. `wordCount` counts only tokens containing a letter/digit (a bare "—" is not a word); tests that hand-count words must match that rule.

- **GT.9 — facts for SC.9/JD.8 (the next two closing smokes) and anyone writing a smoke that must not spend:** **the strongest $0 assertion for a gate is how FAR a request gets, not what comes back.** `smoke-growth-tactics.ts` force-unsets both LLM keys in a `finally` and asserts **503 `llm_not_configured`** — reaching the LLM layer IS the proof the gate opened, and it is unspendable by construction rather than by remembering not to pass `--live`. It is why the script has no `--live` flag at all: the only paid thing in reach is the call it exists to prove was not made. Copy that shape for any surface whose success path costs money. **Second: assert the QUALIFIER, not the count.** The reciprocity step's load-bearing assertion is not `n === 1` — it is that re-stamping the same person's `stageUpdatedAt` to *now* makes the same posted reply stop counting. A count over a shared real DB is baseline-relative noise; the qualifier is the feature, and it is the one thing a fixture written by someone who already believes the rule would never have checked. **Fixture facts:** handles are `gt9*` (≤15 chars or `normalizePersonHandle` drops them silently and every assertion goes vacuous), tweet ids 887-prefixed; a `people` row needs only `handle`+`stage`; the milestone step is PURE and deliberately seeds no `account_snapshots` (that table is contested by four suites and `smoke-pinned-watch.ts` refuses the real DB over it); and `/x/brief` WRITES — it settles goals and rewrites today's C9 diary — so the run closes on a clean brief read asserting the baseline came back (the GR.10 rule). **Local `./stratus.db` is nearly empty** (prod lives on Hetzner), so the reciprocity set reads as just the script's own seeds; that is the fresh-install path and worth seeing, but it means the smoke proves wiring, never calibration — calibration is the prod `POST /x/data/query` recipe in the archived GT.6/GT.7 gotchas. **Docs discipline this pass established:** a prompt-only task has no tab doc, so it silently exits the per-task docs rule and ships invisible — GT.1 and GT.2 both did. Write the user-facing paragraph in the prompt task's own commit; `replies-tab.md` §"Generating and the three variants" and `composer-tab.md` §"Generating and picking a draft" are where prompt behaviour is explained now.

- **SC.8 — facts for SC.9, JD.6/JD.7 and anything that aggregates `metrics_snapshots`:** **the $0 recipe that produced this task's whole result is a FIRST-vs-LATEST pull, and it is one `POST /x/data/query`.** Select `public_metrics`/`age_at_snapshot_min` twice per row — once `ORDER BY snapshot_at ASC LIMIT 1`, once `DESC` — plus `COUNT(*)`, then look at whether the two cohorts differ. Here `snaps` came back `{1: 110, 2: 25}` and the 25 two-snapshot rows had 10× the views: that single line is what exposed the winner re-read, and no fixture would ever have shown it. **Do this before trusting ANY aggregate that reads "the latest snapshot" across posts.** Second recipe, equally cheap: to test whether a normalization is legitimate, bucket the corpus by the thing you are normalizing OUT and check the result is flat. `views*1440/age` gave 452/day under 12 h and 167/day at 12–27 h — not flat, therefore not a normalization — while the RAW counts it was 'correcting' were 129 vs 121. **Fixture design worth copying:** to assert exact quantiles against a trailing-median baseline, pad so that 15+ of every 20-row window sit at one value (here: 3 filler posts after each test post). The base is then pinned and a post's ratio is `views / base` by construction, so p25/p50/p75 can be asserted exactly instead of approximately — and the fillers are deliberately an EXEMPT format so they can never produce a competing fitted cell. **Route facts:** `GET /coach/reach` sits on SC.7's `routes/coach.ts` (second static path, §7.20 moot) but the population is `posts_published` + `metrics_snapshots`, so on local `./stratus.db` it returns all-insufficient cells — prod lives on Hetzner (the SC.5/SC.6 fact, still true), and that empty path is what a fresh install sees. The route describe is declared LAST in `coach.test.ts` because it inserts originals, and its assertion is a **delta** (`measuredPosts` moves by exactly 2), never an absolute — three other suites seed that table. **Panel facts:** no test file (panel convention); coverage is root+extension typecheck, `cd extension && bun run build`, lint, the 14 new tests, and three bundle greps (`dist/sidepanel.js` for `reach band: insufficient data` and `usually `, `dist/assets/sidepanel-*.css` for `coach-reach`). **Browser-verify owed.** The `.coach` box now reads: head → cooldown → **reach** → fix rows → nudge rows → `<details>` passing → disclaimer, and `canSubmit` is STILL byte-untouched after four tasks added numbers to this file. **For SC.9's smoke:** the assertion that matters here is the same shape as GT.9's — not a count, but the *contract*: seed a post whose first snapshot is in-window and whose latest is not, and prove it COUNTS; then prove no `insufficient` cell carries a number. Both are unspendable and both are false under the implementation the plan originally asked for.



## Planning-error log

Corrections to MASTERPLAN.md itself (wrong dep, wrong order discovered live).

- **2026-07-16-niche.md Task 3 done-when** — the "`Pitești` hits nothing" grep contradicts the same task's bit-for-bit equivalence requirement (post prompt §6/§9/§0 carry builder biography outside the §1/§5 extraction scope; no niche field exists to hold them). Resolved at N.3 in favor of equivalence; see D28b for the full resolution + what N.9 must scope differently. Source plans are never edited — this entry is the correction.
- **2026-07-17-ai-layer.md — no task owns the post drafter's askLLM migration.** Task 5's edit list names replies/pillars/playbook/voiceExtract; Task 6 only flips drafter's MOUNT gate; Task 7 adds a sibling route. Yet the plan's Why-section + Done-when #1 spirit require every draft surface to honor the provider setting, and the Task-6 gate flip would 502 `/posts/draft` on OpenRouter-only setups. Resolved at AI.5 by migrating `drafter.ts` there (D47b) — the call-site-migration task was the right home.

