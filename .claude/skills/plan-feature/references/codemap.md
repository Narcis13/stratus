# stratus code map

> **Stamped:** 2026-07-23 at commit `2b763b1` (NT.3 — Wave 2, **the C10 ingest endpoint** [high, $0, 3 files, +5 tests]: **`POST /x/people/engagements`** registered next to the sightings route and copying its validation shape exactly (`invalid_body` / `invalid_engagements` / `too_many_engagements` at >50 / per-index `invalid_engagement_{kind,handle,target_text,seen_at}_<i>`) → `recordEngagements` counts as JSON. **POST, so the §7.20 static-vs-`:handle` trap doesn't bite** — only `GET /people/:handle` exists. Three things to carry: (a) the `kind` whitelist is where the parser's `'other'` dies — server-side `EngagementKind` can't represent it, and the guard `isEngagementKind` was added **beside the union in `people/engagements.ts`** (`isStage`/`isFollowupKind` idiom), one file past the task's budget; (b) the route **re-forces `targetText: null` on a `follow`** even though NT.1's parser already does, so a stale extension build can't feed a follower's bio to the target matcher (§7.3's "the server always recomputes the gate" discipline — observable as two follow cells for one handle deduping to `processed: 1`); (c) an invalid handle is **passed through, not rejected** — `recordEngagements` skips it and reports `skipped`, so one bad scrape never 400s a 50-cell batch. Route tests are colocated in **`src/x/people/engagements.test.ts`** (where the sightings route suite lives), not `routes/people.test.ts` as the plan's edit list said. Gates: `bun run test` **1127/0** (1122 + 5), typecheck clean, full-repo lint clean; no extension file touched, so no extension build. **Next:** NT.4 (background notif-context cache, none-dep) or NT.6 (fans engagement count, dep NT.2✓); **NT.5 still needs NT.4**. **Prior — NT.2 (`ba2c531`, recorded `4e4d9fc` was an amend-orphan):** the engagement substrate — `PERSON_EVENT_TYPES` gained `their_like`/`their_repost`/`their_follow`, in **no** stage set (engagement is not reciprocity: 50 likes + a follow is still `stranger`; decision 1, matrix-tested, do not re-litigate), and new `src/x/people/engagements.ts` mirrors `sightings.ts` — `dedupeEngagements` (key `kind:handle:targetText`), `engagementEventId` (`their_like:notif:<handle>:<tweetId>` → day-bucket `…:<YYYY-MM-DD>` unresolved → `their_follow:notif:<handle>` once ever), pure `matchTargetTweetId` (collapsed-whitespace **prefix** match, ≥20 chars) + DB wrapper `resolveTargetTweetId`, `recordEngagements` (fill-only upsert `source:'notification'`, INSERT OR IGNORE, own-post window loaded **once per batch and not at all for a follow-only batch**, `recomputePerson` once per distinct handle = watermarks only). **Deliberately untouched:** the extension `PersonEventType` union + the **exhaustive** `EVENT_ICONS` Record in `People.tsx` — NT.6 owns them, and the Record's exhaustiveness will force the three labels the moment the union grows. **Prior — NT.1 (`2063b86`, recorded `7f3d2fd` was an amend-orphan):** the pure + **INERT** `extension/src/shared/notifications.ts` — `parseNotificationCell` reads an `article[data-testid="notification"]` cell into `{kind:'like'|'repost'|'follow'|'other', handles[], targetText}`, icon-first with an en+ro keyword fallback; no consumer until NT.5. **The exported icon `d` prefixes are unverified against live X** — an unrecognised glyph falls through to keywords instead of forcing `'other'`, and the fixtures interpolate the constants so a post-verification correction is a one-line edit. Keyword order is **like → repost → follow** (only that ordering makes the loose ro stem `urmăre` safe); `targetText` is forced **null for `follow`** (the prose there is the follower's bio — NT.2's target resolution relies on this). **Prior — RL.8 (`a120207`):** Wave 2, **the RL sub-plan CLOSED** [high, $0, docs+smoke, **no code/schema/route/test change**]: new **`scripts/smoke-reply-lists.ts`** ($0, real DB, rerunnable) walks create → 5 templated items → 10 `/use` (no immediate repeat, ≥3 distinct, vars filled per the item's OWN template) → `preview:true` writes nothing → `409 no_enabled_items` → `replace` swap → **the Playbook `canned` delta** (publish a reply whose text IS a use's `renderedText` + a metrics row; `canned` +1, `unattributed` unmoved) → delete (items cascade, all 10 use rows survive); `--live` = ONE `/generate` asserting the proposal is never persisted. Docs: CLAUDE.md **"Surfaces S7 — Reply lists"** (the plan's own "(S5)" is stale — S5 = Studio 2.0, S6 = Augmented X UI), SURFACES-PLAN §5d, a full **Lists subtab** section in `docs/replies-tab.md` + `canned ▾` bullets in `docs/today-tab.md`. Gates: `bun run test` **1092/0** (unchanged — no test added), typecheck + changed-file lint clean. **Next:** the **NT server lane** (NT.1/NT.2/NT.4 — none-dep). **Prior — RL.7 (`3a4ae56`):** Wave 2, the Playbook **`canned` attribution bucket** [high, $0, 7 files, +5 tests]: `ReplyOrigin` = `'single'|'radar'|'canned'` and `classifyReplyOrigin` takes a 4th **required** `cannedTexts: ReadonlySet<string>` of normalized `reply_list_uses.renderedText`, checked **last** — a canned use writes no `reply_drafts` row (Decision 6), so rendered text is its only evidence, and a reply that is both a posted draft and a text match counts once, as the draft. Loader adds one unwindowed `reply_list_uses` select (empty strings skipped); `buildBatchVsSingle` gains the `canned` cell; the Playbook tab renders a third row. Route test asserts the **delta** (canned +1 / unattributed −1) around its own seed because `reply_list_uses` is FK-free and another suite can leave strays (D79i). Drive-by D83: two literal NUL bytes in `src/x/playbook.ts` made every plain `grep` over that file return nothing — now `'\0'` escapes (§7.32). Gates: `bun run test` **1092/0**, root + extension typecheck, extension build, changed-file lint 0-error. **Prior — RL.6 (`299595f`):** the QuickReplyPicker — one click on a Launch Room early-replier row or a Conversations open loop is one real `POST /use` (never `preview:true`, never a local pick), copied inside the same click handler, text kept visible if the clipboard refuses. **Prior — RL.5 (`b415eb3`):** the reply-list **management UI** [high, $0, 5 files, 0 new tests — panels are untested by repo convention]: `Replies.tsx` grew a **`Reply Master | Lists` subtab** (the **`SubTabs` UI.10 primitive**, per D7 — the first real consumer besides `EmptyState`; the plan's "copy Voice.tsx's `radar-tabs`" is the older idiom) and new **`extension/src/sidepanel/ReplyLists.tsx`** hosts the whole surface: list rail → per-list settings / items editor / humanizer editor / Test render / AI generate box. `api.replyLists.{list,get,create,patch,remove,setItems,patchItem,removeItem,use,generate}` + 15 RL types in `shared/types.ts`. **Three decisions worth carrying:** (a) the humanizer editor holds **NO client-side copy of `DEFAULT_HUMANIZER`** — "Customize for this list" PATCHes **`{humanizer:{}}`**, which the server lenient-parses field-by-field into the full defaults and hands back, so the form is always seeded by the engine's own numbers (verified against the real route); "Reset to defaults" PATCHes `null`. (b) Pools are **one-per-line textareas, not the plan's comma-separated inputs** — the default prefixes literally contain commas (`'honestly,'`, `'man,'`). (c) Test render always uses **`preview:true`** so trying a list never burns its anti-repeat state, and the AI proposal lives in component state only — nothing persists until Append/Overwrite goes through `setItems` `source:'ai'` (Decision 3, Overwrite `confirm()`-guarded). Gates: `bun run test` **1087/0**, root + extension typecheck clean, `cd extension && bun run build` clean, changed-file lint 0-error. **UNVERIFIED IN A BROWSER** (panel-component convention). **Next:** RL.6 (QuickReplyPicker) or RL.7 (Playbook `canned`). **Prior — RL.4 (`f353f6a`):** new pure `src/x/replyLists/generate.ts` (`REPLY_LIST_PROMPT_TEMPLATE` + `REPLY_LIST_GEN_SCHEMA` + `buildListGenInput` + `parseGeneratedItems`) behind **`POST /reply-lists/:id/generate`** — one `askLLM` structured-outputs call that **writes nothing** (proposal-only, the user applies it via `POST /items` `source:'ai'`). **Three overrides of stale plan text (D80):** `askLLM` + `loadPromptSafe('reply-list')` per **D5** (registry `PROMPT_KEYS` **10 → 11**, editable in the Prompts panel, cache key = the bare registry sha since the prompt carries no persona) instead of `askGrok`; **no pre-flight key check** — askLLM's own provider-key enforcement maps to **503 `llm_not_configured`** (ideas.ts/AI.9 precedent), so an OpenRouter-only setup works and the unknown-list **404 fires before any spend**; and `count` **400s past 30 rather than clamping**. `parseGeneratedItems` dedupes case/whitespace-insensitively (casing-only twins would defeat the anti-repeat pick) and returns `[]` for "nothing survived" vs `null` for a parse error — the route 502s differently on each. **Prior — RL.3 (`c3f44d2`):** `POST /reply-lists/:id/use` — pick + compose + stamp; **`lastUsedAt` stamped monotonically past the list's max (§7.13a)**, `composeReply` derives its own protected spans, preview writes nothing, 409 `no_enabled_items`; test `afterAll` must delete `reply_list_uses` explicitly (FK-free). **Prior — RL.2 (`9c0d97b`):** reply lists **schema + CRUD** [high, $0, 5 files, +8 tests]: three tables via migration `0018_flimsy_captain_universe` (DDL-only, no seed) — `reply_lists` (normalized `humanizer` JSON, null = engine defaults) → `reply_list_items` (FK **cascade**, `last_used_at`/`use_count` = the server-side anti-repeat state, index `(list_id,last_used_at)` = `pickItem`'s recency read) → `reply_list_uses` (**FK-free on purpose** — audit log + the Playbook `canned` hook must outlive a deleted list); `src/x/routes/replyLists.ts` always mounted next to `ideasRouter` (collection counts from one grouped select; `/items` append|replace in **one sync txn** §7.13 answering with the whole current set; item PATCH/DELETE scoped by `id` AND `listId`; `humanizer` null-clears / non-object 400s / partial lenient-parses per D76a; item `createdAt` stamped past the list's max so a same-millisecond batch keeps its order). Gates: `bun run test` **1067/0**, typecheck clean, changed-file lint 0-error; migration verified on a copy of the real `stratus.db` (36 tables, pillar seed intact, cascade live). **Next:** RL.3 (`/use`). See §11 for everything earlier.
> This file exists so `/plan-feature` never re-scans the repo. It is the single
> pre-computed answer to "where does X live and how is it wired".
> **Maintenance rule:** any commit that adds/moves/deletes a file, route, table,
> worker, extension surface, or pattern MUST update the touched section here (and
> re-stamp the header). Every plan produced by `/plan-feature` ends with a
> docs-sync task that includes this file.

## 1. Thirty-second orientation

Single-user service (one human, one bearer token) with four goals and a **hard
scope ceiling** (a feature not serving one of these does not get built):

1. Schedule X posts a week ahead (calendar + 60s publisher worker).
2. Track metrics over time on every published post (daily 03:00 UTC once-only snapshot pass).
3. Voice library — stash other people's tweets via $0 DOM scrape for style analysis.
4. Circles — the people layer: CRM stages, conversations/open loops, relationship-aware drafting, Playbook, warmth (quests/digest/icebreakers).

Runtime: **Bun ≥1.1 + Hono** server on Hetzner (`https://stratus-narcis.duckdns.org`, systemd `stratus.service`, deployed via `scripts/deploy.sh`) + **Chrome MV3 side-panel extension** (Vite + React, loaded unpacked). State: **local SQLite via `bun:sqlite`** + Drizzle (`sqlite-core`), auto-migrated at boot. AI: xAI Grok via `src/grok/` (text `askGrok`, images `generateImages`). No queue, no Redis — in-process `setInterval` workers.

## 2. Root directory

| Path | What it is |
|---|---|
| `CLAUDE.md` | Session guardrails + full phase history. Update in the same commit as behavior changes. |
| `PLAN.md` | **Canonical build plan, goals 1–3** (cadence ladders, phased build). |
| `CIRCLES-PLAN.md` | **Canonical build plan, goal 4** (phases C0–C9, all shipped). |
| `SURFACES-PLAN.md` | Canonical plan for the S-phases (S0.x patches, S1 explorer, S2 MCP, S3 studio, S4 images). |
| `X-API-IMPLEMENTATION-PLAN.md` | Reference-only X API spec: endpoint semantics, pricing, policy quirks. Never a build plan. |
| `X-API-PRICING-REFERENCE.md` | Raw pricing tables. |
| `REPLY GUIDE.md` | The growth doctrine (70/30 ratio, reply bands, target roster) that features measure against. |
| `post prompt.md` / `reply prompt.md` / `thread prompt.md` | Prompt sources embedded verbatim as `POST_PROMPT_TEMPLATE` / `REPLY_PROMPT_TEMPLATE` / `THREAD_PROMPT_TEMPLATE` — **byte-sync asserted by bun:test**; edit .md and the TS literal together. **N.3:** post §1/§5 bodies are now `{{PERSONA}}`/`{{BELIEFS}}` placeholders substituted from the active niche (in place, not tail — constant per niche). §6 palette + §9 hook examples still carry builder texture (D28 planning-error residual). **N.4:** reply "Who I am" body → `{{REPLY_PERSONA}}` (heading + parenthetical stay). **AI.7:** `thread prompt.md` (new) = post §0–§5 copied verbatim (same `{{PERSONA}}`/`{{BELIEFS}}`/`{{PILLARS}}`) + thread-specific job/output; tail placeholders `{{PILLARS}}`/`{{FEW_SHOT}}`/`{{IDEA}}`; literal in `src/x/posts/threadPrompt.ts`. |
| `OVERHAUL-PLAN.md`, `REPLY-MASTER-PLAN.md`, `RADAR-REVIEW.md`, `PEOPLE-GRAPH-PLAN.md`, `RELATIONSHIP-OS-PROPOSAL.md`, `MIGRATION-PLAN.md` | Historical/superseded planning docs — background only. |
| `MIGRATION-RUNBOOK.md` | Neon→SQLite one-shot recovery runbook (runtime never touches Postgres). |
| `IPSE-Implementation-PRD.md` | The eventual big product. **Out of scope — never pull patterns from it.** |
| `MANUAL.md`, `DEPLOY.html`, `README.md` | User manual, deploy notes, readme. |
| `docs/` | Per-surface UI docs: one .md per extension tab + `s1`–`s4` + `s6` surface docs (s5=Studio 2.0 is folded into `s3-studio.md`; `s6-augmented-ui.md` = the AX on-page chips/panel). Update the matching file when a tab changes. |
| `evals/` | Reply-quality eval scripts (`analyze-own-replies.ts` — the BAND crosstab) + CSV/md corpora. |
| `scripts/` | Deploy + one-shots + `smoke-*.ts` rerunnable checks (see §9). |
| `public/explorer.html` | S1 data-explorer UI shell (self-contained vanilla JS, served at `GET /explorer`, no build step). |
| `scrape.js`, `humantype.py`, `hammerspoon-init.lua` | Standalone helpers (console harvester origin; typing/paste automation). Not part of the server. |
| `engagement post prompt.md`, `engagement-calendar-14d.md`, `src/my_niche.md` | Content-strategy notes consumed by prompts/planning, not code. |
| `drizzle.config.ts` | Points drizzle-kit at `src/x/db/schema.ts` + `src/db/shared-schema.ts` → `src/db/migrations/`. |
| `biome.json`, `tsconfig.json` | Lint/format; TS strict, `noEmit`, `allowImportingTsExtensions`; tsconfig `include` covers `scripts/**` + `drizzle.config.ts`. **biome `files.ignore` skips `dist`, `node_modules`, `scrape.js`, `Stratus Design System` (UI.8 — the DS folder is reference-only, not built), and `src/db/migrations` (UI.1 — drizzle-generated SQL + snapshot JSON; never hand-linted).** |
| `Stratus Design System/` | Reference-only design-system spec (Decision 9 — never imported/shipped). `tokens/{colors,typography,spacing,radii}.css` are the **source of the `--strat-*` tokens** UI.8 lifted into `styles.css`; `.jsx` components are specs the extension re-implements in TS. biome-ignored. |
| `.env.example` | Every env key, documented. Notables: `API_TOKEN` (one bearer for API+extension+MCP), `SELF_X_USER_ID`, `XAI_API_KEY`, `OPENROUTER_API_KEY` (AI.1 — second LLM provider; env-only, never in `app_settings`), `OPENROUTER_DAILY_BUDGET_USD` (AI.1 — soft, default 1.00, 0 disables), `SQLITE_PATH` (`:memory:` in tests), `X_DAILY_BUDGET_USD` (soft), `XAI_IMAGE_DAILY_BUDGET_USD` (hard 429), `DAILY_METRICS_ENABLED`, `WINNER_REREAD_MIN_VIEWS`, `STRATUS_DEPLOY_HOST`, `MENTION_API_REPLIES` (unread; verify-then-enable carve-out). |
| `.claude/` | `commands/ship.md`; skills: `stratus` (drive the HTTP API), `skill-explainer`, `plan-feature` (this skill), `masterplan` (executes `plans/MASTERPLAN.md` one task/session; state in its `STATE.md`; updates THIS codemap after every task). |
| `plans/` | 12 feature plans + `MASTERPLAN.md` (unified execution order, reasoning levels, waves, adaptations D1–D10). Execution state: `.claude/skills/masterplan/STATE.md`. |

## 3. Server — `src/`

### 3.1 Entry + shared (platform-agnostic) layer

| File | Purpose |
|---|---|
| `src/app.ts` | Hono app. Order: CORS → public `/healthz` → `bearerAuth()` on `/x/*`, `/cost/*`, `/grok/*`, `/llm/*`, `/mcp` → `cost` routes → `mountX(app)` → `mountGrok(app)` → `mountLlm(app)` (always mounts — unlike mountGrok) → `mountMcp(app)` (after mountX so curated tools find routes). `import.meta.main` gates `Bun.serve` + `startXWorkers()` + SIGTERM/SIGINT drain (30s force-exit). Importing from tests never binds a port. |
| `src/middleware/auth.ts` | Bearer guard (`API_TOKEN`). |
| `src/middleware/cors.ts` | `chrome-extension://*` always allowed + `ALLOWED_ORIGINS`. |
| `src/middleware/costTracker.ts` | `makeOnCost(platform, {dailyBudgetUsd})` → writes `cost_events`, platform-tagged; soft budget watchdog logs `BUDGET WATCHDOG`. Platform-agnostic — never hardcode X here. |
| `src/db/client.ts` | The one `bun:sqlite` Database (WAL, `foreign_keys=ON`, boot auto-migrate). **Synchronous driver** — see §7 constraints. |
| `src/db/shared-schema.ts` | `cost_events` (platform free-text: `'x' \| 'grok' \| 'xai' \| 'openrouter'`; AI.1 added the union comment) + `app_settings` (UI.1 — `key`/JSON `value`/`updatedAt` override rows; a missing row = the registry default). |
| `src/settings/store.ts` | UI.1 **platform-agnostic** settings store: sync read-through over `app_settings` (module-level `Map` cache + invalidate-on-write), `getSetting`/`resolveSetting`/`getAllValues(scope?)`/`setSettings`(validate-all → one-txn upsert)/`resetSettings`. Takes a `SettingsRegistry` (never imports `src/x/*`, like `costTracker` takes `platform`). |
| `src/db/migrations/` | `0000`–`0014` + meta (`0013` = `app_settings`, UI.1; `0014` = RU.2 reply `source` + radar `variants`/`model`/`reply_draft_id`). `0000` carries the `content_pillars` seed `INSERT OR IGNORE` — drizzle-kit generate drops seed INSERTs; re-check after every generate. biome-ignored (UI.1). |
| `src/routes/cost.ts` | `GET /cost/today` (per-platform spend + budget flags), `GET /cost/daily?days=` (zero-filled UTC series). |
| `src/routes/healthz.ts` | Public. 503 with `staleWorkers` when a heartbeat goes stale; reports `gitSha` from `.git-sha`; DB errors masked as `db_unreachable`. |
| `src/heartbeats.ts` | Worker heartbeat registry (`beat()` per tick; publisher stale >5min, dailyMetrics >25h). |
| `src/shared/replyBand.ts` | Canonical band classifier (hot/warm/skip/null) + `textLooksLikeReplyBait` — shared server/extension (extension re-export shim). |
| `src/shared/channelSuggest.ts` | Keyword→channel suggestion (word-boundary matching), shared server/extension. |
| `src/mcp.ts` | S2 platform-agnostic MCP bridge: per-request stateless `McpServer` + `WebStandardStreamableHTTPServerTransport` at `POST /mcp`; calls `registerXTools`. |
| `src/test.test.ts` | Grab-bag pure-function suite (best-times, annotateGaps, pinnedSince/buildPinnedWatch, md_to_schedule ladders…). New tests should go in per-area files instead. |
| `src/app.test.ts` | Pre-DB route guards: bearer 401s, CORS preflight, calendar/thread/drafter validation. |

### 3.2 LLM providers — `src/grok/` (text+images) + `src/openrouter/` (AI.1) + `src/llm/` dispatcher (AI.2)

| File | Purpose |
|---|---|
| `grok/client.ts` | `askGrok` — ALL xAI text calls. `/v1/responses`, structured outputs via `text.format` json_schema (NOT `response_format`), `prompt_cache_key`, retry/auth, token pricing → `cost_events` platform `'grok'`; warns on unknown model. |
| `grok/images.ts` | `generateImages` — ALL xAI image calls. `grok-imagine-image` default ($0.02/img; `-quality` $0.05 opt-in), `response_format:b64_json` (never let a raw xAI URL reach the extension — canvas taint), prefers response `usage.cost_in_usd_ticks` over the price table; platform `'xai'`. |
| `grok/pricing.ts` | Token + per-image price maps (`priceForImage`). |
| `grok/routes/ask.ts` | `POST /grok/ask` passthrough. |
| `grok/index.ts` | `mountGrok` (refuses to mount without `XAI_API_KEY`). |
| `openrouter/client.ts` | **AI.1** — `askOpenRouter`, the ONE place all OpenRouter calls go (a second LLM provider, sibling to grok). `POST /api/v1/chat/completions` (OpenAI shape), structured outputs via `response_format.json_schema` (NOT xAI's `text.format`), `usage:{include:true}` + `provider:{require_parameters:true}` ALWAYS; **cost comes straight off `usage.cost` (USD) — NO price table** (warns on a 2xx with tokens but no cost, §9.1); retry/auth/logCost mirror askGrok; `cost_events` platform `'openrouter'`; soft `OPENROUTER_DAILY_BUDGET_USD` watchdog (default $1.00, log-only). `DEFAULT_MODEL='anthropic/claude-sonnet-4.5'` (AI.2 settings own the real default); optional `fetchImpl` test seam; NO `promptCacheKey` (no OpenAI-compat field). INERT until AI.2 wires `askLLM`/`/llm` routes. |
| `openrouter/index.ts` | Re-exports only (`askOpenRouter`, `OpenRouterApiError`, `DEFAULT_MODEL`, types). No route/mount here — `/llm/*` lives in `src/llm/` (AI.2). |
| `llm/ask.ts` | **AI.2** — `askLLM(opts, {defaults})`, the provider dispatcher (§7.1 extended: the ONE place picking grok vs openrouter; each provider keeps its own single entry point). Resolution `opts.provider ?? settings.provider`; merge precedence **opts > DB AI settings > call-site defaults** encoded once here (temperature/maxOutputTokens/reasoningEffort; model: openrouter→`opts.model ?? settings.openrouterModel`, grok→`opts.model` else askGrok's `grok-4.3`). A resolved provider whose env key is missing throws `LlmNotConfiguredError` — **never a silent fallback**. Forwards `fetchImpl` only on the openrouter path, `promptCacheKey` only on grok. Also `llmConfigured()` (either key set — the AI-layer runtime gate) / `llmProviderReady(p)` / pure `resolveProvider` / **`llmErrorPayload(err)`** (AI.5 — shared HTTP mapping: `LlmNotConfiguredError`→503 `llm_not_configured`, `GrokApiError`/`OpenRouterApiError`→429/502 `grok_upstream_error`/`openrouter_upstream_error`, else null so routes keep their generic branch). **LIVE (AI.5 + AI.6)** — call sites: replies single+batch, drafter, pillars/draft, voiceExtract, playbook extract-winners, digest, icebreaker. |
| `llm/settings.ts` | **AI.2** — `getAiSettings`/`saveAiSettings`/`parseAiPatch` over `app_settings` key `'ai'` (REUSES UI.1's table, D1; NOT a registry key so the UI.1 store never touches it). Read-through, NO cache (loadDoctrine discipline). `AiSettings{provider,openrouterModel,temperature|null,maxOutputTokens|null,reasoningEffort|null}`, defaults `grok`/`anthropic/claude-sonnet-4.5`/nulls. Field-by-field `sanitize` (garbage/schema-drift row → defaults). **NO API keys here** — explorer/MCP-visible table (§7.16, Decision 5); keys stay in env. |
| `llm/routes.ts` | **AI.2** — `GET/PATCH /llm/settings` (+ `providers:{grok,openrouter}` env flags; PATCH 400 per field, 409 `provider_not_configured` picking openrouter without its key, returns full settings), `GET /llm/models` (OpenRouter's free list, in-memory 1h cache, 503 `openrouter_not_configured` keyless). |
| `llm/index.ts` | `mountLlm(app)` (**always mounts**, unlike mountGrok) + re-exports `askLLM`/`llmConfigured`/`llmProviderReady`/`LlmNotConfiguredError` + settings helpers/types. |

### 3.3 X slice — `src/x/` (public surface = `index.ts` ONLY; nothing outside imports deeper)

**Primitives**

| File | Purpose |
|---|---|
| `index.ts` | `mountX(app)` + `startXWorkers()` + re-export `registerXTools`. **Read this first for any wiring change — mount order is load-bearing** (see §7 traps). LLM-gated mounts (**AI.6**): `replies`, `drafter`, `voiceExtract` only when `llmConfigured()` (EITHER `XAI_API_KEY` or `OPENROUTER_API_KEY`); `digest`/`playbook`/`pillars` mount always and check `llmConfigured()` at runtime (503); `images` stays `XAI_API_KEY`-only (Decision 6). |
| `client.ts` | `xFetch` — the ONE place all X API calls go (retries, RFC7807 parsing, rate-limit headers, `onCost`, `costHint`). Invariant #4. |
| `auth.ts` / `server.ts` / `token-store.ts` | OAuth2 PKCE; `bun run auth` callback server; single-row token store — refresh-token rotation persisted BEFORE return, serialized by in-process promise-chain mutex (invariant #3 — never reorder). |
| `endpoints.ts` | Typed wrappers: `getMe`, `getTweet`, `getTweetsByIds`, `searchRecent`, `getUserTweets`, `getUserMentions`, `createPost` (URL-surcharge + self-reply + self-quote gates), `deletePost`. Add one endpoint at a time, clamp `max_results` (invariant #5). |
| `fields.ts` / `errors.ts` / `pagination.ts` | Field defaults (`attachments` already included — rides free), `XApiError`+classify, `paginate` async iterator (`perPageSleepMs` for search/all). |
| `pricing.ts` | X price table keyed off endpoint substrings; call sites pass `costHint` when the path can't tell. |
| `db/schema.ts` | All 29 X tables (§4). Type-imports `NicheDoctrine` from `niche/defaults.ts` (type-only, no runtime cycle). |
| `settings/registry.ts` | UI.1 typed settings catalog: `SettingDef`, `SETTINGS_REGISTRY` (doctrine group seeded), `validateSettingValue` (number/bool/string/enum/numberArray + sorted-unique), `settingsByGroup`. Implements the store's `SettingsRegistry` adapter + exports the **bound** `getSetting`/`resolveSetting`/`getAllValues`/`setSettings`/`resetSettings` — consumers import these, never the store. |
| `playground.ts` | `bun run play` scratch calls. |

**Domain modules (pure logic, bun-tested)**

| File | Purpose |
|---|---|
| `conversations.ts` | C2 thread grouping/ranking (`buildThreads`/`rankThreads`/`isActionable`) — no conversation table, regrouped per read. |
| `mentions.ts` | `pullMentions` — since_id checkpoint = max stored tweet_id; answered backfill; C1 person hooks. |
| `playbook.ts` | C4/S0.x aggregations, all gated `DEFAULT_MIN_CELL_N` (n≥20): angles, pillar×register, structures, batch-vs-single (**RU.9:** `classifyReplyOrigin` takes a `postedTweetId→reply_drafts.source` map — a draft-linked reply is `radar` iff `source='radar'`, else `single`; the radar text-match heuristic is now only the fallback for replies with NO draft link, i.e. pre-tooling history; **RL.7:** a 4th param `cannedTexts: ReadonlySet<string>` of normalized `reply_list_uses.renderedText` is checked LAST — a canned use leaves no `reply_drafts` row, so rendered text is its only evidence; `ReplyOrigin` = `'single'|'radar'|'canned'`, `BatchVsSingle` gains a `canned` cell, and a reply that is both a posted draft and a text match counts once, as the draft), band calibration, relationship lift, media (S0.2), latency (S0.5), roster coverage (S0.7), idea payoff (S0.8), **personal-context lift (`buildMeEffectiveness`, ME.5 — replies split by `contextSnapshot.me` present/absent; `withMe`/`withoutMe`/`totalMeasured`, clone of relationship lift + a `totalMeasured` field for the partition invariant)**, **model effectiveness (`buildModelEffectiveness`, AI.12 — posted replies bucketed by raw `reply_drafts.model` string [`/` = provider, never parsed]; `{cells: ModelCell[], totalMeasured}`, cells most-sampled-first, each independently gated; **no lift line** — buckets only, no baseline pair; the OpenRouter-experiment judge)**. |
| `quests.ts` | C9 daily quests (vacuous-done contract — a quiet day never breaks the streak). |
| `digest.ts` | C9 Sunday digest facts assembly (pure; one LLM narration in the route, cached in `digests`). **ME.5:** `DigestGoal` + `DigestFacts.goals` (active `me_goals` + progress; null when none, passthrough in `buildDigestFacts`). **AI.6:** `DIGEST_INSTRUCTIONS` → exported `DIGEST_PROMPT_TEMPLATE` (registry default, key `digest`, `{{FACTS}}` at the tail); `buildDigestInput(facts, template = DIGEST_PROMPT_TEMPLATE)` substitutes via local split/join (no registry import — cycle) — byte-identical to the old concatenation. |
| `conversion.ts` | S0.1 profile-visit → follow conversion math. |
| `people/stage.ts` | Stage engine: stranger→noticed→engaged→responded→mutual (≥2 exchange days)→ally (≥4/60d); ratchets up only. **NT.2:** `PERSON_EVENT_TYPES` gained `their_like`/`their_repost`/`their_follow` and they are in **no** stage set (not NOTICED/INBOUND/OUTBOUND) — engagement is not reciprocity, so 50 likes + a follow is still `stranger` (C10 decision 1, matrix-tested; do not re-litigate). |
| `people/store.ts` | All people writes: `upsertPerson` (fill-only default, overwrite for profile scrapes), `logPersonEvents`/`safeLogPersonEvents` (deterministic ids `type:ref_table:ref_id` + INSERT OR IGNORE), `loadRelationshipFacts(Safe)`. |
| `people/relationship.ts` | C3 relationship block render (server-stamped, variable-tail). |
| `people/angles.ts` | Per-person angle crosstab. |
| `people/followups.ts` | C5 classifier: chain_live > dm_ready > neglected_target > neglected_ally > momentum; S0.6 `pickReupCandidate`; snooze keys `kind:handle` / `reup:<tweetId>`. |
| `people/sightings.ts` | C6 hover-capture ingest (once/day/handle event + snapshot gates). |
| `people/engagements.ts` | **NT.2** — C10 notification-harvest ingest (sightings.ts mirror). `MAX_ENGAGEMENTS_PER_BATCH=50`, `EngagementKind` = like\|repost\|follow (the parser's `'other'` is dropped client-side and is deliberately not representable), `dedupeEngagements` (key `kind:handle:targetText`, freshest wins), `engagementEventId` (`their_like:notif:<handle>:<tweetId>` → day-bucket `…:<YYYY-MM-DD>` when unresolved → `their_follow:notif:<handle>` once ever), pure `matchTargetTweetId(snippet, posts)` (collapsed-whitespace **prefix** match, ≥20 chars or null) + DB wrapper `resolveTargetTweetId`, `recordEngagements` → `{received, processed, skipped, events}` (fill-only upsert `source:'notification'`, INSERT OR IGNORE events with `refTable/refId: null`, own-post window loaded **once per batch** and not at all for a follow-only batch, `recomputePerson` once per distinct handle = watermarks only). **NT.3:** `ENGAGEMENT_KINDS` + the `isEngagementKind` guard live here beside the union (`isStage`/`isFollowupKind` idiom) — `POST /x/people/engagements` is their only caller and the enforcement point for the parser's `'other'`. |
| `people/icebreakers.ts` | C9 grounding renderer (refusal ladder BEFORE key check: 404 → 422 no_shared_context). **AI.6:** `ICEBREAKER_INSTRUCTIONS` → exported `ICEBREAKER_PROMPT_TEMPLATE` (registry default, key `icebreaker`, `{{GROUNDING}}` at the tail); `buildIcebreakerInput(grounding, template = ICEBREAKER_PROMPT_TEMPLATE)` substitutes via local split/join — byte-identical to the old concatenation. |
| `niche/defaults.ts` | **N0.1** — lowest niche layer (imports nothing from prompt/route/store, pillars.ts pattern). `NicheDoctrine` (5 knobs), `DEFAULT_DOCTRINE` (10/20/70/2/10), `Niche` type, `DEFAULT_NICHE` (`builder`; persona/beliefs/replyPersona lifted **verbatim** from post §1/§5 + reply "Who I am", description from `src/my_niche.md` — post §1/§5 moved at N.3, reply "Who I am" moved at N.4; all guarded byte-exact by the equivalence fixtures in `src/test.test.ts`), `NICHE_SLUG_RE` (=PILLAR_SLUG_RE), `resolveDoctrine` (lenient field-by-field: finite positive wins, else default). |
| `niche/store.ts` | **N0.1** — only reader of `niches`: `loadActiveNiche()` (active row else `DEFAULT_NICHE` when none), `loadActiveNicheSafe()` (never throws → default, §7.8), `loadDoctrine()` (resolveDoctrine of the safe active niche). **Post-side consumers wired at N.3** (drafter + pillars/draft), **reply side at N.4** (replies single + batch); **doctrine wired at N.5** — `loadDoctrine()` feeds `targetBand` multipliers + brief reply quota/ratio (voice, brief, followups, playbook, digest). |
| `me/profile.ts` | **ME.1** — pure Me/profile layer (relationship.ts pattern: no DB, callers pass `now`). `ME_KINDS`/`GOAL_KINDS`, window consts (`EMOTION_WINDOW_DAYS=7`/`EVENT_WINDOW_DAYS=30`, inclusive boundary; facts/notes evergreen; `pinned` overrides window+cap), per-kind caps + render hard-caps (`MAX_POST_LINES=14`/`MAX_POST_CHARS=1200`, brief `≤3`/`300`), `ME_INSTRUCTION`/`ME_BRIEF_INSTRUCTION` (instruction rides INSIDE the block, §7.18), `selectEntriesForPrompt`, `goalProgress`/`resolveGoals` (followers ← account_snapshots value, mrr/custom ← manual currentValue; null when no data), `renderMeContext`/`renderMeBrief` (`''` when empty → no injection → prompts byte-identical). Loader `loadMeContextSafe` wired at **ME.2** (`routes/me.ts`); **ME.3** injects the rendered block at all three draft-prompt tails (post drafter + reply single + reply batch). |
| `niche/wizard.ts` | **N0.8** — pure wizard module (pillarDraft.ts pattern): `NICHE_WIZARD_SCHEMA` (structured-outputs `{slug,label,description,persona,beliefs,replyPersona,pillars[3],channels[≤5 w/ keywords]}`), `buildNicheWizardInput(description)` (instruction+anatomy prefix, DEFAULT_NICHE+DEFAULT_PILLARS as the in-prompt EXAMPLE, user description at the variable TAIL §7.15), `parseNicheProposal(raw)` (lenient: coerces slugs to kebab-case, enforces exactly-3 distinct pillars + required non-empty text + length ceilings 120/10000 mirroring the CRUD routes; a malformed channel is dropped not fatal; junk/bad-slug/wrong-count → null). Consumed only by `POST /x/niche/draft`. |
| `posts/prompt.ts` | `POST_PROMPT_TEMPLATE` (byte-synced with `post prompt.md`) + drafter helpers, `buildPostDraftsSchema(slugs)`. **N.3:** `PERSONA_PLACEHOLDER`/`BELIEFS_PLACEHOLDER`; `BuildPostDraftOptions.persona?/beliefs?` (default `DEFAULT_NICHE`) substituted FIRST, in place. **ME.3:** `BuildPostDraftOptions.meContext?` appended at the variable tail before `guidance` (empty → no change). **AI.3:** `BuildPostDraftOptions.template?` — the registry-loaded body (default `POST_PROMPT_TEMPLATE`, so callers/tests untouched). |
| `posts/pillars.ts` | `DEFAULT_PILLARS`, `parsePillar`. |
| `posts/threadPrompt.ts` | **AI.7** — thread drafter (pure): `THREAD_PROMPT_TEMPLATE` (byte-synced with `thread prompt.md`; post §0–§5 verbatim + thread job/output), `buildThreadDraftSchema(slugs)` (`{pillar enum, tweets string[]}` — **no minItems/maxItems**, N.8/RU.3 rejection-risk convention; count steered by prompt), `parseThreadDraft(raw, slugs) → {pillar, tweets, overLong}|null` (≥2 tweets, bad pillar → null, 280-over `overLong` positions computed not rejected), `buildThreadDraftInput(opts)` (persona/beliefs in place; pillar+tweetCount+idea folded into the single `{{FEW_SHOT}}`/`{{IDEA}}` tail — no `{{PILLAR}}` token; meContext+guidance appended). Imports only types/consts from `posts/prompt.ts` (no cycle). |
| `posts/rewritePrompt.ts` | **AI.8** — rewrite assist (pure, **TS-only — Decision 9, no `.md`/byte-sync**): `REWRITE_PROMPT_TEMPLATE` (`{{DRAFT}}`/`{{INSTRUCTION}}` at the tail; **no persona/pillar token** → cache bucket unaffected by niche), `REWRITE_SCHEMA` (static `{variants:[{text, kind}]}`, kind enum `tightened|rehooked|restructured`, **no minItems/maxItems**), `MAX_VARIANT_LENGTH=560`, `parseRewrite(raw, maxLen=560) → RewriteVariant[]|null` (null on malformed `variants`; empty/over-long/unknown-kind entries dropped — array may be `[]`), `buildRewriteInput({draft, instruction?, template?})` (split/join $-safe; missing instruction → `(none — just sharpen it)`). No DB, no niche import. |
| `posts/ideasPrompt.ts` | **AI.9** — idea generator (pure, **TS-only — Decision 9, no `.md`/byte-sync**): `IDEAS_PROMPT_TEMPLATE` (`{{PILLARS}}`/`{{WINNERS}}`/`{{STEER}}` at the tail; **no persona/belief token** → cache bucket niche-independent even though the rendered pillars are niche-scoped, since pillars sit at the variable tail), `IDEAS_SCHEMA` (static `{ideas:[{text,pillar,angle}]}`, angle enum `observation|stance|story|question`, **`pillar` a FREE string not enum-bound** so a mis-tag is nulled not dropped; **no minItems/maxItems**), `MAX_IDEA_TEXT=500`/`MAX_IDEA_COUNT=10`, `parseIdeaProposals(raw, allowedSlugs, maxCount=10) → IdeaProposal[]|null` (null on malformed `ideas`; empty/over-long/unknown-angle entries dropped; a pillar outside `allowedSlugs` → **null, not dropped**; clamped to maxCount — array may be `[]`), `buildIdeasInput({pillars?, winners, steer?, count?, template?})` (split/join $-safe; count folds into the {{STEER}} block like threadPrompt folds tweetCount; empty winners/steer → placeholder text). No DB, no niche import. |
| `posts/pillarDraft.ts` | AI pillar proposal (never persisted directly). **N.3:** hardcoded compact `PERSONA` deleted — `BuildPillarDraftOptions.persona?` (default `DEFAULT_NICHE.persona`); the route passes niche.persona+description. **AI.5:** the persona+format block is now `PILLAR_DRAFT_TEMPLATE` (registry default, key `pillar-draft`) with `{{PERSONA}}` (optional, in-place first) + `{{EXISTING_PILLARS}}`/`{{JOB}}` (required); `BuildPillarDraftOptions.template?`; the job strings stay code-built. |
| `replies/prompt.ts` | `REPLY_PROMPT_TEMPLATE` (byte-synced with `reply prompt.md`; **RU.1**: 3 variants, one per angle extends/contrarian/debate), `buildGrokInput`, `buildBatchGrokInput` (**AI.5**: renders the standalone `REPLY_BATCH_PROMPT_TEMPLATE` — the heading-slicing `sharedVoiceBlock`/`batchReplyHead`/`VOICE_BLOCK_END` machinery is GONE; the batch default embeds the reply default's voice block verbatim, locked by the anti-drift test in `src/test.test.ts`; `{{POSTS}}`/`{{IDEA}}` substitute at the tail with the token-missing fallbacks preserved for pre-token overrides; the C3 relationship note rides inside the POSTS value; **RU.3**: 3 variants/post, `BatchReply={tweetId,variants[]}`, `parseBatchReplies` validates ≥1 variant/entry), `parseContext`/`parseBatchTweets` (NEVER accept `relationship`/`guidance`/`niche` from clients — whitelist builders), `BATCH_REPLY_SCHEMA` (`{replies:[{id,variants:[{text,angle}]}]}`). **N.4:** "Who I am" body → `{{REPLY_PERSONA}}` (`REPLY_PERSONA_PLACEHOLDER`); `buildGrokInput`/`buildBatchGrokInput` gain trailing `opts?: {replyPersona?}` (default `DEFAULT_NICHE.replyPersona`), substituted FIRST/token-tolerant via `substituteReplyPersona` (batch: over the effective template before POSTS/IDEA land); `PostContext.niche?: {slug}` server-stamped. **ME.3:** `PostContext.me?` server-stamped (parseContext allowlist never accepts it) — `buildGrokInput` appends it at the tail after relationship (order relationship→me→guidance); `buildBatchGrokInput`'s opts bag gains `meBrief?`, appended ONCE per batch after guidance. **AI.3/AI.5:** BOTH builders' opts bags carry `template?` (registry body; per-request `override` still beats it). |
| `replyLists/engine.ts` | **RL.1** — canned-reply engine (pure, followups.ts style: no DB, no clock, no `Math.random` — callers inject `rng`). `MAX_REPLY_LENGTH=280`, `TEMPLATE_VARS` (`name`/`first_name`/`handle`), `DEFAULT_HUMANIZER` (prefix .25 / suffix .20 / lowercase .15 / drop-period .10 / typo .05 — opening guesses; per-list JSON overrides), `parseHumanizerConfig` (lenient field-by-field, **null ONLY on a non-object** — that's what lets RL.2's CRUD 400 an invalid humanizer) + `resolveHumanizer`, `renderTemplate` (a missing var takes ONE adjacent separator with it → "Thank you!"; unknown `{foo}` verbatim; emoji/flags stripped from the name), `templateVars`/`availableVarsFor`, `pickItem` (anti-repeat: var-needing items deprioritized, `min(n-1, floor(n/2))` most-recent excluded, uniform pick among the rest — no repeat at n≥2), `humanize` (**fixed `HUMANIZE_DRAWS`=10 draws whether or not a step fires**, so a stubbed sequence maps to steps by position; names/handles/`@`/`#`/links are protected spans never typo'd; an overflowing prefix/suffix is skipped, never truncated), `composeReply`. |
| `replyLists/generate.ts` | **RL.4** — reply-list AI item generator (pure, **TS-only — Decision 9, no `.md`/byte-sync**): `REPLY_LIST_PROMPT_TEMPLATE` (registry default, key `reply-list`; `{{COUNT}}`/`{{REQUEST}}`/`{{EXISTING_ITEMS}}` all required and all at the tail — **no persona token**, so the cacheable prefix is niche-independent and the route's cache key needs no `:slug:updatedAtMs` suffix, same as `ideas`/`rewrite`), `REPLY_LIST_GEN_SCHEMA` (`{items:[{text}]}`, no minItems/maxItems — the strict-mode rejection-risk convention), `buildListGenInput({prompt, count?, existingItems?, template?})` (split/join $-safe; empty list → `(none yet — this list is empty)`; existing items capped at 30), `parseGeneratedItems(raw, maxCount=30) → {text}[]|null` (**null only when `items` is missing/not an array**; empty + >`MAX_REPLY_LENGTH` entries dropped; **dedupe key is lowercased + whitespace-collapsed**, since casing-only twins defeat the anti-repeat pick; `[]` is a legal "nothing survived" the route 502s on). `DEFAULT_GENERATED_ITEMS`=12 / `MAX_GENERATED_ITEMS`=30 / `MAX_GEN_PROMPT_LEN`=2000. |
| `prompts/registry.ts` | **AI.3** — the editable-prompt catalog: `PROMPT_KEYS` (**RL.4: 11 keys — `reply`/`reply-batch`/`post`/`thread`/`rewrite`/`ideas`/`voice-extract`/`pillar-draft`/`digest`/`icebreaker`/`reply-list`** — new keys append at the end; defaults from pure modules, `ideas` from `posts/ideasPrompt.ts`, `reply-list` from `replyLists/generate.ts`), `isPromptKey`, `PROMPT_SPECS` (name/description/`defaultBody` = the byte-synced templates + `required`/`optional` placeholder sets; **defaults import from PURE modules only — a route-file import would cycle**), `loadPrompt(key)`/`loadPromptSafe` (§7.8 — `prompt_overrides` row else default; row presence IS `customized`; sha-8 `cacheKey`, call sites append the niche `:slug:updatedAtMs` suffix), `validatePromptBody` (`{ok,missing,unknown}` — required must survive an edit, niche placeholders optional/token-tolerant, unknown `{{A_Z}}` deduped warning), `renderPrompt` (split/join, $-safe; for later-task call sites), `promptCacheKey`. Imports the prompt modules for defaults incl. `digest.ts`/`people/icebreakers.ts` (AI.6 — both pure-ish, no registry import; no cycle: routes → registry → templates). |
| `voice/extractPrompt.ts` | **AI.5** — pure §8.3 extraction pieces moved out of `routes/voiceExtract.ts` (registry-cycle avoidance): `EXTRACT_PROMPT_TEMPLATE` (registry default, key `voice-extract`, `{{TWEET_TEXT}}` at the tail — renders byte-identical to the old `EXTRACT_PROMPT_PREFIX + text`), `TEMPLATE_SCHEMA`, `TEMPLATE_LENGTHS`/`TemplateLength`/`ExtractedTemplate`, `parseExtractedTemplate`, `TEMPLATE_EXTRACT_MAX_OUTPUT_TOKENS`. Consumed by `routes/voiceExtract.ts` + `routes/playbook.ts`. |
| `data/inspect.ts` | S1 read-only core: second `{readonly:true}` connection (`:memory:` falls back to primary), whitelist from Drizzle exports (**`tokens` excluded entirely**), `readTable` (validated identifiers, escaped LIKE), `runSelect` (single-statement SELECT/WITH, 500-row cap), `describeTable`. Reused by MCP schema tools. |
| `mcp.ts` | S2 `registerXTools` — 19 tools: 3 schema (inspect core), 12 curated (in-process `app.request` with forwarded bearer; `x_digest` forces `factsOnly`; **N0.2 added `x_niche`** → `GET /x/niche`; **M1.6 added `x_me`** → `GET /x/me`), 4 write ($0 only; `x_draft_post` hard-codes `status:'draft'`; **M1.6 added `x_add_me_entry`** → `POST /x/me/entries`, zod enum restricts `kind` to the 4 ME_KINDS — no goal write, journal only). |

**Workers — `src/x/workers/`**

| File | Purpose |
|---|---|
| `publisher.ts` | 60s tick. Claim txn (`pending→publishing` committed BEFORE `createPost`) → finalize; 4xx→`failed`, 5xx/network → stuck `publishing` (ambiguous, shouted every tick, reconcile resolves). Threads: self-replies ~500ms apart, one bad segment freezes the rest (`thread_frozen`); re-verifies self-quote ownership. |
| `dailyMetrics.ts` | 03:00 UTC: getMe → `account_snapshots` (1/UTC-day, + `pinned_tweet_id`); since_id discovery of own tweets (stamps `has_media`); snapshot every non-retired row — **retire batch BEFORE writing snapshots** (invariant #7); winner re-read (≤5/day, claim pollCount 1→2 before the billed call); `pullMentions`. |

### 3.4 HTTP routes — `src/x/routes/` (all under `/x`, bearer-guarded unless noted)

| Router file | Endpoints (method path — one-line semantics) |
|---|---|
| `brief.ts` | `GET /brief` — Today-tab payload: KPI+sparkline, yesterday, leaders, plan+annotated gaps (S0.4), reply quota, ratio, spend, quests+streak (C9), pinnedWatch (S0.9). `?tzOffsetMin=` local-day; spend stays UTC. Pure helpers `pinnedSince`/`buildPinnedWatch` live here. **N.5:** the old `REPLY_TARGET`/`WEEK_REPLY_TARGET_PCT` consts are gone — reply quota/ratio target + the target-roster band all read `loadDoctrine()` (one sync SELECT/request). |
| `calendar.ts` | `POST /posts/scheduled` (URL guard on pending; optional validated `pillar` — validates via `getActivePillars()`, so it **inherits the N.6 active-niche filter** for free, no code change), `POST /posts/threads`, `GET /posts/scheduled` (+filters), `GET/PATCH/DELETE /posts/scheduled/:id` (worker-owned statuses 409; PATCH `media_note`; GET returns thread siblings + `seededBy`). |
| `metrics.ts` | `GET /metrics/replies`, `/metrics/posts`, `/metrics/account` (**S5.5** the Studio milestone card's client `api.metrics.account()` consumes this — `{count,latest,series}`, `AccountSeriesPoint`), `/metrics/best-times` (`?tzOffsetMin=`, top gated n≥3), `/metrics/pillars`, `/metrics/:tweetId`. |
| `posts.ts` | `POST /posts/reconcile` — triggers the daily pass manually. |
| `pillars.ts` | `GET/POST /pillars`, `PATCH/DELETE /pillars/:slug` (last-active 409), `POST /pillars/draft` (proposal, runtime `llmConfigured()` check → 503 `grok_not_configured` AI.6; **N.3** grounds on active niche persona+description; **AI.5** `loadPromptSafe('pillar-draft')` → `template` into `buildPillarDraftInput`, `askLLM` w/ defaults low/700/0.7, body `provider` accepted, cache key `` `${prompt.cacheKey}:${slug}:${updatedAtMs}` `` — `PILLAR_DRAFT_CACHE_KEY` gone). **N.6:** all reads scoped by `ownedByNiche(activeSlug)` = `niche = active.slug OR niche IS NULL` (legacy tolerance); POST stamps `niche = loadActiveNicheSafe().slug`; `getActivePillars()` falls back to `DEFAULT_PILLARS` **only when active niche is `builder`** (else `[]` — the drafter refusal trigger); last-active 409 guard counts **within the active niche's set** (DELETE only guards when the target belongs to that set, so it can't false-fire on another niche's pillar). |
| `prompts.ts` | **AI.4** — the prompt editor over `prompt_overrides` (always mounted next to `pillars` — works with no LLM key). `GET /prompts` (list: key/name/description/required/`customized`=row-presence/`updatedAt`), `GET /prompts/:key` (body+`defaultBody`+customized+required, for diff/reset), `PATCH /prompts/:key {body}` (≤32KB → 413 `body_too_large`; `validatePromptBody` → 400 `missing_placeholder`+`missing`; upsert → `{customized:true, unknownPlaceholders}`), `POST /prompts/:key/reset` (delete row → `{customized:false}`), `POST /prompts/restore-defaults` (delete ALL → `{restored:n}`); unknown key → 404 `unknown_prompt`. Reuses the AI.3 registry; static `restore-defaults` + `:key/reset` param don't collide (§7.20, different segment counts). |
| `niche.ts` | **N0.2** — `GET /niche` (active niche + resolved doctrine, `DEFAULT_NICHE` fallback), `GET /niches` (active-first), `POST /niches` (created INACTIVE, 409 `slug_exists`), `PATCH /niches/:slug` (edit + doctrine; `active:true` → atomic swap in a **sync** txn; `active:false` on the only active → 409 `last_active_niche`), `DELETE /niches/:slug` (409 `niche_active` if active; owned pillars/channels keep their stamp). Validation mirrors pillars; doctrine rejects non-positive/non-numeric (no silent coerce). Always mounted, $0. **N0.8:** `POST /niche/draft` (the wizard) — `{description ≤5000}` → one `askGrok` structured-outputs call (effort low, `maxOutputTokens` 2500, cache key `stratus-niche-wizard`) → parsed `NicheProposal` (never persisted). Description validated (400 `invalid_description`) BEFORE the runtime XAI key check (503 `grok_not_configured`) so the $0 guards are testable without a key; the paid call is last (§7.4). `POST /niche/draft` ≠ `POST /niches` — no `:slug` collision. |
| `me.ts` | **ME.2** — `GET /me?kind=&active=` (entries each w/ server-computed `inWindow` via `isEntryInWindow`; goals each w/ `progress`), `POST/PATCH/DELETE /me/entries[/:id]` (kind/text≤1000/happenedAt-ISO/pinned/active; per-field 400 codes), `POST/PATCH/DELETE /me/goals[/:id]` (label/kind/target>0/unit/deadline/currentValue/status), `GET /me/context?mode=post\|reply` → `{mode, block\|null}` (the exact injected block, §7.18). Exports **`loadMeContextSafe(mode)`** — active entries + active goals + latest `account_snapshots` followers → `selectEntriesForPrompt`/`resolveGoals` → render; null on empty OR any error (§7.8, never throws). Always mounted, $0 (pure SQL). Static `/me` prefix + `:id` params shadow nothing (§7.20). **ME.3** consumes `loadMeContextSafe` in `drafter.ts` (post) + `routes/replies.ts` (single stamps `ctx.me`, batch passes `meBrief`). |
| `drafter.ts` | `POST /posts/draft` (3 register-distinct drafts → `status='draft'` rows), `POST /posts/reup` (self-quote drafts; ownership via posts_published), **`POST /posts/draft-thread`** (**AI.7** — `{idea?,ideaId?,pillar?,tweetCount?,model?,provider?,reasoningEffort?}` → one structured-outputs call via `loadPromptSafe('thread')` + `buildThreadDraftInput`/`buildThreadDraftSchema` → `insertThreadDraft` lands a `status='draft'` head + `status='segment'` tails sharing a threadId, `source='drafter'`; **one auto-regenerate** if a segment >280 chars (nudge names them, still-long → 502 `thread_invalid`); `tweetCount` non-int → 400 `invalid_tweet_count`, clamp 3–8; `consumeIdeaSafe` after insert; URL allowed in drafts. `insertThreadDraft(pillar,tweets,ideaId?)` exported for the route test), **`POST /posts/rewrite`** (**AI.8** — `{text [1–2000], instruction? [≤500], model?, provider?}` → inline validation [`invalid_body`/`invalid_text`/`invalid_instruction`/`invalid_model`/`invalid_provider`, all pre-spend] → `loadPromptSafe('rewrite')` + `buildRewriteInput` → `askLLM` [defaults 0.7/900/low, cache key = bare `prompt.cacheKey`] → **no DB rows**; `parseRewrite` null → 502 `rewrite_parse_error`, `[]` → 502 `rewrite_invalid`; over-long variants dropped, never truncated). LLM-gated mount (**AI.6** `llmConfigured()`). **N.3:** `generateAndInsert` loads `loadActiveNicheSafe()`, passes persona/beliefs, cache key `` `${PROMPT_CACHE_KEY}:${slug}:${updatedAtMs}` ``. **N.6:** both routes refuse `409 {error:'no_pillars_for_niche', niche}` when `getActivePillars()` is `[]` (a custom niche with no active pillars) — BEFORE any Grok spend (§7.4). `POST /pillars/draft` deliberately does NOT refuse (empty `existing` is how the first pillar is made). **AI.3:** the prompt comes from `loadPromptSafe('post')` (`template` into `buildPostDraftInput`), cache key `` `${prompt.cacheKey}:${slug}:${updatedAtMs}` `` (static `PROMPT_CACHE_KEY` gone). **AI.5:** `generateAndInsert` calls `askLLM` (defaults temp 0.7/600/low); `parseCommon` accepts `provider` (400 `invalid_provider`) and returns `reasoningEffort` only when body-supplied (house default rides the defaults tier); errors via `llmErrorPayload`. |
| `replies.ts` | `POST /replies/generate` (band gate 422 BEFORE spend → relationship+guidance stamp → 3 variants, `MAX_OUTPUT_TOKENS=520` → specificity gate w/ one auto-regen), `POST /replies/generate-batch` (≤25, **3 variants/post RU.3**, `maxOutputTokens=min(9000,200+n*420)`, response items `{tweetId,text,angle,variants}` primary=variants[0], no DB rows, persists `radar_drafts`), `GET /replies`, `/replies/outcomes` (§6.2 join), `/replies/default-prompt`, `GET/PATCH/DELETE /replies/:id` (PATCH→posted = paste time, logs my_reply, backfills postedTweetId). LLM-gated mount (**AI.6** `llmConfigured()`). **N.4:** both generate paths `loadActiveNicheSafe()` (single: AFTER the band gate — refuse-before-work), pass `replyPersona`, stamp `ctx.niche={slug}` pre-insert (contextSnapshot per-niche key), cache keys niche-suffixed `:slug:updatedAtMs`. **AI.3:** single generate loads `loadPromptSafe('reply')` AFTER the band gate (`template` into the opts bag — a client `systemPromptOverride` still beats it), cache key `` `${prompt.cacheKey}:${slug}:${updatedAtMs}` ``; `GET /replies/default-prompt` returns the effective (override-aware) registry body. **AI.5:** both generate paths call `askLLM` (house defaults temp 0.7 / 520 & the computed batch cap / effort low via the defaults tier — a stored AI setting sits between body opts and them, D44), accept body `provider` (400 `invalid_provider`, validated before the band gate), and map errors via `llmErrorPayload`; batch loads `loadPromptSafe('reply-batch')` (standalone template, cache key niche-suffixed like single — `BATCH_PROMPT_CACHE_KEY` gone). **RU.8:** `parseBatchTweets` accepts `band:'manual'` (a ⊕ pinned tweet — queue metadata, still never reaches Grok). |
| `voice.ts` | `POST /voice/scrape`, `PUT /voice/authors/:handle` (enrich + snapshot append), `GET /voice/authors`, `PATCH/DELETE /voice/authors/:handle`, `GET /voice/targets` (target band + momentum), `GET /voice/tweets` (filters), `PATCH /voice/tweets/:tweetId` (`tags`/`addTags`), `DELETE /voice/tweets/:tweetId`. Exports `loadTargetHandles`. All $0. **N.5:** `targetBand(myFollowers, band={minX:2,maxX:10})` — the multipliers now come from `loadDoctrine()` (default keeps every call site/test green); call sites in voice/brief/followups/playbook/digest pass the active niche's `targetBandMinX/MaxX`. |
| `voiceExtract.ts` | `POST /voice/tweets/:tweetId/extract`, `POST /voice/extract-batch` (≤50). LLM-gated mount (**AI.6** `llmConfigured()`). **AI.5:** the §8.3 prompt/schema moved to pure `../voice/extractPrompt.ts` (registry-cycle avoidance); `extractOne` renders `loadPromptSafe('voice-extract')` via `renderPrompt` and calls `askLLM` (defaults effort low/250 tok/temp 0.2; cache key = `prompt.cacheKey`, no niche suffix — no persona in this prompt); failures classify `llm_not_configured`/`grok_<status>`/`openrouter_<status>`. |
| `harvest.ts` | `POST /harvest/runs`, `POST /harvest/rows` (≤500; replies-mode reconcile → backfills draft `postedTweetId`), `GET /harvest/runs`. |
| `mentions.ts` | `GET /mentions`, `POST /mentions/refresh` (6/day server cap), `PATCH /mentions/:tweetId`. |
| `conversations.ts` | `GET /conversations` (regrouped per read), `PATCH /conversations/:conversationId` (read/snooze/mute → `conversation_meta`). |
| `people.ts` | `GET /people`, `GET /people/rankmap` (**before `:handle`**), `GET /people/glance` (**AX.1** — timeline-decoration map, **also before `:handle`**; $0: non-retired people + unanswered-mention open-loop counts + `loadTargetHandles()` backfill → `{count, map: {handle→{stage,isTarget,openLoops,lastOutboundAt,lastInboundAt,followersCount}}}`, dates ISO), `GET /people/:handle` (dossier), `PATCH /people/:handle` (only demote path), `POST /people/sightings` (C6 batch ≤50), **`POST /people/engagements`** (**NT.3** — C10 notification harvest, $0; body `{engagements:[{kind,handle,targetText?,seenAt}]}` ≤`MAX_ENGAGEMENTS_PER_BATCH`=50 → `recordEngagements` counts. Sightings-route validation shape: `invalid_body` / `invalid_engagements` / `too_many_engagements` / per-index `invalid_engagement_{kind,handle,target_text,seen_at}_<i>`. **POST, so the §7.20 static-vs-`:handle` trap doesn't bite** — only `GET /people/:handle` exists. `kind` is whitelisted through `isEngagementKind` — that is where the parser's `'other'` dies; `targetText` is **re-forced null for a follow** so a stale extension build can't feed a follower's bio to the target matcher; an invalid handle is passed through and surfaces in `skipped`, never fatal), `POST /people/:handle/events`, `POST /people/:handle/icebreakers` (refuse-before-spend; **AI.6** migrated to `askLLM` via `loadPromptSafe('icebreaker')` + `llmErrorPayload`, gate `llmConfigured()` keeping the 503 `grok_not_configured` string; 404→422→503 ladder order preserved). |
| `followups.ts` | `GET /people/followups` (queue + momentum + S0.6 reup item), `PATCH /people/followups` (snooze; reup special-cases `tweetId`), `GET /people/fans`. **Mounted before peopleRouter.** |
| `launch.ts` | `POST /launch/replies` (C7 early-replier ingest, shared deterministic event id with mentions; never touches `mentions` table). |
| `radar.ts` | `GET /radar/drafts?status=&tweetId=` (**RU.5** `?tweetId=` filter: explicit `status` wins, else tweetId→non-expired, else the ready default; lazy 48h expiry), `PATCH /radar/drafts` (status ratchet), `PATCH /radar/drafts/:tweetId/tags`, **`POST /radar/drafts/:tweetId/confirm` (RU.5)** — promotes the newest live radar draft to a measured `reply_drafts` row ($0, always-mounted): idempotent via the soft `reply_draft_id` link (linked row → 200; dangling → rebuild), else one sync txn (§7.13) inserts `{source:'radar', status:'copied', variants (fallback `[{text:replyText,angle}]`), model `?? 'radar-batch'`, costUsd:null, contextSnapshot=`PostContext` rebuilt from snippet/band+signals/`postedAt=draftedAt−ageMin`}` + stamps `reply_draft_id` + ratchets radar status→`clicked`; 400 `invalid_tweet_id`, 404 `not_found`. No `my_reply` event here — that stays on the PATCH `/replies/:id` posted flip. `copied→posted` already in `ALLOWED_TRANSITIONS`. **RU.8:** `RadarBatchTweet`/`RadarDraftInsert` band widened to `'manual'`; the confirm endpoint **coerces `band:'manual'`→`null`** in the rebuilt `contextSnapshot.signals.band` (queue metadata, never a classifier verdict → never a Playbook hot/warm cell, §7.19). |
| `replyLists.ts` | **RL.2** — canned-reply list CRUD ($0, always mounted next to `ideasRouter`). `GET /reply-lists` (sortOrder asc, each row + `itemCount`/`enabledCount` from ONE grouped select), `POST /reply-lists` (`{name ≤120, description? ≤2000, humanizer?, active?, sortOrder?}`), `GET /reply-lists/:id` → `{list, items}` (items createdAt asc), `PATCH /reply-lists/:id` (partial, `empty_patch` 400), `DELETE /reply-lists/:id` (items cascade via FK; the use log survives on purpose), `POST /reply-lists/:id/items` (`{mode:'append'|'replace', items:[{text}] ≤100 each 1..`MAX_REPLY_LENGTH`, source?:'manual'|'ai'}` — ONE sync txn §7.13, replace = delete+insert, response is always the list's WHOLE current set so both modes read alike; `createdAt` stamped per row past the list's current max so insertion order survives a same-millisecond batch), `PATCH/DELETE /reply-lists/:id/items/:itemId` (**both scoped `id` AND `listId`** — right item under the wrong list is a 404). `humanizer` runs through the engine's `parseHumanizerConfig` and is stored NORMALIZED: `null` clears to `DEFAULT_HUMANIZER`, a non-object → 400 `invalid_humanizer`, a partially-bad object is lenient-parsed and accepted (D76a). Malformed uuid → 400 `invalid_id`, well-formed-but-absent → 404 (ideas.ts idiom). **RL.3** `POST /reply-lists/:id/use` (`{vars?:{name?,handle?} ≤120, targetTweetId? /^\d{1,32}$/, targetHandle? (normalized no-`@`/lowercase, `USERNAME_RE`), preview?}` → `{itemId, text, missingVars, applied}`): loads ALL items (pickItem owns the enabled/var rules — one place), `pickItem(items, availableVarsFor(vars), Math.random)` → **409 `no_enabled_items`** when null, `composeReply(item.text, vars, resolveHumanizer(list.humanizer), Math.random)` (protected spans come from the vars inside composeReply — there is no `protectedValues` arg), then unless `preview` ONE sync txn stamps `lastUsedAt` + `use_count+1` and inserts the `reply_list_uses` row. **`lastUsedAt` is stamped monotonically past the list's max** (§7.13a — the anti-repeat window only holds while the just-used item is STRICTLY most recent); `updatedAt` deliberately untouched (it means "text edited"). A body-less POST is a legal no-target use; a body that parses to a non-object 400s. An `active:false` list is still usable (active is presentation state, not a gate). **RL.4** `POST /reply-lists/:id/generate` (`{prompt 1..2000, count? 1..30 default 12, model?, provider?}` → `{items:[{text}], count, requested, model, costUsd, requestId}`): the only non-$0 route here — one `askLLM` structured-outputs call (defaults temp 0.9 / 1500 tok / low, `promptCacheKey` = the bare registry sha) that **writes NOTHING** (Decision 3 — the panel previews and the user applies via `POST /items` `source:'ai'`, so "overwrite my list" stays a human click). Guard order: `invalid_id` → `invalid_body` → `invalid_prompt` → `invalid_count` (**refused, not clamped** — silently returning 12 of 40 reads as broken) → `invalid_model`/`invalid_provider` → **404 before any spend** → existing item texts (disabled included) into the prompt tail. **No pre-flight key check** (ideas.ts/AI.9 precedent, D80): `askLLM` enforces the resolved provider's key and `llmErrorPayload` maps it → 503 `llm_not_configured` — NOT `grok_not_configured`, so an OpenRouter-only setup works; parse null → 502 `items_parse_error`, `[]` → 502 `items_invalid`. |
| `ideas.ts` | `GET/POST /ideas`, `PATCH /ideas/:id` (consume/reopen provenance rules), `DELETE /ideas/:id`. **AI.9:** `POST /ideas/generate {steer?, count? (1–10, default 8), model?, provider?}` → `{ideas:[{text,pillar,angle}], count, requested, costUsd, model}` — **writes nothing** (the panel saves picked ones via `POST /ideas` `tags:['ai']`); pre-spend guards `invalid_body`/`invalid_steer` [≤2000]/`invalid_count`/`invalid_model`/`invalid_provider` (400) then `getActivePillars()` empty → **409 `no_pillars_for_niche`** (drafter discipline — a custom niche with no pillars never spends nor leaks builder pillars); `topWinners()` few-shot (imported from `drafter.ts` — deferred function call, safe cycle) + `loadPromptSafe('ideas')` → `askLLM` (defaults temp 0.8/1400 tok/low, cache key = bare `prompt.cacheKey`, **no niche suffix** — persona-free prompt, pillars at the tail); `parseIdeaProposals` null → 502 `ideas_parse_error`, `[]` → 502 `ideas_invalid`, `llmErrorPayload` → 503 `llm_not_configured`. |
| `channels.ts` | `GET/POST /channels`, `GET /channels/:slug` (the aggregate room), `PATCH/DELETE /channels/:slug`. **N.6:** `GET /channels` scoped by `ownedByNiche(activeSlug)` (`niche = active.slug OR NULL`); POST stamps `niche = loadActiveNicheSafe().slug`; the `:slug` room 404s when the channel belongs to a non-active niche (NULL-niche legacy rows stay visible); PATCH/DELETE untouched (slug-addressed). Extension `ChannelTags` + content-script chips inherit the filter with zero client change. |
| `playbook.ts` | `GET /playbook?minN=`, `POST /playbook/extract-winners` (≤20 winners/call, runtime `llmConfigured()` check → 503 `grok_not_configured` AI.6; **AI.5** shares `loadPromptSafe('voice-extract')` + `renderPrompt` with voiceExtract's path, `askLLM`, body `provider` accepted). Loaders + `loadReplyGuidanceSafe`/`loadPostGuidanceSafe` (always DEFAULT gate) + exported `loadFollowersByHandle`. **ME.5:** `ReplyRow.hasMe` (from `ctx.me`) feeds `meEffectiveness` in the payload — no new query, same reply rows as relationship lift. **RU.9:** `loadOriginRows` selects `replyDrafts.source` alongside `postedTweetId` and builds the `postedTweetId→source` map fed to `classifyReplyOrigin` (exact radar attribution, text-match only as the no-draft-link fallback). **RL.7:** one more unwindowed select — every `reply_list_uses.renderedText` normalized into a `Set` (empty strings skipped) — feeds the `canned` bucket; the table is FK-free so a deleted list never erases its attribution. |
| `digest.ts` | `GET /digest?week=&tzOffsetMin=&refresh=&factsOnly=` (cached per week; degrades to facts on LLM failure). **ME.5:** facts carry `goals` — `loadGoals(now)` = active `me_goals` × latest `account_snapshots` followers via `resolveGoals`, null when none. **AI.6:** `loadPromptSafe('digest')` → `buildDigestInput(facts, prompt.body)` + `askLLM` (defaults low/700/0.7, `prompt.cacheKey` replaces the static `stratus-digest`); the no-key degrade + catch classify to `llm_not_configured`/`grok_<status>`/`openrouter_<status>`/`llm_parse_error`/`llm_failed` (gate = `llmConfigured()`; the extension `Digest.tsx` matcher moved to `llm_not_configured`). |
| `brief`/`streaks` | streak upsert happens inside `GET /brief` (idempotent per local day). |
| `images.ts` | `POST /images/generate` (n≤2; HARD daily budget 429 before spend; returns data: URLs). |
| `assets.ts` | `POST /assets` (≤2MB), `GET /assets` (metadata only), `GET /assets/:id/png` (bytes), `DELETE /assets/:id`. |
| `data.ts` | `GET /data/tables`, `GET /data/:table`, `POST /data/query` — S1 read-only; plus `explorer` router: `GET /explorer` at root, **outside bearer** (data-free shell). |
| `settings.ts` | UI.1 (mounted next to `data`): `GET /settings` (groups + value + isDefault), `GET /settings/values?scope=mirrored` (flat mirror payload), `PATCH /settings` (per-key validated, all-or-nothing → 400 `unknown_setting`/`invalid_setting_value`), `POST /settings/reset` (`{keys?,group?}`). Always mounted, $0; **landed inert — no store consumer yet** (UI.2–7 wire them). |

Shared/other: `GET /healthz` (public), `GET/cost/today`, `GET /cost/daily`, `POST /grok/ask`, `POST /mcp` (JSON-RPC; GET/DELETE→405).

## 4. Database — 35 tables

`src/db/shared-schema.ts`: **cost_events** (platform-tagged spend ledger) · **app_settings** (UI.1 — settings overrides; key PK, JSON value, `updatedAt`; only overridden keys get a row).

`src/x/db/schema.ts` (schema line numbers as of stamp):

| Table | Purpose / notes |
|---|---|
| `tokens` (L14) | Single row `id='default'` OAuth tokens. Excluded from explorer/MCP entirely. |
| `contentPillars` (L33) | Editable pillars; seed INSERT lives in migration 0000. Never let the active set go empty (409 guards). **N0.1** adds nullable `niche` (backfilled `'builder'`, migration 0015). |
| `channels` (L54) | C8 topic rooms: slug PK, keywords JSON, optional pillar link (not FK-validated). **N0.1** adds nullable `niche` (backfilled `'builder'`). |
| `niches` (L70) | **N0.1** — first-class identity container: slug PK, `persona`/`beliefs`/`replyPersona` (prompt-grounding, lifted verbatim from post §1/§5 + reply "Who I am"), `description`, `doctrine` JSON (null=defaults), `active` (exactly one, v1). Seeded `builder` mirroring `DEFAULT_NICHE` byte-for-byte (migration 0015). **INERT until N0.3/N0.4/N0.5** wire consumers. |
| `mediaAssets` (L77) | S4 Studio PNGs as BLOBs (kind, prompt, w/h, usedOnTweetId). `assets.ts` `ASSET_KINDS` whitelist widened (**S5.5**) to all S5 template ids — `quote/stat/banner/pfp/milestone/streak/code/thread/list/chart/background/other`; unknown → `'other'`. |
| `scheduledPosts` (L94) | Calendar. status `draft\|pending\|publishing\|posted\|failed\|cancelled\|segment`; `thread_id`, `position`, `quote_tweet_id`, `pillar`, `register`, `media_note`, `source`. `publishing`/`posted` worker-owned. |
| `postsPublished` (L139) | Everything actually posted (incl. app-made): `in_reply_to_tweet_id`, `conversation_id`, `has_media` (null=unknown, never "no"). |
| `metricsSnapshots` (L164) | Once-only snapshots; `age_at_snapshot_min`; joined newest-first-wins everywhere. |
| `postTemplates` (L189) | C4 extracted winner structures (hook/skeleton/device, lowercased keys). |
| `accountSnapshots` (L205) | Daily getMe KPI, 1/UTC-day, + `pinned_tweet_id` (S0.9). Latest row = "my size" for banding. |
| `voiceAuthors` (L225) / `voiceAuthorSnapshots` (L253) / `voiceTweets` (L268) | Swipe file (DOM-scraped, $0): profile + follower series (append per enrich) + tweets (`scraped_html`, template columns from §8.3, `tags`). |
| `replyDrafts` (L301) | Grok reply drafts: `variants` JSON, `source` (RU.2 — `'reply_master'\|'radar'`, null=legacy), `contextSnapshot` (records exactly what the model saw — signals, relationship, guidance), `postedTweetId` (the outcome join key), `idea`, `pillar`. `updatedAt` of posted flip = paste time. |
| `radarDrafts` (L357) | C0 persisted batch replies; status `ready\|clicked\|expired` ratchet; 48h lazy expiry; `tags`; RU.2 adds `variants` JSON, `model`, `reply_draft_id` (soft link, no FK); **RU.3** `buildRadarDraftRows` stores `r.variants` from the batch (null when a CLI/smoke caller passes only the primary text/angle); **RU.5** `POST …/confirm` writes `reply_draft_id` + ratchets status→`clicked` when a draft is promoted to a `reply_drafts` row; **RU.8** the `band` column now also stores `'manual'` (a ⊕ pinned tweet), coerced away from the confirmed reply snapshot's classifier signals. |
| `people` (L397) / `personEvents` (L428) / `personSnapshots` (L449) | C1 CRM: handle PK + stage + watermarks; append-only timeline (**deterministic ids** `type:ref_table:ref_id`, INSERT OR IGNORE); ambient follower series (C6 hover, once/day gates). **NT.2 (comment-only, no migration):** `type` is free text — its comment now lists `their_like`/`their_repost`/`their_follow`; `source` lists `hover`/`launch`/`notification` (the old list was already stale). |
| `ideas` (L470) | C6 Idea Inbox: status `open\|consumed\|discarded`, consume provenance (`consumed_by_table/-id` restricted to reply_drafts/scheduled_posts). |
| `followupSnoozes` (L496) | item_key `kind:handle` or `reup:<tweetId>`. |
| `streaks` (L509) | Local-day PK diary (C9); upserted by brief reads. |
| `digests` (L522) | Weekly digest cache (week_key PK). |
| `mentions` (L542) | Inbox; **max stored tweet_id IS the since_id checkpoint** — never insert non-API ids; status flow + `answered_draft_id`. |
| `conversationMeta` (L568) | C2 snooze/read/mute per conversation_id. |
| `harvestRuns` (L582) / `harvestRows` (L595) | DOM harvest; repeated captures on purpose (longitudinal series); content-shape columns. |
| `meEntries` (L689) | **ME.1** Me profile: `kind` (fact/event/emotion/note), `text`, `happened_at` (null=undated → created_at drives windows), `pinned`/`active`. No seed (empty = byte-identical prompts). |
| `meGoals` (L711) | **ME.1** Measurable goals: `kind` (followers/mrr/custom), `target`, `unit`, `current_value` (manual for mrr/custom; followers auto ← account_snapshots), `deadline`, `status` (active/achieved/dropped). D4: GR.7 extends this, not a second table. |
| `replyLists` (L757) / `replyListItems` (L777) / `replyListUses` (L809) | **RL.2** Canned reply lists: list (`humanizer` JSON, null = engine defaults, stored NORMALIZED through `parseHumanizerConfig`) → items (template `text`, `enabled`, `source` manual/ai, **`last_used_at`/`use_count` = the server-side anti-repeat state**, FK `list_id` **ON DELETE CASCADE**, index `(list_id, last_used_at)` = exactly what `pickItem`'s recency window reads) → uses (the audit log AND the Playbook `canned` measurement hook via a normalized `rendered_text` match; **deliberately NOT FK'd** so the history outlives an edited-away item or a deleted list; index `(used_at)`). Migration 0018, DDL-only, no seed. |
| `promptOverrides` (L748) | **AI.3** Editable-prompt OVERRIDE ROWS ONLY (key = registry `PromptKey`, body, updatedAt). Row absent = the shipped default in `prompts/registry.ts` applies; restore = DELETE; `customized` = row presence. No seed by design (a later-deploy default improvement applies unless overridden). Explorer/MCP-visible — never store secrets. |

Migrations `0000`–`0018` (latest: `0018_flimsy_captain_universe` RL.2 `reply_lists`+`reply_list_items`+`reply_list_uses` — 3 CREATE TABLEs + 2 indexes, DDL-only, no seed, nothing touched on existing tables; `0013` app_settings, `0014` RU.2 reply `source` + radar `variants`/`model`/`reply_draft_id`, `0015` N0.1 `niches` table + seed INSERT + nullable `niche` on content_pillars/channels + `UPDATE … SET niche='builder'` backfills — the seed/backfill DML was hand-appended after generate, `0016_high_miek` ME.1 `me_entries`+`me_goals` — DDL-only, no seed, `0017_misty_zeigeist` AI.3 `prompt_overrides` — single CREATE TABLE, no seed by design). Workflow: edit schema → `bun run db:generate` → **inspect the SQL** (drizzle drops seed INSERTs; keep them idempotent) → boot/`deploy.sh` migrates. (biome now ignores `src/db/migrations` — UI.1, D11/D13.)

## 5. Extension — `extension/` (own package.json, Vite + React, MV3)

**Build:** two passes (`vite.config.ts`) — content script first as self-contained **IIFE** (`content.js`; classic script, no imports allowed), then side panel + background as ES modules. Shared modules used by content.ts get inlined. Out-of-tree server modules reach the extension via **re-export shims**: `extension/src/replyBand.ts` → `src/shared/replyBand.ts`, `extension/src/channelSuggest.ts` → `src/shared/channelSuggest.ts` (tsconfig lists them in `include`).

**Manifest:** side panel + background service worker + content script on x.com/twitter.com. Permissions: sidePanel, storage, clipboardWrite/Read, contextMenus, alarms, notifications.

| File | Purpose |
|---|---|
| `src/background.ts` | Service worker. **Single writer of all `chrome.storage.session` state** (radar ring buffer cap 100 + dismissed cap 500, launch room state), writes serialized via promise chain. One HTTP transport: `ApiRequest` message channel — the only Authorization-header owner (incl. `binary` for asset blobs). Rankmap cache (10min TTL, S0.3). C7 launch alarms (`chrome.alarms`, +90s grace, liveness verify, notification). Ideas context menu. **RU.6** `stratus/radar-confirm` handler → `confirmDraft(tweetId)` POSTs `/x/radar/drafts/:id/confirm` (best-effort §7.8; ignores `unconfigured`) then `stampDraftId` writes the returned reply_drafts `id` onto the sighting (single writer, markClicked pattern). **RU.7** `stratus/radar-variants-get`→`getVariants` (session buffer `isReplyVariants(hit.variants)` first, else `GET /x/radar/drafts?tweetId=` → newest row's `.drafts[0]`, variants ?? `[{text:replyText, angle}]`) returns `{variants, draftId}|null`; `stratus/radar-variant-pasted`→`pasteVariant` POSTs the idempotent confirm (covers deep links) then PATCHes `/x/replies/:id {status:'posted', replyTextEdited?}` — the confirm response IS the reply_drafts row so its `replyText` is the primary (chosen text ≠ primary → `replyTextEdited`); best-effort §7.8, warn-only. **AX.6** `stratus/open-person`→`chrome.sidePanel.open({tabId: sender.tab?.id})` [best-effort user-gesture, one-hop] + writes the `stratus:openPerson` session handoff key `{handle, at}` via its own `enqueueOpenPerson` chain [single writer]; `stratus/open-person-clear`→removes the key. |
| `src/content.ts` | On-page: reply-band **border/dim** (`readTweetSignals`; **AX.3** the green stats *badge* is gone — border/dim + `recordRadarSighting` byte-identical, radar stream untouched) + **AX.3 person chips** (`applyPersonChips(article, glance)` from `scan` decorates the `[data-testid="User-Name"]` line via `buildPersonChips` over a `GET /people/glance` `GLANCE_TTL_MS` module cache [channels pattern; sync `getGlanceMap`→bg `refreshGlanceMap`, `unconfigured` silent], recompute-per-scan `data-handle`+`data-sig` skip-guard, chip `<button>`s carry `data-tone`/`data-handle`, **AX.6** click → `sendOpenPerson(handle)` [preventDefault+stopPropagation so X never navigates]; 6 tone CSS classes theme-neutral rgba) + **AX.5 tweet-page context panel** (`syncContextPanel(focusedId)` from `scan`: on a `/status/` page finds the focused article [same tweetId guard as `attachReplyMasterButton`], `getDossier`/`refreshDossier` `GET /people/:handle` module cache [`DOSSIER_TTL_MS` 5min, 3 cached terminal states `ready|missing|unavailable` so no fetch loop; `.finally`→`scheduleScan()` since a status page can be mutation-quiet], renders `buildTweetContextModel` [AX.4] into `div.stratus-context-panel` inserted as the article's **next sibling** [`insertAdjacentElement('afterend')`, not cloned on recycle], `data-renderKey` [state:tweetId:handle:cachedAt] rebuild guard + SPA-nav teardown; header stage chip reuses `.stratus-person-chip[data-tone]`, collapse toggle `▾`/`▸` persisted to `chrome.storage.local['augment:contextCollapsed']` [`initContextCollapsed`], 404→one muted line, unconfigured→nothing; **AX.6** header `title[data-handle]` click → `sendOpenPerson(handle)` [dossier click-through; toggle is a sibling not a child so a collapse click never opens]; defensive `#reply-master-btn{display:none!important}` kill rule [the RETIRED `~/newme/clipx/reply-master` extension's purple sparkle — real fix is uninstalling it]) + **RU.7 on-page variant chips** (`syncVariantChips(focusedId)` from `scan`: for the focused article, ask the background `stratus/radar-variants-get` once/tweetId [`variantCache` Map + `variantFetchInFlight` guard, re-scan on resolve], validate with `isReplyVariants`; when present inject `div.stratus-variant-chips` via `actionRow.appendChild` [own `variantChipsHandled` WeakSet — the `attachReplyMasterButton` discipline: dies with the recycled action row, no orphan, no collision with AX.5's article-sibling panel]; each chip a `<button>` [angle label + `variantChipPreview(text)`, `title`=full text]; click → `clearReplyEditor` [selectAll+delete only if non-empty] + `typeTextInto(findReplyEditor())` [clipboard fallback + hint], mark `data-active`, then `stratus/radar-variant-pasted {tweetId, text}` — posting stays manual §7.28), save-to-stratus + save-author scrape, radar sightings stream (2s flush/60s throttle), **RU.8 ⊕ manual add** (`attachRadarAddButton` from `scan` on every action row — `radarAddHandled` WeakSet + `attachButton` discipline; click → `onRadarAddClick` builds a `band:'manual'` sighting [`readTweetSignals` or `synthManualSignals` zeros + timestamp-derived ageMin], flips ✓ optimistically, sends `stratus/radar-report` **immediately** [one deliberate click, not batched] — the background stays the single buffer writer §7.24), C6 passive hover capture, C7 early-replies stream + launch-get throttle, post-save channel chips, `metrics_unparsed` loud error. |
| `src/harvester.ts` | Timeline harvester (human-like scroll, aria metric parse, CSV + ingest rows, since-last cursors) — driven over a port from the Harvest tab; imported by content.ts. |
| `src/shared/` | `messages.ts` (all message types: `stratus/api`, radar-\*, launch-\*; **RU.4** `RadarReplies.replies[]` carries optional `variants` alongside the primary `reply`; **RU.6** `RadarConfirm {type:'stratus/radar-confirm', tweetId}` + `isRadarConfirm` guard — bun-tested in `messages.test.ts`; **AX.6** `OpenPerson {type:'stratus/open-person', handle}` + `OpenPersonClear` + `isOpenPerson`/`isOpenPersonClear` guards [chip/panel click-through, background-written session handoff]; **RU.7** `RadarVariantsGet {tweetId}` [resp `{variants, draftId}|null`] + `RadarVariantPasted {tweetId, text}` + guards — bun-tested in `messages.test.ts`), `types.ts` (`PostContext`, `RadarSighting`…; **RU.4** `BatchReplyItem.variants: ReplyVariant[]` — the server batch reply always returns all 3 angles), `radar.ts` (merge/cap/rank + `stampTiers`; **RU.4** `RadarSighting.variants?`/`RadarDraftRow.variants` — 3 angle variants survive re-sighting merge like `reply` and rehydrate from the server row; `reply` stays the primary = variants[0].text; **RU.6** `RadarSighting.draftId?` — the confirmed reply_drafts id, survives re-sighting merge like `reply`; **RU.8** `RadarBand` widened to `'hot'|'warm'|'manual'` — a ⊕ pinned tweet: `mergeSightings` never downgrades an existing manual band on a hot/warm re-sight, eviction keeps manual rows over fresher auto-captures, `rankSightings` puts manual first [above roster tier], `isRadarSightings` accepts it; `Radar.tsx` renders a `.radar-band-manual` chip + a "manually added" whyLine for zero-signal pins; `BatchReplyTweet.band` [`shared/types.ts`] widened too so `draftReplies` forwards it), `sightings.ts`, `launch.ts`, `earlyReplies.ts` (happy-dom fixture-tested), **`notifications.ts`** (NT.1 — the `/notifications` cell parser, PURE + INERT until NT.5: `parseNotificationCell(article) → {kind:'like'\|'repost'\|'follow'\|'other', handles[], targetText}|null` [null = not an `article[data-testid="notification"]`]; kind is **icon-first** off the first `svg path` `d` against exported `LIKE_/REPOST_/FOLLOW_ICON_PREFIXES` [locale-proof; **prefixes NOT yet verified against live X — NT.5's browser walk confirms them**, an unrecognised glyph degrades to keywords, never to `'other'`], then en+ro verb stems over the first `[dir]` header block, then the whole cell text, checked **like → repost → follow** so the loose `urmăre` stem is only reachable when the like/repost phrases failed; handles = `[data-testid^="UserAvatar-Container-"]` suffixes regex-validated, lowercased, `unknown` placeholder skipped, deduped in DOM order; `targetText` = longest `[dir="auto"]` block containing no verb stem, trailing `…`/`...` stripped, and **forced null for `follow`** [a follow cell's prose is the follower's bio, which must never reach the server's target resolution]; dependency-free for the content IIFE; 12 bun tests), `metricsAria.ts` (locale-hardened), `harvest.ts`, `bgClient.ts`, **`aiSettings.ts`** (AI.10 — pure `aiSettingsToForm`/`aiFormToPatch` [form fields are strings so blank is representable → explicit null; client-side `invalid_temperature`/`invalid_max_output_tokens` mirror the server codes] + `pricePerMillion` [per-token USD → $/1M label], bun-tested), **`glance.ts`** (AX.2 — `GlanceEntry`/`GlanceMap` typed after `GET /people/glance`, `NEGLECT_DAYS=7`, `GLANCE_TTL_MS=10min`, pure `buildPersonChips(entry, nowMs) → PersonChip[]` [`{kind:'stage'\|'target'\|'owed'\|'neglected', label, tooltip, tone}`]: stage chip only ≥engaged [hardcoded 6-el STAGE_ORDER, no server stage.ts import], `◎` target, `↩ n` owed when openLoops>0, `Nd` neglected when [isTarget or ally/mutual] and lastOutbound `< now−7d` [strict `<`, mirrors followups cutoff — exactly 7d NOT neglected] or null-outbound+inbound-present; order stage/target/owed/neglected; dependency-free for the content IIFE; bun-tested; **AX.3** consumed by content.ts `applyPersonChips`), **`tweetContext.ts`** (AX.4 — the status-page context-panel view-model. Consumes the C1 dossier `GET /people/:handle` as HTTP JSON [all timestamps ISO strings]; pure `buildTweetContextModel(dossier, tweetId, nowMs) → {header, relationship, alreadyReplied, openLoops, outcomes, anglePreference, notes}`: header stage/sinceDays[firstSeenAt]/followers/`momentumPerDay`[followerSeries clamp ≥1 day, mirrors `authorMomentum`, null <2 pts]/tags; relationship inbound/outbound COUNTS from `events` [their_mention+their_reply_to_me vs my_reply], last dates from the `people` watermarks; `alreadyReplied` = a posted outcome whose `sourceTweetId===tweetId` [ageMin off `postedAt ?? draftCreatedAt`]; openLoops = unanswered `mentions` oldest-first; outcomes = measured-only reply outcomes newest-first ≤3 [angle optional — the dossier does NOT emit per-outcome angle today, reads null]; `anglePreference` mirrors `pickAnglePreference` [gate total-measured≥3=`MIN_MEASURED_FOR_ANGLE_PREFERENCE`, best median profileVisits, views tie-break]; notes trimmed. Null-safe for hover-only people; dependency-free for the content IIFE; 15 bun tests. **AX.5** consumed LIVE by content.ts `syncContextPanel`/`renderContextPanel`), **`variantChips.ts`** (RU.7 — pure `variantChipPreview(text, max=60)` [collapse whitespace → clip with `…`, never exceeds max] + `isReplyVariants(v)` [shape guard: NON-empty array of `{text, angle}`, angle a loose string — looser-client-cache tolerance]; the two testable bits of the on-page chips, since content.ts is untested by convention; consumed by content.ts `syncVariantChips` + background `getVariants`; 7 bun tests). |
| `src/sidepanel/api.ts` | Typed client — every call routes through background ApiRequest. **UI.10:** `api.settings.{get,patch,reset}` over `/x/settings`; the `SettingEntry`/`SettingsGroup`/`SettingsResponse` types live in `shared/types.ts` (the panel renders from the GET, never the server registry). **N.7:** `api.niche.{get,list,create,update,remove}` over `/x/niche`+`/x/niches`; `Niche`/`NicheActive`/`NicheDoctrine`/`NicheCreateBody`/`NichePatchBody` in `shared/types.ts`. **N.8:** `api.niche.draft(description)` over `POST /x/niche/draft`; `NicheProposal`/`NichePillarProposal`/`NicheChannelProposal`/`NicheDraftResult` in `shared/types.ts`. **ME.4:** `api.me.{get(opts?),context(mode),addEntry,patchEntry,deleteEntry,addGoal,patchGoal,deleteGoal}` over `/x/me*`; `MeEntry`(+`inWindow`)/`MeGoal`(+`progress`)/`MeGoalProgress`/`MeResponse`/`MeContextResponse`/`MeEntryCreateBody`/`MeEntryPatchBody`/`MeGoalCreateBody`/`MeGoalPatchBody`/`MeKind`/`GoalKind`/`GoalStatus` in `shared/types.ts` (Date columns are ISO strings over the JSON transport; create/patch return the raw row w/o the computed field — the tab reloads, never reads it off a mutation). **AI.7:** `api.drafts.thread(body)` over `POST /x/posts/draft-thread`; `ThreadDraftBody`/`ThreadDraftResponse` in `shared/types.ts`. **AI.8:** `api.drafts.rewrite(body)` over `POST /x/posts/rewrite`; `RewriteBody`/`RewriteVariant`/`RewriteKind`/`RewriteResponse` in `shared/types.ts`. **AI.9:** `api.ideas.generate(body)` over `POST /x/ideas/generate`; `IdeaGenerateBody`/`IdeaProposal`/`IdeaGenerateResponse`/`IdeaAngle` in `shared/types.ts`. **AI.10:** `api.llm.{getSettings,patchSettings,models}` over `/llm/settings`+`/llm/models` and `api.prompts.restoreDefaults()` over `POST /x/prompts/restore-defaults`; `AiSettings`/`AiSettingsResponse`/`AiSettingsPatchBody`/`LlmModel`/`LlmModelsResponse`/`LlmProvider`/`LlmReasoningEffort`/`PromptsRestoreResult` in `shared/types.ts`. **AI.11:** `api.prompts.{list,get,patch,reset}` over `/x/prompts`+`/x/prompts/:key`(+`/reset`); `PromptSummary`/`PromptDetail`/`PromptPatchResult`/`PromptResetResult` in `shared/types.ts`; also re-exported the previously-missing `RewriteVariant` (AI.8 gap, D54). **RL.5:** `api.replyLists.{list,get,create,patch,remove,setItems,patchItem,removeItem,use,generate}` over `/x/reply-lists*` (`setItems` unwraps `{items}` — the route answers with the list's WHOLE current set for both modes, so the panel renders the response directly); `HumanizerConfig`/`ReplyList`/`ReplyListSummary`(+counts)/`ReplyListItem`/`ReplyListDetail`/`ReplyListCreateBody`/`ReplyListPatchBody`(`humanizer: cfg \| {} \| null`)/`ReplyListItemsBody`/`ReplyListItemPatchBody`/`ReplyListItemSource`/`ReplyTemplateVar`/`UseReplyBody`/`UseReplyResponse`/`GenerateItemsBody`/`GenerateItemsResponse` in `shared/types.ts`. |
| `src/sidepanel/App.tsx` | Tabs: today, people, **me** (ME.4), channels, calendar, composer, studio, harvest, voice, replies, ideas, playbook, settings. **UI.10:** the 104px rail is grouped by eyebrow dividers (`TAB_GROUPS` — Operate/Author/Library/Learn/System) with an `icons/icon128.png` brandmark above the lowercase wordmark; not-configured branch renders `<EmptyState>`. **ME.4:** the `me` tab sits in the **Operate** group right after People (`<MePanel settings={settings} />`). Cross-tab handoffs: `onOpenPerson`, `openStudio`, remix→composer. **AX.6:** a `useEffect` reads the `stratus:openPerson` session key (mount + `chrome.storage.onChanged` session area), routes via `setPersonHandle`+`setTab('people')`, then fires `stratus/open-person-clear` so a stale handle can't replay — empty deps (only stable setters referenced). |
| `src/sidepanel/ui/` | **UI.10** thin token-only primitives (className + tokens, no state libs): `Section` (eyebrow + body), `EmptyState` (coach line + hint + action), `SubTabs` (segmented pill — Voice migrates in UI.14), `SettingRow` (typed control per `SettingEntry`: number→`Slider` when bounded else number input, boolean/enum/string/numberArray + reset dot when `!isDefault`), `Slider` (bounded range + tabular readout), `GearPopover` (`⚙` → outside-click hairline card of SettingRows; `settings[]` + `onPatch`). **D7: every NEW Wave-1+ surface builds on these.** CSS: the `.ui-*` block at the tail of styles.css. |
| `src/sidepanel/settingsClient.ts` | **UI.10** thin wrapper over `api.settings` (`loadSettingGroups`/`flattenSettings`/`patchSetting`/`resetGroup`/`resetKeys`) — where a GearPopover's `onPatch` lands its PATCH. Inert until UI.11 + the Wave-5 inline gears wire it. |
| Tab components | `Today.tsx` (hosts: Conversations, DoNext, Fans, LaunchRoom, Digest, PinnedWatchCard, quests/streak, Radar, Targets, TodayPlan), `People.tsx` (+dossier, Icebreakers), `Channels.tsx`+`ChannelTags.tsx` (shared chip picker, 60s cache), `QuickReplyPicker.tsx` (**RL.6** — the shared "canned ▾" popover mounted on every Launch Room early-replier row and every Conversations open loop; module-level 60 s list cache + in-flight guard [ChannelTags shape], `invalidateReplyListsCache()` called from `ReplyLists.tsx`'s unmount so the one panel that mutates lists covers every edit with one line; one click = `api.replyLists.use` then `navigator.clipboard.writeText` **in the same handler** — it NEVER picks locally and never sends `preview:true` [Decision 1: the anti-repeat state is server-owned]; the popover keeps the returned text on screen so a clipboard refusal is still recoverable by hand; only `active` lists are offered [D79e], `enabledCount:0` rows render disabled, 409 `no_enabled_items`/404 surface inline; renders `<></>` when no active list exists), `Calendar.tsx`, `Composer.tsx`+`composerLogic.ts` (best-time suggest, thread mode, jittered minutes; **S5.7** "Make visual" in thread mode emits `{text: seg[0], postId?, template:'thread'}`; **AI.7** the drafter section's button branches on `threadMode` → "Draft thread with AI" calls `api.drafts.thread` [reusing the pillar+idea inputs] then `onEdit(head.id)` to load the thread for editing; **AI.8** an "Improve with AI" button under the single-post textarea [`!threadMode && !isThreadEdit && !isLocked && text.trim()!==''`] → `api.drafts.rewrite` → variant cards reusing `.draft-card` → `applyRewrite` sets the textarea), `Studio.tsx` (**S5.2:** a shell over `sidepanel/studio/` — see below; ~880 lines after S5.3–S5.8 added mascot/pattern/celebration/thread+list/chart wiring; **S5.7** `StudioSeed.template?: 'quote'|'thread'` routes the seed effect — thread seeds the thread-cover hook; **S5.8** chart mode/dataset state), `Harvest.tsx`+`harvestClient.ts`, `Voice.tsx`+`Pillars.tsx` subtab, `Replies.tsx`+`ReplyLists.tsx` subtab (**RL.5** — `Replies.tsx` holds a `'drafts'\|'lists'` view over the **`SubTabs` primitive** [D7; `radar-tabs` is the older Voice/Settings idiom] and short-circuits to `<ReplyListsPanel>` on `'lists'`; `ReplyLists.tsx` = list rail → `ListSettings`/`ItemsEditor`+`ItemRow`/`HumanizerEditor`/`TestRender`/`GenerateBox`, Pillars.tsx busy-notice-error trio + confirm-before-delete, reusing `.pillar-card*` classes + a small `.rl-*` block. Items are added **one reply per line** [client-side caps mirror the route: 100/call, 280 chars]; the humanizer form seeds itself by PATCHing `{humanizer:{}}` — **no client copy of `DEFAULT_HUMANIZER`** — and pools are one-per-line **not** comma-separated [the defaults contain commas]; Test render always sends `preview:true`; the AI proposal is component state only until Append/Overwrite calls `setItems` `source:'ai'`), `Ideas.tsx` (**AI.9** a "Generate ideas with AI" section — optional steer textarea + button → `api.ideas.generate` → proposal cards [pillar+angle badges] each with Save-to-inbox [`api.ideas.create` `tags:['ai']`, removes on success + reloads] / Skip; `savingText`-keyed disable, reference-filter removal), `Me.tsx` (**ME.4** the Me tab — `MePanel`; `GoalsSection`/`GoalCard`/`AddGoal`, `QuickLog`, `EntriesSection`/`EntryRow`, `AiPreview`; loads `api.me.get` + both `api.me.context` modes in one `Promise.all`, reloads after every mutation so the preview stays in sync; `inWindow` drives the "not injected anymore" dashed dimming — never re-derived; no shared pure module → no new tests, manual check over the loaded extension), `Playbook.tsx` (**ME.5** a "Personal context" section — with-me-brief vs cold reply medians + gated lift, silent-until-gated; `Playbook.meEffectiveness` + `DigestFacts.goals?` types in `shared/types.ts`), `Settings.tsx` (**AI.10** a `radar-tabs` **`General | AI | Prompts`** subtab shell [Voice.tsx pattern]: General = `<NicheCard/>` + the existing form [passiveCapture, applyPillarsToReplies, harvest cursors, **UI.9 Appearance: theme/density/text-size selects, patch-on-change**; **N.7** `NicheCard` at top]; AI = `<AiPanel>` [loads `GET /llm/settings`; `currentSettings` `useMemo`'d over the 8 primitives so the effect doesn't refire; provider radio w/ OpenRouter disabled+hint when `providers.openrouter` false; free-text model input + `<datalist>` from `GET /llm/models` fetched only when openrouter-configured; blank temp/tokens = surface-default null; Save → `api.llm.patchSettings`] + `<RestoreDefaultPrompts>` [confirm() → `api.prompts.restoreDefaults`] in the footer; Prompts = `<PromptsTab>` → **AI.11** `<PromptsPanel>` (Prompts.tsx) + `<RestoreDefaultPrompts>` footer), `Prompts.tsx` (**AI.11** the per-prompt editor — list [all registry prompts via `api.prompts.list` — 11 since RL.4; the panel never hardcodes the set, `customized` amber chip] → editor [`api.prompts.get`, monospace textarea, required-placeholder chips green/red via inline `missingPlaceholders` mirroring `validatePromptBody`, Save `api.prompts.patch` disabled while a required token is missing, "Reset this prompt" `api.prompts.reset`, "Show default" read-only diff]; Pillars.tsx edit-list pattern), `Niche.tsx` (**N.7** the Settings Niche card — active-niche editor: label/description/persona/beliefs/replyPersona textareas + 5 doctrine number inputs, dirty-state Save/Reset [doctrine sent whole per D27c]; niche list w/ Activate/Delete [delete disabled on active]; "+ New niche" create-inactive form incl. required persona/beliefs/replyPersona [D33]; **N.8** the `NicheWizard` (paste description → `api.niche.draft` → editable proposal preview [slug/label/description/persona/beliefs/replyPersona + 3 pillars + channels] → Create = niche.create → **activate FIRST** → pillars.create × N → channels.create × N → "switch back to {prev}"); reuses `.pillar-*` CSS). |
| `src/studio/` | S3: `compose.ts` (layer model → canvas → PNG; layoutText wrap/shrink/ellipsize; S5.1 added `path`/`panel`/`pattern` layer kinds + exported `mulberry32` seeded PRNG + pure `patternCoords` — never `Math.random`), `templates.ts` (quote/stat/banner/pfp + S4 background+scrim under text; **S5.3** appends `mascotLayers` to quote (happy, bottom-left)/stat (celebrating if `delta>0` else happy, under sparkline)/banner (thinking, only no-milestone) — all guarded `if (kit.mascot && !data.background …)` so `mascot:false` is byte-identical; **S5.4** `baseLayers` takes an optional `pattern` arg → a `[gradient fill, pattern]` base (ink `withAlpha(contrastOn(kit.bg),0.07)`, full-card box); an AI bitmap always wins over a pattern; `QuoteCardData`/`BannerData` carry `patternKind?`/`patternSeed?`; **S5.5** `milestoneCardSpec`/`streakCardSpec` (both 1200×675) — giant `fmtCount(milestone)`/`${days}` + fixed-seed `blobs` confetti (`withAlpha(kit.accent,0.25)`, seed 11) + celebrating mascot only when a value is present; graceful null-data placeholders; **S5.6** `codeCardSpec({code,title})` 1200×675 — terminal window: desktop `fill shade(kit.bg,-0.5)` + `panel` (radius 20, shadow) screen, 3 monochrome `ring` discs, centered filename, line numbers + tokens laid out by FIXED monospace advance (`STUDIO_MONO_STACK`, `CODE_CARD`) — no canvas measurement; token colors kit-derived (keyword=accent, string/number=shade(accent), comment=muted ink); caps 18 lines/62 cols, over-cap → `⌄ trimmed` footer); **S5.7** `threadCoverSpec({hook,count})` (quote-card skeleton, heavier: ExtraBold 72px hook shrink-floor 36, `['a thread','1/N']` badge, thinking mascot bottom-right, `THREAD_COVER`) + `listCardSpec({title,items})` (accent rule + title + ≤6 rows, each an accent disc `ring` width=2r + centered digit `contrastOn(accent)` + item text maxLines 2; empty → "add one item per line" placeholder; `LIST_CARD`) + pure `parseListItems(raw)→string[]` (trim, drop blanks, strip leading `\d+[.)]`/`-*•` markers, cap 6) — both background-capable via `baseLayers` (carry `background?`/`patternKind?`/`patternSeed?`), thread mascot guarded `!data.background`; **S5.8** `chartCardSpec({mode,points,firstLabel,lastLabel,delta,cells})`/`CHART_CARD` — growth: accent eyebrow + big `fmtCount(last)` + delta badge + big `sparkline` (1040×200) + peak/low + endpoint dates (<2 points → placeholder); heatmap: 7×24 `rule` grid coloured `withAlpha(accent,0.15+0.75·intensity)`, below-gate cells the `withAlpha(ink,0.06)` wash, weekday initials + 0/6/12/18h markers; NOT bg-capable, all normalization in chartData.ts), `chartData.ts` (**S5.8** pure `growthSeries(rows)→{points,firstLabel,lastLabel,delta}` (raw follower points for the sparkline) + `heatmapCells(cells,minN=3)→ChartCell[]` full 7×24 grid, intensity = min→max over the sufficient set (flat→0.5), cells below `minN` or with no value → `sufficient:false`/`intensity:null`; `HEATMAP_MIN_N=3`), `codeTokens.ts` (**S5.6** pure `tokenizeLine(line)→Token[]` — deliberately-loose 5-kind classifier `plain|keyword|string|number|comment`, small cross-lang keyword set; exports `MONO_ADVANCE=0.6` = JetBrains Mono's exact 600/1000 em-advance), `milestones.ts` (**S5.5** `MILESTONES` ladder `[50…100000]` + pure `latestCrossed(series)→{milestone,crossedOn}|null` — peak-based, unordered-tolerant, `>=` boundary; client-side detection over the account series, §7.12), `mascot.ts` (**S5.3** pure `mascotLayers({pose,x,y,scale,kit,seed?})→Layer[]`; poses `happy|celebrating|thinking|sleeping`; one 100×100 viewbox of `path` layers + `text` "zzz"; colours all `shade(kit.accent,…)` — re-skins with the brand; confetti seeded), `brandKit.ts` (**S5.3** `mascot:boolean` field, default true, lenient-parse fallback true; **S5.4** multi-preset: `BrandKits{active,kits}` in `studio:brandKits`, `loadBrandKits` migrates legacy `studio:brandKit`→`kits.default` else seeds `STARTER_KITS` (Midnight/Paper/Neon), keeps writing the legacy key for rollback; pure `parseBrandKitsFile` (accepts both file shapes) + `activeKit`/`patchActiveKit`/`setActivePreset`/`savePresetAs`/`renamePreset`/`canDeletePreset`+`deletePreset` (last-preset delete refused)), `fonts.ts` (**S5.6** bundled Inter + JetBrains Mono WOFF2s — Inter→`StudioInter`, Mono→`StudioMono`, per-face family, non-fatal load). |
| `src/sidepanel/studio/` | **S5.2** the Studio registry split (behavior-neutral). `registry.ts` — `TemplateId` (**S5.5** +`milestone`/`streak`; **S5.6** +`code`; **S5.7** +`thread`/`list`, both `supportsAiBackground:true`; **S5.8** +`chart`, `supportsAiBackground:false`), `TEMPLATES` metadata (`id`/`label`/`size {w,h}`/`supportsAiBackground`), `templateMeta`/`supportsAiBackground` helpers, `EMPTY_STAT`/**S5.5** `EMPTY_MILESTONE`/`EMPTY_STREAK`/**S5.6** `DEFAULT_CODE`/**S5.7** `DEFAULT_THREAD_HOOK`/`DEFAULT_LIST_TITLE`/`DEFAULT_LIST_ITEMS`, `TemplateState` (**S5.4** `patternKind: PatternKind|null`+`patternSeed`; **S5.5** `milestoneData`/`streakData` — shell resolves override-over-auto; **S5.6** `codeTitle`/`codeText`; **S5.7** `threadHook`/`threadCount`/`listTitle`/`listItems` — `list` case runs `parseListItems` in `buildSpec`; **S5.8** `chartMode`+`chartPoints`/`chartFirstLabel`/`chartLastLabel`/`chartDelta`/`chartCells`), and the pure `buildSpec(id, state, kit) → RenderSpec` dispatch (replaces the old render ternary + `BG_TEMPLATES` set). `fields.tsx` — dumb props-driven field sections (`QuoteFields`/`StatFields`/`BannerFields`/`PfpFields`; **S5.5** `MilestoneFields`/`StreakFields` — a status line + manual number override; **S5.6** `CodeFields` — filename input + code textarea; **S5.7** `ThreadFields` hook+length / `ListFields` title+one-per-line textarea; **S5.8** `ChartFields` — growth/heatmap mode toggle + reload) + the presentational `BackgroundFields`/`LibraryRail` (state+handlers stay in the shell). **S5.5** `Studio.tsx` loads `api.metrics.account()`→`latestCrossed` (milestone) and `brief.quests.streak` (streak), override beats auto, `milestoneData`/`streakData` memoized so identity doesn't churn the render effect. **S5.8** `Studio.tsx` lazy-loads both chart datasets on first `chart` view (`api.metrics.account`→`growthSeries`, `api.metrics.bestTimes`→`heatmapCells(cells,minN)`), $0 reads. `KitEditor.tsx` — the brand-kit section (colors/handle/watermark/**S5.3 mascot toggle**/style-suffix) + **S5.4 preset dropdown / Save-as / Rename / Delete** over the `BrandKits` bundle; export/import round-trips both file shapes via `parseBrandKitsFile`. The **S5.4 background segmented control** (`gradient|dots|grid|diagonal|plus|blobs|AI image` + blobs reroll; picking a pattern clears the AI bitmap) lives inline in `Studio.tsx`, not fields.tsx. **Adding a template = 1 registry row + 1 field section**, not another shell branch. |
| `src/sidepanel/styles.css` | The panel stylesheet (~2.4k lines). **UI.8:** `:root` holds the full `--strat-*` design-token set (verbatim from `Stratus Design System/tokens/*.css`) + `--x-*` companion tokens + **legacy short aliases** (`--bg: var(--strat-bg)` …) the sheet still resolves through. **Zero color literals outside `:root`** — tinted fills use exact fill tokens (`--strat-{accent,danger,warn}-fill`, `--strat-pillar-bg`, `--strat-scrim`) or `color-mix(in srgb, var(--strat-*) N%, transparent)` (exact-equivalent, and tracks the base so Task-9 light theme flows through). Inter is `@font-face`d from `/fonts/` and set on `body` via `--strat-font-sans`; metric classes get `tabular-nums`. **UI.9:** `:root[data-theme='light']` re-tints by redefining only the base `--strat-*` tokens (aliases resolve lazily → whole sheet flows through; `color-scheme: light`; direct-use fill tokens + band ink re-authored for a light base, dark text companions kept); density via `[data-density='compact']` and scale via `[data-scale='12'|'14']` override the new `--ui-panel-pad`/`--ui-section-gap`/`--ui-row-gap`/`--ui-root-size` vars that `body`/`.panel`/`.row` now consume. **UI.10:** `.brand`/`.brand-mark`/`.brand-word` + `.tab-group`/`.tab-group-eyebrow` (grouped rail), and a tail `.ui-*` block for the primitives (Section/EmptyState/SubTabs/SettingRow/Slider/GearPopover) — `--strat-*` tokens only, no literals. **RL.5:** a tail `.rl-*` block (rail/detail/items/chances/testvars/preview/proposals) — same rule, `color-mix` over `--strat-accent` for the selected rail chip; everything else leans on `.pillar-card*`/`.field`/`.row`/`.status-line`. **RL.6:** a tail `.quick-reply*` block (relative wrapper + absolutely-positioned `right:0` popover card, list rows, result/status/meta/err) — `--strat-*` tokens only, no literals; the status line flips to `--strat-warn` via `.quick-reply-status-warn` when the clipboard refused. |
| `public/fonts/` | Bundled Inter WOFF2s (Regular/Bold/ExtraBold) + **S5.6** JetBrains Mono WOFF2s (Regular/Bold, OFL-1.1, latin subset) — served at `/fonts/`, loaded by the Studio (`studio/fonts.ts`, FontFace: Inter→`StudioInter`, Mono→`StudioMono`) and the panel sheet (`@font-face`, UI.8). |
| Storage keys | `chrome.storage.local`: brand kit, settings toggles, harvest cursors, `replyMaster:idea(+Id)`, **AX.5 `augment:contextCollapsed`** (tweet-page context-panel collapse state), **UI.9 appearance `theme`/`density`/`uiScale`** (stamped on `<html>` by `main.tsx` as `data-theme`/`data-density`/`data-scale`; `system` resolved via `matchMedia`). `chrome.storage.session`: `radar:sightings`, `radar:dismissed`, `launch:active`, `launch:replies`, **AX.6 `stratus:openPerson`** (`{handle, at}` dossier-open handoff — background-written single writer, App.tsx consumes on mount + `onChanged` then clears via `stratus/open-person-clear`). |

## 6. MCP + integrations

- `POST /mcp` — 19 X tools (see §3.3 `x/mcp.ts`). Client setup: `claude mcp add --transport http stratus https://<host>/mcp --header "Authorization: Bearer $STRATUS_TOKEN"`.
- `.claude/skills/stratus` — drives the HTTP API from Claude Code (scheduling scripts `md_to_schedule.ts` with the cadence ladders 3/day `[9,13,18]`, 4/day `[8,12,16,20]`; always jitter minutes).

## 7. Patterns & conventions (name these in plans; point implementers at the exemplar file)

**Money/API discipline**
1. **One place to call X / Grok / images**: `xFetch` / `askGrok` / `generateImages`. Never raw fetch.
2. **Retire-before-snapshot** (dailyMetrics): a billed read must be unrepeatable — take the row out of the candidate set in a committed txn BEFORE the read/write that follows. At-most-once, never double-charge.
3. **Claim-before-call** (publisher, winner re-read): commit the state flip before the irreversible external call.
4. **Refuse-before-spend**: validation → band gate → grounding checks → THEN the paid call (replies.ts, icebreakers.ts, images.ts hard budget).
5. **`max_results` clamps** on every paginate wrapper (endpoints.ts pattern).
6. **costHint** when the URL can't price itself; `console.warn` on $0-priced billed calls.

**Data discipline**
7. **Deterministic event ids** `type:ref_table:ref_id` + INSERT OR IGNORE — backfill, live hooks, and DOM ingest share one idempotent id space (people/store.ts, launch.ts). **Bounded-re-log variant (sightings.ts, NT.2 engagements.ts):** when no ref resolves, bucket the id by UTC day (`…:<YYYY-MM-DD>`) instead of falling back to a random uuid — a re-scroll then collapses into one event/day/handle/kind rather than flooding the timeline. A once-ever fact (a follow) drops the date too.
8. **Best-effort side writes**: `safeLogPersonEvents`, `persistRadarDrafts`, `consumeIdeaSafe` — a hook failure never fails the paying path.
9. **Fill-only vs overwrite upserts**: hover/launch glimpses fill-only; profile scrapes overwrite (people/store.ts).
10. **Status ratchets**: stages only auto-promote; radar drafts ready→clicked→expired; mentions never regress implicitly.
11. **null = unknown, never "no"** (`has_media`, `pinned_tweet_id`); bucket null separately in aggregations.
12. **No derived-state tables** when a read-time regroup works (conversations C2, momentum C5).
13. **Sync SQLite**: txns take sync callbacks (`.all()/.get()/.run()`, no await inside); never bind a JS `Date` in raw `sql` (use `.getTime()`); token refresh critical section = in-process mutex.
13a. **Millisecond ties break ordering logic**: writes are sub-millisecond here, so any column an ordering/recency rule reads (`created_at` insertion order, `last_used_at` anti-repeat) must be stamped **monotonically past the row set's current max**, not from `Date.now()` alone. Exemplars both in `replyLists.ts`: items `createdAt` (RL.2), `/use` `lastUsedAt` (RL.3 — measured: 241/500 uses shared a millisecond and 4 immediate repeats slipped through without it).

**Prompt discipline**
14. **Byte-sync prompts**: `.md` ↔ TS literal asserted by test; regenerate the literal when editing the .md.
15. **Variable tail**: ALL per-call content ({{TWEET_CONTEXT}}, {{PILLARS}}, relationship, guidance, idea) after the stable cacheable instruction prefix; `prompt_cache_key` set.
16. **Server-stamped fields**: `relationship`/`guidance`/gate signals are stamped server-side, never parsed from clients; persist exactly what the model saw in `contextSnapshot`.
17. **Structured outputs** via `text.format` json_schema on `/v1/responses` (`response_format` 400s there).
18. **No fabrication**: Grok may narrate only supplied FACTS; grounding blocks travel back in responses.
- **Niche persona substitution (N.3/N.4)**: `{{PERSONA}}`/`{{BELIEFS}}` (post) and `{{REPLY_PERSONA}}` (reply, single + batch) substitute IN PLACE (deliberate §7.15 deviation — constant per niche ⇒ prefix byte-stable per niche); all niche fields are server-stamped from `loadActiveNicheSafe()` (never client-parsed) and cache keys carry `:slug:updatedAtMs` to bust on edits. The reply routes also stamp `contextSnapshot.niche={slug}` (single path only — batch has no DB row) and load the niche AFTER the band gate. Seed-niche output is bit-for-bit identical to pre-extraction (equivalence tests in `src/test.test.ts` with independent fixtures — never re-read the .md, it holds the tokens now). **N.5 (doctrine):** the 5 REPLY-GUIDE knobs (reply target min/max, week reply %, target band min/max ×) are owned by `niches.doctrine`; consumers call `loadDoctrine()` (never throws, resolves over `DEFAULT_DOCTRINE`) — `targetBand`'s new 2nd arg defaults to `{minX:2,maxX:10}` so untouched call sites/tests stay green (regression), and null doctrine = today's numbers. Doctrine is method-configuration, NOT a stat — no gates touched (§7.19). **N.6 (ownership):** `content_pillars.niche` + `channels.niche` (nullable, backfilled `builder`) make pillars/channels owned by a niche; all list/active reads scope by `ownedByNiche(activeSlug)` = `niche = active.slug OR niche IS NULL` (legacy tolerance — identity until a 2nd niche exists), POSTs stamp the active slug, the channel `:slug` room 404s cross-niche, and `getActivePillars()` returns `[]` for a non-`builder` niche with no pillars → the drafter refuses `409 no_pillars_for_niche` before any Grok spend (zero builder-pillar leakage into another niche's drafts). Last-active pillar 409 is now per-niche. PATCH/DELETE stay slug-addressed (admin can still edit another niche's rows). **AI.3 (prompt registry):** editable prompts live as OVERRIDE ROWS in `prompt_overrides` — row absent = shipped default, restore = DELETE, `customized` is structural row-presence (never a diff). Call sites load `loadPromptSafe(key)` and thread the body into the builders (`buildGrokInput` opts `template?` / `BuildPostDraftOptions.template?`; a per-request `systemPromptOverride` beats the DB override — explicit > DB > code-default); cache keys = registry sha-8 + the niche `:slug:updatedAtMs` suffix (both dimensions vary the rendered prefix). `validatePromptBody` guards edits: `required` placeholders must survive, the niche persona placeholders are `optional` (token-tolerant — an override hardcoding its persona is sanctioned, D29d), unknown tokens warn. A new editable prompt = one `PROMPT_SPECS` entry + a call-site wire (AI.4 adds the editor routes; AI.5/AI.6 migrated the 7 existing prompts, AI.7/AI.8/AI.9 added the three new-surface prompts thread/rewrite/ideas, RL.4 appended `reply-list` — **11 keys**). Registry defaults MUST come from PURE modules — a route-file import cycles (AI.5's `voice/extractPrompt.ts` move exists for exactly that; digest.ts/icebreakers.ts were already pure enough). (Unnumbered — the numbered `§7.N` refs are load-bearing; don't renumber.)

**Stats discipline**
19. **Gates**: playbook cells + all lifts n≥20/side (`DEFAULT_MIN_CELL_N`); angle preference ≥3 measured; best-times top n≥3; BAND recalibration ≥100 and manual-only. Below gate → `sufficient:false`/null, never a confident number.

**Routing/wiring traps**
20. **Static path before `:param`** (followups+fans and rankmap before `/people/:handle`) — check on every new people-adjacent route.
21. **Public-by-design routes** (`/healthz`, `/explorer`) mount at root outside the `/x/*` guard.
22. **LLM-gated mount vs runtime 503** (AI.6 — was Grok-gated): user-facing-always routes mount always + check `llmConfigured()` (EITHER provider) at runtime → 503; pure LLM routes (`replies`/`drafter`/`voiceExtract`) refuse to mount unless `llmConfigured()`. `askLLM` still enforces the RESOLVED provider's key per request (503 `llm_not_configured`) — the mount/pre-flight gate is only "is ANY provider available". `images` stays `XAI_API_KEY`-only (Decision 6). The pre-flight 503 strings on pillars-draft/extract-winners/icebreaker stay `grok_not_configured` (stable extension matchers); only digest's degrade field renamed to `llm_not_configured`.
23. **Worker-owned statuses**: PATCH/DELETE 409 on `publishing`/`posted`.

**Extension**
24. **Background = single session-storage writer**; content/panel talk via messages, never write directly.
25. **One transport**: all HTTP through `ApiRequest`; blobs via `ApiRequest.binary`.
26. **Content script is an IIFE**: no module imports at runtime; shared code gets inlined — heavy deps stay out.
27. **Shims for shared server modules** (replyBand, channelSuggest) — never fork logic between server and page.
28. **Posting is always manual paste** — OAuth 1.0a media wall + reply policy; nothing auto-posts to others, MCP can only create `draft` rows.
- **Design tokens (UI.8)**: the panel's `styles.css` `:root` carries the `--strat-*` token set (from `Stratus Design System/tokens/*.css`) + `--x-*` companion tokens + legacy short aliases. New UI references `--strat-*` directly; tinted fills use the exact fill tokens or `color-mix(in srgb, var(--strat-*) N%, transparent)`; **no color literal lives outside `:root`**. **UI.9** made it theme-aware: `:root[data-theme='light']` redefines only the base `--strat-*` tokens (the whole sheet re-tints via the aliases), `main.tsx` stamps `data-theme`/`data-density`/`data-scale` on `<html>` from the local `theme`/`density`/`uiScale` settings (`system` resolved via `matchMedia` + listener). Appearance is panel-local (chrome.storage), never server-synced; content-script overlays and the Studio canvas are exempt (they follow X / the brand kit). (Unnumbered — the numbered `§7.N` refs are load-bearing across CLAUDE.md/plans; don't renumber.)
- **Settings platform (UI.1)**: server-side tunable knobs live in `app_settings` (override rows) + the typed `src/x/settings/registry.ts` catalog. Discipline: **only routes/workers read the store** via the bound `getSetting`; **pure modules stay pure** and take an opts object defaulted to today's constant (`computeQuests(rows, opts = QUEST_DEFAULTS)`) — so every existing pure test stays valid and the store is mockable. Registry floors/ceilings are the money/policy guard (an MCP agent editing via `x_update_setting` hits the same wall). The shared store (`src/settings/store.ts`) never imports `src/x/*` — the registry is passed in. (Unnumbered — see the `don't renumber` note above.)
- **UI primitives (UI.10, D7 standing)**: every NEW panel surface from Wave 1 on composes `src/sidepanel/ui/` primitives (Section/EmptyState/SubTabs/SettingRow/Slider/GearPopover) + `--strat-*` tokens — no bespoke `<h3>`/muted-`<p>`/hand-rolled controls. Primitives are pure presentation (className + tokens, no state libs, no API calls); the settings PATCH lives in `settingsClient.ts`, and the panel renders knobs from `GET /x/settings` (extension never imports the server registry). Wave-5 polish (UI.12–16) migrates the pre-masterplan tabs onto them. (Unnumbered — see the `don't renumber` note above.)

**Process**
29. **Docs sync in the same commit**: CLAUDE.md phase entry, the relevant plan doc (PLAN/CIRCLES/SURFACES), the matching `docs/<tab>.md`, and THIS codemap.
30. **Smoke script per feature**: rerunnable, $0 default, `--live` for the one paid verification, cleans up after itself.
31. **No obvious comments; comments explain why** (cost trade-off, policy quirk, race). No emojis.
32. **Never write a raw control char into source — use the escape** (RL.7): `src/x/playbook.ts` held two literal NUL bytes as a composite-key sentinel, which made `file` report it as `data` and made **every `grep` over it silently return nothing** (`grep -a` was the only way to find `classifyReplyOrigin`). Fixed to `'\0'`; identical at runtime, greppable again. One-command audit: `for f in $(git ls-files '*.ts'); do file $f | grep -q ': data' && echo $f; done`.

## 8. Cost & policy invariants (condensed — full text in CLAUDE.md)

- URL in post text = **$0.20 vs $0.015** (13x). `createPost` guard; links go in first reply ($0.030 total).
- **Feb 2026**: programmatic replies to non-self tweets blocked on self-serve (except when @-mentioned — unverified, `MENTION_API_REPLIES=false`). Quote-of-others blocked. Self-reply/self-quote OK (self-quote verified via `verifiedSelfQuote`).
- Token rotation: persist new refresh token BEFORE returning access token.
- Owned reads $0.001; other-user reads $0.005; third-party user lookup $0.010; post $0.015; delete $0.010.
- Media upload requires OAuth 1.0a — **not supported**; Studio images are manual paste.
- Budgets: X soft $0.15/day (log), xAI images hard $0.50/day (429).
- Out of scope v1: multi-tenant, follower sync, media upload, Chrome Web Store, idempotency drafts, per-tier caps.

## 9. Tests & verification map

- `bun test` runs with `SQLITE_PATH=:memory:` (one shared in-memory DB across route suites — seed carefully, clean up; some suites assert exact medians over shared data).
- Pure suites live next to their module (`*.test.ts`); route suites hit the Hono `app` via `app.request` over the in-memory DB; pre-DB guards in `src/app.test.ts`.
- Extension tests: bun:test on shared modules + studio (`templates.test.ts` (S5.6 code-card block; S5.7 thread-cover/list-card/parseListItems blocks; S5.8 chart growth/heatmap blocks), `chartData.test.ts` (S5.8 growthSeries + heatmapCells gate/normalization), `codeTokens.test.ts` (S5.6 tokenizer splits + `MONO_ADVANCE`), `milestones.test.ts` (S5.5 crossing logic), `brandKit.test.ts`, `mascot.test.ts`, `compose.test.ts`); DOM fixtures via happy-dom (earlyReplies); `sidepanel/storage.test.ts` mocks `chrome.storage.local` for the settings round-trip + `resolveTheme` (UI.9); **RU.6** `shared/messages.test.ts` (message-guard suite — `isRadarConfirm`; **RU.7** +`isRadarVariantsGet`/`isRadarVariantPasted`); **RU.7** `shared/variantChips.test.ts` (`variantChipPreview` truncation/whitespace/ellipsis-trim + `isReplyVariants` shape/empty/loose-angle); **AX.4** `shared/tweetContext.test.ts` (hand-built dossier JSON fixture, not happy-dom — rich happy path exact numbers / thin person / angle gate 2-vs-3 + split-cell total / alreadyReplied hit+miss+unlinked / momentum single-point + same-day clamp / open-loop filtering + order / outcome cap).
- `extension/src/shared/notifications.test.ts` (**NT.1**): happy-dom fixtures for the notification cell — the four kinds, aggregated/deduped/lowercased handles, the `unknown` + bad-handle skips, ro keyword fallback (apreciat/redistribuit/urmăre), icon-beats-misleading-header, skeleton cell, ellipsis strip (both `…` and `...`), longest-block target pick, follow-with-bio → null, non-notification element → null. **The fixtures build their glyphs FROM the exported icon-prefix constants**, so correcting a prefix after the live browser walk needs no test edit.
- `src/x/people/{stage,engagements}.test.ts` (**NT.2**): `stage` gained a C10 block — each engagement type is in `PERSON_EVENT_TYPES` but in neither exported stage set and alone computes `stranger` (that last assertion is what covers the unexported `NOTICED_TYPES`), 50 likes + a repost + a follow is still `stranger`, and a `mutual` history plus engagement stays `mutual`. `engagements` — pure dedupe/id/summary/prefix-match matrices, then ingest over the shared in-memory DB: person created `source='notification'` + stage `stranger`, resolved-target vs day-bucket vs follow ids, the same batch twice → `events: 0`, next-day re-log → +1 (follow never repeats), invalid handle skipped, fill-only upsert, `mutual` unmoved with `lastInboundAt` untouched. **Seeded `posts_published` rows carry a January `postedAt` and no metrics snapshot** so they sit outside every window/measured aggregation other suites assert over, and cleanup runs inline + in `afterAll`. **NT.3** appended the route suite to the same file (sightings' colocation precedent — `POST /x/people/sightings` lives in `people/sightings.test.ts`, not `routes/people.test.ts`): batch round-trip + normalized person + re-post logs nothing, invalid handle → `skipped:1` not fatal, two follow cells with different `targetText` collapse to `processed:1` (the route's follow-null coercion), and the full validation matrix ending in "nothing was written" (every guard fires pre-DB).
- `src/openrouter/client.test.ts` (**AI.1**): pure — an injected `fetchImpl` records the request, no network. Covers OpenAI-shape mapping (jsonSchema→response_format, effort 'none' omitted, `usage.include`/`provider.require_parameters` always), cost off `usage.cost` (+ $0/warn when missing), retry classification (429/503 retry via `retry-after:0`, 400 throws once), numeric-error-code coercion, and the soft budget watchdog. Cleans up its `platform='openrouter'` `cost_events` rows in `afterAll`; sets a dummy key + high budget in `beforeEach` (watchdog test uses its own low budget).
- `src/llm/{settings,ask,routes}.test.ts` (**AI.2**): `settings` — defaults on empty DB, save/merge round-trip, garbage-row sanitize, `parseAiPatch` per-field validation. `ask` — runtime gates, `resolveProvider` precedence, the no-silent-fallback `LlmNotConfiguredError` (both provider directions), and the openrouter dispatch via an injected `fetchImpl` (endpoint + response_format mapping + opts>settings>defaults merge). `routes` — bare-Hono `app.request`: GET/PATCH settings, per-field 400s, 409 `provider_not_configured`, models 503. **Env-key discipline:** unset = `process.env.X = ''` (falsy, NOT `delete` — biome `noDelete`; AI.1 pattern), saved/restored per test; all three clean up their `app_settings` `'ai'` row (+ ask cleans openrouter `cost_events`).
- `src/x/prompts/registry.test.ts` (**AI.3**): loadPrompt default-fallback + override-wins over the shared in-memory DB (afterEach deletes all `prompt_overrides` rows — other suites render prompts and must see defaults), sha cache-key stability/distinctness, `validatePromptBody` matrices (missing required / optional-droppable niche placeholders / unknown-token dedup), `renderPrompt` $-safety, and the builders' `template` param (marker template renders; per-request override beats the DB template; `{{REPLY_PERSONA}}` still substitutes in a custom template; explicit default template byte-identical to omitting it).
- `src/x/routes/prompts.test.ts` (**AI.4**): bare-Hono `app.request` over the shared in-memory DB (afterEach+afterAll wipe `prompt_overrides` — other prompt-rendering suites must see defaults) — GET list uncustomized, GET default body, PATCH missing-placeholder 400 + `missing`, valid PATCH → customized + `unknownPlaceholders` + `loadPrompt` reflects it + other key untouched, 32KB→413, empty/absent body 400, reset scoping, restore-defaults `{restored:n}` + list all-uncustomized, unknown-key 404 (GET/PATCH/reset), and the done-when chain (override → `loadPrompt` reflects → restore reverts). The bearer 401 is NOT duplicated (shared `/x` middleware, covered by app.test/mcp.test — niche.test discipline).
- `src/x/replyLists/engine.test.ts` (**RL.1**): stubbed-rng matrices (render degradation, pick exclusion windows at n=2/3/6, humanizer step order + strict thresholds + the fixed 10-draw contract via a `counted` rng) plus seeded `mulberry32` sweeps for the statistical promises — 200 runs asserting protected spans are never mutated and the typo rate tracks the configured chance (0 → none, 5% → single digits). No DB, no `Math.random`.
- `src/x/replyLists/generate.test.ts` (**RL.4**): pure — `buildListGenInput` tail placement (request + existing items after `## Rules`), count/empty-list placeholders, a registry override body as the base + default byte-identity, `$&`/`$'`/`` $` `` survival (split/join), and the no-persona-token assertion; `parseGeneratedItems` happy path, empty/over-280/non-string drops, casing+whitespace dedupe, count cap, malformed → null vs a legal `[]`. No DB, no network.
- `src/x/routes/replyLists.test.ts` (**RL.2**): bare-Hono `app.request` over the shared in-memory DB — CRUD round-trip (trim/defaults, grouped counts, patch, delete→404), cascade (items gone after the list), replace-vs-append semantics (ids swapped, whole-set response, empty replace clears), item writes scoped to their list (right item + wrong list → 404 on PATCH and DELETE), and the guard matrix (name bounds, humanizer null-clears/partial-parses/non-object-400s, item text 1..280 + the 100-cap boundary, bare-string items rejected, unknown-vs-malformed ids 404 vs 400, `empty_patch`). **RL.3** adds the `/use` describe: 10 sequential uses on a 5-item list (no back-to-back repeat, 10 `reply_list_uses` rows, `useCount` sums to 10), preview writes nothing, var fill + missing-var degradation, the use row's rendered text + normalized target, 409 on empty/all-disabled, and the guard matrix. Every text assertion runs on a list whose `humanizer` is all-zero-chance — the route calls `Math.random` for real, and the jitter itself is the engine suite's job. **RL.4** adds the `/generate` describe — guards only, nothing may reach an LLM: the 400 matrix (bad body/prompt/count 0·31·2.5/model/provider/uuid), a 404 for an unknown list *before* the call, and a both-keys-force-unset 503 `llm_not_configured` that also asserts the list stayed empty (proposal-only). Tracks every list it creates and deletes them in `afterAll` — **`reply_list_uses` rows must be deleted explicitly** (FK-free, so they don't cascade and would leak into RL.7's `canned` attribution).
- `src/x/{playbook,routes/playbook}.test.ts` (**RL.7**): the `canned` bucket. Pure — canned checked LAST (a posted draft that also text-matches counts `single`), a right-text/wrong-radar-target reply falls through to `canned`, empty bucket is a zero cell. Route — the `canned attribution (RL.7)` describe seeds a published reply, snapshots `batchVsSingle` BEFORE inserting the `reply_list_uses` row, then asserts the **delta** (canned +1, unattributed −1) rather than absolutes: `reply_list_uses` is FK-free (D79i) so another suite can leave strays; the exact median is asserted only on a clean run.
- Byte-sync prompt tests (post + reply + **thread**, AI.7) fail when .md and TS literal drift.
- `src/x/routes/drafter.test.ts` (**AI.7**): bare-Hono mount of the drafter (+calendar) router — draft-thread pre-spend guards (`invalid_tweet_count`, `invalid_provider` → 400) and the **no-key 503** (force-unsets BOTH `XAI_API_KEY`+`OPENROUTER_API_KEY` in a `finally` so it never spends), plus `insertThreadDraft` directly (head `draft`+tails `segment` sharing a threadId, `source='drafter'`, and `GET /posts/scheduled/:id` returns 3 siblings). Thread pure tests (`parseThreadDraft`/`buildThreadDraftInput`/byte-sync) live in `src/test.test.ts`. **AI.8** added a `rewrite route guards` block here (empty/oversized text, oversized instruction, unknown provider → 400; both-keys-unset → 503) and `parseRewrite`/`buildRewriteInput` pure tests to `src/test.test.ts`.
- Smoke scripts (`scripts/smoke-*.ts`): ai-layer, authoring, c6, c9, channels, conversations, explorer, followups, glance, launch, mcp, me, mentions, niche, people, pillars, pinned-watch, playbook, radar-drafts, radar-reply-flow, reply-lists, studio, targets — rerunnable, $0 default. `glance` (AX.1) mounts the people router in-process over the REAL DB, seeds a throwaway non-retired person + one unanswered mention, asserts the glance-map entry (stage/openLoops/lastOutboundAt), then surgically deletes the rows (idempotent cleanup on entry + exit). `ai-layer` (AI.13) snapshots+restores the `app_settings` 'ai' row and ALL `prompt_overrides` rows so restore-defaults never wipes a real customization; `--live` adds ONE OpenRouter call. `radar-reply-flow` (**RU.10**) mounts the radar + replies routers over the REAL DB: `persistRadarDrafts` seeds a `radar_drafts` row with 3 variants → `GET ?tweetId=` returns it → `POST …/confirm` (201, asserts `reply_drafts` `source='radar'`/`status='copied'`/3 variants/`contextSnapshot` shape incl. `signals.band='hot'` preserved) → confirm again (200 idempotent, same id) → `PATCH /replies/:id` posted with an edited variant + `postedTweetId` → asserts `classifyReplyOrigin`→`'radar'` **by source** (empty text-match fallback map, so it proves source beats the heuristic); deletes every row on entry + exit. `--live` adds ONE 2-tweet `generate-batch` call (~$0.01) asserting 3 variants/tweet + radar_drafts persistence. `reply-lists` (**RL.8**) mounts the replyLists + playbook routers over the REAL DB: create a list (asserts `humanizer:null` = engine defaults) → 5 templated items (insertion order preserved) → **10 `/use` calls** asserting no immediate repeat, ≥3 distinct items, empty `missingVars`, no raw `{var}` left, ≤280 chars, and each var filled **per the item's own template** (an `itemId → template` map, so the assertion holds whichever item the shuffle picks; the fill survives humanization because typos never touch a protected value) → `preview:true` moves neither `use_count` nor the use rows → empty list `409 no_enabled_items` → `mode:'replace'` swaps the set (new ids, `source:'ai'`) → **the Playbook `canned` delta**: snapshot `GET /x/playbook`, insert a `posts_published` reply whose text IS one use's `renderedText` + a `metrics_snapshots` row, re-GET and assert `canned.posted`/`canned.n` **+1** with `unattributed` unmoved (delta, never absolute — the FK-free use table may hold other rows) → DELETE the list, asserting items cascaded and all 10 use rows survived. Cleanup keys off the fixed `TARGET` tweet id + the three list names (uses are FK-free, so they need an explicit delete). Without an LLM key it also asserts `/generate` → `503 llm_not_configured`; with one, the unknown-list **404 before any spend** (no call made). `--live` creates a throwaway list, makes ONE `/generate` call (~$0.003–$0.01) and asserts the proposal is **not persisted**.
- `smoke-me.ts` (**ME.2**): mounts the `me` router in-process over the REAL DB — creates one entry of each kind + a followers goal + an mrr goal, exercises the `inWindow` flag (a 40-day event falls out of the 30d window, a fresh emotion stays in), asserts the mrr goal's deterministic 16% progress + the followers goal reads `null` on a snapshot-less DB, renders both context modes, then **surgically deletes exactly the rows it created (by id — never wipes the table)**, on both success and `fail()`. $0.
- `smoke-niche.ts` (**N.9**): mounts niche/pillars/drafter/brief/voice routers in-process over the real DB; creates+activates `smoke-nutrition` (doctored doctrine) → asserts 0 active pillars, drafter `409 no_pillars_for_niche` (no Grok spend), doctrine-driven brief quota/ratio + voice band, and a `buildGrokInput`/`buildPostDraftInput` assembly with nutrition sentinels present and §1/§5 builder markers (`Alteramens`/`Naval-derived` — NOT §6's `Pitești`, D28b) absent; reactivates builder + asserts restore; **sync `cleanup()` restores builder-active + drops the smoke row on failure** (a mid-run abort must never leave a non-builder niche active). `--live` adds one `POST /x/niche/draft` wizard call (~$0.01, asserts a 3-pillar proposal parses). The live `contextSnapshot.niche.slug` proof rides a real reply-generate (D29b) — deliberately not in the $0 default.
- Studio (S5) browser verification (ST.9, 2026-07-18): the deterministic-render discipline (S3) — a throwaway Bun-served harness bundles the real `compose`/`templates`/`registry` modules and renders every template onto a canvas in Chrome; confirms fonts/mascot/presets/pattern-determinism/Copy-PNG. Reusable pattern for future Studio visual QA; the harness entry is not committed.
- Gates before commit: `bun test` + `bun run typecheck` + `bun run lint`.
- Deploy: `./scripts/deploy.sh` (rsync, migrate — aborts restart on failure, `.git-sha` stamp, `curl -f /healthz`).

## 10. Recipes — where new things go

- **New API surface**: pure logic in `src/x/<domain>.ts` or `src/x/<domain>/` → router in `src/x/routes/<name>.ts` → mount in `src/x/index.ts` (mind order + Grok gating) → route test + pure test → smoke script → docs sync. If MCP-worthy, add a curated tool in `src/x/mcp.ts` (in-process `app.request`).
- **New table/column**: `src/x/db/schema.ts` → `bun run db:generate` → inspect SQL for dropped seeds → consumers. Column semantics: nullable + null=unknown when backfill is impossible.
- **New worker**: `src/x/workers/` → register heartbeat + stop() in `startXWorkers` → healthz staleness window. Prefer read-time computation over a new worker when $0-equivalent (§7.12).
- **New extension surface**: component in `src/sidepanel/` → wire in `App.tsx` → API via `api.ts` → shared pure logic in `src/shared/` or `extension/src/shared/` (bun-tested) → `docs/<tab>-tab.md`. Content-script additions must stay IIFE-safe.
- **New prompt-affecting feature**: NEVER edit the template mid-file — inject at the variable tail; keep byte-sync tests green; stamp what the model saw into the draft's snapshot.
- **New measurement**: pure `build*` in `src/x/playbook.ts` (gated) → loader in `routes/playbook.ts` → Playbook tab section → optionally digest facts.

## 11. Update log

- 2026-07-23 `2b763b1` — **NT.3** (notifications — the ingest endpoint; dep NT.2✓, high, $0, 3 files [+`engagements.ts` for the guard], +5 tests). **`POST /x/people/engagements`** in `routes/people.ts`, registered next to the sightings route and copying its validation shape exactly. **Three notes worth carrying:** (a) the `kind` whitelist is the enforcement point for the parser's `'other'` — server-side `EngagementKind` can't represent it, so the guard `isEngagementKind` was added **to `people/engagements.ts` beside the union** (`isStage`/`isFollowupKind` idiom) rather than inlined in the route, taking the task past its 2-file budget by one; (b) the route **re-forces `targetText: null` for a `follow`** even though NT.1's parser already does — a stale extension build must not be able to feed a follower's bio to the target matcher (§7.3's "the server always recomputes the gate" discipline), and the observable effect is that two follow cells for one handle dedupe to `processed: 1`; (c) an invalid handle is **passed through, not rejected** — `recordEngagements` skips it and reports `skipped`, so one bad scrape never 400s a 50-cell batch. Tests were colocated in **`src/x/people/engagements.test.ts`**, not `routes/people.test.ts` as the plan's edit list said — that is where the sightings route suite lives. Gates: `bun run test` **1127/0** (1122 + 5), typecheck clean, full-repo lint clean; no extension file touched. **Next:** NT.4 (background notif-context cache, none-dep) or NT.6 (fans engagement count, dep NT.2✓); NT.5 unblocks once NT.4 lands.
- 2026-07-23 `ba2c531` — **NT.2** (notifications — engagement event types + the server ingest module; dep none, xhigh, $0, 3 code + 2 test files, +18 tests). `PERSON_EVENT_TYPES` gained `their_like`/`their_repost`/`their_follow`, added to **no** stage set (C10 decision 1 — engagement is not reciprocity; the matrix proves 50 likes + a follow is still `stranger`). New `src/x/people/engagements.ts` mirrors `sightings.ts`. **Design choices worth carrying:** (a) the plan named one `resolveTargetTweetId(targetText)` *and* demanded the post list be cached per batch — split into pure `matchTargetTweetId(snippet, posts)` (unit-testable without a DB) + the DB wrapper, with `recordEngagements` loading the window at most once and **not at all for a follow-only batch**; (b) `recomputePerson` runs **once per distinct handle** (max seenAt), not per row — it reloads the whole history anyway and the `maxStage` ratchet makes it provably equivalent; (c) the day-bucket id is what keeps an unresolved like from flooding the timeline on every scroll (§7.7); (d) `refTable/refId` stay null — the id already encodes the ref (`hoverSightingEventId` precedent); (e) `people.source` comment corrected (it never listed `hover`/`launch`). **Not touched, deliberately:** the extension's `PersonEventType` union + the exhaustive `EVENT_ICONS` Record in `People.tsx` — NT.6 owns those, and because the Record is exhaustive TypeScript will force the three labels the moment the union grows. Nothing imports `engagements.ts` yet; NT.3 mounts it under `POST /x/people/engagements`.
- 2026-07-23 `2063b86` — **NT.1** (notifications — the cell parser; dep none, high, $0, 2 new files, +12 tests; **opens the NT server lane**, first task of the C10 notifications phase). New `extension/src/shared/notifications.ts`: `EngagementKind`/`ParsedNotification` + `parseNotificationCell(article)`, pure and **INERT** — no consumer imports it until NT.5. **Design choices worth carrying:** (a) the exported `LIKE_ICON_PREFIXES` (2) / `REPOST_ICON_PREFIXES` / `FOLLOW_ICON_PREFIXES` are **best-effort recollections of X's glyph `d` strings, NOT captured from the live DOM** (the plan assumed the implementer would scrape them while building fixtures; no browser here) — so an unrecognised glyph returns `null` and falls through to keywords rather than short-circuiting to `'other'`, and the fixtures interpolate the constants instead of hardcoding paths, making a post-verification correction a one-line edit with zero test churn; (b) keyword checks run **like → repost → follow** because only that order makes the deliberately loose ro stem `urmăre` safe (a follow cell carries no post text, so the like/repost phrases have already had their chance); (c) keywords are matched against the **first `[dir]` block** (X renders the "A and 2 others liked your post" header before the quoted post) and only then the whole cell text, keeping post prose from voting; (d) `targetText` is forced **null for `follow`** — X sometimes renders the new follower's bio in the cell, and letting that reach NT.2's `resolveTargetTweetId` would try to match a bio against own posts; (e) handles come only from `UserAvatar-Container-*` testids, regex-validated and with X's `unknown` placeholder dropped. Gates: `bun run test` **1104/0** (1092 + 12), root + extension typecheck clean, `cd extension && bun run build` clean, changed-file lint 0-error. **Binds NT.5** (content script — gated behind NT.1/NT.3/NT.4, part of the D9 content.ts chain), and NT.2's ingest consumes this exact `{kind, handles, targetText}` shape.
- 2026-07-23 `a120207` — **RL.8** (reply lists FINAL — docs-sync + $0 smoke; dep all RL✓, high, **no code/schema/route/test change**; closes the RL sub-plan RL.1–RL.8). New `scripts/smoke-reply-lists.ts` (see §9 for the walk) — passes $0 against the real `stratus.db`, rerunnable, cleans up on entry and exit. Docs: CLAUDE.md phase entry **"Surfaces S7 — Reply lists"** (the plan's own "(S5)" is stale — S5 = Studio 2.0, S6 = Augmented X UI), SURFACES-PLAN.md §5d, `docs/replies-tab.md` (a full "The Lists subtab" section: templates + var degradation, the anti-repeat shuffle, the humanizer table with the two guarantees, proposal-only AI generate, the `canned ▾` surfaces, and the honest measurement caveats; plus a Launch-Room canned workflow) and `docs/today-tab.md` (a `canned ▾` bullet on the Launch Room replier rows and the Conversations open loop, and $0 added to the free list in the spend tip). **Drive-by:** three stale "two variants" lines in `docs/replies-tab.md` fixed to three (RU.1 shipped 3 variants; the body sections were already correct).
- 2026-07-23 `3a4ae56` — **RL.7** (reply lists — the Playbook `canned` attribution bucket; deps RL.3✓+RU.9✓, high, $0, 7 files incl. 2 test + 1 smoke, +5 tests). `ReplyOrigin` is now `'single' | 'radar' | 'canned'` and `classifyReplyOrigin` takes a **4th required param** `cannedTexts: ReadonlySet<string>` (normalized `reply_list_uses.renderedText`). **Required, not optional-with-default:** a defaulted empty set would let a future caller silently misattribute canned replies as unattributed — the one production call site plus `scripts/smoke-radar-reply-flow.ts` were updated instead. **Precedence is the whole design:** draft link → radar text-match → canned. A canned use leaves NO `reply_drafts` row (Decision 6 — polluting reply_drafts would skew the angle crosstabs), so rendered text is its ONLY evidence; checking it last means a reply that is both a posted draft and a text match counts once, as the draft, exactly like RU.9's radar rule. The loader adds one **unwindowed** select (mirrors `radarRows`; the `MAX_PUBLISHED_REPLIES` cap on the published side is what bounds the population), normalizes with the shared `normalizeReplyText`, and **skips empty strings** so a blank rendered text can't swallow a blank published reply. `buildBatchVsSingle` gains a `canned` cell built by the same `cellOf` — counts are ungated (§7.19), the medians inside the cell keep the n≥20 gate. Extension: `PlaybookData.batchVsSingle.canned` + a third table row "Canned (reply lists)" with the status line spelling out the text-match-vs-draft precedence. **Drive-by (D83):** `src/x/playbook.ts` held two literal NUL bytes as the pillar×register key sentinel, which made `file` classify it as `data` and **every plain `grep` over it return silently nothing** — replaced with `'\0'` escapes (identical at runtime); new codemap §7.32. Gates: `bun run test` **1092/0**, root + extension typecheck, extension build ("Canned (reply lists)" in `dist/sidepanel.js`), changed-file lint 0-error. **Next:** RL.8 (docs+smoke, closes the RL sub-plan); the NT server lane stays parallel-ok.
- 2026-07-23 `299595f` — **RL.6** (reply lists — the QuickReplyPicker in Launch Room + Conversations; dep RL.5✓, high, $0, 5 files, 0 new tests). New `extension/src/sidepanel/QuickReplyPicker.tsx`: a `<span className="quick-reply">` holding a "canned ▾" button and an absolutely-positioned popover (GearPopover's outside-`mousedown` close pattern, no new primitive — there is no Menu primitive to reuse and the popover is 30 lines of markup). Module-level 60 s cache + in-flight guard around `api.replyLists.list` (verbatim ChannelTags shape) so N pickers on one screen make ONE request; `invalidateReplyListsCache()` is exported and called from `ReplyLists.tsx` as an **unmount cleanup** (`useEffect(() => invalidateReplyListsCache, [])`) — that panel is the only place lists are mutated, so one line covers create/rename/activate/delete/items instead of six invalidate calls sprinkled through its children. **The contract that matters (Decision 1):** the picker never picks and never sends `preview:true` — one click is one real `POST /use`, which owns the shuffle, the var fill and the humanizer. Client-side it only *normalizes* before sending, mirroring the route's own guards so a bad value degrades instead of 400ing the whole use: `vars.name`/`vars.handle` trimmed + sliced to `MAX_VAR_LEN` 120, `targetHandle` `@`-stripped/lowercased and **dropped unless it matches `USERNAME_RE`**, `targetTweetId` dropped unless `/^\d{1,32}$/` (D79g — those two are audit metadata; a malformed one is a hard 400 and losing the use over it would be absurd). Copy runs **inside the same click handler** right after the round-trip (the panel document is focused, which is what Chrome's clipboard-write gate actually checks); a clipboard rejection is caught and the popover shows the text with "Copy this by hand:" — the item is already spent, so silently swallowing it would lose the reply. Only `active` lists are offered (D79e: `active` is presentation state, an inactive list is still usable by id), `enabledCount === 0` rows render disabled with a tooltip, and a stale-cache 409 `no_enabled_items`/404 still surfaces inline. Wired with vars from the row the machinery already has: LaunchRoom `{name: r.author ?? undefined, handle: r.handle}` + `targetTweetId: r.tweetId`; Conversations `{name: t.counterpartName ?? t.person?.displayName ?? undefined, handle: t.counterpartHandle ?? undefined}` + `targetTweetId: owed.tweetId` (the owed inbound). **No status flips added** — the user still clicks Done after pasting (posting stays manual, §7.28), which is also why the plan's `onUsed?` prop was dropped (D82: no caller had a use for it). No new tests (panel components are untested by repo convention) and **unverified in a browser**; gates `bun run test` 1087/0, root + extension typecheck, extension build (`quick-reply-pop` present in both `dist/sidepanel.js` and the emitted CSS), changed-file lint 0-error. §5 tab-components + styles.css rows. **Next:** RL.7 (Playbook `canned` bucket, deps RL.3✓+RU.9✓) then RL.8; the NT server lane stays parallel-ok.
- 2026-07-23 `b415eb3` — **RL.5** (reply lists — the Lists subtab management UI; deps RL.2✓+RL.3✓+RL.4✓, high, $0, 5 files, 0 new tests). `extension/src/shared/types.ts` gains the 15 RL types (Date columns are ISO strings over the transport; `ReplyListSummary` = list + `itemCount`/`enabledCount`; `ReplyListPatchBody.humanizer` is `HumanizerConfig | Record<string,never> | null` so `{}` is expressible). `api.ts` gains `api.replyLists.*` (10 methods, all through the background ApiRequest transport §7.25; `setItems` unwraps `{items}`). `Replies.tsx` gains a `'drafts' | 'lists'` view rendered with the **`SubTabs` UI.10 primitive** (D7 — the plan said "copy Voice.tsx's `radar-tabs`"; SubTabs is the formalized version of exactly that control and needed no CSS) and short-circuits to `<ReplyListsPanel>` on `'lists'`; the panel title became "Replies" (the subtab now names Reply Master). New `ReplyLists.tsx`: rail of lists (name + "n items · m on", inactive dimmed) + `CreateList`, then per-list `ListSettings` (name/description/active/delete-with-confirm), `ItemsEditor` (**one reply per line** add box → `setItems` `mode:'append'`; client-side 100/call + 280-char guards mirroring the route so a paste gets a message not a bare 400) + `ItemRow` (inline text edit, On/Off, delete, `used N× · last Xd ago` / `never used`, `· ai` for generated rows), `HumanizerEditor`, `TestRender`, `GenerateBox`. **Three things a future session should not re-derive:** (a) **the humanizer form seeds itself from the server, not from a client-side `DEFAULT_HUMANIZER` copy** — "Customize for this list" PATCHes `{humanizer:{}}` and `parseHumanizerConfig` lenient-parses every absent field to its default, so the returned row IS the full engine config (proven with a throwaway route test: create → `humanizer:null`, PATCH `{}` → deep-equals `DEFAULT_HUMANIZER`, PATCH `null` → back to null); "Reset to defaults" is the `null` PATCH. (b) **prefix/suffix pools are one-per-line textareas, NOT the plan's comma-separated inputs** — `DEFAULT_HUMANIZER.prefixes` contains `'honestly,'`/`'man,'`/`'yeah,'`, so a comma split would shred them. (c) **Test render always sends `preview:true`** (§RL.3: preview writes nothing), so exercising a list never advances its anti-repeat state, and the generate proposal is component state only — nothing persists until Append/Overwrite hits `setItems` with `source:'ai'` (Decision 3; Overwrite is `confirm()`-guarded, and each proposal row has a ✕ to drop it before applying). Error copy maps `llm_not_configured` (D80 — NOT `grok_not_configured`), `no_enabled_items`, and the `items_parse_error`/`items_invalid` 502 pair. No new tests (panel components are untested by repo convention — logic lives server-side) and **unverified in a browser**; gates `bun run test` 1087/0, root + extension typecheck, extension build, changed-file lint all green. §5 api.ts/tab-components/styles.css rows.
- 2026-07-23 `f353f6a` — **RL.4** (reply lists — AI item generator: prompt module + registry key + `POST /reply-lists/:id/generate`; dep RL.2✓, high, $0 in tests / ~$0.003–$0.01 per real click, 6 files, +14 tests). New pure `src/x/replyLists/generate.ts`: `REPLY_LIST_PROMPT_TEMPLATE` (TS-only per AI-layer Decision 9), `REPLY_LIST_GEN_SCHEMA`, `buildListGenInput`, `parseGeneratedItems`. **The plan text predates the AI layer and was overridden three ways (D80):** (a) `askLLM` + `loadPromptSafe('reply-list')` instead of `askGrok` + a hardcoded `promptCacheKey` — per **D5**, the registry owns cache keys and a new prompt must be editable, so `PROMPT_KEYS` went **10 → 11** (appended; `docs/settings-tab.md`'s 10-prompt copy updated with it; the extension's Prompts panel is data-driven and needed no change); (b) **no pre-flight `XAI_API_KEY` → 503 `grok_not_configured`** — the ideas.ts/AI.9 precedent instead: askLLM enforces the resolved provider's key and `llmErrorPayload` maps it to **503 `llm_not_configured`**, which is what makes an OpenRouter-only setup work AND keeps the unknown-list 404 from being shadowed by a key check (RL.5's error matcher must expect `llm_not_configured`); (c) `count` **400s past 30 rather than clamping** (the plan's own test asks for a 400 on 31; silently returning 12 of a requested 40 reads as a broken generator). Prompt shape follows §7.15 exactly — instruction block, then `{{COUNT}}`/`{{REQUEST}}`/`{{EXISTING_ITEMS}}` at the tail; **no persona token**, so the cacheable prefix doesn't move with the active niche and the cache key stays the bare registry sha (the `ideas`/`rewrite` precedent). `parseGeneratedItems` dedupes on a **lowercased + whitespace-collapsed** key rather than the plan's "exact texts" — two items differing only in casing defeat the anti-repeat pick they'd feed. Route writes NOTHING (Decision 3): guards → 404 → existing texts into the tail → one call → proposal; the user applies it through the existing `POST /items` `source:'ai'`. Tests: 11 pure (tail placement, `$`-safety, override-vs-default byte identity, the parse matrix) + 3 route guard groups (400 matrix, 404-before-spend, both-keys-unset 503 asserting the list stayed empty). Gates: `bun run test` **1087/0** (+14), typecheck clean, full-repo lint 0-error. **Done-when tail needing live use:** the first real "Generate items" click (RL.5 builds the button) — nothing here has ever called an LLM. **Next:** RL.5 (Lists subtab UI, deps RL.2–4✓) or RL.7 (Playbook `canned` bucket, deps RL.3✓+RU.9✓); the NT server lane stays parallel-ok.
- 2026-07-23 `c3f44d2` — **RL.3** (reply lists — `POST /reply-lists/:id/use`: anti-repeat pick + humanized render + stamp; deps RL.1✓+RL.2✓, high, $0, 2 files, +6 tests). The route is thin on purpose — the engine already owns every decision: load the list (for its normalized `humanizer`) and **all** its items (pickItem owns the enabled/var-availability rules, so the filtering lives in exactly one place), `pickItem(items, availableVarsFor(vars), Math.random)` → **409 `no_enabled_items`** when nothing is usable (covers both an empty list and an all-disabled one), `composeReply(item.text, vars, resolveHumanizer(list.humanizer), Math.random)` — note the plan's `protectedValues = [vars.name, vars.handle]` argument doesn't exist: RL.1's `composeReply` derives the protected spans (name/first_name/handle) internally. Unless `preview:true`, ONE sync txn (§7.13) stamps `lastUsedAt` + `use_count + 1` (via `sql\`${col} + 1\``, the dailyMetrics pollCount idiom) and inserts the `reply_list_uses` row. **The one thing worth carrying forward (D79):** `lastUsedAt` is stamped **monotonically past the list's current max**, not from `Date.now()` — the anti-repeat window only holds while the just-used item is STRICTLY the most recent, and uses land ~0.5ms apart. Measured with a throwaway 500-use stress: with the stamp, 0 duplicate `usedAt` values and 0 immediate repeats; without it, **241/500 uses shared a millisecond and 4 immediate repeats slipped through** — i.e. the plan's own 10-use done-when test would have been ~8%-flaky. Same trap RL.2 hit on item `createdAt`, now §7.13a. Smaller calls: `updatedAt` is NOT bumped on use (it means "the text was edited"); an `active:false` list is still usable (active is presentation state — the contract lists only 404/409, so no new error code was invented); a body-less POST is a legitimate no-target use while a body parsing to a non-object 400s; `targetHandle` is normalized the repo way (`@`-stripped, lowercased, `USERNAME_RE`) and 400s if malformed, since it's audit metadata that later joins people/voice rows. Tests: the 10-use no-repeat + full stamping walk, preview-writes-nothing, var fill + missing-var degradation ("Thank you, {name}!" → "Thank you!" + `missingVars:['name']`), the use row's rendered text/target, 409 on empty and all-disabled, and the guard matrix — all on an all-zero-chance humanizer, because the route calls `Math.random` for real. `afterAll` now also deletes `reply_list_uses` (FK-free by design — they don't cascade and would leak into RL.7's `canned` bucket). Gates: `bun run test` **1073/0** (+6), typecheck clean, full-repo lint 0-error. **Next:** RL.4 (AI generator, dep RL.2✓, **D5** — `askLLM` + a registry key) or RL.5; the NT server lane (NT.1/NT.2/NT.4) stays parallel-ok.
- 2026-07-22 `9c0d97b` — **RL.2** (reply lists — schema + CRUD routes + mount; dep RL.1✓, high, $0, 5 files, +8 tests). **Three tables** (migration `0018_flimsy_captain_universe.sql`, DDL-only, 3 CREATE TABLEs + 2 indexes, **no seed** so the drizzle-dropped-INSERT trap doesn't apply; verified against a copy of the real `stratus.db`: 36 tables after, `content_pillars` seed intact, FK cascade live): `reply_lists` (uuid PK, name, description, `humanizer` JSON, active, sortOrder) → `reply_list_items` (uuid PK, `list_id` FK **ON DELETE CASCADE**, template `text`, `enabled`, `source` manual|ai, **`last_used_at`/`use_count` — the anti-repeat state lives server-side (Decision 1), index `(list_id,last_used_at)` = exactly `pickItem`'s recency read**) → `reply_list_uses` (uuid PK, listId/itemId/`rendered_text`/target/usedAt, index `(used_at)`) — **uses are deliberately FK-free**: they're the audit log AND the Playbook `canned` hook, so the history must outlive an edited-away item or a deleted list. **Routes** `src/x/routes/replyLists.ts` (pillars.ts validation idiom, ideas.ts uuid/404 idiom; always mounted next to `ideasRouter`, $0): collection GET carries `itemCount`/`enabledCount` from ONE grouped select; `POST /:id/items` takes `{mode:'append'|'replace', items:[{text}] ≤100, source?}` and runs **one sync txn** (§7.13 — `.run()`/`.all()` terminals) so a replace never leaves the list emptied-then-crashed, and **always answers with the list's whole current set** so both modes read alike; item PATCH/DELETE are scoped by `id` AND `listId` (right item, wrong list → 404). **Two contract calls worth knowing:** (a) `humanizer` is stored NORMALIZED through the engine's `parseHumanizerConfig` — `null` clears to `DEFAULT_HUMANIZER`, a non-object 400s `invalid_humanizer`, a partially-bad object is lenient-parsed and accepted (D76a made this possible); RL.3's `/use` therefore never re-validates. (b) item `createdAt` is stamped explicitly per row, starting past the list's current max, because a batch insert shares one `unixepoch()` millisecond and an append landing in the same ms as the previous batch would tie — insertion order is what the panel and the AI preview render. `MAX_REPLY_LENGTH` (280) is imported from the engine, not re-declared. Tests: CRUD round-trip, cascade, replace-vs-append, list-scoped item writes, and the guard matrix; every created list is deleted in `afterAll` (shared in-memory DB). Gates: `bun run test` **1067/0** (+8), typecheck clean, changed-file lint 0-error (biome reformatted two long conditionals). **Next:** RL.3 (`POST /reply-lists/:id/use` — pick + compose + stamp, deps RL.1✓+RL.2✓).
- 2026-07-22 `ffbdb3f` — **RL.1** (reply lists — pure engine: render, anti-repeat pick, humanize; dep none, **xhigh**, $0, 2 new files, +39 tests). New `src/x/replyLists/engine.ts` (+`.test.ts`) — the whole feature's decision-making, before any table exists. **(a) `renderTemplate`** fills `{name}`/`{first_name}`/`{handle}` (name emoji/flag/ZWJ-stripped, handle `@`-stripped, first_name = first token); a var the target can't fill is removed **with exactly one adjacent separator** — the leading `", "`/`" "` when there is one, else the trailing one — so "Thank you, {name}!" degrades to "Thank you!" and never "Thank you, !"; a `tidy` pass collapses horizontal runs, pulls spaces off punctuation and drops a dangling trailing comma; unknown `{foo}` is intentional text, left verbatim; every unfilled var is reported in `missingVars` (never blocks the use, Decision 7). **(b) `pickItem`** — enabled only; items whose template needs an unavailable var are deprioritized (used only when nothing else remains — deprioritization outranks recency, so a var-free item repeats rather than blocking); then the `min(n-1, floor(n/2))` most-recently-used are excluded (`lastUsedAt` null = never used = most eligible; stable sort, input order preserved in the eligible set) and the pick is **uniform among the rest** — pure LRU cycling is itself a detectable pattern, so randomness inside the eligible half is the point. n≥2 can never repeat the last-used item; n=1 repeats by necessity; n=0 → null. Generic over `PickableItem` so RL.3 gets its row type back. **(c) `humanize`** — independent rolls in fixed order (prefix .25 → suffix .20 → lowercase .15 → drop-period .10 → typo .05), each skipped rather than truncated when it would clear `MAX_REPLY_LENGTH` (280). **`HUMANIZE_DRAWS`=10 rng draws are taken UP FRONT, every call, whether or not a step fires** — so a stubbed sequence maps to steps by position (0 prefix chance · 1 prefix pick · 2 suffix chance · 3 suffix pick · 4 lowercase · 5 drop-period · 6 typo chance · 7 typo word · 8 typo kind · 9 typo position). The suffix joins with `" "` after terminal punctuation, else `", "`; drop-period leaves an ellipsis intact (`[^.]\.$`); lowercase targets the first letter of the item's own text (a `bodyStart` offset, so an applied prefix doesn't shield it) and skips a protected/`@`/`#` first word; the typo picks one of drop/swap/neighbor(QWERTY row)/double-space on a ≥4-letter all-letter word that is **not** a mention, hashtag, link-ish token, or inside/containing a protected value — names and handles are never mangled (Decision 5). `applied: string[]` names what fired (`['prefix','typo:swap']`). **(d) `parseHumanizerConfig`** is the brandKit lenient parse (unknown fields ignored, bad fields fall back one by one, an explicitly empty pool honored) but returns **null ONLY when the value isn't an object** — the one deliberate contract addition beyond the plan text, because RL.2's CRUD needs to 400 `invalid_humanizer` while still storing a partial one; `resolveHumanizer(raw)` is the `?? DEFAULT_HUMANIZER` form the RL.3 `/use` path wants. `composeReply` = render → humanize with the target's own name/first-name/handle as protected spans. Tests: stubbed-rng matrices for all three plus seeded `mulberry32` sweeps (200 runs: protected span intact every time, typo rate 0 at chance 0 and single-digit at 5%, links/mentions never mutated). Gates: `bun run test` **1059/0** (+39), typecheck clean, changed-file lint 0-error (the emoji class had to become an **alternation** — biome `noMisleadingCharacterClass` forbids a ZWJ inside `[...]`). **D75** (AX.7 sha reconciliation `00ec8a8`→`83f8c4d`, orphan GC'd; 3 out-of-skill commits landed after it — `912a9cb`/`1b2a474`/`2ffca50`, all `.md`/CSV plans+evals, no source touched, ledger no drift).
- 2026-07-21 `83f8c4d` — **AX.7** (augmented X UI FINAL — docs-sync + $0 smoke; dep all AX✓, high, **no code/schema/route/test change**; closes the AX sub-plan AX.1–AX.7). Re-ran `scripts/smoke-glance.ts` against the real `./stratus.db` → **SMOKE OK** ($0: seed non-retired `mutual` person + one unanswered mention → glance-map entry `{stage,isTarget,openLoops:1,lastOutboundAt,followersCount:4200}` → surgical cleanup, absent after). Docs: **new CLAUDE.md phase entry "Surfaces S6 — Augmented X UI"** (the whole AX.1–6 arc — stats-pill removal [border/radar byte-identical], `GET /people/glance` shape + before-`:handle` §7.20, `shared/glance.ts` chip legend, `shared/tweetContext.ts` panel model, status-page panel + `augment:contextCollapsed` + `#reply-master-btn` kill rule, `stratus/open-person` click-through, 19-MCP-unchanged, live-selector tail); **`SURFACES-PLAN.md` §5c "Phase S6"** (SHIPPED); new **`docs/s6-augmented-ui.md`** (what renders where, chip legend, glance/dossier caches, collapse key, the legacy-button selector, verification); one-line App.tsx-handoff note in `docs/people-tab.md`. Codemap: §2 `docs/` row now `s1`–`s4`+`s6`, this line, header re-stamp — §3.4 (glance route)/§4 (no schema change)/§5 (content.ts surfaces, `glance.ts`/`tweetContext.ts`/`variantChips.ts`, messages, `augment:contextCollapsed`/`stratus:openPerson` keys)/§9 (glance smoke + `glance.test.ts`/`tweetContext.test.ts`/`messages.test.ts`) were already accurate from AX.1–6 → verification only. **Naming deviation (D74):** the plan text said "Surfaces S5" — stale, S5 is Studio 2.0; shipped as **S6** (SURFACES-PLAN.md numbers phases S0–S5). Gates: canonical `bun run test` **1020/0** (unchanged — docs-only), typecheck clean, `bun scripts/smoke-glance.ts` → SMOKE OK $0, changed-file lint 0-error. **This CLOSES the AX sub-plan (AX.1–AX.7).** **Wave 2 now RU.1–10✓ AX.1–7✓.** **Next:** the NT server lane (NT.1/NT.2/NT.4 — none-dep, parallel-ok) / RL.1 (reply-list pure engine, none-dep). The D9 content.ts chain's next task NT.5 stays gated behind NT.1/NT.3/NT.4 (server, none done). **Sha reconciliation (D73):** RU.10's recorded `22099a3` was an amend-orphan (`merge-base --is-ancestor`→false, `cat-file -t` MISSING [GC'd]); real RU.10 = `84ce82b` (= HEAD at AX.7 start, subject "(RU.10)", parent `f78b48c`=RU.9). No out-of-skill work; ledger no drift.
- 2026-07-21 `22099a3` — **RU.10** (radar↔reply unification FINAL — docs-sync + $0 smoke; dep all RU✓, high, no code/schema/route change): new **`scripts/smoke-radar-reply-flow.ts`** — mounts the radar + replies routers in-process over the REAL DB and walks the whole unified flow at $0: `persistRadarDrafts` seeds a `radar_drafts` row with 3 variants → `GET /x/radar/drafts?tweetId=` returns it → `POST …/confirm` **201** (asserts the `reply_drafts` row `source='radar'`/`status='copied'`/3 variants + `contextSnapshot` shape: `tweetId`, `metrics.views` rebuilt from signals, `signals.band='hot'` preserved) → confirm again **200** idempotent (same id, no 2nd row) → `PATCH /x/replies/:id` `copied→posted` with an edited variant + a `postedTweetId` → rebuilds the `postedTweetId→source` map like `loadOriginRows` and asserts `classifyReplyOrigin`→`'radar'` **by source** (empty text-match fallback map — proves source beats the heuristic, the edited text matches no variant). Deletes every row on entry + exit; `--live` adds ONE 2-tweet `generate-batch` call (~$0.01) asserting 3 variants/tweet + radar_drafts persistence. Docs-sync: CLAUDE.md new §"Overhaul 7.7" phase entry (migration `0014` columns, the `VOICE_BLOCK_END` trap, paste=posted semantics, manual-band-never-a-classifier-verdict coercion, confirm idempotency, RU.9 source-exact split), PLAN.md top-summary blockquote, `docs/replies-tab.md` (two→three variants + the Radar on-page-chips paste=posted note) + `docs/today-tab.md` Radar section (3 variants, ⊕ manual pin + `manual` band chip + top-rank, on-page angle chips). §9 smoke-list gains `radar-reply-flow`. Gates: canonical `bun run test` **1020/0** (unchanged — a smoke script isn't a bun:test), root+extension typecheck clean (scripts in tsconfig include), `bun run scripts/smoke-radar-reply-flow.ts` → SMOKE PASS $0, changed-file lint 0-error. **This CLOSES the RU sub-plan (RU.1–RU.10).** **Next:** AX.7 (AX docs-sync) / NT server lane (NT.1/NT.2/NT.4) / RL.1. Cost: $0. **Sha reconciliation (D72):** RU.9's recorded `899e126` was an amend-orphan (`merge-base --is-ancestor`→false, `cat-file -t` still finds it, same "(RU.9)" subject as HEAD); real RU.9 = `f78b48c` (= HEAD at RU.10 start, parent `065c382`=RU.8). No out-of-skill work; ledger no drift.
- 2026-07-21 `f78b48c` — **RU.9** (radar↔reply unification — Playbook source-exact reply attribution; dep RU.2✓, high, $0, 5 files incl. 2 test). RU.2 added `reply_drafts.source` (`reply_master`|`radar`|null) and RU.5's confirm endpoint stamps `source='radar'` on radar-confirmed drafts (later PATCHed `posted` with a `postedTweetId`) — which the OLD `classifyReplyOrigin` mislabeled `single` because ANY draft-linked reply (in `draftPostedIds`) went to `single`; radar was only ever caught via the text-match fallback for replies with no draft link. **(a) `src/x/playbook.ts`:** `classifyReplyOrigin`'s 2nd arg changed `draftPostedIds: ReadonlySet<string>` → `draftSourceByPostedId: ReadonlyMap<string, string|null>` (**D71** — merged the Set into the source map since the Set was exactly the map's keys; the plan literally said "gains a source map arg" but a redundant Set+Map was avoided). Branch: `map.has(tweetId)` → `map.get()==='radar' ? 'radar' : 'single'` (radar-confirmed vs reply_master/legacy-null); else the unchanged radar text-match fallback (target match AND collapsed-whitespace text equality) for no-draft-link replies; else null. Source beats heuristic, exact beats reconstructed. **(b) `src/x/routes/playbook.ts`:** `loadOriginRows` selects `replyDrafts.source` alongside `postedTweetId` and builds the `postedTweetId→source` map (last-write-wins on the impossible dup). **(c) `extension/src/sidepanel/Playbook.tsx`:** batch-vs-single status line now notes "Radar rows are confirmed drafts (attributed by reply source, not text match)." Gates (n≥20 §7.19) untouched — attribution accuracy, not a new stat. Tests: `src/x/playbook.test.ts` `classifyReplyOrigin` block rewritten Set→Map (draftSource map `{901:null, 905:'radar'}`) + new `source=radar` beats a text mismatch (905, mismatched text/null inReplyTo → radar) + legacy null-source text-match still → radar; `src/x/routes/playbook.test.ts` `seedReply` gains an optional `source`, new isolated `describe('radar source-exact attribution (RU.9)')` seeds a radar-sourced posted draft (postedTweetId `pb_ru9`, text matching NO radar_drafts row) → asserts `batchVsSingle.single.n===2, radar.n===2` (old code would have been single.n=3, radar.n=1 — the flip is the proof), afterAll cleans up. Gates: canonical `bun run test` **1020/0** (+2), root+extension typecheck clean, `bun run lint` 0-error, extension `vite build` green. **Done-when met:** a confirmed+posted radar draft classifies `radar` without text equality. **Wave 2 now RU.4–9✓ AX.1–6✓.** **Next:** RU.10 (RU docs-sync) / AX.7 (AX docs-sync) / NT server lane (NT.1/NT.2/NT.4) / RL.1. Cost: $0. **Sha reconciliation (D70):** RU.8's recorded `9ba6fba` was an amend-orphan (`merge-base --is-ancestor`→false, `cat-file -t` finds it, same "(RU.8)" subject as HEAD); real RU.8 = `065c382` (= HEAD at RU.9 start, parent `f90084b`=RU.7). No out-of-skill work; ledger no drift.
- 2026-07-21 `9ba6fba` — **RU.8** (radar↔reply unification — manual add-to-radar ⊕; deps RU.4✓+RU.7✓, xhigh, $0, 8 files incl. 3 test files). A round ⊕ on every timeline tweet's action row pushes it into the Radar queue regardless of band — "I want to reply to this one, period." **(a) `shared/radar.ts`:** `RadarBand` widened `'hot'|'warm'` → `+'manual'`; `mergeSightings` never downgrades an existing `manual` band on a hot/warm re-sight (`prev.band==='manual' ? 'manual' : s.band`); eviction sorts non-manual to the front (dropped first) so a human pin outlives fresher auto-captures; `rankSightings` puts a leading manual-first comparison **above roster tier**; `isRadarSightings` accepts `'manual'` (`draftRowToSighting` auto-covers it via the widened `RadarBand|null`). **(b) `shared/types.ts`:** `BatchReplyTweet.band` widened so `Radar.tsx draftReplies` can forward `s.band`. **(c) `content.ts`:** `RADAR_ADD_CLASS` + `radarAddHandled` WeakSet + `attachRadarAddButton` (from `scan`, `attachButton` discipline — anchor on `[data-testid="reply"]`→`div[role="group"]`, require `findPermalink`, `actionRow.appendChild`) + `onRadarAddClick` (resolves the article from `btn.closest`, builds a `band:'manual'` sighting via `readTweetSignals(article) ?? synthManualSignals(article)` [zeros + timestamp-derived ageMin], flips ✓ for 1.5s, sends `stratus/radar-report` **immediately** — the background stays the single §7.24 buffer writer; `recordRadarSighting` untouched) + `.stratus-radar-add-btn` CSS. **(d) server `replies.ts`:** `parseBatchTweets` accepts `band:'manual'`. **(e) server `radar.ts`:** `RadarBatchTweet`/`RadarDraftInsert` band widened; **the confirm endpoint coerces `band:'manual'`→`null`** in the rebuilt `contextSnapshot.signals.band` (Task 8 "How" mandate — a manual pin is queue metadata, never a classifier verdict, so it can't enter the Playbook's hot/warm band cells §7.19). **(f) `Radar.tsx`:** `whyLine` shows "manually added · Nd" for a zero-signal manual pin (band chip already renders generically); `.radar-band-manual` CSS in `styles.css` (accent-tinted). Tests: `radar.test.ts` (+3: manual survives hot re-sight, eviction keeps manual, manual ranks first), `src/test.test.ts` (parseBatchTweets accepts 'manual'), `radar.test.ts` server (+1: a manual-band row confirms with `signals.band` coerced to null). Gates: canonical `bun run test` **1018/0** (+4), root+extension typecheck clean, extension `vite build` green, `bun run lint` 0-error (one biome format fix on the test). content.ts DOM behavior browser-unverified by convention. **Done-when tail needing live use:** ⊕ on a cold tweet lands it top of the Radar queue with a `manual` chip and the next "Draft replies" drafts it. **Advances the D9 content.ts chain** (AX.3✓→AX.5✓→RU.7✓→RU.8✓→NT.5→HV.2→A3.3). **Next:** RU.9 (Playbook source-exact attribution, dep RU.2✓, high, server, parallel-unblocked) or NT.5 (needs RU.8✓ — next content.ts task, but NT.1/NT.3/NT.4 first). Cost: $0. **Sha reconciliation (D69):** RU.7's recorded `bfa2441` was an amend-orphan (`merge-base --is-ancestor`→false, `cat-file -t` finds it, same "(RU.7)" subject as HEAD); real RU.7 = `f90084b` (= HEAD at RU.8 start, parent `0e08af9`=AX.6). No out-of-skill work; ledger no drift.
- 2026-07-21 `bfa2441` — **RU.7** (radar↔reply unification — on-page variant chips + paste→posted; deps RU.5✓+RU.6✓+AX.5✓, xhigh, $0, extension-only): 6 files (2 new: `extension/src/shared/variantChips.ts`+`.test.ts`). A radar-drafted tweet's status page shows 3 angle chips next to its action row; clicking one types the variant into X's reply box and flips the radar draft to a measured `reply_drafts` row `posted`. (a) `shared/variantChips.ts` (new, pure): `variantChipPreview(text, max=60)` (collapse whitespace → clip with `…`) + `isReplyVariants(v)` (NON-empty `{text, angle}[]` shape guard, angle a loose string — looser-client-cache tolerance) — bun-tested (7). (b) `shared/messages.ts`: `RadarVariantsGet {tweetId}` (resp `{ok, variants, draftId}`) + `RadarVariantPasted {tweetId, text}` + `isRadarVariantsGet`/`isRadarVariantPasted` guards (mirror `isRadarConfirm`); +4 guard tests in `messages.test.ts`. (c) `background.ts`: import both guards + `isReplyVariants`; `getVariants(tweetId)` reads the session buffer first (`isReplyVariants(hit.variants)`), else `GET /x/radar/drafts?tweetId=` → `.drafts[0]` (newest, RU.5 non-expired filter) with `variants ?? [{text:replyText, angle}]`; `pasteVariant(tweetId, text)` POSTs the idempotent confirm (RU.5 — covers deep links that skipped the panel), reads `id`+`replyText` (the primary) off the returned reply_drafts row, then PATCHes `/x/replies/:id {status:'posted', ...(text!==primary?{replyTextEdited:text}:{})}` — `copied→posted` is an allowed transition, a 2nd chip click no-ops the status + re-PATCHes replyTextEdited; both dispatch branches after `isRadarConfirm`, best-effort §7.8 warn-only. (d) `content.ts`: `syncVariantChips(focusedId)` from `scan` (after `syncContextPanel`) — for the focused article, `requestVariants` once/tweetId (`variantCache` Map + `variantFetchInFlight` guard, `scheduleScan()` on resolve), when present `injectVariantChips` via `actionRow.appendChild` (own `variantChipsHandled` WeakSet — the `attachReplyMasterButton` discipline, chosen over article-afterend precisely to avoid an orphan + a collision with AX.5's panel); chip click → `clearReplyEditor` (selectAll+delete only if non-empty) + `typeTextInto(findReplyEditor())` (clipboard fallback + hint span), `data-active` marker, then `stratus/radar-variant-pasted`. `.stratus-variant-*` CSS in `injectStyles`. Posting stays manual (§7.28). No server/schema/route change — consumes RU.5's confirm + tweetId-filter + the `copied→posted` transition as-is. Tests: `variantChips.test.ts` (7) + `messages.test.ts` (+4 guards); content.ts DOM behavior is browser-unverified by convention. Gates: canonical `bun run test` **1014/0** (+11), root+extension typecheck clean, extension `vite build` green, `bun run lint` 0-error (3 changed files biome-formatted). **Done-when tails needing live use:** a radar-drafted tweet page shows 3 chips + a chip click types the variant + flips to posted (Replies tab); a deep-linked tweet (panel never clicked) works via the idempotent confirm. Cost: $0. **Advances the D9 content.ts chain** (AX.3✓→AX.5✓→RU.7✓→RU.8→NT.5→HV.2→A3.3). **Next:** RU.8 (manual add-to-radar ⊕, deps RU.4✓+RU.7✓, xhigh — same content.ts lane) or RU.9 (Playbook source attribution, dep RU.2✓, high, server, parallel). **Sha reconciliation (D67):** AX.6's recorded `b3fd7b3` was an amend-orphan (`merge-base --is-ancestor`→false, `cat-file -t` finds it, same "(AX.6)" subject as HEAD); real AX.6 = `0e08af9` (= HEAD at RU.7 start, parent `5e31b8d`=AX.5). No out-of-skill work; ledger no drift.
- 2026-07-21 `b3fd7b3`→**`0e08af9`** — **AX.6** sha reconciliation (D67): recorded `b3fd7b3` was an amend-orphan; real AX.6 = `0e08af9` (= HEAD at RU.7 start, parent `5e31b8d`=AX.5). No out-of-skill work; ledger no drift.
- 2026-07-21 `b3fd7b3` — **AX.6** (augmented X UI — chip/panel dossier click-through; deps AX.3✓+AX.5✓, high, $0, extension-only): 4 files, no new tests (guards trivially typed — the task block says skip; no message-guard suite for these). A timeline person chip or the tweet-page context-panel header now opens that person's dossier in the side panel. (a) `shared/messages.ts`: `OpenPerson {type:'stratus/open-person', handle}` + `OpenPersonClear` + `isOpenPerson`/`isOpenPersonClear` guards (mirror `isRadarConfirm`). (b) `background.ts`: import both guards; new `OPEN_PERSON_KEY='stratus:openPerson'` + its own `enqueueOpenPerson` chain (background stays the single `chrome.storage.session` writer); on `OpenPerson`→`chrome.sidePanel.open({tabId: sender.tab?.id})` inside a `.catch` (user-gesture credit best-effort survives one hop, Chrome ≥116) then writes `{handle, at}` through the enqueue; on `OpenPersonClear`→removes the key. Renamed the onMessage listener's `_sender`→`sender` (now used). (c) `App.tsx`: `useEffect` (empty deps — only stable setters referenced) reads `stratus:openPerson` on mount + `chrome.storage.onChanged` (session area), routes via `setPersonHandle(handle.replace(/^@/,'').toLowerCase())`+`setTab('people')` (openPerson's body inlined so deps stay exhaustive), then fires `stratus/open-person-clear` — the key is a handoff cell, cleared immediately so a later panel open can't replay it. (d) `content.ts`: import `OpenPerson`; new `sendOpenPerson(handle)` helper; the AX.3 chip button's no-op click and the AX.5 panel-title click (`.stratus-ctx-title[data-handle]`, already `cursor:pointer`) both call it (preventDefault+stopPropagation so X never navigates). The collapse toggle is a header sibling (not a title child), so a toggle click never doubles as an open. Gates: canonical `bun run test` **1003/0** (unchanged — no new tests), root+extension typecheck clean, extension `vite build` green, `bun run lint` 0-error (one biome format fix — `onChanged` params on one line). **Done-when tail needing live use:** in the loaded unpacked extension, clicking a timeline chip (panel closed → opens on the dossier; open → tab switches) and the status-page panel header both land on the right person; replayed opens don't recur. Cost: $0. **Next (D9 content.ts chain):** RU.7 (on-page variant chips, xhigh).
- 2026-07-21 `0eb5319`→**`5e31b8d`** — **AX.5** sha reconciliation (D66): recorded `0eb5319` was an amend-orphan (`merge-base --is-ancestor`→false, `cat-file -t` finds it, same "(AX.5)" subject as HEAD); real AX.5 = `5e31b8d` (= HEAD at AX.6 start, parent `39fecb6`=AX.3). No out-of-skill work; ledger no drift.
- 2026-07-21 `0eb5319` — **AX.5** (augmented X UI — tweet-page stratus context panel + legacy reply-master kill; deps AX.3✓+AX.4✓, xhigh, $0, extension-only): 1 file (`extension/src/content.ts`) + codemap + STATE. **(a) Legacy-button kill:** `#reply-master-btn { display:none !important; }` added to `injectStyles` with a why-comment — the purple sparkle circle is the RETIRED standalone `~/newme/clipx/reply-master` extension's button (NOT stratus); real fix is uninstalling it in `chrome://extensions` (**remind the user at ship**), this rule only keeps it hidden if re-enabled. **(b) Dossier cache:** module `dossierCache: Map<handle,{state,dossier,at}>` + `dossierInFlight` guard, `DOSSIER_TTL_MS=5min`; `refreshDossier(handle)` fetches `GET /x/people/{handle}` via `ApiRequest` → **3 cached terminal states** so a status page never fetch-loops: `ready` (dossier), `missing` (HTTP 404 → muted line), `unavailable` (unconfigured/network → render nothing); `getDossier` returns the last cached entry (stale-shown-while-refreshing) and its `.finally` calls **`scheduleScan()`** — the one deviation from the sync glance pattern, since a status page can be mutation-quiet and nothing else re-renders once the async dossier lands. **(c) `syncContextPanel(focusedId)`** once/scan (after the article loop, beside `syncAuthorButton`): `!focusedId` → remove any panel; else find the focused article via `findPermalink().tweetId===focusedId` (same guard as `attachReplyMasterButton`) + its lowercased handle; `unavailable`/loading → remove & return; else a `data-renderKey` (`state:tweetId:handle:cachedAt`) skip-rebuild guard that also requires the panel to still be the article's `nextElementSibling` (SPA-nav + stale-tweet teardown, rebuild-on-data-refresh); insert via `focusedArticle.insertAdjacentElement('afterend', panel)` — the article's **parent flow** (a sibling isn't cloned when X recycles the article; the action row is the article's last block so it reads as "below the action row"). **(d) `renderContextPanel`** from `buildTweetContextModel` (AX.4): header = stage chip (reuses AX.3 `.stratus-person-chip[data-tone]`, sub-engaged→neutral `engaged` tone) + "in your circles · Nd" + collapse toggle `▾`/`▸`; body = followers/momentum, tags, "already replied" banner, exchanges, open loops (≤3), last replies (≤3, views/profile/angle), best angle, notes — each hidden when empty; `renderMissingPanel` → one muted `No stratus file on @handle`; `title.dataset.handle` stamped for AX.6. **(e) Collapse:** `contextCollapsed` module var, `initContextCollapsed()` in `start()` reads `chrome.storage.local['augment:contextCollapsed']` once, toggle flips+persists, applied via `data-collapsed` (CSS hides `.stratus-ctx-body`). **(f) Styles:** native-feel `.stratus-context-panel` (border-top rgba(113,118,123,0.25), system font, 12px 16px pad, `color:inherit`) + header/meta/tags/banner/row/label/list/outcome/notes/empty classes. **No new tests** (content.ts untested by convention; logic is AX.4's bun-tested `tweetContext.ts`). Gates: `bun run test` **1003/0**, root+extension typecheck clean, `bun run lint` **0 errors** (content.ts biome-formatted; the 2 pre-existing `Replies.tsx` a11y errors are now gone — the Wave-1 verification pass fixed them), extension `vite build` green. Cost: $0 (one dossier GET per author per 5 min). **Advances the D9 content.ts chain** (AX.3✓→AX.5✓→RU.7→RU.8→NT.5→HV.2→A3.3). **Browser-verify tail deferred** (D64/D65): the live unpacked-extension walk — panel renders/collapses/survives SPA nav, kill rule with the legacy ext enabled-then-removed, unknown-author muted line — isn't scriptable here. **Sha reconciliation (D65):** AX.3's recorded `bc2c9fe` was an amend-orphan (`merge-base --is-ancestor` → false, `cat-file -t` finds it, same "(AX.3)" subject as HEAD); real AX.3 = `39fecb6` (= HEAD at AX.5 start, parent `7ecb8df` = AX.4), no out-of-skill work, ledger no drift. **Next:** AX.6 (chip/panel click-through → `stratus/open-person`; deps AX.3✓+AX.5✓, high) or RU.7 (on-page variant chips, deps RU.5✓+RU.6✓+AX.5✓, xhigh — same content.ts lane); RU.9 (Playbook source attribution, dep RU.2✓, high, server) still parallel-unblocked.
- 2026-07-21 `bc2c9fe`→**`39fecb6`** — **AX.3** (augmented X UI — timeline person chips replace the band stats pill; deps AX.1✓+AX.2✓, xhigh, $0, extension-only): 1 file (`extension/src/content.ts`) + codemap + STATE. **(a) Killed the green stats badge:** deleted `renderBandBadge` + the `BAND_BADGE_CLASS` const + its style rules + the badge call in `applyBand`; **kept** `readTweetSignals`, `article.dataset.stratusBand` (the hot/warm border + skip dim), and `recordRadarSighting` **byte-identical** — the radar stream and Reply-Master signals path are untouched (only the badge render is gone). Now-unused `BAND_LABEL`/`formatCount`/`Band` imports dropped (unused-import lint caught them). **(b) Glance cache:** `let glanceCache/{map,at}` + `glanceInFlight` promise guard; `refreshGlanceMap()` fetches `GET /x/people/glance` through the background `ApiRequest` (channels-cache shape, `unconfigured`/error → silent, keeps stale); `getGlanceMap()` is SYNC (scan is sync) — returns the cached-or-empty map now and kicks a background refresh when the `GLANCE_TTL_MS` window expires (fresh map lands next scan). **(c) `applyPersonChips(article, glance)`** called from `scan` beside `applyBand`: handle via `findPermalink().username.toLowerCase()` → `glance[handle]` → `buildPersonChips(entry, Date.now()).slice(0,4)`; upserts one `span.stratus-person-chips` inserted `afterend` of `[data-testid="User-Name"]` (the outer author — first match; quote previews nest below), each chip a `.stratus-person-chip` `<button>` with `data-tone`/`data-handle`/`title` + a no-op `stopPropagation` click (AX.6 wires the dossier open); recompute-per-scan with a `data-handle`+`data-sig` skip-guard (recycled-node discipline — same as the old badge; unknown handle or missing header row clears any recycled span). **(d) Styles:** `.stratus-person-chips` (inline-flex, gap 4, margin-left 4, flex-shrink 0, `overflow:hidden` so the header never wraps) + `.stratus-person-chip` (10–11px pill, `all:unset`) + 6 `[data-tone="…"]` classes (ally/mutual/responded/engaged/target/warn) theme-neutral rgba on 0.10 fills — the AX.2-gotcha-mandated tone→CSS mapping, replacing the deleted badge styles. **No new tests** (content.ts untested by convention; chip logic is AX.2's bun-tested `glance.ts`). Gates: `bun run test` **1003/0**, root+extension typecheck clean, `bun run lint` 0 errors (content.ts clean), extension `vite build` green (content.js 72.85 kB — `glance.ts` inlined into the IIFE). Cost: $0 (one glance GET per 10 min per tab). **Opens the D9 content.ts chain** (AX.3→AX.5→RU.7→RU.8→NT.5→HV.2→A3.3). **Browser-verify tail deferred** (D64): the live unpacked-extension walk — pill gone / chips on a known engaged+ person / no dupes after heavy scroll / the `User-Name` insertion point on timeline+status+replies — isn't scriptable here (like AX.2/ME.4/N.7). **Sha reconciliation (D64):** AX.4's recorded `7abec86` was an amend-orphan (`merge-base --is-ancestor` → false, same "(AX.4)" subject as HEAD); real AX.4 = `7ecb8df` (= HEAD at AX.3 start), no out-of-skill work, ledger no drift. **Next:** AX.5 (status-page context panel + legacy-button kill, deps AX.3✓+AX.4✓, xhigh — same content.ts lane, consumes AX.4's `tweetContext.ts`); RU.9 (Playbook source attribution, dep RU.2✓, high, server) still unblocked in parallel.
- 2026-07-21 `7abec86` — **AX.4** (augmented X UI — the tweet-page context-panel view-model; dep none-listed [parallel-ok], high, $0, extension-only): 2 new files (`extension/src/shared/tweetContext.ts`, `.test.ts`). Pure, dependency-free (inlined into the content IIFE): dossier response types (only the consumed fields — `person`/`events`/`replies.outcomes`/`angles`/`mentions`/`followerSeries`, ALL timestamps ISO strings since consumed as HTTP JSON) + `buildTweetContextModel(dossier, tweetId, nowMs) → TweetContextModel {header, relationship, alreadyReplied, openLoops, outcomes, anglePreference, notes}`. Derivations: header stage/`sinceDays`[floor(firstSeenAt→now)]/followers/`momentumPerDay`[followerSeries first→last, `delta/Math.max(days,1)` round2 — mirrors `authorMomentum`; null <2 pts]/tags[`??[]`]; relationship inbound/outbound COUNTS from `events` (`their_mention`+`their_reply_to_me` vs `my_reply`), last dates from `people` watermarks `lastInboundAt`/`lastOutboundAt` (D63 — counts from events, watermarks for dates); `alreadyReplied` = most-recent posted outcome whose `sourceTweetId===tweetId`, `ageMin` off `postedAt ?? draftCreatedAt`; openLoops = unanswered `mentions`, oldest-debt-first; outcomes = measured-only (`outcome!==null`) reply outcomes newest-first ≤3, text snippet 140ch, `angle` OPTIONAL (dossier does NOT emit per-outcome angle today → reads null, forward-compat, D63); `anglePreference` re-implements `pickAnglePreference` inline (gate total-measured ≥`MIN_MEASURED_FOR_ANGLE_PREFERENCE=3`, best median profileVisits, views tie-break, sample-size final) returning `{angle, measured}`; notes `.trim() || null`. Null-safe throughout (hover-only person → header + empty sections, no crash). 15 bun tests (rich exact-numbers happy path / thin person / gate 2→null 3→set + split-cell 2+1=3 / alreadyReplied hit+miss+unlinked / momentum single→null + same-day clamp=50 / open-loop unanswered-only oldest-first / cap-3-newest). Gates: canonical `bun run test` **1003/0** (+15), root+extension typecheck clean, `bun run lint` 0 errors (biome auto-format applied to both files), extension `vite build` green. Cost: $0. **Consumed by AX.5** (status-page DOM plumbing — one dossier fetch/handle, 5-min cache, renders this model). **Next:** AX.3 (timeline revamp — kill stats pill, inject chips into content.ts; deps AX.1✓+AX.2✓, xhigh) opens the D9 content.ts chain (AX.3→AX.5→RU.7→RU.8→NT.5→HV.2→A3.3). RU.9 (Playbook source attribution, dep RU.2✓, high, server) still unblocked in parallel. **Sha reconciliation (D62):** AX.2's recorded `e29b629` was an amend-orphan (`merge-base --is-ancestor` → false, same "(AX.2)" subject as HEAD); real AX.2 = `6c46a56` (= HEAD at AX.4 start), no out-of-skill work, ledger no drift.
- 2026-07-20 `e29b629` — **AX.2** (augmented X UI — glance chip view-model core; dep AX.1✓, high, $0, extension-only, parallel-ok): 2 new files (`extension/src/shared/glance.ts`, `extension/src/shared/glance.test.ts`). Pure, dependency-free (inlined into the content IIFE): `GlanceEntry`/`GlanceMap` typed after AX.1's JSON (`stage` kept `string` not a union — radar's `RankMapEntry` tolerance pattern), `NEGLECT_DAYS=7` (= `NEGLECTED_TARGET_DAYS`), `GLANCE_TTL_MS=10*60_000` (content-cache TTL, channels pattern), and `buildPersonChips(entry, nowMs) → PersonChip[]` (`{kind:'stage'|'target'|'owed'|'neglected', label, tooltip, tone:'ally'|'mutual'|'responded'|'engaged'|'target'|'warn'}`). Rules: **stage** chip only ≥engaged (hardcoded 6-el `STAGE_ORDER`, `ENGAGED_RANK=2`, **no server stage.ts import** — the plan's explicit constraint), label = stage word, tone = the stage (all ≥engaged stages ∈ tone union); **target** `◎` when isTarget, tooltip "2–10x target roster"; **owed** `↩ n` when openLoops>0, tooltip pluralized "n unanswered mention(s) from them"; **neglected** `Nd` when eligible (isTarget OR ally/mutual) AND (`Date.parse(lastOutbound) < now−7d` [strict `<`, mirrors followups.ts cutoff — exactly 7d NOT neglected, 8d is] OR null-outbound-with-inbound-present [a dropped inbound; a never-touched target with no inbound is NOT neglected → avoids decorating the whole roster]), `N`=days since outbound (or since inbound in the never-replied branch), tooltip "no reply from you in Nd", tone warn. Order fixed: stage/target/owed/neglected. Tests (13, 38 asserts): stage gate (below/at/above engaged + unknown stage), target-only-when-flagged, owed count+singular/plural, neglect 7d-vs-8d boundary + null-outbound+inbound + null-both-not-neglected + ally/mutual eligible without target + non-target-stranger/engaged never neglected, chip order, cold handle → []. Gates: canonical `bun run test` **988/0** (+13), root+extension typecheck clean, extension `vite build` green, `bun run lint` 0 errors. Cost: $0. **Next:** AX.4 (`shared/tweetContext.ts` view-model, dep none-listed, high, parallel-ok) or AX.3 (timeline revamp — kill stats pill, inject chips into content.ts; deps AX.1✓+AX.2✓, xhigh) — AX.3 opens the D9 content.ts chain (AX.3→AX.5→RU.7→RU.8→NT.5→HV.2→A3.3). RU.9 (Playbook source attribution, dep RU.2✓, high, server) still unblocked in parallel.
- 2026-07-20 `f1d85c2` — **AX.1** (augmented X UI — the timeline-decoration endpoint; dep none, high, $0, server-only, parallel-ok): 3 files (`src/x/routes/people.ts`, `src/x/routes/people.test.ts`, new `scripts/smoke-glance.ts`). New **`GET /people/glance`** registered immediately after `/people/rankmap` and **before `/people/:handle`** (§7.20 static-path-wins — copies rankmap's NOTE comment). Three parallel queries → a `{count, map: {handle→GlanceEntry}}` map keyed lowercased: (a) `people` where `retired=false` → `stage`/`lastOutboundAt`/`lastInboundAt`/`followersCount`; (b) `mentions` where `status='unanswered'` AND `authorUsername` not-null, `group by lower(author_username)` → `openLoops` count (§7.13 sql template, no Date binds); (c) `loadTargetHandles()` → `isTarget` + a bare `{stage:'stranger',isTarget:true,openLoops:0,...nulls}` backfill for in-band targets with no people row (same as rankmap). Dates emitted ISO via `.toISOString() ?? null` (map value type stays clean `string|null`). Richer than rankmap (all non-retired people, +open loops/recency/followers) but same $0 pure-SQL shape; rankmap left untouched (feeds radar tiering). Local non-exported `GlanceEntry` interface. Tests: new `describe('GET /people/glance')` over the in-memory DB — seeds its own account snapshot (1000→band 2000–10000, independent of rankmap's), a non-retired `mutual` person + a retired one + an in-band voice-target + one unanswered/one answered mention (mixed-case author exercises `lower()`); asserts stage/isTarget/openLoops=1/lastOutboundAt-ISO/followersCount, retired absent, bare-target backfill exact, and 200-not-404 (static path beats `:handle`). Smoke `scripts/smoke-glance.ts` (imitates smoke-targets): in-process router over the REAL DB, seed→assert→delete, idempotent cleanup on entry + every exit. Gates: `bun run test` **975/0** (+1), `bun run typecheck` clean, `bun run lint` 0 errors, `bun run scripts/smoke-glance.ts` → SMOKE OK. **D60** (sha reconciliation: RU.6's recorded `6261673` was an amend-orphan — `merge-base --is-ancestor` false, `cat-file -t` finds it, same "(RU.6)" subject as HEAD; real RU.6 = `1ed57bd` = HEAD at AX.1 start; the 3 preceding commits are RU.5/RU.4/`3fc2bbc`[out-of-skill, D57], no drift). **Next:** AX.2 (`shared/glance.ts` chip view-model, dep AX.1, high, parallel-ok) or RU.9 (Playbook source-exact attribution, dep RU.2✓, high, server). AX.4 (tweetContext view-model, parallel-ok) also unblocked. content.ts chain (D9: AX.3→AX.5→RU.7→…) still gated behind AX.2/AX.4/AX.5.
- 2026-07-20 `6261673`→**`1ed57bd`** — **RU.6** (radar↔reply unification — panel click → confirm wiring; deps RU.4✓+RU.5✓, high, $0, extension-only): 5 files (1 new: `extension/src/shared/messages.test.ts`). Wires the Radar panel's row-open to RU.5's confirm endpoint so a click promotes the radar draft into a real `reply_drafts` row server-side. (a) `shared/messages.ts`: `RadarConfirm {type:'stratus/radar-confirm', tweetId}` + `isRadarConfirm` guard (mirrors `isRadarClick`). (b) `shared/radar.ts`: `RadarSighting.draftId?: string` — the confirmed reply_drafts id, survives `mergeSightings` via the same `s.x ?? prev.x` + `!== undefined` guard as `reply`/`variants`/`clickedAt`. (c) `background.ts`: import `isRadarConfirm`; new `confirmDraft(tweetId)` POSTs `/x/radar/drafts/${tweetId}/confirm` (RU.5 idempotent — best-effort §7.8, ignores `unconfigured`, warns otherwise), extracts `res.data.id`, then `enqueueRadar(() => stampDraftId(tweetId, id))` (single writer, markClicked pattern — writes the id onto the sighting, evicted rows skipped); dispatch branch after `isRadarClick` (`{ok:true/false}`). (d) `Radar.tsx`: import `RadarConfirm`; new `confirmDraft(tweetId)` sends the message; `RadarRow.onOpen` calls it right after `markClicked(s.tweetId)` (existing clipboard-copy-of-primary unchanged). No server/schema/route change — consumes RU.5's always-mounted `POST /x/radar/drafts/:tweetId/confirm` as-is (returns the full `reply_drafts` row directly, so `.id` is top-level). Tests: new `messages.test.ts` (`isRadarConfirm` accepts a well-formed msg, rejects wrong-type/missing-tweetId/non-string/junk — first message-guard suite, none existed); `radar.test.ts` +1 (draftId survives re-sighting merge like `reply`). Gates: canonical `bun run test` **974/0** (+3), root+extension typecheck clean, extension `vite build` green, changed-file lint clean. **Sha reconciliation (D59):** RU.5's recorded `5935107` was an amend-orphan (`merge-base --is-ancestor` → false, `cat-file -t` finds it, same "(RU.5)" subject as HEAD) — real RU.5 = `983fbea` (= HEAD at RU.6 start), no out-of-skill work, ledger no drift. **Done-when tail needing live use:** clicking a ready Radar row in the loaded extension creates the `reply_drafts` row (`GET /x/replies?status=copied`) and stamps `draftId` in the buffer. **Binds RU.7** (paste→posted PATCHes that `draftId`). Cost: $0. **Next:** AX.1 (glance endpoint, high, server, parallel-ok) or RU.7 (on-page variant chips — needs RU.5✓+RU.6✓+AX.5, so AX.1→AX.5 first).
- 2026-07-20 `5935107` — **RU.5** (radar↔reply unification — confirm endpoint promotes radar drafts to measured reply drafts; dep RU.2✓, xhigh, $0, server-only): 2 files (`src/x/routes/radar.ts`, new `src/x/routes/radar.test.ts`). (a) `GET /radar/drafts` accepts `?tweetId=` (validated `TWEET_ID_RE` → 400 `invalid_tweet_id`); status handling now: an explicit `status` wins, else a `tweetId` query returns that tweet's whole **non-expired** history (`ne(status,'expired')` — the on-page variants-get / chip fallback needs a confirmed [clicked] tweet's variants), else the bare list keeps its `ready` default; lazy 48h expiry still runs first. (b) **`POST /radar/drafts/:tweetId/confirm`** (always-mounted, $0): 400 `invalid_tweet_id` → find the newest non-expired radar row for the tweet (404 `not_found` if none) → **idempotent** if `reply_draft_id` set (fetch the linked `reply_drafts` row → 200; a dangling link where the reply_drafts row was deleted falls through to rebuild) → else build a `PostContext` contextSnapshot from the row (band from the `band` column merged with the `signals` JSON [`TweetSignals` has no band]; `metrics.{views,replies}` from signals + reposts/likes 0; `topComments []`; `postedAt = draftedAt − signals.ageMin·60000`, or `draftedAt` ISO when signals null; `url` = row.url ?? `https://x.com/<handle>/status/<id>`) and, in ONE sync txn (§7.13 — `db.transaction`, `.all()`/`.run()`, no await inside), insert the `reply_drafts` row (`source:'radar'`, `status:'copied'`, `variants` fallback `[{text:replyText,angle}]`, `model` `?? 'radar-batch'` to satisfy NOT NULL, `costUsd:null`, `sourcePostedAt` = the derived Date or null) + stamp `reply_draft_id` + ratchet radar status→`clicked`; return 201 on fresh create, 200 on idempotent. The `my_reply` person event + posted flip stay on `PATCH /replies/:id` (§7.8 — no duplicate hook); `copied→posted` already in `ALLOWED_TRANSITIONS` so the created row is PATCHable to posted (RU.7). New route suite `src/x/routes/radar.test.ts` (7 tests) over the in-memory DB. Gates: canonical `bun run test` **971/0** (+7), `bun run typecheck` clean, whole-repo `bun run lint` 0 errors. **D58** (sha reconciliation: RU.4's recorded `a3664af` was an amend-orphan; real RU.4 = `22cf0b1` = HEAD at RU.5 start, no out-of-skill work, ledger no drift) + two decisions (non-expired default for `?tweetId=`-without-status per the Design's fuller wording, closing RU.7's deep-link-after-confirm gap; 201-fresh / 200-idempotent). **Next:** RU.6 (panel click→confirm wiring, dep RU.4✓+RU.5✓, high, extension) or AX.1 (glance endpoint, high, server, parallel-ok).
- 2026-07-20 `a3664af` — **RU.4** (Wave 2 opener, radar↔reply unification — extension buffer carries reply variants; dep RU.3✓, high, $0, extension-only): 7 files (radar.ts, radar.test.ts, messages.ts, types.ts, background.ts, Radar.tsx, styles.css). RU.3 already made the batch route return `variants: ReplyVariant[]` per tweet — this threads them client-side so RU.7's on-page picker has data. `shared/types.ts` `BatchReplyItem.variants: ReplyVariant[]` (required — the server always returns all 3). `shared/messages.ts` `RadarReplies.replies[].variants?: ReplyVariant[]` (optional; imports `ReplyVariant` from `./types.ts` — no cycle, types.ts imports only replyBand). `shared/radar.ts`: imported `ReplyVariant`; `RadarSighting.variants?` (survives `mergeSightings` via the same `s.x ?? prev.x` + `!== undefined` guard as `reply`; `reply` stays primary = variants[0].text); `RadarDraftRow.variants: ReplyVariant[] | null` (required-nullable, matching the server's `db.select()` row shape — `GET /x/radar/drafts` already returns the RU.2 column); `draftRowToSighting` maps `variants` only when non-null/non-empty (a null-variants row rehydrates with NO variants key, so the pre-existing `toEqual` fixture stays green). `background.ts` `attachReplies` param widened + stores variants (guarded on presence; `ReplyVariant` added to the `./shared/types.ts` import). `Radar.tsx` `draftReplies` forwards `r.variants` in the `radar-replies` message; a ready row renders a light **"N angles"** hint (`.radar-angles`, muted, next to "reply ready") when `variants.length > 1` — the full picker is RU.7 on the tweet page, panel stays light. **Scope: strictly variants** — the `manual` band widening + `draftId`/confirm flow are RU.5/6/8, not touched here. Tests: `radar.test.ts` +4 (variants survive re-sighting merge / fresh incoming set wins / rehydrate maps the 3 variants / null-variants row rehydrates without a variants key); `RadarDraftRow` fixture gained `variants: null`. Gates: canonical `bun run test` **964/0** (after a required `bun install` — `@modelcontextprotocol/sdk`+`zod`+`happy-dom` were declared-but-uninstalled in this env, giving 4 pre-existing dep-resolution fails until installed; +4 my new tests), root+extension typecheck clean, extension `vite build` green, `bun run lint` clean. **Reconciliation (D57):** HEAD had advanced 6 commits past AI.13 (`f003e8f`, not the amend-orphan `f86e06a` STATE recorded) via out-of-skill work — verification pass `b920507`, a metrics-backfill feature (`src/x/backfill.ts` + `POST /x/posts/backfill`, `d376ffc`+`aff3bb9`+`dc58567`), evals docs `cbeecf1`, Studio tweaks `6bce793`+`3fc2bbc`; none touch RU.4 files, ledger no drift. **Next:** RU.5 (confirm endpoint `POST /x/radar/drafts/:tweetId/confirm` + `?tweetId=` filter — server, xhigh, dep RU.2✓) or AX.1 (glance endpoint, high) — both unblocked; RU.6 needs RU.4+RU.5.
- 2026-07-19 `f86e06a` — **AI.13** (Wave 1 FINAL, AI-layer — docs-sync + $0 smoke; deps all AI✓, high, no code/schema/route change): new **`scripts/smoke-ai-layer.ts`** — mounts `llm`/`promptsRouter`/`drafter`/`ideasRouter` in-process against the REAL DB and exercises the whole AI surface at $0: (1) AI-settings round-trip over `app_settings` 'ai' + per-field 400s + openrouter-without-key **409** (env unset in-process, then restored); (2) prompt override → `loadPrompt` marker → single reset → **restore-defaults** + missing-placeholder 400 + unknown-key 404; (3) new-surface refuse-before-spend — rewrite/ideas/draft-thread input 400s + the no-key **503** (both `XAI_API_KEY`+`OPENROUTER_API_KEY` deleted so `askLLM` throws `LlmNotConfiguredError` before any network; draft-thread/ideas accept 503 OR 409 `no_pillars_for_niche` — both $0); (4) `buildModelEffectiveness` bucketing/gate/partition shape (pure). **DB-pristine discipline:** snapshots the `ai` settings row + ALL `prompt_overrides` rows up front and restores them on exit (restore-defaults would otherwise wipe a real customization) — verified the row/overrides count is unchanged after a run. `--live` (needs `OPENROUTER_API_KEY`): ONE real `askLLM({provider:'openrouter'})` asserting text back + a `cost_events` row under platform `'openrouter'`. Docs-sync: CLAUDE.md new §"AI layer" phase entry (env-keys-never-in-DB, the reply-batch slicing retirement, the `llmConfigured()` gate flip, 10-key override-rows registry, per-provider structured outputs, the `openrouter` cost platform), PLAN.md AI-layer pointer blockquote, plan Status→shipped, and the four tab docs (`settings-tab.md` General·AI·Prompts subtabs, `composer-tab.md` Improve-with-AI + Draft-thread-with-AI + provider note, `ideas-tab.md` Generate-ideas-with-AI as capture path #3, `playbook-tab.md` Model-effectiveness section). `.env.example` already carried `OPENROUTER_API_KEY`/`OPENROUTER_DAILY_BUDGET_USD` (AI.1) — verified, no change. §9 smoke-list gains `ai-layer`. Gates: canonical `bun run test` green, typecheck clean (scripts in tsconfig include), smoke passes $0 leaving the DB pristine. **This closes Wave 1** (RU.1–3 + N.1–9 + ME.1–7 + AI.1–13 all done). Cost: $0.
- 2026-07-19 `9c59183` — **AI.12** (Wave 1, AI-layer — Playbook model-effectiveness cell; dep none-strict/after AI.6✓, high, $0, server+extension lane): 5 files, no new file. `src/x/playbook.ts` §11 `buildModelEffectiveness(rows: ModelRow[], minN=DEFAULT_MIN_CELL_N)` — the S0.5 `buildLatencyEffectiveness` exemplar copied, but buckets are **dynamic** (model strings, not a fixed const list) and there is **no lift line** (no canonical baseline pair): groups posted+measured replies by the raw `model` string via a `Map`, emits `ModelCell[]` (`OutcomeCell` + `model`) sorted **most-sampled-first** (`b.n - a.n || b.posted - a.posted || model.localeCompare` for a deterministic order) + `totalMeasured`. Model ids grouped **as-is** — `anthropic/claude-sonnet-4.5`'s `/` reads as the provider, never parsed. `routes/playbook.ts`: `model` added to `ReplyRow` + the `loadReplyRows` select (`replyDrafts.model`, `notNull`) + the row map, new `toModelRows(rows)` mapper beside `toLatencyRows`, wired into `GET /x/playbook` as `modelEffectiveness` (the `minN` knob flows through), `buildModelEffectiveness`/`type ModelRow` imported. Extension: `shared/types.ts` `PlaybookModelCell extends PlaybookCell {model}` + `Playbook.modelEffectiveness: {cells, totalMeasured}`; re-exported through `api.ts` (import + re-export blocks, Latency pattern); a **"Model effectiveness"** section in `Playbook.tsx` (after latency, before RosterCoverage) — `cellSummary`-driven table (model/posted/result), silent-until-gated `.pb-note`, "No posted replies yet" empty state. Tests: `playbook.test.ts` `buildModelEffectiveness` (bucketing/provider-slash-kept/most-sampled-first/partition-invariant Σn=totalMeasured & Σposted=rows/per-bucket gate/default-20-silent) + `routes/playbook.test.ts` route bucketing (both seeded posted replies carry `model:'test'` → one bucket n=2 medianViews 250, partition invariant). Gates: canonical `bun run test` **952/0** (+6), root+extension typecheck clean, extension `vite build` green, changed-file lint clean (**the 2 `Replies.tsx` a11y errors are pre-existing on clean HEAD, NOT this change** — I touched only Playbook.tsx/types.ts/api.ts, no dialogs). **STATE sha reconciliation (D55):** AI.11's recorded `e7d5be9` was an amend-orphan (`merge-base --is-ancestor` → false, `cat-file -t` finds it, same subject "feat(extension): prompts editor panel (AI.11)" as HEAD) — real AI.11 = `a8316d6` (= HEAD at AI.12 start, parent `9be4b0a` = AI.10). **Done-when tails needing live use:** a model bucket reaching n≥20 to render a real median (months away — every measured reply seeds `model:'test'` in tests, and the OpenRouter rows only start existing once AI.10's Settings picks a non-Grok provider). Cost: $0 — pure SQL over already-billed data. **Next:** AI.13 (AI docs+smoke — `scripts/smoke-ai-layer.ts` $0 + doc-sync; deps all AI — should mention the Prompts editor + the D54 RewriteVariant fix + this model cell).
- 2026-07-19 `e7d5be9` — **AI.11** (Wave 1, AI-layer — Prompts editor panel; deps AI.4+AI.10, high, extension-only, $0): 4 files (1 new: `extension/src/sidepanel/Prompts.tsx`; the plan's "2 files" understates because the types belong in `shared/types.ts` and the placeholder wire is in `Settings.tsx` — same AI.10 precedent, D53). New `Prompts.tsx`: `<PromptsPanel>` loads `GET /x/prompts` → a `.prompt-list` of the 10 keys (name/`.prompt-key`/description, amber `.badge-auto` "customized" chip, edited-at when overridden); clicking a row mounts `<PromptEditor>` (`GET /x/prompts/:key` → monospace `.prompt-body` textarea rows 20, char count, **required-placeholder chips** [`.prompt-token` green / `.prompt-token-missing` red, driven by a local `missingPlaceholders(body, required)` mirroring `validatePromptBody`'s `body.includes` check — **Save greys out while any required token is missing**; the server still 400s `missing_placeholder`], Save → `api.prompts.patch` [updates local `customized`], "Reset this prompt" [confirm → `api.prompts.reset` → reload pulls the shipped default back], "Show default" toggle rendering `defaultBody` read-only). Returning to the list refetches so the chip + edited-at stay accurate. `Settings.tsx`'s placeholder `<PromptsTab>` now renders `<PromptsPanel>` + keeps `<RestoreDefaultPrompts>` in a footer panel (both AI-footer and Prompts-tab reachable, unchanged). `api.ts`: `prompts.{list,get,patch,reset}` alongside `restoreDefaults`; `shared/types.ts` `PromptSummary`/`PromptDetail`/`PromptPatchResult`/`PromptResetResult`; new `.prompt-*` CSS block (list/row/key/token/body/meta — `--strat-*`/existing status tokens only, no literals). No extracted pure helper (the `includes` check is inline — task said extract only if reused) ⇒ no new tests, server enforces. **Also fixed a pre-existing AI.8 gap (D54):** `RewriteVariant` (imported by `Composer.tsx` from `./api.ts` since AI.8) was never re-exported from `api.ts` → `cd extension && bun run typecheck` (`tsc --build`) was red from AI.8 onward (vite's build strips types, so it never surfaced in the build gate); added `RewriteVariant` to api.ts's import + re-export blocks — extension typecheck green again. No server/schema/route change — consumes AI.4's always-mounted `/x/prompts/*` routes as-is. Gates: canonical `bun run test` **946/0** (no new tests), root+extension typecheck clean, extension `vite build` green, changed-file lint clean (**the 2 `Replies.tsx` a11y errors are pre-existing on clean HEAD, NOT this change** — verified: my `Prompts.tsx` had 1 format error, biome-autofixed). **STATE sha reconciliation (D53-pattern):** AI.10's recorded `a91387f` was an amend-orphan (`merge-base --is-ancestor` → false, `cat-file -t` finds it, same subject as HEAD) — real AI.10 = `9be4b0a` (= HEAD at AI.11 start, parent `bc49503` = AI.9). **Done-when tails needing live use:** edit `reply` → Save → chip appears → `GET /x/replies/default-prompt` reflects it → Reset reverts (spot-check against a running server). Cost: $0. **Next:** AI.12 (Playbook model-effectiveness cell — `buildModelEffectiveness` in playbook.ts; dep none-strict/after AI.6✓, high, parallel-ok — server-side lane, different files).
- 2026-07-19 `a91387f` — **AI.10** (Wave 1, AI-layer — Settings AI subtab + Restore Default Prompts; deps AI.2+AI.4, high, extension-only, $0): 4 files (1 new: `extension/src/shared/aiSettings.ts` + its `.test.ts`). `Settings.tsx` gained a `radar-tabs` **`General | AI | Prompts`** subtab shell (Voice.tsx `radar-tab` pattern): **General** keeps `<NicheCard/>` + the existing form unchanged; **AI** is a new `<AiPanel>` — loads `GET /llm/settings` on mount (`currentSettings` is `useMemo`'d over the 8 primitive fields so the on-mount effect doesn't refire each render; the api transport ignores the arg — the background owns Authorization, §7.25), renders a provider radio (**OpenRouter disabled + "set OPENROUTER_API_KEY on the server" hint when `providers.openrouter` false**), a free-text OpenRouter-model input backed by `<datalist id="or-models">` from `GET /llm/models` (fetched only when openrouter-configured; a 503/error leaves the datalist empty, free-text still works), temperature/max-tokens number inputs (blank = surface default), a reasoning-effort select ('' = surface default), Save → `api.llm.patchSettings` (surfaces the server's `invalid_*`/409 codes); **Prompts** is a placeholder `<PromptsTab>` (the per-prompt editor is AI.11). A shared `<RestoreDefaultPrompts>` (confirm() → `api.prompts.restoreDefaults` → "Restored N prompts") mounts on BOTH the AI-panel footer AND the Prompts tab (user asked for it "in settings"). New pure `extension/src/shared/aiSettings.ts` — `aiSettingsToForm`/`aiFormToPatch` (form fields are all strings so blank is representable; blank numeric/effort → explicit **null**, non-numeric temp/non-integer tokens → client-side `invalid_temperature`/`invalid_max_output_tokens` mirroring the server codes) + `pricePerMillion` (per-token USD → $/1M picker label), bun-tested in `aiSettings.test.ts` (11 cases). `api.ts`: new `llm.{getSettings,patchSettings,models}` + `prompts.restoreDefaults` groups; `shared/types.ts` gained `AiSettings`/`AiSettingsResponse`/`AiSettingsPatchBody`/`LlmModel`/`LlmModelsResponse`/`LlmProvider`/`LlmReasoningEffort`/`PromptsRestoreResult`. **No server change** — consumes the AI.2 `/llm/*` + AI.4 `/x/prompts/*` routes as-is. Gates: canonical `bun run test` **946/0** (+11), root+extension typecheck clean, extension `vite build` green, changed-file lint clean (**the 2 `Replies.tsx` a11y errors are pre-existing on clean HEAD, NOT this change**). **STATE sha reconciliation (D53):** AI.9's recorded `8e53621` was an amend-orphan (`merge-base --is-ancestor` → false, `cat-file -t` finds it, same subject "feat(ideas): AI idea generator — prompt, route, Ideas tab flow (AI.9)" as HEAD) — real AI.9 = `bc49503` (= HEAD at AI.10 start, parent `7a5fe9e` = AI.8). **Done-when tails needing live use:** provider/model round-trip against a running server + a real Restore-Default-Prompts click clearing customized chips. Cost: $0. **Next:** AI.11 (Prompts editor panel — new `Prompts.tsx`, `prompts.list/get/patch/reset`; dep AI.4+AI.10, high — same lane, owns Settings.tsx/api.ts).
- 2026-07-19 `8e53621` — **AI.9** (Wave 1, AI-layer — idea generator: prompt + route + Ideas-tab button; deps AI.2+AI.3, high, $0): 7 files (1 new: `src/x/posts/ideasPrompt.ts`). New **TS-only** `IDEAS_PROMPT_TEMPLATE` (Decision 9 — no `.md`/byte-sync; `{{PILLARS}}`/`{{WINNERS}}`/`{{STEER}}` at the cacheable tail, **no persona token** so the cache bucket is niche-independent even though `getActivePillars()` is niche-scoped — the pillars render into the variable tail, not the prefix), `IDEAS_SCHEMA` (static `{ideas:[{text,pillar,angle}]}`, angle enum `observation|stance|story|question`, **`pillar` a FREE string** — not enum-bound — so a mis-tagged pillar is nulled not dropped; **no minItems/maxItems** — the rewrite/thread/N.8/RU.3 rejection-risk convention), `MAX_IDEA_TEXT=500`/`MAX_IDEA_COUNT=10`, `parseIdeaProposals(raw, allowedSlugs, maxCount=10) → IdeaProposal[]|null` (null on malformed `ideas`; empty/over-500-char/unknown-angle entries dropped; out-of-set pillar → **null, not dropped** — the idea survives; clamped to maxCount — array may be `[]`), `buildIdeasInput({pillars?, winners, steer?, count?, template?})` (split/join $-safe; count folds into the `{{STEER}}` block like threadPrompt folds tweetCount; empty winners/steer → placeholder text). Registry key `ideas` (6th position) → **10 keys — the full set** (`registry.test.ts` key list 9→10). New **`POST /x/ideas/generate {steer?, count? (1–10, default 8), model?, provider?}`** on the always-mounted `ideasRouter` → pre-spend guards (`invalid_body`/`invalid_steer` [≤2000]/`invalid_count`/`invalid_model`/`invalid_provider`, all 400 §7.4) → `getActivePillars()` empty → **409 `no_pillars_for_niche`** (drafter discipline) → `topWinners()` few-shot (imported from `drafter.ts` — a safe cycle: the call is deferred to request time, and drafter already imports `consumeIdeaSafe` back from ideas.ts) + `loadPromptSafe('ideas')` → `askLLM` (defaults temp 0.8 / 1400 tok / low; cache key = bare `prompt.cacheKey`, NO niche suffix — no persona in this prompt) → `parseIdeaProposals` null → 502 `ideas_parse_error`, `[]` → 502 `ideas_invalid`, `llmErrorPayload` → 503 `llm_not_configured`; **writes nothing** (the panel saves picked proposals via `POST /ideas` `tags:['ai']`). Extension: `shared/types.ts` `IdeaGenerateBody`/`IdeaProposal`/`IdeaGenerateResponse`/`IdeaAngle`, `api.ideas.generate`, a **"Generate ideas with AI"** section in `Ideas.tsx` (optional steer textarea + button → proposal cards w/ pillar+angle badges → per-row Save-to-inbox [`api.ideas.create` `tags:['ai']`, reference-filter removal + reload] / Skip; `savingText`-keyed disable; no new CSS — reuses `.voice-tweet*`/`.badge`). Tests: `parseIdeaProposals`/`buildIdeasInput` in `src/test.test.ts` (ideas block — tail placement, angle trim, out-of-set-pillar null, over-long/empty/unknown-angle drop, count clamp, malformed→null/`[]`, empty-winners/steer placeholders), 2 route-guard groups in `src/x/routes/ideas.test.ts` (bad body/steer/count/model/provider → 400; both-keys-unset → 503 `llm_not_configured`), `registry.test.ts` 9→10 (+ `ideas` defaultBody + `isPromptKey`). Gates: canonical `bun run test` **935/0**, root+extension typecheck clean, changed-file lint clean (**2 pre-existing `Replies.tsx` a11y errors NOT this change**), extension `vite build` green. **Done-when tails needing live use:** the first browser "Generate ideas" → Save flow, and a real Grok/OpenRouter ideas call (the $0 default never spends). Cost: $0 (all LLM paths pre-network in tests; ~$0.005/click live). **STATE sha reconciliation (D52):** AI.8's recorded `b9fe98d` was an amend-orphan (`merge-base --is-ancestor` → false, `cat-file -t` finds it, same subject "feat(drafter): rewrite assist — prompt, route, Composer variants UI (AI.8)" as HEAD) — real AI.8 = `7a5fe9e` (= HEAD at AI.9 start, parent `b932a06` = AI.7). **Next:** AI.10 (Settings AI subtab + Restore Default Prompts; deps AI.2+AI.4, high).
- 2026-07-19 `b9fe98d` — **AI.8** (Wave 1, AI-layer — rewrite assist: prompt + route + Composer button; deps AI.2+AI.3, high, $0): 9 files (1 new: `src/x/posts/rewritePrompt.ts`). New **TS-only** `REWRITE_PROMPT_TEMPLATE` (Decision 9 — the `.md`/byte-sync convention is kept only for post/reply/thread; the seven smaller templates are TS constants edited via the DB prompt editor), `REWRITE_SCHEMA` (static `{variants:[{text,kind}]}`, kind enum `tightened|rehooked|restructured`, **no minItems/maxItems** — the threadPrompt/N.8/RU.3 strict-structured-output rejection-risk convention), `parseRewrite(raw, maxLen=560)→RewriteVariant[]|null` (null on a malformed `variants` field; empty/over-`MAX_VARIANT_LENGTH`/unknown-kind entries dropped — the array may be `[]`), `buildRewriteInput({draft,instruction?,template?})` (split/join $-safe; `{{DRAFT}}`/`{{INSTRUCTION}}` at the tail; missing instruction → `(none — just sharpen it)`; **no persona/pillar token** → cache bucket unaffected by a niche switch). Registry key `rewrite` (5th position) → **9 keys** (`registry.test.ts` key list 8→9). New **`POST /x/posts/rewrite`** on the drafter router (inherits AI.6's `llmConfigured()` mount gate — no index.ts change) → inline body validation (`invalid_body`/`invalid_text` [1–2000]/`invalid_instruction` [≤500]/`invalid_model`/`invalid_provider`, all pre-spend §7.4) → `loadPromptSafe('rewrite')` → `askLLM` (defaults temp 0.7 / 900 tok / low; cache key = bare `prompt.cacheKey`, NO niche suffix — no persona in this prompt) → **no DB rows** (the Composer applies the picked variant to its own state); `parseRewrite` null → 502 `rewrite_parse_error`, `[]` → 502 `rewrite_invalid`; over-long variants dropped not truncated; NO one-auto-regenerate (unlike the thread route — plan doesn't ask for it). Extension: `shared/types.ts` `RewriteBody`/`RewriteVariant`/`RewriteKind`/`RewriteResponse`, `api.drafts.rewrite`, an **"Improve with AI"** button under the single-post textarea (`!threadMode && !isThreadEdit && !isLocked && text.trim()!==''`) → variant cards reusing `.draft-card` styling → `applyRewrite` sets the textarea + clears (no new CSS). Tests: `parseRewrite`/`buildRewriteInput` in `src/test.test.ts` (rewrite block — tail-position, trim, over-long/empty/unknown-kind drop, 560 inclusive boundary, malformed→null, `[]`, no-instruction placeholder), 5 route guards in `src/x/routes/drafter.test.ts` (empty/oversized text, oversized instruction, unknown provider → 400; both-keys-unset → 503 `llm_not_configured`), `registry.test.ts` 8→9. Gates: canonical `bun run test` **926/0**, root+extension typecheck clean, changed-file lint clean (**2 pre-existing `Replies.tsx` a11y errors are on clean AI.7 HEAD — NOT this change**, verified via `git stash`), extension `vite build` green. **Done-when tails needing live use:** the first browser "Improve with AI" → variant-apply, and a real Grok/OpenRouter rewrite call (the $0 default never spends). Cost: $0 (all LLM paths pre-network in tests; ~$0.003/click live). **Next:** AI.9 (idea generator — `POST /x/ideas/generate`, register key `ideas`, Ideas.tsx button; parallel-ok, owns Ideas.tsx not Composer).
- 2026-07-19 `995a08b` — **AI.7** (Wave 1, AI-layer — thread drafter: prompt + route + Composer button; deps AI.2+AI.3+AI.5, xhigh, $0): 10 files (3 new: `thread prompt.md`, `src/x/posts/threadPrompt.ts`, `src/x/routes/drafter.test.ts`). New `THREAD_PROMPT_TEMPLATE` byte-synced with `thread prompt.md` (post §0–§5 verbatim + a thread-specific job/output; `{{PILLARS}}`/`{{FEW_SHOT}}`/`{{IDEA}}` at the cacheable tail, `{{PERSONA}}`/`{{BELIEFS}}` in place), `buildThreadDraftSchema(slugs)` (`{pillar enum, tweets string[]}`, **no min/maxItems** — deviation from the plan's "minItems 3/maxItems 9" per the N.8/RU.3 strict-structured-output rejection-risk convention; count steered by the prompt), `parseThreadDraft(raw,slugs)→{pillar,tweets,overLong}|null` (≥2 tweets, bad pillar → null, 280-over positions computed not rejected), `buildThreadDraftInput` (pillar+tweetCount+idea folded into the single steer — **no `{{PILLAR}}` token**, deviation from the post prompt's separate pillar tag; persona/beliefs in place, meContext+guidance at the tail). Registry key `thread` added (5th position) → **8 keys**; `registry.test.ts` + `prompts.test.ts` updated (the latter's three "unknown key" fixtures swapped `thread`→`not-a-prompt` since `thread` is now valid). New `POST /x/posts/draft-thread` on the drafter router (inherits the AI.6 `llmConfigured()` mount gate — no index.ts change) → `loadPromptSafe('thread')` → `askLLM` (defaults 0.7/2000/low, body `provider`/`model`/`reasoningEffort` accepted via `parseCommon`, `llmErrorPayload` catch) → exported `insertThreadDraft(pillar,tweets,ideaId?)` (calendar thread insert shape: head `draft` + tails `segment`, shared threadId, `source='drafter'`, unscheduled; `consumeIdeaSafe` after). One auto-regenerate when a segment >280 chars (nudge names the offenders; still-long → 502 `thread_invalid`) — the reply-specificity-gate discipline, cost summed. `tweetCount` non-int → 400 `invalid_tweet_count`, else clamp 3–8; URL allowed in drafts. Extension: `shared/types.ts` `ThreadDraftBody`/`ThreadDraftResponse`, `api.drafts.thread`, and the Composer drafter-section button branches on `threadMode` (reuses the pillar+idea inputs, `onEdit(head.id)` loads the created thread). Tests: `src/test.test.ts` thread block (byte-sync, tail-position, `parseThreadDraft` shape/over-long/bad-pillar/malformed, `buildThreadDraftInput` fold+tail); `src/x/routes/drafter.test.ts` (2 pre-spend guards + the both-keys-unset 503 + `insertThreadDraft` shape incl. `GET /posts/scheduled/:id` siblings). Gates: canonical `bun run test` **914/0**, root+extension typecheck clean, changed-file lint clean, extension `vite build` green. **Done-when tails needing live use:** the first browser "Draft thread with AI" → thread-edit load, and a real Grok/OpenRouter thread draft (the $0 default never spends). **STATE sha reconciliation (D49):** AI.6's recorded `b82f4ef` was an amend-orphan — real AI.6 = `e2c91ac` (= HEAD at AI.7 start, parent `a579e75` = AI.5). Cost: $0 (all LLM paths pre-network in tests). **Next:** AI.8 (rewrite assist — prompt + route + Composer button, high).
- 2026-07-19 `b82f4ef` — **AI.6** (Wave 1, AI-layer — digest/icebreaker via registry + the LLM-gating switch; deps AI.2+AI.3, xhigh, $0): 15 files. `DIGEST_INSTRUCTIONS`→exported `DIGEST_PROMPT_TEMPLATE` (`{{FACTS}}` tail) + `buildDigestInput(facts, template?)`; `ICEBREAKER_INSTRUCTIONS`→exported `ICEBREAKER_PROMPT_TEMPLATE` (`{{GROUNDING}}` tail) + `buildIcebreakerInput(grounding, template?)` — both substitute via local split/join (no registry import — the registry imports THEM; verified `digest.ts` pulls only conversion/type-playbook/grok-type + `icebreakers.ts` only types, no cycle). Registry keys 5→**7** (`digest`+`icebreaker`; both validate clean — the JSON examples in their bodies use single braces so `v.unknown===[]`; AI.4's `GET /x/prompts` lists them free). Routes migrated to `askLLM`: **digest** (`loadPromptSafe('digest')`, house defaults low/700/0.7, `prompt.cacheKey` replaces static `stratus-digest`; degrade + catch → `llm_not_configured`/`grok_<status>`/`openrouter_<status>`/`llm_parse_error`/`llm_failed`) and **icebreaker** (`loadPromptSafe('icebreaker')`, defaults low/400/0.7, `llmErrorPayload` catch; 404→422→503 ladder order preserved). **§7.22 gate flip** in `src/x/index.ts` `if (XAI_API_KEY)`→`if (llmConfigured())` (replies/drafter/voiceExtract mount on EITHER provider — verified app.test.ts passes with ONLY `OPENROUTER_API_KEY` set) + runtime `!llmConfigured()` on digest/icebreaker/pillars-draft/extract-winners; `images` untouched (Decision 6). **String policy:** only digest's degrade field renamed `grok_not_configured`→`llm_not_configured` (+ `Digest.tsx` matcher + `digest.test.ts`); icebreaker/pillars/playbook pre-flight 503s keep `grok_not_configured` (stable extension matchers, no rename in plan). Tests: registry.test 7-key + 2 defaultBody asserts; digest/icebreaker pure parity tests (byte-identical to old concatenation + template-param); digest/icebreaker/playbook no-key tests unset BOTH env keys (the gate checks both now). Gates: canonical `bun run test` **902/0** (900+2), typecheck clean, changed-file lint clean, extension `vite build` green, `smoke-c9.ts` $0 PASS. **STATE sha reconciliation (D48):** AI.5's recorded `7d0a786` was an amend-orphan — real AI.5 = `a579e75` (= HEAD at AI.6 start, parent `3c649ce` = AI.4). Cost: $0 (all LLM paths pre-network in tests). **Next:** AI.7 (thread drafter — prompt + route + Composer button, xhigh).
- 2026-07-19 `7d0a786` — **AI.5** (Wave 1, AI-layer — batch/extract/pillar-draft via registry + askLLM migration; deps AI.2+AI.3, MAX, D3, $0): 14 files (1 new). Registry keys 2→5 (`reply-batch`/`voice-extract`/`pillar-draft` + specs; AI.4's `GET /x/prompts` lists them with no route change, D46). **Slicing retired (D3):** `REPLY_BATCH_PROMPT_TEMPLATE` standalone in `replies/prompt.ts` (byte-exact from the old assembly incl. the doubled `---` seam; `{{POSTS}}`/`{{IDEA}}` tail + token-missing fallbacks for pre-token overrides; relationship note rides inside the POSTS value so its position survives), anti-drift test locks the batch default to the reply default's voice block (both carry raw `{{REPLY_PERSONA}}`). New pure `src/x/voice/extractPrompt.ts` (moved from routes/voiceExtract.ts — registry→route import would TDZ-cycle; importers updated: playbook.ts, test.test.ts); `PILLAR_DRAFT_TEMPLATE` in pillarDraft.ts (persona optional/in-place-first; jobs code-built). **askLLM live at 6 call sites** incl. drafter's `generateAndInsert` (unowned by any plan task — deviation D47; leaving it on askGrok would strand OpenRouter-only setups at the AI.6 gate flip); house defaults moved to the `defaults` tier, body `provider` accepted (replies×2, drafter×2 via parseCommon, pillars/draft, extract-winners; NOT voiceExtract's bodies — settings-driven), shared `llmErrorPayload` in `llm/ask.ts` (+ index re-export). Cache keys now sha-based (batch/pillar-draft niche-suffixed, voice-extract bare) — one-time xAI cache miss on deploy, by design. Parity: 7 pre/post fixtures byte-identical (batch default/rich/override, pillar new/tweak/default, extract). Tests: registry.test 5-key assertions, anti-drift + batch/pillar template-override + extract-tail-render in test.test.ts, 4 `invalid_provider` guards in app.test.ts. Gates: `bun run test` **900/0** (893+7), typecheck clean, changed-file lint clean (whole-repo = the 2 pre-existing Replies.tsx a11y, D39d). **STATE sha reconciliation:** AI.4's recorded `466b090` was an amend-orphan — real AI.4 = `3c649ce`. Cost: $0 (all LLM paths pre-network in tests). **Next:** AI.6 (digest/icebreaker via registry + the `llmConfigured()` gate flip, xhigh).
- 2026-07-18 `466b090` — **AI.4** (Wave 1, AI-layer — prompt editor routes; dep AI.3, high, $0): 3 files (2 new). New `src/x/routes/prompts.ts` (`promptsRouter`) mounted always in `mountX` next to `pillars` (the editor must work with no LLM key — never gates on `XAI_API_KEY`/`llmConfigured`). Five endpoints over `prompt_overrides` reusing the AI.3 registry (`isPromptKey`/`PROMPT_SPECS`/`validatePromptBody`): `GET /x/prompts` (list, `customized`=row-presence, `updatedAt`), `GET /x/prompts/:key` (body+`defaultBody`+customized+required), `PATCH /x/prompts/:key {body}` (≤32KB → 413; `missing_placeholder`+`missing` → 400; upsert → `{customized:true, unknownPlaceholders}`), `POST /x/prompts/:key/reset` (delete row), `POST /x/prompts/restore-defaults` (delete ALL → `{restored:n}`); unknown key → 404 `unknown_prompt`. NO schema change (`prompt_overrides` shipped in AI.3's `0017`); the static `restore-defaults` + `:key/reset` param paths don't collide (§7.20, different segment counts). Byte cap measured via `TextEncoder` (32*1024). `routes/prompts.test.ts` (12 tests, afterEach+afterAll wipe overrides — shared `:memory:` DB) walks the full lifecycle incl. the done-when chain (override → `loadPrompt` reflects → restore reverts); the bearer 401 is not duplicated (shared `/x` middleware, niche.test discipline). Gates: canonical `bun run test` **893/0** (881 + 12), typecheck clean, changed-file lint clean (whole-repo lint still the 2 pre-existing Replies.tsx a11y, D39d). **STATE sha reconciliation (D45b):** AI.3's recorded `9f33fb2` was an amend-orphan — real AI.3 = `a087a66` (= HEAD at AI.4 start, parent `3d3598c` = AI.2). Cost: $0 (routes read/write the DB only; no LLM/X call). **Next:** AI.5 (registry defaults for reply-batch/voice-extract/pillar-draft + askLLM migration, MAX, D3).
- 2026-07-18 `9f33fb2` — **AI.3** (Wave 1, AI-layer — prompt registry + overrides table, wired into reply + post drafting; dep ME.3, MAX, $0): 8 files (2 new). New `src/x/prompts/registry.ts` (`PROMPT_KEYS` `reply`/`post`, `PROMPT_SPECS` with `required`+`optional` placeholder sets, `loadPrompt`/`loadPromptSafe` (§7.8), sha-8 `promptCacheKey`, `validatePromptBody`, `renderPrompt`) + `prompt_overrides` table (`schema.ts` L748, migration `0017_misty_zeigeist` — single CREATE TABLE, override-rows-only ⇒ NO seed, Decision 1). Builders: `buildGrokInput` opts bag gains `template?` (per-request `systemPromptOverride` still wins), `BuildPostDraftOptions.template?` — additive, existing callers/tests untouched. Routes: reply single loads `loadPromptSafe('reply')` AFTER the band gate, drafter (`generateAndInsert` — covers /posts/draft + /posts/reup) loads `loadPromptSafe('post')`; both compose `promptCacheKey = ${prompt.cacheKey}:${slug}:${updatedAtMs}` replacing the static consts (batch key untouched until AI.5); `GET /replies/default-prompt` returns the effective override-aware body. **Deviations (D45):** template rides the opts bag not the plan's stale positional (D38 precedent); post has FIVE required placeholders not the plan's "four" (`{{PILLARS}}/{{MY_WINNERS}}/{{REMIX}}/{{PILLAR}}/{{IDEA}}`); `optional` set + `loadPromptSafe` + the default-prompt wire added beyond the plan's literal; migration is 0017 not "0013". No-override behavior byte-identical (explicit-default-equals-omission tests); byte-sync + equivalence suites untouched. `registry.test.ts` 13 tests. Gates: canonical `bun run test` **881/0**, typecheck clean, changed-file lint clean (whole-repo lint still the 2 pre-existing Replies.tsx a11y, D39d). **STATE sha reconciliation (D45a):** AI.2's recorded `87f3bd7` was an amend-orphan — real AI.2 = `3d3598c` (= HEAD at AI.3 start). Cost: $0 (registry + routes read the DB only; no LLM/X call added).
- 2026-07-18 `87f3bd7` — **AI.2** (Wave 1, AI-layer — `askLLM` dispatcher + AI settings store + `/llm` routes; dep AI.1+UI.1, $0): 8 files (4 new src + 3 new tests + app.ts). **D1/D43e binding honored — NO schema/migration:** the plan's edit-list says "add `appSettings` + migration `0013`", but UI.1 already shipped both and AI.1 added the `openrouter` platform-union comment; AI.2 REUSES `app_settings` under key `'ai'`, skips db:generate entirely. `src/llm/settings.ts` — `getAiSettings`/`saveAiSettings`/`parseAiPatch` over `app_settings` key `'ai'` (NOT a registry key, so the UI.1 store never sees it — verified `resetSettings`/`getAllValues` only touch registry keys; no collision), **read-through no cache** (loadDoctrine discipline), field-by-field `sanitize` (garbage row → defaults), NO API keys (§7.16/Decision 5 — the table is explorer/MCP-visible). `src/llm/ask.ts` — `askLLM(opts, {defaults})` (§7.1 extended: the ONE dispatch point; grok/openrouter keep their own single entry point), resolution `opts.provider ?? settings.provider`, merge precedence **opts > DB settings > call-site defaults** encoded once (temperature/maxOutputTokens/reasoningEffort; model per-provider — Decision 10 keeps grok's `grok-4.3` separate from `settings.openrouterModel`), `LlmNotConfiguredError` on a missing key (**never silent-fallback**), `fetchImpl` forwarded only to openrouter / `promptCacheKey` only to grok, + `llmConfigured`/`llmProviderReady`/pure `resolveProvider`, `AskLlmResult.provider` added. `src/llm/routes.ts` — `GET/PATCH /llm/settings` (+ providers flags, 400 per field, 409 `provider_not_configured`, returns full settings), `GET /llm/models` (OpenRouter free list, 1h in-memory cache, 503 keyless). `src/llm/index.ts` — `mountLlm` (**always mounts**, unlike mountGrok) + re-exports. `src/app.ts` — `bearerAuth()` on `/llm/*` + `mountLlm(app)` after `mountGrok`. **Deviation (D44):** added `src/llm/ask.test.ts` (not in the plan's edit-list) — Done-when #2 needs a dispatch assertion; the grok branch is proved "by shape" (resolveProvider + the readiness gate; askGrok has no fetch seam), openrouter end-to-end via injected fetch. **INERT** — no call site imports askLLM until AI.3/5/6; only `/llm/*` goes live. Verified through the full app: `/llm/settings`→200 (defaults + `providers:{grok:true,openrouter:false}` on this XAI-keyed box), `/llm/models`→503, bearer 401 without token. §3.1 app.ts row, §3.2 heading + 4 `src/llm/` rows + openrouter-index note, §9 test row, header. Gates: canonical `bun run test` **868/0**, typecheck + changed-file lint clean. **STATE sha reconciliation (D44):** AI.1's recorded `fc5b3c4` was an amend-orphan (`merge-base --is-ancestor` → false, `cat-file -t` finds it, same subject as HEAD) — real AI.1 = `85e11e2` (= HEAD at AI.2 start). Cost: $0 (settings/models are DB/free-endpoint; live LLM spend only via later AI-layer routes behind explicit clicks).
- 2026-07-18 `fc5b3c4` — **AI.1** (Wave 1, AI-layer — OpenRouter client; dep none, $0): 4 files. New `src/openrouter/client.ts` (`askOpenRouter`, `OpenRouterApiError`, `DEFAULT_MODEL`) mirroring `askGrok`'s structure but for the OpenAI chat-completions shape: `POST https://openrouter.ai/api/v1/chat/completions`, `messages`/`max_tokens`/`temperature`, structured outputs via `response_format.json_schema` (NOT `text.format`), `usage:{include:true}` + `provider:{require_parameters:true}` ALWAYS, `reasoning:{effort}` only for low/medium/high ('none' omitted), optional `HTTP-Referer`/`X-Title`. **Cost comes straight off `usage.cost` (USD) — no price table** (2xx w/ tokens but no cost → $0 + `console.warn`, §9.1), like `images.ts` prefers `cost_in_usd_ticks`. Retry on 429/5xx honoring `retry-after`, fire-and-forget `logCost` → `cost_events` platform `'openrouter'`, endpoint `/v1/chat/completions`; soft `OPENROUTER_DAILY_BUDGET_USD` watchdog (default $1.00, sync `.get()` sum after the row lands, log-only, 0 disables). Optional `fetchImpl` test seam (defaults to global fetch — a hermetic mock beats monkeypatching `globalThis.fetch` in the shared-process runner); `promptCacheKey` omitted (no OpenAI-compat field). `src/openrouter/index.ts` re-exports only (no mount — `/llm/*` is AI.2). `src/openrouter/client.test.ts` (10 tests, injected fetch, cleans up its `cost_events` rows). `src/db/shared-schema.ts`: added the `cost_events.platform` union comment (`'x'|'grok'|'xai'|'openrouter'`) — comment-only, **no migration**. `.env.example`: `OPENROUTER_API_KEY` + `OPENROUTER_DAILY_BUDGET_USD` (env-only key, never in `app_settings`). **INERT** — no consumer until AI.2 (`askLLM` dispatcher, `/llm` routes, settings; AI.2 reuses UI.1's `app_settings` per D1). §3.2 renamed "LLM providers" + 2 openrouter rows, §3.1 shared-schema union, §2 .env notables, §9 test row, header. Gates: canonical `bun run test` **827/0**, typecheck + changed-file lint green. **STATE sha reconciliation (D43):** ME.7's recorded `f010bf3` was an amend-orphan (`merge-base --is-ancestor` → false, `cat-file -t` finds it, same subject as HEAD) — real ME.7 = `1cd60d8` (= HEAD at AI.1 start). Cost: $0 (tests mock fetch; live spend only via later AI-layer routes behind explicit clicks).
- 2026-07-18 `f010bf3` — **ME.7** (Wave 1, Me/profile — docs-sync + final smoke; dep all ME, $0, docs-only): closes the ME lane. Re-ran `scripts/smoke-me.ts` against the real `./stratus.db` → `SMOKE OK` (4 entries, 40d event out / fresh emotion in, mrr 16% / followers null, post 411 / reply 158 chars, surgical cleanup). `plans/2026-07-16-me-profile.md` Status → shipped; new PLAN.md **Me layer M1** blockquote (Goal section, after the Niche N0 pointer); new CLAUDE.md phase entry **Me layer M1** (the whole ME.1–ME.6 arc — two tables/migration `0016`, pure `me/profile.ts`, `routes/me.ts` + `loadMeContextSafe`, variable-tail injection at all three draft pipelines, the Me tab, `meEffectiveness` + digest `goals`, `x_me`/`x_add_me_entry` → **19 tools**); `docs/playbook-tab.md` gained the **"Personal context"** section (ME.5 shipped the code + UI but not the doc — docs-sync catch-up) after "Relationship lift" + "personal context" added to the "Lift lines silent" states list; `docs/me-tab.md` verified complete (written at ME.4). No source/schema/route/test change — the per-task codemap updates (§3.3/§3.4/§4/§5/§6/§9 all already carry ME rows; §6 already 19 tools, migration already `0016`) made this a verification pass. **STATE sha reconciliation (D42):** ME.6's recorded `8686d79` was an amend-orphan (`merge-base --is-ancestor` → false, `cat-file -t` still finds it, same subject as HEAD) — real ME.6 = `d4ef3dc` (= HEAD at ME.7 start); corrected in ledger + last-commit here. Gate: docs-only, no code touched — canonical `bun run test` unchanged 815/0. §11 line + header re-stamp only. Cost: $0.
- 2026-07-18 `8686d79` — **ME.6** (Wave 1, Me/profile — MCP `x_me` + `x_add_me_entry`; dep ME.2, $0): 3 files. `src/x/mcp.ts`: curated **`x_me`** (empty inputSchema, in-process `route('/x/me')` — returns entries with `inWindow` + goals with `progress`, mirrors `x_niche`) after the last curated tool; write **`x_add_me_entry`** (zod `{kind: z.enum(ME_KINDS), text≥1, happenedAt?, pinned?}` → `POST /x/me/entries`) after `x_add_person_note` — the zod enum rejects an invalid `kind` at the schema layer (handler never runs, `x_draft_post` discipline); **no `x_add_me_goal`** by design (goals are deliberate tab decisions, a bad target steers every draft). New import `ME_KINDS` from `./me/profile.ts`. **17→19 tools** (3 schema + 12 curated + 4 write). `src/mcp.test.ts`: count assert 17→19 + `has('x_me')`/`has('x_add_me_entry')` + a round-trip (journal an emotion → read back via `x_me`, **then DELETE it via `app.request` so the shared `:memory:` DB stays clean** — `me.test.ts` asserts an empty profile and bun runs all test files in one process) + an invalid-kind rejection (`env.error` set OR isError). `scripts/smoke-mcp.ts`: expected-list gains `x_me`/`x_add_me_entry` (**and the previously-missing `x_niche`** — N.2 never added it) + a §7 Me round-trip (add→read→invalid-kind). Gates: typecheck green; canonical **`bun run test` = 815/0** without token, **817/0 with `API_TOKEN`** (the authed MCP block runs — the cleanup keeps `me.test.ts` green either way); smoke-mcp green (19 tools); lint clean on the 3 changed files (2 pre-existing `Replies.tsx` a11y errors remain, D39d, not in scope). §3.3 mcp row 17→19, §5 `/mcp` count 17→19, header + this line. Cost: $0 — neither tool can reach a billed path. (Sha `8686d79` stamped post-commit; amend re-orphans it, next session's Step-0 reconciles.)
- 2026-07-18 `d611dce` — **ME.5** (Wave 1, Me/profile — measurement: playbook personal-context lift + digest goals fact; dep ME.3, $0): 8 files (6 edited + 2 test files touched, no new files). `src/x/playbook.ts`: `buildMeEffectiveness(rows, minN)` + `MeEffectiveness` — clone of `buildRelationshipLift` reading `hasMe`, plus a `totalMeasured` field so the partition invariant (`withMe.n + withoutMe.n === totalMeasured`) is assertable; uses the shared `cellOf`/`ratio`, lift only when BOTH sides gate. `src/x/routes/playbook.ts`: `ReplyRow.hasMe` (`typeof ctx?.me === 'string' && ctx.me.trim() !== ''`, mirrors `hasRelationship`) + `meEffectiveness` in the `GET /playbook` payload — no new query, same `replyRows` as relationship lift. `src/x/digest.ts`: `DigestGoal` interface + `DigestFactInputs.goals`/`DigestFacts.goals` (`DigestGoal[] | null`) + passthrough `goals: i.goals` in `buildDigestFacts`. `src/x/routes/digest.ts`: `loadGoals(now)` (active `me_goals` → latest `account_snapshots` followers → `resolveGoals` → `{label,unit,target,current,pct}`, null when no active goals) wired into `loadFacts`; imports `meGoals` + `type MeGoal`/`resolveGoals`. `extension/src/shared/types.ts`: `Playbook.meEffectiveness` + `DigestFacts.goals?` (optional/nullable — cached-digest contract like S4). `extension/src/sidepanel/Playbook.tsx`: a "Personal context" section (with-me-brief vs cold medians + gated lift line, silent-until-gated) after Relationship lift. Tests: `playbook.test.ts` (`buildMeEffectiveness` bucketing/gate/partition on fixtures); `routes/playbook.test.ts` (`seedReply` gains `me?`; pb_r1 carries a me-brief → `meEffectiveness.withMe.n===1`, gated null, minN=1 → 4x lift, partition invariant); `digest.test.ts` (`BASE_INPUTS.goals: null` + goals passthrough/null); `routes/digest.test.ts` (seeds an active mrr goal → `facts.goals` carries 16% progress, cleaned up in afterAll). Gates: root typecheck green, extension typecheck + `vite build` green, changed-file lint clean, canonical **`bun run test` = 815/0**. (Bare `bun test` shows 2 `inspect.test.ts` fails — the documented D25d/RU.3 file-vs-`:memory:` trap, NOT a regression; the canonical script sets `SQLITE_PATH=:memory:`.) §3.3 playbook/digest rows, §3.4 playbook/digest route rows, §5 Playbook.tsx row, header + this line. Cost: $0 (read-time SQL over already-billed data; digest narration cost unchanged, facts-only in tests).
- 2026-07-18 `92499ab` — **ME.4** (Wave 1, Me/profile — the extension **Me tab**; dep ME.2, extension-only, $0): 6 files (5 edited + 1 new component + 1 new doc). `shared/types.ts`: `MeKind`/`GoalKind`/`GoalStatus` unions, `MeEntry` (Date columns as ISO strings + server `inWindow`), `MeGoal` (+ server `progress`), `MeGoalProgress`, `MeResponse`, `MeContextResponse`, and the create/patch body types. `sidepanel/api.ts`: new `me` namespace (`get(opts?)`/`context(mode)`/`addEntry`/`patchEntry`/`deleteEntry`/`addGoal`/`patchGoal`/`deleteGoal`) + the type re-exports. `sidepanel/Me.tsx` (NEW, ~630 lines): `MePanel` loads `api.me.get` + both `context` modes in one `Promise.all`, reloads after every mutation. Three sections + preview — **Goals** (`GoalCard`: progress bar `progress.pct`, current/target/unit, `daysLeft` amber when overdue; followers = "auto · from daily snapshot" no editor, mrr/custom = inline number PATCH-on-blur; achieved/dropped/reactivate/delete; `AddGoal` form label/kind/target/unit/deadline), **Quick log** (`QuickLog`: text + fact/event/emotion/note chips + optional date + pin), **Entries** (`EntriesSection`/`EntryRow`: grouped by kind newest-first, pin-star / retire / inline-edit / delete; server `inWindow` → dashed "not injected anymore" dimming, retired rows dimmed), **`AiPreview`** (collapsible "What the AI sees", two `<pre>` of the exact post/reply blocks, refetched on mutation). `App.tsx`: `Tab` union + `TAB_GROUPS` Operate group gain `me` (label "Me") after People; render branch `<MePanel>`. `styles.css`: `.me-*` block (progress-track/fill, chips, star, preview `<pre>`) — token-only. `docs/me-tab.md` (NEW). **Freshness windows are NEVER re-derived client-side** — the tab reads the server's `inWindow` flag (§7.27 spirit, plan Task-4). Create/patch responses lack the computed `inWindow`/`progress` (route returns the raw row) — harmless, the tab reloads and never reads them off a mutation. NO new tests (no shared pure module — Task-4 spec: behavioral check is manual over the loaded-unpacked extension). Gates: root + extension typecheck green, extension `vite build` green, root `bun test` **811/0** (unchanged — extension-only), changed-file lint clean (2 PRE-EXISTING `Replies.tsx` a11y errors from C6 commit `61bf692` remain — untouched file, not from ME.4). §5 App.tsx/api.ts/Me.tsx rows + header stamp. Cost: $0 (reads/writes the always-mounted $0 /x/me routes only).
- 2026-07-18 `82f0291` — **ME.3** (Wave 1, Me/profile — inject the personal-context block at all three draft-prompt tails; prompt-surgery chain link after N.4): 6 files. `posts/prompt.ts`: `BuildPostDraftOptions.meContext?` appended in `buildPostDraftInput` at the variable tail, BEFORE `guidance` (empty/whitespace → no change). `routes/drafter.ts::generateAndInsert`: `const meContext = await loadMeContextSafe('post')` threaded via conditional spread (covers /posts/draft + /posts/reup — one edit). `replies/prompt.ts`: `PostContext.me?` (server-stamped JSDoc); `buildGrokInput` appends `ctx.me` after the relationship block (order relationship→me→pillars→guidance — the §8.6 pillars opt-in sits between me and guidance, harmless); `buildBatchGrokInput`'s 6th opts bag widened to `{replyPersona?; meBrief?}`, `meBrief` appended ONCE after guidance (D38 — plan wanted a 6th positional `meBrief`, but N0.4 already made arg 6 the opts bag; adding to the bag is cleaner + matches current style). `routes/replies.ts`: single path stamps `ctx.me = await loadMeContextSafe('reply')` right after the relationship stamp, BEFORE the `contextSnapshot: ctx` insert (so a generated draft records whether it saw the block — ME.5 me-lift); batch path loads `meBrief` once and passes it in the opts bag (conditional spread for `exactOptionalPropertyTypes`). `parseContext`/`parseBatchTweets` UNTOUCHED — strict allowlists already drop a client `me` (verified by a new test). **Templates + both `.md` files byte-untouched — byte-sync + N0.3/N0.4 equivalence tests all green.** Tests (`src/test.test.ts`, +6): post-drafter additive-tail byte-identity (`warm === cold + '\n\n' + block`) + empty→no-change + meContext-before-guidance; single-reply appended-after-`</idea>` + empty→no-change + relationship→me→guidance order; batch meBrief-exactly-once-at-tail; parseContext strips a client `me`. §2 prompt row unaffected (no template edit). §3.3 me/profile + posts/prompt + replies/prompt rows, §3.4 me route row. Full suite **811/0**, typecheck + changed-file lint green. Cost: +~150–500 tail tokens per EXISTING Grok call (~+$0.0005–0.001), no new calls, prefix-cache unaffected (tail injection).
- 2026-07-18 `a3a94bb` — **ME.2** (Wave 1, Me/profile — routes + loader + smoke; the ME.1 pure module gets its DB face): new `src/x/routes/me.ts` (pillars.ts validation style, always mounted in `mountX` after `nicheRouter`, $0 pure SQL) — `GET /me?kind=&active=` returns entries each with a server-computed `inWindow` (= the exported `isEntryInWindow`, the UI must not fork the window logic) + goals each with `progress` (`resolveGoals`, followers ← latest `account_snapshots.followersCount`); `POST/PATCH/DELETE /me/entries[/:id]` + `/me/goals[/:id]` with per-field 400 codes (`invalid_kind`/`invalid_text`/`invalid_happened_at`/`invalid_target`/`invalid_status`/…); `happenedAt`/`deadline` parsed from ISO strings into Drizzle column ops (never a raw Date in a `sql` template, §7.13); `GET /me/context?mode=post|reply` → `{mode, block|null}` (the exact injected block, §7.18 transparency). Exported **`loadMeContextSafe(mode)`** (the three ME.3 pipelines' entry point) — shares one internal `renderFor(mode)` with the context route: `active=1` entries + `status='active'` goals + latest followers → `selectEntriesForPrompt`/`resolveGoals` → render; returns null on `''` or ANY error (console.error, never throws — §7.8 `loadReplyGuidanceSafe` discipline). Tests: `src/x/routes/me.test.ts` (3 tests / 56 asserts over the in-memory DB — empty-profile null context + loader, entry CRUD + every 400 + 404 + the `inWindow`/pin-overrides-window flag + kind filter, goal CRUD + followers auto-progress over a year-3000 seeded snapshot [cleaned up by exact-date match] + mrr manual 16% + status flip drops an achieved goal from the block; `afterAll` wipes the me_* tables — exclusive to this feature). `scripts/smoke-me.ts` (rerunnable, $0 — real DB, one entry/kind + 2 goals, window flag, both context modes, surgical by-id cleanup on success + `fail()`). Consumers still unwired — ME.3 injects the block at the three prompt tails. §3.3 me/profile row (loader now wired), §3.4 me route row, §9 smoke row + list. Full suite 805/0, typecheck + changed-file lint green.
- 2026-07-18 `f0c91f4` — **ME.1** (Wave 1, Me/profile — schema + pure renderer, first ME task): new `src/x/me/profile.ts` (relationship.ts pattern — pure, no DB, callers pass `now`) + `profile.test.ts` (20 pure tests): `ME_KINDS`/`GOAL_KINDS`, freshness windows (`EMOTION_WINDOW_DAYS=7`/`EVENT_WINDOW_DAYS=30`, inclusive; facts/notes evergreen; `pinned` overrides window+cap), per-kind selection caps + render hard-caps (`MAX_POST_LINES=14`/`MAX_POST_CHARS=1200`; brief `≤3`/`300`), `ME_INSTRUCTION`/`ME_BRIEF_INSTRUCTION` (instruction INSIDE the block, §7.18), `selectEntriesForPrompt`, `goalProgress`/`resolveGoals` (followers ← account_snapshots value, mrr/custom ← manual currentValue, null when no data), `renderMeContext`/`renderMeBrief` (`''` when empty → no injection → prompts byte-identical — the rollback story). Schema: `me_entries` + `me_goals` tables (§4; ideas/streaks shape, `$defaultFn(crypto.randomUUID)`, `timestamp_ms`), migration `0016_high_miek` (DDL-only, **no seed** — plan said 0013 but 0013–0015 taken; `bun run db:generate` inspected, migration 0000 pillar seed untouched). Module is INERT — consumers wired at ME.2 (`loadMeContextSafe`) / ME.3 (three prompt tails). §3.3 me row, §4 count 29→31 + two rows + migration line, §3.3 X-table count 27→29. Full suite 802/0, typecheck + changed-file lint green.
- 2026-07-18 `78d1d93` — **N.9** (Wave 1, FINAL niche task — docs-sync + smoke): new `scripts/smoke-niche.ts` (rerunnable, $0 default) — mounts niche/pillars/drafter/brief/voice routers in-process over the real DB and walks the full second-niche story: create+activate `smoke-nutrition` with a doctored doctrine → 0 active pillars, drafter `409 no_pillars_for_niche` (no Grok spend), doctrine-driven brief quota/ratio (5–8/day, 55%) + voice band (4–12×), and a `buildGrokInput`/`buildPostDraftInput` assembly with nutrition sentinels present + §1/§5 builder markers (`Alteramens`/`Naval-derived`) absent (NOT §6's `Pitești` — D28b); reactivate builder → restore assertions; delete. **Sync `cleanup()` restores builder-active + drops the smoke row even on `fail()`** — a mid-run abort must never leave a non-builder niche active (would skew every other route + suite). `--live` adds one `POST /x/niche/draft` wizard call (~$0.01, asserts a 3-pillar proposal parses); the live `contextSnapshot.niche.slug` proof rides a real reply-generate (D29b), deliberately out of the $0 default. Verified non-destructive against the real `./stratus.db` (builder sole-active before + after). Docs: CLAUDE.md §"Niche N0" phase entry (in-place-substitution deviation + why, `no_pillars_for_niche` guard, seed-equivalence guarantee, 17 MCP tools, open "done-when" tails); PLAN.md scope note (goal-serving config is niche-scoped; `my_niche.md` is now seed-only); `src/my_niche.md` reference-only header pointing to the `niches` table; `docs/settings-tab.md` "Your niche" section (editor + doctrine + list + wizard) + the pillars-are-niche-owned note; `docs/voice-tab.md` pillars-owned-by-active-niche note. Codemap: §9 smoke row, header re-stamp, this line. Full suite 782/0, typecheck + changed-file lint green. **Sha reconciliation:** N.8's recorded `527ad21` was an amend-orphan (`git merge-base --is-ancestor 527ad21 HEAD` → false, `cat-file -t` still finds it, same subject as HEAD); real N.8 = `5c1031d` (= HEAD at N.9 start), corrected in header + STATE (D35). **Niche N0 (N.1–N.9) is COMPLETE.**
- 2026-07-18 `527ad21` — **N.8** (Wave 1, AI niche wizard — prose → proposed niche): new pure `src/x/niche/wizard.ts` (`NICHE_WIZARD_SCHEMA`, `buildNicheWizardInput`, `parseNicheProposal`) + `POST /x/niche/draft` in `niche.ts` (runtime XAI 503, description ≤5000 validated BEFORE the key check so the $0 guards test without a key, paid `askGrok` last §7.4; effort low, `maxOutputTokens` 2500, cache key `stratus-niche-wizard`; proposal NEVER persisted — the `POST /pillars/draft` contract). Prompt uses DEFAULT_NICHE+DEFAULT_PILLARS as the in-prompt EXAMPLE, user description at the TAIL. `parseNicheProposal`: coerce slugs, exactly-3 distinct pillars + required non-empty text + length ceilings (mirrors CRUD), a malformed channel dropped-not-fatal, junk/bad-slug/wrong-count → null. Extension: `api.niche.draft(description)` + `NicheProposal`/`NichePillarProposal`/`NicheChannelProposal`/`NicheDraftResult` types; `Niche.tsx` `NicheWizard` (paste → Generate → editable preview → Create = niche.create → **activate FIRST** so pillar/channel POSTs stamp the new niche → pillars.create × N → channels.create × N → "switch back to {prev}"). **Deviation D34b:** the wizard flow landed in `Niche.tsx` (where N.7 moved the card), NOT `Settings.tsx` as the plan's edit-list said; `Settings.tsx` untouched. Fixed a duplicate `invalid_description` ERR key the linter caught. Tests: 4 new guard/parse cases in `niche.test.ts` (empty/oversize → 400 pre-network, valid-no-key → 503 with force-unset+restore so it NEVER spends, `parseNicheProposal` valid/bad-slug/wrong-count/junk, description-at-tail). Full suite 782/0, root+ext typecheck, ext build, changed-file lint green. **Live wizard call + browser walk deferred to N.9 `--live` smoke / Wave-5 QA** (same class as N.7). §3.3 wizard row, §3.4 niche route, §5 api.ts + Tab rows. **Sha reconciliation:** N.7's recorded `25a8c99` was an amend-orphan (`git merge-base --is-ancestor 25a8c99 HEAD` → false, `cat-file -t` still finds it, same subject as HEAD); real N.7 = `3d80988` (= HEAD at N.8 start), corrected in header + STATE (D34).
- 2026-07-18 `25a8c99` — **N.7** (Wave 1, Settings Niche card + api client — extension-only, $0, no server change): new `extension/src/sidepanel/Niche.tsx` (`NicheCard`) rendered at the top of `Settings.tsx` (fragment wrap). Active-niche editor over `GET /x/niche` (`{niche, doctrine:resolved}`): label/description/persona/beliefs/replyPersona textareas + the 5 doctrine number inputs, Pillars.tsx dirty-state Save/Reset — sends only changed text fields, but the **full 5-knob doctrine object** whenever any knob changed (D27c PATCH-replace semantics; `parseDoctrine` requires all finite-positive). Niche list over `GET /x/niches` (active-first) with Activate (`PATCH {active:true}`) / Delete (confirm; disabled on active), 409s (`last_active_niche`/`niche_active`) surfaced inline. "+ New niche" create-inactive form — **includes required persona/beliefs/replyPersona** because `POST /x/niches` rejects empty ones (D33 — plan said "slug+label only"; current code wins, the wizard N.8 fills the rest). `api.ts` gained `api.niche.{get,list,create,update,remove}`; `shared/types.ts` gained `Niche`/`NicheDoctrine`/`NicheActive`/`NicheCreateBody`/`NichePatchBody` (dates serialize to ISO strings, `doctrine` = `Partial<NicheDoctrine>|null` on the row, resolved-full on `NicheActive.doctrine`). Reuses `.pillar-*` CSS + inline flex-wrap for the doctrine row — no styles.css edit (D6: keep feature Settings UI minimal, UI.11 rebuilds). NicheCard self-loads `getSettings()` (SettingsPanel has no props; the api ignores the settings arg anyway — background owns the transport). Gates: ext typecheck+build, changed-file lint, root typecheck, `bun run test` 774/0 — all green. **Live browser check deferred** (create+activate nutrition → Composer pillar dropdown empties) — needs unpacked extension + live server, N.9 smoke / Wave-5 QA territory, same as prior UI opening-guess flags. §5 api.ts + Tab-components rows.
- 2026-07-18 `730a009` — **N.6** (Wave 1, pillars + channels owned and filtered by the active niche): `content_pillars`/`channels` reads now scope to the active niche via a small `ownedByNiche(slug)` = `or(eq(niche,slug), isNull(niche))` helper (NULL = legacy tolerance; identity until a 2nd niche exists). `pillars.ts`: `GET /pillars` (both branches) + `getActivePillars()` scoped; POST stamps `niche = loadActiveNicheSafe().slug`; `getActivePillars()` falls back to `DEFAULT_PILLARS` **only when active niche is `builder`** (else `[]`); last-active 409 guard counts within the active niche's set (DELETE only guards when the target belongs to that set — no false-fire on another niche's pillar). `drafter.ts`: `/posts/draft` + `/posts/reup` refuse `409 {error:'no_pillars_for_niche', niche}` when `getActivePillars()` is `[]` — BEFORE any Grok spend (§7.4); `/pillars/draft` deliberately tolerates empty existing (it's how the first pillar is made). `channels.ts`: `GET /channels` scoped, POST stamps niche, `:slug` room 404s cross-niche, PATCH/DELETE untouched. `calendar.ts` `POST /posts/scheduled` pillar validation inherits the filter for free (goes through `getActivePillars()`) — verified by test, no code change. New route suite `src/x/routes/niche-ownership.test.ts` (9 tests over the in-memory DB: builder-stamp/visibility, activate nutrition → empty sets + zero builder leakage, drafter `no_pillars_for_niche` with **no cost_events written**, cross-niche room 404, POST-under-nutrition stamping, per-niche last-active 409, calendar pillar validation follows the active niche, reactivate builder → sets swap back; restores builder-active + deletes its rows in afterAll, niche.test.ts discipline). Full suite 774/0, typecheck + changed-file lint green. §3.4 pillars/drafter/channels/calendar rows, §7 niche bullet, §9 smoke unchanged (N.9 owns `smoke-niche.ts`). **Sha reconciliation:** N.5's recorded `20faa44` was an amend-orphan (`git merge-base --is-ancestor 20faa44 HEAD` → false, `cat-file -t` still finds it); real N.5 = `7e9213d` (= HEAD at N.6 start), corrected in header + STATE (D31).
- 2026-07-18 `20faa44` — **N.5** (Wave 1, doctrine knobs threaded into brief/targets/consumers — the third and final niche consumer): the 5 REPLY-GUIDE numbers (reply target min/max, week reply %, target band min/max ×) now come from the active niche's `doctrine` JSON via `loadDoctrine()`. `voice.ts`: `targetBand(myFollowers, band={minX:2,maxX:10})` gains a 2nd band arg (default = old behavior, keeps every existing call site + `src/test.test.ts` green); `/voice/targets` + `loadTargetHandles` pass the doctrine multipliers. `brief.ts`: deleted the `REPLY_TARGET`/`WEEK_REPLY_TARGET_PCT` consts — one `loadDoctrine()` per request drives reply quota (`replyQuota.target` + quests' `repliesTarget`), `week.targetReplyPct`, and the target-roster band. `followups.ts`/`playbook.ts` (`loadMyTargetBand`)/`digest.ts` (`loadNeglectedTargets`) each load doctrine at their `targetBand` call site. Doctrine is method-configuration, not a stat — no gates touched (§7.19). Tests: new `src/x/routes/doctrine.test.ts` — null doctrine → today's defaults (10–20/day, 70%, 2–10x band incl. a 1000-follower author), PATCHed `{replyTargetMin:3,...,targetBandMinX:3,MaxX:5}` → quota `{min:3,max:9}`/55%/3–5x band (excludes the 1000-follower author); restores builder doctrine null in afterAll (niche.test.ts shared-DB discipline). Full suite 765/0, typecheck+changed-file lint green. §3.3 store row, §3.4 brief/voice rows, §7 niche bullet. **Sha reconciliation:** N.4's recorded `0668451` was an amend-orphan (`git merge-base --is-ancestor` → false, `cat-file -t` still finds it); real N.4 = `943a8d0` (= HEAD at N.5 start), corrected in header + STATE (D30).
- 2026-07-16 `2a7693e` — initial map (post-C9, post-S4, post grok-imagine migration).
- 2026-07-17 — §2: added `plans/` + masterplan skill rows (planning only; no src/extension changes — stamp sha unchanged).
- 2026-07-18 `95b9fff` — §5: `compose.ts` gained `path`/`panel`/`pattern` layer kinds + `mulberry32`/`patternCoords` pure exports (ST.1 / S5.1).
- 2026-07-18 `40c718e` (styles.css in `61a04e7`, D12) — **UI.8**: `styles.css` adopts the `--strat-*` design tokens (legacy aliases retained), bundles Inter for the panel, 44 color literals → tokens/`color-mix`; §2 DS-folder + biome-ignore rows, §5 `styles.css`/`public/fonts` rows, §7 design-token pattern.
- 2026-07-18 `7e07dd5` (code in `61a04e7`, D12/D13) — **UI.1**: `app_settings` (table #28, migration `0013`) + platform-agnostic `src/settings/store.ts` + typed `src/x/settings/registry.ts` (doctrine group) + `src/x/routes/settings.ts` (GET/values/PATCH/reset, mounted inert next to `data`); biome ignores `src/db/migrations`. §3.1 store, §3.3 registry, §3.4 settings routes, §4 count 27→28 + migrations →0013, §7 settings-platform pattern.
- 2026-07-18 `f9ff346` — **UI.9**: light theme (`:root[data-theme='light']` over base `--strat-*`) + Appearance (theme/density/text-size) stamped on `<html>` by `main.tsx` from local `theme`/`density`/`uiScale`; `body`/`.panel`/`.row` consume new `--ui-*` vars; `storage.ts` getters/patch + `resolveTheme`/normalizers + `storage.test.ts`; Settings.tsx Appearance selects. §5 styles.css/Settings/Storage-keys rows, §7 theme-aware note, §9 storage test. (Sha corrected from the orphaned `8c89950` amend — see STATE D14a / sha-stamp-trap gotcha.)
- 2026-07-18 `c37d504` — **UI.10**: `src/sidepanel/ui/` primitives (Section/EmptyState/SubTabs/SettingRow/Slider/GearPopover) + `settingsClient.ts` + `api.settings.{get,patch,reset}` + `SettingEntry`/`SettingsGroup`/`SettingsResponse` in `shared/types.ts`; grouped rail (`TAB_GROUPS` eyebrow dividers) + brandmark + `EmptyState` in App.tsx; `.brand*`/`.tab-group*` + tail `.ui-*` CSS block. §5 App.tsx/ui/settingsClient/api.ts/styles.css rows, §7 UI-primitives pattern note.
- 2026-07-18 `dffd60a` — **ST.2**: behavior-neutral Studio shell refactor — new `src/sidepanel/studio/{registry,fields,KitEditor}.tsx` (template registry + `buildSpec` dispatch, dumb field/kit components); `Studio.tsx` 771→554 lines. §5 Tab-components + new `sidepanel/studio/` row. (Stamped `860725f` at ship — that sha was an amend-orphan; real commit is `dffd60a`, corrected at ST.3.)
- 2026-07-18 `292603d`→**`b82058b`** — **ST.3**: deterministic cloud mascot — new `src/studio/mascot.ts` (pure `mascotLayers`, 4 poses, all-`path` viewbox, kit-derived colours, seeded confetti) + `mascot.test.ts`; `brandKit.ts` gains `mascot:boolean` (default true, parse fallback); `templates.ts` appends the mascot to quote/stat/banner guarded so `mascot:false` is byte-identical (stat pose flips on `delta>0`); `KitEditor.tsx` mascot checkbox. §5 `src/studio/` + `sidepanel/studio/` rows. Browser-verified: all 4 poses + card placements + stat pose-flip render correctly. (`292603d` was an amend-orphan; real commit `b82058b`, corrected at ST.4 — same sha-stamp trap.)
- 2026-07-18 `bc3f1e8`→**`1f6b20f`** — **ST.5**: milestone + streak celebration cards — new `src/studio/milestones.ts` (`MILESTONES` ladder + pure `latestCrossed`) + `milestones.test.ts`; `templates.ts` `milestoneCardSpec`/`streakCardSpec` (blobs confetti seed 11, celebrating mascot, null-graceful) + `templates.test.ts` blocks; `registry.ts` `TemplateId`+milestone/streak, `EMPTY_MILESTONE`/`EMPTY_STREAK`, `TemplateState.milestoneData/streakData`; `fields.tsx` `MilestoneFields`/`StreakFields`; `Studio.tsx` loads account-series/streak + override; `api.ts` `metrics.account()` + `MetricsAccountResponse`/`AccountSeriesPoint` types; `assets.ts` `ASSET_KINDS` widened to all S5 kinds + `assets.test.ts` case. §3.4 metrics client, §4 mediaAssets kinds, §5 studio rows, §9 tests. **Unverified in a browser** (live account-series/streak fill + Copy-PNG rides ST.9's Studio walk).
- 2026-07-18 `c1b0314`→**`15f2563`** — **ST.4**: background patterns + named theme presets — `templates.ts` `baseLayers` gains an optional pattern arg (`[fill, pattern]` base at 0.07-alpha ink, AI bitmap still wins); `QuoteCardData`/`BannerData` carry `patternKind?`/`patternSeed?`; `brandKit.ts` multi-preset (`BrandKits`, `studio:brandKits`, legacy migration, `STARTER_KITS` Midnight/Paper/Neon, pure `parseBrandKitsFile`+preset mutations, last-preset delete guard); `registry.ts` `TemplateState`+`patternKind/patternSeed`; `KitEditor.tsx` preset dropdown/Save-as/Rename/Delete + bundle import; `Studio.tsx` inline bg-mode segmented control + blobs reroll. §5 `src/studio/`+`sidepanel/studio/` rows. Tests: `brandKit.test.ts` multi-preset block, `templates.test.ts` patterns block. **Unverified in a browser** — rides ST.9's Studio walk.
- 2026-07-18 `b65266f` — **ST.6**: code/terminal card + bundled JetBrains Mono — new `extension/public/fonts/JetBrainsMono-{Regular,Bold}.woff2` (OFL-1.1, latin subset); `fonts.ts` loads them as `StudioMono` (per-face family, non-fatal); new `src/studio/codeTokens.ts` (pure `tokenizeLine` + `MONO_ADVANCE=0.6`) + `codeTokens.test.ts`; `templates.ts` `codeCardSpec`/`CODE_CARD`/`STUDIO_MONO_STACK` (terminal window, fixed-advance token layout, kit-derived colors, 18-line/62-col caps + trimmed footer) + `templates.test.ts` code block; `registry.ts` `TemplateId`+`code`, `DEFAULT_CODE`, `TemplateState.codeTitle/codeText`, `buildSpec` case; `fields.tsx` `CodeFields`; `Studio.tsx` wiring. `assets.ts` `ASSET_KINDS` already whitelisted `code` (ST.5) — no server edit. §5 studio rows + public/fonts, §9 tests. **Unverified in a browser** — rides ST.9's Studio walk (MONO_ADVANCE=0.6 is JetBrains Mono's exact 600/1000 em-advance, so the "verify visually" is a check, not a discovery). (Stamped `b65266f` at ship — an amend-orphan; real commit `00b9615`, corrected at ST.7.)
- 2026-07-18 `acd95ec` — **ST.7**: thread cover + numbered list card + Composer thread handoff — `templates.ts` `threadCoverSpec`/`listCardSpec`/`THREAD_COVER`/`LIST_CARD` + pure `parseListItems` (both bg-capable via `baseLayers`; thread carries the thinking mascot, list has no mascot) + `templates.test.ts` blocks (thread/list/parseListItems); `registry.ts` `TemplateId`+`thread`/`list`, TEMPLATES rows (`supportsAiBackground:true`), `TemplateState.threadHook/threadCount/listTitle/listItems`, `DEFAULT_THREAD_HOOK`/`DEFAULT_LIST_TITLE`/`DEFAULT_LIST_ITEMS`, `buildSpec` cases; `fields.tsx` `ThreadFields`/`ListFields`; `Studio.tsx` `StudioSeed.template?` seed routing + state/render wiring; `Composer.tsx` thread-mode "Make visual" emits `template:'thread'`. `App.tsx` needed NO edit (D20 — `StudioSeed` imported, `openStudio` generic). `assets.ts` `ASSET_KINDS` already whitelisted `thread`/`list` (ST.5) — no server edit. §5 studio rows, §9 tests. **Unverified in a browser** — the Composer→thread-cover handoff + list render ride ST.9's Studio walk; spec math + parseListItems are bun-tested. (Stamped `2104f46` at ship — an amend-orphan; real commit `acd95ec`, corrected at ST.8 — same sha-stamp trap.)
- 2026-07-18 `d38bbc5`→**`289ec2c`** — **ST.8**: chart card — follower-growth curve + best-times heatmap — new `src/studio/chartData.ts` (pure `growthSeries`/`heatmapCells` — full 7×24 grid, min→max normalization, S0.4 `minN` gate → `sufficient:false`/null intensity) + `chartData.test.ts`; `templates.ts` `chartCardSpec`/`CHART_CARD` (growth sparkline + delta badge + peak/low/endpoint labels, <2 pts placeholder; heatmap 168 `rule` cells accent-alpha by intensity, below-gate ink wash, weekday/hour labels; NOT bg-capable) + `templates.test.ts` chart block; `registry.ts` `TemplateId`+`chart`, TEMPLATES row (`supportsAiBackground:false`), `TemplateState` chart fields, `buildSpec` case; `fields.tsx` `ChartFields` (growth/heatmap toggle + reload); `Studio.tsx` lazy dual-load via `api.metrics.account`+`api.metrics.bestTimes` ($0). §5 studio rows, §9 tests. Browser-verified at ST.9 (`d38bbc5` was an amend-orphan; real commit `289ec2c`, corrected at ST.9 — same sha-stamp trap).
- 2026-07-18 `47b10d2`→**`d1902e5`** — **ST.9** (final S5): Studio docs-sync + smoke + browser verification (no extension/server code changes — a throwaway harness `extension/src/studio/__harness_entry.ts` rendered the real `compose`/`templates`/`registry` modules and was deleted before commit). `scripts/smoke-studio.ts` extended: every S5 asset kind (`milestone/streak/code/thread/list/chart`) round-trips and survives the `ASSET_KINDS` whitelist (not coerced to `'other'`), $0 default, cleans up. Docs: CLAUDE.md §"Surfaces S5", `SURFACES-PLAN.md` §5b (marked SHIPPED), `docs/s3-studio.md` rewritten as the single authoritative Studio doc (S3·S4·S5 — gallery of ten, mascot, patterns, presets). **Browser-verified (Browser 1, Chrome/macOS):** all ten templates × the 3 starter presets render with bundled Inter+JetBrains Mono; mascot poses correct + kit-colored (quote=happy, stat/streak=celebrating on +delta, thread=thinking); confetti renders; chart growth curve + heatmap with below-gate cells muted; preset switch re-skins every template (Midnight→Paper light incl. light code terminal→Neon); every spec + the seeded `blobs` pattern render byte-identical across two renders (determinism holds); Copy-PNG wrote a 174KB `image/png` `ClipboardItem` from a real click. **This clears the "unverified in a browser" flags carried by ST.1/2/4/5/6/7/8** (ST.3's mascot was already browser-verified). §11 log + header re-stamp. Full unpacked-extension in-panel walk (native Load-unpacked + live-token server) not scriptable here; harness renders identical module output; live-data fills used representative sample data of the exact `/x/brief`·`/x/metrics/*` shapes.
- 2026-07-18 `9c05464`→**`63df913`** — **RU.1** (Wave 1 start, prompt-surgery chain link 1): Reply Master drafts **three** angle variants (one each — extends/contrarian/debate) instead of two. `reply prompt.md` + `REPLY_PROMPT_TEMPLATE` heading `## The two variants`→`## The three variants`, Output shape → 3 objects, "build all three variants" (byte-sync test green); `VOICE_BLOCK_END` renamed to match (the batch-slice trap — a desync would leak the single-reply section into `buildBatchGrokInput`'s head); `MAX_OUTPUT_TOKENS` 350→520 in `routes/replies.ts`. `REPLY_VARIANTS_SCHEMA` unchanged (array already unbounded); parser/primary-pick/injection order untouched. Tests: `src/test.test.ts` "three variants, one per angle" + "batch head slice guard" (batch prompt keeps `Forbidden openers` but NOT `## The three variants`/`{{TWEET_CONTEXT}}`/`{{IDEA}}`). $0 CI; live 3-variant verification deferred to RU.10 smoke `--live`. Stale-comment note: `db/schema.ts:319`'s "two-variant call" is now inaccurate — RU.2/RU.3 own that file. §3.3/§3.4 replies rows. **Sha reconciliation:** ST.9's recorded `47b10d2` was an amend-orphan; real ST.9 = `d1902e5`, and HEAD before RU.1 was `906f2d3` ("eval 1" — the Wave-0 `src/db/client.ts` `:memory:`-under-test fix, already RESOLVED in STATE gotchas), reconciled per Step-0.
- 2026-07-18 `ddecb5f` — **RU.2** (migration prep for the unified reply/radar machine): four nullable columns, DDL-only ALTERs in `0014_swift_rhodey.sql`. `reply_drafts.source` (`'reply_master'\|'radar'`, null=legacy; generate insert stamps `'reply_master'`). `radar_drafts` gains `variants` JSON (3 angles — null until RU.3 supplies them), `model` (copied onto the confirmed reply_drafts row at RU.5), `reply_draft_id` (soft link on confirm, no FK). `RadarDraftInsert`/`buildRadarDraftRows`/`persistRadarDrafts` gain a `model` arg (call site passes `result.model`; variants stay null for now). Fixed the RU.1-flagged stale `schema.ts` comment ("two-variant"→"three-variant"). Nothing reads the new columns yet — every intermediate state coherent. Tests: `buildRadarDraftRows` model passthrough (`src/test.test.ts`); smoke-radar-drafts updated for the 3-arg signature ($0). §4 replyDrafts/radarDrafts rows + migrations →0014.
- 2026-07-18 `0668451` — **N.4** (Wave 1, reply prompt persona extraction + snapshot stamp — second prompt consumer of the niche): `reply prompt.md` + `REPLY_PROMPT_TEMPLATE` "Who I am" body (3 bullets + closing paragraph) → `{{REPLY_PERSONA}}` (heading + parenthetical stay; edited together, byte-sync test untouched). `prompt.ts`: exported `REPLY_PERSONA_PLACEHOLDER`; `substituteReplyPersona(template, replyPersona?)` (token-tolerant — custom overrides predating the token pass through, the {{IDEA}} tolerance); `buildGrokInput` + `buildBatchGrokInput` gain trailing `opts?: {replyPersona?}` (default `DEFAULT_NICHE.replyPersona`), substituted FIRST (single) / over the head after `sharedVoiceBlock()` slicing (batch — D3's `'## The three variants'` bound untouched, single/batch can't drift); `PostContext.niche?: {slug}` (server-stamped, parseContext whitelist never accepts it). `routes/replies.ts`: both paths `loadActiveNicheSafe()` — single AFTER the band gate (refuse-before-work; 422 path byte-identical, now also route-tested in `app.test.ts`), stamps `ctx.niche={slug}` pre-insert; both cache keys niche-suffixed `:slug:updatedAtMs` (DEFAULT_NICHE → `:builder:0`, D26d). **Bit-for-bit equivalence proven**: 5 fixed-input renders (single default/rich/override, batch default/rich) byte-identical pre/post edit (scratchpad diff) + `src/test.test.ts` N0.4 equivalence tests with independent `ORIGINAL_WHO_I_AM_BODY` fixture (pins `DEFAULT_NICHE.replyPersona` against drift; never re-read the .md); slice-guard test gained the raw-token leak sentinel. `grep solopreneur src/x/replies src/x/routes/replies.ts` → 0 hits. Route-level `contextSnapshot.niche` assertion deferred to N.9 smoke (needs a live Grok call — same as every Grok-dependent route behavior; D29). Full suite 763/0. §2 prompt row, §3.3 replies/prompt + niche rows, §3.4 replies row, §7 niche-substitution bullet.
- 2026-07-18 `b06770c` — **N.3** (Wave 1, post prompt persona/beliefs extraction — first prompt consumer of the niche): `post prompt.md` + `POST_PROMPT_TEMPLATE` §1/§5 bodies → `{{PERSONA}}`/`{{BELIEFS}}` (headings stay; edited together, byte-sync test untouched). `buildPostDraftInput` substitutes persona/beliefs FIRST, in place (deliberate §7.15 deviation — constant per niche ⇒ prefix stable; substitution order blocks token injection via idea/steer). `drafter.ts::generateAndInsert` (covers /posts/draft + /posts/reup) loads `loadActiveNicheSafe()`, passes persona/beliefs, cache key niche-suffixed `:slug:updatedAtMs` (DEFAULT_NICHE → `:builder:0`, D26d). `pillarDraft.ts` hardcoded compact `PERSONA` DELETED — `persona?` opt (default `DEFAULT_NICHE.persona`); `pillars.ts` draft route passes niche.persona+`\n\n`+description and niche-suffixes `PILLAR_DRAFT_CACHE_KEY` too. **Bit-for-bit equivalence proven**: default-rendered prompt byte-identical pre/post edit (scratchpad diff) + `src/test.test.ts` equivalence test with independent ORIGINAL_SECTION_1/5_BODY fixtures (also pin `DEFAULT_NICHE.persona/beliefs` against drift; never re-read the .md — it holds tokens now). §1/§5 markers `Alteramens`/`Naval-derived` gone from src/x/posts+routes; **`Pitești` remains in §6's palette line** — plan's done-when grep was a planning error (§6/§9/§0 builder texture is outside the extraction scope and frozen by equivalence; D28, binds N.9). Full suite 757/0. §2 prompt row, §3.3 niche/store + posts/prompt + pillarDraft rows, §3.4 drafter + pillars rows, §7 unnumbered niche-substitution bullet.
- 2026-07-18 `c76a29b` — **N.2** (Wave 1, niche CRUD + activation ratchet + MCP; sha corrected from orphaned `4312a0e` at N.3): new `src/x/routes/niche.ts` (`GET /niche` active+resolved doctrine w/ `DEFAULT_NICHE` fallback, `GET /niches` active-first, `POST /niches` created-inactive + 409 `slug_exists`, `PATCH /niches/:slug` field/doctrine edit + `active:true` **atomic sync-txn swap** + `active:false`-on-only-active 409 `last_active_niche`, `DELETE /niches/:slug` 409 `niche_active`), mounted always in `mountX` next to `pillars`. Validation mirrors pillars.ts; `readDoctrine` rejects non-positive/non-numeric supplied knobs (no silent coerce — lenient merge is stored-row-only). New curated MCP tool `x_niche` (in-process `GET /x/niche`) → **17 tools** (was 16). Tests: `src/x/routes/niche.test.ts` (GET shape, create-inactive, slug/dup/missing-field 400/409, active-first list, edit, atomic swap + at-most-one-active invariant, deactivate-last 409, doctrine round-trip + garbage 400, delete active/inactive/unknown) + `src/mcp.test.ts` (tool count 17, `x_niche` call). Consumers still unwired (N0.3/N0.4/N0.5). Full suite 754/0. §3.3 mcp row 16→17, §3.4 niche route row, §5 `/mcp` 16→17. **Sha reconciliation:** N.1's recorded `da97495` was an amend-orphan; real N.1 = `0bbca12` (= HEAD at N.2 start), corrected in header + STATE (D27). **Route-test bearer note:** the router carries no auth (shared `/x` bearer middleware, already covered by app.test/mcp.test), so the plan's "bearer 401" test case is deliberately not duplicated here — the CRUD suite mounts a bare Hono like channels.test.
- 2026-07-18 `0bbca12` — **N.1** (Wave 1, niche schema + defaults + store — the identity-container foundation): new `niches` table (slug PK, persona/beliefs/replyPersona/description/doctrine/active + timestamps) + nullable `niche` on `content_pillars`/`channels`, migration `0015_faulty_blazing_skull.sql` (DDL from generate + **hand-appended** seed `INSERT OR IGNORE INTO niches` byte-exact from `DEFAULT_NICHE` + `UPDATE … SET niche='builder'` backfills). New lowest-layer `src/x/niche/defaults.ts` (`NicheDoctrine`/`DEFAULT_DOCTRINE`/`Niche`/`DEFAULT_NICHE` with persona·beliefs·replyPersona lifted **verbatim** from post §1/§5 + reply "Who I am", description from `src/my_niche.md`; `NICHE_SLUG_RE`; lenient `resolveDoctrine`) + `src/x/niche/store.ts` (`loadActiveNiche`/`loadActiveNicheSafe`/`loadDoctrine`). `schema.ts` type-imports `NicheDoctrine` (type-only, no cycle). **Table is INERT** — no consumer wired (N0.3/N0.4/N0.5 do that); the rollback story. Tests: `src/x/niche/niche.test.ts` (resolveDoctrine matrix, DEFAULT_NICHE/slug rule, migration-boot byte-exact seed round-trip + content_pillars backfill, loadActiveNiche empty→default/active-wins/inactive-ignored, loadDoctrine resolve) — full suite 744/0. §3.3 niche rows + schema type-import note, §4 niches row + count 28→29 + migrations →0015. **Sha reconciliation:** RU.3's recorded `d8ecc9a` was an amend-orphan (`git merge-base --is-ancestor` → false, object still exists); real RU.3 = `c81046a` (= HEAD at N.1 start), corrected in header + STATE (D26).
- 2026-07-18 `d8ecc9a`→**`c81046a`** — **RU.3** (Wave 1, batch endpoint → 3 variants/tweet — code-only, no migration): `POST /replies/generate-batch` now returns **three angle variants per tweet** in one Grok call. `batchReplyHead()` rewritten (`## The three variants for each post`, Output `{replies:[{id,variants:[{text,angle}]}]}`); `BATCH_REPLY_SCHEMA` reshaped; `BatchReply`→`{tweetId,variants:ReplyVariant[]}`; `parseBatchReplies` validates ≥1 variant/entry, blank-lines each, bad angle→`extends`. Route: `maxOutputTokens=min(9000,200+n*420)` (×3 output, user-accepted); the anchor/dedupe loop maps `{tweetId, text:variants[0].text, angle:variants[0].angle, variants}` (primary stays back-compat for un-updated panels), `persistRadarDrafts(tweets,out,model)`. `buildRadarDraftRows`/`persistRadarDrafts` param widened to optional `variants?` → stores `r.variants` (null when only a primary is supplied — RU.2 "unknown" semantics; smoke script + CLI callers unbroken). No band gate / no reply_drafts write here (decision 6, deliberately lightweight). **Slice-guard test sentinel** switched `'## The three variants'`→`'not three paraphrases'` (L91, single-reply-only) since the batch head now legitimately contains that heading; `{{}}` placeholder checks kept. Tests: `parseBatchReplies` 3-variant happy/blank/missing-variants/empty-array; `buildRadarDraftRows` stores variants + primary-only→null; `src/test.test.ts` 147/0. §3.3/§3.4/§4. **Sha reconciliation:** RU.2's recorded `ddecb5f` was an amend-orphan (`git merge-base --is-ancestor` → false); real RU.2 = `182716a`, corrected in header + STATE (D25). **Pre-existing red flagged:** `inspect.test.ts` fails 2/2 under **bare `bun test`** (not `bun run test`) — `906f2d3`'s `:memory:`-under-NODE_ENV=test fix in `client.ts` wasn't mirrored into `inspect.ts:101` (still defaults `./stratus.db` when `SQLITE_PATH` unset) → primary writes `:memory:`, inspect readonly reads the file. Green under canonical `bun run test` (732/0). Data-core/S1 fix, NOT RU.3 scope.
